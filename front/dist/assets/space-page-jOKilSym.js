import { c as yr, a as v, j as c, F as zn } from "./createLucideIcon-fWv1XcFy.js";
import { r as Le, n as je, e as Q, b as j, c as ce, d as $, o as Cu, J as ku, i as se, l as Ru, x as xu, a as Tu, S as ut, g as za } from "./runtime-entry-ClkZDmNs.js";
import { v as hr, s as en, l as Mu, c as Au } from "./site-config-DrnclGFw.js";
import { N as Du, a as Vr, w as Pu, n as zu, g as Fu, B as Ai, E as Eu, b as Ou, d as $u, i as ju, M as Bu, c as Vu, P as ct, H as Uu } from "./normalize-BCEPlWl_.js";
import { w as Gs, x as Lu, o as Ku, y as Hs, z as qu, A as Fa, B as Ea, n as Di, C as Gu, D as Ws, E as co, F as cr, G as Hu, g as Oa, H as $a, I as Wu, l as ja, J as Yu, K as Xu, L as Jo, i as Zu, d as Ju, f as to, h as Pi } from "./space-storyboard-shot-card-DVUe0KAE.js";
import { C as bs } from "./circle-alert-B2uOyl1_.js";
import { C as zo } from "./circle-check-DEleXseO.js";
import { L as sn } from "./vanilla-BSPxkY5-.js";
import { A as Qu } from "./arrow-left-8fGzp-c8.js";
import { C as Ba } from "./chevron-down-e5qsfp_F.js";
import { E as Ar } from "./eye-D9RIhpvx.js";
import { F as Ys } from "./file-text-GWInsYzS.js";
import { G as el } from "./git-branch-fr-ihES2.js";
import { H as Va } from "./history-BnF8Oyah.js";
import { F as go, I as zi, V as Fi } from "./first-frame-video-DlIx6mwp.js";
import { M as Ua, b as La, r as Ka, c as qa, n as Ga, d as tl, e as nl, f as rl, m as ol, g as sl, h as il, C as al } from "./interaction-Cyugb7TD.js";
import { n as cl, S as dl, M as ul } from "./power-menu-DU0NNyd7.js";
import { P as Ur } from "./play-Cgnd9XVW.js";
import { P as Xs } from "./_commonjsHelpers-BNFp87fY.js";
import { S as Ha } from "./save-kV_dHGFf.js";
import { r as er, b as Zs, c as Fo, C as Wa, d as ll, T as fl, P as pl, i as ml } from "./power-icon-B4F9A-tn.js";
import { U as gl } from "./upload-BAn1zipX.js";
import { U as yl, W as hl } from "./workflow-DCWxX26l.js";
import { X as Ya } from "./in-flight-request-CXY2yBH9.js";
import { t as G } from "./index-Cf7idtTi.js";
import { u as wl } from "./site-brand-E6f0Ffb2.js";
import { a2 as Lt, a3 as P, a4 as Xa, n as Ei, a5 as Za, _ as Se, z as $n, a6 as an, g as We, F as Ja, a7 as bl, $ as $e, T as _l, E as no, q as Nl, a8 as Hn, y as Il, p as Js, U as Sl, a1 as yo, V as Ue, a9 as Qa, aa as vl, Z as Ot, ab as qn, ac as Dr, d as Cl, ad as ec, W as kl, ae as _s, af as Rl, ag as xl } from "./storyboard-grid-view-BldHSQpc.js";
import { O as tc, P as Tl, m as nc, l as Ml, k as Al, j as Dl, h as Pl, b as zl, Q as Fl, s as rc, S as oc, R as ho, T as El, c as Ol, n as $l, I as jl, w as wr, i as sc, t as ic, U as ur, C as lr, u as Bl, v as Vl, q as Ul, o as Oi, V as Ll } from "./space-content-view-TucLzffi.js";
import { C as Kl } from "./copy-BlmHyHAH.js";
import { P as ac } from "./pencil-DsS_UhAq.js";
import { R as ql } from "./rotate-ccw-BOBeflIt.js";
import { T as Gl } from "./trash-2-C2PWG3er.js";
import { C as Hl, a as lt } from "./space-entry-DvXW-3-r.js";
import "./media-inspector-gallery-TNes-xFo.js";
import { F as Wl } from "./music-DaWYUdzx.js";
const Yl = [
  [
    "path",
    {
      d: "m16.24 7.76-1.804 5.411a2 2 0 0 1-1.265 1.265L7.76 16.24l1.804-5.411a2 2 0 0 1 1.265-1.265z",
      key: "9ktpf1"
    }
  ],
  ["circle", { cx: "12", cy: "12", r: "10", key: "1mglay" }]
], Xl = yr("compass", Yl);
const Zl = [
  [
    "path",
    {
      d: "M6 22a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h8a2.4 2.4 0 0 1 1.704.706l3.588 3.588A2.4 2.4 0 0 1 20 8v12a2 2 0 0 1-2 2z",
      key: "1oefj6"
    }
  ],
  ["path", { d: "M14 2v5a1 1 0 0 0 1 1h5", key: "wfsgrz" }],
  ["circle", { cx: "11.5", cy: "14.5", r: "2.5", key: "1bq0ko" }],
  ["path", { d: "M13.3 16.3 15 18", key: "2quom7" }]
], cc = yr("file-search", Zl);
const Jl = [
  ["circle", { cx: "12", cy: "12", r: "3", key: "1v7zrd" }],
  ["path", { d: "M3 7V5a2 2 0 0 1 2-2h2", key: "aa7l1z" }],
  ["path", { d: "M17 3h2a2 2 0 0 1 2 2v2", key: "4qcy5o" }],
  ["path", { d: "M21 17v2a2 2 0 0 1-2 2h-2", key: "6vwrx8" }],
  ["path", { d: "M7 21H5a2 2 0 0 1-2-2v-2", key: "ioqczr" }]
], Ql = yr("focus", Jl);
const ef = [
  [
    "path",
    {
      d: "M15 14c.2-1 .7-1.7 1.5-2.5 1-.9 1.5-2.2 1.5-3.5A6 6 0 0 0 6 8c0 1 .2 2.2 1.5 3.5.7.7 1.3 1.5 1.5 2.5",
      key: "1gvzjb"
    }
  ],
  ["path", { d: "M9 18h6", key: "x1upvd" }],
  ["path", { d: "M10 22h4", key: "ceow96" }]
], tf = yr("lightbulb", ef);
const nf = [
  [
    "path",
    {
      d: "M14.106 5.553a2 2 0 0 0 1.788 0l3.659-1.83A1 1 0 0 1 21 4.619v12.764a1 1 0 0 1-.553.894l-4.553 2.277a2 2 0 0 1-1.788 0l-4.212-2.106a2 2 0 0 0-1.788 0l-3.659 1.83A1 1 0 0 1 3 19.381V6.618a1 1 0 0 1 .553-.894l4.553-2.277a2 2 0 0 1 1.788 0z",
      key: "169xi5"
    }
  ],
  ["path", { d: "M15 5.764v15", key: "1pn4in" }],
  ["path", { d: "M9 3.236v15", key: "1uimfh" }]
], rf = yr("map", nf);
const of = [
  [
    "path",
    {
      d: "M4.037 4.688a.495.495 0 0 1 .651-.651l16 6.5a.5.5 0 0 1-.063.947l-6.124 1.58a2 2 0 0 0-1.438 1.435l-1.579 6.126a.5.5 0 0 1-.947.063z",
      key: "edeuup"
    }
  ]
], dc = yr("mouse-pointer-2", of);
function Qs(e) {
  return e.purpose ? e.purpose : e.id.startsWith("script-item-edge-") || e.id.startsWith("script-compose-edge-") ? "dependency" : e.id.startsWith("script-edge-") ? "structure" : "media";
}
function uc(e) {
  return Qs(e) === "media";
}
const sf = { width: 720, height: 420 }, $i = { width: 360, height: 240 }, ji = { width: 2400, height: 1600 }, af = 48, Qo = 16;
function lc(e, t) {
  return e.filter((n) => n.groupId === t);
}
function wo(e, t) {
  const n = e.find((r) => r.id === t);
  return n ? n.type === "group" ? lc(e, n.id) : [n] : [];
}
function cf(e, t) {
  return !(!e || !t || e.id === t.id || e.type === "group" && t.groupId === e.id || t.type === "group" && e.groupId === t.id || e.groupId !== t.groupId && t.type !== "group" && t.groupId);
}
function df(e, t, n) {
  const r = e.find((i) => i.id === t);
  if (!r || r.x === n.x && r.y === n.y)
    return e;
  const o = n.x - r.x, s = n.y - r.y;
  return e.map((i) => i.id === t ? { ...i, ...n } : r.type === "group" && i.groupId === r.id ? {
    ...i,
    x: i.x + o,
    y: i.y + s
  } : i);
}
function Ns(e, t, n) {
  const r = e.find((i) => i.id === t);
  if (!r || r.type === "group")
    return e;
  const o = { ...r, ...n }, s = uf(e, o);
  return (r.groupId || "") === s ? e : e.map(
    (i) => i.id === t ? { ...i, groupId: s || void 0 } : i
  );
}
function wn(e, t) {
  const n = new Map(e.map((s) => [s.id, s])), r = /* @__PURE__ */ new Set(), o = [];
  for (const s of t) {
    const i = s.logicalFrom || s.from, a = s.logicalTo || s.to, d = n.get(i), l = n.get(a);
    if (!d || !l)
      continue;
    const u = d.groupId !== l.groupId, m = u && d.type !== "group" && d.groupId ? d.groupId : d.id, N = u && l.type !== "group" && l.groupId ? l.groupId : l.id;
    if (!m || !N || m === N)
      continue;
    const b = `${i}\0${a}\0${Qs(s)}`;
    r.has(b) || (r.add(b), o.push({ ...s, from: m, to: N, logicalFrom: i, logicalTo: a }));
  }
  return o;
}
function uf(e, t) {
  const n = t.x + t.width / 2, r = t.y + t.height / 2;
  return e.filter(
    (s) => s.type === "group" && s.id !== t.id && n >= s.x + Qo && n <= s.x + s.width - Qo && r >= s.y + af && r <= s.y + s.height - Qo
  ).sort(
    (s, i) => s.width * s.height - i.width * i.height
  )[0]?.id || "";
}
const lf = [
  {
    name: "基础",
    options: [
      { key: "none", name: "无转场" },
      { key: "fade", name: "淡化" },
      { key: "crossfade", name: "交叉溶解" },
      { key: "fadeblack", name: "黑场淡化" },
      { key: "fadewhite", name: "白场淡化" }
    ]
  },
  {
    name: "擦除",
    options: [
      { key: "wipeleft", name: "向左擦除" },
      { key: "wiperight", name: "向右擦除" },
      { key: "wipeup", name: "向上擦除" },
      { key: "wipedown", name: "向下擦除" }
    ]
  },
  {
    name: "滑动",
    options: [
      { key: "slideleft", name: "向左滑动" },
      { key: "slideright", name: "向右滑动" },
      { key: "slideup", name: "向上滑动" },
      { key: "slidedown", name: "向下滑动" }
    ]
  },
  {
    name: "平滑",
    options: [
      { key: "smoothleft", name: "向左平滑" },
      { key: "smoothright", name: "向右平滑" },
      { key: "smoothup", name: "向上平滑" },
      { key: "smoothdown", name: "向下平滑" }
    ]
  },
  {
    name: "镜头",
    options: [
      { key: "zoomin", name: "放大切换" },
      { key: "circleopen", name: "圆形展开" },
      { key: "circleclose", name: "圆形收拢" }
    ]
  },
  {
    name: "覆盖",
    options: [
      { key: "coverleft", name: "向左覆盖" },
      { key: "coverright", name: "向右覆盖" },
      { key: "coverup", name: "向上覆盖" },
      { key: "coverdown", name: "向下覆盖" }
    ]
  },
  {
    name: "揭示",
    options: [
      { key: "revealleft", name: "向左揭示" },
      { key: "revealright", name: "向右揭示" },
      { key: "revealup", name: "向上揭示" },
      { key: "revealdown", name: "向下揭示" }
    ]
  }
];
function Pb(e) {
  const t = Math.round(e * 10) / 10;
  return Number.isInteger(t) ? t.toString() : t.toFixed(1);
}
function zb() {
  return {
    version: 3,
    clips: [],
    audioTracks: [],
    settings: {
      resolution: "auto",
      fps: 0
    }
  };
}
function fc(e) {
  const t = Lt(e);
  if (Number(t.version || 0) !== 3)
    return;
  const n = Array.isArray(t.clips) ? t.clips.map(pf).filter(Boolean) : [], r = Lt(t.settings), o = Array.isArray(t.audioTracks ?? t.audio_tracks) ? (t.audioTracks ?? t.audio_tracks).map(hf).filter(Boolean) : [];
  return {
    version: 3,
    clips: n,
    audioTracks: o,
    settings: {
      resolution: De(r.resolution) || "auto",
      fps: Jn(r.fps, 0, 120, 0)
    }
  };
}
function Fb(e) {
  const t = e.clips.reduce(
    (r, o) => r + Math.max(0, o.duration),
    0
  ), n = e.clips.reduce((r, o, s) => s >= e.clips.length - 1 || o.transitionToNext.type === "none" ? r : r + o.transitionToNext.durationMs / 1e3, 0);
  return Math.max(0, t - n);
}
function Eb(e) {
  const t = e.clips.flatMap(
    (r, o) => r.blockingIssues.map(
      (s) => `${r.title || `镜头 ${o + 1}`}：${s}`
    )
  ), n = e.audioTracks.flatMap(
    (r, o) => r.audio ? [] : [`全片声音 ${o + 1}：缺少音频素材`]
  );
  return [...t, ...n];
}
function ff(e) {
  return e ? `${e.assetId}:${e.versionId}` : "";
}
function Ob(e) {
  return e ? [
    ff(e),
    Number(e.mediaIndex || 0),
    e.mediaUrl || ""
  ].join(":") : "";
}
function pf(e) {
  const t = Lt(e), n = De(t.id);
  if (!n)
    return null;
  const r = bo(
    t.visualVideo ?? t.visual_video
  ), o = bo(
    t.originalAudioSource ?? t.original_audio_source
  ), s = Lt(
    t.transitionToNext ?? t.transition_to_next
  ), i = Lt(
    t.storyboardTransitionToNext ?? t.storyboard_transition_to_next
  ), a = Array.isArray(t.speechTracks ?? t.speech_tracks) ? (t.speechTracks ?? t.speech_tracks).map(yf).filter(Boolean) : [], d = Array.isArray(
    t.subtitleTracks ?? t.subtitle_tracks
  ) ? (t.subtitleTracks ?? t.subtitle_tracks).map(gf).filter(Boolean) : [], l = mf(
    i
  ), u = mc(s.type), m = De(t.sourceEdgeId ?? t.source_edge_id);
  return {
    id: n,
    title: De(t.title) || r?.label || "镜头",
    ...m ? { sourceEdgeId: m } : {},
    ...r ? { visualVideo: r } : {},
    ...o ? { originalAudioSource: o } : {},
    duration: wf(t.duration),
    originalVolume: Jn(
      t.originalVolume ?? t.original_volume,
      0,
      1,
      1
    ),
    speechTracks: a,
    subtitleTracks: d,
    useOriginalVideo: gc(
      t.useOriginalVideo ?? t.use_original_video
    ),
    blockingIssues: _f(
      t.blockingIssues ?? t.blocking_issues
    ),
    transitionToNext: {
      type: u,
      durationMs: u === "none" ? 0 : Jn(
        s.durationMs ?? s.duration_ms,
        100,
        5e3,
        500
      )
    },
    ...l ? { storyboardTransitionToNext: l } : {}
  };
}
function mf(e) {
  if (!Object.keys(e).length)
    return;
  const t = mc(e.type);
  return {
    type: t,
    durationMs: t === "none" ? 0 : Jn(e.durationMs ?? e.duration_ms, 100, 5e3, 500)
  };
}
function gf(e) {
  const t = Lt(e), n = De(t.id), r = De(t.text);
  if (!n || !r)
    return null;
  const o = De(t.source) === "speech" ? "speech" : "caption", s = De(t.speechId ?? t.speech_id), i = P(t.endTime ?? t.end_time);
  return {
    id: n,
    text: r,
    startTime: Math.max(0, P(t.startTime ?? t.start_time)),
    ...i > 0 ? { endTime: i } : {},
    ...s ? { speechId: s } : {},
    source: o
  };
}
function yf(e) {
  const t = Lt(e), n = De(t.id);
  if (!n)
    return null;
  const r = bo(t.audio), o = De(t.kind) === "narration" ? "narration" : "dialogue", s = De(t.characterId ?? t.character_id);
  return {
    id: n,
    ...r ? { audio: r } : {},
    startTime: Math.max(0, P(t.startTime ?? t.start_time)),
    sourceStart: Math.max(
      0,
      P(t.sourceStart ?? t.source_start)
    ),
    fit: pc(t.fit, "trim"),
    kind: o,
    ...s ? { characterId: s } : {},
    text: De(t.text),
    volume: Jn(t.volume, 0, 1, 1)
  };
}
function hf(e) {
  const t = Lt(e), n = De(t.id);
  if (!n)
    return null;
  const r = bo(t.audio), o = De(t.kind) === "narration" ? "narration" : "music";
  return {
    id: n,
    ...r ? { audio: r } : {},
    startTime: Math.max(0, P(t.startTime ?? t.start_time)),
    sourceStart: Math.max(
      0,
      P(t.sourceStart ?? t.source_start)
    ),
    kind: o,
    volume: Jn(t.volume, 0, 1, o === "music" ? 0.35 : 1),
    fit: pc(t.fit, o === "music" ? "trim" : "strict"),
    loop: o === "music" && gc(t.loop),
    fadeOut: Jn(
      t.fadeOut ?? t.fade_out,
      0,
      10,
      o === "music" ? 1 : 0
    )
  };
}
function pc(e, t) {
  return De(e) === "strict" ? "strict" : De(e) === "trim" ? "trim" : t;
}
function wf(e) {
  const t = P(e);
  return t > 0 ? Math.max(1, Math.floor(t)) : 0;
}
function bo(e) {
  const t = Lt(e), n = P(t.assetId ?? t.asset_id), r = P(t.versionId ?? t.version_id);
  if (!n || !r)
    return;
  const o = bf(
    t.mediaItems ?? t.media_items ?? t.refMediaItems ?? t.ref_media_items
  );
  return {
    assetId: n,
    versionId: r,
    label: De(t.label),
    ...P(t.mediaIndex ?? t.media_index) > 0 ? { mediaIndex: P(t.mediaIndex ?? t.media_index) } : {},
    ...De(t.mediaUrl ?? t.media_url) ? { mediaUrl: De(t.mediaUrl ?? t.media_url) } : {},
    ...o.length ? { mediaItems: o } : {}
  };
}
function bf(e) {
  if (!Array.isArray(e))
    return [];
  const t = [], n = /* @__PURE__ */ new Set();
  for (const r of e) {
    const o = Lt(r), s = De(o.url), i = P(o.index);
    if (!s && i <= 0)
      continue;
    const a = i > 0 ? `index:${i}` : `url:${s}`;
    n.has(a) || (n.add(a), t.push({
      url: s,
      index: i > 0 ? i : 0,
      ...De(o.usage) ? { usage: De(o.usage) } : {}
    }));
  }
  return t;
}
function mc(e) {
  const t = De(e);
  return lf.some(
    (n) => n.options.some((r) => r.key === t)
  ) ? t : "none";
}
function De(e) {
  return String(e ?? "").trim();
}
function _f(e) {
  return Array.isArray(e) ? e.map(De).filter(Boolean) : [];
}
function Jn(e, t, n, r) {
  const o = Number(e);
  return Number.isFinite(o) ? Math.min(n, Math.max(t, o)) : r;
}
function gc(e) {
  return e === !0 || e === 1 || e === "1" || e === "true";
}
const Is = {
  id: 0,
  team_id: 0,
  name: "自由",
  kind: "richtext",
  cardinality: "multiple",
  status: 1,
  sort: 0,
  virtual: !0
}, Nf = { width: 180, height: 180 }, If = { width: 240, height: 160 }, Sf = { width: 620, height: 360 };
function vf(e) {
  const t = ve(e);
  return {
    project: Mf(t.project),
    team: Af(t.team),
    release: Df(t.release),
    assetCates: Jt(t.asset_cates).map(Pf),
    flows: Jt(t.flows).map(Ff),
    canvases: Of(t.canvas),
    assets: Jt(ve(t.assets).items).map(vc),
    initialAssetCateId: P(t.active_asset_cate_id)
  };
}
function ro(e) {
  return {
    assetCateId: e,
    nextNodeNo: 1,
    nodes: [],
    edges: [],
    viewport: {}
  };
}
function yc(e, t = 0) {
  const n = ve(e), r = P(
    Se(n.asset_cate_id, t)
  );
  return hc({
    assetCateId: r,
    nextNodeNo: Math.max(1, P(n.next_node_no)),
    nodes: Jt(n.nodes).map(Bf).filter((o) => !!o),
    edges: Jt(n.edges).map(Qf).filter((o) => !!o),
    viewport: ep(n.viewport),
    updatedAt: R(n.updated_at)
  });
}
function hc(e) {
  let t = ei(e.nodes, e.nextNodeNo), n = t !== e.nextNodeNo;
  const r = e.nodes.map((o) => {
    if (!wc(o) || Number(o.nodeNo || 0) > 0)
      return o;
    const s = t++, i = o.titleMode || "manual";
    return n = !0, {
      ...o,
      nodeNo: s,
      titleMode: i,
      ...i === "auto" && !o.storyboardItem ? { title: bc(o, s) } : {}
    };
  });
  return n ? { ...e, nextNodeNo: t, nodes: r } : e;
}
function ei(e, t = 1) {
  return Math.max(
    1,
    Number(t || 1),
    ...e.map((n) => Number(n.nodeNo || 0) + 1)
  );
}
function wc(e) {
  return e.type === "power" || e.type === "agent" || e.type === "flow";
}
function bc(e, t) {
  let n = "节点";
  if (e.type === "power") {
    const r = er(
      e.power,
      e.kind,
      e.outputType
    );
    n = r.outputType !== "general" && r.outputName || r.kindName;
  } else e.type === "agent" ? n = String(e.role?.name || "智能体").trim() || "智能体" : e.type === "flow" && (n = String(e.flow?.name || "流程").trim() || "流程");
  return `${n}-${t}`;
}
function Cf(e) {
  const t = ve(e);
  return {
    roles: Jt(t.roles).map(zf),
    powers: Jt(t.powers).map(Nc),
    powerCategories: Jt(t.power_cates).map(cl),
    powerKinds: Jt(t.power_kinds).map(Ef),
    outputTypes: Jt(t.output_types).map(Sc)
  };
}
function mr(e) {
  return vc(ve(e));
}
function _c(e) {
  return e.assetCates.length > 0 ? e.assetCates : [Is];
}
function kf(e) {
  return _c(e)[0]?.id ?? 0;
}
function Ss(e, t) {
  const n = e.length > 0 ? e : [Is];
  return n.find((r) => r.id === t) || n[0] || Is;
}
function uo(e, t) {
  return Ss(e.assetCates, t);
}
function Rf(e, t) {
  return t === 0 ? e.flows.slice(0, 4) : e.flows.filter((n) => tp(n).has(t)).slice(0, 4);
}
function xf(e) {
  return e.create_status !== 2;
}
function Tf(e) {
  return e.createStatus !== 2;
}
function Eo(e, t, n, r, o) {
  const s = r?.x ?? 420 + n % 3 * 190, i = r?.y ?? 610 + Math.floor(n / 3) * 170, a = o?.asset, d = o?.flow, l = o?.functionOption, u = o?.power, m = o?.role, N = u ? er(u) : null, b = Number(a?.asset_cate_id || t.id), w = {
    asset: [
      a?.name || "资产引用",
      a ? Ei(a.kind) : Ei(t.kind),
      a && Za(a.version?.content) || "引用已有资产，作为其他节点的上下文。"
    ],
    power: [
      u?.name || np(t.kind),
      N?.outputName || N?.kindName || "能力节点",
      u ? `调用 ${u.name} 能力，按参数生成内容。` : "输入提示词和参数，直接生成文本、图片、视频或音频。"
    ],
    agent: [
      m?.name || "智能体节点",
      m?.role_type || "角色执行",
      m?.assignment || "调用团队角色或指定智能体完成一段任务。"
    ],
    flow: [
      d?.name || "流程节点",
      "团队流程",
      d?.goal || "执行一组团队预设流程。"
    ],
    function: [
      l?.label || "保存节点",
      "功能",
      l?.description || "开始、导入、保存、展示等功能节点。"
    ],
    group: [
      "未命名分组",
      "分组",
      "拖入节点后，可统一接收上下文并执行组内节点。"
    ]
  }, [z, F, U] = w[e], A = kc(e, u);
  return {
    id: `local-${e}-${Date.now()}-${n}`,
    type: e,
    title: z,
    titleMode: e === "power" || e === "agent" || e === "flow" ? "auto" : void 0,
    subtitle: F,
    description: U,
    x: s,
    y: i,
    width: A.width,
    height: A.height,
    assetCateId: b,
    kind: a?.kind || u?.kind || t.kind,
    outputType: u?.outputType,
    cardinality: t.cardinality,
    asset: a,
    flow: d,
    functionOption: l,
    power: u,
    role: m,
    group: e === "group" ? { origin: "manual" } : void 0,
    local: !0
  };
}
function Mf(e) {
  const t = ve(e), n = ve(t.team);
  return {
    id: P(t.id),
    body_id: P(t.body_id),
    team_id: P(t.team_id),
    release_id: P(t.release_id),
    name: R(t.name) || "未命名作品",
    description: R(t.description),
    mode: R(t.mode) || "team",
    team: {
      id: P(n.id),
      name: R(n.name),
      version: P(n.version)
    }
  };
}
function Af(e) {
  const t = ve(e);
  return {
    id: P(t.id),
    name: R(t.name) || "自由团队",
    description: R(t.description)
  };
}
function Df(e) {
  const t = ve(e);
  return {
    id: P(t.id),
    team_id: P(t.team_id),
    version: P(t.version),
    status: R(t.status)
  };
}
function Pf(e) {
  return {
    id: P(e.id),
    team_id: P(e.team_id),
    name: R(e.name) || "未命名资产",
    kind: R(e.kind) || "text",
    cardinality: R(e.cardinality) || "single",
    status: P(e.status),
    sort: P(e.sort)
  };
}
function zf(e) {
  return {
    id: P(e.id),
    team_id: P(e.team_id),
    role_type: R(e.role_type),
    role_key: R(e.role_key),
    name: R(e.name),
    agent_id: P(e.agent_id),
    assignment: R(e.assignment),
    create_status: Ic(e.create_status)
  };
}
function Ff(e) {
  return {
    id: P(e.id),
    name: R(e.name),
    key: R(e.key),
    goal: R(e.goal),
    config: ve(e.config),
    status: P(e.status),
    sort: P(e.sort),
    output_asset_cate_ids: Rc(e.output_asset_cate_ids)
  };
}
function Nc(e) {
  const t = R(e.kind) || "text", n = Sc(ve(e.output)), r = R(e.output_type) || "general";
  return {
    id: P(e.id),
    cate_id: P(e.cate_id),
    name: R(e.name) || R(e.key) || "未命名能力",
    key: R(e.key),
    icon: R(e.icon),
    description: R(e.description),
    outputType: r,
    output: n.key ? n : void 0,
    kind: t,
    createStatus: Ic(e.create_status)
  };
}
function Ic(e) {
  return Number(e) === 2 ? 2 : 1;
}
function Sc(e) {
  return {
    key: R(e.key),
    name: R(e.name),
    allowedKinds: lo(e.allowed_kinds),
    viewMode: R(e.view_mode),
    defaultWidth: P(e.default_width),
    defaultHeight: P(e.default_height),
    structured: !!e.structured,
    sort: P(e.sort)
  };
}
function Ef(e) {
  return {
    id: R(e.id),
    value: R(e.value) || R(e.name) || R(e.id)
  };
}
function vc(e) {
  const t = ti(ve(e.version)), n = ni(e.versions);
  return {
    id: P(e.id),
    project_id: P(Se(e.project_id, e.projectID)),
    body_id: P(Se(e.body_id, e.bodyID)),
    team_id: P(Se(e.team_id, e.teamID)),
    flow_id: P(Se(e.flow_id, e.flowID)),
    asset_cate_id: P(
      Se(e.asset_cate_id, e.assetCateID)
    ),
    node_key: R(Se(e.node_key, e.nodeKey)),
    name: R(e.name),
    kind: R(e.kind) || "text",
    role: R(e.role),
    version_id: P(Se(e.version_id, e.versionID)),
    status: R(e.status),
    sort: P(e.sort),
    created_at: R(Se(e.created_at, e.createdAt)),
    version: t,
    versions: n.length ? n : void 0
  };
}
function ti(e) {
  const t = P(e.id);
  if (!(!t && e.content == null))
    return {
      id: t,
      asset_id: P(Se(e.asset_id, e.assetID)),
      run_id: P(Se(e.run_id, e.runID)),
      node_run_id: P(Se(e.node_run_id, e.nodeRunID)),
      release_id: P(Se(e.release_id, e.releaseID)),
      request_id: R(Se(e.request_id, e.requestID)),
      node_key: R(Se(e.node_key, e.nodeKey)),
      source: ve(e.source),
      version: P(e.version),
      summary: R(e.summary),
      content: e.content,
      created_at: R(Se(e.created_at, e.createdAt)),
      updated_at: R(Se(e.updated_at, e.updatedAt))
    };
}
function ni(e) {
  return Jt(e).map(ti).filter((t) => !!t);
}
function Of(e) {
  const t = ve(e), n = {};
  for (const [r, o] of Object.entries(t)) {
    const s = yc(o, P(r));
    n[String(s.assetCateId)] = s;
  }
  return n;
}
function $f(e, t) {
  const n = new Map(
    t.filter((o) => o.id > 0).map((o) => [o.id, o])
  ), r = new Map(
    t.filter((o) => o.key).map((o) => [o.key, o])
  );
  for (const o of Object.values(e))
    o.nodes = o.nodes.map((s) => {
      if (s.type !== "power" || !s.power)
        return s;
      const i = n.get(Number(s.power.id || 0)) || r.get(s.power.key);
      if (!i)
        return s;
      const a = s.outputType || s.power.outputType || i.outputType;
      return {
        ...s,
        outputType: a,
        power: {
          ...i,
          outputType: a,
          output: i.outputType === a ? i.output : s.power.output
        }
      };
    });
  return e;
}
function jf(e, t) {
  return $f(
    { [String(e.assetCateId)]: e },
    t
  )[String(e.assetCateId)];
}
function Bf(e) {
  const t = R(e.id), n = R(e.type);
  if (!t || !n)
    return null;
  const r = {
    id: t,
    nodeNo: P(e.node_no) || void 0,
    type: n,
    title: R(e.title),
    titleMode: R(e.title_mode) === "manual" ? "manual" : R(e.title_mode) === "auto" ? "auto" : void 0,
    subtitle: R(e.subtitle),
    description: R(e.description),
    x: P(e.x),
    y: P(e.y),
    width: P(e.width),
    height: P(e.height),
    groupId: R(e.group_id),
    group: Uf(e.group),
    storyboardItem: Lf(e.storyboard_item),
    storyboardMaterializedSignature: R(
      e.storyboard_materialized_signature
    ),
    assetCateId: P(e.asset_cate_id),
    outputType: R(e.output_type),
    count: e.count == null ? void 0 : P(e.count),
    functionOption: Wf(e.function_option),
    composerDraft: Xf(e.composer_draft),
    resultRef: Jf(e.result_ref),
    resultOutput: e.result_output,
    resultView: Vf(e.result_view),
    runError: R(e.run_error),
    local: e.local !== !1
  }, o = R(e.kind), s = R(
    e.cardinality
  ), i = Kf(e.flow), a = qf(e.role), d = Gf(e.asset), l = Hf(e.power);
  return o && (r.kind = o), s && (r.cardinality = s), i && (r.flow = i), a && (r.role = a), d && (r.asset = d), l && (r.power = l, r.outputType = r.outputType || l.outputType), r;
}
function Vf(e) {
  const t = ve(e), n = an(t.width), r = an(t.height);
  if (n == null || r == null || n <= 0 || r <= 0)
    return;
  const o = an(t.offset_x), s = an(t.offset_y);
  return {
    width: n,
    height: r,
    ...o == null ? {} : { offsetX: o },
    ...s == null ? {} : { offsetY: s }
  };
}
function Uf(e) {
  const t = ve(e);
  if (Object.keys(t).length)
    return {
      origin: R(t.origin),
      sourceNodeId: R(t.source_node_id),
      syncKey: R(t.sync_key),
      layoutKey: R(t.layout_key)
    };
}
function Lf(e) {
  const t = ve(e), n = R(t.source_node_id), r = R(t.item_type), o = R(t.item_id);
  if (!(!n || !o || ![
    "character",
    "scene",
    "prop",
    "shot_image",
    "shot",
    "speech",
    "subtitle",
    "lip_sync",
    "video_compose"
  ].includes(r)))
    return {
      sourceNodeId: n,
      itemType: r,
      itemId: o,
      generatedPrompt: R(t.generated_prompt),
      dependencyNodeIds: lo(t.dependency_node_ids),
      referenceNodeIds: lo(t.reference_node_ids),
      externalReferenceAssetIds: Rc(t.external_reference_asset_ids),
      shotId: R(t.shot_id),
      speechId: R(t.speech_id),
      speechIds: lo(t.speech_ids),
      characterId: R(t.character_id),
      speechKind: R(t.speech_kind),
      speakerMode: R(t.speaker_mode),
      startTime: an(t.start_time),
      shotDuration: an(t.shot_duration),
      continuityAnchor: R(t.continuity_anchor),
      optional: t.optional === !0 || t.optional === 1 || String(t.optional || "").toLowerCase() === "true",
      sourceSignature: R(t.source_signature),
      resultSourceSignature: R(t.result_source_signature),
      stale: t.stale === !0 || t.stale === 1 || String(t.stale || "").toLowerCase() === "true"
    };
}
function Kf(e) {
  const t = ve(e), n = P(t.id), r = R(t.key), o = R(t.name);
  if (!(!n && !r && !o))
    return {
      id: n,
      key: r,
      name: o,
      goal: R(t.goal)
    };
}
function qf(e) {
  const t = ve(e), n = P(t.id), r = R(t.name);
  if (!(!n && !r))
    return {
      id: n,
      name: r,
      role_type: R(t.role_type),
      agent_id: P(t.agent_id)
    };
}
function Gf(e) {
  const t = ve(e), n = P(t.id);
  if (n)
    return {
      id: n,
      project_id: 0,
      body_id: 0,
      team_id: 0,
      flow_id: 0,
      asset_cate_id: P(t.asset_cate_id),
      name: R(t.name),
      kind: R(t.kind),
      role: R(t.role),
      version_id: P(t.version_id),
      sort: 0
    };
}
function Hf(e) {
  const t = ve(e), n = P(t.id), r = R(t.key);
  if (!(!n && !r))
    return Nc({
      ...t,
      id: n,
      key: r,
      name: R(t.name)
    });
}
function Wf(e) {
  const t = ve(e), n = R(t.key);
  if (n)
    return {
      key: n,
      label: R(t.label),
      description: R(t.description)
    };
}
function Cc(e) {
  const t = ve(e);
  if (!Object.keys(t).length)
    return;
  const n = R(t.storyboardGridLayout);
  return {
    prompt: R(t.prompt),
    promptContent: Zf(t.promptContent),
    paramValues: ve(t.paramValues),
    selectedTargetId: P(t.selectedTargetId),
    videoComposition: fc(t.videoComposition),
    storyboardReferences: tc(
      t.storyboardReferences
    ),
    storyboardGridLayout: n ? Xa(n) : void 0,
    multiImageMode: Yf(t.multiImageMode)
  };
}
function Yf(e) {
  const t = R(e);
  return t === "per_image" || t === "shared_reference" ? t : void 0;
}
function Xf(e) {
  const t = ve(e);
  if (Object.keys(t).length)
    return Cc({
      prompt: t.prompt,
      promptContent: t.prompt_content,
      paramValues: t.param_values,
      selectedTargetId: t.selected_target_id,
      videoComposition: t.video_composition,
      storyboardReferences: t.storyboard_references,
      storyboardGridLayout: t.storyboard_grid_layout,
      multiImageMode: t.multi_image_mode
    });
}
function Zf(e) {
  const t = ve(e), n = Array.isArray(t.parts) ? t.parts.filter((r) => {
    const o = ve(r);
    return o.type === "text" || o.type === "reference";
  }) : [];
  if (!(Number(t.version) !== 1 || n.length === 0))
    return { version: 1, parts: n };
}
function Jf(e) {
  const t = ve(e);
  if (Object.keys(t).length)
    return {
      run_id: P(t.run_id),
      request_id: R(t.request_id),
      flow_run_id: P(t.flow_run_id),
      node_run_id: P(t.node_run_id),
      asset_id: P(t.asset_id),
      version_id: P(t.version_id),
      release_id: P(t.release_id),
      role: R(t.role),
      status: R(t.status),
      updated_at: R(t.updated_at)
    };
}
function Qf(e) {
  const t = R(e.from), n = R(e.to);
  if (!t || !n)
    return null;
  const r = R(e.purpose);
  return {
    id: R(e.id) || `edge-${t}-${n}`,
    from: t,
    to: n,
    logicalFrom: R(e.logical_from) || void 0,
    logicalTo: R(e.logical_to) || void 0,
    purpose: r === "media" || r === "structure" || r === "dependency" ? r : void 0,
    executionMode: R(e.execution_mode) === "manual" ? "manual" : void 0,
    mediaUsage: R(e.media_usage) || void 0
  };
}
function ep(e) {
  const t = ve(e), n = {};
  return t.x != null && (n.x = P(t.x)), t.y != null && (n.y = P(t.y)), t.zoom != null && (n.zoom = P(t.zoom)), n;
}
function tp(e) {
  return new Set(e.output_asset_cate_ids);
}
function np(e) {
  switch (e) {
    case "image":
      return "生图能力";
    case "video":
      return "生视频能力";
    case "audio":
      return "生音频能力";
    default:
      return "文生文能力";
  }
}
function kc(e, t) {
  switch (e) {
    case "agent":
      return { width: 154, height: 154 };
    case "flow":
      return { width: 210, height: 160 };
    case "function":
      return { width: 128, height: 46 };
    case "group":
      return { ...sf };
    case "power":
      return Oo(t);
    default:
      return { width: 250, height: 170 };
  }
}
function Oo(e) {
  if (Zs(e))
    return { ...If };
  const t = rp(e);
  return t || (Fo(e) ? { ...Sf } : { ...Nf });
}
function rp(e) {
  const t = Number(e?.output?.defaultWidth || 0), n = Number(e?.output?.defaultHeight || 0);
  return t > 0 && n > 0 ? { width: t, height: n } : null;
}
function op(e) {
  const t = kc(
    e.type,
    e.power || {
      kind: String(e.kind || ""),
      outputType: e.outputType || ""
    }
  );
  return e.width === t.width && e.height === t.height;
}
function lo(e) {
  return Array.isArray(e) ? e.map(R).filter(Boolean) : [];
}
function Rc(e) {
  if (!Array.isArray(e))
    return [];
  const t = [], n = /* @__PURE__ */ new Set();
  for (const r of e) {
    const o = P(r);
    !o || o <= 0 || n.has(o) || (n.add(o), t.push(o));
  }
  return t;
}
function Jt(e) {
  return Array.isArray(e) ? e.filter($n) : [];
}
function ve(e) {
  return $n(e) ? e : {};
}
function R(e) {
  return e == null ? "" : String(e).trim();
}
function xc(e) {
  return {
    asset_cate_id: Number(e.assetCateId || 0),
    next_node_no: Math.max(1, Number(e.nextNodeNo || 1)),
    nodes: e.nodes.map(sp),
    edges: e.edges.map((t) => ({
      id: t.id,
      from: t.from,
      to: t.to,
      ...t.logicalFrom ? { logical_from: t.logicalFrom } : {},
      ...t.logicalTo ? { logical_to: t.logicalTo } : {},
      ...t.purpose ? { purpose: t.purpose } : {},
      ...t.executionMode === "manual" ? { execution_mode: "manual" } : {},
      ...t.mediaUsage ? { media_usage: t.mediaUsage } : {}
    })),
    viewport: {
      ...e.viewport.x == null ? {} : { x: e.viewport.x },
      ...e.viewport.y == null ? {} : { y: e.viewport.y },
      ...e.viewport.zoom == null ? {} : { zoom: e.viewport.zoom }
    }
  };
}
function sp(e) {
  const t = {
    id: e.id,
    type: e.type,
    title: e.title,
    subtitle: e.subtitle,
    description: e.description,
    x: e.x,
    y: e.y,
    width: e.width,
    height: e.height
  };
  if (fo(t, "node_no", e.nodeNo), e.titleMode && (t.title_mode = e.titleMode), Zt(t, "group_id", e.groupId), e.group) {
    const i = {};
    Zt(i, "origin", e.group.origin), Zt(i, "source_node_id", e.group.sourceNodeId), Zt(i, "sync_key", e.group.syncKey), Zt(i, "layout_key", e.group.layoutKey), Object.keys(i).length > 0 && (t.group = i);
  }
  if (e.storyboardItem) {
    const i = e.storyboardItem;
    t.storyboard_item = {
      source_node_id: i.sourceNodeId,
      item_type: i.itemType,
      item_id: i.itemId,
      ...i.generatedPrompt ? { generated_prompt: i.generatedPrompt } : {},
      dependency_node_ids: i.dependencyNodeIds || [],
      reference_node_ids: i.referenceNodeIds || [],
      external_reference_asset_ids: i.externalReferenceAssetIds || [],
      ...i.shotId ? { shot_id: i.shotId } : {},
      ...i.speechId ? { speech_id: i.speechId } : {},
      ...i.speechIds?.length ? { speech_ids: i.speechIds } : {},
      ...i.characterId ? { character_id: i.characterId } : {},
      ...i.speechKind ? { speech_kind: i.speechKind } : {},
      ...i.speakerMode ? { speaker_mode: i.speakerMode } : {},
      ...Number.isFinite(i.startTime) ? { start_time: i.startTime } : {},
      ...Number.isFinite(i.shotDuration) ? { shot_duration: i.shotDuration } : {},
      ...i.continuityAnchor ? { continuity_anchor: i.continuityAnchor } : {},
      ...i.optional ? { optional: !0 } : {},
      ...i.sourceSignature ? { source_signature: i.sourceSignature } : {},
      ...i.resultSourceSignature ? { result_source_signature: i.resultSourceSignature } : {},
      ...i.stale ? { stale: !0 } : {}
    };
  }
  Zt(
    t,
    "storyboard_materialized_signature",
    e.storyboardMaterializedSignature
  ), fo(t, "asset_cate_id", e.assetCateId), Zt(t, "kind", e.kind), Zt(t, "output_type", e.outputType), Zt(t, "cardinality", e.cardinality), fo(t, "count", e.count), e.flow && (t.flow = {
    id: e.flow.id,
    key: e.flow.key,
    name: e.flow.name,
    goal: e.flow.goal
  }), e.role && (t.role = {
    id: e.role.id,
    name: e.role.name,
    role_type: e.role.role_type,
    agent_id: e.role.agent_id
  }), e.asset && (t.asset = {
    id: e.asset.id,
    name: e.asset.name,
    kind: e.asset.kind,
    role: e.asset.role,
    asset_cate_id: e.asset.asset_cate_id,
    version_id: e.asset.version_id
  }), e.power && (t.power = {
    id: e.power.id,
    key: e.power.key,
    name: e.power.name,
    kind: e.power.kind,
    icon: e.power.icon,
    output_type: e.power.outputType,
    output: cp(e.power.output)
  }), e.functionOption && (t.function_option = {
    key: e.functionOption.key,
    label: e.functionOption.label,
    description: e.functionOption.description
  });
  const n = ap(e.composerDraft);
  n && (t.composer_draft = n);
  const r = fp(e.resultRef);
  r && (t.result_ref = r), !(Number(r?.asset_id || 0) > 0 && Number(r?.version_id || 0) > 0) && e.resultOutput != null && Mn(e.resultOutput) && (t.result_output = e.resultOutput);
  const s = ip(e.resultView);
  return s && (t.result_view = s), Zt(t, "run_error", e.runError), e.local != null && (t.local = e.local), t;
}
function ip(e) {
  if (!e)
    return;
  const t = an(e.width), n = an(e.height);
  if (t == null || n == null || t <= 0 || n <= 0)
    return;
  const r = { width: t, height: n }, o = an(e.offsetX), s = an(e.offsetY);
  return o != null && (r.offset_x = o), s != null && (r.offset_y = s), r;
}
function ap(e) {
  if (!$n(e))
    return null;
  const t = {};
  Zt(t, "prompt", e.prompt);
  const n = dp(e.promptContent);
  n && Mn(n) && (t.prompt_content = n), fo(t, "selected_target_id", e.selectedTargetId);
  const r = up(e.paramValues);
  r && (t.param_values = r);
  const o = fc(e.videoComposition);
  o && Mn(o) && (t.video_composition = o);
  const s = tc(
    e.storyboardReferences
  );
  return s.length > 0 && Mn(s) && (t.storyboard_references = s), e.storyboardGridLayout && (t.storyboard_grid_layout = Xa(
    e.storyboardGridLayout
  )), (e.multiImageMode === "per_image" || e.multiImageMode === "shared_reference") && (t.multi_image_mode = e.multiImageMode), Object.keys(t).length ? t : null;
}
function cp(e) {
  if (e)
    return {
      key: e.key,
      name: e.name,
      allowed_kinds: e.allowedKinds,
      view_mode: e.viewMode,
      default_width: e.defaultWidth,
      default_height: e.defaultHeight,
      structured: e.structured,
      sort: e.sort
    };
}
function dp(e) {
  if (!(!$n(e) || Number(e.version || 0) !== 1 || !Array.isArray(e.parts)))
    return e;
}
function up(e) {
  if (!$n(e))
    return null;
  const t = {};
  for (const [n, r] of Object.entries(e))
    lp(r) || Mn(r) && (t[n] = r);
  return Object.keys(t).length ? t : null;
}
function lp(e) {
  return $n(e) ? !!(e.file || e.blob || e.preview || e.progress != null || e.uploading != null) : !1;
}
function fp(e) {
  if (!$n(e))
    return null;
  const t = {};
  for (const n of [
    "run_id",
    "request_id",
    "flow_run_id",
    "node_run_id",
    "asset_id",
    "version_id",
    "release_id",
    "role",
    "status",
    "updated_at"
  ]) {
    const r = e[n];
    r != null && Mn(r) && (t[n] = r);
  }
  return Object.keys(t).length ? t : null;
}
function Zt(e, t, n) {
  typeof n == "string" && n.trim() && (e[t] = n);
}
function fo(e, t, n) {
  const r = Number(n || 0);
  r > 0 && (e[t] = r);
}
function Mn(e) {
  return e == null || ["string", "number", "boolean"].includes(typeof e) ? !0 : Array.isArray(e) ? e.every(Mn) : $n(e) ? Object.values(e).every(Mn) : !1;
}
async function pp(e, t = 0) {
  const n = await Le(je("workspace/bootstrap"), "get", {
    project_id: e,
    asset_cate_id: t
  });
  return vf(
    hr(n, "加载创作空间失败")
  );
}
async function mp(e) {
  const t = await Le(je("workspace/canvas"), "get", {
    project_id: e.projectId,
    asset_cate_id: e.assetCateId
  }), n = en(t, "加载分类画布失败"), r = n.assets || {}, o = Array.isArray(r.items) ? r.items : Array.isArray(r) ? r : [];
  return {
    canvas: yc(n.canvas, e.assetCateId),
    assets: o.map(mr)
  };
}
async function gp(e) {
  const t = await Le(je("project/canvas_config"), "get", {
    project_id: e
  });
  return Cf(
    hr(t, "加载能力列表失败")
  );
}
async function vs(e) {
  const t = await Le(je("project/canvas_power_form"), "get", {
    project_id: e.projectId,
    flow_id: e.flowId || 0,
    power_id: e.powerId,
    power_key: e.powerKey,
    target_id: e.targetId || 0
  });
  return kp(
    hr(t, "加载能力参数失败")
  );
}
async function yp(e) {
  const t = await Le(je("workspace/canvas_execute"), "post", {
    project_id: e.projectId,
    asset_cate_id: e.assetCateId,
    start_node_id: e.startNodeId,
    request_id: e.requestId || "",
    single_node: !!e.singleNode,
    execution_scope: e.executionScope || "",
    canvas: xc(e.canvas),
    input: e.runInput || {}
  });
  return en(t, "画布运行失败");
}
async function hp(e) {
  const t = await Le(
    je("workspace/canvas_node_title"),
    "post",
    {
      project_id: e.projectId,
      node_key: e.nodeKey,
      version_id: e.versionId,
      prompt: e.prompt || ""
    }
  ), n = en(t, "生成节点标题失败");
  return {
    nodeKey: String(n.node_key || e.nodeKey),
    versionId: Number(n.version_id || e.versionId || 0),
    title: String(n.title || "").trim()
  };
}
function Lr(e, t, n) {
  const r = en(e, t).asset;
  if (!r)
    throw new Error(n);
  return mr(r);
}
async function es(e) {
  const t = await Le(
    je("workspace/canvas_execution_list"),
    "get",
    {
      project_id: e.projectId,
      scope: e.scope,
      asset_cate_id: e.assetCateId || 0,
      run_ids: (e.runIds || []).filter((r) => r > 0).join(","),
      before_id: e.beforeId || 0,
      limit: e.limit || 20,
      summary_only: e.summaryOnly ? 1 : 0
    }
  ), n = en(t, "读取画布运行记录失败");
  return {
    count: Number(n.count || 0),
    items: Array.isArray(n.items) ? n.items : [],
    hasMore: !!n.has_more,
    beforeId: Number(n.before_id || 0)
  };
}
async function Tc(e) {
  const t = Number(e.executionId || 0), n = String(e.requestId || "").trim(), r = Number(e.runId || 0), o = await Le(
    je("workspace/canvas_execution"),
    "get",
    {
      project_id: e.projectId,
      execution_id: t,
      request_id: t > 0 ? "" : n,
      run_id: t > 0 || n ? 0 : r
    }
  );
  return en(o, "读取画布运行详情失败");
}
async function wp(e) {
  const t = await Le(je("run/approval"), "post", {
    project_id: e.projectId,
    run_id: e.runId,
    request_id: e.requestId,
    node_key: e.nodeKey,
    approval_id: e.approvalId || 0,
    data: e.feedback || {},
    decision: e.decision || "approved",
    comment: e.comment || ""
  });
  return hr(t, "继续画布运行失败");
}
async function bp(e) {
  const t = await Le(je("run/status"), "get", {
    project_id: e.projectId,
    run_id: e.runId || 0,
    request_id: e.requestId || "",
    view: "summary"
  });
  return hr(t, "读取流程状态失败");
}
async function _p(e) {
  const t = await Le(je("run/interaction"), "post", {
    project_id: e.projectId,
    run_id: e.runId,
    node_run_id: e.nodeRunId || 0,
    interaction_id: e.interactionId,
    data: e.data
  });
  return hr(t, "提交信息失败");
}
async function Np(e) {
  const t = await Le(
    je("project/update_asset_version"),
    "post",
    {
      project_id: e.projectId,
      asset_id: e.assetId,
      version_id: e.versionId,
      content: e.content
    }
  );
  return Lr(
    t,
    "保存资产版本失败",
    "资产版本保存结果为空"
  );
}
async function $b(e) {
  const t = await Le(
    je("project/restore_asset_version"),
    "post",
    {
      project_id: e.projectId,
      asset_id: e.assetId,
      version_id: e.versionId,
      request_id: e.requestId,
      node_key: e.nodeKey
    }
  );
  return Lr(
    t,
    "恢复资产版本失败",
    "资产版本恢复结果为空"
  );
}
async function jb(e) {
  const t = await Le(
    je("project/confirm_storyboard"),
    "post",
    {
      project_id: e.projectId,
      asset_id: e.assetId,
      version_id: e.versionId,
      production_plan: e.productionPlan
    }
  );
  return Lr(t, "确认分镜失败", "确认分镜结果为空");
}
async function Bb(e) {
  const t = await Le(
    je("project/create_storyboard_revision"),
    "post",
    {
      project_id: e.projectId,
      asset_id: e.assetId,
      version_id: e.versionId,
      request_id: e.requestId,
      node_key: e.nodeKey
    }
  );
  return Lr(
    t,
    "创建分镜修订稿失败",
    "创建分镜修订稿结果为空"
  );
}
async function Vb(e) {
  const t = e.storyboard.shots.findIndex(
    (s) => s.id === e.shotId
  );
  if (t < 0)
    throw new Error("目标镜头不存在");
  const n = await Le(
    je("project/generate_storyboard_shot"),
    "post",
    {
      project_id: e.projectId,
      asset_id: e.assetId,
      version_id: e.versionId,
      flow_id: e.flowId,
      asset_cate_id: e.assetCateId,
      request_id: e.requestId,
      node_key: e.nodeKey,
      node_name: e.nodeName,
      power_id: e.powerId,
      power_key: e.powerKey,
      source_target_id: e.sourceTargetId,
      params: e.params,
      storyboard: e.storyboard,
      shot_id: e.shotId,
      instruction: e.instruction
    }
  ), r = en(n, "生成镜头失败"), o = Tl(r, t);
  if (!o || o.shot.id !== e.shotId)
    throw new Error("生成镜头结果格式无效");
  return o;
}
async function Ub(e) {
  const t = await Le(je("project/asset_detail"), "get", {
    project_id: e.projectId,
    asset_id: e.assetId,
    current_only: e.currentOnly ? 1 : 0
  }), n = en(t, "读取资产详情失败"), r = n.asset;
  if (!r)
    throw new Error("资产详情为空");
  const o = ni(n.versions);
  return {
    asset: mr(r),
    versions: o,
    versionTotal: Number(n.version_total || o.length),
    hasMore: !!n.has_more
  };
}
async function Lb(e) {
  const t = await Le(je("project/asset_versions"), "get", {
    project_id: e.projectId,
    asset_id: e.assetId,
    page: e.page,
    page_size: e.pageSize || 20
  }), n = en(t, "读取资产版本失败"), r = ni(n.items);
  return {
    items: r,
    page: Number(n.page || e.page || 1),
    pageSize: Number(n.page_size || e.pageSize || 20),
    total: Number(n.total || r.length),
    hasMore: !!n.has_more
  };
}
async function Kb(e) {
  const t = await Le(
    je("project/asset_version_detail"),
    "get",
    {
      project_id: e.projectId,
      asset_id: e.assetId,
      version_id: e.versionId
    }
  ), n = en(t, "读取历史版本失败").version, r = ti(
    n && typeof n == "object" && !Array.isArray(n) ? n : {}
  );
  if (!r?.id)
    throw new Error("历史版本内容为空");
  return r;
}
function Ip(e) {
  const t = {
    project_id: e.projectId,
    asset_cate_id: e.assetCateId,
    name: e.name,
    kind: e.kind,
    content: e.content,
    request_id: e.requestId || ""
  };
  if (e.runId && (t.run_id = e.runId), e.nodeRunId && (t.node_run_id = e.nodeRunId), e.releaseId && (t.release_id = e.releaseId), e.nodeKey && (t.node_key = e.nodeKey), e.source) {
    const n = e.source;
    n.sourceKey && (t.source_key = n.sourceKey), n.sourceRunId && (t.source_run_id = n.sourceRunId), n.sourceNodeRunId && (t.source_node_run_id = n.sourceNodeRunId), n.sourceAssetId && (t.source_asset_id = n.sourceAssetId), n.sourceVersionId && (t.source_version_id = n.sourceVersionId), n.sourceReleaseId && (t.source_release_id = n.sourceReleaseId), n.sourceRequestId && (t.source_request_id = n.sourceRequestId), n.sourceNodeKey && (t.source_node_key = n.sourceNodeKey), n.sourceNodeType && (t.source_node_type = n.sourceNodeType), n.sourceStatus && (t.source_status = n.sourceStatus);
  }
  return t;
}
async function Mc(e, t) {
  const n = await Le(je("project/save_asset"), "post", {
    ...Ip(t),
    role: e
  });
  return Lr(n, "保存资产失败", "保存资产结果为空");
}
function Sp(e) {
  return Mc("work", e);
}
function vp(e) {
  return Mc("material", e);
}
async function Cp(e, t, n) {
  const r = await Le(je("workspace/canvas"), "post", {
    project_id: e,
    asset_cate_id: t,
    base_revision: n.updatedAt || "",
    canvas: xc(n)
  }), o = en(r, "保存画布失败");
  return {
    assetCateId: Number(o.asset_cate_id || t || 0),
    updatedAt: String(o.updated_at || n.updatedAt || "")
  };
}
function kp(e) {
  const t = e && typeof e == "object" ? e : {};
  return {
    ...t,
    sources: Array.isArray(t.sources) ? t.sources : [],
    params: Array.isArray(t.params) ? t.params : [],
    selected_target_id: Number(t.selected_target_id || 0),
    source_rule: Number(t.source_rule || 0),
    primary_param_key: String(t.primary_param_key || "")
  };
}
const Bi = 520, Rp = 8e3;
function xp({
  projectId: e,
  enabled: t,
  canvases: n,
  setCanvases: r,
  onError: o
}) {
  const s = Q(n), i = Q({}), a = Q({}), d = Q({}), l = Q(/* @__PURE__ */ new Set()), u = Q({}), m = Q(0), N = Q(null), [b, w] = j(0), [z, F] = j({});
  ce(() => {
    s.current = n;
  }, [n]);
  const U = $((k) => {
    const C = u.current[k];
    C != null && (window.clearTimeout(C), delete u.current[k]);
  }, []), A = $(
    (k, C = Bi) => {
      if (!t || !e || typeof window > "u")
        return;
      U(k);
      const T = m.current;
      u.current[k] = window.setTimeout(() => {
        delete u.current[k], N.current?.(k, T);
      }, C);
    },
    [U, t, e]
  ), X = $(
    async (k, C) => {
      if (C !== m.current || !t || !e || l.current.has(k))
        return;
      const T = i.current[k] || 0;
      if (T <= (a.current[k] || 0))
        return;
      const re = s.current[k];
      if (re) {
        l.current.add(k), F((ne) => ({ ...ne, [k]: "saving" }));
        try {
          const ne = await Cp(
            e,
            re.assetCateId,
            re
          );
          if (C !== m.current)
            return;
          a.current[k] = Math.max(
            a.current[k] || 0,
            T
          ), d.current[k] = 0, (i.current[k] || 0) === T ? (r(
            (ie) => ie[k] === re ? {
              ...ie,
              [k]: {
                ...re,
                updatedAt: ne.updatedAt || re.updatedAt
              }
            } : ie
          ), F((ie) => ({ ...ie, [k]: "saved" }))) : F((ie) => ({ ...ie, [k]: "dirty" }));
        } catch (ne) {
          if (C !== m.current)
            return;
          const me = (d.current[k] || 0) + 1;
          d.current[k] = me, F((ie) => ({ ...ie, [k]: "error" })), me === 1 && o(ne), A(
            k,
            Math.min(Rp, Bi * 2 ** me)
          );
        } finally {
          l.current.delete(k), C === m.current && (i.current[k] || 0) > (a.current[k] || 0) && (d.current[k] || 0) === 0 && A(k);
        }
      }
    },
    [t, o, e, A, r]
  );
  N.current = X;
  const Z = $((k) => {
    const C = String(k);
    i.current[C] = (i.current[C] || 0) + 1, d.current[C] = 0, F((T) => ({ ...T, [C]: "dirty" })), w((T) => T + 1);
  }, []), de = $(
    (k) => {
      m.current += 1;
      for (const T of Object.keys(u.current))
        U(T);
      i.current = {}, a.current = {}, d.current = {}, l.current.clear();
      const C = {};
      for (const T of Object.keys(k))
        C[T] = "saved";
      F(C);
    },
    [U]
  );
  return ce(() => {
    for (const [k, C] of Object.entries(i.current))
      C > (a.current[k] || 0) && A(k);
  }, [A, b]), ce(
    () => () => {
      m.current += 1;
      for (const k of Object.values(u.current))
        window.clearTimeout(k);
      u.current = {};
    },
    []
  ), {
    markCanvasDirty: Z,
    resetCanvasAutosave: de,
    canvasSaveStatus: z
  };
}
const Tp = 6e4, Mp = 60;
class Ap {
  scopeKey = "";
  catalogs = /* @__PURE__ */ new Map();
  powerForms = /* @__PURE__ */ new Map();
  setScope(t, n) {
    const r = Dp(t, n);
    this.scopeKey !== r && (this.scopeKey = r, this.catalogs.clear(), this.powerForms.clear());
  }
  loadCatalog(t, n, r, o = !1) {
    this.setScope(t, n);
    const s = this.scopeKey, i = this.catalogs.get(s) || { loadedAt: 0 };
    if (!o && i.value && Date.now() - i.loadedAt < Tp)
      return Promise.resolve(i.value);
    if (i.inFlight)
      return i.inFlight;
    const a = r().then((d) => (this.scopeKey === s && this.catalogs.set(s, { value: d, loadedAt: Date.now() }), d)).catch((d) => {
      throw this.scopeKey === s && this.catalogs.set(s, {
        value: i.value,
        loadedAt: i.loadedAt
      }), d;
    });
    return this.catalogs.set(s, { ...i, inFlight: a }), a;
  }
  loadPowerForm(t, n) {
    this.setScope(t.projectId, t.releaseId);
    const r = this.scopeKey, o = Pp(t), s = this.powerForms.get(o);
    if (s?.value)
      return this.touchPowerForm(o, s), Promise.resolve(s.value);
    if (s?.inFlight)
      return s.inFlight;
    const i = n().then((a) => (this.scopeKey === r && (this.touchPowerForm(o, { value: a }), this.trimPowerForms()), a)).catch((a) => {
      throw this.scopeKey === r && this.powerForms.delete(o), a;
    });
    return this.touchPowerForm(o, { inFlight: i }), this.trimPowerForms(), i;
  }
  touchPowerForm(t, n) {
    this.powerForms.delete(t), this.powerForms.set(t, n);
  }
  trimPowerForms() {
    for (; this.powerForms.size > Mp; ) {
      const t = this.powerForms.keys().next().value;
      if (!t)
        return;
      this.powerForms.delete(t);
    }
  }
}
function Dp(e, t) {
  return `${e || 0}:${t || 0}`;
}
function Pp(e) {
  return [
    e.projectId || 0,
    e.releaseId || 0,
    e.flowId || 0,
    e.powerId || 0,
    e.powerKey || "",
    e.targetId || 0
  ].join(":");
}
function zp(e, t, n) {
  const r = new Map(t.map((d) => [d.id, d])), o = Fp(n), s = /* @__PURE__ */ new Set(), i = /* @__PURE__ */ new Map();
  for (const d of t)
    d.groupId && i.set(d.groupId, [
      ...i.get(d.groupId) || [],
      d.id
    ]);
  const a = (d) => {
    for (const l of o.get(d) || []) {
      if (s.has(l))
        continue;
      s.add(l);
      const u = r.get(l);
      if (u?.type === "group")
        for (const m of i.get(u.id) || [])
          s.has(m) || (s.add(m), a(m));
      (!u || !Ac(u)) && a(l);
    }
  };
  return a(e), [...s];
}
function Ac(e) {
  return e.type === "function" && (e.functionOption?.key === "save" || e.functionOption?.key === "display");
}
function $o(e) {
  return e.type === "power" ? !!(Number(e.power?.id || 0) > 0 || e.power?.key) : ["asset", "agent", "flow"].includes(e.type) ? !0 : e.type === "function" && (e.functionOption?.key === "save" || e.functionOption?.key === "display");
}
function Fp(e) {
  const t = /* @__PURE__ */ new Map();
  for (const n of e) {
    if (!n.from || !n.to || n.executionMode === "manual")
      continue;
    const r = n.logicalFrom || n.from, o = n.logicalTo || n.to;
    t.set(r, [
      ...t.get(r) || [],
      o
    ]);
  }
  return t;
}
function Dc({
  targets: e,
  nodesByID: t,
  hasResult: n
}) {
  const r = new Set(e.map((o) => o.id));
  for (const o of e) {
    const s = o.storyboardItem;
    if (!s)
      continue;
    const i = /* @__PURE__ */ new Set([
      ...s.dependencyNodeIds || [],
      ...s.referenceNodeIds || []
    ]);
    for (const a of i) {
      if (r.has(a))
        continue;
      const d = t.get(a);
      if (!d)
        return "前置素材节点不存在，请重新同步分镜脚本";
      const l = d.title || "未命名素材";
      if (!n(d))
        return d.storyboardItem?.stale ? `请先重新生成前置素材“${l}”` : `请先生成前置素材“${l}”`;
    }
  }
  return "";
}
function Ep({
  members: e,
  runningNodes: t,
  groupState: n,
  hasResult: r
}) {
  const o = e.filter($o), s = o.filter(
    (u) => u.storyboardItem?.stale
  ).length, i = o.map((u) => t[u.id]).filter((u) => !!u), d = n?.status === "running" || n?.status === "waiting" ? o.filter((u) => {
    const m = t[u.id];
    return m?.status === "success" || !m && r(u);
  }).length : o.filter((u) => {
    const m = t[u.id];
    return m?.status === "success" ? !0 : m ? !1 : r(u);
  }).length, l = i.filter(
    (u) => u.status === "error"
  ).length;
  return {
    memberCount: e.length,
    runnableCount: o.length,
    completedCount: d,
    failedCount: l,
    staleCount: s,
    status: jp(n, i, l)
  };
}
async function Op(e, t) {
  const n = new Map(
    e.filter($o).map((s) => [s.id, s])
  ), r = /* @__PURE__ */ new Set(), o = /* @__PURE__ */ new Map();
  for (; n.size > 0 && ($p(n, o), n.size !== 0); ) {
    const s = [...n.values()].filter(
      (a) => Pc(a).every(
        (d) => !n.has(d) || r.has(d)
      )
    );
    if (s.length === 0) {
      for (const a of n.values())
        o.set(a.id, new Error("节点依赖关系存在循环"));
      n.clear();
      break;
    }
    (await Promise.allSettled(
      s.map(async (a) => (await t(a), a.id))
    )).forEach((a, d) => {
      const l = s[d];
      if (n.delete(l.id), a.status === "fulfilled") {
        r.add(l.id);
        return;
      }
      o.set(l.id, a.reason);
    });
  }
  if (o.size > 0) {
    const s = o.values().next().value, i = s instanceof Error ? s.message : "节点更新失败";
    throw new Error(`${o.size} 个节点更新失败：${i}`);
  }
}
function $p(e, t) {
  let n = !0;
  for (; n; ) {
    n = !1;
    for (const r of e.values())
      Pc(r).find(
        (s) => t.has(s)
      ) && (e.delete(r.id), t.set(r.id, new Error("上游节点更新失败")), n = !0);
  }
}
function Pc(e) {
  return e.storyboardItem?.dependencyNodeIds || [];
}
function jp(e, t, n) {
  return t.some((r) => r.status === "running") ? "running" : e?.status === "waiting" || t.some((r) => r.status === "waiting") ? "waiting" : e?.status === "error" || n > 0 ? "error" : e?.status === "running" ? "running" : "idle";
}
function Bp(e, t) {
  const [n, r] = j(e);
  return Cu(() => {
    t || r(e);
  }, [e, t]), { flowNodes: n, setFlowNodes: r };
}
function Vp({
  point: e,
  canShowDetail: t,
  canCopy: n = !0,
  canDelete: r = !0,
  canEditStructure: o = !1,
  canResetStoryboardPrompt: s = !1,
  onClose: i,
  onCopy: a,
  onDelete: d,
  onDetail: l,
  onEditStructure: u,
  onResetStoryboardPrompt: m
}) {
  return /* @__PURE__ */ v(zn, { children: [
    /* @__PURE__ */ c("div", { className: "ws-node-action-backdrop", onMouseDown: i }),
    /* @__PURE__ */ v(
      "section",
      {
        className: "ws-node-action-menu",
        style: { left: e.x, top: e.y },
        onMouseDown: (N) => N.stopPropagation(),
        children: [
          t ? /* @__PURE__ */ v("button", { type: "button", onClick: l, children: [
            /* @__PURE__ */ c(Ar, { size: 15 }),
            /* @__PURE__ */ c("span", { children: "详情" })
          ] }) : null,
          o && u ? /* @__PURE__ */ v("button", { type: "button", onClick: u, children: [
            /* @__PURE__ */ c(ac, { size: 15 }),
            /* @__PURE__ */ c("span", { children: "编辑分镜" })
          ] }) : null,
          s && m ? /* @__PURE__ */ v("button", { type: "button", onClick: m, children: [
            /* @__PURE__ */ c(ql, { size: 15 }),
            /* @__PURE__ */ c("span", { children: "恢复脚本提示词" })
          ] }) : null,
          n ? /* @__PURE__ */ v("button", { type: "button", onClick: a, children: [
            /* @__PURE__ */ c(Kl, { size: 15 }),
            /* @__PURE__ */ c("span", { children: "复制" })
          ] }) : null,
          r ? /* @__PURE__ */ v("button", { type: "button", className: "is-danger", onClick: d, children: [
            /* @__PURE__ */ c(Gl, { size: 15 }),
            /* @__PURE__ */ c("span", { children: "删除" })
          ] }) : null
        ]
      }
    )
  ] });
}
function Up({
  showMiniMap: e,
  snapToGrid: t,
  zoom: n,
  onToggleMiniMap: r,
  onToggleSnap: o,
  onReset: s,
  onZoomIn: i,
  onZoomOut: a,
  onZoomChange: d
}) {
  return /* @__PURE__ */ v("div", { className: "ws-view-controls nodrag nopan", children: [
    /* @__PURE__ */ c(We, { label: e ? "隐藏小地图" : "显示小地图", children: /* @__PURE__ */ c(
      "button",
      {
        type: "button",
        className: e ? "is-active" : "",
        onClick: r,
        "aria-label": e ? "隐藏小地图" : "显示小地图",
        children: /* @__PURE__ */ c(rf, { size: 16 })
      }
    ) }),
    /* @__PURE__ */ c(We, { label: t ? "关闭网格吸附" : "开启网格吸附", children: /* @__PURE__ */ c(
      "button",
      {
        type: "button",
        className: t ? "is-active" : "",
        onClick: o,
        "aria-label": t ? "关闭网格吸附" : "开启网格吸附",
        children: /* @__PURE__ */ c(dc, { size: 16 })
      }
    ) }),
    /* @__PURE__ */ c(We, { label: "重置视图", children: /* @__PURE__ */ c("button", { type: "button", onClick: s, "aria-label": "重置视图", children: /* @__PURE__ */ c(Ua, { size: 15 }) }) }),
    /* @__PURE__ */ v("div", { className: "ws-view-zoom", children: [
      /* @__PURE__ */ c(We, { label: "缩小", children: /* @__PURE__ */ c("button", { type: "button", onClick: a, "aria-label": "缩小", children: /* @__PURE__ */ c(La, { size: 15 }) }) }),
      /* @__PURE__ */ c(
        "input",
        {
          type: "range",
          min: "0.35",
          max: "1.45",
          step: "0.01",
          value: Math.max(0.35, Math.min(1.45, n)),
          onChange: (l) => d(Number(l.target.value)),
          "aria-label": "画布缩放"
        }
      ),
      /* @__PURE__ */ c(We, { label: "放大", children: /* @__PURE__ */ c("button", { type: "button", onClick: i, "aria-label": "放大", children: /* @__PURE__ */ c(Xs, { size: 15 }) }) })
    ] })
  ] });
}
const zc = [
  {
    position: "top-left",
    className: "is-top-left",
    horizontalDirection: -1,
    verticalDirection: -1,
    top: !0,
    left: !0
  },
  {
    position: "top-right",
    className: "is-top-right",
    horizontalDirection: 1,
    verticalDirection: -1,
    top: !0,
    left: !1
  },
  {
    position: "bottom-left",
    className: "is-bottom-left",
    horizontalDirection: -1,
    verticalDirection: 1,
    top: !1,
    left: !0
  },
  {
    position: "bottom-right",
    className: "is-bottom-right",
    horizontalDirection: 1,
    verticalDirection: 1,
    top: !1,
    left: !1
  }
], _o = 140, ri = 100, Tr = 720, Vi = { width: 280, height: 64 };
function Lp(e, t, n) {
  const r = Ec(n), o = e.find((s) => s.id === t);
  return !o || Wp(o, r) ? e : e.map(
    (s) => s.id === t ? { ...s, ...r } : s
  );
}
function Kp(e, t, n) {
  const r = Pr(n), o = e.find((s) => s.id === t);
  return !o || Yp(o.resultView, r) ? e : e.map(
    (s) => s.id === t ? { ...s, resultView: r } : s
  );
}
function qp({
  node: e,
  enabled: t,
  resizable: n,
  onResizeStart: r,
  onResizeEnd: o
}) {
  if (!t || !n || !o)
    return null;
  const s = e.type === "group", i = e.type === "power" && Zs(e.power, e.kind);
  return /* @__PURE__ */ c(zn, { children: zc.map((a) => /* @__PURE__ */ c(
    Du,
    {
      position: a.position,
      className: `ws-resize-control ws-node-resize-control ${a.className} nodrag nopan`,
      minWidth: s ? $i.width : i ? Vi.width : _o,
      minHeight: s ? $i.height : i ? Vi.height : ri,
      maxWidth: s ? ji.width : Tr,
      maxHeight: s ? ji.height : Tr,
      keepAspectRatio: !s && !i,
      onResizeStart: () => r?.(e.id),
      onResizeEnd: (d, l) => o(e.id, Ec(l))
    },
    a.position
  )) });
}
function Gp({
  value: e,
  enabled: t,
  onResizeStart: n,
  onResize: r,
  onResizeEnd: o
}) {
  const s = Q(null);
  if (!t)
    return null;
  const i = (l, u) => {
    if (l.button !== 0)
      return;
    l.preventDefault(), l.stopPropagation();
    const m = Pr(e), N = l.currentTarget.parentElement?.getBoundingClientRect().width || m.width;
    s.current = {
      pointerId: l.pointerId,
      startX: l.clientX,
      startY: l.clientY,
      startView: m,
      currentView: m,
      corner: u,
      scale: Math.max(0.01, N / m.width)
    }, l.currentTarget.setPointerCapture(l.pointerId), n?.();
  }, a = (l) => {
    const u = s.current;
    if (!u || u.pointerId !== l.pointerId)
      return;
    l.preventDefault(), l.stopPropagation();
    const m = Hp(
      u.startView,
      u.corner,
      (l.clientX - u.startX) / u.scale,
      (l.clientY - u.startY) / u.scale
    );
    u.currentView = m, r(m);
  }, d = (l) => {
    const u = s.current;
    !u || u.pointerId !== l.pointerId || (l.preventDefault(), l.stopPropagation(), s.current = null, l.currentTarget.hasPointerCapture(l.pointerId) && l.currentTarget.releasePointerCapture(l.pointerId), o(Pr(u.currentView)));
  };
  return /* @__PURE__ */ c(zn, { children: zc.map((l) => /* @__PURE__ */ c(
    "div",
    {
      className: `ws-resize-control ws-floating-resize-control ${l.className} nodrag nopan nowheel`,
      onPointerDown: (u) => i(u, l),
      onPointerMove: a,
      onPointerUp: d,
      onPointerCancel: d,
      onClick: (u) => {
        u.preventDefault(), u.stopPropagation();
      }
    },
    l.position
  )) });
}
function Hp(e, t, n, r) {
  const o = e.width / e.height, s = e.width + t.horizontalDirection * n, i = (e.height + t.verticalDirection * r) * o, a = Fc(
    Math.abs(s - e.width) >= Math.abs(i - e.width) ? s : i,
    o
  ), d = a / o, l = Number(e.offsetX || 0), u = Number(e.offsetY || 0);
  return Pr({
    width: a,
    height: d,
    offsetX: t.left ? l + e.width - a : l,
    offsetY: t.top ? u + (e.height - d) / 2 : u + (d - e.height) / 2
  });
}
function Fc(e, t) {
  const n = Math.max(_o, ri * t), r = Math.min(Tr, Tr * t);
  return n > r ? Math.min(Tr, Math.max(_o, e)) : Math.min(r, Math.max(n, e));
}
function Pr(e) {
  const t = Ui(e.width, _o), n = Ui(e.height, ri), r = t / n, o = Fc(t, r);
  return {
    width: Math.round(o),
    height: Math.round(o / r),
    offsetX: Math.round(Li(e.offsetX, 0)),
    offsetY: Math.round(Li(e.offsetY, 0))
  };
}
function Ui(e, t) {
  const n = Number(e);
  return Number.isFinite(n) && n > 0 ? n : t;
}
function Li(e, t) {
  const n = Number(e);
  return Number.isFinite(n) ? n : t;
}
function Ec(e) {
  return {
    x: Math.round(e.x),
    y: Math.round(e.y),
    width: Math.round(e.width),
    height: Math.round(e.height)
  };
}
function Wp(e, t) {
  return e.x === t.x && e.y === t.y && e.width === t.width && e.height === t.height;
}
function Yp(e, t) {
  return e?.width === t.width && e?.height === t.height && Number(e?.offsetX || 0) === Number(t.offsetX || 0) && Number(e?.offsetY || 0) === Number(t.offsetY || 0);
}
function Xp(e, t) {
  const n = e || Oc(), r = rl({
    type: "stream",
    output: t
  }), o = r.event, s = r.activity, i = em(n.text, r.delta, t, o), a = tm(o, s) ? {
    ...n.output,
    ...r.output,
    ...i ? { text: i } : {}
  } : n.output, d = s && !s.anchorText ? { ...s, anchorText: i } : s, l = d ? ol(n.activities, d) : n.activities, u = sl(
    il(n.document, r.output),
    Ga(r.output.document)
  ), m = qa(a) || n.interaction, N = Ka(a);
  return {
    started: !0,
    text: i,
    output: a,
    activities: l,
    document: u,
    interaction: m,
    suggestions: N.length > 0 ? N : n.suggestions,
    error: r.error || n.error
  };
}
function Zp(e) {
  const t = Jp(e);
  return {
    started: Object.keys(t).length > 0,
    text: No(t.text),
    output: t,
    activities: tl(t),
    document: Ga(t.document),
    interaction: qa(t),
    suggestions: Ka(t),
    error: No(t.error)
  };
}
function Jp(e) {
  const t = nl(e);
  if (No(t.text))
    return t;
  const n = Ja(e);
  if (!n)
    return t;
  const r = { ...t, text: n };
  return delete r.rich, r;
}
function Qp(e) {
  return !!(e && (e.started || e.text || e.activities.length > 0 || e.document || e.interaction || e.suggestions.length > 0 || Object.keys(e.output).length > 0));
}
function Oc() {
  return {
    started: !1,
    text: "",
    output: {},
    activities: [],
    suggestions: [],
    error: ""
  };
}
function em(e, t, n, r) {
  return t ? `${e}${t}` : r === "final" && No(n.text) || e;
}
function tm(e, t) {
  return !(t || e === "start" || e === "delta");
}
function No(e) {
  return e == null ? "" : String(e);
}
function nm(e) {
  const t = Se(e.result?.asset, e.result?.data?.asset);
  if (!t || Number(t.id || 0) <= 0 || !t.version?.id)
    return null;
  const r = e.previousAssets?.find(
    (i) => i.id === Number(t.id || 0)
  ), o = r ? An(r, e.previousAsset) : e.previousAsset || null, s = An(
    t,
    o
  );
  return o?.version?.content != null && Number(o.version.id || 0) === Number(s.version?.id || 0) ? An(o, s) : s.version?.id ? s : null;
}
function rm(e, t) {
  const n = sm(e);
  return t ? {
    ...n,
    asset: t
  } : n;
}
function om(e, t = "") {
  return String(e.kind || e.power?.kind || t || "richtext");
}
function An(e, t) {
  const n = qi(e), r = t ? qi(t) : null, o = [
    ...n.versions || [],
    ...r?.versions || []
  ], s = im(
    [cm(n.version)].filter(
      (i) => !!i
    ),
    o
  );
  return {
    ...r,
    ...n,
    version: n.version || r?.version,
    versions: s.length ? s : void 0
  };
}
function Ki(e, t) {
  if (t.length === 0)
    return e;
  const n = new Map(t.map((o) => [o.id, o])), r = e.map((o) => {
    const s = n.get(o.id);
    return s ? (n.delete(o.id), An(s, o)) : o;
  });
  return [
    ...[...n.values()].map(
      (o) => An(o)
    ),
    ...r
  ];
}
function sm(e) {
  if (!e || typeof e != "object" || Array.isArray(e))
    return e;
  const { version: t, ...n } = e;
  return n;
}
function qi(e) {
  const t = Gi(e.version), n = (e.versions || []).map(Gi).filter((r) => !!r);
  return {
    ...e,
    version: t,
    versions: n.length ? n : e.versions
  };
}
function Gi(e) {
  if (!e)
    return;
  const t = bl(e.content);
  return t ? {
    ...e,
    content: { rich: t }
  } : e;
}
function im(...e) {
  const t = e.flat(), n = [], r = /* @__PURE__ */ new Map();
  for (const s of t) {
    if (!s || Number(s.id || 0) <= 0)
      continue;
    const i = String(s.id), a = r.get(i);
    if (a !== void 0) {
      n[a] = am(
        n[a],
        s
      );
      continue;
    }
    r.set(i, n.length), n.push(s);
  }
  let o = !1;
  return n.sort(
    (s, i) => Number(ts(i)) - Number(ts(s)) || Number(i.version || i.id || 0) - Number(s.version || s.id || 0)
  ).map((s) => ts(s) ? o ? dm(s) : (o = !0, s) : s);
}
function am(e, t) {
  const n = { ...e };
  for (const [r, o] of Object.entries(t))
    o !== void 0 && o !== "" && (n[r] = o);
  return n;
}
function ts(e) {
  return !!(e.is_current || e.current);
}
function cm(e) {
  return e ? { ...e, current: !0 } : void 0;
}
function dm(e) {
  const { current: t, is_current: n, ...r } = e;
  return r;
}
function Io(e) {
  const t = e?.asset || e?.data?.asset, n = e?.version || t?.version || e?.data?.version, r = {};
  return Un(r, "execution_id", e?.execution_id), Un(r, "run_id", e?.run_id || n?.run_id), ns(r, "request_id", e?.request_id), Un(r, "flow_run_id", e?.flow_run_id), Un(
    r,
    "node_run_id",
    e?.node_run_id || n?.node_run_id
  ), Un(r, "asset_id", t?.id), Un(r, "version_id", n?.id || t?.version_id), Un(
    r,
    "release_id",
    n?.release_id || e?.release_id
  ), ns(r, "role", e?.role || t?.role), ns(r, "status", e?.status), Object.keys(r).length > 0 && (r.updated_at = (/* @__PURE__ */ new Date()).toISOString()), Object.keys(r).length > 0 ? r : void 0;
}
function um(e) {
  const t = e?.resultRef;
  if (!e || !t)
    return null;
  const n = Number(t.asset_id || 0), r = String(t.node_run_id ? e.nodeId : "").trim();
  return {
    sourceRunId: Number(t.run_id || 0),
    sourceNodeRunId: Number(t.node_run_id || 0),
    sourceAssetId: n,
    sourceVersionId: Number(t.version_id || 0),
    sourceReleaseId: Number(t.release_id || 0),
    sourceRequestId: String(t.request_id || ""),
    sourceNodeKey: r,
    sourceNodeType: String(e.type || ""),
    sourceStatus: String(t.status || ""),
    sourceKey: r || (n > 0 ? `asset:${n}` : "") || ""
  };
}
function Un(e, t, n) {
  const r = Number(n || 0);
  r > 0 && (e[t] = r);
}
function ns(e, t, n) {
  typeof n == "string" && n.trim() && (e[t] = n.trim());
}
const lm = [
  { key: "all", label: "全部" },
  { key: "text", label: "文本" },
  { key: "richtext", label: "富文本" },
  { key: "image", label: "图片" },
  { key: "audio", label: "音频" },
  { key: "video", label: "视频" },
  { key: "storyboard", label: "分镜" },
  { key: "agent", label: "智能体" },
  { key: "flow", label: "流程" }
], fm = Object.fromEntries(
  lm.filter((e) => e.key !== "all").map((e) => [e.key, e.label])
);
function pm(e) {
  return fm[e] || "文本";
}
function mm(e) {
  const t = new Map(e.nodes.map((i) => [i.id, i])), n = new Map(
    e.nodes.filter((i) => i.type === "group").map((i) => [i.id, i])
  ), r = gm(
    e.assets,
    e.assetCateId
  ), o = e.nodes.filter(wc).map((i) => {
    const a = r.get(i.id) || i.asset, d = i.groupId ? n.get(i.groupId) : void 0, l = d?.group?.sourceNodeId ? t.get(d.group.sourceNodeId) : void 0, u = e.nodeOutput(i);
    return {
      key: `node:${i.id}`,
      role: "material",
      title: i.title || pm(Hi(i)),
      sourcePath: l?.title && d?.title ? `${l.title} / ${d.title}` : d?.title,
      nodeType: Hi(i),
      status: ym(
        i,
        a,
        e.nodeHasResult(i)
      ),
      preview: e.nodePreview(i),
      output: u,
      node: i,
      nodeId: i.id,
      nodeNo: i.nodeNo,
      groupId: i.groupId,
      groupTitle: d?.title,
      asset: a,
      assetId: a?.id,
      versionId: a?.version?.id || a?.version_id
    };
  }).filter((i) => !!(i.assetId && i.versionId));
  return [...e.assets.filter(
    (i) => Number(i.asset_cate_id || 0) === e.assetCateId && String(i.role || "material") === "work" && String(i.status || "") !== "archived"
  ).map(
    (i) => ({
      key: `asset:${i.id}`,
      role: "work",
      title: i.name || `作品 ${i.id}`,
      nodeType: $c(i.kind),
      status: i.version?.id || i.version_id ? "ready" : "empty",
      preview: e.assetPreview(i),
      output: i.version?.content,
      asset: i,
      assetId: i.id,
      versionId: i.version?.id || i.version_id
    })
  ), ...o].filter(
    (i) => !!(i.assetId && i.versionId)
  );
}
function Hi(e) {
  if (e.type === "agent") return "agent";
  if (e.type === "flow") return "flow";
  const t = String(
    e.outputType || e.power?.outputType || e.power?.output?.key || ""
  ).toLowerCase(), n = String(e.power?.output?.viewMode || "").toLowerCase();
  return t === "storyboard" || n === "storyboard" ? "storyboard" : $c(e.power?.kind || e.kind);
}
function $c(e) {
  switch (String(e || "text").toLowerCase()) {
    case "rich":
    case "richtext":
      return "richtext";
    case "image":
      return "image";
    case "audio":
      return "audio";
    case "video":
      return "video";
    default:
      return "text";
  }
}
function gm(e, t) {
  const n = /* @__PURE__ */ new Map();
  for (const r of e) {
    if (Number(r.asset_cate_id || 0) !== t || String(r.role || "") !== "material" || String(r.status || "") === "archived")
      continue;
    const o = String(
      r.node_key || r.version?.node_key || ""
    ).trim();
    o && !n.has(o) && n.set(o, r);
  }
  return n;
}
function ym(e, t, n) {
  const r = String(
    e.running?.status || e.resultRef?.status || ""
  ).toLowerCase();
  return r === "running" || r === "waiting" || e.running === !0 ? "running" : r === "error" || r === "failed" || r === "failure" ? "failed" : n || (t?.version?.id || t?.version_id) ? "ready" : "empty";
}
function br(e) {
  const t = e?.output && typeof e.output == "object" ? e.output : {}, n = e?.run && typeof e.run == "object" ? e.run : {};
  return {
    execution_id: Number(e?.execution_id || 0),
    run_id: Number(e?.run_id || n.id || 0),
    request_id: String(e?.request_id || n.request_id || ""),
    asset_cate_id: Number(e?.asset_cate_id || 0),
    start_node_id: String(e?.start_node_id || ""),
    flow_run_id: Number(e?.flow_run_id || n.flow_run_id || 0),
    release_id: Number(e?.release_id || n.release_id || 0),
    status: Vr(e?.status || n.status),
    error: Fn(e?.error || n.error),
    executed: Number(e?.executed || e?.output?.executed || 0),
    total: Number(e?.total || e?.output?.total || 0),
    single_node: !!e?.single_node,
    created_at: String(e?.created_at || ""),
    updated_at: String(e?.updated_at || ""),
    title: String(e?.title || ""),
    output: e?.output || n.output,
    approvals: Array.isArray(e?.approvals) ? e.approvals : Array.isArray(e?.data?.approvals) ? e.data.approvals : [],
    interactions: Array.isArray(e?.interactions) ? e.interactions : Array.isArray(e?.data?.interactions) ? e.data.interactions : [],
    node_results: jc(
      e?.node_results || t.node_results
    ),
    pending_node: oi(
      e?.pending_node || t.pending_node
    ),
    execution_plan: Nm(e?.execution_plan),
    node_runs: Array.isArray(e?.node_runs) ? e.node_runs.map(vm).filter((r) => !!r) : []
  };
}
function oi(e) {
  if (!e || typeof e != "object")
    return null;
  const t = String(e.node_key || "");
  return t ? {
    node_key: t,
    execution_id: Number(e.execution_id || 0),
    node_type: String(e.node_type || ""),
    node_run_id: Number(e.node_run_id || 0),
    run_id: Number(e.run_id || 0),
    request_id: String(e.request_id || ""),
    child_run_id: Number(e.child_run_id || 0),
    child_request_id: String(e.child_request_id || ""),
    status: Vr(e.status),
    error: Fn(e.error),
    output: e.output,
    asset: e.asset,
    version: e.version,
    result: e.result,
    approval: e.approval,
    interaction: e.interaction,
    persists_result: !!e.persists_result,
    agent_run_id: Number(e.agent_run_id || 0),
    source_signature: String(e.source_signature || "")
  } : null;
}
function jc(e) {
  return Array.isArray(e) ? e.map(oi).filter((t) => !!t) : [];
}
function hm(e, t = "") {
  if (!e || typeof e != "object")
    return null;
  const n = String(t || "").trim(), r = jc(e.node_results), o = n ? r.find((s) => s.node_key === n) : r[0];
  return o || oi(
    n && !String(e.node_key || "").trim() ? { ...e, node_key: n } : e
  );
}
function si(e) {
  return e ? Lc(
    e.error,
    e.result?.error,
    e.output?.error
  ) : "";
}
function Bc(e) {
  if (!e)
    return "";
  const t = [...e.node_results || []].reverse().find((n) => Vr(
    n.status || n.result?.status
  ) === "fail");
  return Lc(
    si(t),
    e.output?.error,
    e.error
  );
}
function wm(e, t = "节点运行失败") {
  return ii(
    si(e),
    t
  );
}
function Vc(e, t = "画布运行失败") {
  return ii(Bc(e), t);
}
function ii(e, t = "运行失败") {
  const n = Fn(e);
  return n ? n.includes("InputImageSensitiveContentDetected") || n.includes("PrivacyInformation") ? "参考图片可能包含真人或隐私信息，请更换参考图后重试。" : n.includes("资产当前版本已变化") ? "引用的资产版本已变化，请刷新画布后重试。" : n.length > 500 ? `${n.slice(0, 497)}...` : n : t;
}
function bm(...e) {
  for (const t of e) {
    const n = Fn(t);
    if (n)
      return n;
  }
  return "";
}
const Uc = /* @__PURE__ */ new Set([
  "画布运行失败",
  "节点运行失败",
  "节点执行失败",
  "运行失败",
  "执行出错"
]);
function _m(e) {
  return Uc.has(Fn(e));
}
function Lc(...e) {
  let t = "";
  for (const n of e) {
    const r = Fn(n);
    if (r && (t ||= r, !Uc.has(r)))
      return r;
  }
  return t;
}
function Fn(e) {
  if (typeof e == "string")
    return e.trim();
  if (e instanceof Error)
    return e.message.trim();
  if (!e || typeof e != "object" || Array.isArray(e))
    return "";
  const t = e, n = bm(t.error, t.message, t.msg);
  if (n)
    return n;
  const r = Fn(t.code), o = Fn(t.detail);
  return [r, o].filter(Boolean).join(": ");
}
function Nm(e) {
  if (!e || typeof e != "object")
    return null;
  const t = Array.isArray(e.nodes) ? e.nodes.map(Im).filter(
    (r) => !!r
  ) : [], n = Array.isArray(e.edges) ? e.edges.map(Sm).filter(
    (r) => !!r
  ) : [];
  return {
    nodes: t,
    edges: n,
    incoming: Wi(e.incoming),
    outgoing: Wi(e.outgoing),
    order: Array.isArray(e.order) ? e.order.map((r) => String(r || "")).filter(Boolean) : t.map((r) => r.id)
  };
}
function Im(e) {
  const t = String(e?.id || "");
  return t ? {
    id: t,
    type: String(e?.type || ""),
    title: String(e?.title || ""),
    kind: String(e?.kind || ""),
    output_type: String(e?.output_type || ""),
    group_id: String(e?.group_id || ""),
    function_key: String(e?.function_key || ""),
    asset_cate_id: Number(e?.asset_cate_id || 0),
    persists_result: !!e?.persists_result,
    stops_flow: !!e?.stops_flow
  } : null;
}
function Sm(e) {
  const t = String(e?.source || ""), n = String(e?.target || "");
  return !t || !n ? null : {
    id: String(e?.id || `${t}-${n}`),
    source: t,
    target: n
  };
}
function Wi(e) {
  const t = /* @__PURE__ */ new Map();
  if (!e || typeof e != "object" || Array.isArray(e))
    return t;
  for (const [n, r] of Object.entries(e)) {
    const o = Array.isArray(r) ? r.map((s) => String(s || "")).filter(Boolean) : [];
    t.set(String(n), o);
  }
  return t;
}
function vm(e) {
  const t = String(e?.node_key || ""), n = Number(e?.node_run_id || 0);
  return !t || n <= 0 ? null : {
    node_run_id: n,
    node_id: Number(e?.node_id || 0),
    node_key: t,
    node_type: String(e?.node_type || ""),
    status: Vr(e?.status),
    persists_result: !!e?.persists_result
  };
}
async function Kc(e) {
  const t = String(e.requestId || "").trim();
  if (!t)
    throw new Error("request_id 不能为空");
  return Pu({
    streamApi: Cm(e.projectId),
    requestID: t,
    lastID: e.lastId || "0-0",
    blockMs: 15e3,
    signal: e.signal,
    acceptErrorResult: !0,
    initialState: null,
    reduceFrame: (n, r) => (e.onFrame(r), n)
  });
}
function Cm(e) {
  const t = new URL(je("run/stream"), window.location.origin);
  return t.searchParams.set("project_id", String(e || 0)), t.toString();
}
const qc = "反馈已被新的运行替换";
function km(e) {
  return e instanceof Error && e.message === qc;
}
function Wn(e) {
  return (Array.isArray(e?.feedbackRequests) ? e.feedbackRequests : []).filter((n) => n && n.id);
}
function Rm(e, t) {
  const n = Number(t.approval?.id || 0);
  return {
    id: `${e.id}:${n || Date.now()}:${Math.random().toString(36).slice(2, 7)}`,
    nodeId: e.id,
    title: t.title || e.title || "补充信息",
    description: t.description || "",
    prompt: t,
    status: "pending",
    createdAt: (/* @__PURE__ */ new Date()).toISOString()
  };
}
function xm(e, t, n) {
  return e.map(
    (r) => r.id === t ? {
      ...r,
      values: n,
      prompt: {
        ...r.prompt,
        values: n
      },
      status: "submitted",
      submittedAt: (/* @__PURE__ */ new Date()).toISOString()
    } : r
  );
}
function Tm(e, t, n) {
  if (!e)
    return !1;
  const r = t.find((s) => s.id === e.node.id) || e.node, o = Wn(r).find(
    (s) => s.id === e.recordId
  );
  return o ? !(o.status === "pending" && n?.nodeId === e.node.id && n.recordId === e.recordId) : !1;
}
function Mm(e) {
  const t = e?.run || e?.data?.run || e || {}, n = Array.isArray(e?.approvals) ? e.approvals : Array.isArray(e?.data?.approvals) ? e.data.approvals : [], r = Array.isArray(e?.interactions) ? e.interactions : Array.isArray(e?.data?.interactions) ? e.data.interactions : [];
  return {
    runId: Number(t?.id || e?.run_id || 0),
    requestId: String(t?.request_id || e?.request_id || ""),
    status: Vr(t?.status || e?.status || "running"),
    output: Se(t?.output, e?.output, e?.data?.output),
    error: String(t?.error || e?.error || ""),
    approvals: n.map(zm).filter(Boolean),
    interactions: r.map(Dm).filter(Boolean),
    raw: e
  };
}
function Am(e) {
  const t = e.interactions.find(
    (s) => !!(s.interaction?.id && s.interaction?.type)
  );
  if (t)
    return Gc(t);
  const n = e.approvals.find(Fm);
  if (!n?.id)
    return null;
  const r = Em(n), o = ai(r);
  return {
    approval: n,
    title: $e(r.title, n.title, "补充信息"),
    description: $e(
      r.description,
      "补充信息后继续执行流程。"
    ),
    fields: o,
    values: ci(r, o)
  };
}
function Gc(e) {
  const t = e.interaction, n = ai(t);
  return {
    approval: {
      id: 0,
      title: String(t.title || ""),
      status: "pending",
      decision: "pending",
      content: {}
    },
    interaction: e,
    title: $e(t.title, "补充信息"),
    description: $e(
      t.description,
      "补充信息后继续执行流程。"
    ),
    fields: n,
    values: ci(t, n)
  };
}
function Dm(e) {
  const t = zu(e);
  return {
    runId: t.runId,
    nodeRunId: t.nodeRunId,
    interaction: t.interaction
  };
}
function ai(e) {
  return (Array.isArray(e.fields) ? e.fields : Array.isArray(e.params) ? e.params : []).map((n, r) => Om(n, r)).filter((n) => !!n.key);
}
function ci(e, t) {
  const n = Gs(t), r = e.values && typeof e.values == "object" ? e.values : {}, o = {
    ...n,
    ...r
  }, s = Number(
    e.source_target_id || e.sourceTargetId || 0
  );
  return s > 0 && (o.source_target_id = s), o;
}
function Pm(e, t) {
  const n = $m(e);
  if (!n)
    return null;
  const r = ai(n);
  return {
    approval: {
      id: 0,
      title: t,
      status: "pending",
      decision: "pending",
      content: { kind: "agent_interaction", interaction: n }
    },
    title: $e(n.title, t, "补充信息"),
    description: $e(
      n.description,
      "补充信息后继续执行智能体。"
    ),
    fields: r,
    values: ci(n, r)
  };
}
function zm(e) {
  const t = e?.content && typeof e.content == "object" ? e.content : {};
  return {
    id: Number(e?.id || e?.approval_id || 0),
    title: String(e?.title || ""),
    status: String(e?.status || ""),
    decision: String(e?.decision || ""),
    content: t
  };
}
function Fm(e) {
  return e.status === "pending" || e.decision === "pending";
}
function Em(e) {
  const t = e.content || {}, n = t.interaction;
  return n && typeof n == "object" ? n : t;
}
function Om(e, t) {
  const n = Array.isArray(e?.options) ? e.options : [];
  return {
    id: Number(e?.id || t + 1),
    power_param_id: Number(e?.power_param_id || e?.powerParamId || 0),
    name: String(e?.name || e?.label || e?.title || e?.key || ""),
    key: String(e?.key || e?.name || `field_${t + 1}`),
    icon: String(e?.icon || ""),
    type: String(e?.type || "input"),
    preview_type: String(e?.preview_type || e?.previewType || "none"),
    usage: Number(e?.usage || 1),
    value_type: e?.value_type || e?.valueType || "string",
    default_value: String(e?.default_value ?? e?.defaultValue ?? ""),
    active_when_key: String(
      e?.active_when_key ?? e?.activeWhenKey ?? ""
    ),
    active_when_value: String(
      e?.active_when_value ?? e?.activeWhenValue ?? ""
    ),
    upload_rule_id: Number(
      e?.upload_rule_id || e?.uploadRuleId || 0
    ),
    max_files: Number(e?.max_files ?? e?.maxFiles ?? 0),
    accepted_kinds: Yi(
      e?.accepted_kinds ?? e?.acceptedKinds
    ),
    asset_kinds: Yi(
      e?.asset_kinds ?? e?.assetKinds
    ),
    required: !!e?.required,
    sort: Number(e?.sort || t + 1),
    options: n.map((r, o) => ({
      id: Number(r?.id || o + 1),
      name: String(r?.name || r?.label || r?.value || ""),
      value: String(
        r?.value || r?.id || r?.label || r?.name || ""
      ),
      native_value: String(r?.native_value || r?.nativeValue || ""),
      preview_url: String(r?.preview_url || r?.previewUrl || ""),
      sort: Number(r?.sort || o + 1)
    }))
  };
}
function Yi(e) {
  return Array.isArray(e) ? e.map((t) => String(t || "").trim()).filter(Boolean) : [];
}
function $m(e) {
  const t = rs(
    e?.output,
    e?.data?.output,
    e?.content,
    e
  );
  if (!t)
    return null;
  if (!String(t.event || "").toLowerCase().includes("interaction")) {
    const r = rs(e?.interaction);
    return r && $e(r.type) ? r : null;
  }
  const n = rs(t.interaction, t.content?.interaction);
  return n && $e(n.type) ? n : null;
}
function rs(...e) {
  for (const t of e)
    if (t && typeof t == "object" && !Array.isArray(t))
      return t;
  return null;
}
async function Hc(e) {
  return (await _l({
    teamID: e.teamID,
    projectID: e.projectID,
    files: e.files,
    ruleID: e.ruleID
  })).map(
    ({ sourceFile: n, uploadedFile: r, asset: o }) => Bm(r, n, o)
  );
}
function jm(e) {
  const t = String(e.type || "").toLowerCase();
  return t.startsWith("image/") ? "image" : t.startsWith("video/") ? "video" : t.startsWith("audio/") ? "audio" : "file";
}
function Bm(e, t, n) {
  const r = String(
    e?.url || e?.open_url || e?.download || ""
  ), o = String(e?.kind || jm(t));
  return {
    name: String(e?.name || t.name),
    alias: String(e?.name || t.name),
    kind: o,
    source: "upload",
    type: String(e?.mime || t.type || o),
    url: r,
    text: String(e?.name || t.name),
    output: e,
    asset: n
  };
}
function Vm({
  id: e,
  sourceX: t,
  sourceY: n,
  targetX: r,
  targetY: o,
  sourcePosition: s,
  targetPosition: i,
  markerEnd: a,
  style: d,
  data: l
}) {
  const [u, m, N] = Fu({
    sourceX: t,
    sourceY: n,
    sourcePosition: s,
    targetX: r,
    targetY: o,
    targetPosition: i
  }), b = l || {}, w = !!b.isSelected, z = !!(b.isHighlighted || w), F = b.highlightColor || "#0ea5e9";
  return /* @__PURE__ */ v(zn, { children: [
    z ? /* @__PURE__ */ c(
      Ai,
      {
        path: u,
        style: {
          stroke: F,
          strokeWidth: 7,
          opacity: 0.12
        }
      }
    ) : null,
    /* @__PURE__ */ c(
      Ai,
      {
        path: u,
        markerEnd: a,
        style: {
          ...d,
          stroke: w ? "var(--ws-edge-selected)" : z ? F : "var(--ws-edge)",
          strokeWidth: w ? 2.8 : z ? 2.4 : 1.45,
          opacity: z ? 0.96 : 0.62,
          transition: "stroke 160ms ease, stroke-width 160ms ease, opacity 160ms ease"
        }
      }
    ),
    w ? /* @__PURE__ */ c(Eu, { children: /* @__PURE__ */ c(
      "button",
      {
        type: "button",
        className: "ws-edge-delete nodrag nopan",
        style: {
          transform: `translate(-50%, -50%) translate(${m}px, ${N}px)`
        },
        "aria-label": "删除连线",
        onMouseDown: (Z) => {
          Z.preventDefault(), Z.stopPropagation();
        },
        onClick: (Z) => {
          Z.preventDefault(), Z.stopPropagation(), b.onDelete?.(String(e));
        },
        children: /* @__PURE__ */ c(Lu, { size: 15 })
      }
    ) }) : null,
    z ? /* @__PURE__ */ v(zn, { children: [
      /* @__PURE__ */ c("circle", { r: "3", fill: F, children: /* @__PURE__ */ c("animateMotion", { dur: "2.8s", repeatCount: "indefinite", path: u }) }),
      /* @__PURE__ */ c("circle", { r: "1.8", fill: "rgba(255, 255, 255, 0.92)", children: /* @__PURE__ */ c("animateMotion", { dur: "2.8s", repeatCount: "indefinite", path: u }) })
    ] }) : null
  ] });
}
function os({
  title: e,
  className: t,
  fallback: n = "未命名节点",
  onRename: r
}) {
  const [o, s] = j(!1), [i, a] = j(e), d = Q(null);
  ce(() => {
    o || a(e);
  }, [o, e]), ce(() => {
    o && (d.current?.focus(), d.current?.select());
  }, [o]);
  const l = () => {
    const u = i.trim() || n;
    s(!1), a(u), u !== e && r?.(u);
  };
  return o && r ? /* @__PURE__ */ c(
    "input",
    {
      ref: d,
      className: `ws-canvas-node-title-input nodrag nowheel ${t || ""}`.trim(),
      value: i,
      maxLength: 64,
      "aria-label": "节点名称",
      onChange: (u) => a(u.target.value),
      onBlur: l,
      onPointerDown: (u) => u.stopPropagation(),
      onKeyDown: (u) => {
        u.stopPropagation(), u.key === "Enter" ? (u.preventDefault(), l()) : u.key === "Escape" && (u.preventDefault(), a(e), s(!1));
      }
    }
  ) : /* @__PURE__ */ c(We, { label: r ? "双击重命名" : e, children: /* @__PURE__ */ c(
    "span",
    {
      className: t,
      onDoubleClick: r ? (u) => {
        u.preventDefault(), u.stopPropagation(), s(!0);
      } : void 0,
      children: e || n
    }
  ) });
}
const Xi = 160, Zi = 72, Ji = 72, So = 72, zr = 24, Fr = 24, Er = 40, Yn = 2, vo = { width: 180, height: 180 }, Um = "storyboard-derived-layout-v7";
function Lm(e) {
  const t = e.groups.map((w) => ({
    ...w,
    size: Hm(w)
  })), n = /* @__PURE__ */ new Map(), r = [...t].sort(oo), o = Wm(
    "workspace",
    r
  ), s = t.filter((w) => w.direction === "upstream").sort(oo), i = t.filter(
    (w) => w.direction === "downstream" && w.powerKind !== "audio"
  ).sort(oo), a = t.filter(
    (w) => w.direction === "downstream" && w.powerKind === "audio"
  ).sort(oo), d = i.reduce(
    (w, z) => Math.max(w, z.size.height),
    0
  ), l = i.length ? e.sourceNode.y - d - Ji : e.sourceNode.y, u = e.sourceNode.x - Xi;
  let m = l;
  for (const w of s)
    n.set(w.key, {
      bounds: {
        x: u - w.size.width,
        y: m,
        width: w.size.width,
        height: w.size.height
      },
      layoutKey: `${o}:${w.key}`
    }), m += w.size.height + Ji;
  let N = e.sourceNode.x;
  for (const w of i)
    n.set(w.key, {
      bounds: {
        x: N,
        y: l,
        width: w.size.width,
        height: w.size.height
      },
      layoutKey: `${o}:${w.key}`
    }), N += w.size.width + Zi;
  let b = e.sourceNode.x + e.sourceNode.width + Xi;
  for (const w of a)
    n.set(w.key, {
      bounds: {
        x: b,
        y: e.sourceNode.y,
        width: w.size.width,
        height: w.size.height
      },
      layoutKey: `${o}:${w.key}`
    }), b += w.size.width + Zi;
  return n;
}
function Km(e, t, n) {
  const r = t.filter((a) => a.groupId === e.id), o = n.kind === "audio", s = o ? n.width : Math.max(vo.width, n.width), i = o ? n.height : Math.max(vo.height, n.height);
  for (let a = 0; a < r.length + 100; a += 1) {
    const d = a % Yn, l = Math.floor(a / Yn), u = {
      x: e.x + zr + d * (s + Fr),
      y: e.y + So + l * (i + Er)
    };
    if (r.every(
      (m) => !Ym(
        { ...u, width: n.width, height: n.height },
        m
      )
    ))
      return u;
  }
  return {
    x: e.x + zr,
    y: e.y + So
  };
}
function qm(e, t) {
  const n = t.some((s) => s.kind === "audio"), r = {
    width: Math.max(
      n ? 0 : vo.width,
      ...t.map((s) => s.width)
    ),
    height: Math.max(
      n ? 0 : vo.height,
      ...t.map((s) => s.height)
    )
  }, o = /* @__PURE__ */ new Map();
  return t.forEach((s, i) => {
    const a = i % Yn, d = Math.floor(i / Yn);
    o.set(s.id, {
      x: e.x + zr + a * (r.width + Fr),
      y: e.y + So + d * (r.height + Er)
    });
  }), {
    ...Wc(t.length, r),
    positions: o
  };
}
function Gm(e) {
  return Oo(e || void 0);
}
function Hm(e) {
  const t = Gm(
    e.power || { kind: e.powerKind, outputType: "" }
  );
  return Wc(e.itemCount, t);
}
function Wc(e, t) {
  const n = Math.max(1, Math.ceil(e / Yn));
  return {
    width: zr * 2 + t.width * Yn + Fr * (Yn - 1),
    height: So + zr + n * t.height + (n - 1) * Er
  };
}
function Wm(e, t) {
  return [
    Um,
    e,
    ...t.map(
      (n) => `${n.key}:${n.itemCount}:${n.size.width}x${n.size.height}`
    )
  ].join("|");
}
function oo(e, t) {
  return e.layoutIndex - t.layoutIndex || e.key.localeCompare(t.key);
}
function Ym(e, t) {
  return !(e.x + e.width + Fr <= t.x || t.x + t.width + Fr <= e.x || e.y + e.height + Er <= t.y || t.y + t.height + Er <= e.y);
}
const Xm = [
  "characters",
  "scenes",
  "props"
], Qi = {
  character: "生成纯角色设定图，只展示当前角色的正面、侧面、背面和关键细节，不得出现其他人物、文字、水印或界面元素",
  scene: "生成纯场景设定图，只展示固定空间的外观、内部结构、光线和关键区域，不得出现任何人物、角色、动物、文字、水印或界面元素",
  prop: "生成纯道具设定图，只展示当前道具的多角度造型、比例、材质和关键细节，不得出现人物、手持者、文字、水印或界面元素"
}, Cs = [
  ss("characters", "角色组", "character", 0),
  ss("scenes", "场景组", "scene", 1),
  ss("props", "道具组", "prop", 2),
  {
    key: "shot_images",
    title: "镜头参考图组",
    itemType: "shot_image",
    powerKind: "image",
    outputType: "general",
    direction: "downstream",
    sourceGroupKeys: Xm,
    layoutIndex: 0,
    enabled: nc,
    items: (e) => e.shots.flatMap((t, n) => {
      if (t.continue_previous)
        return [];
      const r = og(e, t, n);
      return [
        {
          type: "shot_image",
          id: t.id,
          title: `镜头 ${t.order || n + 1} 参考图`,
          prompt: ag(
            e,
            t,
            r.previousShot,
            r.externalReferences
          ),
          dependencyItems: r.dependencyItems,
          referenceItems: r.referenceItems,
          externalReferences: r.externalReferences,
          paramValues: mg(e),
          shotId: t.id
        }
      ];
    })
  },
  {
    key: "shots",
    title: "镜头视频组",
    itemType: "shot",
    powerKind: "video",
    outputType: "general",
    direction: "downstream",
    sourceGroupKeys: ["shot_images"],
    layoutIndex: 1,
    enabled: Ml,
    items: (e) => e.shots.map((t, n) => {
      const r = ig(e, t, n);
      return {
        type: "shot",
        id: t.id,
        title: `镜头 ${t.order || n + 1}`,
        prompt: cg(
          e,
          t,
          r.externalReferences
        ),
        ...r,
        paramValues: gg(e, t),
        shotId: t.id,
        shotDuration: t.duration,
        continuityAnchor: t.continuity_anchor
      };
    })
  },
  {
    key: "speech",
    title: "角色配音组",
    itemType: "speech",
    powerKind: "audio",
    outputType: "speech",
    direction: "downstream",
    layoutIndex: 2,
    enabled: Al,
    items: Zm
  },
  {
    key: "subtitles",
    title: "字幕组",
    itemType: "subtitle",
    powerKind: "text",
    outputType: "general",
    local: !0,
    direction: "downstream",
    layoutIndex: 3,
    enabled: Dl,
    items: Qm
  },
  {
    key: "lip_sync",
    title: "口型同步组",
    itemType: "lip_sync",
    powerKind: "video",
    outputType: "lip_sync",
    direction: "downstream",
    sourceGroupKeys: ["shots", "speech"],
    layoutIndex: 4,
    enabled: Pl,
    items: eg
  }
];
function ss(e, t, n, r) {
  return {
    key: e,
    title: t,
    itemType: n,
    powerKind: "image",
    outputType: "general",
    direction: "upstream",
    layoutIndex: r,
    enabled: (o) => nc(o) && o.materials.some((s) => s.type === n),
    items: (o) => o.materials.filter((s) => s.type === n).map((s) => {
      const i = ug(
        o,
        s
      );
      return {
        type: n,
        id: s.id,
        title: s.name,
        prompt: ng(
          o,
          s,
          i
        ),
        externalReferences: i
      };
    })
  };
}
function Zm(e) {
  return e.shots.flatMap(
    (t, n) => t.speech.filter((r) => r.text.trim()).map((r, o) => {
      const s = Jm(e, r);
      return {
        type: "speech",
        id: r.id,
        title: tg(
          e,
          r,
          t.order || n + 1,
          o
        ),
        prompt: r.text.trim(),
        ...s ? { paramValues: { voice: s } } : {},
        shotId: t.id,
        speechId: r.id,
        characterId: r.character_id,
        speechKind: r.kind,
        speakerMode: r.speaker_mode,
        startTime: r.start_time,
        shotDuration: t.duration
      };
    })
  );
}
function Jm(e, t) {
  return t.kind === "narration" ? e.narrator_voice.trim() : (e.materials.find(
    (n) => n.type === "character" && n.id === t.character_id
  )?.voice || "").trim();
}
function Qm(e) {
  return e.shots.flatMap((t, n) => {
    const r = zl(t);
    return r.length ? [
      {
        type: "subtitle",
        id: t.id,
        title: `镜头 ${t.order || n + 1} 字幕`,
        prompt: r.map((o) => o.text).join(" / "),
        localOutput: {
          type: "storyboard_subtitles",
          shot_id: t.id,
          tracks: r
        },
        shotId: t.id,
        shotDuration: t.duration
      }
    ] : [];
  });
}
function eg(e) {
  return e.shots.flatMap((t, n) => {
    const r = t.speech.filter(Fl);
    if (!r.length)
      return [];
    const o = r.map((a) => a.id), s = r[0]?.character_id, i = t.speech.filter((a) => a.text.trim()).map((a) => a.id);
    return [
      {
        type: "lip_sync",
        id: t.id,
        title: `镜头 ${t.order || n + 1} 口型`,
        prompt: `同步镜头 ${t.order || n + 1} 的角色口型`,
        dependencyItems: [
          { type: "shot", id: t.id },
          ...i.map((a) => ({ type: "speech", id: a }))
        ],
        referenceItems: [
          { type: "shot", id: t.id },
          ...i.map((a) => ({ type: "speech", id: a }))
        ],
        shotId: t.id,
        speechIds: o,
        characterId: s,
        shotDuration: t.duration,
        optional: !0
      }
    ];
  });
}
function tg(e, t, n, r) {
  if (t.kind === "narration")
    return `镜头 ${n} 旁白 ${r + 1}`;
  const o = e.materials.find(
    (s) => s.type === "character" && s.id === t.character_id
  );
  return `镜头 ${n} ${o?.name || "角色"}配音`;
}
function ng(e, t, n) {
  const r = Xc(n), o = t.prompt.trim();
  if (o)
    return ho(
      e,
      `${r}${o}。${Qi[t.type]}`
    );
  const s = e.shots.filter((a) => a.material_ids.includes(t.id)).map((a) => a.description.trim()).filter(Boolean), i = s.length ? `相关镜头：${s.join("；")}` : "保持整部作品的统一视觉风格";
  return ho(
    e,
    `${r}${oc[t.type]}“${t.name}”的素材生成图。${i}。${Qi[t.type]}`
  );
}
function rg(e, t) {
  return rc(e, t).map((n) => ({
    type: n.type,
    id: n.id
  }));
}
function og(e, t, n) {
  const r = rg(e, t), o = lg(e, t), s = t.match_previous ? sg(e, n) : void 0;
  if (!s)
    return {
      previousShot: void 0,
      dependencyItems: [],
      referenceItems: r,
      externalReferences: o
    };
  const i = {
    type: "shot_image",
    id: s.id
  };
  return {
    previousShot: s,
    dependencyItems: [i],
    referenceItems: [i, ...r],
    externalReferences: o
  };
}
function sg(e, t) {
  for (let n = t - 1; n >= 0; n -= 1) {
    const r = e.shots[n];
    if (!r.continue_previous)
      return r;
  }
}
function ig(e, t, n) {
  const r = fg(e, t), o = n > 0 ? e.shots[n - 1] : void 0;
  if (t.continue_previous && o) {
    const s = { type: "shot", id: o.id };
    return {
      dependencyItems: [s],
      referenceItems: [s],
      externalReferences: r
    };
  }
  return {
    dependencyItems: [],
    referenceItems: [{ type: "shot_image", id: t.id }],
    externalReferences: r
  };
}
function ag(e, t, n, r = []) {
  const o = rc(e, t), s = [
    ...r.map(
      (a, d) => `参考图${d + 1}是${Zc(a)}`
    ),
    n ? `参考图${r.length + 1}是前序镜头 ${n.order} 的参考画面` : "",
    ...o.map(
      (a, d) => `参考图${r.length + d + (n ? 2 : 1)}是${oc[a.type]}“${a.name}”`
    )
  ].filter(Boolean).join("，"), i = [
    `镜头 ${t.order} 的单张参考画面`,
    s ? `图片顺序说明：${s}` : "",
    s ? "必须严格按照上述图片顺序识别素材，不得交换、合并或忽略参考对象" : "",
    n ? "当前镜头明确要求匹配上一镜画面；前序镜头只用于保持共同主体状态、光线与空间关系，当前素材清单中不存在的对象不得继续保留" : "",
    Yc(e, t),
    `入镜关键帧状态：${t.continuity_state.entry.trim()}`,
    "当前图片只表现镜头开始时的入镜状态，不提前表现本镜头动作完成后的出镜状态",
    "严格保持参考角色的五官、发型、服装、配色和体型，保持场景结构、道具造型以及整部作品画风一致",
    "不同参考对象必须保持各自独立的轮廓、材质和尺度，不得把角色与道具融合、机械化、穿戴化或互换材质",
    "角色必须保留参考图中的发饰数量与位置以及完整服装，道具必须保持参考图中的原始尺寸比例",
    t.description.trim(),
    t.camera_instruction.trim() ? `镜头语言：${t.camera_instruction.trim()}` : "",
    Qc(t),
    `画幅：${e.aspect_ratio}`,
    "画面中不要出现字幕、对白文字、水印或界面元素"
  ].filter(Boolean);
  return ho(e, Jc(i));
}
function cg(e, t, n = []) {
  const r = t.video_prompt.trim() || El(t), o = [
    Yc(e, t),
    `入镜状态：${t.continuity_state.entry.trim()}`,
    `出镜状态：${t.continuity_state.exit.trim()}`,
    "视频必须从入镜状态开始，只完成本镜头的主要动作，并准确停在出镜状态",
    r,
    Xc(n),
    t.continue_previous ? `使用上一镜头真实尾帧继续生成。连续性锚点：${t.continuity_anchor}。保持人物、服装、道具、场景光线和动作方向一致，但不要重复上一镜头内容` : "这是新的镜头段落，以当前镜头参考图为画面锚点建立画面",
    Qc(t),
    `画幅：${e.aspect_ratio}`,
    "不生成可辨识对白、旁白、字幕或背景音乐，只保留环境声、动作声和不可辨识的人物声音",
    `时长 ${t.duration} 秒`
  ].filter(Boolean);
  return ho(e, Jc(o));
}
function Yc(e, t) {
  const n = e.shots.findIndex((i) => i.id === t.id), o = (n <= 0 ? e.storyline.setup : n >= e.shots.length - 1 ? e.storyline.payoff : e.storyline.development).trim(), s = e.shots[n + 1]?.transition.trim();
  return [
    `故事目标：${e.summary.trim()}`,
    o ? `当前叙事阶段：${o}` : "",
    `本镜变化：${t.beat.trim()}`,
    t.transition.trim() ? `从上一镜进入本镜：${t.transition.trim()}` : "",
    dg(t, n),
    s ? `本镜结束需为下一镜建立：${s}` : ""
  ].filter(Boolean).join("；");
}
function dg(e, t) {
  if (t <= 0)
    return "";
  const n = jl[e.transition_type];
  return e.transition_type === "none" ? `进入本镜的剪辑方式：${n}` : `进入本镜的剪辑方式：${n}，时长 ${e.transition_duration_ms} 毫秒；这是后期剪辑信息，画面本身不要生成转场叠影`;
}
function ug(e, t) {
  return di([
    ...e.references.filter(
      (n) => n.kind === "image" && t.reference_keys.includes(n.key)
    ),
    ...e.references.filter(
      (n) => n.kind === "image" && n.purpose === "visual_style"
    )
  ]);
}
function lg(e, t) {
  return di([
    ...e.references.filter(
      (n) => n.kind === "image" && t.reference_keys.includes(n.key)
    ),
    ...e.references.filter(
      (n) => n.kind === "image" && n.purpose === "visual_style"
    )
  ]);
}
function fg(e, t) {
  return di([
    ...e.references.filter(
      (n) => n.kind === "video" && t.reference_keys.includes(n.key)
    ),
    ...e.references.filter(
      (n) => n.kind === "video" && (n.purpose === "motion_style" || n.purpose === "visual_style")
    )
  ]);
}
function Xc(e) {
  const t = e.map(Zc);
  return t.length > 0 ? `参考素材：${t.join("；")}。` : "";
}
const pg = {
  visual_style: "只参考画风、色彩、光线和材质，不复制其中的人物或剧情",
  motion_style: "只参考运镜、动作和剪辑节奏，不沿用原视频主体、剧情或声音",
  character: "作为指定角色的外观与身份锚点",
  scene: "作为指定场景的空间、陈设与光线锚点",
  prop: "作为指定道具的造型、材质与比例锚点",
  shot: "作为指定镜头的主体、构图与空间关系锚点"
};
function Zc(e) {
  return `${e.label}（${[
    pg[e.purpose],
    e.instruction
  ].filter(Boolean).join("；")}）`;
}
function di(e) {
  const t = [], n = /* @__PURE__ */ new Set();
  for (const r of e)
    n.has(r.asset_id) || (n.add(r.asset_id), t.push(r));
  return t;
}
function Jc(e) {
  return e.map((t) => t.trim().replace(/[。！？!?；;，,：:]+$/g, "")).filter(Boolean).join("。");
}
function mg(e) {
  return { aspectRatio: e.aspect_ratio, resolution: "2k" };
}
function gg(e, t) {
  return {
    aspectRatio: e.aspect_ratio,
    duration: t.duration
  };
}
function Qc(e) {
  return !Ol(e) || ![...$l(e)][0] ? "" : "出镜说话角色是画面中唯一清晰可识别的正脸，其他人物使用背面、侧后方、远景或遮挡构图";
}
function yg(e) {
  const t = new Map(
    (e.current?.clips || []).map((r) => [r.id, r])
  ), n = e.storyboard.shots.map(
    (r, o) => hg({
      ...e,
      shot: r,
      index: o,
      current: t.get(r.id)
    })
  );
  return {
    version: 3,
    clips: Ku(
      n,
      (e.current?.clips || []).map((r) => r.id),
      (r) => r.id
    ),
    audioTracks: e.current?.audioTracks || [],
    settings: {
      resolution: e.current?.settings.resolution || "auto",
      fps: e.current?.settings.fps ?? 0
    }
  };
}
function hg(e) {
  const t = Co(
    e.nodes,
    e.sourceNodeId,
    "shot",
    e.shot.id
  ), n = Co(
    e.nodes,
    e.sourceNodeId,
    "lip_sync",
    e.shot.id
  ), r = ks(t), o = n?.storyboardItem?.stale ? void 0 : ks(n), s = !!e.current?.useOriginalVideo, i = [];
  t?.power ? r || i.push("镜头视频尚未生成") : i.push("未配置镜头视频能力");
  const a = new Map(
    (e.current?.speechTracks || []).map((w) => [w.id, w])
  ), d = e.shot.speech.filter((w) => w.text.trim()).map(
    (w) => Ng(
      e.nodes,
      e.sourceNodeId,
      w,
      a.get(w.id),
      i
    )
  ), l = _g(
    e.nodes,
    e.sourceNodeId,
    e.shot.id
  ), u = !s && o ? o : r, m = e.storyboard.shots[e.index + 1], N = m ? {
    type: m.transition_type,
    durationMs: m.transition_type === "none" ? 0 : m.transition_duration_ms
  } : { type: "none", durationMs: 0 }, b = wg(
    e.current,
    N
  );
  return {
    id: e.shot.id,
    title: `镜头 ${e.shot.order || e.index + 1}`,
    ...u ? { visualVideo: u } : {},
    ...r ? { originalAudioSource: r } : {},
    duration: e.shot.duration,
    originalVolume: e.current?.originalVolume ?? (d.length ? 0.45 : 1),
    speechTracks: d,
    subtitleTracks: l,
    useOriginalVideo: s,
    blockingIssues: Ig(i),
    transitionToNext: b,
    storyboardTransitionToNext: N
  };
}
function wg(e, t) {
  if (!e)
    return t;
  const n = e.storyboardTransitionToNext;
  return n && bg(
    e.transitionToNext,
    n
  ) ? t : e.transitionToNext;
}
function bg(e, t) {
  return e.type === t.type && e.durationMs === t.durationMs;
}
function _g(e, t, n) {
  const r = Co(
    e,
    t,
    "subtitle",
    n
  ), o = Lt(r?.resultOutput);
  return (Array.isArray(o.tracks) ? o.tracks : []).flatMap((i) => {
    const a = Lt(i), d = no(a.id), l = no(a.text);
    if (!d || !l)
      return [];
    const u = ea(a.end_time ?? a.endTime), m = no(a.speech_id ?? a.speechId);
    return [
      {
        id: d,
        text: l,
        startTime: Math.max(
          0,
          ea(a.start_time ?? a.startTime)
        ),
        ...u > 0 ? { endTime: u } : {},
        ...m ? { speechId: m } : {},
        source: no(a.source) === "speech" ? "speech" : "caption"
      }
    ];
  });
}
function Ng(e, t, n, r, o) {
  const s = Co(e, t, "speech", n.id), i = ks(s);
  return s?.power ? i || o.push(`语音“${n.text}”尚未生成`) : o.push(`语音“${n.text}”未配置语音合成能力`), {
    id: n.id,
    ...i ? { audio: i } : {},
    startTime: n.start_time,
    sourceStart: r?.sourceStart ?? 0,
    fit: r?.fit ?? "trim",
    kind: n.kind,
    ...n.character_id ? { characterId: n.character_id } : {},
    text: n.text,
    volume: r?.volume ?? 1
  };
}
function Co(e, t, n, r) {
  return e.find(
    (o) => o.storyboardItem?.sourceNodeId === t && o.storyboardItem.itemType === n && o.storyboardItem.itemId === r
  );
}
function ks(e) {
  const t = Number(e?.resultRef?.asset_id || 0), n = Number(e?.resultRef?.version_id || 0), r = t && n ? t : Number(e?.asset?.id || 0), o = t && n ? n : Number(e?.asset?.version_id || e?.asset?.version?.id || 0);
  if (!(!r || !o))
    return {
      assetId: r,
      versionId: o,
      label: e?.title || "素材"
    };
}
function Ig(e) {
  return [...new Set(e.filter(Boolean))];
}
function ea(e) {
  const t = Number(e);
  return Number.isFinite(t) ? t : 0;
}
function ed(e) {
  const t = String(
    e.storyboardItem?.generatedPrompt || ""
  ).trim(), n = String(e.composerDraft?.prompt || "").trim();
  return !!(t && n && n !== t);
}
function Sg(e, t) {
  const n = String(
    e.storyboardItem?.generatedPrompt || ""
  ).trim(), r = String(e.composerDraft?.prompt || "");
  if (!n || r.trim() === n)
    return null;
  const o = new Set(
    e.storyboardItem?.referenceNodeIds || []
  ), s = t.filter((N) => o.has(N.id)).map(
    (N) => nd(
      N,
      e.storyboardItem?.itemType
    )
  ).filter((N) => !!N), i = new Set(
    e.storyboardItem?.externalReferenceAssetIds || []
  ), a = Ea(
    e.composerDraft?.promptContent
  ).filter((N) => i.has(N.refId)), d = t.find(
    (N) => N.id === e.storyboardItem?.sourceNodeId
  ), u = ((d ? wr([
    d.asset?.version?.content,
    d.resultOutput
  ]) : null)?.references || []).filter((N) => i.has(N.asset_id)).map(rd), m = [
    ...s,
    ...a,
    ...u
  ];
  return {
    ...e.composerDraft || {},
    prompt: n,
    promptContent: m.length ? Hs(n, m) : void 0,
    paramValues: id(
      e.composerDraft?.paramValues,
      r,
      n
    )
  };
}
function ko(e, t, n) {
  const r = na(n);
  return e.find(
    (o) => Number(o.id || 0) > 0 && String(o.kind || "").trim().toLowerCase() === t && na(o.outputType) === r
  ) || null;
}
function Rs(e) {
  let t = e.canvas;
  for (const n of e.canvas.nodes) {
    if (n.type !== "power" || !Fo(
      n.power,
      n.kind,
      n.outputType
    ))
      continue;
    const r = wr([
      n.asset?.version?.content,
      n.resultOutput
    ]);
    if (!r || !sc(r))
      continue;
    const o = t.nodes.find((l) => l.id === n.id) || n, s = vg(
      o,
      r
    ), i = String(
      o.storyboardMaterializedSignature || ""
    ), a = Cg(
      t.nodes,
      o.id
    );
    t = (i ? i !== s : !a) ? Tg({
      canvas: t,
      storyboardNode: o,
      storyboard: r,
      assetCate: e.assetCate,
      powers: e.powers
    }) : Rg({
      canvas: t,
      storyboardNode: o,
      storyboard: r,
      assetCate: e.assetCate,
      powers: e.powers
    }), t = kg(
      t,
      o.id,
      s
    );
  }
  return t;
}
function vg(e, t) {
  return At(
    JSON.stringify([
      e.id,
      Number(e.resultRef?.asset_id || e.asset?.id || 0),
      Number(
        e.resultRef?.version_id || e.asset?.version_id || e.asset?.version?.id || 0
      ),
      t.workflow.confirmed_at,
      t.production_plan,
      t.materials.map((n) => [n.type, n.id]),
      t.shots.map((n) => n.id)
    ])
  );
}
function Cg(e, t) {
  return e.some(
    (n) => n.storyboardItem?.sourceNodeId === t || n.type === "group" && n.group?.origin === "script" && n.group.sourceNodeId === t
  );
}
function kg(e, t, n) {
  const r = e.nodes.findIndex((s) => s.id === t);
  if (r < 0 || e.nodes[r].storyboardMaterializedSignature === n)
    return e;
  const o = [...e.nodes];
  return o[r] = {
    ...o[r],
    storyboardMaterializedSignature: n
  }, { ...e, nodes: o };
}
function Rg(e) {
  const t = [...e.canvas.nodes];
  let n = !1, r = "";
  const o = Cs.filter(
    (a) => a.enabled(e.storyboard)
  );
  for (const a of o) {
    const d = a.local ? null : ko(e.powers, a.powerKind, a.outputType);
    for (const l of a.items(e.storyboard)) {
      const u = t.findIndex(
        (w) => Or(
          w,
          e.storyboardNode.id,
          l.type,
          l.id
        )
      );
      if (u < 0)
        continue;
      const m = td(
        l,
        t,
        e.storyboardNode.id
      ), N = t[u], b = od(
        N,
        N.groupId || "",
        m,
        a,
        d,
        { preserveStructure: !0 }
      );
      b !== N && (t[u] = b, n = !0);
    }
  }
  if (ic(e.storyboard)) {
    const a = fd({
      nodes: t,
      storyboardNode: e.storyboardNode,
      storyboard: e.storyboard,
      assetCate: e.assetCate,
      power: ko(e.powers, "video", "video_compose"),
      nextNodeNo: e.canvas.nextNodeNo,
      createMissing: !1,
      preservePosition: !0
    });
    n = n || !!a?.changed, r = a?.node.id || "";
  }
  let s = ud(
    e.canvas.edges,
    t,
    e.storyboardNode.id,
    o
  );
  s = ld(s, t, e.storyboardNode.id), s = r ? pd(
    s,
    t,
    e.storyboardNode.id,
    r,
    o
  ) : md(s, e.storyboardNode.id);
  const i = s !== e.canvas.edges;
  return n || i ? {
    ...e.canvas,
    nodes: n ? t : e.canvas.nodes,
    edges: s
  } : e.canvas;
}
function xg(e) {
  return JSON.stringify(
    e.nodes.filter(
      (t) => !!t.storyboardItem || t.type === "power" && Fo(
        t.power,
        t.kind,
        t.outputType
      )
    ).map((t) => [
      t.id,
      Number(t.resultRef?.asset_id || 0),
      Number(t.resultRef?.version_id || 0),
      Number(t.asset?.id || 0),
      Number(t.asset?.version_id || t.asset?.version?.id || 0)
    ])
  );
}
function Tg(e) {
  const t = [...e.canvas.nodes], n = /* @__PURE__ */ new Set(), r = new Set(
    Cs.map((b) => b.itemType)
  ), o = Cs.filter(
    (b) => b.enabled(e.storyboard)
  ), s = ic(
    e.storyboard
  );
  r.add("video_compose"), s && n.add(
    po(
      e.storyboardNode.id,
      "video_compose",
      "composition"
    )
  );
  const i = o.map((b) => ({
    spec: b,
    items: b.items(e.storyboard),
    power: b.local ? null : ko(e.powers, b.powerKind, b.outputType)
  }));
  for (const { items: b } of i)
    for (const w of b)
      n.add(
        po(e.storyboardNode.id, w.type, w.id)
      );
  let a = !1;
  for (let b = t.length - 1; b >= 0; b -= 1) {
    const w = t[b].storyboardItem;
    !w || w.sourceNodeId !== e.storyboardNode.id || !r.has(w.itemType) || n.has(
      po(
        w.sourceNodeId,
        w.itemType,
        w.itemId
      )
    ) || (t.splice(b, 1), a = !0);
  }
  const d = Lm({
    sourceNode: e.storyboardNode,
    groups: i.map(({ spec: b, items: w, power: z }) => ({
      key: b.key,
      layoutIndex: b.layoutIndex,
      itemCount: w.length,
      power: z,
      powerKind: b.powerKind,
      direction: b.direction
    }))
  });
  let l = ei(t, e.canvas.nextNodeNo);
  a = a || l !== e.canvas.nextNodeNo;
  for (const { spec: b, items: w, power: z } of i) {
    const F = d.get(b.key);
    if (!F)
      continue;
    const U = Dg({
      nodes: t,
      storyboardNode: e.storyboardNode,
      spec: b,
      layout: F,
      assetCate: e.assetCate
    });
    a = a || U.changed;
    for (const k of w) {
      const C = td(
        k,
        t,
        e.storyboardNode.id
      ), T = t.findIndex(
        (ne) => Or(
          ne,
          e.storyboardNode.id,
          C.type,
          C.id
        )
      );
      if (T >= 0) {
        const ne = t[T], me = od(
          ne,
          U.node.id,
          C,
          b,
          z
        );
        me !== ne && (t[T] = me, a = !0);
        continue;
      }
      const re = Pg({
        nodes: t,
        group: U.node,
        storyboardNode: e.storyboardNode,
        item: C,
        assetCate: e.assetCate,
        spec: b,
        power: z
      });
      re.nodeNo = l++, t.push(re), a = !0;
    }
    const A = w.map(
      (k) => t.find(
        (C) => C.groupId === U.node.id && Or(
          C,
          e.storyboardNode.id,
          k.type,
          k.id
        )
      )
    ).filter((k) => !!k), X = qm(
      U.node,
      A
    );
    for (const k of A) {
      const C = X.positions.get(k.id);
      if (!C || k.x === C.x && k.y === C.y)
        continue;
      const T = t.indexOf(k);
      t[T] = { ...k, ...C }, a = !0;
    }
    const Z = t.findIndex((k) => k.id === U.node.id), de = t[Z];
    (de.width !== X.width || de.height !== X.height) && (t[Z] = {
      ...de,
      width: X.width,
      height: X.height
    }, a = !0);
  }
  const u = new Set(o.map((b) => b.key));
  for (let b = t.length - 1; b >= 0; b -= 1) {
    const w = t[b];
    w.type !== "group" || w.group?.origin !== "script" || w.group.sourceNodeId !== e.storyboardNode.id || !w.group.syncKey || u.has(
      w.group.syncKey
    ) || (t.splice(b, 1), a = !0);
  }
  const m = s ? fd({
    nodes: t,
    storyboardNode: e.storyboardNode,
    storyboard: e.storyboard,
    assetCate: e.assetCate,
    power: ko(e.powers, "video", "video_compose"),
    nextNodeNo: l
  }) : null;
  m && (l = m.nextNodeNo, a = a || m.changed);
  let N = ud(
    e.canvas.edges,
    t,
    e.storyboardNode.id,
    o
  );
  return N = ld(N, t, e.storyboardNode.id), N = m ? pd(
    N,
    t,
    e.storyboardNode.id,
    m.node.id,
    o
  ) : md(N, e.storyboardNode.id), N !== e.canvas.edges && (a = !0), a ? { ...e.canvas, nextNodeNo: l, nodes: t, edges: N } : e.canvas;
}
function td(e, t, n) {
  const r = (w) => (w || []).map(
    (z) => t.find(
      (F) => Or(F, n, z.type, z.id)
    )
  ).filter((z) => !!z), o = r(e.dependencyItems), s = r(e.referenceItems), i = $g([...o, ...s]), a = e.externalReferences || [], d = {
    ...e,
    dependencyNodeIds: o.map((w) => w.id),
    referenceNodeIds: s.map((w) => w.id),
    sourceSignatureParts: [
      ...e.sourceSignatureParts || [],
      ...i.map(jg),
      ...a.map(Ag)
    ]
  };
  if (!["character", "scene", "prop", "shot_image", "shot", "lip_sync"].includes(
    e.type
  ))
    return d;
  const l = Mg(
    e.prompt,
    s,
    a
  ), u = l === e.prompt ? d : { ...d, prompt: l }, m = a.map(
    rd
  );
  if (m.push(
    ...s.map((w) => nd(w, e.type)).filter((w) => !!w)
  ), !m.length)
    return u;
  const N = Hs(
    l,
    m
  ), b = qu(N);
  return Fa(N) ? { ...d, prompt: b, promptContent: N } : { ...u, prompt: b };
}
function Mg(e, t, n = []) {
  const r = [], o = /* @__PURE__ */ new Set();
  for (const s of n) {
    const i = Di(s.label), a = i ? `@${i}` : "";
    !a || o.has(a) || e.includes(a) || (o.add(a), r.push(a));
  }
  for (const s of t) {
    const i = Di(s.title), a = i ? `@${i}` : "";
    !a || o.has(a) || e.includes(a) || (o.add(a), r.push(a));
  }
  return [r.join(" "), e].filter(Boolean).join(" ").trim();
}
function nd(e, t) {
  const n = Number(e.resultRef?.asset_id || e.asset?.id || 0), r = Number(
    e.resultRef?.version_id || e.asset?.version_id || e.asset?.version?.id || 0
  );
  return !n || !r ? null : {
    refType: "asset",
    refId: n,
    versionId: r,
    label: e.title,
    usage: t === "shot" && e.storyboardItem?.itemType === "shot_image" ? "firstFrame" : void 0
  };
}
function rd(e) {
  return {
    refType: "asset",
    refId: e.asset_id,
    versionId: e.version_id,
    label: e.label
  };
}
function Ag(e) {
  return [
    "asset",
    e.asset_id,
    e.version_id || 0,
    e.kind,
    e.purpose,
    e.instruction
  ].join(":");
}
function Dg(e) {
  const t = ui(
    e.nodes,
    e.storyboardNode.id,
    e.spec.key
  );
  if (t) {
    const o = t.title !== e.spec.title;
    if (t.group?.layoutKey === e.layout.layoutKey && !o)
      return { node: t, changed: !1 };
    if (t.group?.layoutKey === e.layout.layoutKey) {
      const d = { ...t, title: e.spec.title };
      return e.nodes[e.nodes.indexOf(t)] = d, { node: d, changed: !0 };
    }
    const s = e.layout.bounds.x - t.x, i = e.layout.bounds.y - t.y;
    let a = t;
    for (const [d, l] of e.nodes.entries()) {
      if (l.id !== t.id && l.groupId !== t.id)
        continue;
      const u = {
        ...l,
        x: l.x + s,
        y: l.y + i,
        ...l.id === t.id ? {
          title: e.spec.title,
          width: e.layout.bounds.width,
          height: e.layout.bounds.height,
          group: {
            ...l.group || {},
            layoutKey: e.layout.layoutKey
          }
        } : {}
      };
      e.nodes[d] = u, l.id === t.id && (a = u);
    }
    return { node: a, changed: !0 };
  }
  const n = e.layout.bounds, r = Eo("group", e.assetCate, e.nodes.length, {
    x: n.x,
    y: n.y
  });
  return r.id = fi(
    e.nodes,
    `script-group-${At(e.storyboardNode.id)}-${e.spec.key}`
  ), r.title = e.spec.title, r.width = n.width, r.height = n.height, r.group = {
    origin: "script",
    sourceNodeId: e.storyboardNode.id,
    syncKey: e.spec.key,
    layoutKey: e.layout.layoutKey
  }, e.nodes.push(r), { node: r, changed: !0 };
}
function ui(e, t, n) {
  return e.find(
    (r) => r.type === "group" && r.group?.origin === "script" && r.group.sourceNodeId === t && r.group.syncKey === n
  );
}
function Pg(e) {
  const t = Eo(
    "power",
    e.assetCate,
    e.nodes.length,
    { x: e.group.x, y: e.group.y },
    e.power ? { power: e.power } : void 0
  );
  t.id = fi(
    e.nodes,
    `script-item-${At(
      po(
        e.storyboardNode.id,
        e.item.type,
        e.item.id
      )
    )}`
  ), t.title = e.item.title, t.description = e.item.prompt, t.kind = e.spec.powerKind, t.outputType = e.spec.outputType, e.power || Object.assign(
    t,
    Oo({
      kind: e.spec.powerKind,
      outputType: e.spec.outputType
    })
  ), !e.power && !e.spec.local && (t.subtitle = `未配置${Og(e.spec)}能力`, t.description = `${t.subtitle}。配置并启用能力后可运行此条目。`), e.spec.local && (t.subtitle = "本地字幕轨", t.description = e.item.prompt || "当前镜头字幕轨", t.resultOutput = e.item.localOutput), t.groupId = e.group.id, t.composerDraft = {
    prompt: e.item.prompt,
    promptContent: e.item.promptContent,
    paramValues: e.item.paramValues
  }, t.storyboardItem = ad(
    e.storyboardNode.id,
    e.item
  );
  const n = Km(
    e.group,
    e.nodes,
    t
  );
  return t.x = n.x, t.y = n.y, t;
}
function od(e, t, n, r, o, s = {}) {
  const i = e.storyboardItem;
  if (!i)
    return e;
  const a = i.sourceSignature || cd({
    ...n,
    prompt: i.generatedPrompt,
    promptContent: e.composerDraft?.promptContent
  }), d = String(e.composerDraft?.prompt || ""), l = !d.trim() || d === i.generatedPrompt, u = l ? n.prompt : d, m = u !== d, N = zg(
    e.composerDraft?.paramValues,
    d,
    u,
    n.paramValues
  ), b = N !== e.composerDraft?.paramValues, w = l ? n.promptContent : Fg(
    d,
    e.composerDraft?.promptContent,
    n.promptContent
  ), z = JSON.stringify(e.composerDraft?.promptContent || null) !== JSON.stringify(w || null), F = ad(i.sourceNodeId, n), U = sd(e), A = i.resultSourceSignature || (U ? a : "");
  A && !r.local && (F.resultSourceSignature = A), F.stale = r.local ? !1 : !!(U && A !== F.sourceSignature);
  const X = s.preserveStructure && e.titleMode === "manual" ? e.title : n.title, Z = e.title !== X, de = s.preserveStructure ? e.groupId || "" : t, k = !e.power && !!o, C = e.kind !== r.powerKind, T = e.outputType !== r.outputType, re = r.local && JSON.stringify(e.resultOutput || null) !== JSON.stringify(n.localOutput || null);
  return (e.groupId || "") === de && !m && !b && !z && !Z && !k && !C && !T && !re && dd(i, F) ? e : {
    ...e,
    title: X,
    kind: r.powerKind,
    outputType: r.outputType,
    ...k ? {
      power: o || void 0,
      subtitle: o?.output?.name || o?.name || e.subtitle
    } : {},
    description: m || k ? u : e.description,
    ...r.local ? { resultOutput: n.localOutput } : {},
    groupId: de,
    composerDraft: m || b || z ? {
      ...e.composerDraft || {},
      prompt: u,
      promptContent: w,
      paramValues: N
    } : e.composerDraft,
    storyboardItem: F
  };
}
function sd(e) {
  return Number(e.resultRef?.version_id || 0) > 0 || Number(e.asset?.version_id || e.asset?.version?.id || 0) > 0 || e.resultOutput != null;
}
function id(e, t, n) {
  if (!e || !t || t === n)
    return e;
  let r;
  for (const [o, s] of Object.entries(e))
    s === t && (r ||= { ...e }, r[o] = n);
  return r || e;
}
function zg(e, t, n, r) {
  let o = id(
    e,
    t,
    n
  );
  for (const [s, i] of Object.entries(r || {}))
    o?.[s] !== i && (o = { ...o || {}, [s]: i });
  return o;
}
function Fg(e, t, n) {
  return !n || !Fa(n) ? t : Hs(
    e,
    Ea(n)
  );
}
function ad(e, t) {
  return {
    sourceNodeId: e,
    itemType: t.type,
    itemId: t.id,
    generatedPrompt: t.prompt,
    dependencyNodeIds: t.dependencyNodeIds,
    referenceNodeIds: t.referenceNodeIds,
    externalReferenceAssetIds: (t.externalReferences || []).map(
      (n) => n.asset_id
    ),
    shotId: t.shotId,
    speechId: t.speechId,
    speechIds: t.speechIds,
    characterId: t.characterId,
    speechKind: t.speechKind,
    speakerMode: t.speakerMode,
    startTime: t.startTime,
    shotDuration: t.shotDuration,
    continuityAnchor: t.continuityAnchor,
    optional: t.optional,
    sourceSignature: cd(t),
    stale: !1
  };
}
function cd(e) {
  return At(
    JSON.stringify([
      e.prompt,
      e.promptContent || null,
      e.paramValues || null,
      e.localOutput || null,
      e.sourceSignatureParts || [],
      e.shotId || "",
      e.speechId || "",
      e.speechIds || [],
      e.characterId || "",
      e.speechKind || "",
      e.speakerMode || "",
      e.startTime ?? null,
      e.shotDuration ?? null,
      e.continuityAnchor || "",
      !!e.optional
    ])
  );
}
function dd(e, t) {
  return e.sourceNodeId === t.sourceNodeId && e.itemType === t.itemType && e.itemId === t.itemId && e.generatedPrompt === t.generatedPrompt && so(e.dependencyNodeIds, t.dependencyNodeIds) && so(e.referenceNodeIds, t.referenceNodeIds) && so(
    e.externalReferenceAssetIds,
    t.externalReferenceAssetIds
  ) && e.shotId === t.shotId && e.speechId === t.speechId && so(e.speechIds, t.speechIds) && e.characterId === t.characterId && e.speechKind === t.speechKind && e.speakerMode === t.speakerMode && e.startTime === t.startTime && e.shotDuration === t.shotDuration && e.continuityAnchor === t.continuityAnchor && !!e.optional == !!t.optional && e.sourceSignature === t.sourceSignature && e.resultSourceSignature === t.resultSourceSignature && !!e.stale == !!t.stale;
}
function Or(e, t, n, r) {
  const o = e.storyboardItem;
  return !!(o && o.sourceNodeId === t && o.itemType === n && o.itemId === r);
}
function ud(e, t, n, r) {
  const o = r.map((d) => ({
    spec: d,
    group: t.find(
      (l) => l.type === "group" && l.group?.origin === "script" && l.group.sourceNodeId === n && l.group.syncKey === d.key
    )
  })).filter(
    (d) => !!d.group
  ), s = `script-edge-${At(n)}-`, i = e.filter((d) => !d.id.startsWith(s));
  for (const { spec: d, group: l } of o) {
    const u = d.direction === "upstream", m = (d.sourceGroupKeys || []).map((b) => ui(t, n, b)).filter((b) => !!b), N = u ? [l] : m.length ? m : [t.find((b) => b.id === n)].filter(
      (b) => !!b
    );
    for (const b of N) {
      const w = b.id, z = u ? n : l.id;
      i.push({
        id: pi(
          i,
          `${s}${d.key}-${At(w)}`
        ),
        from: w,
        to: z,
        logicalFrom: w,
        logicalTo: z,
        purpose: "structure",
        executionMode: u ? void 0 : "manual"
      });
    }
  }
  const a = wn(t, i);
  return li(e, a) ? e : a;
}
function ld(e, t, n) {
  const r = `script-item-edge-${At(n)}-`, o = e.filter((d) => !d.id.startsWith(r)), s = t.filter(
    (d) => d.storyboardItem?.sourceNodeId === n && (!!d.groupId || d.storyboardItem.itemType === "video_compose")
  ), i = new Map(s.map((d) => [d.id, d]));
  for (const d of s)
    for (const l of d.storyboardItem?.dependencyNodeIds || []) {
      const u = i.get(l);
      !u || u.id === d.id || o.push({
        id: pi(
          o,
          `${r}${At(u.id)}-${At(d.id)}`
        ),
        from: u.id,
        to: d.id,
        logicalFrom: u.id,
        logicalTo: d.id,
        purpose: "dependency",
        executionMode: u.groupId && u.groupId === d.groupId ? void 0 : "manual"
      });
    }
  const a = wn(t, o);
  return li(e, a) ? e : a;
}
function fd(e) {
  const t = e.nodes.find(
    (a) => Or(
      a,
      e.storyboardNode.id,
      "video_compose",
      "composition"
    )
  ), n = yg({
    storyboard: e.storyboard,
    sourceNodeId: e.storyboardNode.id,
    nodes: e.nodes,
    current: t?.composerDraft?.videoComposition
  }), r = Eg(n), o = {
    sourceNodeId: e.storyboardNode.id,
    itemType: "video_compose",
    itemId: "composition",
    generatedPrompt: "",
    sourceSignature: r,
    stale: !1
  };
  if (t) {
    const a = e.preservePosition ? { x: t.x, y: t.y } : ta(e.nodes, e.storyboardNode), d = sd(t), l = t.storyboardItem?.resultSourceSignature || (d ? t.storyboardItem?.sourceSignature : "");
    l && (o.resultSourceSignature = l), o.stale = !!(d && l !== r);
    const u = !t.power && !!e.power, m = JSON.stringify(t.composerDraft?.videoComposition || null) !== JSON.stringify(n), N = t.x !== a.x || t.y !== a.y;
    if (!u && !m && !N && t.kind === "video" && t.outputType === "video_compose" && dd(t.storyboardItem, o))
      return {
        node: t,
        changed: !1,
        nextNodeNo: e.nextNodeNo
      };
    const b = {
      ...t,
      x: a.x,
      y: a.y,
      kind: "video",
      outputType: "video_compose",
      ...u ? {
        power: e.power || void 0,
        subtitle: e.power?.output?.name || e.power?.name || "视频合成",
        description: "按镜头顺序合成画面、原声和配音。"
      } : {},
      composerDraft: {
        ...t.composerDraft || {},
        videoComposition: n
      },
      storyboardItem: o
    };
    return e.nodes[e.nodes.indexOf(t)] = b, {
      node: b,
      changed: !0,
      nextNodeNo: e.nextNodeNo
    };
  }
  if (e.createMissing === !1)
    return null;
  const s = ta(
    e.nodes,
    e.storyboardNode
  ), i = Eo(
    "power",
    e.assetCate,
    e.nodes.length,
    s,
    e.power ? { power: e.power } : void 0
  );
  return i.id = fi(
    e.nodes,
    `script-compose-${At(e.storyboardNode.id)}`
  ), i.nodeNo = e.nextNodeNo, i.title = "视频合成", i.kind = "video", i.outputType = "video_compose", i.description = "按镜头顺序合成画面、原声和配音。", e.power || (i.subtitle = "未配置视频合成能力", i.description = "未配置视频合成能力。配置并启用后可生成最终视频。"), i.composerDraft = { videoComposition: n }, i.storyboardItem = o, e.nodes.push(i), {
    node: i,
    changed: !0,
    nextNodeNo: e.nextNodeNo + 1
  };
}
function Eg(e) {
  const t = e.clips.map((n) => {
    const r = { ...n };
    return delete r.storyboardTransitionToNext, r;
  });
  return At(JSON.stringify({ ...e, clips: t }));
}
function ta(e, t) {
  const n = e.filter(
    (s) => s.type === "group" && s.group?.origin === "script" && s.group.sourceNodeId === t.id
  ), r = n.reduce(
    (s, i) => Math.max(s, i.x + i.width),
    t.x + t.width + 160
  ), o = n.reduce(
    (s, i) => Math.min(s, i.y),
    t.y
  );
  return { x: r + 72, y: o };
}
function pd(e, t, n, r, o) {
  const i = ["shots", "speech", "subtitles", "lip_sync"].filter(
    (u) => o.some((m) => m.key === u)
  ).map((u) => ui(t, n, u)).filter((u) => !!u), a = `script-compose-edge-${At(n)}-`, d = e.filter((u) => !u.id.startsWith(a));
  for (const u of i.length ? i : t.filter((m) => m.id === n))
    d.push({
      id: pi(
        d,
        `${a}${At(u.id)}`
      ),
      from: u.id,
      to: r,
      logicalFrom: u.id,
      logicalTo: r,
      purpose: "dependency",
      executionMode: "manual"
    });
  const l = wn(t, d);
  return li(e, l) ? e : l;
}
function md(e, t) {
  const n = `script-compose-edge-${At(t)}-`, r = e.filter((o) => !o.id.startsWith(n));
  return r.length === e.length ? e : r;
}
function li(e, t) {
  if (e.length !== t.length)
    return !1;
  const n = new Map(t.map((r) => [r.id, r]));
  return e.every((r) => {
    const o = n.get(r.id);
    return !!(o && r.from === o.from && r.to === o.to && (r.logicalFrom || "") === (o.logicalFrom || "") && (r.logicalTo || "") === (o.logicalTo || "") && (r.purpose || "") === (o.purpose || "") && (r.executionMode || "auto") === (o.executionMode || "auto") && (r.mediaUsage || "") === (o.mediaUsage || ""));
  });
}
function po(e, t, n) {
  return `${e}\0${t}\0${n}`;
}
function fi(e, t) {
  return gd(new Set(e.map((n) => n.id)), t);
}
function pi(e, t) {
  return gd(new Set(e.map((n) => n.id)), t);
}
function gd(e, t) {
  if (!e.has(t))
    return t;
  let n = 2;
  for (; e.has(`${t}-${n}`); )
    n += 1;
  return `${t}-${n}`;
}
function At(e) {
  let t = 2166136261;
  for (const n of e)
    t ^= n.codePointAt(0) || 0, t = Math.imul(t, 16777619);
  return (t >>> 0).toString(36);
}
function na(e) {
  return String(e || "general").trim().toLowerCase() || "general";
}
function Og(e) {
  return e.outputType === "speech" ? "语音合成" : e.outputType === "lip_sync" ? "口型同步" : e.powerKind === "image" ? "图片" : "视频";
}
function so(e, t) {
  return JSON.stringify(e || []) === JSON.stringify(t || []);
}
function $g(e) {
  const t = /* @__PURE__ */ new Set();
  return e.filter((n) => t.has(n.id) ? !1 : (t.add(n.id), !0));
}
function jg(e) {
  return [
    e.id,
    Number(e.resultRef?.version_id || 0),
    Number(e.asset?.version_id || e.asset?.version?.id || 0),
    e.storyboardItem?.sourceSignature || "",
    e.storyboardItem?.resultSourceSignature || ""
  ].join(":");
}
const Bg = {
  characters: { section: "materials", materialType: "character" },
  scenes: { section: "materials", materialType: "scene" },
  props: { section: "materials", materialType: "prop" }
};
function yd(e) {
  if (!e)
    return;
  if (e.type === "group") {
    const n = String(e.group?.syncKey || "");
    return Bg[n] || { section: "shots" };
  }
  const t = e.storyboardItem;
  if (t)
    return t.itemType === "character" || t.itemType === "scene" || t.itemType === "prop" ? {
      section: "materials",
      materialType: t.itemType,
      materialId: t.itemId
    } : t.itemType === "video_compose" ? { section: "shots" } : {
      section: "shots",
      shotId: t.shotId || t.itemId
    };
}
const ra = 52, oa = 72, Vg = 48, Ug = {
  width: 360,
  height: 52
};
function xs(e) {
  return gi(e);
}
function sa(e, t) {
  if (t.storyboardItem?.sourceNodeId)
    return t.storyboardItem.sourceNodeId;
  if (t.type === "group" && t.group?.origin === "script")
    return t.group.sourceNodeId || "";
  if (gi(e).has(t.id))
    return t.id;
  if (!t.groupId)
    return "";
  const r = e.find((o) => o.id === t.groupId);
  return r?.group?.origin === "script" && r.group.sourceNodeId || "";
}
function hd(e, t) {
  const n = gi(e), r = [];
  for (const o of n) {
    const s = e.find((m) => m.id === o);
    if (!s)
      continue;
    const i = e.filter(
      (m) => m.type === "group" && m.group?.origin === "script" && m.group.sourceNodeId === o
    ), a = new Set(i.map((m) => m.id)), d = e.filter(
      (m) => m.id === o || m.group?.sourceNodeId === o || m.storyboardItem?.sourceNodeId === o || !!(m.groupId && a.has(m.groupId))
    );
    if (d.length <= 1)
      continue;
    const l = d.filter(
      (m) => m.storyboardItem?.sourceNodeId === o && !m.storyboardItem.optional
    ), u = Gg(d);
    r.push({
      id: mi(o),
      sourceNodeId: o,
      title: s.title || "分镜脚本",
      memberNodeIds: d.map((m) => m.id),
      workNodeIds: l.map((m) => m.id),
      groupCount: i.length,
      workNodeCount: l.length,
      completedCount: l.filter(
        (m) => !m.storyboardItem?.stale && t(m)
      ).length,
      bounds: u
    });
  }
  return r.sort(
    (o, s) => o.bounds.y - s.bounds.y || o.bounds.x - s.bounds.x
  );
}
function wd(e, t, n) {
  const r = new Map(t.map((u) => [u.id, u])), o = e.workNodeIds.map((u) => r.get(u)).filter((u) => !!u), s = new Set(
    o.filter((u) => u.storyboardItem?.stale || !n(u)).map((u) => u.id)
  );
  let i = !0;
  for (; i; ) {
    i = !1;
    for (const u of o)
      s.has(u.id) || u.storyboardItem?.itemType === "video_compose" || qg(u).some(
        (m) => s.has(m)
      ) && (s.add(u.id), i = !0);
  }
  const a = o.find(
    (u) => u.storyboardItem?.itemType === "video_compose"
  );
  s.size > 0 && a && s.add(a.id);
  const d = o.filter(
    (u) => s.has(u.id)
  );
  if (d.length === 0)
    return { pendingNodeIds: [], blockedReason: "制作区已完成" };
  const l = d.find(
    (u) => !$o(u)
  );
  return l ? {
    pendingNodeIds: d.map((u) => u.id),
    blockedReason: `“${l.title || "未命名节点"}”未配置可用能力`
  } : {
    pendingNodeIds: d.map((u) => u.id),
    blockedReason: Dc({
      targets: d,
      nodesByID: r,
      hasResult: n
    })
  };
}
function Lg(e, t, n) {
  let r = !1;
  const o = e.map((s) => {
    const i = s.storyboardItem;
    return !i || i.sourceNodeId !== t || !n.has(s.id) ? s : (r = !0, {
      ...s,
      storyboardItem: {
        ...i,
        resultSourceSignature: i.sourceSignature || i.resultSourceSignature,
        stale: !1
      }
    });
  });
  return r ? o : e;
}
function ia(e, t) {
  return t ? {
    x: e.bounds.x,
    y: e.bounds.y,
    ...Ug
  } : e.bounds;
}
function Kg(e, t, n) {
  const r = bd(t, n);
  if (r.x === 0 && r.y === 0)
    return e;
  const o = new Set(t.memberNodeIds);
  return e.map(
    (s) => o.has(s.id) ? { ...s, x: s.x + r.x, y: s.y + r.y } : s
  );
}
function bd(e, t) {
  return {
    x: t.x - e.bounds.x,
    y: t.y - e.bounds.y
  };
}
function mi(e) {
  return `storyboard-frame:${e}`;
}
function qg(e) {
  return [
    ...e.storyboardItem?.dependencyNodeIds || [],
    ...e.storyboardItem?.referenceNodeIds || []
  ];
}
function gi(e) {
  const t = /* @__PURE__ */ new Set();
  for (const n of e)
    n.storyboardMaterializedSignature && t.add(n.id), n.group?.origin === "script" && n.group.sourceNodeId && t.add(n.group.sourceNodeId), n.storyboardItem?.sourceNodeId && t.add(n.storyboardItem.sourceNodeId);
  return t;
}
function Gg(e) {
  let t = Number.POSITIVE_INFINITY, n = Number.POSITIVE_INFINITY, r = Number.NEGATIVE_INFINITY, o = Number.NEGATIVE_INFINITY;
  for (const s of e) {
    const i = aa(s.width, 180), a = aa(s.height, 180);
    t = Math.min(t, s.x), n = Math.min(n, s.y), r = Math.max(r, s.x + i), o = Math.max(o, s.y + a);
  }
  return {
    x: t - ra,
    y: n - oa,
    width: r - t + ra * 2,
    height: o - n + oa + Vg
  };
}
function aa(e, t) {
  return Number.isFinite(e) && e > 0 ? e : t;
}
const Hg = ku(function({
  data: t
}) {
  const n = t, r = Wg(n), o = n.running ? "制作区正在执行" : n.runBlockedReason || (n.completedCount > 0 ? "只执行尚未完成或上次失败的内容" : "按依赖顺序生成制作区内容");
  return /* @__PURE__ */ v(
    "section",
    {
      className: `ws-storyboard-frame ${n.collapsed ? "is-collapsed" : ""}`,
      "aria-label": `${n.title} 分镜制作区`,
      children: [
        /* @__PURE__ */ v("header", { className: "ws-storyboard-frame-header", children: [
          /* @__PURE__ */ c("span", { className: "ws-storyboard-frame-icon", "aria-hidden": "true", children: /* @__PURE__ */ c(Wa, { size: 15 }) }),
          /* @__PURE__ */ v("strong", { children: [
            n.title,
            " · 分镜制作区"
          ] }),
          /* @__PURE__ */ v("span", { className: "ws-storyboard-frame-progress", children: [
            n.groupCount,
            " 组 · ",
            n.completedCount,
            "/",
            n.workNodeCount,
            " ",
            "完成"
          ] }),
          n.runActionEnabled ? /* @__PURE__ */ c(We, { label: o, children: /* @__PURE__ */ v(
            "button",
            {
              type: "button",
              className: "nodrag nopan ws-storyboard-frame-run",
              "aria-label": r,
              disabled: n.running || !!n.runBlockedReason,
              onClick: is(n.onRun),
              children: [
                n.running ? /* @__PURE__ */ c(sn, { size: 14, className: "ws-spin" }) : /* @__PURE__ */ c(Ur, { size: 14, fill: "currentColor" }),
                /* @__PURE__ */ c("span", { children: r })
              ]
            }
          ) }) : null,
          /* @__PURE__ */ c(We, { label: "聚焦制作区", children: /* @__PURE__ */ c(
            "button",
            {
              type: "button",
              className: "nodrag nopan",
              "aria-label": "聚焦制作区",
              onClick: is(n.onFocus),
              children: /* @__PURE__ */ c(Ql, { size: 14 })
            }
          ) }),
          /* @__PURE__ */ c(We, { label: n.collapsed ? "展开制作区" : "折叠制作区", children: /* @__PURE__ */ c(
            "button",
            {
              type: "button",
              className: "nodrag nopan",
              "aria-label": n.collapsed ? "展开制作区" : "折叠制作区",
              onClick: is(n.onToggleCollapsed),
              children: n.collapsed ? /* @__PURE__ */ c(Ba, { size: 15 }) : /* @__PURE__ */ c(al, { size: 15 })
            }
          ) })
        ] }),
        n.collapsed ? null : /* @__PURE__ */ c("div", { className: "ws-storyboard-frame-surface", "aria-hidden": "true" })
      ]
    }
  );
});
function Wg(e) {
  return e.running ? "生成中" : e.workNodeCount > 0 && e.completedCount >= e.workNodeCount ? "已完成" : e.completedCount > 0 ? "继续生成" : "开始生成";
}
function is(e) {
  return (t) => {
    t.preventDefault(), t.stopPropagation(), e();
  };
}
const fr = /* @__PURE__ */ new Map(), Yg = 100;
function Xg(e, t) {
  const n = String(t.runError || "").trim(), r = t.id, o = Number(t.resultRef?.execution_id || 0), s = String(t.resultRef?.request_id || "").trim(), i = Number(t.resultRef?.run_id || 0), a = Jg(
    e,
    r,
    o,
    s,
    i
  ), [d, l] = j(n), [u, m] = j(!1);
  return ce(() => {
    if (l(n), !n || !a || !_m(n)) {
      m(!1);
      return;
    }
    let N = !0;
    return m(!0), Zg({
      projectId: e,
      nodeId: r,
      executionId: o,
      requestId: s,
      runId: i,
      cacheKey: a,
      fallback: n
    }).then((b) => {
      N && b && l(b);
    }).catch(() => {
    }).finally(() => {
      N && m(!1);
    }), () => {
      N = !1;
    };
  }, [
    o,
    n,
    r,
    e,
    a,
    s,
    i
  ]), { error: d || n, loading: u };
}
function Zg({
  projectId: e,
  nodeId: t,
  executionId: n,
  requestId: r,
  runId: o,
  cacheKey: s,
  fallback: i
}) {
  const a = fr.get(s);
  if (a)
    return a;
  const d = Tc({
    projectId: e,
    executionId: n,
    requestId: r,
    runId: o
  }).then((l) => {
    const u = br(l), m = [...u.node_results || []].reverse().find((b) => b.node_key === t), N = si(m) || Bc(u);
    return ii(N, i);
  });
  return fr.set(s, d), Qg(), d.catch(() => fr.delete(s)), d;
}
function Jg(e, t, n, r, o) {
  const s = n ? `execution:${n}` : r ? `request:${r}` : o ? `run:${o}` : "";
  return e > 0 && s ? `${e}:${s}:${t}` : "";
}
function Qg() {
  for (; fr.size > Yg; ) {
    const e = fr.keys().next().value;
    if (!e)
      return;
    fr.delete(e);
  }
}
function ey({
  space: e,
  canvases: t,
  cache: n
}) {
  const [r, o] = j([]), [s, i] = j([]), [a, d] = j([]), [l, u] = j(!1), m = `${e?.project.id || 0}:${e?.release.id || e?.project.release_id || 0}`, N = Q(m), b = se(
    () => ty(t),
    [t]
  );
  ce(() => {
    N.current = m, o([]), i([]), d([]), u(!1);
  }, [m]);
  const w = $(
    async (z = !1) => {
      if (!e)
        return !1;
      const F = m;
      try {
        const U = await n.loadCatalog(
          e.project.id,
          Number(e.release?.id || e.project.release_id || 0),
          () => gp(e.project.id),
          z
        );
        return N.current !== F ? !1 : (o(U.roles), i(U.powers), d(U.powerCategories), u(!0), !0);
      } catch (U) {
        return G.error(U instanceof Error ? U.message : "加载能力列表失败"), !1;
      }
    },
    [n, m, e]
  );
  return ce(() => {
    !e || !b || l || w();
  }, [w, l, b, e]), {
    roles: r,
    powers: s,
    powerCategories: a,
    loaded: l,
    required: b,
    load: w
  };
}
function ty(e) {
  return Object.values(e).some(
    (t) => t.nodes.some((n) => n.type !== "power" ? !1 : n.storyboardItem && !n.power ? !0 : Fo(n.power, n.kind, n.outputType) ? !!wr([
      n.asset?.version?.content,
      n.resultOutput
    ]) : !1)
  );
}
function ny({
  node: e,
  memberCount: t,
  runnableCount: n,
  completedCount: r,
  failedCount: o,
  staleCount: s,
  status: i,
  frameRunning: a = !1,
  selected: d,
  managed: l = !1,
  onRename: u,
  onEditStructure: m,
  onRun: N,
  runBlockedReason: b = "",
  children: w
}) {
  const [z, F] = j(!1), [U, A] = j(e.title), X = Q(null), Z = i === "running" || i === "waiting", de = Z ? "分组正在执行" : a ? "制作区正在执行" : b || (n === 0 ? "分组内暂无可运行节点" : s > 0 ? `重新生成 ${s} 个已变更节点` : "运行分组"), k = !N || n === 0 || Z || a || !!b;
  ce(() => {
    z || A(e.title);
  }, [z, e.title]), ce(() => {
    z && (X.current?.focus(), X.current?.select());
  }, [z]);
  const C = () => {
    const T = U.trim() || "未命名分组";
    F(!1), A(T), T !== e.title && u?.(T);
  };
  return /* @__PURE__ */ v(
    "div",
    {
      className: `ws-node-group-wrap ${d ? "is-selected" : ""} ${Z ? "is-running" : ""} ${i === "error" ? "is-error" : ""} ${l ? "is-managed" : ""}`,
      children: [
        /* @__PURE__ */ v("header", { className: "ws-node-group-header", children: [
          /* @__PURE__ */ c("span", { className: "ws-node-group-icon", "aria-hidden": "true", children: /* @__PURE__ */ c(Wl, { size: 15 }) }),
          z ? /* @__PURE__ */ c(
            "input",
            {
              ref: X,
              className: "ws-node-group-title-input nodrag nowheel",
              value: U,
              maxLength: 64,
              "aria-label": "分组名称",
              onChange: (T) => A(T.target.value),
              onBlur: C,
              onKeyDown: (T) => {
                T.key === "Enter" ? (T.preventDefault(), C()) : T.key === "Escape" && (T.preventDefault(), A(e.title), F(!1));
              }
            }
          ) : /* @__PURE__ */ c(
            We,
            {
              label: l ? "名称由分镜脚本管理" : "双击重命名",
              children: /* @__PURE__ */ c(
                "strong",
                {
                  className: "ws-node-group-title",
                  onDoubleClick: (T) => {
                    T.preventDefault(), T.stopPropagation(), !(l || !u) && F(!0);
                  },
                  children: e.title || "未命名分组"
                }
              )
            }
          ),
          /* @__PURE__ */ c("span", { className: "ws-node-group-count", children: Z ? `${r}/${n}` : `${t} 个节点` }),
          i === "waiting" ? /* @__PURE__ */ c("span", { className: "ws-node-group-status", children: "等待反馈" }) : i === "error" ? /* @__PURE__ */ c("span", { className: "ws-node-group-status", children: o > 0 ? `失败 ${o}` : "运行失败" }) : b ? /* @__PURE__ */ c(We, { label: b, children: /* @__PURE__ */ c("span", { className: "ws-node-group-status", children: "等待前置" }) }) : a ? /* @__PURE__ */ c("span", { className: "ws-node-group-status", children: "等待调度" }) : s > 0 ? /* @__PURE__ */ c(We, { label: "上游素材或提示词已变化；当前结果仍可使用，重新运行可更新", children: /* @__PURE__ */ v("span", { className: "ws-node-group-status is-stale", children: [
            "可更新 ",
            s
          ] }) }) : n > 0 && r === n ? /* @__PURE__ */ v("span", { className: "ws-node-group-status is-complete", children: [
            /* @__PURE__ */ c(zo, { size: 12 }),
            "已完成"
          ] }) : null,
          m ? /* @__PURE__ */ c(We, { label: "编辑分镜结构", children: /* @__PURE__ */ c(
            "button",
            {
              type: "button",
              className: "ws-node-group-edit nodrag nopan",
              onClick: (T) => {
                T.preventDefault(), T.stopPropagation(), m();
              },
              "aria-label": "编辑分镜结构",
              children: /* @__PURE__ */ c(ac, { size: 13 })
            }
          ) }) : null,
          /* @__PURE__ */ c(We, { label: de, children: /* @__PURE__ */ c(
            "button",
            {
              type: "button",
              className: "ws-node-group-run nodrag nopan",
              disabled: k,
              onClick: (T) => {
                T.preventDefault(), T.stopPropagation(), N?.();
              },
              "aria-label": "运行分组",
              children: Z ? /* @__PURE__ */ c(sn, { size: 14, className: "ws-spin" }) : /* @__PURE__ */ c(Ur, { size: 14 })
            }
          ) })
        ] }),
        /* @__PURE__ */ c("div", { className: "ws-node-group-surface", "aria-hidden": "true" }),
        w
      ]
    }
  );
}
function _d({
  output: e,
  fallback: t,
  preview: n,
  mediaLabel: r,
  className: o = "",
  style: s,
  onOpen: i,
  onOpenIntent: a,
  resizeControls: d,
  children: l,
  customContentIsPureMedia: u = !1,
  followContent: m = !1,
  followKey: N
}) {
  const b = Q(null), w = Q(!0), z = ur(e, n), F = l != null, U = u || !F && !z && Xn(n), A = !!i && (!F || u), X = [
    "ws-result-view",
    U ? "" : "nodrag",
    "nopan",
    "nowheel",
    U ? "has-pure-media" : "",
    o
  ].filter(Boolean).join(" ");
  return ce(() => {
    if (!m) {
      w.current = !0;
      return;
    }
    const k = b.current;
    k && w.current && (k.scrollTop = k.scrollHeight);
  }, [m, N]), /* @__PURE__ */ v(
    "div",
    {
      role: A ? "button" : void 0,
      tabIndex: A ? 0 : void 0,
      className: X,
      style: s,
      onPointerDown: (k) => {
        (!U || iy(k)) && k.stopPropagation();
      },
      onClick: (k) => {
        k.stopPropagation(), !(!i || Ts(k.target, k.currentTarget) || sy(k)) && (k.preventDefault(), i());
      },
      onPointerEnter: i ? a : void 0,
      onFocus: i ? a : void 0,
      onKeyDown: (k) => {
        k.stopPropagation(), !(!i || Ts(k.target, k.currentTarget) || k.key !== "Enter" && k.key !== " ") && (k.preventDefault(), i());
      },
      children: [
        /* @__PURE__ */ c(
          "div",
          {
            ref: b,
            className: "ws-result-view-scroll ws-node-scroll-content nowheel",
            onScroll: (k) => {
              if (!m)
                return;
              const C = k.currentTarget;
              w.current = C.scrollHeight - C.scrollTop - C.clientHeight < 16;
            },
            children: F ? l : z ? /* @__PURE__ */ c(
              lr,
              {
                output: e,
                fallback: t,
                mediaGridKind: n.videoUrl ? "video" : n.imageUrl ? "image" : void 0,
                className: "ws-canvas-content-view ws-result-content-view"
              }
            ) : /* @__PURE__ */ c(ry, { preview: n, label: r ?? t })
          }
        ),
        d
      ]
    }
  );
}
function ry({
  preview: e,
  label: t
}) {
  return e.imageUrl ? /* @__PURE__ */ v("figure", { className: "ws-result-view-media", children: [
    /* @__PURE__ */ c(
      "img",
      {
        src: e.imageUrl,
        alt: t || "图片结果",
        loading: "lazy",
        decoding: "async"
      }
    ),
    t ? /* @__PURE__ */ c("figcaption", { children: t }) : null
  ] }) : e.videoUrl ? /* @__PURE__ */ v("figure", { className: "ws-result-view-media", children: [
    /* @__PURE__ */ c(
      go,
      {
        src: e.videoUrl,
        muted: !0,
        playsInline: !0,
        preload: "metadata",
        controls: !0
      },
      e.videoUrl
    ),
    t ? /* @__PURE__ */ c("figcaption", { children: t }) : null
  ] }) : e.audioUrl ? /* @__PURE__ */ v("div", { className: "ws-result-view-audio", children: [
    /* @__PURE__ */ c("audio", { src: e.audioUrl, controls: !0, preload: "metadata" }),
    t ? /* @__PURE__ */ c("span", { children: t }) : null
  ] }) : e.fileUrl ? /* @__PURE__ */ v(
    "a",
    {
      className: "ws-result-view-file",
      href: e.fileUrl,
      target: "_blank",
      rel: "noreferrer",
      children: [
        /* @__PURE__ */ c(Ys, { size: 16 }),
        /* @__PURE__ */ c("span", { children: t || "查看文件" })
      ]
    }
  ) : /* @__PURE__ */ c(
    lr,
    {
      output: oy(t),
      fallback: t,
      className: "ws-canvas-content-view ws-result-content-view"
    }
  );
}
function oy(e) {
  return e ? { text: e } : void 0;
}
function Xn(e) {
  return !!(e.imageUrl || e.videoUrl || e.audioUrl || e.fileUrl);
}
function Ts(e, t) {
  if (!(e instanceof Element))
    return !1;
  const n = e.closest(
    "a, button, input, textarea, select, audio, video[controls], [role='button'], .ws-resize-control"
  );
  return !!(n && n !== t);
}
function sy(e) {
  const t = e.currentTarget.querySelector(
    ":scope > .ws-result-view-scroll"
  );
  if (!t || t.scrollHeight <= t.clientHeight)
    return !1;
  const n = t.getBoundingClientRect();
  return e.clientX >= n.right - 10;
}
function iy(e) {
  const t = e.target;
  if (!(t instanceof Element))
    return !1;
  const n = t.closest("video[controls]");
  if (n instanceof HTMLVideoElement) {
    const r = n.getBoundingClientRect(), o = Math.min(56, r.height * 0.25);
    return e.clientY >= r.bottom - o;
  }
  return Ts(t, e.currentTarget);
}
function ay({
  references: e,
  disabled: t = !1,
  onChange: n
}) {
  if (e.length === 0)
    return null;
  const r = (o, s) => {
    n(
      e.map(
        (i) => i.key === o ? { ...i, ...s } : i
      )
    );
  };
  return /* @__PURE__ */ v("section", { className: "ws-storyboard-input-references", "aria-label": "脚本参考素材", children: [
    /* @__PURE__ */ v("header", { children: [
      /* @__PURE__ */ c("strong", { children: "参考素材" }),
      /* @__PURE__ */ c("span", { children: "为每个素材指定用途，生成后仍可在脚本详情中调整。" })
    ] }),
    /* @__PURE__ */ c("div", { className: "ws-storyboard-input-reference-list", children: e.map((o) => /* @__PURE__ */ v("div", { className: "ws-storyboard-input-reference", children: [
      /* @__PURE__ */ c("span", { className: "ws-storyboard-input-reference-kind", children: /* @__PURE__ */ c(Nl, { kind: o.kind }) }),
      /* @__PURE__ */ c(We, { label: o.label, children: /* @__PURE__ */ c("span", { className: "ws-storyboard-input-reference-name", children: o.label }) }),
      /* @__PURE__ */ c(
        "select",
        {
          className: "nodrag nopan",
          value: o.purpose,
          disabled: t,
          "aria-label": `${o.label}的参考用途`,
          onChange: (s) => r(o.key, {
            purpose: s.target.value
          }),
          children: Bl(o.kind).map((s) => /* @__PURE__ */ c("option", { value: s.value, children: s.label }, s.value))
        }
      ),
      /* @__PURE__ */ c(
        "input",
        {
          className: "nodrag nopan",
          value: o.instruction,
          disabled: t,
          "aria-label": `${o.label}的补充说明`,
          placeholder: `${Vl[o.purpose]}说明（可选）`,
          onChange: (s) => r(o.key, {
            instruction: s.target.value
          })
        }
      )
    ] }, o.key)) })
  ] });
}
function cy({
  output: e,
  status: t,
  started: n = !1,
  generatedShotCount: r = 0,
  onOpenDetail: o
}) {
  if (t === "running")
    return /* @__PURE__ */ v("div", { className: "ws-storyboard-node-state is-running", "aria-live": "polite", children: [
      /* @__PURE__ */ v("div", { className: "ws-storyboard-node-skeleton", "aria-hidden": "true", children: [
        /* @__PURE__ */ c("span", {}),
        /* @__PURE__ */ c("span", {}),
        /* @__PURE__ */ c("span", {})
      ] }),
      /* @__PURE__ */ c("strong", { children: n ? r > 0 ? `分镜正在生成，已生成 ${r} 个分镜` : "分镜正在生成" : "分镜等待生成" })
    ] });
  if (t === "error")
    return /* @__PURE__ */ c(
      as,
      {
        icon: /* @__PURE__ */ c(bs, { size: 28 }),
        title: "分镜生成失败",
        description: "请检查输入后重新生成",
        tone: "error"
      }
    );
  if (t === "empty")
    return /* @__PURE__ */ c(
      as,
      {
        icon: /* @__PURE__ */ c(Wa, { size: 28 }),
        title: "分镜等待生成",
        description: "运行后展示镜头卡片，详情中可以编辑"
      }
    );
  const s = wr(e);
  if (!s)
    return /* @__PURE__ */ c(
      as,
      {
        icon: /* @__PURE__ */ c(bs, { size: 28 }),
        title: "分镜格式异常",
        description: "打开详情查看原始结果或重新生成",
        tone: "error",
        onOpenDetail: o
      }
    );
  const i = sc(s);
  return /* @__PURE__ */ v("section", { className: "ws-storyboard-node is-complete", children: [
    /* @__PURE__ */ v("header", { className: "ws-storyboard-node-summary", children: [
      /* @__PURE__ */ v("div", { children: [
        /* @__PURE__ */ c("strong", { children: s.title || "分镜脚本" }),
        /* @__PURE__ */ v("span", { className: "ws-storyboard-node-complete", children: [
          /* @__PURE__ */ c(zo, { size: 14 }),
          i ? "已确认" : "草稿"
        ] })
      ] }),
      /* @__PURE__ */ v("span", { children: [
        s.shots.length,
        " 个镜头 ·",
        " ",
        Ul(s),
        " 秒",
        Oi(s) > 0 ? ` · ${Oi(s)} 条语音` : ""
      ] })
    ] }),
    /* @__PURE__ */ v("div", { className: "ws-storyboard-node-body nowheel", children: [
      /* @__PURE__ */ c("div", { className: "ws-storyboard-node-cards", children: s.shots.slice(0, 4).map((a, d) => /* @__PURE__ */ c(
        Gu,
        {
          shot: a,
          index: d,
          storyboard: s,
          onOpen: o
        },
        a.id
      )) }),
      s.shots.length > 4 ? /* @__PURE__ */ v("span", { className: "ws-storyboard-node-more", children: [
        "还有 ",
        s.shots.length - 4,
        " 个镜头"
      ] }) : null
    ] }),
    o ? /* @__PURE__ */ c("footer", { className: "ws-storyboard-node-actions", children: /* @__PURE__ */ c(Nd, { onOpenDetail: o }) }) : null
  ] });
}
function as({
  icon: e,
  title: t,
  description: n,
  tone: r = "default",
  onOpenDetail: o
}) {
  return /* @__PURE__ */ v(
    "div",
    {
      className: `ws-storyboard-node-state is-${r}`,
      role: r === "error" ? "alert" : void 0,
      children: [
        /* @__PURE__ */ c("span", { className: "ws-storyboard-node-state-icon", children: e }),
        /* @__PURE__ */ c("strong", { children: t }),
        /* @__PURE__ */ c("span", { children: n }),
        o ? /* @__PURE__ */ c(Nd, { label: "打开详情", onOpenDetail: o }) : null
      ]
    }
  );
}
function Nd({
  label: e = "打开完整分镜",
  onOpenDetail: t
}) {
  return /* @__PURE__ */ v(
    "button",
    {
      type: "button",
      className: "ws-storyboard-detail-button nodrag nopan",
      onMouseDown: (n) => n.stopPropagation(),
      onClick: (n) => {
        n.preventDefault(), n.stopPropagation(), t();
      },
      children: [
        /* @__PURE__ */ c(Ua, { size: 13 }),
        /* @__PURE__ */ c("span", { children: e })
      ]
    }
  );
}
function Sn(e) {
  let t;
  const n = () => (t || (t = e().catch((r) => {
    throw t = void 0, r;
  })), t);
  return {
    Component: Ru(n),
    preload: () => n().then(
      () => {
      },
      () => {
      }
    )
  };
}
const dy = Sn(
  () => import("./interaction-panel-Cqu34wUb.js").then((e) => e.default).then((e) => ({
    default: e.AgentInteractionPanel
  }))
), uy = dy.Component, Id = Sn(
  () => import("./space-add-node-menu-CF015NCo.js").then((e) => ({
    default: e.AddNodeMenu
  }))
), ly = Id.Component, Sd = Id.preload, vd = Sn(
  () => import("./asset-page-C3BMxAAc.js").then((e) => e.a).then((e) => ({
    default: e.AssetBrowser
  }))
), fy = vd.Component, py = vd.preload, Cd = Sn(
  () => import("./stream-request-HZQ_bZ-N.js").then((e) => e.b).then((e) => ({
    default: e.AssetPickerDialog
  }))
), ca = Cd.Component, da = Cd.preload, kd = Sn(
  () => import("./space-run-history-5TYZ991Q.js").then((e) => ({
    default: e.CanvasRunHistoryDrawer
  }))
), my = kd.Component, gy = kd.preload, yy = Sn(
  () => import("./space-agent-result-BfCkh8V1.js").then((e) => ({
    default: e.CanvasAgentResultContent
  }))
), hy = yy.Component, Rd = Sn(
  () => import("./node-detail-dialog-CR2x0PmR.js").then((e) => ({
    default: e.NodeDetailDialog
  }))
), wy = Rd.Component, Qn = Rd.preload, xd = Sn(
  () => import("./space-prompt-composer-DJXsUWzF.js").then((e) => ({
    default: e.PromptComposer
  }))
), ua = xd.Component, la = xd.preload, by = Sn(
  () => import("./space-video-compose-view-BzR4cTvy.js").then((e) => e.s).then((e) => ({
    default: e.VideoComposeView
  }))
), _y = by.Component, { normalizeAgentResultOutputValue: Ny } = za(
  "@/lib/agent-result-protocol"
), yi = za(
  "@/components/agent/stream-request-params"
), dr = yi.filterActivePowerParams || ((e) => e), fa = yi.isPowerParamConditionController || (() => !1), Iy = yi.shouldDisplayPowerParam || (() => !0), Sy = {}, pa = [], vy = [], Ro = [], Cy = 800;
function Dn(e, t) {
  return Ms(e, /* @__PURE__ */ new Set([t]));
}
function ky(e) {
  const t = Q([]), n = Q(0), r = $(() => {
    n.current && (window.cancelAnimationFrame(n.current), n.current = 0);
    const s = t.current;
    t.current = [], s.length !== 0 && e(
      (i) => s.reduce((a, d) => d(a), i)
    );
  }, [e]), o = $(
    (s) => {
      t.current.push(s), !n.current && (n.current = window.requestAnimationFrame(() => {
        n.current = 0, r();
      }));
    },
    [r]
  );
  return ce(
    () => () => {
      n.current && window.cancelAnimationFrame(n.current), t.current = [];
    },
    []
  ), se(() => ({ enqueue: o, flush: r }), [o, r]);
}
function Ms(e, t) {
  let n = null;
  for (const r of t)
    Object.prototype.hasOwnProperty.call(e, r) && (n ||= { ...e }, delete n[r]);
  return n || e;
}
function Td(e, t) {
  const n = /* @__PURE__ */ new Set();
  for (const r of t)
    if (n.add(r.id), r.type === "group")
      for (const o of lc(e, r.id))
        n.add(o.id);
  return n;
}
function Ry(e) {
  return Object.values(e).some(Qt);
}
function Qt(e) {
  return e?.status === "running" || e?.status === "waiting";
}
function xy(e) {
  return e instanceof Element && e.classList.contains("react-flow__pane");
}
function Ty(e, t, n) {
  return {
    left: Math.min(e.x, t.x) - n.left,
    top: Math.min(e.y, t.y) - n.top,
    width: Math.abs(t.x - e.x),
    height: Math.abs(t.y - e.y)
  };
}
function My(e, t, n) {
  const r = {
    left: Math.min(t.x, n.x),
    top: Math.min(t.y, n.y),
    right: Math.max(t.x, n.x),
    bottom: Math.max(t.y, n.y)
  };
  return e.filter((o) => {
    const s = bu(o), i = o.x + s.width, a = o.y + s.height;
    return o.type === "group" ? r.left <= o.x && r.top <= o.y && r.right >= i && r.bottom >= a : r.left <= i && r.right >= o.x && r.top <= a && r.bottom >= o.y;
  }).map((o) => o.id);
}
function Ay(e, t) {
  return [.../* @__PURE__ */ new Set([...e, ...t])];
}
const Dy = {
  id: 0,
  name: "上传",
  key: "files",
  type: "files",
  usage: 2,
  max_files: 6
}, Py = [Dy], zy = {
  workSpace: Gw,
  storyboardFrame: Hg
}, Fy = {
  animated: Vm
};
function Ey({
  onInitialLoadComplete: e
}) {
  const t = xu(), n = Mu(), r = se(() => Zw(), []), o = se(() => new Ap(), []), [s, i] = j(null), [a, d] = j(0), l = Q(0), [u, m] = j(null), N = Q(null), [b, w] = j([]), z = b[b.length - 1] || "", [F, U] = j({}), A = Q(F), [X, Z] = j("create"), { resolvedTheme: de, setTheme: k } = Tu();
  wl(n.site.appearance, de);
  const [C, T] = j(null), [re, ne] = j(!0), me = Q(!0), [ie, H] = j({}), J = ky(H), [ae, xe] = j({}), [le, Be] = j(null), [ye, Ce] = j(), [Pe, Y] = j(
    null
  ), [Gt, fe] = j(null), [Qe, gt] = j(!1), [cn, dn] = j(""), [et, _t] = j(null), [Fe, Nt] = j(""), [Ke, kt] = j([]), [Xe, un] = j([]), [Nr, qe] = j(!1), [nr, Ne] = j(""), [Rt, Ae] = j(!1), [Ee, Dt] = j(1), [Bn, yt] = j(!1), [Ge, ht] = j(null), [Pt, tt] = j(!1), Ve = Q(null), nt = Q(!1), tn = Q(/* @__PURE__ */ new Set()), nn = Q(/* @__PURE__ */ new Set()), It = Q(/* @__PURE__ */ new Set()), Te = Q([]), ft = Q(!1), St = Q(!1), ln = Q([0]), vn = Q(null), Cn = Q(/* @__PURE__ */ new Map()), ze = Q(
    /* @__PURE__ */ new Map()
  ), wt = Q(null), {
    roles: pt,
    powers: Me,
    powerCategories: zt,
    loaded: kn,
    required: xt,
    load: M
  } = ey({
    space: s,
    canvases: F,
    cache: o
  });
  ce(() => {
    A.current = F;
  }, [F]), ce(() => {
    l.current = a;
  }, [a]), ce(() => {
    Te.current = Ke;
  }, [Ke]);
  const W = $((f) => {
    G.error(f instanceof Error ? f.message : "保存画布失败");
  }, []), { markCanvasDirty: B, resetCanvasAutosave: ue, canvasSaveStatus: he } = xp({
    projectId: r,
    enabled: !!s,
    canvases: F,
    setCanvases: U,
    onError: W
  });
  ce(() => {
    if (It.current.size === 0)
      return;
    const f = [...It.current];
    It.current.clear();
    for (const g of f)
      B(g);
  }, [F, B]);
  const ke = $(async () => {
    if (!r) {
      Nt("缺少作品 ID"), ne(!1);
      return;
    }
    ne(!0), Nt("");
    try {
      const f = await pp(
        r,
        l.current
      ), g = mw(
        f.canvases || {},
        f.assets || []
      ), y = Number(f.initialAssetCateId || 0) || kf(f);
      i(f), A.current = g, U(g), ue(g), l.current = y, d(y), m(null), N.current = null, tn.current = /* @__PURE__ */ new Set(), nn.current = /* @__PURE__ */ new Set(), Te.current = [], kt([]), un([]), Dt(1), yt(!1), ln.current = [0], xn(
        r,
        f,
        g,
        "recovery",
        { assetCateId: y }
      );
    } catch (f) {
      Nt(f instanceof Error ? f.message : "加载创作空间失败");
    } finally {
      ne(!1);
    }
  }, [r, ue]), rt = $((f) => {
    Y(f);
  }, []);
  ce(() => {
    ke();
  }, [ke]), ce(() => {
    re || !me.current || (me.current = !1, e());
  }, [re, e]);
  const ot = s?.assetCates, Ht = se(() => s ? _c(s) : [], [s]), rn = s ? s.assetCates.length > 0 : !1, q = se(
    () => s ? uo(s, a) : null,
    [a, s]
  ), Rn = se(
    () => s && q ? Rf(s, q.id) : [],
    [q, s]
  ), Wt = se(() => pt.filter(xf), [pt]), qo = se(() => Me.filter(Tf), [Me]), Go = se(() => Rn, [Rn]), we = se(
    () => q ? F[String(q.id)] || ro(q.id) : ro(0),
    [q, F]
  ), rr = se(
    () => Object.entries(F).map(
      ([f, g]) => `${f}:${xg(g)}`
    ).join("|"),
    [F]
  ), Ze = se(
    () => Ly(we, ae),
    [we, ae]
  ), Gr = Hn, Hr = se(
    () => mm({
      nodes: Ze.nodes,
      assets: s?.assets || [],
      assetCateId: q?.id || 0,
      nodeOutput: In,
      nodePreview: _r,
      assetPreview: (f) => {
        const g = f.version?.content ?? f.name, y = jn(
          g,
          String(f.kind || "")
        );
        return En(y) || (y.text = f.name), y;
      },
      nodeHasResult: Mt
    }),
    [q?.id, Ze.nodes, s?.assets]
  ), Wr = se(
    () => uw(Hr),
    [Hr]
  );
  ce(() => {
    !ot || xt && !kn || U((f) => {
      let g = f;
      for (const [y, S] of Object.entries(f)) {
        const _ = Number(y || S.assetCateId || 0), O = Rs({
          canvas: S,
          assetCate: Ss(ot, _),
          powers: Me
        }), E = Ta(O, _);
        Ma(S, E) || (g === f && (g = { ...f }), g[y] = E, It.current.add(_));
      }
      return g;
    });
  }, [
    ot,
    kn,
    xt,
    Me,
    rr
  ]);
  const He = $(
    (f = "") => {
      da(), dn(f), Ve.current = we.nodes.find((g) => g.id === f) || (Ve.current?.id === f ? Ve.current : null), T(null), Z("create"), gt(!0);
    },
    [we.nodes]
  ), fn = $(
    (f, g) => {
      if (!Number.isInteger(f) || f < 0)
        return;
      const y = (O) => {
        const E = String(f), L = O[E] || ro(f), ee = Ta(
          g(L),
          f
        );
        return Ma(L, ee) ? O : (It.current.add(f), {
          ...O,
          [E]: ee
        });
      }, S = A.current, _ = y(S);
      A.current = _, U((O) => {
        const E = O === S ? _ : y(O);
        return A.current = E, E;
      });
    },
    []
  ), Tt = $(
    (f) => {
      q && fn(q.id, f);
    },
    [q, fn]
  ), Ir = $(
    (f, g, y) => {
      xe(
        (S) => Ky(S, g, y)
      ), fn(f, (S) => {
        const _ = {
          ...S,
          nodes: S.nodes.map(
            (E) => E.id === g ? { ...E, ...y } : E
          )
        };
        return ot ? Rs({
          canvas: _,
          assetCate: Ss(ot, f),
          powers: Me
        }) : _;
      });
    },
    [ot, Me, fn]
  ), mt = $(
    (f, g) => {
      Ir(Number(q?.id || 0), f, g), Be(
        (y) => y?.id === f ? { ...y, ...g } : y
      );
    },
    [q?.id, Ir]
  ), pn = $(
    (f, g, y) => {
      if (!xh(g, y))
        return;
      const S = Yd(y), _ = `${g.id}:${S}`;
      if (tn.current.has(_))
        return;
      tn.current.add(_);
      const O = g.title.trim(), E = A.current[String(f)];
      hp({
        projectId: r,
        nodeKey: g.id,
        versionId: S,
        prompt: Rh(g, E)
      }).then((L) => {
        const ee = L.title.trim();
        !ee || ee === O || L.versionId !== S || fn(f, (te) => {
          const K = te.nodes.find(
            (ge) => ge.id === g.id
          );
          return !K || K.titleMode !== "auto" || K.title.trim() !== O || !Wd(K) ? te : {
            ...te,
            nodes: te.nodes.map(
              (ge) => ge.id === g.id ? { ...ge, title: ee } : ge
            )
          };
        });
      }).catch(() => {
        tn.current.delete(_);
      });
    },
    [r, fn]
  ), mn = $(
    async (f) => {
      const g = Number(f.assetCate.id || 0);
      g && B(g);
    },
    [B]
  ), Yr = $(
    (f, g) => {
      Tt((y) => ({
        ...y,
        nodes: y.nodes.map(
          (S) => S.id === f ? {
            ...S,
            composerDraft: jo(g)
          } : S
        )
      }));
    },
    [Tt]
  ), Xr = $(
    (f) => {
      Tt((g) => {
        const y = g.edges.filter((S) => S.id !== f);
        return y.length === g.edges.length ? g : { ...g, edges: y };
      });
    },
    [Tt]
  ), Ho = $(
    (f, g) => {
      Qn(), Ce(g), Be(f);
    },
    []
  ), be = $(
    (f, g) => {
      xe((y) => {
        const S = we.nodes.find(
          (E) => E.id === f
        );
        if (!S)
          return y;
        const _ = y[f] || {}, O = {
          ...S,
          ..._
        };
        return {
          ...y,
          [f]: {
            ..._,
            feedbackRequests: g(Wn(O))
          }
        };
      });
    },
    [we.nodes]
  ), Yt = $((f) => {
    const g = new Set(f.filter(Boolean));
    if (g.size !== 0) {
      if (wt.current && g.has(wt.current.nodeId)) {
        const y = wt.current;
        wt.current = null, y.reject(new Error(qc));
      }
      ht(
        (y) => y && g.has(y.node.id) ? null : y
      ), xe((y) => {
        const S = { ...y };
        let _ = !1;
        for (const O of g) {
          const E = S[O] || {};
          S[O] = {
            ...E,
            feedbackRequests: []
          }, _ = !0;
        }
        return _ ? S : y;
      });
    }
  }, []), st = $((f) => {
    !f || !f.id || i((g) => g && {
      ...g,
      assets: Ki(g.assets, [f])
    });
  }, []), jt = $(
    ({ node: f, prompt: g }) => {
      const y = wi(f, g), S = zs(
        Wn(f),
        y
      );
      return be(
        f.id,
        (_) => zs(_, y)
      ), tt(!1), new Promise((_, O) => {
        wt.current = {
          nodeId: f.id,
          recordId: y.id,
          resolve: _,
          reject: O
        }, ht({
          node: { ...f, feedbackRequests: S },
          recordId: y.id,
          prompt: g
        });
      });
    },
    [be]
  ), Sr = $(
    async (f) => {
      const g = wt.current;
      if (!(!g || Pt)) {
        tt(!0);
        try {
          await g.submit?.(f), be(
            g.nodeId,
            (y) => xm(y, g.recordId, f)
          ), wt.current = null, ht(null), g.resolve(f);
        } catch (y) {
          G.error(y instanceof Error ? y.message : "提交反馈失败");
        } finally {
          tt(!1);
        }
      }
    },
    [be, Pt]
  ), Bt = $(() => {
    ht(null);
  }, []), Wo = $(
    (f, g) => {
      if (wt.current?.nodeId === f.id && wt.current.recordId === g.id && g.status === "pending") {
        ht({
          node: f,
          recordId: g.id,
          prompt: g.prompt
        });
        return;
      }
      if (g.status === "pending") {
        const y = kh(
          Te.current,
          f,
          g
        );
        if (y) {
          tt(!1), wt.current = {
            nodeId: f.id,
            recordId: g.id,
            resolve: () => {
            },
            reject: () => {
            },
            submit: async (S) => {
              await Gd(
                r,
                y.run,
                y.pending,
                y.prompt,
                S
              ), G.success("已提交反馈，流程继续执行"), window.setTimeout(() => vn.current?.(), 0);
            }
          }, ht({
            node: f,
            recordId: g.id,
            prompt: y.prompt
          });
          return;
        }
      }
      ht({
        node: f,
        recordId: g.id,
        prompt: {
          ...g.prompt,
          values: g.values || g.prompt.values || {}
        }
      });
    },
    [r]
  ), vr = $(
    async (f) => {
      if (!s || !q)
        return;
      let g = null;
      try {
        Yt(
          zp(
            f.id,
            Ze.nodes,
            Ze.edges
          )
        ), g = {
          projectId: r,
          assetCate: q,
          space: s,
          startNode: f,
          nodes: Ze.nodes,
          edges: Ze.edges,
          viewport: we.viewport,
          onNodeResult: mt,
          onAssetCreated: st,
          setRunningNode: H,
          runningNodeBatcher: J,
          requestFlowFeedback: jt,
          requestNodeTitle: (y, S) => pn(q.id, y, S)
        }, await cs(g), await mn(g), G.success("开始节点执行完成");
      } catch (y) {
        if (km(y))
          return;
        const S = y instanceof Error ? y.message : "开始节点执行失败";
        H((_) => ({
          ..._,
          [f.id]: {
            nodeId: f.id,
            title: f.title,
            startedAt: Date.now(),
            progress: 92,
            status: "error"
          }
        })), G.error(S), window.setTimeout(() => {
          H((_) => Dn(_, f.id));
        }, 1400);
      } finally {
        await xn(
          r,
          s,
          A.current,
          "recovery",
          { runIds: [Number(g?.canvasRun?.run_id || 0)] }
        );
      }
    },
    [
      q,
      we.viewport,
      Ze.edges,
      Ze.nodes,
      F,
      Yt,
      r,
      pn,
      jt,
      J,
      mn,
      H,
      s,
      mt,
      st
    ]
  ), Yo = $(
    async (f) => {
      if (!s || !q)
        return;
      const g = Number(q.id || 0), y = A.current[String(g)] || we, S = y.nodes.find(
        (te) => te.id === f
      );
      if (!S) {
        G.error("分镜脚本节点不存在");
        return;
      }
      const _ = hd(
        y.nodes,
        Mt
      ).find((te) => te.sourceNodeId === f);
      if (!_) {
        G.error("当前分镜脚本尚未生成制作组");
        return;
      }
      const O = wd(
        _,
        y.nodes,
        Mt
      );
      if (O.blockedReason) {
        G.error(O.blockedReason);
        return;
      }
      const E = mi(f), L = {
        projectId: r,
        assetCate: q,
        space: s,
        startNode: S,
        executionScope: "storyboard_frame",
        patchStartNodeResult: !1,
        nodes: y.nodes,
        edges: y.edges,
        viewport: y.viewport,
        onNodeResult: mt,
        onAssetCreated: st,
        setRunningNode: H,
        runningNodeBatcher: J,
        requestFlowFeedback: jt,
        requestNodeTitle: (te, K) => pn(g, te, K)
      };
      Yt(O.pendingNodeIds), H((te) => ({
        ...te,
        [E]: {
          nodeId: E,
          title: `${S.title || "分镜脚本"}制作区`,
          startedAt: Date.now(),
          progress: 0,
          status: "running"
        }
      }));
      let ee = 650;
      try {
        await cs(L), G.success("制作区执行完成");
      } catch (te) {
        ee = 1400;
        const K = te instanceof Error ? te.message : "制作区执行失败";
        H((ge) => ({
          ...ge,
          [E]: {
            ...ge[E] || {
              nodeId: E,
              title: `${S.title || "分镜脚本"}制作区`,
              startedAt: Date.now(),
              progress: 0
            },
            status: "error"
          }
        })), G.error(K);
      } finally {
        const te = new Set(
          (L.canvasRun?.node_results || []).filter((K) => _n(K) === "success").map((K) => K.node_key).filter(Boolean)
        );
        te.size > 0 && (Tt(
          (K) => ma({
            canvas: K,
            sourceNodeId: f,
            successfulNodeIds: te,
            assetCate: q,
            powers: Me
          })
        ), await mn(L)), window.setTimeout(() => {
          H((K) => Dn(K, E));
        }, ee), await xn(
          r,
          s,
          A.current,
          "recovery",
          { runIds: [Number(L.canvasRun?.run_id || 0)] }
        );
      }
    },
    [
      we,
      q,
      Yt,
      mn,
      Me,
      r,
      pn,
      jt,
      J,
      s,
      Tt,
      mt,
      st
    ]
  ), Zr = $(
    async (f, g) => {
      if (!s || !q)
        return;
      const y = A.current[String(q.id)] || we, S = y.nodes.find((ee) => ee.id === f.id) || f, _ = Hy({
        ...S,
        composerDraft: {
          ...S.composerDraft || {},
          ...f.composerDraft || {}
        }
      }), O = y.nodes.map(
        (ee) => ee.id === _.id ? _ : ee
      ), E = iu(
        f.id,
        O,
        y.edges
      ), L = {
        projectId: r,
        assetCate: q,
        space: s,
        startNode: _,
        singleNode: !0,
        nodes: O,
        edges: y.edges,
        viewport: y.viewport,
        runInput: {
          _manual_input_context: E || void 0,
          _agent_turn_input: g?.agentInput,
          manual_node_id: f.id
        },
        onNodeResult: mt,
        onAssetCreated: st,
        setRunningNode: H,
        runningNodeBatcher: J,
        requestFlowFeedback: jt,
        requestNodeTitle: (ee, te) => pn(q.id, ee, te)
      };
      mt(_.id, { runError: "" }), H((ee) => ({
        ...ee,
        [_.id]: {
          ...ee[_.id] || {},
          nodeId: _.id,
          title: _.title,
          startedAt: ee[_.id]?.startedAt || Date.now(),
          progress: Math.max(ee[_.id]?.progress || 0, 8),
          status: "running"
        }
      }));
      try {
        await cs(L), await mn(L);
      } catch (ee) {
        throw mt(_.id, {
          runError: ee instanceof Error ? ee.message : "节点运行失败"
        }), H((te) => ({
          ...te,
          [_.id]: {
            ...te[_.id] || {
              nodeId: _.id,
              title: _.title,
              startedAt: Date.now()
            },
            progress: 92,
            status: "error"
          }
        })), window.setTimeout(() => {
          H((te) => Dn(te, _.id));
        }, 1400), ee;
      } finally {
        await xn(
          r,
          s,
          A.current,
          "recovery",
          { runIds: [Number(L.canvasRun?.run_id || 0)] }
        );
      }
    },
    [
      q,
      we,
      r,
      pn,
      jt,
      J,
      mn,
      H,
      s,
      mt,
      st
    ]
  ), Xo = $(
    async (f) => {
      if (!q)
        throw new Error("当前分类不存在");
      return Nu({
        node: f,
        projectId: r,
        assetCate: q,
        inputContext: f.inputContext || null,
        onNodeResult: mt,
        onAssetCreated: st,
        onRunStartNode: vr,
        onOpenImportPicker: He
      });
    },
    [
      q,
      He,
      r,
      vr,
      mt,
      st
    ]
  );
  ce(() => {
    s && p(
      Ke,
      we,
      a,
      s
    );
  }, [we, a, Ke, s]);
  async function Jr(f) {
    if (N.current != null)
      return !1;
    const g = String(f);
    let y = !1;
    if (!Object.prototype.hasOwnProperty.call(A.current, g)) {
      N.current = f, m(f);
      try {
        const _ = await mp({
          projectId: r,
          assetCateId: f
        }), O = pu(
          jf(_.canvas, Me),
          _.assets
        );
        i(
          (L) => L && {
            ...L,
            assets: Ki(L.assets, _.assets)
          }
        );
        const E = {
          ...A.current,
          [g]: O
        };
        A.current = E, U(E), y = !0;
      } catch (_) {
        return G.error(_ instanceof Error ? _.message : "加载分类画布失败"), !1;
      } finally {
        N.current = null, m(null);
      }
    }
    if (l.current = f, d(f), w([]), fe(null), T(null), !s)
      return !0;
    const S = A.current[String(f)] || ro(f);
    return p(Ke, S, f, s), y && xn(
      r,
      s,
      A.current,
      "recovery",
      { assetCateId: f }
    ), !0;
  }
  function Cr(f) {
    fe((g) => ({
      nodeId: f,
      nonce: (g?.nonce || 0) + 1
    }));
  }
  const Zo = $((f) => {
    fe((g) => !g || g.nodeId !== f.nodeId || g.nonce !== f.nonce ? g : null);
  }, []);
  async function xn(f, g, y, S, _ = {}) {
    if (!ft.current) {
      ft.current = !0;
      try {
        const O = S === "active" ? mh(Te.current) : [], E = (_.runIds || []).filter((Oe) => Oe > 0), L = E.length > 0 ? E : S === "active" ? O.map((Oe) => Number(Oe.run_id || 0)) : [], ee = S === "recovery" && L.length === 0;
        let te = await es({
          projectId: f,
          scope: S,
          assetCateId: _.assetCateId,
          runIds: L,
          summaryOnly: ee
        }), K = ds(te.items);
        if (ee) {
          const Oe = gh(K, y);
          Oe.length === 0 ? K = [] : (te = await es({
            projectId: f,
            scope: S,
            assetCateId: _.assetCateId,
            runIds: Oe
          }), K = ds(te.items));
        }
        if (S === "active" && O.length > 0) {
          const Oe = new Set(K.map(To)), on = O.filter(
            (Vt) => !Oe.has(To(Vt))
          );
          if (on.length > 0) {
            const Vt = await Promise.all(
              on.map(async (Tn) => {
                try {
                  const Et = await Tc({
                    projectId: f,
                    executionId: Number(Tn.execution_id || 0),
                    runId: Number(Tn.run_id || 0),
                    requestId: String(Tn.request_id || "")
                  });
                  return Ud(Et);
                } catch {
                  return null;
                }
              })
            );
            K = ba(
              K,
              Vt.filter(
                (Tn) => !!Tn
              )
            );
          }
        }
        const ge = ba(
          Te.current,
          K
        );
        Te.current = ge, kt(ge);
        for (const [Oe, on] of Object.entries(y)) {
          const Vt = Number(on.assetCateId || Oe || 0);
          p(K, on, Vt, g);
        }
      } catch {
      } finally {
        ft.current = !1;
      }
    }
  }
  async function or(f, g = 0) {
    if (St.current)
      return;
    const y = ln.current[g] || 0;
    St.current = !0, qe(!0), Ne("");
    try {
      const S = await es({
        projectId: f,
        scope: "history",
        beforeId: y,
        limit: 20
      });
      un(
        ds(S.items)
      ), Dt(g + 1), yt(S.hasMore);
      const _ = ln.current.slice(0, g + 1);
      S.hasMore && S.beforeId > 0 && (_[g + 1] = S.beforeId), ln.current = _;
    } catch (S) {
      Ne(
        S instanceof Error ? S.message : "读取画布运行记录失败"
      );
    } finally {
      St.current = !1, qe(!1);
    }
  }
  const kr = se(
    () => Vd(Ke),
    [Ke]
  ), Qr = kr.length > 0;
  vn.current = s ? () => {
    xn(
      r,
      s,
      A.current,
      "active",
      { assetCateId: l.current }
    );
  } : null, ce(() => {
    if (!s || !Qr)
      return;
    const f = window.setInterval(() => {
      vn.current?.();
    }, zd);
    return () => window.clearInterval(f);
  }, [Qr, r, s]), ce(() => {
    const f = Cn.current, g = ze.current, y = [];
    if (s)
      for (const _ of kr) {
        const O = _.run, E = String(O.request_id || "").trim();
        E && y.push({
          key: `${r}:${E}`,
          requestId: E,
          run: O,
          managedNodeIds: _.managedNodeIds
        });
      }
    const S = new Set(y.map((_) => _.key));
    for (const [_, O] of f)
      S.has(_) || (O.controller.abort(), f.delete(_), g.delete(_));
    for (const _ of y) {
      const O = f.get(_.key);
      if (O) {
        O.managedNodeIds = _.managedNodeIds;
        for (const ee of $s(_.run))
          O.finishedNodeIds.add(ee);
        continue;
      }
      const E = new AbortController(), L = {
        controller: E,
        managedNodeIds: _.managedNodeIds,
        finishedNodeIds: $s(_.run)
      };
      f.set(_.key, L), Kc({
        projectId: r,
        requestId: _.requestId,
        lastId: g.get(_.key) || "0-0",
        signal: E.signal,
        onFrame: (ee) => {
          E.signal.aborted || (ee.stream_id && g.set(_.key, ee.stream_id), dh(
            { setRunningNode: H, runningNodeBatcher: J },
            ee,
            L.managedNodeIds,
            L.finishedNodeIds
          ));
        }
      }).catch(() => {
        !E.signal.aborted && f.get(_.key) === L && f.delete(_.key);
      });
    }
  }, [r, kr, J, s]), ce(
    () => () => {
      for (const f of Cn.current.values())
        f.controller.abort();
      Cn.current.clear(), ze.current.clear();
    },
    []
  );
  function p(f, g, y, S) {
    const _ = f.filter(
      (L) => ph(L, y) && !Ld(L, g)
    );
    if (_.length === 0)
      return;
    const O = /* @__PURE__ */ new Set(), E = /* @__PURE__ */ new Set();
    for (const L of _) {
      const ee = new Set(
        bi(L).filter((K) => E.has(K) ? !1 : (E.add(K), !0))
      );
      if (ee.size === 0)
        continue;
      const te = (L.node_results || []).filter((K) => {
        const ge = K.node_key;
        return !ge || !ee.has(ge) || O.has(ge) ? !1 : (O.add(ge), !0);
      });
      h(
        L,
        g,
        S,
        te,
        ee
      );
    }
  }
  function h(f, g, y, S = f.node_results || [], _) {
    const O = Nh(f, g.nodes);
    if (!O)
      return;
    const E = uo(y, g.assetCateId);
    if (!E)
      return;
    const L = {
      projectId: r,
      assetCate: E,
      space: y,
      startNode: O,
      nodes: g.nodes,
      edges: g.edges,
      viewport: g.viewport,
      onNodeResult: (te, K) => Ir(g.assetCateId, te, K),
      onAssetCreated: st,
      setRunningNode: H,
      requestFlowFeedback: jt,
      requestNodeTitle: (te, K) => pn(g.assetCateId, te, K),
      canvasRun: f
    }, ee = S.filter((te) => {
      const K = Ih(f, te);
      return !K || nn.current.has(K) ? !1 : (nn.current.add(K), !0);
    });
    Hd(L, ee), jd(L, ee), D(L, f, O), pr(L, f, _), Bd(L, f, _);
  }
  function D(f, g, y) {
    if (g.single_node || String(g.status || "").trim().toLowerCase() !== "success" || !wr(y.resultOutput))
      return;
    const S = new Set(
      (g.node_results || []).filter((_) => _n(_) === "success").map((_) => _.node_key).filter(Boolean)
    );
    fn(
      f.assetCate.id,
      (_) => ma({
        canvas: _,
        sourceNodeId: y.id,
        successfulNodeIds: S,
        assetCate: f.assetCate,
        powers: Me
      })
    );
  }
  function x(f, g, y) {
    if (!q)
      return null;
    const S = Eo(
      f,
      q,
      we.nodes.length,
      g,
      y
    ), _ = s ? uo(s, Us(S) || q.id) : q;
    f === "asset" && (S.cardinality = _.cardinality);
    const O = f === "asset" && y?.replaceSingleAssetNode ? Us(S) || Number(_.id || 0) : 0, E = O ? Ra(
      we.nodes,
      we.edges,
      O,
      y?.connectFromNodeId
    ) : null, L = E ? xa(
      we.nodes,
      we.edges,
      O,
      y?.connectFromNodeId,
      E.id
    ) : /* @__PURE__ */ new Set(), ee = E?.id || S.id, te = C?.connection;
    if (Tt((K) => {
      let ge = K.edges;
      const Oe = O ? Ra(
        K.nodes,
        K.edges,
        O,
        y?.connectFromNodeId
      ) : null;
      if (Oe) {
        const Vt = xa(
          K.nodes,
          K.edges,
          O,
          y?.connectFromNodeId,
          Oe.id
        );
        if (ge = K.edges.filter(
          (Et) => !Vt.has(Et.from) && !Vt.has(Et.to)
        ), te) {
          const Et = ka(
            te,
            Oe.id
          );
          ge = Ln(ge, Et.source, Et.target);
        } else y?.connectFromNodeId ? ge = Ln(
          ge,
          y.connectFromNodeId || "",
          Oe.id
        ) : y?.connectToNodeId && (ge = Ln(
          ge,
          Oe.id,
          y.connectToNodeId || ""
        ));
        const Tn = K.nodes.filter((Et) => !Vt.has(Et.id)).map(
          (Et) => Et.id === Oe.id ? ms(Et, S) : Et
        );
        return {
          ...K,
          nodes: Tn,
          edges: wn(Tn, ge)
        };
      }
      if (te) {
        const Vt = ka(te, S.id);
        ge = Ln(ge, Vt.source, Vt.target);
      } else y?.connectFromNodeId ? ge = Ln(
        ge,
        y.connectFromNodeId || "",
        S.id
      ) : y?.connectToNodeId && (ge = Ln(ge, S.id, y.connectToNodeId || ""));
      const on = Ns(
        [...K.nodes, S],
        S.id,
        { x: S.x, y: S.y }
      );
      return {
        ...K,
        nodes: on,
        edges: wn(on, ge)
      };
    }), E) {
      const K = ms(E, S);
      xe((ge) => {
        const Oe = { ...ge };
        for (const on of L)
          delete Oe[on];
        return Oe[E.id] = {
          ...Oe[E.id] || {},
          ...fw(K)
        }, Oe;
      });
    }
    return y?.selectCreated !== !1 && (w([ee]), Cr(ee)), Z("create"), T(null), E ? ms(E, S) : S;
  }
  function I(f, g) {
    if (!q)
      return;
    if (xs(we.nodes).has(f.id)) {
      G.info("脚本托管节点不能复制，请在分镜脚本中修改结构");
      return;
    }
    const y = ew(
      f,
      q.id,
      we.nodes.length,
      g
    );
    Tt((S) => {
      const _ = Ns(
        [...S.nodes, y],
        y.id,
        { x: y.x, y: y.y }
      );
      return {
        ...S,
        nodes: _,
        edges: wn(_, S.edges)
      };
    }), xe((S) => {
      const _ = S[f.id];
      return _ ? { ...S, [y.id]: _ } : S;
    }), w([y.id]), Cr(y.id), T(null), G.success("已复制节点");
  }
  function V(f, g = {}) {
    const y = Td(
      we.nodes,
      f
    );
    if (y.size === 0)
      return;
    const S = xs(we.nodes);
    if (!g.allowStoryboardFrame && [...y].some((_) => S.has(_))) {
      G.info("脚本托管节点不能单独删除，请编辑分镜脚本或删除整个制作区");
      return;
    }
    Tt((_) => {
      const O = _.nodes.filter((E) => !y.has(E.id));
      return {
        ..._,
        nodes: O,
        edges: wn(O, _.edges)
      };
    }), xe(
      (_) => Ms(_, y)
    ), H((_) => Ms(_, y)), w([]), fe(
      (_) => _ && y.has(_.nodeId) ? null : _
    ), Be(
      (_) => _ && y.has(_.id) ? null : _
    ), dn(
      (_) => y.has(_) ? "" : _
    ), Ve.current && y.has(Ve.current.id) && (Ve.current = null), G.success(
      f.length > 1 || y.size > 1 ? `已删除 ${y.size} 个节点` : "已删除节点"
    );
  }
  function oe(f, g) {
    x("asset", g, { asset: f });
  }
  function _e(f, g) {
    if (!f)
      return;
    const y = we.nodes.find((_) => _.id === f) || (Ve.current?.id === f ? Ve.current : null);
    if (!y)
      return;
    const S = Ao(g.version?.content) || $t(g.version?.content);
    mt(
      f,
      bn(
        {
          ...y,
          kind: g.kind || q.kind,
          assetCateId: Number(g.asset_cate_id || q.id || 0)
        },
        {
          output: S,
          asset: g
        },
        "导入资产"
      )
    ), pe(f, S);
  }
  async function pe(f, g) {
    if (!f)
      return;
    const y = we.edges.filter((S) => S.from === f).map((S) => we.nodes.find((_) => _.id === S.to)).filter(
      (S) => !!S && S.type === "function" && S.functionOption?.key === "display"
    );
    if (y.length !== 0)
      for (const S of y)
        mt(
          S.id,
          bn(S, { output: g }, "展示导入结果")
        );
  }
  function Ie(f) {
    const g = cn;
    if (!g) {
      oe(f);
      return;
    }
    if (!(we.nodes.find((S) => S.id === g) || (Ve.current?.id === g ? Ve.current : null))) {
      oe(f), dn(""), Ve.current = null;
      return;
    }
    _e(g, f);
  }
  function Re() {
    gt(!1), dn(""), Ve.current = null;
  }
  function it(f, g) {
    da(), _t({ nodeId: f, frameIndex: g }), T(null), Z("create");
  }
  async function at(f) {
    const g = et;
    if (!g || nt.current)
      return;
    const y = Ze.nodes.find((L) => L.id === g.nodeId);
    if (!y) {
      G.error("宫格节点不存在");
      return;
    }
    const S = f.filter(
      (L) => L.kind === "image" && L.id > 0 && L.versionID > 0
    ), _ = Number.isInteger(g.frameIndex);
    if (_ && S.length === 0) {
      G.error("请选择一张图片");
      return;
    }
    if (!_ && S.length === 0) {
      G.error("请至少选择一张图片");
      return;
    }
    if (!_ && S.length > Hn) {
      G.error(`一次最多导入 ${Hn} 张图片`);
      return;
    }
    const O = Js([
      y.asset?.version?.content,
      y.resultOutput
    ]), E = _ ? qy(
      O,
      Number(g.frameIndex),
      S[0],
      y.title
    ) : Md(
      S,
      O,
      y.title
    );
    if (!E) {
      G.error("当前宫格内容不可编辑");
      return;
    }
    nt.current = !0;
    try {
      const L = Number(y.asset?.id || 0), ee = Number(
        y.asset?.version?.id || y.asset?.version_id || 0
      ), te = L > 0 && ee > 0 ? await Np({
        projectId: r,
        assetId: L,
        versionId: ee,
        content: E
      }) : await vp({
        projectId: r,
        assetCateId: Number(y.assetCateId || q?.id || 0),
        name: E.title || y.title || "宫格图片",
        kind: "collection",
        content: E,
        nodeKey: y.id,
        requestId: `storyboard-grid-import:${y.id}:${Date.now()}`
      }), K = An(
        te,
        y.asset
      );
      st(K), mt(
        y.id,
        As(y, K)
      ), G.success(_ ? "宫格图片已替换" : "图片已导入宫格");
    } catch (L) {
      G.error(L instanceof Error ? L.message : "导入宫格图片失败");
    } finally {
      nt.current = !1;
    }
  }
  function vt(f, g) {
    x("power", g, { power: f });
  }
  function Ft(f = "") {
    He(f);
  }
  async function Ct(f) {
    const g = await Hc({
      projectID: r,
      teamID: Number(s?.project.team_id || 0),
      files: f
    }), y = [];
    for (const S of g) {
      const _ = mr(S.asset);
      _.id && st(_);
      const O = Il(S.asset);
      O.id && y.push(O);
    }
    return y;
  }
  function Xt(f, g) {
    x("agent", g, { role: f });
  }
  function Vn(f, g) {
    x("flow", g, { flow: f });
  }
  function sr(f) {
    x("group", f);
  }
  function gn(f, g) {
    const y = x("function", g, { functionOption: f });
    f.key === "import" && (Ve.current = y, Ft(y?.id || ""));
  }
  function yn(f, g, y) {
    Sd(), Z("create"), T({
      x: f.x,
      y: f.y,
      position: g,
      connection: y
    }), M();
  }
  function eo() {
    k(de === "dark" ? "light" : "dark");
  }
  return re ? me.current ? null : /* @__PURE__ */ c(Hl, {}) : Fe || !s || !q ? /* @__PURE__ */ c("main", { className: `ws-page is-${de} ws-loading-screen`, children: /* @__PURE__ */ c("div", { className: "ws-loading-card ws-error-card", children: /* @__PURE__ */ c("span", { children: Fe || "创作空间不存在" }) }) }) : /* @__PURE__ */ v("main", { className: `ws-page is-${de} is-${X}-view`, children: [
    /* @__PURE__ */ c(
      Vy,
      {
        activeCate: q,
        mode: X,
        interactive: X === "create",
        nodes: Ze.nodes,
        edges: Ze.edges,
        viewport: we.viewport,
        selectedNodeId: z,
        selectedNodeIds: b,
        onSelectNodes: (f) => {
          w(f), T(null);
        },
        onOpenNodeMenu: yn,
        onAddConfiguredNode: x,
        onCopyNode: I,
        onDeleteNodes: V,
        onShowNodeDetail: Ho,
        onNodesCommit: (f) => Tt((g) => ({ ...g, nodes: f })),
        onEdgesCommit: (f) => Tt((g) => ({ ...g, edges: f })),
        onConnectedMediaEdgeRemove: Xr,
        onViewportCommit: (f) => Tt((g) => g.nodes.length === 0 && g.edges.length === 0 ? g : { ...g, viewport: f }),
        focusNodeRequest: Gt,
        onFocusNodeRequestConsumed: Zo,
        projectId: r,
        space: s,
        canvasReferenceItems: Wr,
        catalogCache: o,
        runningNodes: ie,
        setRunningNode: H,
        onNodeResult: mt,
        onNodeDraftChange: Yr,
        onAssetCreated: st,
        onRunStartNode: vr,
        onRunStoryboardFrame: Yo,
        onRunFunctionNode: Xo,
        onRunBackendNode: Zr,
        onOpenImportPicker: Ft,
        onOpenStoryboardGridImport: it,
        onClearFeedbackRecords: Yt,
        requestConfirm: rt,
        onOpenFeedbackRecord: Wo
      }
    ),
    /* @__PURE__ */ c(
      Oy,
      {
        space: s,
        cates: Ht,
        activeCate: q,
        saveStatus: he[String(q.id)] || "saved",
        hasAssetCates: rn,
        loadingCateId: u,
        onBack: () => t({ to: "/bot/work" }),
        onSelectCate: Jr,
        onRefresh: ke,
        onOpenRunHistory: () => {
          Ae(!0), or(r, 0);
        },
        onRunHistoryIntent: gy,
        theme: de,
        onToggleTheme: eo
      }
    ),
    Rt ? /* @__PURE__ */ c(
      ut,
      {
        fallback: /* @__PURE__ */ c(lt, { label: "正在加载运行历史", overlay: !0 }),
        children: /* @__PURE__ */ c(
          my,
          {
            open: !0,
            runs: Xe,
            loading: Nr,
            error: nr,
            page: Ee,
            hasNextPage: Bn,
            onOpenChange: Ae,
            onRefresh: () => or(r, 0),
            onPreviousPage: () => or(
              r,
              Math.max(0, Ee - 2)
            ),
            onNextPage: () => or(r, Ee),
            onLocateRun: (f) => {
              const g = String(f.start_node_id || "");
              g && (Ae(!1), Jr(Number(f.asset_cate_id || a)).then(
                (y) => {
                  y && window.requestAnimationFrame(() => Cr(g));
                }
              ));
            }
          }
        )
      }
    ) : null,
    /* @__PURE__ */ c(
      By,
      {
        mode: X,
        onModeIntent: (f) => {
          f === "result" && py();
        },
        onSelectMode: (f) => {
          Z(f), T(null);
        }
      }
    ),
    Qe ? /* @__PURE__ */ c(
      ut,
      {
        fallback: /* @__PURE__ */ c(lt, { label: "正在加载资产选择器", overlay: !0 }),
        children: /* @__PURE__ */ c(
          ca,
          {
            open: !0,
            teamID: s.project.team_id,
            scopeProjectID: s.project.id,
            title: "导入资产",
            description: "选择已有资产或上传本地文件，确认后加入当前画布。",
            initialFilters: {
              sourceType: "project",
              projectID: s.project.id
            },
            confirmSelection: !0,
            contentMode: "full",
            validateAsset: (f) => f.versionID > 0 ? "" : "该资产没有可用版本，无法导入。",
            onUpload: Ct,
            onClose: Re,
            onConfirm: (f) => {
              const g = f[0];
              g && Ie(mr(g));
            }
          }
        )
      }
    ) : null,
    et ? /* @__PURE__ */ c(
      ut,
      {
        fallback: /* @__PURE__ */ c(lt, { label: "正在加载图片选择器", overlay: !0 }),
        children: /* @__PURE__ */ c(
          ca,
          {
            open: !0,
            teamID: s.project.team_id,
            scopeProjectID: s.project.id,
            title: Number.isInteger(et.frameIndex) ? "替换宫格图片" : "导入宫格图片",
            description: Number.isInteger(et.frameIndex) ? "选择一张已有图片或上传本地图片。" : `选择 1-${Gr} 张已有图片，或上传本地图片。`,
            initialFilters: {
              sourceType: "project",
              projectID: s.project.id,
              kind: "image"
            },
            allowedKinds: ["image"],
            multiple: !Number.isInteger(et.frameIndex),
            maxSelection: Gr,
            confirmSelection: !0,
            contentMode: "full",
            uploadAccept: "image/*",
            validateAsset: (f) => f.kind !== "image" ? "请选择图片资产。" : f.versionID > 0 ? "" : "该图片没有可用版本，无法导入。",
            onUpload: Ct,
            onClose: () => _t(null),
            onConfirm: (f) => {
              at(f);
            }
          }
        )
      }
    ) : null,
    X === "result" ? /* @__PURE__ */ c("div", { className: "ws-workspace-overlay ws-asset-workspace", children: /* @__PURE__ */ c(ut, { fallback: /* @__PURE__ */ c(lt, { label: "正在加载资产" }), children: /* @__PURE__ */ c(
      fy,
      {
        teamID: s.project.team_id,
        scopeProjectID: s.project.id,
        onLocalUpload: Ct,
        initialFilters: {
          sourceType: "project",
          projectID: s.project.id,
          assetCateID: rn ? q.id : 0
        },
        headerAction: /* @__PURE__ */ c(We, { label: "关闭资产", children: /* @__PURE__ */ v("button", { type: "button", onClick: () => Z("create"), children: [
          /* @__PURE__ */ c(Ya, { "aria-hidden": "true" }),
          /* @__PURE__ */ c("span", { className: "sr-only", children: "关闭资产" })
        ] }) })
      }
    ) }) }) : null,
    Ge ? /* @__PURE__ */ c(
      Bw,
      {
        prompt: Ge.prompt,
        running: Pt,
        readonly: Tm(
          Ge,
          Ze.nodes,
          wt.current
        ),
        history: Wn(
          Ze.nodes.find(
            (f) => f.id === Ge.node.id
          ) || Ge.node
        ),
        activeRecordId: Ge.recordId,
        onSelectRecord: (f) => {
          const g = Ze.nodes.find(
            (y) => y.id === Ge.node.id
          ) || Ge.node;
          ht({
            node: g,
            recordId: f.id,
            prompt: {
              ...f.prompt,
              values: f.values || f.prompt.values || {}
            }
          });
        },
        onClose: Bt,
        onSubmit: Sr
      },
      `${Ge.node.id}-${Ge.recordId}`
    ) : null,
    Pe ? /* @__PURE__ */ c(
      Uy,
      {
        request: Pe,
        onClose: () => Y(null)
      }
    ) : null,
    C ? /* @__PURE__ */ c(
      ut,
      {
        fallback: /* @__PURE__ */ c(lt, { label: "正在加载节点菜单", overlay: !0 }),
        children: /* @__PURE__ */ c(
          ly,
          {
            menu: C,
            flows: Go,
            powers: qo,
            powerCategories: zt,
            roles: Wt,
            onClose: () => T(null),
            onSelectFlow: (f) => Vn(f, C.position),
            onSelectFunction: (f) => gn(f, C.position),
            onSelectGroup: () => sr(C.position),
            onSelectRole: (f) => Xt(f, C.position),
            onSelectPower: (f) => vt(f, C.position)
          }
        )
      }
    ) : null,
    le ? /* @__PURE__ */ c(
      ut,
      {
        fallback: /* @__PURE__ */ c(lt, { label: "正在加载节点详情", overlay: !0 }),
        children: /* @__PURE__ */ c(
          wy,
          {
            projectId: s.project.id,
            teamId: s.team.id,
            assetCateId: Number(
              le.assetCateId || le.asset?.asset_cate_id || q?.id || 0
            ),
            node: le,
            storyboardFocus: ye,
            canvasNodes: Ze.nodes,
            connectedMediaReferences: su(
              Ze.nodes,
              Ze.edges,
              le.id
            ),
            canvasReferenceItems: Wr.filter(
              (f) => f.source !== "current" || f.id !== le.id
            ),
            onNodeDraftChange: (f) => {
              f && (Yr(le.id, f), Be(
                (g) => g?.id === le.id ? { ...g, composerDraft: f } : g
              ));
            },
            onConnectedMediaEdgeRemove: Xr,
            onRunNode: Zr,
            onAssetUpdated: (f) => {
              const g = An(
                f,
                le.asset
              );
              st(g);
              const y = As(
                le,
                g
              );
              le.id.startsWith("asset-detail-") || mt(le.id, y), Be(
                (S) => S?.id === le.id ? {
                  ...S,
                  ...y
                } : S
              );
            },
            onClose: () => {
              Be(null), Ce(void 0);
            }
          }
        )
      }
    ) : null
  ] });
}
function Oy({
  space: e,
  cates: t,
  activeCate: n,
  saveStatus: r,
  hasAssetCates: o,
  loadingCateId: s,
  onBack: i,
  onSelectCate: a,
  onRefresh: d,
  onOpenRunHistory: l,
  onRunHistoryIntent: u,
  theme: m,
  onToggleTheme: N
}) {
  const b = Math.max(
    0,
    t.findIndex((w) => w.id === n.id)
  );
  return /* @__PURE__ */ v("header", { className: "ws-topbar", children: [
    /* @__PURE__ */ v("div", { className: "ws-project-head", children: [
      /* @__PURE__ */ c(
        "button",
        {
          type: "button",
          className: "ws-back-button",
          onClick: i,
          "aria-label": "返回工作台",
          children: /* @__PURE__ */ c(Qu, { size: 18 })
        }
      ),
      /* @__PURE__ */ v("div", { className: "ws-project-copy", children: [
        /* @__PURE__ */ c("strong", { children: e.project.name }),
        /* @__PURE__ */ c("span", { children: e.team.name || e.project.team?.name || "自由团队" })
      ] })
    ] }),
    o ? /* @__PURE__ */ v(
      "nav",
      {
        className: "ws-cate-strip",
        "aria-label": "资产类型",
        style: {
          "--ws-cate-total": t.length,
          "--ws-cate-active": b
        },
        children: [
          /* @__PURE__ */ c("span", { className: "ws-cate-indicator" }),
          t.map((w) => /* @__PURE__ */ v(
            "button",
            {
              type: "button",
              className: `ws-cate ${w.id === n.id ? "is-active" : ""}`,
              disabled: s != null,
              onClick: () => {
                a(w.id);
              },
              children: [
                s === w.id ? /* @__PURE__ */ c(sn, { size: 12, className: "animate-spin" }) : null,
                /* @__PURE__ */ c("span", { className: "ws-cate-name", children: w.name })
              ]
            },
            w.id
          ))
        ]
      }
    ) : null,
    /* @__PURE__ */ v("div", { className: "ws-top-actions", children: [
      /* @__PURE__ */ c($y, { status: r }),
      /* @__PURE__ */ c(We, { label: "查看画布运行记录", children: /* @__PURE__ */ v(
        "button",
        {
          type: "button",
          className: "ws-action",
          onPointerEnter: u,
          onFocus: u,
          onClick: l,
          children: [
            /* @__PURE__ */ c(Va, { size: 15 }),
            "运行记录"
          ]
        }
      ) }),
      /* @__PURE__ */ v("button", { type: "button", className: "ws-action", onClick: N, children: [
        m === "dark" ? /* @__PURE__ */ c(dl, { size: 15 }) : /* @__PURE__ */ c(ul, { size: 15 }),
        m === "dark" ? "亮色" : "暗色"
      ] }),
      /* @__PURE__ */ v("button", { type: "button", className: "ws-action", onClick: d, children: [
        /* @__PURE__ */ c(zo, { size: 15 }),
        "刷新"
      ] })
    ] })
  ] });
}
function $y({ status: e }) {
  const t = e === "saving" ? "保存中" : e === "error" ? "保存失败，正在重试" : e === "dirty" ? "未保存" : "已保存";
  return /* @__PURE__ */ c(We, { label: t, children: /* @__PURE__ */ v("span", { className: `ws-save-indicator is-${e}`, children: [
    e === "saving" ? /* @__PURE__ */ c(sn, { size: 14, className: "ws-spin" }) : /* @__PURE__ */ c(Ha, { size: 14 }),
    t
  ] }) });
}
const jy = [
  { key: "create", label: "创作", icon: Vu },
  { key: "result", label: "资产", icon: cc }
];
function By({
  mode: e,
  onModeIntent: t,
  onSelectMode: n
}) {
  return /* @__PURE__ */ c("nav", { className: "ws-dock", "aria-label": "画布视角", children: jy.map((r) => {
    const o = r.icon;
    return /* @__PURE__ */ v(
      "button",
      {
        type: "button",
        className: `ws-dock-button ${r.key === e ? "is-active" : ""}`,
        onPointerEnter: () => t(r.key),
        onFocus: () => t(r.key),
        onClick: () => n(r.key),
        children: [
          /* @__PURE__ */ c(o, { size: 20 }),
          r.label
        ]
      },
      r.key
    );
  }) });
}
function Vy({
  activeCate: e,
  mode: t,
  interactive: n,
  nodes: r,
  edges: o,
  viewport: s,
  selectedNodeId: i,
  selectedNodeIds: a,
  onSelectNodes: d,
  onOpenNodeMenu: l,
  onAddConfiguredNode: u,
  onCopyNode: m,
  onDeleteNodes: N,
  onShowNodeDetail: b,
  onNodesCommit: w,
  onEdgesCommit: z,
  onConnectedMediaEdgeRemove: F,
  onViewportCommit: U,
  focusNodeRequest: A,
  onFocusNodeRequestConsumed: X,
  projectId: Z,
  space: de,
  canvasReferenceItems: k,
  catalogCache: C,
  runningNodes: T,
  setRunningNode: re,
  onNodeResult: ne,
  onNodeDraftChange: me,
  onAssetCreated: ie,
  onRunStartNode: H,
  onRunStoryboardFrame: J,
  onRunFunctionNode: ae,
  onRunBackendNode: xe,
  onOpenImportPicker: le,
  onOpenStoryboardGridImport: Be,
  onClearFeedbackRecords: ye,
  requestConfirm: Ce,
  onOpenFeedbackRecord: Pe
}) {
  const [Y, Gt] = j(null), [fe, Qe] = j(""), [gt, cn] = j(""), [dn, et] = j(""), [_t, Fe] = j(null), [Nt, Ke] = j(null), [kt, Xe] = j(""), [un, Nr] = j(!0), [qe, nr] = j(!1), [Ne, Rt] = j(() => /* @__PURE__ */ new Set()), [Ae, Ee] = j(1), [Dt, Bn] = j(null), yt = se(
    () => new Set(a),
    [a]
  ), Ge = Q(null), ht = Q(/* @__PURE__ */ new Map()), Pt = Q(/* @__PURE__ */ new Map()), tt = Q(null), Ve = Q(!1), nt = Q(!1), tn = Q(!1), nn = Q(null), It = Q(!1), Te = Q(o);
  Te.current = o;
  const ft = $(
    (p) => {
      Te.current = p, z(p);
    },
    [z]
  ), St = $(
    (p) => {
      const h = Object.entries(p);
      if (h.length === 0)
        return;
      const D = new Map(h);
      let x = !1;
      const I = Te.current.map((V) => {
        if (!D.has(V.id))
          return V;
        const oe = D.get(V.id) || void 0;
        return (V.mediaUsage || void 0) === oe ? V : (x = !0, { ...V, mediaUsage: oe });
      });
      x && ft(I);
    },
    [ft]
  ), ln = $(
    (p) => {
      const h = Te.current.filter((D) => D.id !== p);
      h.length !== Te.current.length && (Te.current = h, Xe((D) => D === p ? "" : D), F(p));
    },
    [F]
  ), vn = (p, h) => {
    if (et(""), !n)
      return;
    const D = Lp(r, p, h);
    D !== r && w(D);
  }, Cn = (p, h) => {
    if (et(""), !n)
      return;
    const D = Kp(r, p, h);
    D !== r && w(D);
  }, ze = Q({
    onNodeResult: ne,
    onNodeDraftChange: me,
    onAssetCreated: ie,
    onRunStartNode: H,
    onRunFunctionNode: ae,
    onOpenImportPicker: le,
    onOpenStoryboardGridImport: Be,
    onClearFeedbackRecords: ye,
    onOpenFeedbackRecord: Pe,
    onShowNodeDetail: b,
    requestConfirm: Ce,
    onRunBackendNode: xe,
    onConnectedMediaUsagesChange: St,
    onConnectedMediaEdgeRemove: ln,
    onNodeResizeStart: et,
    onNodeResizeEnd: vn,
    onResultViewResizeEnd: Cn
  });
  ze.current = {
    onNodeResult: ne,
    onNodeDraftChange: me,
    onAssetCreated: ie,
    onRunStartNode: H,
    onRunFunctionNode: ae,
    onOpenImportPicker: le,
    onOpenStoryboardGridImport: Be,
    onClearFeedbackRecords: ye,
    onOpenFeedbackRecord: Pe,
    onShowNodeDetail: b,
    requestConfirm: Ce,
    onRunBackendNode: xe,
    onConnectedMediaUsagesChange: St,
    onConnectedMediaEdgeRemove: ln,
    onNodeResizeStart: et,
    onNodeResizeEnd: vn,
    onResultViewResizeEnd: Cn
  };
  const wt = se(
    () => ({
      onNodeResult: (p, h) => ze.current.onNodeResult(p, h),
      onNodeDraftChange: (p, h) => ze.current.onNodeDraftChange(p, h),
      onAssetCreated: (p) => ze.current.onAssetCreated(p),
      onRunStartNode: (p) => ze.current.onRunStartNode(p),
      onRunFunctionNode: (p) => ze.current.onRunFunctionNode(p),
      onOpenImportPicker: (p) => ze.current.onOpenImportPicker(p),
      onOpenStoryboardGridImport: (p, h) => ze.current.onOpenStoryboardGridImport(p, h),
      onClearFeedbackRecords: (p) => ze.current.onClearFeedbackRecords(p),
      onOpenFeedbackRecord: (p, h) => ze.current.onOpenFeedbackRecord(p, h),
      onShowNodeDetail: (p, h) => ze.current.onShowNodeDetail(p, h),
      requestConfirm: (p) => ze.current.requestConfirm(p),
      onRunBackendNode: (p, h) => ze.current.onRunBackendNode(p, h),
      onConnectedMediaUsagesChange: (p) => ze.current.onConnectedMediaUsagesChange(p),
      onConnectedMediaEdgeRemove: (p) => ze.current.onConnectedMediaEdgeRemove(p),
      onNodeResizeStart: (p) => ze.current.onNodeResizeStart(p),
      onNodeResizeEnd: (p, h) => ze.current.onNodeResizeEnd(p, h),
      onResultViewResizeEnd: (p, h) => ze.current.onResultViewResizeEnd(p, h)
    }),
    []
  ), pt = se(
    () => hd(r, Mt),
    [r]
  ), Me = se(
    () => new Map(pt.map((p) => [p.id, p])),
    [pt]
  ), zt = se(
    () => xs(r),
    [r]
  ), kn = se(() => {
    const p = /* @__PURE__ */ new Map();
    for (const h of pt)
      for (const D of h.memberNodeIds)
        p.set(D, h.sourceNodeId);
    return p;
  }, [pt]);
  ce(() => {
    const p = new Set(pt.map((h) => h.id));
    Rt((h) => {
      const D = new Set(
        [...h].filter((I) => p.has(I))
      );
      return D.size === h.size && [...D].every((I) => h.has(I)) ? h : D;
    });
  }, [pt]);
  const xt = se(() => {
    const p = /* @__PURE__ */ new Set();
    for (const h of pt)
      if (Ne.has(h.id))
        for (const D of h.memberNodeIds)
          p.add(D);
    return p;
  }, [Ne, pt]), M = $(
    (p) => {
      const h = Me.get(p), D = Ge.current?.getBoundingClientRect();
      if (!h || !Y || !D)
        return;
      const x = ia(
        h,
        Ne.has(h.id)
      ), I = Math.max(1, D.width - 144), V = Math.max(1, D.height - 144), oe = Math.max(
        0.35,
        Math.min(
          0.9,
          I / x.width,
          V / x.height
        )
      );
      Y.setCenter?.(
        x.x + x.width / 2,
        x.y + x.height / 2,
        { zoom: oe, duration: 320 }
      ), Ee(oe);
    },
    [Ne, Y, Me]
  ), W = $(
    (p) => {
      const h = Me.get(p);
      if (!h)
        return;
      const D = !Ne.has(p);
      if (Rt((x) => {
        const I = new Set(x);
        return I.has(p) ? I.delete(p) : I.add(p), I;
      }), D) {
        const x = new Set(h.memberNodeIds);
        d(
          a.filter((I) => !x.has(I))
        ), Qe(""), Xe(""), Ke(null);
      }
    },
    [
      Ne,
      d,
      a,
      Me
    ]
  ), B = Q({
    onRun: J,
    onFocus: M,
    onToggle: W
  });
  B.current = {
    onRun: J,
    onFocus: M,
    onToggle: W
  };
  const ue = se(() => r.length === 0 ? "" : `${e.id}:${r.map((p) => p.id).join("|")}`, [e.id, r]), he = se(
    () => ou(r, o),
    [o, r]
  ), ke = se(() => {
    const p = /* @__PURE__ */ new Set(), h = r.filter((I) => !xt.has(I.id)).map((I) => {
      p.add(I.id);
      const V = { x: I.x, y: I.y }, oe = yt.has(I.id), _e = oe && a.length === 1, pe = I.type === "power" ? er(I.power, I.kind, I.outputType).viewMode : "", Ie = _e || I.type === "flow", Re = Ie ? de : null, it = Ie || pe === "storyboard" || pe === "video_compose" ? k : vy, at = (pe === "video_compose" || _e && (I.type === "power" || I.type === "agent")) && he.incomingMediaReferencesByNodeId.get(I.id) || Ro, vt = zt.has(I.id), Ft = kn.get(I.id) || sa(r, I), Ct = Ft && he.nodeById.get(Ft) || null, Xt = !!(Ft && Qt(
        T[mi(Ft)]
      )), Vn = `ws-flow-node ws-flow-node-${I.type}`, sr = T[I.id] || null, gn = I.type === "group" && he.groupMembersById.get(I.id) || pa, yn = I.type === "group" || I.type === "function" && I.functionOption?.key === "start" ? T : Sy, eo = he.inputContextByNodeId.get(I.id) || null, f = Dc({
        targets: I.type === "group" ? gn : [I],
        nodesByID: he.nodeById,
        hasResult: Mt
      }), g = ht.current.get(I.id), y = g?.data, _ = y?.sourceNode === I && y.projectId === Z && y.space === Re && y.runningNode === sr && mu(y.groupMembers || [], gn) && y.canvasRunningNodes === yn && y.canvasReferenceItems === it && y.connectedMediaReferences === at && y.interactive === n && y.structureLocked === vt && y.storyboardSourceNode === Ct && y.storyboardFrameRunning === Xt && y.runBlockedReason === f && y.showNodeSettings === _e && nw(y.inputContext, eo) ? y : {
        ...I,
        sourceNode: I,
        projectId: Z,
        space: Re,
        catalogCache: C,
        runningNode: sr,
        groupMembers: gn,
        canvasRunningNodes: yn,
        canvasReferenceItems: it,
        connectedMediaReferences: at,
        interactive: n,
        structureLocked: vt,
        storyboardSourceNode: Ct,
        storyboardFrameRunning: Xt,
        runBlockedReason: f,
        showNodeSettings: _e,
        setRunningNode: re,
        ...wt,
        inputContext: eo
      }, O = g?.style, E = bu(I);
      if (g && g.position.x === V.x && g.position.y === V.y && g.data === _ && g.selected === oe && g.className === Vn && g.draggable === (n && !vt) && g.deletable === !vt && g.zIndex === (I.type === "group" ? 0 : I.groupId ? 2 : 1) && O?.width === E.width && O?.height === E.height)
        return g;
      const L = {
        ...g,
        id: I.id,
        type: "workSpace",
        position: V,
        data: _,
        selected: oe,
        className: Vn,
        draggable: n && !vt,
        deletable: !vt,
        zIndex: I.type === "group" ? 0 : I.groupId ? 2 : 1,
        style: {
          ...g?.style,
          width: E.width,
          height: E.height
        }
      };
      return ht.current.set(I.id, L), L;
    });
    for (const I of ht.current.keys())
      p.has(I) || ht.current.delete(I);
    const D = /* @__PURE__ */ new Set(), x = pt.map((I) => {
      D.add(I.id);
      const V = Ne.has(I.id), oe = ia(I, V), _e = wd(
        I,
        r,
        Mt
      ), pe = Qt(T[I.id]) || I.workNodeIds.some(
        (Xt) => Qt(T[Xt])
      ), Ie = Pt.current.get(I.id), Re = Ie?.data, at = Re?.title === I.title && Re.groupCount === I.groupCount && Re.workNodeCount === I.workNodeCount && Re.completedCount === I.completedCount && Re.running === pe && Re.runBlockedReason === _e.blockedReason && Re.collapsed === V && Re ? Re : {
        type: "storyboardFrame",
        title: I.title,
        groupCount: I.groupCount,
        workNodeCount: I.workNodeCount,
        completedCount: I.completedCount,
        running: pe,
        runBlockedReason: _e.blockedReason,
        collapsed: V,
        onRun: Re?.onRun || (() => {
          B.current.onRun(
            I.sourceNodeId
          );
        }),
        onFocus: Re?.onFocus || (() => B.current.onFocus(I.id)),
        onToggleCollapsed: Re?.onToggleCollapsed || (() => B.current.onToggle(I.id))
      }, vt = yt.has(I.id), Ft = Ie?.style;
      if (Ie && Ie.position.x === oe.x && Ie.position.y === oe.y && Ie.data === at && Ie.selected === vt && Ie.draggable === n && Ie.selectable === n && Ie.focusable === n && Ft?.width === oe.width && Ft?.height === oe.height)
        return Ie;
      const Ct = {
        ...Ie,
        id: I.id,
        type: "storyboardFrame",
        position: { x: oe.x, y: oe.y },
        data: at,
        selected: vt,
        className: "ws-flow-node ws-flow-node-storyboard-frame",
        zIndex: -1,
        draggable: n,
        selectable: n,
        connectable: !1,
        deletable: !1,
        focusable: n,
        dragHandle: ".ws-storyboard-frame-header",
        style: {
          ...Ie?.style,
          width: oe.width,
          height: oe.height
        }
      };
      return Pt.current.set(I.id, Ct), Ct;
    });
    for (const I of Pt.current.keys())
      D.has(I) || Pt.current.delete(I);
    return [...x, ...h];
  }, [
    Ne,
    he,
    xt,
    n,
    zt,
    r,
    Z,
    T,
    i,
    a.length,
    yt,
    re,
    de,
    C,
    k,
    wt,
    pt,
    kn
  ]), { flowNodes: rt, setFlowNodes: ot } = Bp(
    ke,
    gt || dn
  ), Ht = $(
    (p) => {
      n && (Xe(""), z(o.filter((h) => h.id !== p)));
    },
    [o, n, z]
  ), rn = $(
    (p) => {
      !n || !o.some((h) => h.id === p) || Ce({
        title: "删除连线",
        description: "删除后，上下游节点将不再通过这条连线传递内容。",
        confirmText: "删除",
        tone: "danger",
        onConfirm: () => Ht(p)
      });
    },
    [Ht, o, n, Ce]
  ), q = $(
    (p) => {
      if (!n || p.length === 0)
        return;
      const h = Td(r, p);
      if (h.size === 0)
        return;
      if ([...h].some(
        (I) => zt.has(I)
      )) {
        G.info("脚本托管节点不能单独删除，请编辑分镜脚本或删除整个制作区");
        return;
      }
      const D = p.length === 1 ? p[0] : null, x = p.some((I) => I.type === "group");
      Ce({
        title: D ? `删除「${D.title}」` : `删除 ${h.size} 个节点`,
        description: x ? "会同时删除组内节点，并移除与这些节点相连的连线。" : D ? "会同时移除与该节点相连的连线。" : "会同时移除与这些节点相连的连线。",
        confirmText: "删除",
        tone: "danger",
        onConfirm: () => {
          Xe(""), N(p);
        }
      });
    },
    [
      n,
      zt,
      r,
      N,
      Ce
    ]
  ), Rn = $(
    (p, h = []) => {
      if (!n || p.length === 0)
        return;
      const D = new Set(
        p.flatMap((pe) => pe.memberNodeIds)
      ), x = /* @__PURE__ */ new Set([
        ...D,
        ...h.map((pe) => pe.id)
      ]), I = r.filter((pe) => x.has(pe.id));
      if (I.length === 0)
        return;
      const V = p.length === 1 && h.every((pe) => D.has(pe.id)) ? p[0] : null, oe = I.filter(
        (pe) => pe.type === "group"
      ).length, _e = I.length - oe;
      Ce({
        title: V ? `删除「${V.title}」制作区` : `删除 ${I.length} 个节点`,
        description: V ? `将删除其中 ${oe} 个分组和 ${_e} 个节点，并移除相关连线。已生成素材会归档保留。` : "会同时删除所选制作区内的节点、分组及相关连线；已生成素材会归档保留。",
        confirmText: "删除",
        tone: "danger",
        onConfirm: () => {
          Xe(""), N(I, { allowStoryboardFrame: !0 });
        }
      });
    },
    [n, r, N, Ce]
  ), Wt = se(() => {
    const p = new Map(r.map((V) => [V.id, V])), h = Aa(
      i,
      r,
      o
    ), D = Aa(
      fe,
      r,
      o
    ), x = /* @__PURE__ */ new Set([
      ...h,
      ...D
    ]), I = h.size > 0 ? i : D.size > 0 ? fe : "";
    return o.filter((V) => p.has(V.from) && p.has(V.to) && !xt.has(V.from) && !xt.has(V.to)).map((V) => ({
      id: V.id,
      source: V.from,
      sourceHandle: "output-0",
      target: V.to,
      targetHandle: "input-0",
      type: "animated",
      animated: !1,
      data: {
        physicalFrom: V.from,
        physicalTo: V.to,
        logicalFrom: V.logicalFrom,
        logicalTo: V.logicalTo,
        purpose: Qs(V),
        executionMode: V.executionMode,
        mediaUsage: V.mediaUsage
      }
    })).map(
      (V) => Nw(
        V,
        p,
        fe,
        i,
        kt,
        x,
        I,
        rn
      )
    );
  }, [
    o,
    xt,
    fe,
    r,
    rn,
    kt,
    i
  ]), qo = se(
    () => _t ? [...Wt, _t] : Wt,
    [Wt, _t]
  );
  ce(() => {
    if (!Y || !ue || A || s.zoom != null || typeof window > "u")
      return;
    const p = setTimeout(() => {
      Y.fitView?.({ padding: 0.32, duration: 250, maxZoom: 0.72 });
    }, 150);
    return () => clearTimeout(p);
  }, [ue, Y, A, s.zoom]), ce(() => {
    if (!Y || !A || typeof window > "u")
      return;
    const p = r.find((x) => x.id === A.nodeId);
    if (!p) {
      X(A);
      return;
    }
    const h = pt.find(
      (x) => Ne.has(x.id) && x.memberNodeIds.includes(p.id)
    );
    if (h) {
      Rt((x) => {
        const I = new Set(x);
        return I.delete(h.id), I;
      });
      return;
    }
    const D = window.setTimeout(() => {
      const x = { x: p.x, y: p.y }, I = p.type === "power" ? 1.02 : 0.96;
      Y.setCenter?.(
        x.x + (p.width || 180) / 2,
        x.y + (p.height || 180) / 2,
        { zoom: I, duration: 320 }
      ), Ee(I), X(A);
    }, 80);
    return () => window.clearTimeout(D);
  }, [
    Ne,
    Y,
    A,
    r,
    X,
    pt
  ]);
  const Go = $(
    (p) => {
      if (!n)
        return;
      ot((x) => {
        const I = Ou(p, x);
        for (const V of I)
          ht.current.set(V.id, V);
        return I;
      });
      const h = new Set(a);
      let D = !1;
      for (const x of p)
        x.type === "select" && (D = !0, x.selected ? (h.delete(x.id), h.add(x.id)) : h.delete(x.id));
      D && d([...h]);
    },
    [n, d, a]
  ), we = $(
    (p) => {
      if (!n)
        return;
      let h = "", D = !1;
      for (const V of p)
        V.type === "select" && (D = !0, V.selected && (h = V.id));
      D && Xe(h);
      const x = p.filter(
        (V) => V.type !== "select"
      );
      if (x.length === 0)
        return;
      const I = $u(x, Wt);
      ft(pw(I));
    },
    [ft, Wt, n]
  ), rr = $(
    async (p, h) => {
      if (Te.current.some((pe) => {
        const Ie = jr(pe);
        return Ie.sourceNodeId === p && Ie.targetNodeId === h;
      }))
        return;
      const x = r.find((pe) => pe.id === h), V = wo(r, p).filter(Ws);
      let oe, _e;
      if (x?.type === "power" && x.power && V.length > 0)
        try {
          const pe = Number(
            x.composerDraft?.selectedTargetId || 0
          ), Ie = Number(
            de.release?.id || de.project.release_id || 0
          ), it = (await C.loadPowerForm(
            {
              projectId: Z,
              releaseId: Ie,
              flowId: Number(x.flow?.id || 0),
              powerId: Number(x.power?.id || 0),
              powerKey: x.power?.key || "",
              targetId: pe
            },
            () => vs({
              projectId: Z,
              flowId: Number(x.flow?.id || 0),
              powerId: Number(x.power?.id || 0),
              powerKey: x.power?.key || "",
              targetId: pe
            })
          )).params || [], at = hi(x), vt = su(
            r,
            Te.current,
            h
          ), Ft = Ps(
            it,
            at
          ), Ct = Ks({
            node: x,
            content: at.promptContent,
            items: k,
            connections: vt,
            params: it,
            values: Ft,
            requestedMode: at.multiImageMode,
            additionalSources: V
          }), Xt = Ct.active ? Ct.mode : void 0;
          if (Ct.error) {
            G.error(Ct.error);
            return;
          }
          const Vn = co(
            it,
            Ft,
            [
              ...vt.map((yn) => yn.source),
              ...V
            ],
            Xt
          ), sr = cr(
            dr(it, Vn)
          ), gn = Hu(
            vt,
            sr,
            V,
            at.promptContent,
            k,
            Xt
          );
          if (gn.error) {
            G.error(gn.error);
            return;
          }
          if (oe = gn.usage, Ct.active && Xt) {
            const yn = jo({
              ...at,
              paramValues: Vn,
              multiImageMode: Xt
            });
            Ds(yn) !== Ds(at) && (_e = yn);
          }
        } catch (pe) {
          G.error(
            pe instanceof Error ? `媒体用途加载失败，未建立连线：${pe.message}` : "媒体用途加载失败，未建立连线"
          );
          return;
        }
      x && _e && me(x.id, _e), ft(
        wn(
          r,
          Ln(
            Te.current,
            p,
            h,
            oe
          )
        )
      );
    },
    [
      k,
      C,
      ft,
      r,
      me,
      Z,
      de
    ]
  ), Ze = $(
    (p) => {
      n && (Ve.current = !0, !(!p.source || !p.target || p.source === p.target) && rr(
        p.source || "",
        p.target || ""
      ));
    },
    [rr, n]
  ), Gr = $(
    (p, h) => {
      if (!n)
        return;
      const D = String(h?.nodeId || "");
      D && (tn.current = !0, d([]), Ke(null)), tt.current = D ? {
        nodeId: D,
        handleId: h?.handleId || null,
        handleType: h?.handleType || null
      } : null, Ve.current = !1, Xe("");
    },
    [n, d]
  ), Hr = $(
    (p) => {
      if (!n) {
        tt.current = null, Ve.current = !1;
        return;
      }
      const h = tt.current;
      if (tt.current = null, h?.nodeId && typeof window < "u" && window.setTimeout(() => {
        tn.current = !1;
      }, 0), Ve.current) {
        Ve.current = !1;
        return;
      }
      if (!h?.nodeId)
        return;
      const D = Sw(p);
      D && (nt.current = !0, l(
        D,
        ir(Y, D),
        h
      ));
    },
    [Y, n, l]
  ), Wr = $(
    (p, h) => {
      n && (p.preventDefault(), p.stopPropagation(), Ke(null), d([]), Xe(h.id));
    },
    [n, d]
  );
  ce(() => {
    if (!kt || typeof window > "u")
      return;
    function p(h) {
      !n || !Da(h) || (h.preventDefault(), rn(kt));
    }
    return window.addEventListener("keydown", p), () => window.removeEventListener("keydown", p);
  }, [n, rn, kt]), ce(() => {
    if (a.length === 0 || kt || typeof window > "u")
      return;
    function p(h) {
      if (!n || !Da(h))
        return;
      const D = r.filter(
        (I) => yt.has(I.id)
      ), x = a.map((I) => Me.get(I)).filter((I) => !!I);
      if (!(D.length === 0 && x.length === 0)) {
        if (h.preventDefault(), x.length > 0) {
          Rn(x, D);
          return;
        }
        q(D);
      }
    }
    return window.addEventListener("keydown", p), () => window.removeEventListener("keydown", p);
  }, [
    n,
    r,
    q,
    Rn,
    kt,
    a.length,
    a,
    yt,
    Me
  ]);
  const He = $((p) => {
    Fe(
      (h) => bw(h, p) ? h : p
    );
  }, []), fn = $(
    (p) => {
      if (!n)
        return !1;
      const h = r.find(
        (x) => x.id === p.source
      ), D = r.find(
        (x) => x.id === p.target
      );
      return Vs(h, D);
    },
    [n, r]
  ), Tt = $(
    (p, h) => {
      if (!n)
        return;
      const D = Me.get(h.id);
      if (D) {
        const _e = bd(
          D,
          h.position
        ), pe = new Set(D.memberNodeIds);
        ot(
          (Ie) => Ie.map((Re) => {
            if (!pe.has(Re.id))
              return Re;
            const it = r.find((at) => at.id === Re.id);
            return it ? {
              ...Re,
              position: {
                x: it.x + _e.x,
                y: it.y + _e.y
              }
            } : Re;
          })
        ), He(null);
        return;
      }
      if (zt.has(h.id)) {
        He(null);
        return;
      }
      const x = r.find((_e) => _e.id === h.id);
      if (!x) {
        He(null);
        return;
      }
      if (x.type === "group") {
        const _e = h.position.x - x.x, pe = h.position.y - x.y;
        ot(
          (Ie) => Ie.map((Re) => {
            const it = r.find((at) => at.id === Re.id);
            return it?.groupId !== x.id ? Re : {
              ...Re,
              position: {
                x: it.x + _e,
                y: it.y + pe
              }
            };
          })
        ), He(null);
        return;
      }
      if (Wt.some(
        (_e) => _e.source === h.id || _e.target === h.id
      )) {
        He(null);
        return;
      }
      const V = _w(h, rt, r);
      if (!V) {
        He(null);
        return;
      }
      const oe = hw(
        x,
        V.domainNode
      );
      if (!oe) {
        He(null);
        return;
      }
      He(ww(oe));
    },
    [
      Wt,
      rt,
      n,
      zt,
      r,
      ot,
      Me,
      He
    ]
  ), Ir = $(
    (p, h) => {
      if (!n) {
        cn(""), He(null);
        return;
      }
      const D = Me.get(h.id);
      if (D) {
        const oe = Kg(
          r,
          D,
          h.position
        );
        oe !== r && w(oe), cn(""), He(null);
        return;
      }
      if (zt.has(h.id)) {
        cn(""), He(null);
        return;
      }
      const x = r.find((oe) => oe.id === h.id);
      let I = !1;
      if (x) {
        const oe = df(
          r,
          h.id,
          h.position
        ), _e = Ns(
          oe,
          h.id,
          h.position
        );
        I = (x.groupId || "") !== (_e.find((Ie) => Ie.id === x.id)?.groupId || ""), _e !== r && w(_e);
        const pe = wn(_e, o);
        gu(o, pe) || z(pe);
      }
      if (cn(""), I) {
        He(null);
        return;
      }
      if (!_t) {
        He(null);
        return;
      }
      Wt.some(
        (oe) => oe.source === _t.source && oe.target === _t.target
      ) || rr(
        _t.source,
        _t.target
      ), He(null);
    },
    [
      o,
      rr,
      Wt,
      n,
      zt,
      w,
      r,
      _t,
      Me,
      He
    ]
  ), mt = $(
    (p) => {
      !n || p.button !== 2 || !xy(p.target) || (nn.current = {
        pointerId: p.pointerId,
        start: { x: p.clientX, y: p.clientY },
        baseNodeIds: p.ctrlKey || p.metaKey ? [...a] : [],
        moved: !1
      }, It.current = !1, p.preventDefault(), p.stopPropagation(), p.currentTarget.setPointerCapture?.(p.pointerId));
    },
    [n, a]
  ), pn = $(
    (p) => {
      const h = nn.current;
      if (!h || h.pointerId !== p.pointerId || !Y || !Ge.current)
        return;
      const D = p.clientX - h.start.x, x = p.clientY - h.start.y;
      if (!h.moved && Math.hypot(D, x) < 5)
        return;
      h.moved = !0, p.preventDefault(), p.stopPropagation();
      const I = Ge.current.getBoundingClientRect();
      Bn(
        Ty(
          h.start,
          {
            x: p.clientX,
            y: p.clientY
          },
          I
        )
      );
      const V = My(
        r,
        ir(Y, h.start),
        ir(Y, {
          x: p.clientX,
          y: p.clientY
        })
      );
      d(Ay(h.baseNodeIds, V)), Xe(""), Ke(null);
    },
    [Y, r, d]
  ), mn = $(
    (p) => {
      const h = nn.current;
      !h || h.pointerId !== p.pointerId || (nn.current = null, p.currentTarget.hasPointerCapture?.(p.pointerId) && p.currentTarget.releasePointerCapture(p.pointerId), Bn(null), h.moved && (It.current = !0, p.preventDefault(), p.stopPropagation()));
    },
    []
  ), Yr = $(
    (p) => {
      if (!n)
        return;
      if (nt.current) {
        nt.current = !1;
        return;
      }
      if (d([]), Xe(""), Ke(null), !("detail" in p) || p.detail !== 2)
        return;
      p.preventDefault(), p.stopPropagation();
      const h = { x: p.clientX, y: p.clientY };
      l(h, ir(Y, h));
    },
    [Y, n, l, d]
  ), Xr = $(
    (p) => {
      if (!n)
        return;
      if (p.preventDefault(), p.stopPropagation(), It.current) {
        It.current = !1;
        return;
      }
      const h = { x: p.clientX, y: p.clientY };
      l(h, ir(Y, h));
    },
    [Y, n, l]
  ), Ho = $(
    (p, h) => {
      if (n) {
        if (p.preventDefault(), p.stopPropagation(), Me.has(h.id)) {
          Xe(""), yt.has(h.id) || d([h.id]), Ke({
            nodeId: h.id,
            x: p.clientX,
            y: p.clientY
          });
          return;
        }
        Xe(""), yt.has(h.id) || d([h.id]), Ke({
          nodeId: h.id,
          x: p.clientX,
          y: p.clientY
        });
      }
    },
    [n, d, yt, Me]
  ), be = Nt && r.find((p) => p.id === Nt.nodeId) || null, Yt = Nt && Me.get(Nt.nodeId) || null, st = !!(be && zt.has(be.id)), jt = be && r.find(
    (p) => p.id === sa(r, be)
  ) || null, Sr = be ? { x: be.x, y: be.y } : void 0;
  function Bt() {
    Ke(null);
  }
  function Wo() {
    if (!(!n || !be)) {
      if (st) {
        G.info("脚本托管节点不能复制，请在分镜脚本中修改结构"), Bt();
        return;
      }
      m(
        be,
        Sr ? { x: Sr.x + 34, y: Sr.y + 34 } : void 0
      ), Bt();
    }
  }
  function vr() {
    if (!n || !be && !Yt)
      return;
    if (Yt) {
      const h = Yt;
      Bt(), Rn([h]);
      return;
    }
    if (!be)
      return;
    if (st) {
      G.info("脚本托管节点不能单独删除，请编辑分镜脚本或删除整个制作区"), Bt();
      return;
    }
    const p = be;
    Bt(), q([p]);
  }
  function Yo() {
    be && (b(be), Bt());
  }
  function Zr() {
    jt && (b(
      jt,
      yd(be)
    ), Bt());
  }
  function Xo() {
    if (!be)
      return;
    const p = Sg(be, r);
    if (!p) {
      Bt();
      return;
    }
    me(be.id, p), G.success("已恢复脚本生成的提示词"), Bt();
  }
  const Jr = $(
    (p) => {
      n && (p.preventDefault(), p.dataTransfer && (p.dataTransfer.dropEffect = "move"));
    },
    [n]
  ), Cr = $(
    (p) => {
      if (!n || (p.preventDefault(), !Y || !u)) return;
      const h = p.dataTransfer.getData(
        "application/shemic-nodetype"
      );
      if (!h) return;
      const D = p.dataTransfer.getData("application/shemic-detail"), x = D ? JSON.parse(D) : void 0, I = Y.screenToFlowPosition ? Y.screenToFlowPosition({
        x: p.clientX,
        y: p.clientY
      }) : ir(Y, {
        x: p.clientX,
        y: p.clientY
      });
      h === "asset" && x ? u("asset", I, { asset: x }) : h === "power" && x ? u("power", I, { power: x }) : h === "agent" && x ? u("agent", I, { role: x }) : h === "flow" && x ? u("flow", I, { flow: x }) : h === "function" && x ? u("function", I, { functionOption: x }) : u(h, I);
    },
    [Y, n, u]
  ), Zo = $(() => {
    Y?.fitView?.({ padding: 0.32, duration: 260, maxZoom: 0.9 });
  }, [Y]), xn = $(
    (p) => {
      const h = Math.max(0.35, Math.min(1.45, p));
      Ee(h), Y?.zoomTo?.(h, { duration: 120 });
    },
    [Y]
  ), or = $(() => {
    const p = Math.min(1.45, Ae + 0.12);
    Ee(p), Y?.zoomIn?.({ duration: 140 });
  }, [Y, Ae]), kr = $(() => {
    const p = Math.max(0.35, Ae - 0.12);
    Ee(p), Y?.zoomOut?.({ duration: 140 });
  }, [Y, Ae]), Qr = [
    "ws-canvas-wrap",
    gt ? "is-dragging" : "",
    Dt ? "is-selecting" : "",
    dn ? "is-resizing" : "",
    n ? "is-interactive" : "is-passive",
    t === "result" ? "is-result-mode" : ""
  ].filter(Boolean).join(" ");
  return /* @__PURE__ */ v(
    "section",
    {
      ref: Ge,
      className: Qr,
      style: Lw(Ae),
      onPointerEnter: () => {
        Sd(), Qn(), la();
      },
      onPointerDownCapture: mt,
      onPointerMoveCapture: pn,
      onPointerUpCapture: mn,
      onPointerCancelCapture: mn,
      children: [
        /* @__PURE__ */ c(
          ju,
          {
            nodes: rt,
            edges: qo,
            onlyRenderVisibleElements: !0,
            nodeTypes: zy,
            edgeTypes: Fy,
            onNodesChange: Go,
            onEdgesChange: we,
            onConnect: Ze,
            onConnectStart: Gr,
            onConnectEnd: Hr,
            isValidConnection: fn,
            connectionLineStyle: {
              stroke: "var(--ws-green)",
              strokeWidth: 2.5,
              strokeLinecap: "round",
              strokeDasharray: "8 6"
            },
            onEdgeClick: Wr,
            onNodeClick: () => {
              if (n) {
                if (tn.current) {
                  tn.current = !1;
                  return;
                }
                Xe(""), Ke(null);
              }
            },
            onNodeContextMenu: Ho,
            onNodeDragStart: (p, h) => {
              n && cn(h.id);
            },
            onNodeDrag: Tt,
            onNodeDragStop: Ir,
            onDragOver: Jr,
            onDrop: Cr,
            onNodeMouseEnter: (p, h) => {
              Qe(h.id), Qn(), la();
            },
            onNodeMouseLeave: () => Qe(""),
            onInit: (p) => {
              const h = p;
              Gt(h), s.x != null && s.y != null && s.zoom != null ? (h.setViewport?.({
                x: s.x,
                y: s.y,
                zoom: s.zoom
              }), Ee(s.zoom)) : Ee(h.getZoom?.() || 1);
            },
            onMove: (p, h) => {
              Ee(
                (D) => Math.abs(D - h.zoom) > 5e-3 ? h.zoom : D
              );
            },
            onMoveEnd: (p, h) => {
              U({
                x: h.x,
                y: h.y,
                zoom: h.zoom
              });
            },
            onPaneClick: Yr,
            onPaneContextMenu: Xr,
            nodesDraggable: n,
            nodesConnectable: n,
            nodesFocusable: n,
            edgesFocusable: n,
            elementsSelectable: n,
            deleteKeyCode: null,
            multiSelectionKeyCode: ["Control", "Meta"],
            panOnDrag: n,
            panOnScroll: !1,
            zoomOnScroll: n,
            zoomOnPinch: n,
            snapToGrid: qe,
            snapGrid: [18, 18],
            zoomOnDoubleClick: !1,
            minZoom: 0.35,
            maxZoom: 1.45,
            defaultEdgeOptions: {
              type: "animated",
              animated: !1
            },
            fitView: s.zoom == null,
            fitViewOptions: { padding: 0.32, maxZoom: 0.72 },
            children: n && un && r.length > 0 ? /* @__PURE__ */ c(
              Bu,
              {
                position: "bottom-left",
                pannable: !0,
                zoomable: !0,
                nodeClassName: (p) => p.type === "storyboardFrame" ? "ws-minimap-storyboard-frame" : "",
                nodeColor: (p) => p.type === "storyboardFrame" ? "transparent" : Xw(p.data)
              }
            ) : null
          }
        ),
        Dt ? /* @__PURE__ */ c(
          "div",
          {
            className: "ws-canvas-selection-marquee",
            style: Dt,
            "aria-hidden": "true"
          }
        ) : null,
        n ? /* @__PURE__ */ c(
          Up,
          {
            showMiniMap: un,
            snapToGrid: qe,
            zoom: Ae,
            onToggleMiniMap: () => Nr((p) => !p),
            onToggleSnap: () => nr((p) => !p),
            onReset: Zo,
            onZoomIn: or,
            onZoomOut: kr,
            onZoomChange: xn
          }
        ) : null,
        n && r.length === 0 ? /* @__PURE__ */ v("div", { className: "ws-empty-note", role: "note", children: [
          /* @__PURE__ */ v("span", { className: "ws-empty-action", children: [
            /* @__PURE__ */ c(dc, { size: 16 }),
            /* @__PURE__ */ c("strong", { children: "双击屏幕" })
          ] }),
          /* @__PURE__ */ c("span", { className: "ws-empty-copy", children: "画布自由生成" })
        ] }) : null,
        n && Nt && (be || Yt) ? /* @__PURE__ */ c(
          Vp,
          {
            point: Nt,
            canShowDetail: !!(be && Mt(be)),
            canCopy: !!(be && !st),
            canDelete: !!(Yt || !st),
            canEditStructure: !!(jt && jt.id !== be?.id),
            canResetStoryboardPrompt: !!(be && ed(be)),
            onClose: Bt,
            onCopy: Wo,
            onDelete: vr,
            onDetail: Yo,
            onEditStructure: Zr,
            onResetStoryboardPrompt: Xo
          }
        ) : null
      ]
    }
  );
}
function Uy({
  request: e,
  onClose: t
}) {
  const [n, r] = j(!1);
  async function o() {
    if (n)
      return;
    r(!0);
    const s = e.onConfirm;
    t();
    try {
      Promise.resolve(s()).catch((i) => {
        G.error(i instanceof Error ? i.message : "操作失败");
      });
    } catch (i) {
      G.error(i instanceof Error ? i.message : "操作失败");
    }
  }
  return /* @__PURE__ */ c(
    "div",
    {
      className: "ws-confirm-backdrop",
      role: "dialog",
      "aria-modal": "true",
      onMouseDown: t,
      children: /* @__PURE__ */ v(
        "section",
        {
          className: "ws-confirm-card",
          onMouseDown: (s) => s.stopPropagation(),
          children: [
            /* @__PURE__ */ v("div", { className: "ws-confirm-copy", children: [
              /* @__PURE__ */ c("h3", { children: e.title }),
              /* @__PURE__ */ c("p", { children: e.description })
            ] }),
            /* @__PURE__ */ v("div", { className: "ws-confirm-actions", children: [
              /* @__PURE__ */ c("button", { type: "button", disabled: n, onClick: t, children: "取消" }),
              /* @__PURE__ */ c(
                "button",
                {
                  type: "button",
                  className: e.tone === "danger" ? "is-danger" : "is-primary",
                  disabled: n,
                  onClick: () => {
                    o();
                  },
                  children: n ? "处理中..." : e.confirmText || "确认"
                }
              )
            ] })
          ]
        }
      )
    }
  );
}
function Ly(e, t) {
  return Object.keys(t).length === 0 ? e : {
    ...e,
    nodes: e.nodes.map((n) => {
      const r = t[n.id];
      return r ? { ...n, ...r } : n;
    })
  };
}
function Ky(e, t, n) {
  const r = e[t];
  if (!r)
    return e;
  const o = Object.keys(n);
  if (!o.some(
    (a) => Object.prototype.hasOwnProperty.call(r, a)
  ))
    return e;
  const s = { ...r };
  for (const a of o)
    delete s[a];
  const i = { ...e };
  return Object.keys(s).length === 0 ? delete i[t] : i[t] = s, i;
}
function ma({
  canvas: e,
  sourceNodeId: t,
  successfulNodeIds: n,
  assetCate: r,
  powers: o
}) {
  let s = e;
  const i = Math.max(2, n.size + 1);
  for (let a = 0; a < i; a += 1) {
    const d = {
      ...s,
      nodes: Lg(
        s.nodes,
        t,
        n
      )
    };
    if (s = Rs({
      canvas: d,
      assetCate: r,
      powers: o
    }), !s.nodes.some((u) => {
      const m = u.storyboardItem;
      return !m || m.sourceNodeId !== t || !n.has(u.id) ? !1 : !!(m.stale || m.sourceSignature && m.resultSourceSignature !== m.sourceSignature);
    }))
      break;
  }
  return s;
}
function Md(e, t, n, r = e.length) {
  const o = Math.min(
    Hn,
    Math.max(2, e.length, r)
  );
  return {
    type: "storyboard_grid",
    version: Math.max(1, Number(t?.version || 1)),
    title: $e(t?.title, n, "宫格图片"),
    summary: t?.summary || "",
    frames: Array.from(
      { length: o },
      (s, i) => e[i] ? Dd(e[i], i) : Ad(i)
    )
  };
}
function qy(e, t, n, r) {
  if (t < 0 || t >= Hn || (e?.frames.length || 0) > Hn)
    return null;
  const o = e || Md([], null, r, t + 1), s = Math.min(
    Hn,
    Math.max(2, o.frames.length, t + 1)
  ), i = Dd(n, t);
  return {
    ...o,
    frames: Array.from(
      { length: s },
      (a, d) => o.frames[d] || Ad(d)
    ).map(
      (a, d) => d === t ? {
        ...a,
        image: i.image,
        status: "success",
        error: "",
        assetID: i.assetID,
        assetVersionID: i.assetVersionID
      } : a
    )
  };
}
function Ad(e) {
  const t = e + 1;
  return {
    id: `frame-${String(t).padStart(2, "0")}`,
    order: t,
    title: `画面 ${String(t).padStart(2, "0")}`,
    description: "",
    prompt: "",
    status: "pending",
    image: "",
    error: "",
    assetID: 0,
    assetVersionID: 0
  };
}
function Dd(e, t) {
  const n = t + 1;
  return {
    id: `frame-${String(n).padStart(2, "0")}`,
    order: n,
    title: e.name || `画面 ${String(n).padStart(2, "0")}`,
    description: "",
    prompt: "",
    status: "success",
    image: Cl(e.version?.content, "image")[0] || "",
    error: "",
    assetID: e.id,
    assetVersionID: e.versionID
  };
}
function bn(e, t, n) {
  const r = Se(
    t?.output,
    t?.asset?.version?.content,
    t?.version?.content,
    t?.result?.output,
    t?.result?.asset?.version?.content,
    t?.data?.output,
    t?.data?.content,
    t?.data?.result,
    t?.data
  ), o = e.type === "agent" && r != null ? Ue(r) : Ao(r) || $t(r), s = e.type === "power" && er(e.power, e.kind, e.outputType).viewMode === "storyboard" ? wr([
    r,
    t?.asset?.version?.content,
    t?.version?.content,
    t?.result,
    o
  ]) : null, i = e.type === "power" && ll(e.power, e.kind, e.outputType) ? Js([
    r,
    t?.asset?.version?.content,
    t?.version?.content,
    t?.result,
    o
  ]) : null, a = $e(
    String(t?.asset?.kind || ""),
    String(t?.kind || ""),
    Bo(e, o)
  ), d = jn(o, a), l = Kt(o, ""), u = $e(
    i?.title,
    s?.title
  ), m = i?.summary || s?.summary || d.text || (Nn(l) ? "" : l) || d.imageUrl || d.videoUrl || d.audioUrl || d.fileUrl || (n ? `已按提示生成：${n}` : "生成完成");
  return {
    ...u && e.titleMode === "auto" ? { title: u } : {},
    description: m,
    resultRef: Io(t),
    resultOutput: i || s || o,
    asset: t?.asset || e.asset,
    kind: t?.asset?.kind || e.power?.kind || e.kind
  };
}
function As(e, t) {
  const n = t.version?.content;
  return {
    ...bn(
      e,
      {
        asset: t,
        output: n
      },
      Za(n)
    ),
    asset: t
  };
}
function jo(e) {
  return Cc(e) || {
    prompt: "",
    paramValues: {},
    selectedTargetId: 0
  };
}
function Ds(e) {
  return JSON.stringify([
    e.prompt,
    e.promptContent || null,
    e.paramValues || {},
    e.selectedTargetId || 0,
    e.storyboardReferences || [],
    e.storyboardGridLayout || "",
    e.multiImageMode || ""
  ]);
}
function Pd(e) {
  const t = jo(e), n = Gy(t.promptContent);
  return n ? { ...t, prompt: n } : t;
}
function hi(e) {
  return Pd(e.composerDraft);
}
function Gy(e) {
  return e?.parts?.some((t) => t.type === "reference") ? e.parts.map((t) => {
    if (t.type === "text")
      return t.text;
    const n = String(t.label || "").trim();
    return n.startsWith("@") ? n : `@${n}`;
  }).join("") : "";
}
function Hy(e) {
  const t = hi(e);
  return e.type !== "power" && e.type !== "agent" ? e : {
    ...e,
    composerDraft: {
      ...e.composerDraft,
      prompt: t.prompt,
      promptContent: t.promptContent,
      paramValues: t.paramValues,
      selectedTargetId: t.selectedTargetId,
      multiImageMode: t.multiImageMode
    }
  };
}
function Ps(e, t) {
  const n = Gs(e), r = t.paramValues || {};
  for (const s of e)
    s.key && Object.prototype.hasOwnProperty.call(r, s.key) && (n[s.key] = Oa(
      s,
      r[s.key]
    ));
  const o = e.find($a);
  return o?.key && t.prompt.trim() && (n[o.key] = t.prompt), n;
}
function wi(e, t) {
  const n = Rm(e, t);
  return {
    ...n,
    id: Wy(e, t, n.id)
  };
}
function Wy(e, t, n) {
  const r = Number(t.approval?.id || 0);
  if (r > 0)
    return `${e.id}:${r}`;
  const o = String(t.interaction?.interaction?.id || "");
  if (o)
    return `${e.id}:${o}`;
  const s = yo({
    title: t.title,
    fields: (t.fields || []).map((i) => i.key),
    content: t.approval?.content
  });
  return s ? `${e.id}:feedback:${qd(s)}` : n;
}
function zs(e, t) {
  const n = Number(t.prompt?.approval?.id || 0), r = e.findIndex(
    (o) => o.id === t.id || n > 0 && Number(o.prompt?.approval?.id || 0) === n
  );
  return r < 0 ? [...e, t] : e.map((o, s) => {
    if (s !== r)
      return o;
    const i = o.status === "submitted", a = o.values || o.prompt?.values || t.values;
    return {
      ...o,
      title: t.title,
      description: t.description,
      prompt: {
        ...t.prompt,
        values: a || t.prompt.values || {}
      },
      values: o.values,
      status: i ? o.status : t.status,
      submittedAt: o.submittedAt
    };
  });
}
const ga = 3600 * 1e3, zd = 2e3, Yy = 3;
async function cs(e) {
  const t = Sh(e.startNode.id), n = /* @__PURE__ */ new Set();
  let r = !1, o = "0-0", s = await yp({
    projectId: e.projectId,
    assetCateId: Number(e.assetCate.id || 0),
    startNodeId: e.startNode.id,
    requestId: t,
    singleNode: e.singleNode,
    executionScope: e.executionScope,
    canvas: {
      assetCateId: Number(e.assetCate.id || 0),
      nextNodeNo: ei(e.nodes),
      nodes: e.nodes,
      edges: e.edges,
      viewport: e.viewport || {}
    },
    runInput: {
      ...e.runInput || {},
      start_node_id: e.startNode.id
    }
  });
  for (let i = 0; i < 8; i += 1) {
    const a = await Xy(
      e,
      s,
      o,
      (u) => {
        const m = Hd(
          e,
          u,
          n
        );
        jd(e, u), r = r || m > 0;
      },
      () => r,
      (u) => {
        o = u;
      }
    );
    if (e.canvasRun = a, pr(e, a), xo(
      e,
      a,
      r
    ) && (a.status === "running" || a.status === "pending")) {
      G.info("节点结果已返回，后台运行仍在收尾");
      return;
    }
    const d = Number(a.executed || 0);
    !e.singleNode && e.patchStartNodeResult !== !1 && e.onNodeResult(
      e.startNode.id,
      kw(
        a,
        Ah(a, d)
      )
    );
    const l = String(a.status || "").toLowerCase();
    if (l !== "waiting") {
      if (Bd(e, a), l === "fail" || l === "error")
        throw new Error(Vc(a));
      if (l === "canceled" || l === "cancelled")
        throw new Error("画布运行已取消");
      return;
    }
    await Ch(e, a), s = {
      ...a,
      status: "running",
      pending_node: null
    };
  }
  throw new Error("画布运行多次等待反馈，请稍后继续");
}
async function Xy(e, t, n, r, o, s) {
  let i = Mr(
    e,
    br(t)
  );
  if (i = Es(e, i), r(i.node_results || []), pr(e, i), i.status !== "running" && i.status !== "pending" || !i.run_id && !i.request_id)
    return i;
  const a = String(i.request_id || ""), d = new AbortController(), l = Date.now() + ga;
  let u = !1, m = null;
  const N = window.setTimeout(() => {
    u = !0, d.abort();
  }, ga);
  try {
    const b = await rh(
      e,
      a,
      n,
      (w) => {
        w.stream_id && s(w.stream_id);
        const z = sh(w, e);
        if (!z) {
          $d(e, w);
          return;
        }
        i = Mr(
          e,
          Os(i, z)
        ), r(i.node_results || []), pr(e, i);
      },
      d.signal
    );
    b && (i = Mr(
      e,
      Os(i, b)
    ), pr(e, i)), r(i.node_results || []);
  } catch (b) {
    m = b;
  } finally {
    window.clearTimeout(N), d.abort(), e.runningNodeBatcher?.flush();
  }
  if (i = Es(e, i), Fs(e, i) && !xo(
    e,
    i,
    o()
  ))
    try {
      i = await Zy(
        e,
        i,
        a,
        r,
        o,
        l
      );
    } catch (b) {
      throw m instanceof Error && !u ? m : b;
    }
  if (!Fs(e, i) || xo(
    e,
    i,
    o()
  ))
    return i;
  throw m instanceof Error && !u ? m : new Error("画布仍在运行，请稍后刷新查看结果");
}
async function Zy(e, t, n, r, o, s) {
  let i = t, a = 0;
  for (; ; ) {
    if (Date.now() >= s)
      throw new Error("画布仍在运行，请稍后刷新查看结果");
    try {
      i = await Qy(
        e,
        i,
        n,
        r
      ), a = 0;
    } catch (d) {
      if (a += 1, a >= Yy)
        throw d;
    }
    if (!Fs(e, i) || xo(
      e,
      i,
      o()
    ))
      return i;
    await nh(
      Math.min(zd, s - Date.now())
    );
  }
}
function xo(e, t, n) {
  return !!(e.singleNode && !Ed(e) && n && !Jy(t));
}
function Jy(e) {
  return String(e.status || "").trim().toLowerCase() === "waiting" || Od(e) ? !0 : [
    e.pending_node,
    e.output,
    ...e.node_results || []
  ].some((r) => !!Pn(r));
}
async function Qy(e, t, n, r) {
  let o = t;
  const s = Number(o.run_id || 0), i = String(o.request_id || n || "");
  if (!s && !i)
    return o;
  const a = await bp({
    projectId: e.projectId,
    runId: s,
    requestId: i
  });
  return o = Mr(
    e,
    Os(o, br(a))
  ), o = Es(e, o), r(o.node_results || []), pr(e, o), o;
}
function Fs(e, t) {
  const n = String(t.status || "").trim().toLowerCase();
  return n !== "running" && n !== "pending" ? !1 : !Fd(e, t);
}
function Fd(e, t) {
  return wh(
    t
  ) ? !0 : e.singleNode ? (t.node_results || []).some(
    (n) => n.node_key === e.startNode.id && Kr(_n(n))
  ) : !1;
}
function Es(e, t) {
  return eh(e, t) ? {
    ...t,
    status: th(t.node_results || [])
  } : t;
}
function eh(e, t) {
  const n = String(t.status || "").trim().toLowerCase();
  return n !== "running" && n !== "pending" ? !1 : Fd(e, t);
}
function th(e) {
  for (const t of e) {
    const n = _n(t);
    if (n === "fail")
      return "fail";
    if (n === "canceled" || n === "cancelled")
      return "canceled";
  }
  return "success";
}
function nh(e) {
  return new Promise((t) => {
    window.setTimeout(t, e);
  });
}
async function rh(e, t, n, r, o) {
  let s = null;
  return await Kc({
    projectId: e.projectId,
    requestId: t,
    lastId: n,
    signal: o,
    onFrame: (i) => {
      if (r(i), String(i.type || "").toLowerCase() === "result") {
        if (oh(i))
          throw new Error(i.msg || "画布流返回失败");
        s = Mr(
          e,
          br(i.output || {})
        );
      }
    }
  }), s;
}
function oh(e) {
  return Number(e.status || 0) === 2;
}
function Mr(e, t) {
  if (!e.singleNode || Ed(e))
    return t;
  const n = (t.node_results || []).find(
    (d) => d.node_key === e.startNode.id
  ), r = String(t.status || n?.status || "");
  if (n && (r !== "waiting" || t.pending_node))
    return t;
  const o = Od(t), s = Se(n?.output, t.output), i = {
    execution_id: Number(t.execution_id || n?.execution_id || 0),
    node_key: e.startNode.id,
    node_type: e.startNode.type,
    node_run_id: Number(n?.node_run_id || 0),
    run_id: Number(t.run_id || n?.run_id || 0),
    request_id: String(t.request_id || n?.request_id || ""),
    status: r || "success",
    error: n?.error || t.error,
    output: s,
    asset: n?.asset,
    version: n?.version,
    result: {
      ...n?.result || {},
      run_id: t.run_id,
      request_id: t.request_id,
      flow_run_id: t.flow_run_id,
      release_id: t.release_id,
      status: r || "success",
      error: n?.error || t.error,
      output: s,
      approval: o
    },
    approval: Se(n?.approval, o),
    interaction: Se(
      n?.interaction,
      Pn(n?.result),
      Pn(t.pending_node),
      Pn(t.output)
    ),
    persists_result: !!n?.persists_result,
    agent_run_id: Number(n?.agent_run_id || 0)
  }, a = [
    ...(t.node_results || []).filter(
      (d) => d.node_key !== e.startNode.id
    ),
    i
  ];
  return {
    ...t,
    node_results: a,
    pending_node: r === "waiting" ? {
      ...i,
      status: "waiting",
      approval: i.approval,
      interaction: i.interaction
    } : t.pending_node
  };
}
function Ed(e) {
  return e.singleNode && e.startNode.type === "group";
}
function Od(e) {
  const t = e.output;
  return (Array.isArray(e.approvals) ? e.approvals : t && typeof t == "object" && Array.isArray(t.approvals) ? t.approvals : t && typeof t == "object" && Array.isArray(t.data?.approvals) ? t.data.approvals : []).find(
    (r) => r && typeof r == "object" && (r.status === "pending" || r.decision === "pending")
  );
}
function sh(e, t) {
  if (String(e.type || "").toLowerCase() === "result")
    return br(e.output || {});
  const n = e.output || {}, r = String(n.event || "");
  if (String(n.scope || "") === "canvas_child" && r !== "waiting" || r !== "node_finished" && r !== "waiting")
    return null;
  const o = ih(n, {
    requireDisplayableResult: r !== "waiting",
    node: t?.nodes.find(
      (s) => s.id === String(n.node_key || n.node_id || "")
    )
  });
  return o ? {
    execution_id: Number(n.execution_id || 0),
    request_id: String(e.request_id || n.parent_request_id || ""),
    run_id: Number(n.parent_run_id || n.run_id || 0),
    flow_run_id: Number(n.parent_flow_run_id || n.flow_run_id || 0),
    release_id: Number(n.release_id || 0),
    status: r === "waiting" ? "waiting" : "running",
    node_results: [o],
    pending_node: r === "waiting" ? o : null
  } : null;
}
function ih(e, t = {}) {
  const n = String(e.node_key || e.node_id || "");
  if (!n)
    return null;
  const r = e.output, o = r && typeof r == "object" ? r : {}, s = hm(o, n);
  if (!s)
    return null;
  const i = s;
  return t.requireDisplayableResult && !ch(e, i, t.node) ? null : {
    ...s,
    node_key: n,
    execution_id: Number(
      e.execution_id || s.execution_id || o.execution_id || 0
    ),
    node_type: String(e.node_type || s.node_type || ""),
    node_run_id: Number(e.node_run_id || s.node_run_id || 0),
    run_id: Number(
      e.run_id || s.run_id || o.run_id || 0
    ),
    request_id: String(
      e.request_id || s.request_id || o.request_id || ""
    ),
    child_run_id: Number(
      e.child_run_id || s.child_run_id || o.child_run_id || 0
    ),
    child_request_id: String(
      e.child_request_id || s.child_request_id || o.child_request_id || ""
    ),
    status: String(e.status || s.status || ""),
    error: String(
      e.error || s.error || o.error || ""
    ),
    output: s.output ?? o.output ?? r,
    asset: s.asset ?? o.asset,
    version: s.version ?? o.version ?? s.asset?.version,
    result: s.result ?? s,
    approval: Se(
      s.approval,
      ah(e, i)
    ),
    interaction: Se(
      e.interaction,
      s.interaction,
      Pn(s)
    ),
    persists_result: !!(e.persists_result || s.persists_result),
    agent_run_id: Number(
      e.agent_run_id || s.agent_run_id || o.agent_run_id || 0
    ),
    source_signature: s.source_signature
  };
}
function ah(e, t) {
  const n = Se(
    e.approval,
    t.approval,
    t.result?.approval
  );
  if (n && typeof n == "object")
    return n;
  const r = Number(
    Se(
      e.approval_id,
      t.approval_id,
      t.result?.approval_id
    ) || 0
  );
  return r > 0 ? { id: r } : void 0;
}
function Pn(e, t = 0) {
  if (!e || typeof e != "object" || Array.isArray(e) || t > 6)
    return;
  const n = e;
  if (n.interaction && typeof n.interaction == "object" && !Array.isArray(n.interaction))
    return n.interaction;
  for (const o of ["result", "pending_node"]) {
    const s = Pn(n[o], t + 1);
    if (s)
      return s;
  }
  const r = Array.isArray(n.node_results) ? n.node_results : [];
  for (const o of r) {
    const s = Pn(o, t + 1);
    if (s)
      return s;
  }
}
function ch(e, t, n) {
  return e.persists_result || String(e.node_type || "") !== "function" || String(
    e.function_key || t.function_key || n?.functionOption?.key || ""
  ) === "display" ? !0 : !!(t.asset || t.version || t.asset?.version || t.data?.asset || t.data?.version);
}
function $d(e, t) {
  if (!e.setRunningNode)
    return;
  const n = t.output || {}, r = String(n.event || ""), o = String(n.node_key || n.node_id || "");
  if (o) {
    if (r !== "node_output" && e.runningNodeBatcher?.flush(), r === "node_started") {
      e.setRunningNode((s) => ({
        ...s,
        [o]: {
          nodeId: o,
          title: String(n.node_name || n.node_key || o),
          startedAt: Date.now(),
          status: "running",
          progress: Math.max(s[o]?.progress || 0, 18),
          agent: s[o]?.agent
        }
      }));
      return;
    }
    if (r === "node_output") {
      const s = (i) => {
        const a = i[o];
        if (!a || a.status !== "running")
          return i;
        const d = ya(n.output), l = String(n.node_type || "").toLowerCase(), u = l === "power", m = l === "agent", N = String(
          d.semantic_event || d.event || ""
        ).toLowerCase(), b = ya(d.meta), w = u && N === "status" && !!String(b.output_type || ""), z = w && !!(d.json && typeof d.json == "object"), F = u && (N === "audio_ready" || z || kl(d)), U = Number(b.generated_count || 0), A = w ? Math.max(
          a.generatedCount || 0,
          Number.isFinite(U) ? U : 0
        ) : a.generatedCount, X = u && typeof d.text == "string" && (N === "delta" || !N) ? d.text : "", Z = F ? d : a.streamOutput;
        return {
          ...i,
          [o]: {
            ...a,
            progress: Math.max(a.progress, 72),
            streamText: X ? `${a.streamText || ""}${X}` : a.streamText,
            streamOutput: Z,
            streamStarted: a.streamStarted || !!X || !!Z,
            ...w ? { streamStarted: !0, generatedCount: A } : {},
            agent: m ? Xp(a.agent, d) : a.agent
          }
        };
      };
      e.runningNodeBatcher ? e.runningNodeBatcher.enqueue(s) : e.setRunningNode(s);
    }
  }
}
function dh(e, t, n, r) {
  const o = t.output || {}, s = String(o.event || ""), i = String(o.node_key || o.node_id || "");
  if (!(!i || n.size > 0 && !n.has(i) || r.has(i))) {
    if (s === "node_finished") {
      r.add(i), e.runningNodeBatcher?.flush();
      return;
    }
    $d(e, t);
  }
}
function ya(e) {
  return !e || typeof e != "object" || Array.isArray(e) ? {} : e;
}
function Os(e, t) {
  const n = [...e.node_results || []];
  for (const r of t.node_results || []) {
    const o = Mo(r), s = n.findIndex(
      (i) => Mo(i) === o
    );
    s >= 0 ? n[s] = r : n.push(r);
  }
  return {
    ...e,
    ...t,
    node_runs: t.node_runs?.length ? t.node_runs : e.node_runs,
    execution_plan: t.execution_plan || e.execution_plan,
    node_results: n,
    pending_node: t.status === "waiting" ? t.pending_node || e.pending_node : t.pending_node || null
  };
}
function jd(e, t) {
  if (!e.setRunningNode)
    return;
  const n = t.filter(
    (r) => Kr(r.status)
  );
  n.length !== 0 && (e.setRunningNode(
    (r) => uh(e, r, n)
  ), window.setTimeout(() => {
    e.setRunningNode?.((r) => {
      let o = !1, s = r;
      for (const i of n) {
        const a = i.node_key, d = s[a];
        !d || d.status === "running" || (s === r && (s = { ...r }), delete s[a], o = !0);
      }
      return o ? s : r;
    });
  }, 650));
}
function uh(e, t, n) {
  let r = !1;
  const o = { ...t };
  for (const s of n) {
    const i = s.node_key, a = e.nodes.find((l) => l.id === i);
    if (s.status === "waiting") {
      o[i] = {
        nodeId: i,
        title: a?.title || i,
        startedAt: t[i]?.startedAt || Date.now(),
        progress: 92,
        status: "waiting"
      }, r = !0;
      continue;
    }
    const d = o[i];
    d && (o[i] = {
      ...d,
      progress: 100,
      status: s.status === "success" ? "success" : "error",
      agent: a?.type === "agent" ? Zp(s.output) : d.agent
    }, r = !0);
  }
  return r ? o : t;
}
function Bd(e, t, n) {
  if (!e.setRunningNode || t.status === "running" || t.status === "pending" || t.status === "waiting")
    return;
  const r = t.status === "success" || t.status === "canceled" ? "success" : "error";
  e.setRunningNode((o) => {
    let s = !1;
    const i = { ...o }, a = ha(
      e,
      t,
      o,
      n
    );
    for (const d of a) {
      const l = i[d];
      l && (i[d] = {
        ...l,
        progress: r === "success" ? 100 : Math.max(l.progress, 92),
        status: r
      }, s = !0);
    }
    return s ? i : o;
  }), window.setTimeout(
    () => {
      e.setRunningNode?.((o) => {
        let s = !1, i = o;
        const a = ha(
          e,
          t,
          o,
          n
        );
        for (const d of a) {
          const l = i[d];
          !l || l.status === "running" || (i === o && (i = { ...o }), delete i[d], s = !0);
        }
        return s ? i : o;
      });
    },
    r === "success" ? 650 : 1200
  );
}
function ha(e, t, n, r) {
  const o = new Set(
    fh(e, t).filter(
      (s) => !r || r.has(s)
    )
  );
  return Object.keys(n).filter((s) => o.has(s));
}
function lh(e, t, n) {
  if (!e.setRunningNode)
    return;
  const r = String(t.status || "").trim().toLowerCase();
  if (!["running", "pending", "waiting"].includes(r))
    return;
  const o = $s(t), s = /* @__PURE__ */ new Map();
  let i = !1, a = "";
  for (const u of t.node_runs || []) {
    const m = String(u.node_key || "");
    if (!m || o.has(m) || n && !n.has(m))
      continue;
    i = !0;
    const N = String(u.status || "").trim().toLowerCase();
    N === "running" ? s.set(m, "running") : N === "waiting" ? s.set(m, "waiting") : N === "pending" && !a && (a = m);
  }
  const d = String(t.pending_node?.node_key || "");
  if (d && !o.has(d) && (!n || n.has(d)) && s.set(d, "waiting"), s.size === 0 && a && s.set(a, "running"), s.size === 0 && !i) {
    const u = String(t.start_node_id || "");
    u && !o.has(u) && (!n || n.has(u)) && s.set(
      u,
      r === "waiting" ? "waiting" : "running"
    );
  }
  if (s.size === 0)
    return;
  const l = Date.parse(String(t.created_at || ""));
  e.setRunningNode((u) => {
    let m = !1;
    const N = { ...u };
    for (const [b, w] of s) {
      const z = e.nodes.find((U) => U.id === b);
      if (!z)
        continue;
      const F = u[b];
      F?.status !== w && (N[b] = {
        nodeId: b,
        title: z.title,
        startedAt: F?.startedAt || (Number.isFinite(l) ? l : Date.now()),
        progress: w === "waiting" ? 92 : F?.progress || 0,
        status: w
      }, m = !0);
    }
    return m ? N : u;
  });
}
function fh(e, t) {
  const n = new Set(bi(t));
  return e.singleNode && n.add(e.startNode.id), [...n];
}
function ph(e, t) {
  const n = Number(e.asset_cate_id || 0);
  return n === 0 || n === Number(t || 0);
}
function bi(e) {
  const t = /* @__PURE__ */ new Set(), n = String(e.start_node_id || "");
  n && t.add(n);
  for (const r of e.node_runs || [])
    r.node_key && t.add(r.node_key);
  for (const r of e.node_results || [])
    r.node_key && t.add(r.node_key);
  for (const r of e.execution_plan?.nodes || [])
    r.id && t.add(r.id);
  return [...t];
}
function $s(e) {
  return new Set(
    (e.node_results || []).filter(
      (t) => Kr(_n(t))
    ).map((t) => t.node_key).filter(Boolean)
  );
}
function mh(e) {
  return Vd(e).map((t) => t.run);
}
function Vd(e) {
  const t = /* @__PURE__ */ new Set(), n = [];
  for (const r of e) {
    const o = /* @__PURE__ */ new Set();
    for (const i of bi(r))
      t.has(i) || (t.add(i), o.add(i));
    if (o.size === 0)
      continue;
    const s = String(r.status || "").trim().toLowerCase();
    (s === "running" || s === "pending" || s === "waiting") && n.push({ run: r, managedNodeIds: o });
  }
  return n;
}
function ds(e) {
  return e.map(Ud).filter((t) => !!t);
}
function Ud(e) {
  const t = br(e);
  return t.run_id || t.request_id ? t : null;
}
function gh(e, t) {
  const n = /* @__PURE__ */ new Set();
  for (const r of e) {
    const o = Number(r.run_id || 0);
    o > 0 && !yh(r, t) && n.add(o);
  }
  return [...n];
}
function yh(e, t) {
  const n = Number(e.asset_cate_id || 0);
  return (n ? [t[String(n)]].filter(
    (o) => !!o
  ) : Object.values(t)).some(
    (o) => Ld(e, o)
  );
}
function Ld(e, t) {
  const n = String(e.status || "").trim().toLowerCase();
  if (!["success", "fail", "failed", "error", "canceled", "cancelled"].includes(
    n
  ))
    return !1;
  const r = new Map(t.nodes.map((s) => [s.id, s]));
  if (!e.single_node) {
    const s = (e.node_results || []).filter(
      (a) => a.node_key
    );
    if (s.length === 0)
      return !1;
    let i = 0;
    for (const a of s) {
      const d = r.get(a.node_key);
      if (d && (i += 1, !wa(d.resultRef, e, a)))
        return !1;
    }
    return i > 0;
  }
  const o = r.get(String(e.start_node_id || ""));
  return wa(o?.resultRef, e);
}
function wa(e, t, n) {
  if (!e)
    return !1;
  const r = Number(t.execution_id || 0), o = Number(e.execution_id || 0);
  if (r > 0 && o > 0 && o >= r)
    return !0;
  const s = Number(t.run_id || 0), i = Number(e.run_id || 0);
  if (s > 0 && i > 0 && i >= s)
    return !0;
  const a = Number(n?.node_run_id || 0), d = Number(e.node_run_id || 0);
  if (a > 0 && d > 0 && d >= a)
    return !0;
  const l = String(n?.request_id || t.request_id || "");
  return !!(l && e.request_id === l);
}
function To(e) {
  const t = Number(e.execution_id || 0);
  if (t > 0)
    return `execution:${t}`;
  const n = Number(e.run_id || 0);
  return n > 0 ? `run:${n}` : `request:${String(e.request_id || "")}`;
}
function ba(e, t) {
  const n = /* @__PURE__ */ new Map();
  for (const r of e)
    n.set(To(r), r);
  for (const r of t)
    n.set(To(r), r);
  return [...n.values()].sort(hh).slice(0, 50);
}
function hh(e, t) {
  const n = Number(t.execution_id || 0) - Number(e.execution_id || 0);
  if (n !== 0)
    return n;
  const r = _a(t) - _a(e);
  return r !== 0 ? r : Number(t.run_id || 0) - Number(e.run_id || 0);
}
function _a(e) {
  const t = Date.parse(String(e.updated_at || e.created_at || ""));
  return Number.isFinite(t) ? t : 0;
}
function wh(e) {
  const t = bh(e);
  if (t.size === 0)
    return !1;
  const n = /* @__PURE__ */ new Set();
  for (const r of e.node_results || []) {
    const o = _n(r);
    if (o === "waiting" || o === "running" || o === "pending")
      return !1;
    Kr(o) && n.add(r.node_key);
  }
  for (const r of t)
    if (!n.has(r))
      return !1;
  return !0;
}
function bh(e) {
  const t = /* @__PURE__ */ new Set();
  for (const o of e.execution_plan?.nodes || [])
    _h(o) && t.add(o.id);
  if (t.size > 0)
    return t;
  const n = /* @__PURE__ */ new Set();
  for (const o of e.node_runs || [])
    o.node_key && n.add(o.node_key);
  if (n.size > 0)
    return n;
  const r = String(e.start_node_id || "");
  return r && (e.node_results || []).some((o) => o.node_key === r) ? /* @__PURE__ */ new Set([r]) : /* @__PURE__ */ new Set();
}
function _h(e) {
  return ["asset", "power", "agent", "flow"].includes(String(e.type || "")) ? !0 : e.type !== "function" ? !1 : e.function_key === "save" || e.function_key === "display";
}
function Nh(e, t) {
  const n = e.start_node_id || e.execution_plan?.order?.[0] || e.execution_plan?.nodes?.[0]?.id || "";
  if (n) {
    const r = t.find((o) => o.id === n);
    if (r)
      return r;
  }
  return t.find(Cw) || t[0] || null;
}
function Ih(e, t) {
  return [
    e.run_id || e.request_id || "",
    Mo(t)
  ].join(":");
}
function Kr(e) {
  const t = String(e || "").trim().toLowerCase();
  return t === "success" || t === "fail" || t === "canceled" || t === "cancelled";
}
function _n(e) {
  if (!e)
    return "";
  const t = String(e.status || e.result?.status || "").trim().toLowerCase();
  return t === "error" ? "fail" : t === "cancelled" ? "canceled" : t;
}
function Sh(e) {
  return `canvas-${typeof crypto < "u" && typeof crypto.randomUUID == "function" ? crypto.randomUUID() : `${Date.now()}-${Math.random().toString(36).slice(2)}`}-${e}`.slice(0, 64);
}
function Kd(e, t, n) {
  const r = typeof n == "string" ? n : yo(n), o = Math.floor(Date.now() / 5e3);
  return `${e}-${t}-${o}-${qd(r)}`.slice(
    0,
    96
  );
}
function qd(e) {
  let t = 5381;
  for (let n = 0; n < e.length; n += 1)
    t = t * 33 ^ e.charCodeAt(n);
  return (t >>> 0).toString(36);
}
function pr(e, t, n) {
  vh(e, t), lh(e, t, n);
}
function vh(e, t) {
  if (t.status !== "waiting")
    return;
  const n = t.pending_node;
  if (!n?.node_key)
    return;
  const r = e.nodes.find((d) => d.id === n.node_key);
  if (!r)
    return;
  const o = _i(n, r);
  if (!o)
    return;
  const s = wi(r, o), i = Wn(r), a = zs(
    i,
    s
  );
  if (yo(i) !== yo(a)) {
    const d = {
      ...r,
      feedbackRequests: a
    };
    e.nodes = e.nodes.map(
      (l) => l.id === r.id ? d : l
    ), e.onNodeResult(r.id, { feedbackRequests: a });
  }
  e.setRunningNode?.((d) => ({
    ...d,
    [r.id]: {
      nodeId: r.id,
      title: r.title,
      startedAt: d[r.id]?.startedAt || Date.now(),
      progress: 92,
      status: "waiting"
    }
  }));
}
async function Ch(e, t) {
  const n = t.pending_node;
  if (!n?.node_key)
    throw new Error("画布运行等待反馈，但缺少等待节点");
  const r = e.nodes.find((i) => i.id === n.node_key);
  if (!r)
    throw new Error("画布运行等待节点不存在");
  const o = _i(n, r);
  if (!o || !e.requestFlowFeedback)
    throw new Error(`${r.title} 需要补充信息，请单独处理后继续`);
  const s = await e.requestFlowFeedback({ node: r, prompt: o });
  return Gd(
    e.projectId,
    t,
    n,
    o,
    s
  );
}
async function Gd(e, t, n, r, o) {
  return r.interaction ? _p({
    projectId: e,
    runId: Number(r.interaction.runId || n.child_run_id || 0),
    nodeRunId: Number(r.interaction.nodeRunId || 0),
    interactionId: String(r.interaction.interaction.id || ""),
    data: o
  }) : wp({
    projectId: e,
    runId: Number(t.run_id || 0),
    requestId: String(t.request_id || ""),
    nodeKey: n.node_key,
    approvalId: Number(r.approval.id || 0),
    feedback: o
  });
}
function kh(e, t, n) {
  for (const r of e) {
    if (String(r.status || "").trim().toLowerCase() !== "waiting")
      continue;
    const o = r.pending_node;
    if (!o || o.node_key !== t.id)
      continue;
    const s = _i(o, t);
    if (!s)
      continue;
    if (wi(t, s).id === n.id)
      return { run: r, pending: o, prompt: s };
  }
  return null;
}
function _i(e, t) {
  const n = e.interaction && typeof e.interaction == "object" ? e.interaction : Pn(e);
  if (n?.interaction?.id)
    return Gc({
      runId: Number(n.run_id || e.child_run_id || 0),
      nodeRunId: Number(n.node_run_id || 0),
      interaction: n.interaction
    });
  if ((e.node_type || t.type) === "flow") {
    const o = e.output && typeof e.output == "object" ? { ...e.output } : e.result && typeof e.result == "object" ? { ...e.result } : {}, s = Se(
      e.approval,
      e.result?.approval,
      e.output?.approval
    );
    s && !Array.isArray(o.approvals) && (o.approvals = [s]);
    const i = Mm(o);
    return Am(i);
  }
  return Pm(
    Xd(e),
    t.title
  );
}
function Hd(e, t, n) {
  let r = 0;
  const o = new Map(e.nodes.map((s) => [s.id, s]));
  for (const s of t) {
    const i = o.get(s.node_key), a = _n(s);
    if (!i || !Kr(a))
      continue;
    const d = Mo(s);
    if (a === "success" && d && n?.has(d))
      continue;
    const l = Th(e, i, s);
    e.onNodeResult(i.id, l), d && n?.add(d), a === "success" && (r += 1);
    const u = {
      ...i,
      ...l
    };
    o.set(i.id, u), e.nodes = e.nodes.map(
      (m) => m.id === i.id ? u : m
    ), a === "success" && l.asset && e.onAssetCreated(l.asset), a === "success" && e.requestNodeTitle?.(u, s);
  }
  return r;
}
function Rh(e, t) {
  const n = hi(e).prompt.trim(), o = (t ? iu(e.id, t.nodes, t.edges) : null)?.text.trim() || "", s = [n, o ? `上游内容：
${o}` : ""].filter(Boolean).join(`

`);
  return Array.from(s).slice(0, Cy).join("");
}
function xh(e, t) {
  return e.type !== "power" || e.titleMode !== "auto" || e.storyboardItem || _n(t) !== "success" || Yd(t) <= 0 || !Wd(e) ? !1 : er(e.power, e.kind, e.outputType).viewMode !== "storyboard";
}
function Wd(e) {
  const t = Number(e.nodeNo || 0);
  return t > 0 && e.title.trim() === bc(e, t).trim();
}
function Yd(e) {
  const t = e, n = [
    t.version_id,
    t.versionId,
    t.version?.id,
    t.asset?.version_id,
    t.asset?.versionId,
    t.asset?.version?.id,
    t.result?.version_id,
    t.result?.versionId,
    t.result?.version?.id,
    t.result?.asset?.version_id,
    t.result?.asset?.version?.id,
    t.output?.version_id,
    t.output?.version?.id,
    t.output?.asset?.version_id,
    t.output?.asset?.version?.id,
    t.data?.version_id,
    t.data?.version?.id,
    t.data?.asset?.version_id,
    t.data?.asset?.version?.id
  ];
  for (const r of n) {
    const o = Number(r || 0);
    if (Number.isInteger(o) && o > 0)
      return o;
  }
  return 0;
}
function Mo(e) {
  return [
    e.node_key,
    e.execution_id || "",
    e.request_id || "",
    e.node_run_id || "",
    e.child_run_id || "",
    e.status || "",
    e.source_signature || "",
    Number(
      e.version?.id || e.asset?.version?.id || e.result?.version?.id || 0
    ),
    Number(e.asset?.id || e.result?.asset?.id || 0)
  ].join(":");
}
function Th(e, t, n) {
  const r = Xd(n), o = _n(n);
  if (o === "fail")
    return us(t, {
      resultRef: Io(r),
      runError: wm(n)
    });
  if (o === "canceled" || o === "cancelled")
    return us(t, {
      resultRef: Io(r),
      runError: "节点运行已取消"
    });
  const s = nm({
    result: r,
    previousAsset: t.asset,
    previousAssets: e.space.assets
  }), i = (a) => us(
    t,
    Mh(
      t,
      a,
      n.source_signature
    )
  );
  return i(s ? {
    ...bn(
      t,
      rm(r, s),
      "后端执行结果"
    ),
    runError: ""
  } : {
    ...bn(t, r, "后端执行结果"),
    runError: ""
  });
}
function Mh(e, t, n) {
  const r = e.storyboardItem, o = String(n || "").trim();
  if (!r || !o)
    return t;
  const s = String(r.sourceSignature || "").trim();
  return {
    ...t,
    storyboardItem: {
      ...r,
      resultSourceSignature: o,
      stale: s ? o !== s : !!r.stale
    }
  };
}
function us(e, t) {
  const n = Wn(e);
  return n.length === 0 || Array.isArray(t.feedbackRequests) ? t : {
    ...t,
    feedbackRequests: n
  };
}
function Xd(e) {
  return {
    ...e.result && typeof e.result == "object" ? e.result : {},
    execution_id: e.execution_id || e.result?.execution_id,
    run_id: e.run_id || e.result?.run_id,
    request_id: e.request_id || e.result?.request_id,
    node_run_id: e.node_run_id || e.result?.node_run_id,
    child_run_id: e.child_run_id || e.result?.child_run_id,
    child_request_id: e.child_request_id || e.result?.child_request_id,
    status: e.status || e.result?.status,
    error: e.error || e.result?.error,
    output: e.output ?? e.result?.output,
    asset: e.asset || e.result?.asset,
    version: e.version || e.result?.version || e.asset?.version,
    agent_run_id: e.agent_run_id || e.result?.agent_run_id
  };
}
function Ah(e, t) {
  return e.status === "waiting" ? `已执行 ${t} 个连接节点，等待补充信息` : e.status === "fail" || e.status === "error" ? Vc(
    e,
    `画布运行失败，已执行 ${t} 个连接节点`
  ) : `已执行 ${t} 个连接节点`;
}
async function Dh(e) {
  const t = Ph(e.assetCateId);
  if (!t)
    throw new Error("当前团队没有配置资产分类，不能保存作品");
  const n = await Sp({
    projectId: e.projectId,
    assetCateId: t,
    name: e.name,
    kind: e.kind,
    content: e.content,
    runId: Number(e.runRef?.run_id || 0),
    nodeRunId: Number(e.runRef?.node_run_id || 0),
    releaseId: Number(e.runRef?.release_id || 0),
    nodeKey: e.nodeKey,
    requestId: e.requestId,
    source: e.source
  }), r = e.previousAsset || e.previousAssets?.find((o) => o.id === n.id) || null;
  return An(n, r);
}
function Ph(e) {
  return Math.max(0, Number(e || 0));
}
function _r(e) {
  const t = In(e), n = jn(
    t,
    Bo(e, t)
  );
  return En(n) || (n.text = Kt(t, "")), n;
}
function Bo(e, t) {
  const n = Gh(t);
  return e.type === "power" ? $e(
    String(e.power?.kind || ""),
    n,
    String(e.asset?.kind || ""),
    String(e.kind || "")
  ) : $e(
    String(e.asset?.kind || ""),
    String(e.power?.kind || ""),
    n,
    String(e.kind || "")
  );
}
function ls(e) {
  const t = _r(e);
  if (En(t))
    return t;
  const n = jn(
    In(e),
    String(e.kind || e.power?.kind || "")
  );
  return En(n) || (n.text = Kt(In(e), "")), n;
}
function Ni(e) {
  return Fh(e) || Vh(e);
}
function Gn(e) {
  return e.storyboardItem?.itemType === "subtitle" ? { text: e.description || "字幕轨已准备" } : In(e);
}
function fs(e) {
  return [
    e.asset?.version?.content,
    e.resultOutput,
    Gn(e)
  ];
}
function zh(e) {
  const t = e.composerDraft?.paramValues || {};
  return $e(
    t.aspectRatio,
    t.aspect_ratio,
    t.ratio
  );
}
function Fh(e) {
  return Eh(
    e.asset?.version?.content,
    e.resultOutput
  );
}
function Eh(...e) {
  for (const t of e) {
    const n = Zn(t);
    if (n)
      return n;
  }
  return null;
}
function Ao(...e) {
  for (const t of e) {
    const n = Zd(t);
    if (Ye(n))
      return n;
  }
  return "";
}
function Zd(e) {
  const t = Ue(e), n = qr(t);
  if (n !== t)
    return Zd(n);
  const r = Ny?.(t) ?? t, o = Kn(r, /* @__PURE__ */ new Set());
  if (Ye(o))
    return o;
  if (r !== t) {
    const a = Kn(t, /* @__PURE__ */ new Set());
    if (Ye(a))
      return a;
  }
  const s = Vo(t);
  if (s)
    return _s?.(s) ?? s;
  const i = Ii(e);
  return i !== t && Ye(i) ? i : "";
}
function Kn(e, t) {
  const n = Ue(e), r = qr(n);
  if (r !== n)
    return Kn(r, t);
  if (typeof n == "string") {
    const d = Qd(n);
    if (Ye(d))
      return d;
    const l = Dr(n);
    return l ? { text: l } : On(n) ? "" : n;
  }
  if (Array.isArray(n)) {
    const d = n.map((l) => Kn(l, t)).filter(Ye);
    return d.length > 0 ? d : "";
  }
  if (!n || typeof n != "object")
    return n;
  if (tr(n)) {
    const d = au(n);
    if (d !== void 0)
      return Kn(d, t);
    const l = cu(n);
    if (l)
      return { text: l };
    const u = Zn(n) || Ot(n);
    return u ? { rich: u } : n;
  }
  if (t.has(n))
    return "";
  if (t.add(n), ki(n))
    return js(n);
  if (eu(n))
    return n;
  for (const d of ["output", "result", "data", "content", "json", "value"]) {
    if (!(d in n))
      continue;
    const l = Kn(n[d], t);
    if (Ye(l))
      return l;
  }
  const o = Si(n);
  if (o) {
    const d = { rich: o };
    return _s?.(d) ?? d;
  }
  const s = Ot(n);
  if (s)
    return { rich: s };
  const i = Vo(n);
  if (i)
    return _s?.(i) ?? i;
  const a = $t(n);
  if (a !== n)
    return Kn(a, t);
  if (Oh(n))
    return js(n);
  if (Ko(n)) {
    const d = $e(n.message, n.error, n.status);
    return d ? { text: d } : "";
  }
  return Uo(n) ? n : "";
}
function Oh(e) {
  return e && typeof e == "object" && !Array.isArray(e) && (e.format || e.result_mode || e.rich || e.images || e.videos || e.audios || e.files);
}
function js(e) {
  const t = {}, n = Ue(e.content);
  n && typeof n == "object" && !Array.isArray(n) && Ia(t, n), Ia(t, e);
  const r = $h(e);
  return r && (t.text = r), !Ye(t) && n && typeof n == "object" ? n : Uo(t) ? t : "";
}
function $h(e) {
  const t = $e(e.text);
  if (t)
    return t;
  const n = Ue(e.content);
  return typeof n == "string" ? n.trim() : n && typeof n == "object" && !Array.isArray(n) ? $e(n.text) : "";
}
function Ii(e) {
  const t = qr(e);
  if (t !== e)
    return Ii(t);
  const n = Vo(e);
  if (n)
    return n;
  const r = Qd(e);
  if (Ye(r))
    return r;
  const o = Ue(e), s = Ot(o);
  if (s)
    return { rich: s };
  const i = $t(o);
  if (i !== o) {
    const a = Ot(i);
    return a ? { rich: a } : i;
  }
  return o;
}
function Vo(e) {
  const t = Zn(e);
  return t ? { rich: t } : null;
}
function Zn(e, t = /* @__PURE__ */ new Set()) {
  const n = Ue(e), r = qr(n);
  if (r !== n)
    return Zn(r, t);
  if (Array.isArray(n)) {
    if (t.has(n))
      return null;
    t.add(n);
    for (const i of n) {
      const a = Zn(i, t);
      if (a)
        return a;
    }
    return null;
  }
  if (!n || typeof n != "object" || t.has(n))
    return null;
  if (t.add(n), tr(n))
    return Jd(n, t) || n;
  const o = n, s = [
    Je(o, ["output", "content", "rich"]),
    Je(o, ["output", "rich"]),
    Je(o, ["content", "rich"]),
    o.content,
    o.rich,
    o.text,
    o.summary
  ];
  for (const i of s) {
    if (i === n)
      continue;
    const a = Zn(i, t);
    if (a)
      return a;
  }
  return null;
}
function Jd(e, t) {
  const n = Do(e);
  for (const r of n) {
    const o = Na(r, t);
    if (o)
      return o;
  }
  return Na(n.join(""), t);
}
function Na(e, t) {
  const n = String(e || "").trim();
  if (!On(n))
    return null;
  const r = ec(n);
  return r === n || r === e ? null : Zn(r, t);
}
function jh(e) {
  return Do(e).join("");
}
function Do(e, t = /* @__PURE__ */ new Set()) {
  if (!e)
    return [];
  if (typeof e == "string")
    return [e];
  if (Array.isArray(e))
    return e.flatMap((r) => Do(r, t));
  if (typeof e != "object")
    return [];
  if (t.has(e))
    return [];
  t.add(e);
  const n = [];
  return typeof e.text == "string" && n.push(e.text), Array.isArray(e.content) && n.push(...Do(e.content, t)), n;
}
function Qd(e) {
  const t = qt(e);
  if (t)
    return { rich: t };
  const n = Ue(e);
  if (!n)
    return "";
  if (typeof n == "string") {
    const r = Dr(n);
    return r ? { text: r } : "";
  }
  return "";
}
function qt(e, t = /* @__PURE__ */ new Set()) {
  const n = Ue(e);
  if (tr(n))
    return Jd(n, t) || n;
  if (Array.isArray(n))
    return Ot(Ci(n));
  if (!n || typeof n != "object" || t.has(n))
    return null;
  t.add(n);
  const r = n, o = Si(r);
  if (o)
    return o;
  const s = [
    Je(r, ["output", "content", "rich"]),
    Je(r, ["output", "content"]),
    Je(r, ["output", "rich"]),
    Je(r, ["content", "output", "content", "rich"]),
    Je(r, ["content", "output", "content"]),
    Je(r, ["content", "rich"]),
    Je(r, ["data", "output", "content", "rich"]),
    Je(r, ["data", "output", "content"]),
    Je(r, ["data", "content", "rich"]),
    r.rich,
    r.output,
    r.result,
    r.content,
    r.data,
    r.value,
    r.json,
    r.text,
    r.message
  ];
  for (const i of s) {
    if (i == null || i === n)
      continue;
    const a = qt(i, t);
    if (a)
      return a;
  }
  for (const [i, a] of Object.entries(r)) {
    if (!du(i) || !a || typeof a != "object")
      continue;
    const d = qt(a, t);
    if (d)
      return d;
  }
  return null;
}
function Si(e) {
  return tr(e) ? e : Array.isArray(e.content) && (String(e.format || "").toLowerCase() === "rich_json" || String(e.content?.format || "").toLowerCase() === "rich_json" || e.type === void 0) ? Ot({
    type: "doc",
    content: e.content
  }) : (String(e.format || "").toLowerCase() === "rich_json" || String(e.content?.format || "").toLowerCase() === "rich_json") && e.rich != null ? qt(e.rich) : (String(e.format || "").toLowerCase() === "rich_json" || String(e.content?.format || "").toLowerCase() === "rich_json") && e.content?.rich != null ? qt(e.content.rich) : null;
}
function tr(e) {
  return !!(e && typeof e == "object" && !Array.isArray(e) && e.type === "doc" && Array.isArray(e.content));
}
function Ia(e, t) {
  for (const n of [
    "format",
    "title",
    "text",
    "reasoning",
    "rich",
    "images",
    "videos",
    "audios",
    "files",
    "json",
    "error",
    "progress",
    "meta"
  ])
    Ye(t[n]) && (e[n] = n === "rich" ? Bh(t[n]) : t[n]);
}
function Bh(e) {
  return qt(e) || Ot(Ci(e)) || Ot(e) || e;
}
function Uo(e) {
  return Object.entries(e).some(([t, n]) => t.startsWith("_") || t === "format" ? !1 : Ye(n));
}
function eu(e) {
  return !e || typeof e != "object" || Array.isArray(e) || "output" in e || "result" in e || "data" in e || "content" in e || "kind" in e || "event" in e ? !1 : [
    "text",
    "rich",
    "images",
    "videos",
    "audios",
    "files",
    "json",
    "error"
  ].some((t) => Ye(e[t]));
}
function Ye(e) {
  if (e == null || e === "")
    return !1;
  if (typeof e == "string") {
    const t = e.trim();
    return t.length > 0 && !On(t) && !Ut(t);
  }
  return typeof e == "number" || typeof e == "boolean" ? !0 : Array.isArray(e) ? e.some(Ye) : typeof e != "object" ? !1 : qt(e) ? !0 : Ko(e) ? !1 : Uo(e);
}
function vi(e) {
  return Kt(
    In(e),
    e.description || e.title
  );
}
function Vh(e) {
  const t = [
    In(e),
    e.asset?.version?.content,
    e.description
  ];
  for (const n of t) {
    const r = qt(n) || Ot(Ii(n)) || Ot($t(n)) || Ot(n);
    if (r)
      return r;
  }
  return null;
}
function Kt(e, t = "") {
  const n = $t(e), r = Ot(n), o = r ? qn(r).trim() : "";
  if (o && !Ut(o))
    return o;
  const s = qn(n).trim();
  if (Ut(s))
    return "";
  const i = Dr(s);
  if (i)
    return i;
  if (s && !On(s))
    return s;
  if (Ri(s)) {
    const u = qn(
      $t(Ue(s))
    ).trim();
    if (u && u !== s && !Ut(u))
      return u;
  }
  const a = String(t || "").trim();
  if (Ut(a))
    return "";
  const d = Dr(a);
  if (d)
    return d;
  if (!On(a))
    return a;
  const l = qn(
    $t(Ue(a))
  ).trim();
  return l && l !== a && !Ut(l) ? l : "";
}
function Ut(e) {
  const t = e.trim();
  return t ? Uh(t) || Lh(t) : !1;
}
function Uh(e) {
  const t = e.trim();
  return t === "map[]" || t === "<nil>";
}
function Lh(e) {
  const t = e.trim();
  return t ? /^(i\s+(will|ll|'ll)\s+(start|begin)|let'?s\s+(list|check|inspect)|first,\s*i\s+(will|ll|'ll)|i'?m\s+going\s+to\s+(check|inspect))/i.test(
    t
  ) : !1;
}
function jn(e, t) {
  const n = {
    text: "",
    imageUrl: "",
    videoUrl: "",
    audioUrl: "",
    fileUrl: ""
  }, r = $t(e);
  return $r(n, r, t), !En(n) && r !== e && $r(n, e, t), Xn(n) && On(n.text) && (n.text = ""), n;
}
function Kh(e, t) {
  return {
    text: $e(e.text, t.text),
    imageUrl: e.imageUrl || t.imageUrl,
    videoUrl: e.videoUrl || t.videoUrl,
    audioUrl: e.audioUrl || t.audioUrl,
    fileUrl: e.fileUrl || t.fileUrl
  };
}
function $r(e, t, n, r = /* @__PURE__ */ new Set(), o = 0) {
  if (o > 12 || t == null)
    return;
  if (typeof t == "string") {
    Sa(e, t, n);
    return;
  }
  if (Array.isArray(t)) {
    for (const l of t)
      if ($r(e, l, n, r, o + 1), Xn(e))
        return;
    return;
  }
  if (typeof t != "object") {
    e.text = String(t);
    return;
  }
  if (r.has(t))
    return;
  r.add(t);
  const s = t, i = qh(e, s, n), a = Kt(t, "");
  a && a !== i && !Nn(a) && (e.text ||= a);
  const d = ar(s.url, s.src, s.href);
  if (d && d !== i && Sa(e, d, n), e.imageUrl ||= ar(
    s.image,
    s.image_url,
    s.imageUrl,
    bt(s.images),
    bt(s.imageUrls)
  ), e.videoUrl ||= ar(
    s.video,
    s.video_url,
    s.videoUrl,
    bt(s.videos),
    bt(s.videoUrls)
  ), e.audioUrl ||= ar(
    s.audio,
    s.audio_url,
    s.audioUrl,
    bt(s.audios),
    bt(s.audioUrls)
  ), e.fileUrl ||= ar(
    s.file,
    s.file_url,
    s.fileUrl,
    bt(s.files),
    bt(s.fileUrls)
  ), !Xn(e)) {
    for (const l of ["output", "result", "content", "body", "data", "rich"])
      if (s[l] && typeof s[l] == "object" && ($r(e, s[l], n, r, o + 1), Xn(e)))
        return;
  }
  if (!e.text && !En(e) && !Jh(s) && Uo(s))
    try {
      const l = JSON.stringify(t, null, 2);
      gr(l) || (e.text = l);
    } catch {
      const l = String(t);
      gr(l) || (e.text = l);
    }
}
function qh(e, t, n) {
  const r = tu(
    n,
    t.kind,
    t.media_kind,
    t.mediaKind,
    t.media_type,
    t.mediaType,
    t.type,
    t.name
  );
  if (!r)
    return "";
  const o = ar(...Hh(t, r));
  return o ? (r === "image" && (e.imageUrl ||= o), r === "video" && (e.videoUrl ||= o), r === "audio" && (e.audioUrl ||= o), r === "file" && (e.fileUrl ||= o), o) : "";
}
function Gh(e) {
  if (!e || typeof e != "object" || Array.isArray(e))
    return "";
  const t = e;
  return tu(
    t.kind,
    t.media_kind,
    t.mediaKind,
    t.media_type,
    t.mediaType,
    t.type,
    t.name
  );
}
function tu(...e) {
  for (const t of e) {
    const n = Lo(String(t || ""));
    if (n)
      return n;
  }
  return "";
}
function Lo(e) {
  const t = e.trim().toLowerCase();
  return t === "image" || t === "images" || t === "picture" || t === "pictures" || t === "mediaimage" || t === "editor media image" || t === "editormediaimage" || t.includes("image") || t === "图片" || t === "图像" ? "image" : t === "video" || t === "videos" || t === "mediavideo" || t === "editor media video" || t === "editormediavideo" || t.includes("video") || t === "视频" ? "video" : t === "audio" || t === "audios" || t === "music" || t === "voice" || t === "mediaaudio" || t === "editor media audio" || t === "editormediaaudio" || t.includes("audio") || t === "音频" || t === "音乐" || t === "语音" ? "audio" : t === "file" || t === "files" || t === "attachment" || t === "attachments" || t === "mediafile" || t === "editorfile" || t === "editor media file" || t === "editormediafile" || t === "文件" || t === "附件" ? "file" : "";
}
function Hh(e, t) {
  const n = [
    e.url,
    e.src,
    e.href,
    e.path,
    e.file_url,
    e.fileUrl,
    e.text,
    e.content,
    e.value,
    Je(e, ["attrs", "src"]),
    Je(e, ["attrs", "url"]),
    Je(e, ["attrs", "href"])
  ];
  return t === "image" ? [
    e.image,
    e.image_url,
    e.imageUrl,
    bt(e.images),
    bt(e.imageUrls),
    ...n
  ] : t === "video" ? [
    e.video,
    e.video_url,
    e.videoUrl,
    bt(e.videos),
    bt(e.videoUrls),
    ...n
  ] : t === "audio" ? [
    e.audio,
    e.audio_url,
    e.audioUrl,
    bt(e.audios),
    bt(e.audioUrls),
    ...n
  ] : [
    e.file,
    e.file_url,
    e.fileUrl,
    bt(e.files),
    bt(e.fileUrls),
    ...n
  ];
}
function Sa(e, t, n) {
  const r = t.trim();
  if (!r || Ut(r))
    return;
  if (Ri(r)) {
    const a = Ue(r);
    if (a !== r && ($r(e, a, n), Xn(e)))
      return;
    const d = Kt(a, "");
    d && !Nn(d) && (e.text ||= d);
    return;
  }
  const o = Dr(r);
  if (o) {
    e.text ||= o;
    return;
  }
  const s = qn(r);
  if (s && s !== r) {
    e.text ||= s;
    return;
  }
  const i = Wh(r, n);
  if (i) {
    Xh(e, i.kind, i.url), e.text ||= i.caption;
    return;
  }
  if (Nn(r)) {
    const a = Lo(n);
    if (a === "image" || /\.(png|jpe?g|gif|webp|avif|svg)(\?.*)?$/i.test(r)) {
      e.imageUrl ||= r;
      return;
    }
    if (a === "video" || /\.(mp4|webm|mov|m4v)(\?.*)?$/i.test(r)) {
      e.videoUrl ||= r;
      return;
    }
    if (a === "audio" || /\.(mp3|wav|ogg|m4a|aac)(\?.*)?$/i.test(r)) {
      e.audioUrl ||= r;
      return;
    }
    e.fileUrl ||= r;
    return;
  }
  e.text ||= r;
}
function Wh(e, t) {
  const n = nu(e, t), r = e.match(
    /!\[([^\]]*)\]\(\s*<?([^)\s>]+)>?(?:\s+["'][^"']*["'])?\s*\)/
  );
  if (r) {
    const l = ps(r[2]);
    if (l)
      return {
        kind: "image",
        url: l,
        caption: io(e, r[0], r[1])
      };
  }
  const o = e.match(
    /!\[[^\]]*]\(\s*<?((?:https?:\/\/|data:|blob:)[^\s<>)]+)/i
  );
  if (o) {
    const l = ps(o[1]);
    if (l)
      return {
        kind: "image",
        url: l,
        caption: io(e, o[0], "")
      };
  }
  const s = /\[([^\]]+)\]\(\s*<?([^)\s>]+)>?(?:\s+["'][^"']*["'])?\s*\)/g;
  let i;
  for (; i = s.exec(e); ) {
    const l = ps(i[2]), u = va(l, n);
    if (u)
      return {
        kind: u,
        url: l,
        caption: io(e, i[0], i[1])
      };
  }
  const a = Zh(e), d = va(a, n);
  return d ? {
    kind: d,
    url: a,
    caption: io(e, a, "")
  } : null;
}
function nu(e, t) {
  return Lo(t) ? t : Yh(e) ? "image" : t;
}
function Yh(e) {
  const t = /(?:图片|图像|image|photo|picture).{0,40}(?:https?:\/\/|data:|blob:)/i;
  return /!\[[^\]]*]\(/.test(e) || t.test(e);
}
function Xh(e, t, n) {
  t === "image" && (e.imageUrl ||= n), t === "video" && (e.videoUrl ||= n), t === "audio" && (e.audioUrl ||= n), t === "file" && (e.fileUrl ||= n);
}
function va(e, t) {
  if (!e || !Nn(e))
    return "";
  const n = Lo(t);
  return n === "image" || /\.(png|jpe?g|gif|webp|avif|svg)(\?.*)?$/i.test(e) ? "image" : n === "video" || /\.(mp4|webm|mov|m4v)(\?.*)?$/i.test(e) ? "video" : n === "audio" || /\.(mp3|wav|ogg|m4a|aac)(\?.*)?$/i.test(e) ? "audio" : n === "file" ? "file" : "";
}
function io(e, t, n) {
  const r = e.replace(t, "").replace(/\s+/g, " ").trim();
  return r && r !== e.trim() && !Nn(r) ? r : String(n || "").trim();
}
function ps(e) {
  const t = ru(e);
  return Nn(t) ? t : "";
}
function Zh(e) {
  const t = e.match(/(?:https?:\/\/|data:|blob:)[^\s<>)]+/i);
  return t ? ru(t[0]) : "";
}
function ru(e) {
  return String(e || "").trim().replace(/^<|>$/g, "").replace(/[.,，。；;]+$/g, "");
}
function Jh(e) {
  return !!(e.output || e.result || e.content || e.rich || e.agent_run_id || e.approval_id);
}
function En(e) {
  const t = String(e.text || "").trim();
  return !!(t && !gr(t) || e.imageUrl || e.videoUrl || e.audioUrl || e.fileUrl);
}
function Qh(...e) {
  for (const t of e) {
    if (typeof t == "string" && t.trim())
      return t.trim();
    if (t && typeof t == "object") {
      const n = $e(
        t.url,
        t.src,
        t.href,
        t.path,
        t.file,
        t.file_url,
        t.fileUrl,
        t.image,
        t.image_url,
        t.imageUrl,
        t.video,
        t.video_url,
        t.videoUrl,
        t.audio,
        t.audio_url,
        t.audioUrl,
        Je(t, ["attrs", "src"]),
        Je(t, ["attrs", "url"]),
        Je(t, ["attrs", "href"])
      );
      if (n)
        return n;
    }
  }
  return "";
}
function ar(...e) {
  const t = Qh(...e);
  return Nn(t) ? t : "";
}
function bt(e) {
  return Array.isArray(e) ? e[0] : void 0;
}
function Nn(e) {
  return /^(https?:\/\/|\/|data:|blob:)/i.test(e);
}
function ir(e, t) {
  return e?.screenToFlowPosition ? e.screenToFlowPosition(t) : e?.project ? e.project(t) : t;
}
function ew(e, t, n, r) {
  const o = r?.x ?? e.x + 34, s = r?.y ?? e.y + 34;
  return {
    ...e,
    id: `local-${e.type}-${Date.now()}-${n}`,
    nodeNo: void 0,
    title: `${e.title} 副本`,
    titleMode: "manual",
    x: o,
    y: s,
    assetCateId: e.assetCateId || t,
    group: e.type === "group" && e.group?.origin === "script" ? { origin: "manual" } : e.group,
    storyboardItem: void 0,
    storyboardMaterializedSignature: void 0,
    local: !0
  };
}
function ou(e, t) {
  const n = new Map(e.map((u) => [u.id, u])), r = new Set(
    t.flatMap(
      (u) => wo(
        e,
        jr(u).sourceNodeId
      ).map((m) => m.id)
    )
  ), o = /* @__PURE__ */ new Map(), s = /* @__PURE__ */ new Map();
  for (const u of e) {
    if (u.groupId) {
      const m = o.get(u.groupId) || [];
      m.push(u), o.set(u.groupId, m);
    }
    if (r.has(u.id)) {
      const m = tw(u);
      m && Ca(m).trim() !== "" && s.set(u.id, m);
    }
  }
  const i = /* @__PURE__ */ new Map(), a = /* @__PURE__ */ new Map(), d = /* @__PURE__ */ new Map();
  for (const u of t) {
    const { sourceNodeId: m, targetNodeId: N } = jr(u);
    if (n.has(N))
      for (const b of wo(e, m)) {
        if (uc(u) && Ws(b)) {
          const U = a.get(N) || [];
          U.push({ edge: u, source: b }), a.set(N, U);
        }
        const w = s.get(b.id), z = d.get(N) || /* @__PURE__ */ new Set();
        if (!w || z.has(b.id))
          continue;
        z.add(b.id), d.set(N, z);
        const F = i.get(N) || [];
        F.push(w), i.set(N, F);
      }
  }
  const l = /* @__PURE__ */ new Map();
  for (const [u, m] of i)
    l.set(u, {
      sources: m,
      text: m.map(Ca).join(`

`)
    });
  return {
    nodeById: n,
    groupMembersById: o,
    inputContextByNodeId: l,
    incomingMediaReferencesByNodeId: a
  };
}
function jr(e) {
  return {
    sourceNodeId: e.logicalFrom || e.from,
    targetNodeId: e.logicalTo || e.to
  };
}
function su(e, t, n) {
  const r = [];
  for (const o of t) {
    if (!uc(o))
      continue;
    const s = jr(o);
    if (s.targetNodeId === n)
      for (const i of wo(
        e,
        s.sourceNodeId
      ))
        Ws(i) && r.push({ edge: o, source: i });
  }
  return r;
}
function iu(e, t, n) {
  return ou(t, n).inputContextByNodeId.get(e) || null;
}
function tw(e) {
  if (!Mt(e))
    return null;
  const t = In(e), n = jn(
    t,
    Bo(e, t)
  );
  return En(n) || (n.text = Kt(
    t,
    e.description || e.title
  )), {
    nodeId: e.id,
    title: e.title,
    type: e.type,
    output: t,
    preview: n,
    resultRef: e.resultRef
  };
}
function nw(e, t) {
  return e === t ? !0 : !e || !t || e.text !== t.text ? !1 : e.sources.length === t.sources.length && e.sources.every((n, r) => {
    const o = t.sources[r];
    return n.nodeId === o.nodeId && n.title === o.title && n.type === o.type && n.output === o.output && n.resultRef === o.resultRef && n.preview.text === o.preview.text && n.preview.imageUrl === o.preview.imageUrl && n.preview.videoUrl === o.preview.videoUrl && n.preview.audioUrl === o.preview.audioUrl && n.preview.fileUrl === o.preview.fileUrl;
  });
}
function Ca(e) {
  const t = e.preview, n = t.text || t.imageUrl || t.videoUrl || t.audioUrl || t.fileUrl || fu(e.output);
  return String(n || "").trim() ? `[${e.title}]
${n}` : "";
}
function In(e) {
  return rw(
    Sl(e.asset?.version?.content, e.resultOutput)
  );
}
function rw(...e) {
  let t;
  for (const n of e) {
    if (n == null)
      continue;
    t === void 0 && (t = n);
    const r = au(n);
    if (r !== void 0)
      return r;
    const o = cu(n);
    if (o)
      return { text: o };
    const s = Ao(n) || $t(n);
    if (Ye(s) || xi(s))
      return s;
  }
  if (t !== void 0)
    return Ao(t) || $t(t);
}
function au(e) {
  const t = Ue(e), n = tr(t) ? t : qt(t);
  if (!n)
    return;
  const r = jh(n).trim();
  if (!On(r))
    return;
  const o = ec(r);
  if (o === r)
    return;
  const s = $t(o);
  if (Ye(s) || xi(s))
    return s;
}
function cu(e) {
  const t = Ue(e), n = tr(t) ? t : qt(t);
  return Ja(n);
}
function $t(e) {
  const t = Ue(e), n = qr(t);
  if (n !== t)
    return $t(n);
  if (eu(t))
    return t;
  if (ki(t)) {
    const s = js(t);
    if (Ye(s))
      return s;
  }
  const r = Vo(t);
  if (r)
    return r.rich;
  if (tr(t))
    return t;
  const o = qt(t);
  return o || Ci(Po(t, /* @__PURE__ */ new Set()));
}
function Po(e, t) {
  if (!e || typeof e != "object" || t.has(e))
    return e;
  t.add(e);
  const n = e, r = uu(n);
  if (r !== void 0)
    return r;
  const o = ow(n, t);
  if (o !== void 0)
    return o;
  for (const s of sw) {
    const i = Je(n, s);
    if (i === void 0 || i === e)
      continue;
    const a = Po(
      Ue(i),
      t
    );
    if (Bs(a))
      return a;
  }
  if (Ko(n))
    for (const s of ["output", "result", "data", "body"]) {
      if (n[s] === void 0 || n[s] === e)
        continue;
      const i = Po(
        Ue(n[s]),
        t
      );
      if (Bs(i))
        return i;
    }
  return e;
}
function ow(e, t) {
  for (const [n, r] of Object.entries(e)) {
    if (!du(n) || !r || typeof r != "object")
      continue;
    const o = Po(Ue(r), t);
    if (Bs(o))
      return o;
  }
}
function du(e) {
  return /^(node|step|task|power|agent)[_-]?\d+$/i.test(e);
}
const sw = [
  ["output", "content", "rich"],
  ["output", "content"],
  ["output", "rich"],
  ["content", "output", "content", "rich"],
  ["content", "output", "content"],
  ["content", "output", "rich"],
  ["content", "data", "text"],
  ["content", "data", "content"],
  ["content", "rich"],
  ["content", "text"],
  ["data", "output", "content", "rich"],
  ["data", "output", "content"],
  ["data", "content", "rich"],
  ["data", "content"],
  ["rich"]
];
function uu(e) {
  const t = Si(e);
  if (t)
    return t;
  if (String(e.result_mode || "").toLowerCase() === "inline" && e.content != null) {
    const n = Ue(e.content);
    if (n && typeof n == "object") {
      const r = uu(n);
      if (r !== void 0)
        return r;
    }
  }
}
function Ci(e) {
  const t = Ue(e);
  return t && typeof t == "object" && !Array.isArray(t) && !t.type && Array.isArray(t.content) ? {
    type: "doc",
    content: t.content
  } : Array.isArray(t) ? {
    type: "doc",
    content: t
  } : t;
}
function Ko(e) {
  return !!(e.agent_run_id || e.approval_id || e.node_run_id || e.request_id || e.approved !== void 0 || e.message !== void 0);
}
function Bs(e) {
  if (e == null)
    return !1;
  if (typeof e == "string") {
    const n = e.trim();
    return n.length > 0 && !On(n) && !Ut(n);
  }
  if (Array.isArray(e))
    return e.length > 0;
  if (typeof e != "object" || qt(e) || Ot(e))
    return !0;
  if (Ko(e))
    return !1;
  const t = qn(e).trim();
  return !!(t && !gr(t));
}
function Je(e, t) {
  let n = e;
  for (const r of t) {
    if (!n || typeof n != "object" || !(r in n))
      return;
    n = n[r];
  }
  return n;
}
function qr(e) {
  if (typeof e != "string")
    return e;
  const t = e.trim();
  for (const n of ["agent-result", "agent-output", "json"]) {
    const r = iw(t, n);
    if (r !== void 0)
      return r;
  }
  return e;
}
function iw(e, t) {
  const n = `\`\`\`${t}`, r = e.toLowerCase().indexOf(n);
  if (r < 0)
    return;
  let o = r + n.length;
  for (; o < e.length && /\s/.test(e[o] || ""); )
    o += 1;
  let s = o;
  for (; s < e.length; ) {
    const i = e.indexOf("```", s), a = i >= 0 ? e.slice(o, i) : e.slice(o), d = aw(a, t === "json");
    if (d)
      return d;
    if (i < 0)
      return;
    s = i + 3;
  }
}
function aw(e, t = !1) {
  const n = e.trim(), r = Rl(n);
  for (const o of xl([n, r])) {
    const s = Ue(o);
    if (s !== o && (t ? cw(s) : ki(s)))
      return s;
  }
  return null;
}
function cw(e) {
  if (!e || typeof e != "object" || Array.isArray(e))
    return !1;
  const t = String(e.kind || e.type || e.event || "").toLowerCase().trim();
  return [
    "final",
    "result",
    "final_result",
    "answer",
    "tool",
    "tool_result",
    "power_result"
  ].includes(t) || "content" in e || "tasks" in e || "suggestions" in e || "rich" in e;
}
function ki(e) {
  if (!e || typeof e != "object" || Array.isArray(e))
    return !1;
  const t = String(e.kind || e.type || e.event || "").toLowerCase().trim();
  return [
    "final",
    "result",
    "final_result",
    "answer",
    "tool",
    "tool_result",
    "power_result"
  ].includes(t) || "content" in e || "tasks" in e || "suggestions" in e || [
    "title",
    "text",
    "rich",
    "images",
    "videos",
    "audios",
    "files",
    "json"
  ].some((n) => Ye(e[n]));
}
function Ri(e) {
  const t = String(e || "").trim();
  return t.startsWith("{") && t.endsWith("}") || t.startsWith("[") && t.endsWith("]");
}
function On(e) {
  const t = String(e || "").trim();
  return !!(t && (Ri(t) || t.startsWith("{") || t.startsWith("[") || t.includes('"agent_run_id"') || t.includes('"node_run_id"') || t.includes('"approval_id"')));
}
function dw(e, t = []) {
  return { current: lw([
    ...t,
    ...(e?.sources || []).map((r) => ({
      id: r.nodeId,
      title: r.title,
      kind: lu(r.preview, String(r.type || "")),
      source: "current",
      output: r.output,
      preview: r.preview
    }))
  ]) };
}
function uw(e) {
  return e.map(
    (t) => ({
      id: t.role === "material" ? t.nodeId || t.key : String(t.assetId || t.key),
      title: t.title,
      kind: lu(t.preview, t.nodeType),
      role: t.role,
      source: t.role === "material" ? "current" : "asset",
      refType: "asset",
      refId: t.assetId,
      versionID: t.versionId,
      output: t.output,
      preview: t.preview,
      asset: t.asset
    })
  );
}
function lw(e) {
  const t = [], n = /* @__PURE__ */ new Set();
  for (const r of e) {
    const o = `${r.source}:${r.id}`;
    n.has(o) || (n.add(o), t.push(r));
  }
  return t;
}
function lu(e, t) {
  return e.imageUrl ? "image" : e.videoUrl ? "video" : e.audioUrl ? "audio" : e.fileUrl ? "file" : e.text ? "text" : String(t || "file").toLowerCase();
}
function fu(e) {
  if (e == null)
    return "";
  if (typeof e == "string")
    return Ut(e) ? "" : e;
  const t = qn(e).trim();
  if (t && Ut(t))
    return "";
  try {
    const n = JSON.stringify(e);
    return gr(n) ? "" : n;
  } catch {
    const n = String(e);
    return Ut(n) ? "" : n;
  }
}
function xi(e) {
  const t = fu(e).trim();
  return !!(t && !gr(t));
}
function gr(e) {
  const t = e.trim();
  return !t || t === "{}" || t === "[]" || t === "null" || Ut(t);
}
function Vs(e, t) {
  return cf(e, t);
}
function ka(e, t) {
  return e.handleType === "target" ? {
    source: t,
    target: e.nodeId
  } : {
    source: e.nodeId,
    target: t
  };
}
function Ln(e, t, n, r) {
  return !t || !n || t === n || e.some((s) => {
    const i = jr(s);
    return i.sourceNodeId === t && i.targetNodeId === n;
  }) ? e : [
    ...e,
    {
      id: `edge-${t}-${n}-${Date.now()}`,
      from: t,
      to: n,
      purpose: "media",
      ...r ? { mediaUsage: r } : {}
    }
  ];
}
function Us(e) {
  return Number(e.asset?.asset_cate_id || e.assetCateId || 0);
}
function Ls(e, t) {
  return e.type === "asset" && Us(e) === t;
}
function Ra(e, t, n, r) {
  const o = new Map(e.map((s) => [s.id, s]));
  if (r)
    for (const s of t) {
      if (s.from !== r)
        continue;
      const i = o.get(s.to);
      if (i && Ls(i, n))
        return i;
    }
  return e.find((s) => Ls(s, n)) || null;
}
function xa(e, t, n, r, o) {
  const s = /* @__PURE__ */ new Set();
  if (!r || !n)
    return s;
  const i = new Map(e.map((a) => [a.id, a]));
  for (const a of t) {
    if (a.from !== r || a.to === o)
      continue;
    const d = i.get(a.to);
    d && Ls(d, n) && s.add(d.id);
  }
  return s;
}
function ms(e, t) {
  return {
    ...t,
    id: e.id,
    x: e.x,
    y: e.y,
    width: e.width || t.width,
    height: e.height || t.height,
    groupId: e.groupId,
    local: e.local !== !1
  };
}
function fw(e) {
  return {
    title: e.title,
    subtitle: e.subtitle,
    description: e.description,
    assetCateId: e.assetCateId,
    kind: e.kind,
    cardinality: e.cardinality,
    asset: e.asset
  };
}
function pw(e) {
  return e.map((t) => ({
    id: String(t.id || `edge-${t.source}-${t.target}`),
    from: String(t.data?.physicalFrom || t.source || ""),
    to: String(t.data?.physicalTo || t.target || ""),
    logicalFrom: String(t.data?.logicalFrom || "") || void 0,
    logicalTo: String(t.data?.logicalTo || "") || void 0,
    purpose: t.data?.purpose === "structure" || t.data?.purpose === "dependency" ? t.data.purpose : "media",
    executionMode: String(t.data?.executionMode || "") === "manual" ? "manual" : void 0,
    mediaUsage: String(t.data?.mediaUsage || "") || void 0
  })).filter((t) => t.from && t.to && t.from !== t.to);
}
function Ta(e, t) {
  const n = new Set(e.nodes.map((i) => i.id));
  let r = !1;
  const o = e.nodes.map((i) => {
    const a = Number(i.assetCateId ?? t), d = i.local !== !1;
    return i.assetCateId === a && i.local === d ? i : (r = !0, {
      ...i,
      assetCateId: a,
      local: d
    });
  }), s = e.edges.filter(
    (i) => n.has(i.from) && n.has(i.to)
  );
  return hc({
    assetCateId: t,
    nextNodeNo: e.nextNodeNo,
    nodes: r ? o : e.nodes,
    edges: s.length === e.edges.length ? e.edges : s,
    viewport: e.viewport || {},
    updatedAt: e.updatedAt
  });
}
function mw(e, t) {
  return Object.fromEntries(
    Object.entries(e).map(([n, r]) => [
      n,
      pu(r, t)
    ])
  );
}
function pu(e, t) {
  if (!t.length)
    return e;
  const n = new Map(t.map((o) => [o.id, o])), r = new Map(
    t.filter(
      (o) => String(o.role || "") === "material" && String(o.status || "") !== "archived" && String(o.node_key || o.version?.node_key || "").trim()
    ).map((o) => [
      String(o.node_key || o.version?.node_key || "").trim(),
      o
    ])
  );
  return {
    ...e,
    nodes: e.nodes.map(
      (o) => gw(o, n, r)
    )
  };
}
function gw(e, t, n) {
  const r = yw(e), o = (r > 0 ? t.get(r) : void 0) || (e.type === "power" || e.type === "agent" || e.type === "flow" ? n.get(e.id) : void 0);
  if (!o)
    return e;
  const s = As(e, o), i = Number(s.resultRef?.run_id || 0), a = Number(e.resultRef?.run_id || 0);
  return {
    ...e,
    ...s,
    ...e.runError && i > a ? { runError: "" } : {},
    asset: o
  };
}
function yw(e) {
  return Number(e.resultRef?.asset_id || e.asset?.id || 0);
}
function Ma(e, t) {
  return e === t || e.assetCateId === t.assetCateId && mu(e.nodes, t.nodes) && gu(e.edges, t.edges) && e.viewport.x === t.viewport.x && e.viewport.y === t.viewport.y && e.viewport.zoom === t.viewport.zoom && e.updatedAt === t.updatedAt;
}
function mu(e, t) {
  return e === t || e.length === t.length && e.every((n, r) => n === t[r]);
}
function gu(e, t) {
  return e === t || e.length === t.length && e.every((n, r) => {
    const o = t[r];
    return n === o || n.id === o.id && n.from === o.from && n.to === o.to && (n.logicalFrom || "") === (o.logicalFrom || "") && (n.logicalTo || "") === (o.logicalTo || "") && (n.purpose || "") === (o.purpose || "") && (n.executionMode || "auto") === (o.executionMode || "auto") && (n.mediaUsage || "") === (o.mediaUsage || "");
  });
}
function hw(e, t) {
  return Vs(e, t) ? { source: e.id, target: t.id } : Vs(t, e) ? { source: t.id, target: e.id } : null;
}
function ww(e) {
  return {
    id: "proximity-preview",
    source: e.source,
    sourceHandle: "output-0",
    target: e.target,
    targetHandle: "input-0",
    type: "animated",
    animated: !0,
    style: {
      stroke: "#0ea5e9",
      strokeWidth: 2,
      strokeDasharray: "5 5",
      opacity: 0.86,
      animation: "ws-dashdraw 0.5s linear infinite"
    },
    data: {
      isHighlighted: !0,
      highlightColor: "#0ea5e9"
    }
  };
}
function bw(e, t) {
  return !e && !t ? !0 : !e || !t ? !1 : e.source === t.source && e.target === t.target;
}
function _w(e, t, n) {
  const o = new Map(n.map((i) => [i.id, i]));
  let s = null;
  for (const i of t) {
    if (i.id === e.id)
      continue;
    const a = o.get(i.id);
    if (!a)
      continue;
    const d = i.position.x - e.position.x, l = i.position.y - e.position.y, u = Math.sqrt(d * d + l * l);
    u < 150 && (!s || u < s.distance) && (s = { distance: u, domainNode: a });
  }
  return s;
}
function Nw(e, t, n, r, o, s, i, a) {
  const d = e.id === o, l = s.has(e.id), u = d || l || e.source === n || e.target === n || e.source === r || e.target === r, m = e.source === n || e.target === n ? n : r;
  return {
    ...e,
    data: {
      ...e.data,
      isHighlighted: u,
      isSelected: d,
      onDelete: a,
      highlightColor: u ? Iw(
        t.get(
          l ? i : m
        )
      ) : "var(--ws-edge)"
    }
  };
}
function Aa(e, t, n) {
  const r = t.find((l) => l.id === e);
  if (!r || r.type !== "function" || r.functionOption?.key !== "start")
    return /* @__PURE__ */ new Set();
  const o = /* @__PURE__ */ new Map();
  for (const l of n)
    o.set(l.from, [...o.get(l.from) || [], l]);
  const s = new Map(t.map((l) => [l.id, l])), i = /* @__PURE__ */ new Set(), a = /* @__PURE__ */ new Set([e]), d = [...o.get(e) || []];
  for (; d.length > 0; ) {
    const l = d.shift();
    if (!l || i.has(l.id) || (i.add(l.id), a.has(l.to)))
      continue;
    a.add(l.to);
    const u = s.get(l.to);
    u && Ac(u) || d.push(...o.get(l.to) || []);
  }
  return i;
}
function Iw(e) {
  return e?.type === "asset" ? "#10b981" : e?.type === "power" ? "#8b5cf6" : e?.type === "agent" ? "#f59e0b" : e?.type === "flow" ? "#3b82f6" : e?.type === "function" || e?.type === "group" ? "#f43f5e" : "#3b82f6";
}
function Sw(e) {
  const t = e?.changedTouches?.[0] || e?.touches?.[0];
  return t ? { x: t.clientX, y: t.clientY } : typeof e?.clientX == "number" && typeof e?.clientY == "number" ? { x: e.clientX, y: e.clientY } : null;
}
function vw(e) {
  return e instanceof HTMLElement ? !!e.closest("input, textarea, select, [contenteditable='true']") : !1;
}
function Da(e) {
  return !e.repeat && (e.key === "Delete" || e.key === "Backspace") && !vw(e.target);
}
function yu(e) {
  return e === "start" ? Ur : e === "import" ? gl : e === "display" ? Ar : Ha;
}
function Cw(e) {
  return e.type !== "function" ? !1 : e.functionOption?.key === "start" || e.title === "开始";
}
function hu(e) {
  if (e.type !== "function")
    return !1;
  const t = e.functionOption?.key || "";
  return t === "import" || t === "save" || t === "display";
}
function Ti(e) {
  return hu(e) && Mt(e);
}
function wu(e) {
  return {
    description: e
  };
}
function kw(e, t) {
  return {
    ...wu(t),
    resultRef: Io({
      ...e,
      asset: void 0,
      version: void 0,
      role: void 0
    })
  };
}
function bu(e) {
  return e.type === "function" ? Ti(e) ? op(e) ? Rw(e) : { width: e.width, height: e.height } : { width: 128, height: 46 } : {
    width: e.width,
    height: e.height
  };
}
const _u = 44;
function Rw(e) {
  const t = _r(e), n = t.audioUrl ? "audio" : t.videoUrl ? "video" : t.imageUrl ? "image" : String(e.kind || ""), r = Oo({
    kind: n,
    outputType: "",
    output: void 0
  });
  return {
    width: r.width,
    height: r.height + _u
  };
}
function dt({
  id: e,
  type: t,
  position: n,
  className: r,
  style: o
}) {
  return /* @__PURE__ */ c(
    Uu,
    {
      id: e,
      type: t,
      position: n,
      className: `ws-rf-handle ${r}`,
      style: o,
      children: /* @__PURE__ */ c("span", { "aria-hidden": "true", children: t === "target" ? /* @__PURE__ */ c(La, { size: 12 }) : /* @__PURE__ */ c(Xs, { size: 12 }) })
    }
  );
}
function hn({
  node: e,
  selected: t
}) {
  const n = e.onNodeResizeStart, r = e.onNodeResizeEnd, o = e.type === "asset" || e.type === "power" || e.type === "group" || e.type === "function" && Ti(e), s = !!e.showNodeSettings, i = /* @__PURE__ */ c(
    qp,
    {
      node: e,
      enabled: !!e.interactive && !e.structureLocked,
      resizable: o,
      onResizeStart: n,
      onResizeEnd: r
    }
  );
  if (e.type === "asset" || e.type === "group" || e.type === "function" || e.type === "power" && ml(e.power, e.kind, e.outputType) || (!t || !s) && e.type !== "flow")
    return i;
  const { projectId: a, runningNode: d, setRunningNode: l } = e, u = e.onNodeResult, m = e.onNodeDraftChange, N = e.onAssetCreated, b = e.onRunStartNode, w = e.onOpenImportPicker, z = e.onClearFeedbackRecords, F = e.requestConfirm, U = e.onRunBackendNode;
  return /* @__PURE__ */ v(zn, { children: [
    i,
    /* @__PURE__ */ c(
      jw,
      {
        node: e,
        projectId: a,
        runningNode: d,
        setRunningNode: l,
        onNodeResult: u || (() => {
        }),
        onNodeDraftChange: m || (() => {
        }),
        onAssetCreated: N,
        onRunStartNode: b,
        onOpenImportPicker: w,
        onClearFeedbackRecords: z,
        requestConfirm: F,
        onRunBackendNode: U
      },
      e.id
    )
  ] });
}
function Rr({
  node: e,
  onShowNodeDetail: t
}) {
  return !t || !Mt(e) ? null : /* @__PURE__ */ c(
    "button",
    {
      type: "button",
      className: "ws-node-quick-view nodrag nopan",
      "aria-label": "查看详情",
      onPointerEnter: Qn,
      onFocus: Qn,
      onMouseDown: (n) => n.stopPropagation(),
      onClick: (n) => {
        n.preventDefault(), n.stopPropagation(), t(e);
      },
      children: /* @__PURE__ */ c(Ar, { size: 14 })
    }
  );
}
const xw = {
  width: 270,
  height: 250,
  offsetX: 0,
  offsetY: 0
};
function gs({
  node: e,
  runningNode: t,
  onShowNodeDetail: n
}) {
  const r = Pr(
    e.resultView || xw
  ), {
    width: o,
    height: s,
    offsetX: i,
    offsetY: a
  } = r, [d, l] = j(r), [u, m] = j(!1);
  ce(() => {
    l({
      width: o,
      height: s,
      offsetX: i,
      offsetY: a
    });
  }, [
    s,
    i,
    a,
    o
  ]);
  const N = e.type === "agent" ? t?.agent : void 0, b = Qp(N);
  if (!Mt(e) && !b)
    return null;
  const w = _r(e), z = vi(e), F = $e(
    Kt(w.text, ""),
    Kt(z, ""),
    Kt(e.description, ""),
    e.title,
    "暂无结果"
  ), U = Ni(e), A = Gn(e), X = Ye(A) ? A : U ? { rich: U } : F, Z = Xn(w) ? w : Kh(
    w,
    jn(F, nu(F, ""))
  ), de = e.onNodeResizeStart, k = e.onResultViewResizeEnd, C = !!(e.interactive && k), T = e.setRunningNode, re = e.onRunBackendNode, ne = Se(
    e.resultOutput,
    e.asset?.version?.content
  ), me = e.type === "agent" && T && re ? async (ie) => {
    if (!Qt(t)) {
      T((H) => ({
        ...H,
        [e.id]: {
          nodeId: e.id,
          title: e.title,
          startedAt: Date.now(),
          progress: 0,
          status: "running",
          agent: Oc()
        }
      }));
      try {
        await re(e, { agentInput: ie });
      } catch (H) {
        T((J) => {
          const ae = J[e.id];
          return ae ? {
            ...J,
            [e.id]: {
              ...ae,
              status: "error",
              progress: Math.max(ae.progress, 92)
            }
          } : J;
        }), G.error(
          H instanceof Error ? H.message : "智能体继续运行失败"
        ), window.setTimeout(() => {
          T(
            (J) => J[e.id]?.status === "error" ? Dn(J, e.id) : J
          );
        }, 1200);
      }
    }
  } : void 0;
  return /* @__PURE__ */ c(ut, { fallback: /* @__PURE__ */ c(lt, { label: "正在加载节点结果" }), children: /* @__PURE__ */ c(
    _d,
    {
      output: X,
      fallback: F,
      preview: Z,
      mediaLabel: Br(Z),
      className: `ws-agent-result-bubble ${u ? "is-resizing" : ""}`,
      followContent: !!(t && t.status !== "error"),
      followKey: N,
      style: {
        width: d.width,
        height: d.height,
        left: `calc(100% + 12px + ${Number(d.offsetX || 0)}px)`,
        top: `calc(50% + ${Number(d.offsetY || 0)}px)`
      },
      onOpen: n ? () => n(e) : void 0,
      onOpenIntent: Qn,
      resizeControls: /* @__PURE__ */ c(
        Gp,
        {
          value: d,
          enabled: C,
          onResizeStart: () => {
            m(!0), de?.(e.id);
          },
          onResize: l,
          onResizeEnd: (ie) => {
            l(ie), m(!1), k?.(e.id, ie);
          }
        }
      ),
      children: e.type === "agent" ? /* @__PURE__ */ c(
        ut,
        {
          fallback: /* @__PURE__ */ c(lt, { label: "正在加载智能体结果", compact: !0 }),
          children: /* @__PURE__ */ c(
            hy,
            {
              output: ne,
              runtime: N,
              fallback: F,
              running: Qt(t),
              onContinue: me
            }
          )
        }
      ) : void 0
    }
  ) });
}
function Tw({
  node: e,
  running: t = !1,
  onShowNodeDetail: n
}) {
  const r = _r(e), o = Ni(e), s = Gn(e), i = $e(
    vi(e),
    Kt(r.text, ""),
    Kt(e.description, ""),
    "暂无内容"
  ), a = Ye(s) ? s : o ? { rich: o } : i, d = !ur(a, r) && !!(r.imageUrl || r.videoUrl || r.audioUrl), l = e.onNodeResult, u = d && !r.audioUrl ? mo(
    e,
    l,
    _u
  ) : void 0;
  return /* @__PURE__ */ c(ut, { fallback: /* @__PURE__ */ c(lt, { label: "正在加载节点结果" }), children: /* @__PURE__ */ c(
    _d,
    {
      output: a,
      fallback: i,
      preview: r,
      mediaLabel: Br(r),
      className: `ws-node-function-result-card ${d ? "has-media" : ""}`,
      customContentIsPureMedia: d,
      onOpen: n ? () => n(e) : void 0,
      onOpenIntent: Qn,
      children: d ? /* @__PURE__ */ c(
        vu,
        {
          preview: r,
          output: a,
          fallback: i,
          generating: t,
          showMediaCaption: !1,
          onMediaSize: u
        }
      ) : void 0
    }
  ) });
}
function ys({
  node: e,
  onOpenFeedbackRecord: t
}) {
  const n = Wn(e);
  if (!t || n.length === 0)
    return null;
  const r = n.filter(
    (s) => s.status === "pending"
  ).length, o = [...n].reverse().find((s) => s.status === "pending") || n[n.length - 1];
  return /* @__PURE__ */ v(
    "button",
    {
      type: "button",
      className: `ws-node-feedback-beacon nodrag nopan ${r > 0 ? "is-pending" : "is-done"}`,
      "aria-label": r > 0 ? "继续填写反馈" : "查看反馈记录",
      onMouseDown: (s) => s.stopPropagation(),
      onClick: (s) => {
        s.preventDefault(), s.stopPropagation(), t(e, o);
      },
      children: [
        /* @__PURE__ */ c(tf, { size: 15, fill: "currentColor" }),
        n.length > 1 ? /* @__PURE__ */ c("span", { children: n.length }) : null
      ]
    }
  );
}
function Mw(e) {
  const t = e.resultRef;
  return !!(t?.run_id || t?.node_run_id || t?.asset_id || t?.version_id || t?.request_id);
}
function Mt(e) {
  if (!Aw(e) || !Mw(e) && e.asset?.version?.content == null && e.resultOutput == null)
    return !1;
  const t = In(e);
  if (t == null)
    return !1;
  const n = jn(
    t,
    Bo(e, t)
  );
  return En(n) || xi(t);
}
function Aw(e) {
  return e.type !== "function" ? !0 : hu(e);
}
async function Nu(e) {
  const t = e.node.functionOption?.key || "", n = Fw(e.inputContext);
  if (t === "display") {
    if (n == null)
      throw new Error("展示节点没有可展示的上游结果");
    return e.onNodeResult(
      e.node.id,
      bn(
        e.node,
        { output: n },
        "展示上游结果"
      )
    ), G.success("已展示上游结果"), !0;
  }
  if (t === "save") {
    if (n == null)
      throw new Error("保存节点没有可保存的上游结果");
    const r = await Dh({
      projectId: e.projectId,
      assetCateId: Number(e.node.assetCateId || e.assetCate?.id || 0),
      name: Ow(e.node, e.inputContext),
      kind: om(e.node),
      content: n,
      nodeKey: e.node.id,
      requestId: Kd(
        "save",
        e.node.id,
        n
      ),
      source: Ew(e.inputContext),
      previousAsset: e.node.asset
    });
    return e.onAssetCreated?.(r), e.onNodeResult(
      e.node.id,
      bn(
        e.node,
        {
          output: r.version?.content || n,
          asset: r
        },
        "保存上游结果"
      )
    ), G.success("资产已保存"), !0;
  }
  return t === "start" ? e.onRunStartNode ? (await e.onRunStartNode(e.node), G.success("开始节点执行完成"), !0) : (e.onNodeResult(
    e.node.id,
    wu("开始节点已启动，请运行后续连接节点。")
  ), G.success("开始节点已启动"), !0) : t === "import" ? (e.onOpenImportPicker?.(e.node.id), !0) : (e.onNodeResult(
    e.node.id,
    bn(
      e.node,
      { output: "操作已应用" },
      "操作已应用"
    )
  ), G.success("操作已应用"), !0);
}
function Dw(e, t, n = []) {
  const r = Gs(e), o = new Map(
    n.map((s) => [s.key, s])
  );
  for (const s of e) {
    const i = o.get(s.key);
    s.key && i && Object.prototype.hasOwnProperty.call(t, s.key) && Pw(s, i, t[s.key]) && (r[s.key] = Oa(s, t[s.key]));
  }
  return r;
}
function Pw(e, t, n) {
  if (e.type !== t.type || e.value_type !== t.value_type)
    return !1;
  if (e.type === "option" || e.type === "select") {
    const r = e.options || [];
    return r.length === 0 || !!Pi(r, n);
  }
  if (e.type === "multi_option") {
    const r = e.options || [];
    return r.length === 0 || zw(n).every(
      (o) => !!Pi(r, o)
    );
  }
  return e.value_type === "number" ? n === "" || Number.isFinite(Number(n)) : !0;
}
function zw(e) {
  return Array.isArray(e) ? e.map((t) => String(t)).filter(Boolean) : typeof e == "string" ? e ? [e] : [] : e == null ? [] : [String(e)];
}
function Mi(e) {
  const t = e?.sources || [];
  return t.length > 0 ? t[t.length - 1] : null;
}
function Fw(e) {
  const t = Mi(e);
  return t?.output != null ? t.output : e?.text ? { text: e.text } : null;
}
function Ew(e) {
  const t = Mi(e);
  return um(t);
}
function Ow(e, t) {
  const n = Mi(t);
  return $e(n?.title, e.title, "画布资产");
}
function $w(e, t) {
  const n = (r) => {
    const o = Yu(e, t, r);
    return o ? cr(dr(e, o)) : [];
  };
  return {
    per_image: n("per_image"),
    shared_reference: n("shared_reference")
  };
}
function Ks({
  node: e,
  content: t,
  items: n,
  connections: r,
  params: o,
  values: s,
  requestedMode: i,
  additionalSources: a = []
}) {
  return Wu({
    targetKind: ja(e) || e.power?.kind || e.kind,
    content: t,
    items: n,
    connections: r,
    mediaOptionsByMode: $w(o, s),
    requestedMode: i,
    additionalSources: a
  });
}
function ao(e) {
  return Number(e?.source_rule || 0) === 2;
}
function jw({
  node: e,
  projectId: t,
  runningNode: n,
  setRunningNode: r,
  onNodeResult: o,
  onNodeDraftChange: s,
  onAssetCreated: i,
  onRunStartNode: a,
  onOpenImportPicker: d,
  onClearFeedbackRecords: l,
  requestConfirm: u,
  onRunBackendNode: m
}) {
  const N = e.composerDraft, b = se(
    () => Pd(N),
    [N]
  ), w = se(
    () => Ds(b),
    [b]
  ), z = Q(b), [F, U] = j(b.prompt || ""), [A, X] = j(b.promptContent), [Z, de] = j(b.storyboardReferences || []), [k, C] = j(!1), [T, re] = j(null), [ne, me] = j(!1), [ie, H] = j(
    b.selectedTargetId || 0
  ), [J, ae] = j(
    b.paramValues || {}
  ), [xe, le] = j(b.multiImageMode), Be = Q(T), ye = e.inputContext || null, Ce = String(e.runBlockedReason || "");
  ce(() => {
    z.current = b;
  }, [b]), ce(() => {
    Be.current = T;
  }, [T]);
  const Pe = k || Qt(n), Y = e.type, Gt = e.id, fe = e.flow?.id || 0, Qe = e.type === "power" && e.power?.id || 0, gt = e.type === "power" && e.power?.key || "", cn = e.type === "agent" ? Number(e.role?.agent_id || 0) : 0, dn = Kw(), et = e.space || null, _t = e.canvasReferenceItems || [], Fe = e.connectedMediaReferences || Ro, Nt = e.onConnectedMediaUsagesChange, Ke = e.onConnectedMediaEdgeRemove, kt = e.catalogCache, Xe = Number(
    et?.release?.id || et?.project.release_id || 0
  ), un = Number(e.assetCateId || 0), Nr = et ? uo(et, un) : null, qe = se(
    () => dw(
      ye,
      _t.filter((M) => M.id !== e.id)
    ),
    [_t, ye, e.id]
  ), nr = e.type === "power" && er(e.power, e.kind, e.outputType)?.viewMode === "storyboard";
  ce(() => {
    if (Y === "power" && (Qe || gt)) {
      const M = z.current.selectedTargetId || 0;
      let W = !1;
      return me(!0), kt.loadPowerForm(
        {
          projectId: t,
          releaseId: Xe,
          flowId: fe,
          powerId: Qe,
          powerKey: gt,
          targetId: M
        },
        () => vs({
          projectId: t,
          flowId: fe,
          powerId: Qe,
          powerKey: gt,
          targetId: M
        })
      ).then((B) => {
        if (W)
          return;
        const ue = z.current;
        re(B), H(
          ao(B) && (B.selected_target_id || M) || 0
        ), ae(
          Ps(B.params || [], ue)
        ), U(ue.prompt || ""), X(ue.promptContent), de(ue.storyboardReferences || []), le(ue.multiImageMode);
      }).catch((B) => {
        W || G.error(
          B instanceof Error ? B.message : "加载能力参数失败"
        );
      }).finally(() => {
        W || me(!1);
      }), () => {
        W = !0;
      };
    }
    if (re(null), Y === "agent") {
      const M = z.current;
      ae(M.paramValues || {}), U(M.prompt || ""), X(M.promptContent), de(M.storyboardReferences || []), le(void 0), H(0);
      return;
    }
    ae({}), H(0), U(""), X(void 0), de([]), le(void 0);
  }, [
    kt,
    t,
    Xe,
    fe,
    cn,
    Gt,
    Y,
    Qe,
    gt
  ]);
  const Ne = T?.params || [], Rt = se(
    () => Ks({
      node: e,
      content: A,
      items: qe.current,
      connections: Fe,
      params: Ne,
      values: J,
      requestedMode: xe
    }),
    [
      qe,
      Fe,
      e,
      J,
      Ne,
      A,
      xe
    ]
  ), Ae = Rt.active ? Rt.mode : void 0, Ee = se(
    () => co(
      Ne,
      J,
      Fe.map((M) => M.source),
      Ae
    ),
    [
      Fe,
      J,
      Ne,
      Ae
    ]
  ), Dt = se(
    () => dr(Ne, Ee),
    [Ee, Ne]
  ), Bn = se(
    () => Dt.filter(
      (M) => Iy(M, Ne) && !(Rt.active && Xu(M))
    ),
    [Dt, Rt.active, Ne]
  ), yt = se(
    () => Y === "power" ? cr(Dt) : [],
    [Dt, Y]
  ), Ge = Y === "power" && ["image", "video"].includes(ja(e) || ""), ht = se(
    () => Y === "power" && !ne ? Jo(
      Fe,
      A,
      qe.current,
      yt,
      {},
      Ge,
      Ae
    ) : "",
    [
      qe,
      Fe,
      yt,
      ne,
      A,
      Ge,
      Y,
      Ae
    ]
  ), Pt = Ce || Rt.error || ht, tt = se(
    () => Dt.find($a) || null,
    [Dt]
  ), Ve = se(
    () => Bn.filter(
      (M) => M.key !== tt?.key && (Zu(M) || Ju(M) || fa(M, Ne))
    ),
    [Bn, Ne, tt?.key]
  ), nt = tt ? String(Ee[tt.key] ?? "") : F, nn = String(
    T?.power?.description || e.power?.description || ""
  ).trim() || (tt ? "在此处为该能力输入生成提示词..." : "当前能力无需填写提示词"), It = ao(T), Te = It ? ie : 0;
  ce(() => {
    if (Y !== "power" && Y !== "agent")
      return;
    const M = z.current, W = Be.current;
    U(M.prompt || ""), X(M.promptContent), de(M.storyboardReferences || []), le(M.multiImageMode), H(
      Y === "power" && ao(W) && (M.selectedTargetId || W?.selected_target_id) || 0
    ), ae(
      Y === "power" && W ? Ps(
        W.params || [],
        M
      ) : M.paramValues || {}
    );
  }, [w, Y]);
  const ft = $(
    (M) => {
      const W = Object.prototype.hasOwnProperty.call(
        M,
        "promptContent"
      ) ? M.promptContent : A, B = jo({
        ...z.current,
        ...M,
        promptContent: W
      });
      z.current = B, le(B.multiImageMode), s(e.id, B);
    },
    [e.id, s, A]
  ), St = $(
    (M, W) => {
      ae(M), ft({ ...W, paramValues: M });
    },
    [ft]
  );
  ce(() => {
    Ee !== J && St(Ee, {
      prompt: nt,
      promptContent: A,
      selectedTargetId: Te,
      multiImageMode: Ae
    });
  }, [
    Te,
    Ee,
    J,
    nt,
    A,
    St,
    Ae
  ]);
  function ln(M, W) {
    const B = to(
      A,
      W,
      qe.current,
      yt,
      Fe,
      Ae
    ), ue = B.content;
    Object.keys(B.assignments).length > 0 && Nt?.(B.assignments), U(M), X(ue);
    const he = nr ? Ll(
      ue,
      Z,
      qe.current,
      M
    ) : Z;
    de(he);
    const ke = z.current.paramValues || J, rt = tt ? { ...ke, [tt.key]: M } : ke;
    St(rt, {
      prompt: M,
      promptContent: ue,
      selectedTargetId: Te,
      storyboardReferences: he,
      multiImageMode: Ae
    });
  }
  function vn(M) {
    de(M), ft({
      prompt: nt,
      promptContent: A,
      paramValues: J,
      selectedTargetId: Te,
      storyboardReferences: M,
      multiImageMode: Ae
    });
  }
  function Cn(M, W) {
    const B = {
      ...z.current.paramValues || J,
      [M]: W
    }, ue = Ne.find((ke) => ke.key === M), he = Ae;
    if (ue && fa(ue, Ne)) {
      const ke = cr(
        dr(Ne, B)
      ), rt = to(
        A,
        A,
        qe.current,
        ke,
        Fe,
        he
      );
      Object.keys(rt.assignments).length > 0 && Nt?.(rt.assignments), X(rt.content), St(B, {
        prompt: nt,
        promptContent: rt.content,
        selectedTargetId: Te,
        multiImageMode: he
      });
      return;
    }
    St(B, {
      prompt: nt,
      promptContent: A,
      selectedTargetId: Te,
      multiImageMode: he
    });
  }
  function ze(M) {
    const W = Rt.options.find(
      (ot) => ot.value === M
    );
    if (!Rt.active || !W?.enabled) {
      G.error(W?.reason || "当前能力不支持该多图生成方式");
      return;
    }
    const B = z.current.paramValues || J, ue = co(
      Ne,
      B,
      Fe.map((ot) => ot.source),
      M
    ), he = cr(
      dr(Ne, ue)
    ), ke = to(
      A,
      A,
      qe.current,
      he,
      Fe,
      M
    ), rt = Jo(
      Fe,
      ke.content,
      qe.current,
      he,
      ke.assignments,
      Ge,
      M
    );
    rt && G.error(rt), Object.keys(ke.assignments).length > 0 && Nt?.(ke.assignments), X(ke.content), St(ue, {
      prompt: nt,
      promptContent: ke.content,
      selectedTargetId: Te,
      storyboardReferences: Z,
      multiImageMode: M
    });
  }
  function wt(M, W) {
    U(M), X(W), ft({
      prompt: M,
      promptContent: W,
      paramValues: J,
      selectedTargetId: 0
    });
  }
  function pt(M, W) {
    const B = {
      ...z.current.paramValues || J,
      [M]: W
    };
    St(B, {
      prompt: F,
      selectedTargetId: 0
    });
  }
  async function Me(M, W) {
    const B = await Hc({
      projectID: t,
      teamID: Number(et?.project.team_id || 0),
      files: M,
      ruleID: W.upload_rule_id
    });
    for (const ue of B) {
      const he = mr(ue.asset);
      he.id && i?.(he);
    }
    return B;
  }
  async function zt(M) {
    if (!(Pe || !It) && !(e.type !== "power" || !e.power))
      try {
        const W = await kt.loadPowerForm(
          {
            projectId: t,
            releaseId: Xe,
            flowId: e.flow?.id || 0,
            powerId: e.power.id,
            powerKey: e.power.key,
            targetId: M
          },
          () => vs({
            projectId: t,
            flowId: e.flow?.id || 0,
            powerId: e.power?.id || 0,
            powerKey: e.power?.key || "",
            targetId: M
          })
        ), B = W.params || [], ue = Dw(
          B,
          z.current.paramValues || J,
          T?.params || []
        ), he = Ks({
          node: e,
          content: A,
          items: qe.current,
          connections: Fe,
          params: B,
          values: ue,
          requestedMode: xe || Ae
        }), ke = he.active ? he.mode : void 0;
        if (he.error) {
          G.error(`无法切换能力来源：${he.error}`);
          return;
        }
        const rt = co(
          B,
          ue,
          Fe.map((Rn) => Rn.source),
          ke
        ), ot = cr(
          dr(B, rt)
        ), Ht = to(
          A,
          A,
          qe.current,
          ot,
          Fe,
          ke
        ), rn = Jo(
          Fe,
          Ht.content,
          qe.current,
          ot,
          Ht.assignments,
          Ge,
          ke
        );
        if (rn) {
          G.error(`无法切换能力来源：${rn}`);
          return;
        }
        Object.keys(Ht.assignments).length > 0 && Nt?.(Ht.assignments), re(W);
        const q = ao(W) ? W.selected_target_id || M : 0;
        H(q), X(Ht.content), St(rt, {
          prompt: nt,
          promptContent: Ht.content,
          selectedTargetId: q,
          multiImageMode: ke
        });
      } catch (W) {
        G.error(W instanceof Error ? W.message : "加载能力参数失败");
      }
  }
  const kn = async () => {
    l?.([e.id]), e.runError && o(e.id, { runError: "" }), C(!0), r((B) => ({
      ...B,
      [e.id]: {
        nodeId: e.id,
        title: e.title,
        startedAt: Date.now(),
        progress: 0,
        status: "running"
      }
    }));
    let M = !1, W = "error";
    try {
      if (e.type === "asset") {
        const B = e.asset?.version?.content ?? e.resultOutput ?? e.asset;
        if (B == null)
          throw new Error("资产节点没有可载入的内容");
        o(
          e.id,
          bn(
            e,
            {
              output: B,
              asset: e.asset,
              version: e.asset?.version,
              request_id: Kd("asset", e.id, B)
            },
            "载入资产结果"
          )
        ), G.success("资产结果已载入"), M = !0;
      } else if (e.type === "power" && e.power) {
        if (!m)
          throw new Error("当前节点缺少后端运行入口");
        ft({
          prompt: nt,
          promptContent: A,
          paramValues: Ee,
          selectedTargetId: Te,
          storyboardReferences: Z,
          multiImageMode: Ae
        }), await m({
          ...e,
          composerDraft: {
            ...z.current,
            prompt: nt,
            promptContent: A,
            paramValues: Ee,
            selectedTargetId: Te,
            storyboardReferences: Z,
            multiImageMode: Ae
          }
        }), G.success("能力节点执行成功"), M = !0;
      } else if (e.type === "agent" && e.role) {
        if (!m)
          throw new Error("当前节点缺少后端运行入口");
        ft({
          prompt: F,
          promptContent: A,
          paramValues: J,
          selectedTargetId: 0
        }), await m({
          ...e,
          composerDraft: {
            prompt: F,
            promptContent: A,
            paramValues: J,
            selectedTargetId: 0
          }
        }), M = !0, W = "success";
      } else if (e.type === "flow" && e.flow) {
        if (!m)
          throw new Error("当前节点缺少后端运行入口");
        await m(e), M = !0, W = "success";
      } else e.type === "function" && (M = await Nu({
        node: e,
        projectId: t,
        assetCate: Nr,
        inputContext: ye,
        onNodeResult: o,
        onAssetCreated: i,
        onRunStartNode: a,
        onOpenImportPicker: d
      }));
      M && (W = "success");
    } catch (B) {
      W = "error";
      const ue = B instanceof Error ? B.message : "执行出错";
      o(e.id, { runError: ue }), r((he) => {
        const ke = he[e.id];
        return ke ? {
          ...he,
          [e.id]: {
            ...ke,
            status: "error",
            progress: Math.max(ke.progress, 92)
          }
        } : he;
      }), G.error(ue);
    } finally {
      C(!1), r((B) => {
        const ue = B[e.id];
        return ue ? {
          ...B,
          [e.id]: {
            ...ue,
            status: W,
            progress: W === "success" ? 100 : Math.max(ue.progress, 92)
          }
        } : B;
      }), window.setTimeout(
        () => {
          r((B) => {
            const ue = B[e.id];
            return ue?.status === "error" && ue.streamStarted ? B : Dn(B, e.id);
          });
        },
        W === "success" ? 650 : 1200
      );
    }
  }, xt = async () => {
    if (!(Pe || k)) {
      if (Pt) {
        G.error(Pt);
        return;
      }
      if (u && Iu(e)) {
        u({
          title: e.type === "function" ? `执行「${e.title}」` : `运行「${e.title}」`,
          description: Su(e),
          confirmText: e.type === "flow" || e.type === "function" ? "执行" : "运行",
          onConfirm: kn
        });
        return;
      }
      await kn();
    }
  };
  return ce(() => {
    if (!k)
      return;
    const M = window.setInterval(() => {
      r((W) => {
        const B = W[e.id];
        return !B || B.status !== "running" ? W : {
          ...W,
          [e.id]: {
            ...B,
            progress: Math.min(94, B.progress + 7)
          }
        };
      });
    }, 1e3);
    return () => window.clearInterval(M);
  }, [e.id, k, r]), e.type === "power" ? /* @__PURE__ */ c(
    "div",
    {
      className: "ws-node-bottom-settings is-composer nodrag nowheel",
      onClick: (M) => M.stopPropagation(),
      style: hs,
      children: ne && !Pe && !T ? /* @__PURE__ */ v("div", { className: "ws-prompt-loading", children: [
        /* @__PURE__ */ c(sn, { size: 16, className: "ws-spin" }),
        /* @__PURE__ */ c("span", { children: "正在加载能力参数..." })
      ] }) : /* @__PURE__ */ v(zn, { children: [
        /* @__PURE__ */ c(
          ut,
          {
            fallback: /* @__PURE__ */ c(lt, { label: "正在加载参数编辑器", compact: !0 }),
            children: /* @__PURE__ */ c(
              ua,
              {
                value: nt,
                referenceContent: A,
                placeholder: nn,
                running: Pe,
                textInputEnabled: !!tt,
                showMediaParamButtons: !0,
                mediaParamPower: T?.power || e.power,
                sourceOptions: It ? T?.sources || [] : [],
                selectedSourceId: Te,
                params: Ve,
                paramValues: Ee,
                assetLibrary: qe,
                assetReference: {
                  teamID: Number(et?.project.team_id || 0),
                  projectID: t,
                  assetCateID: un
                },
                connectedMediaReferences: Fe,
                mediaUsageOptions: yt,
                multiImagePlan: Rt,
                multiImageMode: Ae,
                onConnectedMediaEdgeRemove: Ke,
                disabled: ne,
                submitDisabled: !!Pt,
                submitDisabledReason: Pt,
                onChange: ln,
                onParamChange: Cn,
                onMultiImageModeChange: ze,
                onSourceChange: It ? (M) => {
                  zt(M);
                } : void 0,
                onLocalUpload: Me,
                onSubmit: xt
              }
            )
          }
        ),
        nr ? /* @__PURE__ */ c(
          ut,
          {
            fallback: /* @__PURE__ */ c(lt, { label: "正在加载分镜参考", compact: !0 }),
            children: /* @__PURE__ */ c(
              ay,
              {
                references: Z,
                disabled: ne || Pe,
                onChange: vn
              }
            )
          }
        ) : null
      ] })
    }
  ) : e.type === "agent" ? /* @__PURE__ */ c(
    "div",
    {
      className: "ws-node-bottom-settings is-composer nodrag nowheel",
      onClick: (M) => M.stopPropagation(),
      style: hs,
      children: /* @__PURE__ */ c(
        ut,
        {
          fallback: /* @__PURE__ */ c(lt, { label: "正在加载参数编辑器", compact: !0 }),
          children: /* @__PURE__ */ c(
            ua,
            {
              value: F,
              referenceContent: A,
              placeholder: "向智能体发送任务指令...",
              running: Pe,
              params: Py,
              paramValues: J,
              assetLibrary: qe,
              assetReference: {
                teamID: Number(et?.project.team_id || 0),
                projectID: t,
                assetCateID: un
              },
              connectedMediaReferences: Fe,
              onConnectedMediaEdgeRemove: Ke,
              onChange: wt,
              onParamChange: pt,
              onLocalUpload: Me,
              onSubmit: xt
            }
          )
        }
      )
    }
  ) : e.type === "flow" && e.flow ? /* @__PURE__ */ c(
    "div",
    {
      className: "ws-node-bottom-settings is-flow-run-only nodrag nowheel",
      onClick: (M) => M.stopPropagation(),
      style: dn,
      children: /* @__PURE__ */ v(
        "button",
        {
          type: "button",
          className: "ws-node-flow-run",
          disabled: Pe,
          onClick: xt,
          children: [
            Pe ? /* @__PURE__ */ c(sn, { size: 15, className: "ws-spin" }) : /* @__PURE__ */ c(Ur, { size: 15, fill: "currentColor" }),
            /* @__PURE__ */ c("span", { children: Pe ? "运行中" : "执行" })
          ]
        }
      )
    }
  ) : /* @__PURE__ */ v(
    "div",
    {
      className: "ws-node-bottom-settings nodrag nowheel",
      onClick: (M) => M.stopPropagation(),
      style: hs,
      children: [
        /* @__PURE__ */ v("div", { className: "ws-node-settings-head", children: [
          /* @__PURE__ */ v("div", { className: "ws-node-settings-icon", children: [
            /* @__PURE__ */ c(Xs, { size: 16 }),
            /* @__PURE__ */ c("span", { children: "配置" })
          ] }),
          /* @__PURE__ */ v("div", { className: "ws-node-settings-copy", children: [
            /* @__PURE__ */ v("div", { children: [
              /* @__PURE__ */ c("strong", { children: e.title }),
              /* @__PURE__ */ v("span", { children: [
                e.type,
                " /",
                " ",
                String(
                  e.kind || e.functionOption?.key || e.subtitle || "default"
                )
              ] })
            ] }),
            /* @__PURE__ */ c("p", { children: e.description || "配置该节点的专属参数和功能动作。" })
          ] })
        ] }),
        /* @__PURE__ */ c("div", { className: "ws-node-settings-line" }),
        /* @__PURE__ */ v("div", { className: "ws-node-settings-stack", style: { gap: "12px" }, children: [
          e.type === "asset" && /* @__PURE__ */ v("div", { className: "ws-node-settings-row", children: [
            /* @__PURE__ */ v("div", { className: "ws-node-settings-actions", children: [
              /* @__PURE__ */ c(xr, { icon: Ar, label: "放大预览", accent: "green" }),
              /* @__PURE__ */ c(xr, { icon: Xl, label: "查看来源" }),
              /* @__PURE__ */ c(xr, { icon: Va, label: "查看版本" }),
              /* @__PURE__ */ c(xr, { icon: el, label: "查看引用关系" }),
              /* @__PURE__ */ c(xr, { icon: cc, label: "查看生成记录" })
            ] }),
            /* @__PURE__ */ v(
              "button",
              {
                type: "button",
                className: "ws-node-save-run",
                disabled: Pe,
                onClick: xt,
                style: { cursor: "pointer", marginLeft: "12px" },
                children: [
                  Pe ? /* @__PURE__ */ c(sn, { size: 12, className: "ws-spin" }) : /* @__PURE__ */ c(Ar, { size: 12 }),
                  /* @__PURE__ */ c("span", { children: Pe ? "载入中" : "载入结果" })
                ]
              }
            )
          ] }),
          e.type === "function" && /* @__PURE__ */ v("div", { className: "ws-node-settings-row", children: [
            /* @__PURE__ */ c("span", { className: "ws-node-settings-state is-function-desc", children: e.functionOption?.description || "执行该功能节点。" }),
            /* @__PURE__ */ v(
              "button",
              {
                type: "button",
                className: "ws-node-save-run",
                disabled: Pe,
                onClick: xt,
                style: { cursor: "pointer", marginLeft: "12px" },
                children: [
                  Pe ? /* @__PURE__ */ c(sn, { size: 12, className: "ws-spin" }) : (() => {
                    const M = yu(e.functionOption?.key || "");
                    return /* @__PURE__ */ c(M, { size: 12 });
                  })(),
                  /* @__PURE__ */ c("span", { children: e.functionOption?.key === "start" ? "开始" : e.functionOption?.key === "display" ? "展示" : e.functionOption?.key === "save" ? "保存" : "执行" })
                ]
              }
            )
          ] })
        ] })
      ]
    }
  );
}
function Bw({
  prompt: e,
  running: t,
  readonly: n,
  history: r,
  activeRecordId: o,
  onSelectRecord: s,
  onClose: i,
  onSubmit: a
}) {
  const d = se(
    () => Uw(e),
    [e]
  );
  if (typeof document > "u")
    return null;
  const l = document.querySelector(".ws-page") || document.body;
  return Au(
    /* @__PURE__ */ c("div", { className: "ws-flow-feedback-backdrop", onMouseDown: i, children: /* @__PURE__ */ v(
      "div",
      {
        className: "ws-flow-feedback-modal",
        onMouseDown: (u) => u.stopPropagation(),
        children: [
          /* @__PURE__ */ v("header", { className: "ws-flow-feedback-head", children: [
            /* @__PURE__ */ v("div", { children: [
              /* @__PURE__ */ c("strong", { children: e.title || "补充信息" }),
              n ? /* @__PURE__ */ c("span", { children: "已提交的反馈记录，可查看之前填写的内容。" }) : e.description ? /* @__PURE__ */ c("span", { children: e.description }) : null
            ] }),
            /* @__PURE__ */ c(
              "button",
              {
                type: "button",
                className: "ws-flow-feedback-close",
                disabled: t,
                onClick: i,
                "aria-label": "关闭",
                children: /* @__PURE__ */ c(Ya, { size: 18 })
              }
            )
          ] }),
          r && r.length > 1 ? /* @__PURE__ */ c("div", { className: "ws-flow-feedback-tabs", children: r.map((u, m) => /* @__PURE__ */ v(
            "button",
            {
              type: "button",
              className: u.id === o ? "is-active" : "",
              onClick: () => s?.(u),
              children: [
                /* @__PURE__ */ c("span", { children: m + 1 }),
                u.status === "pending" ? "待反馈" : "已提交"
              ]
            },
            u.id
          )) }) : null,
          /* @__PURE__ */ c("div", { className: "ws-flow-feedback-body custom-scrollbar", children: /* @__PURE__ */ c(ut, { fallback: /* @__PURE__ */ c(lt, { label: "正在加载交互表单" }), children: /* @__PURE__ */ c(
            uy,
            {
              interaction: d,
              disabled: t,
              readonly: n,
              hideHeader: !0,
              layout: "dialog",
              initialData: n ? e.values : void 0,
              onSubmit: (u) => a(
                Vw(e, d, u.data)
              )
            }
          ) }) }),
          n ? /* @__PURE__ */ c("footer", { className: "ws-flow-feedback-foot", children: /* @__PURE__ */ v(
            "button",
            {
              type: "button",
              className: "ws-flow-feedback-submit",
              onClick: i,
              children: [
                /* @__PURE__ */ c(zo, { size: 16 }),
                /* @__PURE__ */ c("span", { children: "知道了" })
              ]
            }
          ) }) : null
        ]
      }
    ) }),
    l
  );
}
function Vw(e, t, n) {
  return String(t.type || "").toLowerCase() === "power_params" ? n : {
    ...e.values || {},
    ...n
  };
}
function Uw(e) {
  const t = e.interaction?.interaction || {}, n = Array.isArray(t.fields) ? t.fields : e.fields.length > 0 ? e.fields : [
    {
      id: 0,
      key: "text",
      name: "补充信息",
      type: "textarea",
      required: !0
    }
  ];
  return {
    ...t,
    id: String(t.id || `flow-feedback-${e.approval?.id || 0}`),
    type: String(t.type || "form"),
    title: String(t.title || e.title || "补充信息"),
    description: String(t.description || e.description || ""),
    fields: n,
    values: e.values
  };
}
const hs = { zIndex: 999 };
function Lw(e) {
  const t = Math.max(
    0.35,
    Math.min(1.45, Number.isFinite(e) ? e : 1)
  );
  return {
    "--ws-node-overlay-scale": String(1 / t),
    "--ws-node-overlay-gap": `${16 / t}px`
  };
}
function Kw() {
  return {
    zIndex: 999,
    "--ws-node-overlay-scale": "1",
    "--ws-node-overlay-gap": "16px"
  };
}
function Iu(e) {
  return e.type === "function" && e.functionOption?.key === "start";
}
function Su(e) {
  return e.type === "function" && e.functionOption?.key === "start" ? "将从该开始节点沿连接线执行后续节点，直到保存或展示。" : e.type === "flow" ? "将执行该流程，并把最终结果展示在节点旁。" : e.type === "agent" ? "将把当前提示词、文件和上下文发送给该智能体。" : e.type === "power" ? "将使用当前参数运行该能力节点。" : "确认后开始执行该节点。";
}
function xr({
  icon: e,
  label: t,
  accent: n,
  menu: r
}) {
  return /* @__PURE__ */ v(
    "button",
    {
      type: "button",
      className: `ws-node-setting-button ${n ? `is-${n}` : ""}`,
      children: [
        /* @__PURE__ */ c(e, { size: 12 }),
        /* @__PURE__ */ c("span", { children: t }),
        r ? /* @__PURE__ */ c(Ba, { size: 11 }) : null
      ]
    }
  );
}
async function qw(e, t, n, r) {
  if (e.group?.origin !== "script") {
    await r(t);
    return;
  }
  const o = n.filter($o), s = o.filter(
    (a) => a.storyboardItem?.stale || !Mt(a)
  ), i = s.length > 0 ? s : o;
  await Op(i, r);
}
function Gw({ data: e, selected: t }) {
  const n = e, r = e.sourceNode || n, o = Number(e.projectId || 0), s = e.runningNode, i = e.canvasRunningNodes || {}, a = e.setRunningNode, d = e.onShowNodeDetail, l = e.onNodeResult, u = e.onOpenFeedbackRecord, m = n.type === "power" ? er(n.power, n.kind, n.outputType) : null, N = m?.viewMode === "storyboard", b = m?.viewMode === "storyboard_grid", w = m?.viewMode === "video_compose", z = e.canvasReferenceItems || [], F = e.connectedMediaReferences || Ro, U = e.onConnectedMediaEdgeRemove, A = e.onNodeDraftChange, X = e.onOpenStoryboardGridImport, Z = e.onRunBackendNode, de = !!e.structureLocked, k = e.storyboardSourceNode;
  if (n.type === "group") {
    const C = e.groupMembers || [], T = !!e.storyboardFrameRunning, re = Ep({
      members: C,
      runningNodes: i,
      groupState: s,
      hasResult: Mt
    }), ne = e.onRunBackendNode, me = String(e.runBlockedReason || "");
    let ie;
    return ne && !me && !T && (ie = () => {
      a?.((H) => ({
        ...H,
        [n.id]: {
          nodeId: n.id,
          title: n.title,
          startedAt: Date.now(),
          progress: 8,
          status: "running"
        }
      })), qw(
        n,
        r,
        C,
        ne
      ).then(() => {
        a?.((H) => Dn(H, n.id));
      }).catch((H) => {
        a?.((J) => ({
          ...J,
          [n.id]: {
            ...J[n.id] || {
              nodeId: n.id,
              title: n.title,
              startedAt: Date.now(),
              progress: 8
            },
            status: "error"
          }
        })), G.error(
          H instanceof Error ? H.message : "分组运行失败"
        ), window.setTimeout(() => {
          a?.((J) => Dn(J, n.id));
        }, 1400);
      });
    }), /* @__PURE__ */ c(ut, { fallback: /* @__PURE__ */ c(lt, { label: "正在加载分组" }), children: /* @__PURE__ */ v(
      ny,
      {
        node: n,
        memberCount: re.memberCount,
        runnableCount: re.runnableCount,
        completedCount: re.completedCount,
        failedCount: re.failedCount,
        staleCount: re.staleCount,
        status: re.status,
        frameRunning: T,
        selected: t,
        managed: de,
        onRename: l && !de ? (H) => l(n.id, { title: H, titleMode: "manual" }) : void 0,
        onEditStructure: k && d ? () => d(
          k,
          yd(n)
        ) : void 0,
        onRun: ie,
        runBlockedReason: me,
        children: [
          /* @__PURE__ */ c(
            dt,
            {
              id: "input-0",
              type: "target",
              position: ct.Left,
              className: "is-in"
            }
          ),
          /* @__PURE__ */ c(
            dt,
            {
              id: "output-0",
              type: "source",
              position: ct.Right,
              className: "is-out"
            }
          ),
          /* @__PURE__ */ c(hn, { node: n, selected: t })
        ]
      }
    ) });
  }
  if (n.type === "agent") {
    const C = Qt(s) || s?.status === "success";
    return /* @__PURE__ */ v(
      "div",
      {
        className: `ws-node-agent-wrap ${t ? "is-selected" : ""} ${C ? "is-running" : ""}`,
        children: [
          /* @__PURE__ */ c(
            dt,
            {
              id: "input-0",
              type: "target",
              position: ct.Left,
              className: "is-in",
              style: { left: "4px" }
            }
          ),
          /* @__PURE__ */ c(
            dt,
            {
              id: "output-0",
              type: "source",
              position: ct.Right,
              className: "is-out",
              style: { right: "4px" }
            }
          ),
          /* @__PURE__ */ v("div", { className: "ws-node-circle", children: [
            /* @__PURE__ */ c("div", { className: "ws-node-circle-avatar", children: /* @__PURE__ */ c(yl, { size: 20, className: "ws-icon-amber" }) }),
            /* @__PURE__ */ c(
              os,
              {
                className: "ws-node-circle-title",
                title: n.title,
                onRename: l ? (T) => l(n.id, { title: T, titleMode: "manual" }) : void 0
              }
            )
          ] }),
          C ? /* @__PURE__ */ v(
            "svg",
            {
              className: "ws-node-running-border is-spin is-circle is-agent",
              "aria-hidden": "true",
              viewBox: "0 0 100 100",
              preserveAspectRatio: "none",
              children: [
                /* @__PURE__ */ c(
                  "circle",
                  {
                    className: "ws-node-running-track",
                    cx: "50",
                    cy: "50",
                    r: "47",
                    pathLength: "100"
                  }
                ),
                /* @__PURE__ */ c(
                  "circle",
                  {
                    className: "ws-node-running-progress",
                    cx: "50",
                    cy: "50",
                    r: "47",
                    pathLength: "100",
                    strokeDasharray: "18 82",
                    strokeDashoffset: "0"
                  }
                )
              ]
            }
          ) : null,
          /* @__PURE__ */ c(
            ys,
            {
              node: n,
              onOpenFeedbackRecord: u
            }
          ),
          /* @__PURE__ */ c(
            gs,
            {
              node: n,
              runningNode: s,
              onShowNodeDetail: d
            }
          ),
          /* @__PURE__ */ c(hn, { node: n, selected: t })
        ]
      }
    );
  }
  if (n.type === "flow") {
    const C = Qt(s) || s?.status === "success";
    return /* @__PURE__ */ v(
      "div",
      {
        className: `ws-node-flow-wrap ${t ? "is-selected" : ""} ${C ? "is-running" : ""}`,
        children: [
          /* @__PURE__ */ c(
            "svg",
            {
              className: "ws-hexagon-svg",
              viewBox: "0 0 100 100",
              fill: "currentColor",
              children: /* @__PURE__ */ c(
                "polygon",
                {
                  points: "50,4 93,27 93,73 50,96 7,73 7,27",
                  stroke: t ? "var(--ws-blue)" : "var(--ws-border)",
                  strokeWidth: "1.5",
                  strokeLinejoin: "round"
                }
              )
            }
          ),
          C ? /* @__PURE__ */ v(
            "svg",
            {
              className: "ws-node-running-border is-spin is-hexagon",
              "aria-hidden": "true",
              viewBox: "0 0 100 100",
              preserveAspectRatio: "none",
              children: [
                /* @__PURE__ */ c(
                  "polygon",
                  {
                    className: "ws-node-running-track",
                    points: "50,5 92,28 92,72 50,95 8,72 8,28",
                    pathLength: "100"
                  }
                ),
                /* @__PURE__ */ c(
                  "polygon",
                  {
                    className: "ws-node-running-progress",
                    points: "50,5 92,28 92,72 50,95 8,72 8,28",
                    pathLength: "100",
                    strokeDasharray: "18 82",
                    strokeDashoffset: "0"
                  }
                )
              ]
            }
          ) : null,
          /* @__PURE__ */ v("div", { className: "ws-node-flow-content", children: [
            /* @__PURE__ */ c("div", { className: "ws-node-flow-avatar", children: /* @__PURE__ */ c(hl, { size: 16, className: "ws-icon-blue" }) }),
            /* @__PURE__ */ c(
              os,
              {
                className: "ws-node-flow-title",
                title: n.title,
                onRename: l ? (T) => l(n.id, { title: T, titleMode: "manual" }) : void 0
              }
            )
          ] }),
          /* @__PURE__ */ c(
            dt,
            {
              id: "input-0",
              type: "target",
              position: ct.Left,
              className: "is-in",
              style: { left: "11px" }
            }
          ),
          /* @__PURE__ */ c(
            dt,
            {
              id: "output-0",
              type: "source",
              position: ct.Right,
              className: "is-out",
              style: { right: "11px" }
            }
          ),
          /* @__PURE__ */ c(
            ys,
            {
              node: n,
              onOpenFeedbackRecord: u
            }
          ),
          /* @__PURE__ */ c(gs, { node: n, onShowNodeDetail: d }),
          /* @__PURE__ */ c(hn, { node: n, selected: t })
        ]
      }
    );
  }
  if (n.type === "function") {
    const C = n.functionOption?.key || (n.title.includes("保存") ? "save" : ""), T = C === "start", re = yu(C), ne = n.onRunStartNode, me = n.onRunFunctionNode, ie = n.onOpenImportPicker, H = n.requestConfirm, J = Qt(s), ae = T && Ry(i), xe = J, le = Ti(n), Be = le ? { left: "0px", top: "19px" } : { left: "0px" }, ye = le ? { left: "128px", right: "auto", top: "19px" } : { right: "0px" }, Ce = (fe) => {
      a && (a((Qe) => ({
        ...Qe,
        [n.id]: {
          nodeId: n.id,
          title: n.title,
          startedAt: Date.now(),
          progress: fe === "success" ? 100 : fe === "error" ? 92 : 0,
          status: fe
        }
      })), fe !== "running" && fe !== "waiting" && window.setTimeout(
        () => a((Qe) => Dn(Qe, n.id)),
        fe === "success" ? 650 : 1200
      ));
    }, Pe = () => {
      if (xe || ae)
        return;
      const fe = !T, Qe = me ? C === "start" && ne ? () => ne(n).then(() => !0) : () => me(n) : C === "start" && ne ? () => ne(n).then(() => !0) : C === "import" && ie ? () => (ie(n.id), Promise.resolve(!0)) : null;
      if (Qe) {
        if (H && Iu(n)) {
          H({
            title: `执行「${n.title}」`,
            description: Su(n),
            confirmText: "执行",
            onConfirm: () => {
              fe && Ce("running"), Qe().then(() => {
                fe && Ce("success");
              }).catch((gt) => {
                fe && Ce("error"), G.error(gt instanceof Error ? gt.message : "执行出错");
              });
            }
          });
          return;
        }
        fe && Ce("running"), Qe().then(() => {
          fe && Ce("success");
        }).catch((gt) => {
          fe && Ce("error"), G.error(gt instanceof Error ? gt.message : "执行出错");
        });
      }
    }, Y = (fe) => {
      fe.preventDefault(), fe.stopPropagation(), Pe();
    }, Gt = !!me || C === "start" && !!ne || C === "import" && !!ie;
    return /* @__PURE__ */ v(
      "div",
      {
        className: `ws-node-function-wrap ${t ? "is-selected" : ""} ${xe ? "is-running" : ""} ${le ? "has-result-card" : ""} is-${C || "default"}`,
        children: [
          /* @__PURE__ */ v(
            "div",
            {
              className: "ws-node-function-pill",
              role: Gt ? "button" : void 0,
              tabIndex: Gt ? 0 : void 0,
              "aria-disabled": Gt ? xe : void 0,
              onClick: Gt ? Y : void 0,
              onKeyDown: (fe) => {
                !Gt || fe.key !== "Enter" && fe.key !== " " || (fe.preventDefault(), fe.stopPropagation(), Pe());
              },
              children: [
                /* @__PURE__ */ c("div", { className: "ws-node-function-icon", children: J ? /* @__PURE__ */ c(sn, { size: 15, className: "ws-spin" }) : /* @__PURE__ */ c(
                  re,
                  {
                    size: 15,
                    fill: T ? "currentColor" : "none"
                  }
                ) }),
                /* @__PURE__ */ c("span", { className: "ws-node-function-title", children: J ? s?.status === "waiting" ? "等待中" : "运行中" : n.title })
              ]
            }
          ),
          le ? /* @__PURE__ */ c(
            Tw,
            {
              node: n,
              running: xe,
              onShowNodeDetail: d
            }
          ) : null,
          /* @__PURE__ */ c(
            dt,
            {
              id: "input-0",
              type: "target",
              position: ct.Left,
              className: "is-in",
              style: Be
            }
          ),
          /* @__PURE__ */ c(
            dt,
            {
              id: "output-0",
              type: "source",
              position: ct.Right,
              className: "is-out",
              style: ye
            }
          ),
          /* @__PURE__ */ c(
            ys,
            {
              node: n,
              onOpenFeedbackRecord: u
            }
          ),
          le ? null : /* @__PURE__ */ c(gs, { node: n, onShowNodeDetail: d }),
          /* @__PURE__ */ c(hn, { node: n, selected: t })
        ]
      }
    );
  }
  if (n.type === "asset") {
    if (n.kind === "image") {
      const ae = ls(n), xe = Gn(n), le = ur(xe, ae), Be = mo(n, l), ye = [
        "ws-node-image-wrap",
        t ? "is-selected" : "",
        ae.imageUrl ? "has-media" : ""
      ].filter(Boolean).join(" ");
      return /* @__PURE__ */ v("div", { className: ye, children: [
        /* @__PURE__ */ v("div", { className: "ws-node-floating-label", children: [
          /* @__PURE__ */ c(zi, { size: 13, className: "ws-icon-green" }),
          /* @__PURE__ */ c("span", { children: n.title || "图片资产" })
        ] }),
        /* @__PURE__ */ c("div", { className: "ws-node-image-container ws-node-content-container", children: le ? /* @__PURE__ */ c("div", { className: "ws-node-scroll-content nowheel", children: /* @__PURE__ */ c(
          lr,
          {
            output: xe,
            fallback: ae.text || n.description || "图片资产",
            mediaGridKind: "image",
            className: "ws-canvas-content-view"
          }
        ) }) : ae.imageUrl ? /* @__PURE__ */ c(
          qs,
          {
            src: ae.imageUrl,
            alt: n.title,
            className: "ws-node-image-raw",
            onMediaSize: Be
          }
        ) : /* @__PURE__ */ v("div", { className: "ws-node-image-empty", children: [
          /* @__PURE__ */ c(zi, { size: 24 }),
          /* @__PURE__ */ c("span", { children: ae.text || n.description || "图片资产" })
        ] }) }),
        /* @__PURE__ */ c(
          dt,
          {
            id: "input-0",
            type: "target",
            position: ct.Left,
            className: "is-in"
          }
        ),
        /* @__PURE__ */ c(
          dt,
          {
            id: "output-0",
            type: "source",
            position: ct.Right,
            className: "is-out"
          }
        ),
        /* @__PURE__ */ c(
          Rr,
          {
            node: n,
            onShowNodeDetail: d
          }
        ),
        /* @__PURE__ */ c(hn, { node: n, selected: t })
      ] });
    }
    if (n.kind === "video") {
      const ae = ls(n), xe = Gn(n), le = ur(xe, ae), Be = mo(n, l), ye = [
        "ws-node-video-wrap",
        t ? "is-selected" : "",
        ae.videoUrl || ae.imageUrl ? "has-media" : ""
      ].filter(Boolean).join(" ");
      return /* @__PURE__ */ v("div", { className: ye, children: [
        /* @__PURE__ */ v("div", { className: "ws-node-floating-label", children: [
          /* @__PURE__ */ c(Fi, { size: 13, className: "ws-icon-green" }),
          /* @__PURE__ */ c("span", { children: n.title || "视频资产" })
        ] }),
        /* @__PURE__ */ v("div", { className: "ws-node-video-container ws-node-content-container", children: [
          le ? /* @__PURE__ */ c("div", { className: "ws-node-scroll-content nowheel", children: /* @__PURE__ */ c(
            lr,
            {
              output: xe,
              fallback: ae.text || n.description || "视频资产",
              mediaGridKind: "video",
              className: "ws-canvas-content-view"
            }
          ) }) : ae.videoUrl ? /* @__PURE__ */ c(
            go,
            {
              src: ae.videoUrl,
              className: "ws-node-video-raw",
              muted: !0,
              playsInline: !0,
              preload: "metadata",
              onLoadedMetadata: (Ce) => Be?.(
                Ce.currentTarget.videoWidth,
                Ce.currentTarget.videoHeight
              )
            },
            ae.videoUrl
          ) : ae.imageUrl ? /* @__PURE__ */ c(
            qs,
            {
              src: ae.imageUrl,
              alt: n.title,
              className: "ws-node-video-raw",
              onMediaSize: Be
            }
          ) : /* @__PURE__ */ v("div", { className: "ws-node-image-empty", children: [
            /* @__PURE__ */ c(Fi, { size: 24 }),
            /* @__PURE__ */ c("span", { children: ae.text || n.description || "视频资产" })
          ] }),
          le ? null : /* @__PURE__ */ c("div", { className: "ws-node-video-play", children: /* @__PURE__ */ c("div", { children: /* @__PURE__ */ c(Ur, { size: 14, fill: "currentColor" }) }) })
        ] }),
        /* @__PURE__ */ c(
          dt,
          {
            id: "input-0",
            type: "target",
            position: ct.Left,
            className: "is-in"
          }
        ),
        /* @__PURE__ */ c(
          dt,
          {
            id: "output-0",
            type: "source",
            position: ct.Right,
            className: "is-out"
          }
        ),
        /* @__PURE__ */ c(
          Rr,
          {
            node: n,
            onShowNodeDetail: d
          }
        ),
        /* @__PURE__ */ c(hn, { node: n, selected: t })
      ] });
    }
    const C = ls(n), T = Ni(n), re = Gn(n), ne = vi(n), me = Ye(re) ? re : T ? { rich: T } : ne || C.text, ie = ur(me, C), H = !!(C.imageUrl || C.videoUrl || C.audioUrl), J = [
      "ws-node-text-wrap",
      t ? "is-selected" : "",
      H ? "has-media" : ""
    ].filter(Boolean).join(" ");
    return /* @__PURE__ */ v("div", { className: J, children: [
      /* @__PURE__ */ v("div", { className: "ws-node-floating-label", children: [
        /* @__PURE__ */ c(fl, { size: 13, className: "ws-icon-green" }),
        /* @__PURE__ */ c("span", { children: n.title })
      ] }),
      /* @__PURE__ */ c("div", { className: "ws-node-text-card", children: !ie && C.imageUrl ? /* @__PURE__ */ c("div", { className: "ws-node-text-media", children: /* @__PURE__ */ c(
        "img",
        {
          src: C.imageUrl,
          alt: Br(C) || n.title,
          loading: "lazy",
          decoding: "async"
        }
      ) }) : !ie && C.videoUrl ? /* @__PURE__ */ c("div", { className: "ws-node-text-media", children: /* @__PURE__ */ c(
        go,
        {
          src: C.videoUrl,
          muted: !0,
          playsInline: !0,
          preload: "metadata"
        },
        C.videoUrl
      ) }) : !ie && C.audioUrl ? /* @__PURE__ */ c("div", { className: "ws-node-text-media is-audio", children: /* @__PURE__ */ c(
        ut,
        {
          fallback: /* @__PURE__ */ c(lt, { label: "正在加载音频", compact: !0 }),
          children: /* @__PURE__ */ c(Qa, { src: C.audioUrl })
        }
      ) }) : !ie && C.fileUrl ? /* @__PURE__ */ v("div", { className: "ws-node-text-file", children: [
        /* @__PURE__ */ c(Ys, { size: 16 }),
        /* @__PURE__ */ c("span", { children: Br(C) || "文件内容" })
      ] }) : /* @__PURE__ */ c("div", { className: "ws-node-scroll-content nowheel", children: /* @__PURE__ */ c(
        lr,
        {
          output: me,
          fallback: ne || C.text || "暂无内容",
          className: "ws-canvas-content-view"
        }
      ) }) }),
      /* @__PURE__ */ c(
        dt,
        {
          id: "input-0",
          type: "target",
          position: ct.Left,
          className: "is-in"
        }
      ),
      /* @__PURE__ */ c(
        dt,
        {
          id: "output-0",
          type: "source",
          position: ct.Right,
          className: "is-out"
        }
      ),
      /* @__PURE__ */ c(
        Rr,
        {
          node: n,
          onShowNodeDetail: d
        }
      ),
      /* @__PURE__ */ c(hn, { node: n, selected: t })
    ] });
  }
  if (n.type === "power") {
    const C = Qt(s), T = Zs(n.power, n.kind), re = b ? Js(
      C ? [s?.streamOutput, fs(n)] : fs(n)
    ) : null, ne = Mt(n), me = C ? "running" : s?.status === "error" ? "error" : s?.status === "success" && !ne ? "running" : ne ? "complete" : "empty", ie = !!(!N && !b && !w && s?.streamStarted && (s.streamText || s.streamOutput) && s.status !== "success"), H = ie ? s?.streamOutput ? jn(s.streamOutput, "audio") : {
      text: s?.streamText || "",
      imageUrl: "",
      videoUrl: "",
      audioUrl: "",
      fileUrl: ""
    } : _r(n), J = ie ? s?.streamOutput || { text: s?.streamText || "" } : Gn(n), ae = N || b || w || ie || ne, xe = !N && !b && !w && !!(H.imageUrl || H.videoUrl || H.audioUrl || H.fileUrl), le = mo(n, l), Be = [
      "ws-node-power-wrap",
      t ? "is-selected" : "",
      C ? "is-running" : "",
      n.runError && !C ? "is-error" : "",
      N ? "is-storyboard" : "",
      b ? "is-storyboard-grid" : "",
      w ? "is-video-compose" : "",
      T ? "is-audio" : "",
      ae ? "has-content" : "",
      xe ? "has-media" : ""
    ].filter(Boolean).join(" ");
    return /* @__PURE__ */ v("div", { className: Be, children: [
      /* @__PURE__ */ v("div", { className: "ws-node-floating-label", children: [
        /* @__PURE__ */ c(
          pl,
          {
            power: n.power,
            kind: n.kind,
            outputType: n.outputType,
            size: 13,
            className: "ws-icon-violet"
          }
        ),
        /* @__PURE__ */ c(
          os,
          {
            title: n.title,
            onRename: l && !de ? (ye) => l(n.id, { title: ye, titleMode: "manual" }) : void 0
          }
        ),
        n.storyboardItem?.stale ? /* @__PURE__ */ c(We, { label: "上游素材或提示词已变化；当前结果仍可使用，重新运行可更新", children: /* @__PURE__ */ c("span", { className: "ws-node-stale-badge", children: "可更新" }) }) : null,
        ed(n) ? /* @__PURE__ */ c("span", { className: "ws-node-prompt-override-badge", children: "提示词已修改" }) : null
      ] }),
      /* @__PURE__ */ v("div", { className: "ws-node-power-card", children: [
        C ? /* @__PURE__ */ v("svg", { className: "ws-node-running-border is-spin", "aria-hidden": "true", children: [
          /* @__PURE__ */ c(
            "rect",
            {
              className: "ws-node-running-track",
              x: "0",
              y: "0",
              width: "100%",
              height: "100%",
              rx: "6",
              pathLength: "100"
            }
          ),
          /* @__PURE__ */ c(
            "rect",
            {
              className: "ws-node-running-progress",
              x: "0",
              y: "0",
              width: "100%",
              height: "100%",
              rx: "6",
              pathLength: "100",
              strokeDasharray: "18 82",
              strokeDashoffset: "0"
            }
          )
        ] }) : null,
        w ? /* @__PURE__ */ c(
          ut,
          {
            fallback: /* @__PURE__ */ c(lt, { label: "正在加载视频合成" }),
            children: /* @__PURE__ */ c(
              _y,
              {
                composition: n.composerDraft?.videoComposition,
                referenceItems: z.filter(
                  (ye) => ye.source !== "current" || ye.id !== n.id
                ),
                connectedMediaReferences: F,
                running: C,
                onChange: A ? (ye) => A(n.id, {
                  ...n.composerDraft || {},
                  videoComposition: ye
                }) : void 0,
                onConnectedMediaEdgeRemove: U,
                onRun: Z ? (ye) => {
                  Z({
                    ...n,
                    composerDraft: {
                      ...n.composerDraft || {},
                      videoComposition: ye
                    }
                  }).catch(
                    (Ce) => G.error(
                      Ce instanceof Error ? Ce.message : "视频合成失败"
                    )
                  );
                } : void 0,
                onOpenDetail: d ? () => d(n) : void 0
              }
            )
          }
        ) : N ? /* @__PURE__ */ c(
          ut,
          {
            fallback: /* @__PURE__ */ c(lt, { label: "正在加载分镜内容", compact: !0 }),
            children: /* @__PURE__ */ c(
              cy,
              {
                output: C ? s?.streamText || "" : fs(n),
                status: me,
                started: !!s?.streamStarted,
                generatedShotCount: s?.generatedCount || 0,
                referenceItems: z.filter(
                  (ye) => ye.source !== "current" || ye.id !== n.id
                ),
                onOpenDetail: ne && d ? () => d(n) : void 0
              }
            )
          }
        ) : b ? /* @__PURE__ */ c(
          vl,
          {
            grid: re,
            aspectRatio: zh(n),
            running: C,
            layout: n.composerDraft?.storyboardGridLayout,
            onLayoutChange: A ? (ye) => A(n.id, {
              ...n.composerDraft || {},
              storyboardGridLayout: ye
            }) : void 0,
            onImport: X ? () => X(n.id) : void 0,
            onFrameImport: X ? (ye, Ce) => X(n.id, Ce) : void 0,
            onSlotImport: X ? (ye) => X(n.id, ye) : void 0,
            onEdit: re && d ? () => d(n) : void 0
          }
        ) : ae ? /* @__PURE__ */ c(
          vu,
          {
            preview: H,
            output: J,
            fallback: n.description,
            streaming: C && ie,
            generating: C && xe && !ie,
            onMediaSize: le
          }
        ) : /* @__PURE__ */ c(Yw, {})
      ] }),
      n.runError && !C ? /* @__PURE__ */ c(
        Hw,
        {
          projectId: o,
          node: n,
          onOpenDetail: d ? () => d(n) : void 0
        }
      ) : null,
      /* @__PURE__ */ c(
        dt,
        {
          id: "input-0",
          type: "target",
          position: ct.Left,
          className: "is-in"
        }
      ),
      /* @__PURE__ */ c(
        dt,
        {
          id: "output-0",
          type: "source",
          position: ct.Right,
          className: "is-out"
        }
      ),
      N || b || w ? null : /* @__PURE__ */ c(
        Rr,
        {
          node: n,
          onShowNodeDetail: d
        }
      ),
      /* @__PURE__ */ c(hn, { node: n, selected: t })
    ] });
  }
  return /* @__PURE__ */ v("div", { className: `ws-node ${t ? "is-selected" : ""}`, children: [
    /* @__PURE__ */ c(
      dt,
      {
        id: "input-0",
        type: "target",
        position: ct.Left,
        className: "is-in"
      }
    ),
    /* @__PURE__ */ c(
      dt,
      {
        id: "output-0",
        type: "source",
        position: ct.Right,
        className: "is-out"
      }
    ),
    /* @__PURE__ */ c("div", { className: "ws-node-title", children: n.title }),
    /* @__PURE__ */ c("div", { className: "ws-node-desc", children: n.description }),
    /* @__PURE__ */ c(Rr, { node: n, onShowNodeDetail: d }),
    /* @__PURE__ */ c(hn, { node: n, selected: t })
  ] });
}
function Hw({
  projectId: e,
  node: t,
  onOpenDetail: n
}) {
  const { error: r } = Xg(e, t), o = /* @__PURE__ */ v(zn, { children: [
    /* @__PURE__ */ c(bs, { size: 14 }),
    /* @__PURE__ */ c("span", { children: r })
  ] });
  return /* @__PURE__ */ c(We, { label: r, children: n ? /* @__PURE__ */ c(
    "button",
    {
      type: "button",
      className: "ws-node-run-error is-action nodrag nowheel",
      "aria-label": `打开错误详情：${r}`,
      onClick: (s) => {
        s.stopPropagation(), n();
      },
      children: o
    }
  ) : /* @__PURE__ */ c("div", { className: "ws-node-run-error", children: o }) });
}
function vu({
  preview: e,
  output: t,
  fallback: n,
  streaming: r,
  generating: o = !1,
  onMediaSize: s,
  showMediaCaption: i = !0
}) {
  const a = Q(null), d = Q(!0), l = i ? Br(e) : "", u = ur(t, e);
  return ce(() => {
    if (!r) {
      d.current = !0;
      return;
    }
    const m = a.current;
    !m || !d.current || (m.scrollTop = m.scrollHeight);
  }, [e.text, r]), !u && e.imageUrl ? /* @__PURE__ */ v(
    "div",
    {
      className: `ws-node-generated-media ${o ? "is-generating" : ""}`,
      children: [
        /* @__PURE__ */ c(
          qs,
          {
            src: e.imageUrl,
            alt: l || "生成图片",
            onMediaSize: s
          }
        ),
        l ? /* @__PURE__ */ c("p", { children: l }) : null,
        /* @__PURE__ */ c(ws, { active: o })
      ]
    }
  ) : !u && e.videoUrl ? /* @__PURE__ */ v(
    "div",
    {
      className: `ws-node-generated-media ${o ? "is-generating" : ""}`,
      children: [
        /* @__PURE__ */ c(
          go,
          {
            src: e.videoUrl,
            className: "nopan nowheel",
            controls: !0,
            playsInline: !0,
            preload: "metadata",
            onLoadedMetadata: (m) => s?.(
              m.currentTarget.videoWidth,
              m.currentTarget.videoHeight
            )
          },
          e.videoUrl
        ),
        l ? /* @__PURE__ */ c("p", { children: l }) : null,
        /* @__PURE__ */ c(ws, { active: o })
      ]
    }
  ) : !u && e.audioUrl ? /* @__PURE__ */ v(
    "div",
    {
      className: `ws-node-generated-media is-audio ${o ? "is-generating" : ""}`,
      children: [
        /* @__PURE__ */ c(
          ut,
          {
            fallback: /* @__PURE__ */ c(lt, { label: "正在加载音频", compact: !0 }),
            children: /* @__PURE__ */ c(Qa, { src: e.audioUrl, autoPlay: r })
          }
        ),
        /* @__PURE__ */ c(ws, { active: o })
      ]
    }
  ) : !u && e.fileUrl ? /* @__PURE__ */ v("div", { className: "ws-node-generated-file", children: [
    /* @__PURE__ */ c(Ys, { size: 16 }),
    /* @__PURE__ */ c("span", { children: l || "文件内容" })
  ] }) : /* @__PURE__ */ c(
    "div",
    {
      ref: a,
      className: "ws-node-generated-text ws-node-scroll-content nowheel",
      onScroll: (m) => {
        const N = m.currentTarget;
        d.current = N.scrollHeight - N.scrollTop - N.clientHeight < 12;
      },
      children: /* @__PURE__ */ c(
        lr,
        {
          output: t,
          fallback: e.text || n,
          streaming: r,
          mediaGridKind: e.videoUrl ? "video" : e.imageUrl ? "image" : void 0,
          className: "ws-canvas-content-view"
        }
      )
    }
  );
}
function qs({
  src: e,
  alt: t,
  className: n,
  onMediaSize: r
}) {
  const [o, s] = j(e);
  return ce(() => {
    if (!e || e === o)
      return;
    let i = !0;
    const a = new Image();
    return a.onload = () => {
      (a.decode?.() || Promise.resolve()).catch(() => {
      }).then(() => {
        i && s(e);
      });
    }, a.src = e, () => {
      i = !1, a.onload = null;
    };
  }, [o, e]), /* @__PURE__ */ c(
    "img",
    {
      src: o,
      alt: t,
      className: n,
      loading: "lazy",
      decoding: "async",
      onLoad: (i) => r?.(
        i.currentTarget.naturalWidth,
        i.currentTarget.naturalHeight
      )
    }
  );
}
function ws({ active: e }) {
  return e ? /* @__PURE__ */ v(
    "div",
    {
      className: "ws-node-media-generating nodrag nopan nowheel",
      role: "status",
      "aria-live": "polite",
      onPointerDown: (t) => t.stopPropagation(),
      onClick: (t) => t.stopPropagation(),
      children: [
        /* @__PURE__ */ c(sn, { size: 18, className: "ws-spin" }),
        /* @__PURE__ */ c("span", { children: "生成中" })
      ]
    }
  ) : null;
}
function Br(e) {
  const t = String(e.text || "").trim();
  return !t || Nn(t) ? "" : t;
}
function Ww(e, t) {
  if (!Number.isFinite(e) || !Number.isFinite(t) || e <= 0 || t <= 0)
    return null;
  const n = e / t, r = 330, o = 340;
  let s = r, i = s / n;
  return i > o && (i = o, s = i * n), {
    width: Math.round(Pa(s, 150, r)),
    height: Math.round(Pa(i, 150, o))
  };
}
function mo(e, t, n = 0) {
  if (!(e.groupId || !t))
    return (r, o) => {
      const s = Ww(r, o);
      if (!s)
        return;
      const i = {
        width: s.width,
        height: s.height + n
      };
      Math.abs((e.width || 0) - i.width) <= 2 && Math.abs((e.height || 0) - i.height) <= 2 || t(e.id, i);
    };
}
function Pa(e, t, n) {
  return Math.max(t, Math.min(n, e));
}
function Yw() {
  return /* @__PURE__ */ v("div", { className: "ws-node-power-empty", "aria-hidden": "true", children: [
    /* @__PURE__ */ c("span", {}),
    /* @__PURE__ */ c("span", {}),
    /* @__PURE__ */ c("span", {})
  ] });
}
function Xw(e) {
  return e.type === "group" ? "#e85d75" : e.type === "asset" ? "#23c483" : e.type === "power" ? "#8b5cf6" : e.type === "agent" ? "#f59e0b" : e.type === "flow" ? "#3b82f6" : "#e85d75";
}
function Zw() {
  if (typeof window > "u")
    return 0;
  const e = new URLSearchParams(window.location.search);
  return Number(e.get("project_id") || e.get("id") || 0);
}
const qb = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  WorkSpacePage: Ey
}, Symbol.toStringTag, { value: "Module" }));
export {
  lf as V,
  ii as a,
  im as b,
  Bc as c,
  jb as d,
  zb as e,
  Ub as f,
  Bb as g,
  Vb as h,
  Kb as i,
  Lb as j,
  $b as k,
  Pb as l,
  An as m,
  Fb as n,
  Eb as o,
  ff as p,
  qb as q,
  Zp as r,
  Np as s,
  Xg as u,
  Ob as v
};
