import { c as Hn, a as v, j as c, F as Qt } from "./createLucideIcon-CEtb6KSk.js";
import { r as Me, j as xe, a as te, u as $, b as le, d as P, h as ku, c as ue, g as ca, l as Nt, o as xu, i as Au, k as st } from "./runtime-entry-CIrzyMsA.js";
import { c as Tu } from "./react-dom-C2oimP4o.js";
import { N as Mu, a as wr, w as Pu, n as Du, g as Eu, B as Qs, E as Fu, b as zu, d as Ou, i as $u, M as ju, c as Bu, P as je, H as Uu } from "./normalize-D6EFkkMI.js";
import { S as Vu, e as Ku, f as Lu, A as qu, o as Gu, b as gs, g as Hu, h as ua, c as da, n as ei } from "./space-DNu08Ce2.js";
import { C as Yu } from "./circle-alert-QPWZCk4j.js";
import { C as cr } from "./circle-check-DjAs7CDF.js";
import { L as Bt } from "./loader-circle-QnfinZ3F.js";
import { A as Wu } from "./arrow-left-Dav-TMCY.js";
import { C as uo } from "./chevron-down-DQJPZ33_.js";
import { E as ur } from "./eye-EJzwoxVH.js";
import { F as ys } from "./file-text-CclNuHuN.js";
import { G as Xu } from "./git-branch-gcLa7N9D.js";
import { H as la } from "./history-DOFgCRvO.js";
import { I as ti } from "./image-BBD2HfB8.js";
import { M as Ju, b as fa, r as pa, c as ma, n as ga, d as Zu, e as Qu, f as ed, m as td, g as nd, h as rd, C as od } from "./interaction-d6W_Ir2J.js";
import { S as sd, M as id } from "./configured-icon-BkvRvz03.js";
import { P as lo } from "./play-cbWwOmIe.js";
import { P as hs } from "./plus-Di9i7LEg.js";
import { S as ya } from "./save-CZ_LAFk4.js";
import { P as ha, a as ni, C as ad, T as cd } from "./space-power-icon-DPR3KYFq.js";
import { U as ud } from "./upload-I0iT6F7Q.js";
import { U as dd, W as ld } from "./workflow-Cs2Tatpf.js";
import { V as ri } from "./video-BAp8-tqb.js";
import { X as wa } from "./x-D8YQA7_X.js";
import { t as ne } from "./index-DqjOvQjw.js";
import { g as fd } from "./site-config-BtBYhKpy.js";
import { u as pd } from "./use-body-appearance-Br-DLopK.js";
import { an as md, ao as gd, ap as yd, aq as hd, ar as Ut, as as wd, at as ba, au as bd, b as Je, av as Na, aw as oi, ax as si, a9 as _a, ad as Ia, ay as Nd, u as _d, n as Sa, d as Id, az as Sd, aA as ws, aB as va, G as vd, I as Cd, J as Rd, K as kd, q as xd, aC as Ad, v as Ca, w as Ra, aD as qr, aE as Td, t as Md, P as Pd, $ as Dd, aF as bs, p as br, i as Ed, L as ka, aG as xa, aH as fo, aI as Xt, aJ as Fd, aK as zd, aL as Ur, aM as Od, aN as $d, aO as jd, aP as Er, aQ as ii, aR as Bd, r as Nr, aS as Ud, aT as Vd, aU as Gr, aV as Kd, aW as qo, aX as Ld, aY as qd, a7 as Gd, a8 as ke, aZ as Ns, a_ as Hd, a$ as Yd, c as sr, C as Vr, b0 as dr, ak as Wd, ab as Xd, b1 as Aa, ac as Go, b2 as Jd, b3 as Zd } from "./upload-asset-api-DAbIOMVJ.js";
import { i as Oe } from "./api-response-C-VXY2RJ.js";
import { C as Qd } from "./copy-B2Ci6O8V.js";
import { P as el } from "./pencil-tXNtQW8N.js";
import { R as tl } from "./rotate-ccw-DgOB4jhS.js";
import { T as nl } from "./trash-2-EsqTj1ob.js";
import { u as rl } from "./asset-reference-provider-qeDIBhpP.js";
import { C as ol, a as it } from "./space-entry-DO3nFNaO.js";
const sl = [
  [
    "path",
    {
      d: "m16.24 7.76-1.804 5.411a2 2 0 0 1-1.265 1.265L7.76 16.24l1.804-5.411a2 2 0 0 1 1.265-1.265z",
      key: "9ktpf1"
    }
  ],
  ["circle", { cx: "12", cy: "12", r: "10", key: "1mglay" }]
], il = Hn("compass", sl);
const al = [
  [
    "path",
    {
      d: "M6 22a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h8a2.4 2.4 0 0 1 1.704.706l3.588 3.588A2.4 2.4 0 0 1 20 8v12a2 2 0 0 1-2 2z",
      key: "1oefj6"
    }
  ],
  ["path", { d: "M14 2v5a1 1 0 0 0 1 1h5", key: "wfsgrz" }],
  ["circle", { cx: "11.5", cy: "14.5", r: "2.5", key: "1bq0ko" }],
  ["path", { d: "M13.3 16.3 15 18", key: "2quom7" }]
], Ta = Hn("file-search", al);
const cl = [
  ["circle", { cx: "12", cy: "12", r: "3", key: "1v7zrd" }],
  ["path", { d: "M3 7V5a2 2 0 0 1 2-2h2", key: "aa7l1z" }],
  ["path", { d: "M17 3h2a2 2 0 0 1 2 2v2", key: "4qcy5o" }],
  ["path", { d: "M21 17v2a2 2 0 0 1-2 2h-2", key: "6vwrx8" }],
  ["path", { d: "M7 21H5a2 2 0 0 1-2-2v-2", key: "ioqczr" }]
], ul = Hn("focus", cl);
const dl = [
  [
    "path",
    {
      d: "M15 14c.2-1 .7-1.7 1.5-2.5 1-.9 1.5-2.2 1.5-3.5A6 6 0 0 0 6 8c0 1 .2 2.2 1.5 3.5.7.7 1.3 1.5 1.5 2.5",
      key: "1gvzjb"
    }
  ],
  ["path", { d: "M9 18h6", key: "x1upvd" }],
  ["path", { d: "M10 22h4", key: "ceow96" }]
], ll = Hn("lightbulb", dl);
const fl = [
  [
    "path",
    {
      d: "M14.106 5.553a2 2 0 0 0 1.788 0l3.659-1.83A1 1 0 0 1 21 4.619v12.764a1 1 0 0 1-.553.894l-4.553 2.277a2 2 0 0 1-1.788 0l-4.212-2.106a2 2 0 0 0-1.788 0l-3.659 1.83A1 1 0 0 1 3 19.381V6.618a1 1 0 0 1 .553-.894l4.553-2.277a2 2 0 0 1 1.788 0z",
      key: "169xi5"
    }
  ],
  ["path", { d: "M15 5.764v15", key: "1pn4in" }],
  ["path", { d: "M9 3.236v15", key: "1uimfh" }]
], pl = Hn("map", fl);
const ml = [
  [
    "path",
    {
      d: "M4.037 4.688a.495.495 0 0 1 .651-.651l16 6.5a.5.5 0 0 1-.063.947l-6.124 1.58a2 2 0 0 0-1.438 1.435l-1.579 6.126a.5.5 0 0 1-.947.063z",
      key: "edeuup"
    }
  ]
], Ma = Hn("mouse-pointer-2", ml);
function Pa(e) {
  return {
    asset_cate_id: Number(e.assetCateId || 0),
    next_node_no: Math.max(1, Number(e.nextNodeNo || 1)),
    nodes: e.nodes.map(gl),
    edges: e.edges.map((t) => ({
      id: t.id,
      from: t.from,
      to: t.to,
      ...t.logicalFrom ? { logical_from: t.logicalFrom } : {},
      ...t.logicalTo ? { logical_to: t.logicalTo } : {},
      ...t.executionMode === "manual" ? { execution_mode: "manual" } : {},
      ...t.mediaUsage ? { media_usage: t.mediaUsage } : {}
    })),
    viewport: {
      ...e.viewport.x == null ? {} : { x: e.viewport.x },
      ...e.viewport.y == null ? {} : { y: e.viewport.y },
      ...e.viewport.zoom == null ? {} : { zoom: e.viewport.zoom }
    }
  };
}
function gl(e) {
  const t = {
    id: e.id,
    type: e.type,
    title: e.title,
    subtitle: e.subtitle,
    description: e.description,
    x: e.x,
    y: e.y,
    width: e.width,
    height: e.height
  };
  if (Kr(t, "node_no", e.nodeNo), e.titleMode && (t.title_mode = e.titleMode), Mt(t, "group_id", e.groupId), e.group) {
    const i = {};
    Mt(i, "origin", e.group.origin), Mt(i, "source_node_id", e.group.sourceNodeId), Mt(i, "sync_key", e.group.syncKey), Mt(i, "layout_key", e.group.layoutKey), Object.keys(i).length > 0 && (t.group = i);
  }
  if (e.storyboardItem) {
    const i = e.storyboardItem;
    t.storyboard_item = {
      source_node_id: i.sourceNodeId,
      item_type: i.itemType,
      item_id: i.itemId,
      ...i.generatedPrompt ? { generated_prompt: i.generatedPrompt } : {},
      dependency_node_ids: i.dependencyNodeIds || [],
      reference_node_ids: i.referenceNodeIds || [],
      external_reference_asset_ids: i.externalReferenceAssetIds || [],
      ...i.shotId ? { shot_id: i.shotId } : {},
      ...i.speechId ? { speech_id: i.speechId } : {},
      ...i.speechIds?.length ? { speech_ids: i.speechIds } : {},
      ...i.characterId ? { character_id: i.characterId } : {},
      ...i.speechKind ? { speech_kind: i.speechKind } : {},
      ...i.speakerMode ? { speaker_mode: i.speakerMode } : {},
      ...Number.isFinite(i.startTime) ? { start_time: i.startTime } : {},
      ...Number.isFinite(i.shotDuration) ? { shot_duration: i.shotDuration } : {},
      ...i.continuityAnchor ? { continuity_anchor: i.continuityAnchor } : {},
      ...i.optional ? { optional: !0 } : {},
      ...i.sourceSignature ? { source_signature: i.sourceSignature } : {},
      ...i.resultSourceSignature ? { result_source_signature: i.resultSourceSignature } : {},
      ...i.stale ? { stale: !0 } : {}
    };
  }
  Mt(
    t,
    "storyboard_materialized_signature",
    e.storyboardMaterializedSignature
  ), Kr(t, "asset_cate_id", e.assetCateId), Mt(t, "kind", e.kind), Mt(t, "output_type", e.outputType), Mt(t, "cardinality", e.cardinality), Kr(t, "count", e.count), e.flow && (t.flow = {
    id: e.flow.id,
    key: e.flow.key,
    name: e.flow.name,
    goal: e.flow.goal
  }), e.role && (t.role = {
    id: e.role.id,
    name: e.role.name,
    role_type: e.role.role_type,
    agent_id: e.role.agent_id
  }), e.asset && (t.asset = {
    id: e.asset.id,
    name: e.asset.name,
    kind: e.asset.kind,
    role: e.asset.role,
    asset_cate_id: e.asset.asset_cate_id,
    version_id: e.asset.version_id
  }), e.power && (t.power = {
    id: e.power.id,
    key: e.power.key,
    name: e.power.name,
    kind: e.power.kind,
    icon: e.power.icon,
    output_type: e.power.outputType,
    output: wl(e.power.output)
  }), e.functionOption && (t.function_option = {
    key: e.functionOption.key,
    label: e.functionOption.label,
    description: e.functionOption.description
  });
  const n = hl(e.composerDraft);
  n && (t.composer_draft = n);
  const r = Il(e.resultRef);
  r && (t.result_ref = r), !(Number(r?.asset_id || 0) > 0 && Number(r?.version_id || 0) > 0) && e.resultOutput != null && cn(e.resultOutput) && (t.result_output = e.resultOutput);
  const s = yl(e.resultView);
  return s && (t.result_view = s), Mt(t, "run_error", e.runError), e.local != null && (t.local = e.local), t;
}
function yl(e) {
  if (!e)
    return;
  const t = Fr(e.width), n = Fr(e.height);
  if (t == null || n == null || t <= 0 || n <= 0)
    return;
  const r = { width: t, height: n }, o = Fr(e.offsetX), s = Fr(e.offsetY);
  return o != null && (r.offset_x = o), s != null && (r.offset_y = s), r;
}
function hl(e) {
  if (!Yn(e))
    return null;
  const t = {};
  Mt(t, "prompt", e.prompt);
  const n = bl(e.promptContent);
  n && cn(n) && (t.prompt_content = n), Kr(t, "selected_target_id", e.selectedTargetId);
  const r = Nl(e.paramValues);
  r && (t.param_values = r);
  const o = md(e.videoComposition);
  o && cn(o) && (t.video_composition = o);
  const s = gd(
    e.storyboardReferences
  );
  return s.length > 0 && cn(s) && (t.storyboard_references = s), Object.keys(t).length ? t : null;
}
function wl(e) {
  if (e)
    return {
      key: e.key,
      name: e.name,
      allowed_kinds: e.allowedKinds,
      view_mode: e.viewMode,
      default_width: e.defaultWidth,
      default_height: e.defaultHeight,
      structured: e.structured,
      sort: e.sort
    };
}
function bl(e) {
  if (!(!Yn(e) || Number(e.version || 0) !== 1 || !Array.isArray(e.parts)))
    return e;
}
function Nl(e) {
  if (!Yn(e))
    return null;
  const t = {};
  for (const [n, r] of Object.entries(e))
    _l(r) || cn(r) && (t[n] = r);
  return Object.keys(t).length ? t : null;
}
function _l(e) {
  return Yn(e) ? !!(e.file || e.blob || e.preview || e.progress != null || e.uploading != null) : !1;
}
function Il(e) {
  if (!Yn(e))
    return null;
  const t = {};
  for (const n of [
    "run_id",
    "request_id",
    "flow_run_id",
    "node_run_id",
    "asset_id",
    "version_id",
    "release_id",
    "role",
    "status",
    "updated_at"
  ]) {
    const r = e[n];
    r != null && cn(r) && (t[n] = r);
  }
  return Object.keys(t).length ? t : null;
}
function Mt(e, t, n) {
  typeof n == "string" && n.trim() && (e[t] = n);
}
function Kr(e, t, n) {
  const r = Number(n || 0);
  r > 0 && (e[t] = r);
}
function Fr(e) {
  if (e == null || e === "")
    return;
  const t = Number(e);
  return Number.isFinite(t) ? t : void 0;
}
function Yn(e) {
  return !!(e && typeof e == "object" && !Array.isArray(e));
}
function cn(e) {
  return e == null || ["string", "number", "boolean"].includes(typeof e) ? !0 : Array.isArray(e) ? e.every(cn) : Yn(e) ? Object.values(e).every(cn) : !1;
}
async function Sl(e, t = 0) {
  const n = await Me(xe("workspace/bootstrap"), "get", {
    project_id: e,
    asset_cate_id: t
  });
  if (!Oe(n))
    throw new Error(n.message || n.msg || "加载创作空间失败");
  return yd(n.data);
}
async function vl(e) {
  const t = await Me(xe("workspace/canvas"), "get", {
    project_id: e.projectId,
    asset_cate_id: e.assetCateId
  });
  if (!Oe(t))
    throw new Error(t.message || t.msg || "加载分类画布失败");
  const n = t.data || {}, r = n.assets || {}, o = Array.isArray(r.items) ? r.items : Array.isArray(r) ? r : [];
  return {
    canvas: wd(n.canvas, e.assetCateId),
    assets: o.map(Ut)
  };
}
async function Cl(e) {
  const t = await Me(xe("project/canvas_config"), "get", {
    project_id: e
  });
  if (!Oe(t))
    throw new Error(t.message || t.msg || "加载能力列表失败");
  return hd(t.data);
}
async function Ho(e) {
  const t = await Me(xe("project/canvas_power_form"), "get", {
    project_id: e.projectId,
    flow_id: e.flowId || 0,
    power_id: e.powerId,
    power_key: e.powerKey,
    target_id: e.targetId || 0
  });
  if (!Oe(t))
    throw new Error(t.message || t.msg || "加载能力参数失败");
  return Fl(t.data);
}
async function Rl(e) {
  const t = await Me(xe("workspace/canvas_execute"), "post", {
    project_id: e.projectId,
    asset_cate_id: e.assetCateId,
    start_node_id: e.startNodeId,
    request_id: e.requestId || "",
    single_node: !!e.singleNode,
    execution_scope: e.executionScope || "",
    canvas: Pa(e.canvas),
    input: e.runInput || {}
  });
  return Da(t, "画布运行失败");
}
async function kl(e) {
  const t = await Me(
    xe("workspace/canvas_node_title"),
    "post",
    {
      project_id: e.projectId,
      node_key: e.nodeKey,
      version_id: e.versionId,
      prompt: e.prompt || ""
    }
  ), n = Da(t, "生成节点标题失败");
  return {
    nodeKey: String(n.node_key || e.nodeKey),
    versionId: Number(n.version_id || e.versionId || 0),
    title: String(n.title || "").trim()
  };
}
function Da(e, t) {
  if (!Oe(e))
    throw new Error(e?.message || e?.msg || t);
  return e?.data && typeof e.data == "object" ? e.data : {};
}
async function xo(e) {
  const t = await Me(
    xe("workspace/canvas_execution_list"),
    "get",
    {
      project_id: e.projectId,
      scope: e.scope,
      asset_cate_id: e.assetCateId || 0,
      run_ids: (e.runIds || []).filter((r) => r > 0).join(","),
      before_id: e.beforeId || 0,
      limit: e.limit || 20,
      summary_only: e.summaryOnly ? 1 : 0
    }
  );
  if (!Oe(t))
    throw new Error(t.message || t.msg || "读取画布运行记录失败");
  const n = t.data || {};
  return {
    count: Number(n.count || 0),
    items: Array.isArray(n.items) ? n.items : [],
    hasMore: !!n.has_more,
    beforeId: Number(n.before_id || 0)
  };
}
async function Ea(e) {
  const t = Number(e.executionId || 0), n = String(e.requestId || "").trim(), r = Number(e.runId || 0), o = await Me(
    xe("workspace/canvas_execution"),
    "get",
    {
      project_id: e.projectId,
      execution_id: t,
      request_id: t > 0 ? "" : n,
      run_id: t > 0 || n ? 0 : r
    }
  );
  if (!Oe(o))
    throw new Error(o.message || o.msg || "读取画布运行详情失败");
  return o.data || {};
}
async function xl(e) {
  const t = await Me(xe("run/approval"), "post", {
    project_id: e.projectId,
    run_id: e.runId,
    request_id: e.requestId,
    node_key: e.nodeKey,
    approval_id: e.approvalId || 0,
    data: e.feedback || {},
    decision: e.decision || "approved",
    comment: e.comment || ""
  });
  if (!Oe(t))
    throw new Error(t.message || t.msg || "继续画布运行失败");
  return t.data;
}
async function Al(e) {
  const t = await Me(xe("run/status"), "get", {
    project_id: e.projectId,
    run_id: e.runId || 0,
    request_id: e.requestId || "",
    view: "summary"
  });
  if (!Oe(t))
    throw new Error(t.message || t.msg || "读取流程状态失败");
  return t.data;
}
async function Tl(e) {
  const t = await Me(xe("run/interaction"), "post", {
    project_id: e.projectId,
    run_id: e.runId,
    node_run_id: e.nodeRunId || 0,
    interaction_id: e.interactionId,
    data: e.data
  });
  if (!Oe(t))
    throw new Error(t.message || t.msg || "提交信息失败");
  return t.data;
}
async function mw(e) {
  const t = await Me(
    xe("project/update_asset_version"),
    "post",
    {
      project_id: e.projectId,
      asset_id: e.assetId,
      version_id: e.versionId,
      content: e.content
    }
  );
  if (!Oe(t))
    throw new Error(t.message || t.msg || "保存资产版本失败");
  const n = t.data?.asset;
  if (!n)
    throw new Error("资产版本保存结果为空");
  return Ut(n);
}
async function gw(e) {
  const t = await Me(
    xe("project/restore_asset_version"),
    "post",
    {
      project_id: e.projectId,
      asset_id: e.assetId,
      version_id: e.versionId,
      request_id: e.requestId,
      node_key: e.nodeKey
    }
  );
  if (!Oe(t))
    throw new Error(t.message || t.msg || "恢复资产版本失败");
  const n = t.data?.asset;
  if (!n)
    throw new Error("资产版本恢复结果为空");
  return Ut(n);
}
async function yw(e) {
  const t = await Me(
    xe("project/confirm_storyboard"),
    "post",
    {
      project_id: e.projectId,
      asset_id: e.assetId,
      version_id: e.versionId,
      production_plan: e.productionPlan
    }
  );
  if (!Oe(t))
    throw new Error(t.message || t.msg || "确认分镜失败");
  const n = t.data?.asset;
  if (!n)
    throw new Error("确认分镜结果为空");
  return Ut(n);
}
async function hw(e) {
  const t = await Me(
    xe("project/create_storyboard_revision"),
    "post",
    {
      project_id: e.projectId,
      asset_id: e.assetId,
      version_id: e.versionId,
      request_id: e.requestId,
      node_key: e.nodeKey
    }
  );
  if (!Oe(t))
    throw new Error(t.message || t.msg || "创建分镜修订稿失败");
  const n = t.data?.asset;
  if (!n)
    throw new Error("创建分镜修订稿结果为空");
  return Ut(n);
}
async function ww(e) {
  const t = await Me(xe("project/asset_detail"), "get", {
    project_id: e.projectId,
    asset_id: e.assetId,
    current_only: e.currentOnly ? 1 : 0
  });
  if (!Oe(t))
    throw new Error(t.message || t.msg || "读取资产详情失败");
  const n = t.data?.asset;
  if (!n)
    throw new Error("资产详情为空");
  const r = t.data || {}, o = ba(r.versions);
  return {
    asset: Ut(n),
    versions: o,
    versionTotal: Number(r.version_total || o.length),
    hasMore: !!r.has_more
  };
}
async function bw(e) {
  const t = await Me(xe("project/asset_versions"), "get", {
    project_id: e.projectId,
    asset_id: e.assetId,
    page: e.page,
    page_size: e.pageSize || 20
  });
  if (!Oe(t))
    throw new Error(t.message || t.msg || "读取资产版本失败");
  const n = t.data || {}, r = ba(n.items);
  return {
    items: r,
    page: Number(n.page || e.page || 1),
    pageSize: Number(n.page_size || e.pageSize || 20),
    total: Number(n.total || r.length),
    hasMore: !!n.has_more
  };
}
async function Nw(e) {
  const t = await Me(
    xe("project/asset_version_detail"),
    "get",
    {
      project_id: e.projectId,
      asset_id: e.assetId,
      version_id: e.versionId
    }
  );
  if (!Oe(t))
    throw new Error(t.message || t.msg || "读取历史版本失败");
  const n = t.data?.version, r = bd(
    n && typeof n == "object" && !Array.isArray(n) ? n : {}
  );
  if (!r?.id)
    throw new Error("历史版本内容为空");
  return r;
}
function Ml(e) {
  const t = {
    project_id: e.projectId,
    asset_cate_id: e.assetCateId,
    name: e.name,
    kind: e.kind,
    content: e.content,
    request_id: e.requestId || ""
  };
  if (e.runId && (t.run_id = e.runId), e.nodeRunId && (t.node_run_id = e.nodeRunId), e.releaseId && (t.release_id = e.releaseId), e.nodeKey && (t.node_key = e.nodeKey), e.source) {
    const n = e.source;
    n.sourceKey && (t.source_key = n.sourceKey), n.sourceRunId && (t.source_run_id = n.sourceRunId), n.sourceNodeRunId && (t.source_node_run_id = n.sourceNodeRunId), n.sourceAssetId && (t.source_asset_id = n.sourceAssetId), n.sourceVersionId && (t.source_version_id = n.sourceVersionId), n.sourceReleaseId && (t.source_release_id = n.sourceReleaseId), n.sourceRequestId && (t.source_request_id = n.sourceRequestId), n.sourceNodeKey && (t.source_node_key = n.sourceNodeKey), n.sourceNodeType && (t.source_node_type = n.sourceNodeType), n.sourceStatus && (t.source_status = n.sourceStatus);
  }
  return t;
}
async function Pl(e, t) {
  const n = await Me(xe("project/save_asset"), "post", {
    ...Ml(t),
    role: e
  });
  if (!Oe(n))
    throw new Error(n.message || n.msg || "保存资产失败");
  const r = n.data?.asset;
  if (!r)
    throw new Error("保存资产结果为空");
  return Ut(r);
}
function Dl(e) {
  return Pl("work", e);
}
async function El(e, t, n) {
  const r = await Me(xe("workspace/canvas"), "post", {
    project_id: e,
    asset_cate_id: t,
    base_revision: n.updatedAt || "",
    canvas: Pa(n)
  });
  if (!Oe(r))
    throw new Error(r.message || r.msg || "保存画布失败");
  const o = r.data || {};
  return {
    assetCateId: Number(o.asset_cate_id || t || 0),
    updatedAt: String(o.updated_at || n.updatedAt || "")
  };
}
function Fl(e) {
  const t = e && typeof e == "object" ? e : {};
  return {
    ...t,
    sources: Array.isArray(t.sources) ? t.sources : [],
    params: Array.isArray(t.params) ? t.params : [],
    selected_target_id: Number(t.selected_target_id || 0),
    source_rule: Number(t.source_rule || 0),
    primary_param_key: String(t.primary_param_key || "")
  };
}
const ai = 520, zl = 8e3;
function Ol({
  projectId: e,
  enabled: t,
  canvases: n,
  setCanvases: r,
  onError: o
}) {
  const s = te(n), i = te({}), a = te({}), d = te({}), u = te(/* @__PURE__ */ new Set()), l = te({}), f = te(0), b = te(null), [_, w] = $(0), [D, A] = $({});
  le(() => {
    s.current = n;
  }, [n]);
  const z = P((C) => {
    const T = l.current[C];
    T != null && (window.clearTimeout(T), delete l.current[C]);
  }, []), V = P(
    (C, T = ai) => {
      if (!t || !e || typeof window > "u")
        return;
      z(C);
      const O = f.current;
      l.current[C] = window.setTimeout(() => {
        delete l.current[C], b.current?.(C, O);
      }, T);
    },
    [z, t, e]
  ), R = P(
    async (C, T) => {
      if (T !== f.current || !t || !e || u.current.has(C))
        return;
      const O = i.current[C] || 0;
      if (O <= (a.current[C] || 0))
        return;
      const H = s.current[C];
      if (H) {
        u.current.add(C), A((re) => ({ ...re, [C]: "saving" }));
        try {
          const re = await El(
            e,
            H.assetCateId,
            H
          );
          if (T !== f.current)
            return;
          a.current[C] = Math.max(
            a.current[C] || 0,
            O
          ), d.current[C] = 0, (i.current[C] || 0) === O ? (r(
            (oe) => oe[C] === H ? {
              ...oe,
              [C]: {
                ...H,
                updatedAt: re.updatedAt || H.updatedAt
              }
            } : oe
          ), A((oe) => ({ ...oe, [C]: "saved" }))) : A((oe) => ({ ...oe, [C]: "dirty" }));
        } catch (re) {
          if (T !== f.current)
            return;
          const X = (d.current[C] || 0) + 1;
          d.current[C] = X, A((oe) => ({ ...oe, [C]: "error" })), X === 1 && o(re), V(
            C,
            Math.min(zl, ai * 2 ** X)
          );
        } finally {
          u.current.delete(C), T === f.current && (i.current[C] || 0) > (a.current[C] || 0) && (d.current[C] || 0) === 0 && V(C);
        }
      }
    },
    [t, o, e, V, r]
  );
  b.current = R;
  const G = P((C) => {
    const T = String(C);
    i.current[T] = (i.current[T] || 0) + 1, d.current[T] = 0, A((O) => ({ ...O, [T]: "dirty" })), w((O) => O + 1);
  }, []), ee = P(
    (C) => {
      f.current += 1;
      for (const O of Object.keys(l.current))
        z(O);
      i.current = {}, a.current = {}, d.current = {}, u.current.clear();
      const T = {};
      for (const O of Object.keys(C))
        T[O] = "saved";
      A(T);
    },
    [z]
  );
  return le(() => {
    for (const [C, T] of Object.entries(i.current))
      T > (a.current[C] || 0) && V(C);
  }, [V, _]), le(
    () => () => {
      f.current += 1;
      for (const C of Object.values(l.current))
        window.clearTimeout(C);
      l.current = {};
    },
    []
  ), {
    markCanvasDirty: G,
    resetCanvasAutosave: ee,
    canvasSaveStatus: D
  };
}
const $l = 6e4, jl = 60;
class Bl {
  scopeKey = "";
  catalogs = /* @__PURE__ */ new Map();
  powerForms = /* @__PURE__ */ new Map();
  setScope(t, n) {
    const r = Ul(t, n);
    this.scopeKey !== r && (this.scopeKey = r, this.catalogs.clear(), this.powerForms.clear());
  }
  loadCatalog(t, n, r, o = !1) {
    this.setScope(t, n);
    const s = this.scopeKey, i = this.catalogs.get(s) || { loadedAt: 0 };
    if (!o && i.value && Date.now() - i.loadedAt < $l)
      return Promise.resolve(i.value);
    if (i.inFlight)
      return i.inFlight;
    const a = r().then((d) => (this.scopeKey === s && this.catalogs.set(s, { value: d, loadedAt: Date.now() }), d)).catch((d) => {
      throw this.scopeKey === s && this.catalogs.set(s, {
        value: i.value,
        loadedAt: i.loadedAt
      }), d;
    });
    return this.catalogs.set(s, { ...i, inFlight: a }), a;
  }
  loadPowerForm(t, n) {
    this.setScope(t.projectId, t.releaseId);
    const r = this.scopeKey, o = Vl(t), s = this.powerForms.get(o);
    if (s?.value)
      return this.touchPowerForm(o, s), Promise.resolve(s.value);
    if (s?.inFlight)
      return s.inFlight;
    const i = n().then((a) => (this.scopeKey === r && (this.touchPowerForm(o, { value: a }), this.trimPowerForms()), a)).catch((a) => {
      throw this.scopeKey === r && this.powerForms.delete(o), a;
    });
    return this.touchPowerForm(o, { inFlight: i }), this.trimPowerForms(), i;
  }
  touchPowerForm(t, n) {
    this.powerForms.delete(t), this.powerForms.set(t, n);
  }
  trimPowerForms() {
    for (; this.powerForms.size > jl; ) {
      const t = this.powerForms.keys().next().value;
      if (!t)
        return;
      this.powerForms.delete(t);
    }
  }
}
function Ul(e, t) {
  return `${e || 0}:${t || 0}`;
}
function Vl(e) {
  return [
    e.projectId || 0,
    e.releaseId || 0,
    e.flowId || 0,
    e.powerId || 0,
    e.powerKey || "",
    e.targetId || 0
  ].join(":");
}
function Kl(e, t, n) {
  const r = new Map(t.map((d) => [d.id, d])), o = Ll(n), s = /* @__PURE__ */ new Set(), i = /* @__PURE__ */ new Map();
  for (const d of t)
    d.groupId && i.set(d.groupId, [
      ...i.get(d.groupId) || [],
      d.id
    ]);
  const a = (d) => {
    for (const u of o.get(d) || []) {
      if (s.has(u))
        continue;
      s.add(u);
      const l = r.get(u);
      if (l?.type === "group")
        for (const f of i.get(l.id) || [])
          s.has(f) || (s.add(f), a(f));
      (!l || !Fa(l)) && a(u);
    }
  };
  return a(e), [...s];
}
function Fa(e) {
  return e.type === "function" && (e.functionOption?.key === "save" || e.functionOption?.key === "display");
}
function po(e) {
  return e.type === "power" ? !!(Number(e.power?.id || 0) > 0 || e.power?.key) : ["asset", "agent", "flow"].includes(e.type) ? !0 : e.type === "function" && (e.functionOption?.key === "save" || e.functionOption?.key === "display");
}
function Ll(e) {
  const t = /* @__PURE__ */ new Map();
  for (const n of e) {
    if (!n.from || !n.to || n.executionMode === "manual")
      continue;
    const r = n.logicalFrom || n.from, o = n.logicalTo || n.to;
    t.set(r, [
      ...t.get(r) || [],
      o
    ]);
  }
  return t;
}
function za({
  targets: e,
  nodesByID: t,
  hasResult: n
}) {
  const r = new Set(e.map((o) => o.id));
  for (const o of e) {
    const s = o.storyboardItem;
    if (!s)
      continue;
    const i = /* @__PURE__ */ new Set([
      ...s.dependencyNodeIds || [],
      ...s.referenceNodeIds || []
    ]);
    for (const a of i) {
      if (r.has(a))
        continue;
      const d = t.get(a);
      if (!d)
        return "前置素材节点不存在，请重新同步分镜脚本";
      const u = d.title || "未命名素材";
      if (!n(d))
        return d.storyboardItem?.stale ? `请先重新生成前置素材“${u}”` : `请先生成前置素材“${u}”`;
    }
  }
  return "";
}
function ql({
  members: e,
  runningNodes: t,
  groupState: n,
  hasResult: r
}) {
  const o = e.filter(po), s = o.filter(
    (l) => l.storyboardItem?.stale
  ).length, i = o.map((l) => t[l.id]).filter((l) => !!l), d = n?.status === "running" || n?.status === "waiting" ? o.filter((l) => {
    const f = t[l.id];
    return f?.status === "success" || !f && r(l);
  }).length : o.filter((l) => {
    const f = t[l.id];
    return f?.status === "success" ? !0 : f ? !1 : r(l);
  }).length, u = i.filter(
    (l) => l.status === "error"
  ).length;
  return {
    memberCount: e.length,
    runnableCount: o.length,
    completedCount: d,
    failedCount: u,
    staleCount: s,
    status: Yl(n, i, u)
  };
}
async function Gl(e, t) {
  const n = new Map(
    e.filter(po).map((s) => [s.id, s])
  ), r = /* @__PURE__ */ new Set(), o = /* @__PURE__ */ new Map();
  for (; n.size > 0 && (Hl(n, o), n.size !== 0); ) {
    const s = [...n.values()].filter(
      (a) => Oa(a).every(
        (d) => !n.has(d) || r.has(d)
      )
    );
    if (s.length === 0) {
      for (const a of n.values())
        o.set(a.id, new Error("节点依赖关系存在循环"));
      n.clear();
      break;
    }
    (await Promise.allSettled(
      s.map(async (a) => (await t(a), a.id))
    )).forEach((a, d) => {
      const u = s[d];
      if (n.delete(u.id), a.status === "fulfilled") {
        r.add(u.id);
        return;
      }
      o.set(u.id, a.reason);
    });
  }
  if (o.size > 0) {
    const s = o.values().next().value, i = s instanceof Error ? s.message : "节点更新失败";
    throw new Error(`${o.size} 个节点更新失败：${i}`);
  }
}
function Hl(e, t) {
  let n = !0;
  for (; n; ) {
    n = !1;
    for (const r of e.values())
      Oa(r).find(
        (s) => t.has(s)
      ) && (e.delete(r.id), t.set(r.id, new Error("上游节点更新失败")), n = !0);
  }
}
function Oa(e) {
  return e.storyboardItem?.dependencyNodeIds || [];
}
function Yl(e, t, n) {
  return t.some((r) => r.status === "running") ? "running" : e?.status === "waiting" || t.some((r) => r.status === "waiting") ? "waiting" : e?.status === "error" || n > 0 ? "error" : e?.status === "running" ? "running" : "idle";
}
function Wl(e, t) {
  const [n, r] = $(e);
  return ku(() => {
    t || r(e);
  }, [e, t]), { flowNodes: n, setFlowNodes: r };
}
function Xl({
  point: e,
  canShowDetail: t,
  canCopy: n = !0,
  canDelete: r = !0,
  canEditStructure: o = !1,
  canResetStoryboardPrompt: s = !1,
  onClose: i,
  onCopy: a,
  onDelete: d,
  onDetail: u,
  onEditStructure: l,
  onResetStoryboardPrompt: f
}) {
  return /* @__PURE__ */ v(Qt, { children: [
    /* @__PURE__ */ c("div", { className: "ws-node-action-backdrop", onMouseDown: i }),
    /* @__PURE__ */ v(
      "section",
      {
        className: "ws-node-action-menu",
        style: { left: e.x, top: e.y },
        onMouseDown: (b) => b.stopPropagation(),
        children: [
          t ? /* @__PURE__ */ v("button", { type: "button", onClick: u, children: [
            /* @__PURE__ */ c(ur, { size: 15 }),
            /* @__PURE__ */ c("span", { children: "详情" })
          ] }) : null,
          o && l ? /* @__PURE__ */ v("button", { type: "button", onClick: l, children: [
            /* @__PURE__ */ c(el, { size: 15 }),
            /* @__PURE__ */ c("span", { children: "编辑分镜" })
          ] }) : null,
          s && f ? /* @__PURE__ */ v("button", { type: "button", onClick: f, children: [
            /* @__PURE__ */ c(tl, { size: 15 }),
            /* @__PURE__ */ c("span", { children: "恢复脚本提示词" })
          ] }) : null,
          n ? /* @__PURE__ */ v("button", { type: "button", onClick: a, children: [
            /* @__PURE__ */ c(Qd, { size: 15 }),
            /* @__PURE__ */ c("span", { children: "复制" })
          ] }) : null,
          r ? /* @__PURE__ */ v("button", { type: "button", className: "is-danger", onClick: d, children: [
            /* @__PURE__ */ c(nl, { size: 15 }),
            /* @__PURE__ */ c("span", { children: "删除" })
          ] }) : null
        ]
      }
    )
  ] });
}
function Jl({
  showMiniMap: e,
  snapToGrid: t,
  zoom: n,
  onToggleMiniMap: r,
  onToggleSnap: o,
  onReset: s,
  onZoomIn: i,
  onZoomOut: a,
  onZoomChange: d
}) {
  return /* @__PURE__ */ v("div", { className: "ws-view-controls nodrag nopan", children: [
    /* @__PURE__ */ c(Je, { label: e ? "隐藏小地图" : "显示小地图", children: /* @__PURE__ */ c(
      "button",
      {
        type: "button",
        className: e ? "is-active" : "",
        onClick: r,
        "aria-label": e ? "隐藏小地图" : "显示小地图",
        children: /* @__PURE__ */ c(pl, { size: 16 })
      }
    ) }),
    /* @__PURE__ */ c(Je, { label: t ? "关闭网格吸附" : "开启网格吸附", children: /* @__PURE__ */ c(
      "button",
      {
        type: "button",
        className: t ? "is-active" : "",
        onClick: o,
        "aria-label": t ? "关闭网格吸附" : "开启网格吸附",
        children: /* @__PURE__ */ c(Ma, { size: 16 })
      }
    ) }),
    /* @__PURE__ */ c(Je, { label: "重置视图", children: /* @__PURE__ */ c("button", { type: "button", onClick: s, "aria-label": "重置视图", children: /* @__PURE__ */ c(Ju, { size: 15 }) }) }),
    /* @__PURE__ */ v("div", { className: "ws-view-zoom", children: [
      /* @__PURE__ */ c(Je, { label: "缩小", children: /* @__PURE__ */ c("button", { type: "button", onClick: a, "aria-label": "缩小", children: /* @__PURE__ */ c(fa, { size: 15 }) }) }),
      /* @__PURE__ */ c(
        "input",
        {
          type: "range",
          min: "0.35",
          max: "1.45",
          step: "0.01",
          value: Math.max(0.35, Math.min(1.45, n)),
          onChange: (u) => d(Number(u.target.value)),
          "aria-label": "画布缩放"
        }
      ),
      /* @__PURE__ */ c(Je, { label: "放大", children: /* @__PURE__ */ c("button", { type: "button", onClick: i, "aria-label": "放大", children: /* @__PURE__ */ c(hs, { size: 15 }) }) })
    ] })
  ] });
}
const $a = [
  {
    position: "top-left",
    className: "is-top-left",
    horizontalDirection: -1,
    verticalDirection: -1,
    top: !0,
    left: !0
  },
  {
    position: "top-right",
    className: "is-top-right",
    horizontalDirection: 1,
    verticalDirection: -1,
    top: !0,
    left: !1
  },
  {
    position: "bottom-left",
    className: "is-bottom-left",
    horizontalDirection: -1,
    verticalDirection: 1,
    top: !1,
    left: !0
  },
  {
    position: "bottom-right",
    className: "is-bottom-right",
    horizontalDirection: 1,
    verticalDirection: 1,
    top: !1,
    left: !1
  }
], Hr = 140, _s = 100, ir = 720, ci = { width: 280, height: 64 };
function Zl(e, t, n) {
  const r = Ba(n), o = e.find((s) => s.id === t);
  return !o || rf(o, r) ? e : e.map(
    (s) => s.id === t ? { ...s, ...r } : s
  );
}
function Ql(e, t, n) {
  const r = lr(n), o = e.find((s) => s.id === t);
  return !o || of(o.resultView, r) ? e : e.map(
    (s) => s.id === t ? { ...s, resultView: r } : s
  );
}
function ef({
  node: e,
  enabled: t,
  resizable: n,
  onResizeStart: r,
  onResizeEnd: o
}) {
  if (!t || !n || !o)
    return null;
  const s = e.type === "group", i = e.type === "power" && Na(e.power, e.kind);
  return /* @__PURE__ */ c(Qt, { children: $a.map((a) => /* @__PURE__ */ c(
    Mu,
    {
      position: a.position,
      className: `ws-resize-control ws-node-resize-control ${a.className} nodrag nopan`,
      minWidth: s ? si.width : i ? ci.width : Hr,
      minHeight: s ? si.height : i ? ci.height : _s,
      maxWidth: s ? oi.width : ir,
      maxHeight: s ? oi.height : ir,
      keepAspectRatio: !s && !i,
      onResizeStart: () => r?.(e.id),
      onResizeEnd: (d, u) => o(e.id, Ba(u))
    },
    a.position
  )) });
}
function tf({
  value: e,
  enabled: t,
  onResizeStart: n,
  onResize: r,
  onResizeEnd: o
}) {
  const s = te(null);
  if (!t)
    return null;
  const i = (u, l) => {
    if (u.button !== 0)
      return;
    u.preventDefault(), u.stopPropagation();
    const f = lr(e), b = u.currentTarget.parentElement?.getBoundingClientRect().width || f.width;
    s.current = {
      pointerId: u.pointerId,
      startX: u.clientX,
      startY: u.clientY,
      startView: f,
      currentView: f,
      corner: l,
      scale: Math.max(0.01, b / f.width)
    }, u.currentTarget.setPointerCapture(u.pointerId), n?.();
  }, a = (u) => {
    const l = s.current;
    if (!l || l.pointerId !== u.pointerId)
      return;
    u.preventDefault(), u.stopPropagation();
    const f = nf(
      l.startView,
      l.corner,
      (u.clientX - l.startX) / l.scale,
      (u.clientY - l.startY) / l.scale
    );
    l.currentView = f, r(f);
  }, d = (u) => {
    const l = s.current;
    !l || l.pointerId !== u.pointerId || (u.preventDefault(), u.stopPropagation(), s.current = null, u.currentTarget.hasPointerCapture(u.pointerId) && u.currentTarget.releasePointerCapture(u.pointerId), o(lr(l.currentView)));
  };
  return /* @__PURE__ */ c(Qt, { children: $a.map((u) => /* @__PURE__ */ c(
    "div",
    {
      className: `ws-resize-control ws-floating-resize-control ${u.className} nodrag nopan nowheel`,
      onPointerDown: (l) => i(l, u),
      onPointerMove: a,
      onPointerUp: d,
      onPointerCancel: d,
      onClick: (l) => {
        l.preventDefault(), l.stopPropagation();
      }
    },
    u.position
  )) });
}
function nf(e, t, n, r) {
  const o = e.width / e.height, s = e.width + t.horizontalDirection * n, i = (e.height + t.verticalDirection * r) * o, a = ja(
    Math.abs(s - e.width) >= Math.abs(i - e.width) ? s : i,
    o
  ), d = a / o, u = Number(e.offsetX || 0), l = Number(e.offsetY || 0);
  return lr({
    width: a,
    height: d,
    offsetX: t.left ? u + e.width - a : u,
    offsetY: t.top ? l + (e.height - d) / 2 : l + (d - e.height) / 2
  });
}
function ja(e, t) {
  const n = Math.max(Hr, _s * t), r = Math.min(ir, ir * t);
  return n > r ? Math.min(ir, Math.max(Hr, e)) : Math.min(r, Math.max(n, e));
}
function lr(e) {
  const t = ui(e.width, Hr), n = ui(e.height, _s), r = t / n, o = ja(t, r);
  return {
    width: Math.round(o),
    height: Math.round(o / r),
    offsetX: Math.round(di(e.offsetX, 0)),
    offsetY: Math.round(di(e.offsetY, 0))
  };
}
function ui(e, t) {
  const n = Number(e);
  return Number.isFinite(n) && n > 0 ? n : t;
}
function di(e, t) {
  const n = Number(e);
  return Number.isFinite(n) ? n : t;
}
function Ba(e) {
  return {
    x: Math.round(e.x),
    y: Math.round(e.y),
    width: Math.round(e.width),
    height: Math.round(e.height)
  };
}
function rf(e, t) {
  return e.x === t.x && e.y === t.y && e.width === t.width && e.height === t.height;
}
function of(e, t) {
  return e?.width === t.width && e?.height === t.height && Number(e?.offsetX || 0) === Number(t.offsetX || 0) && Number(e?.offsetY || 0) === Number(t.offsetY || 0);
}
function sf(e, t) {
  const n = e || Ua(), r = ed({
    type: "stream",
    output: t
  }), o = r.event, s = r.activity, i = df(n.text, r.delta, t, o), a = lf(o, s) ? {
    ...n.output,
    ...r.output,
    ...i ? { text: i } : {}
  } : n.output, d = s && !s.anchorText ? { ...s, anchorText: i } : s, u = d ? td(n.activities, d) : n.activities, l = nd(
    rd(n.document, r.output),
    ga(r.output.document)
  ), f = ma(a) || n.interaction, b = pa(a);
  return {
    started: !0,
    text: i,
    output: a,
    activities: u,
    document: l,
    interaction: f,
    suggestions: b.length > 0 ? b : n.suggestions,
    error: r.error || n.error
  };
}
function af(e) {
  const t = cf(e);
  return {
    started: Object.keys(t).length > 0,
    text: Yr(t.text),
    output: t,
    activities: Zu(t),
    document: ga(t.document),
    interaction: ma(t),
    suggestions: pa(t),
    error: Yr(t.error)
  };
}
function cf(e) {
  const t = Qu(e);
  if (Yr(t.text))
    return t;
  const n = _a(e);
  if (!n)
    return t;
  const r = { ...t, text: n };
  return delete r.rich, r;
}
function uf(e) {
  return !!(e && (e.started || e.text || e.activities.length > 0 || e.document || e.interaction || e.suggestions.length > 0 || Object.keys(e.output).length > 0));
}
function Ua() {
  return {
    started: !1,
    text: "",
    output: {},
    activities: [],
    suggestions: [],
    error: ""
  };
}
function df(e, t, n, r) {
  return t ? `${e}${t}` : r === "final" && Yr(n.text) || e;
}
function lf(e, t) {
  return !(t || e === "start" || e === "delta");
}
function Yr(e) {
  return e == null ? "" : String(e);
}
function ff(e) {
  const t = Nf(e.result?.asset, e.result?.data?.asset);
  if (!t || Number(t.id || 0) <= 0 || !t.version?.id)
    return null;
  const r = e.previousAssets?.find(
    (i) => i.id === Number(t.id || 0)
  ), o = r ? Rn(r, e.previousAsset) : e.previousAsset || null, s = Rn(
    t,
    o
  );
  return o?.version?.content != null && Number(o.version.id || 0) === Number(s.version?.id || 0) ? Rn(o, s) : s.version?.id ? s : null;
}
function pf(e, t) {
  const n = gf(e);
  return t ? {
    ...n,
    asset: t
  } : n;
}
function mf(e, t = "") {
  return String(e.kind || e.power?.kind || t || "richtext");
}
function Rn(e, t) {
  const n = fi(e), r = t ? fi(t) : null, o = [
    ...n.versions || [],
    ...r?.versions || []
  ], s = yf(
    [wf(n.version)].filter(
      (i) => !!i
    ),
    o
  );
  return {
    ...r,
    ...n,
    version: n.version || r?.version,
    versions: s.length ? s : void 0
  };
}
function li(e, t) {
  if (t.length === 0)
    return e;
  const n = new Map(t.map((o) => [o.id, o])), r = e.map((o) => {
    const s = n.get(o.id);
    return s ? (n.delete(o.id), Rn(s, o)) : o;
  });
  return [
    ...[...n.values()].map(
      (o) => Rn(o)
    ),
    ...r
  ];
}
function gf(e) {
  if (!e || typeof e != "object" || Array.isArray(e))
    return e;
  const { version: t, ...n } = e;
  return n;
}
function fi(e) {
  const t = pi(e.version), n = (e.versions || []).map(pi).filter((r) => !!r);
  return {
    ...e,
    version: t,
    versions: n.length ? n : e.versions
  };
}
function pi(e) {
  if (!e)
    return;
  const t = Ia(e.content);
  return t ? {
    ...e,
    content: { rich: t }
  } : e;
}
function yf(...e) {
  const t = e.flat(), n = [], r = /* @__PURE__ */ new Map();
  for (const s of t) {
    if (!s || Number(s.id || 0) <= 0)
      continue;
    const i = String(s.id), a = r.get(i);
    if (a !== void 0) {
      n[a] = hf(
        n[a],
        s
      );
      continue;
    }
    r.set(i, n.length), n.push(s);
  }
  let o = !1;
  return n.sort(
    (s, i) => Number(Ao(i)) - Number(Ao(s)) || Number(i.version || i.id || 0) - Number(s.version || s.id || 0)
  ).map((s) => Ao(s) ? o ? bf(s) : (o = !0, s) : s);
}
function hf(e, t) {
  const n = { ...e };
  for (const [r, o] of Object.entries(t))
    o !== void 0 && o !== "" && (n[r] = o);
  return n;
}
function Ao(e) {
  return !!(e.is_current || e.current);
}
function wf(e) {
  return e ? { ...e, current: !0 } : void 0;
}
function bf(e) {
  const { current: t, is_current: n, ...r } = e;
  return r;
}
function Nf(...e) {
  return e.find((t) => t != null);
}
function Wr(e) {
  const t = e?.asset || e?.data?.asset, n = e?.version || t?.version || e?.data?.version, r = {};
  return _n(r, "execution_id", e?.execution_id), _n(r, "run_id", e?.run_id || n?.run_id), To(r, "request_id", e?.request_id), _n(r, "flow_run_id", e?.flow_run_id), _n(
    r,
    "node_run_id",
    e?.node_run_id || n?.node_run_id
  ), _n(r, "asset_id", t?.id), _n(r, "version_id", n?.id || t?.version_id), _n(
    r,
    "release_id",
    n?.release_id || e?.release_id
  ), To(r, "role", e?.role || t?.role), To(r, "status", e?.status), Object.keys(r).length > 0 && (r.updated_at = (/* @__PURE__ */ new Date()).toISOString()), Object.keys(r).length > 0 ? r : void 0;
}
function _f(e) {
  const t = e?.resultRef;
  if (!e || !t)
    return null;
  const n = Number(t.asset_id || 0), r = String(t.node_run_id ? e.nodeId : "").trim();
  return {
    sourceRunId: Number(t.run_id || 0),
    sourceNodeRunId: Number(t.node_run_id || 0),
    sourceAssetId: n,
    sourceVersionId: Number(t.version_id || 0),
    sourceReleaseId: Number(t.release_id || 0),
    sourceRequestId: String(t.request_id || ""),
    sourceNodeKey: r,
    sourceNodeType: String(e.type || ""),
    sourceStatus: String(t.status || ""),
    sourceKey: r || (n > 0 ? `asset:${n}` : "") || ""
  };
}
function _n(e, t, n) {
  const r = Number(n || 0);
  r > 0 && (e[t] = r);
}
function To(e, t, n) {
  typeof n == "string" && n.trim() && (e[t] = n.trim());
}
function If({
  className: e,
  children: t
}) {
  return /* @__PURE__ */ c("div", { className: `ws-workspace-overlay ${e || ""}`.trim(), children: t });
}
const Sf = [
  { key: "all", label: "全部" },
  { key: "text", label: "文本" },
  { key: "richtext", label: "富文本" },
  { key: "image", label: "图片" },
  { key: "audio", label: "音频" },
  { key: "video", label: "视频" },
  { key: "storyboard", label: "分镜" },
  { key: "agent", label: "智能体" },
  { key: "flow", label: "流程" }
], vf = Object.fromEntries(
  Sf.filter((e) => e.key !== "all").map((e) => [e.key, e.label])
);
function Cf(e) {
  return vf[e] || "文本";
}
function Rf(e) {
  const t = new Map(e.nodes.map((i) => [i.id, i])), n = new Map(
    e.nodes.filter((i) => i.type === "group").map((i) => [i.id, i])
  ), r = kf(
    e.assets,
    e.assetCateId
  ), o = e.nodes.filter(Nd).map((i) => {
    const a = r.get(i.id) || i.asset, d = i.groupId ? n.get(i.groupId) : void 0, u = d?.group?.sourceNodeId ? t.get(d.group.sourceNodeId) : void 0, l = e.nodeOutput(i);
    return {
      key: `node:${i.id}`,
      role: "material",
      title: i.title || Cf(mi(i)),
      sourcePath: u?.title && d?.title ? `${u.title} / ${d.title}` : d?.title,
      nodeType: mi(i),
      status: xf(
        i,
        a,
        e.nodeHasResult(i)
      ),
      preview: e.nodePreview(i),
      output: l,
      node: i,
      nodeId: i.id,
      nodeNo: i.nodeNo,
      groupId: i.groupId,
      groupTitle: d?.title,
      asset: a,
      assetId: a?.id,
      versionId: a?.version?.id || a?.version_id
    };
  }).filter((i) => !!(i.assetId && i.versionId));
  return [...e.assets.filter(
    (i) => Number(i.asset_cate_id || 0) === e.assetCateId && String(i.role || "material") === "work" && String(i.status || "") !== "archived"
  ).map(
    (i) => ({
      key: `asset:${i.id}`,
      role: "work",
      title: i.name || `作品 ${i.id}`,
      nodeType: Va(i.kind),
      status: i.version?.id || i.version_id ? "ready" : "empty",
      preview: e.assetPreview(i),
      output: i.version?.content,
      asset: i,
      assetId: i.id,
      versionId: i.version?.id || i.version_id
    })
  ), ...o].filter(
    (i) => !!(i.assetId && i.versionId)
  );
}
function mi(e) {
  if (e.type === "agent") return "agent";
  if (e.type === "flow") return "flow";
  const t = String(
    e.outputType || e.power?.outputType || e.power?.output?.key || ""
  ).toLowerCase(), n = String(e.power?.output?.viewMode || "").toLowerCase();
  return t === "storyboard" || n === "storyboard" ? "storyboard" : Va(e.power?.kind || e.kind);
}
function Va(e) {
  switch (String(e || "text").toLowerCase()) {
    case "rich":
    case "richtext":
      return "richtext";
    case "image":
      return "image";
    case "audio":
      return "audio";
    case "video":
      return "video";
    default:
      return "text";
  }
}
function kf(e, t) {
  const n = /* @__PURE__ */ new Map();
  for (const r of e) {
    if (Number(r.asset_cate_id || 0) !== t || String(r.role || "") !== "material" || String(r.status || "") === "archived")
      continue;
    const o = String(
      r.node_key || r.version?.node_key || ""
    ).trim();
    o && !n.has(o) && n.set(o, r);
  }
  return n;
}
function xf(e, t, n) {
  const r = String(
    e.running?.status || e.resultRef?.status || ""
  ).toLowerCase();
  return r === "running" || r === "waiting" || e.running === !0 ? "running" : r === "error" || r === "failed" || r === "failure" ? "failed" : n || (t?.version?.id || t?.version_id) ? "ready" : "empty";
}
function Wn(e) {
  const t = e?.output && typeof e.output == "object" ? e.output : {}, n = e?.run && typeof e.run == "object" ? e.run : {};
  return {
    execution_id: Number(e?.execution_id || 0),
    run_id: Number(e?.run_id || n.id || 0),
    request_id: String(e?.request_id || n.request_id || ""),
    asset_cate_id: Number(e?.asset_cate_id || 0),
    start_node_id: String(e?.start_node_id || ""),
    flow_run_id: Number(e?.flow_run_id || n.flow_run_id || 0),
    release_id: Number(e?.release_id || n.release_id || 0),
    status: wr(e?.status || n.status),
    error: ln(e?.error || n.error),
    executed: Number(e?.executed || e?.output?.executed || 0),
    total: Number(e?.total || e?.output?.total || 0),
    single_node: !!e?.single_node,
    created_at: String(e?.created_at || ""),
    updated_at: String(e?.updated_at || ""),
    title: String(e?.title || ""),
    output: e?.output || n.output,
    approvals: Array.isArray(e?.approvals) ? e.approvals : Array.isArray(e?.data?.approvals) ? e.data.approvals : [],
    interactions: Array.isArray(e?.interactions) ? e.interactions : Array.isArray(e?.data?.interactions) ? e.data.interactions : [],
    node_results: Array.isArray(e?.node_results || t.node_results) ? (e.node_results || t.node_results).map(gi).filter((r) => !!r) : [],
    pending_node: gi(
      e?.pending_node || t.pending_node
    ),
    execution_plan: Pf(e?.execution_plan),
    node_runs: Array.isArray(e?.node_runs) ? e.node_runs.map(Ff).filter((r) => !!r) : []
  };
}
function gi(e) {
  if (!e || typeof e != "object")
    return null;
  const t = String(e.node_key || "");
  return t ? {
    node_key: t,
    execution_id: Number(e.execution_id || 0),
    node_type: String(e.node_type || ""),
    node_run_id: Number(e.node_run_id || 0),
    run_id: Number(e.run_id || 0),
    request_id: String(e.request_id || ""),
    child_run_id: Number(e.child_run_id || 0),
    child_request_id: String(e.child_request_id || ""),
    status: wr(e.status),
    error: ln(e.error),
    output: e.output,
    asset: e.asset,
    version: e.version,
    result: e.result,
    approval: e.approval,
    interaction: e.interaction,
    persists_result: !!e.persists_result,
    agent_run_id: Number(e.agent_run_id || 0),
    source_signature: String(e.source_signature || "")
  } : null;
}
function Is(e) {
  return e ? Ga(
    e.error,
    e.result?.error,
    e.output?.error
  ) : "";
}
function Ka(e) {
  if (!e)
    return "";
  const t = [...e.node_results || []].reverse().find((n) => wr(
    n.status || n.result?.status
  ) === "fail");
  return Ga(
    Is(t),
    e.output?.error,
    e.error
  );
}
function Af(e, t = "节点运行失败") {
  return Ss(
    Is(e),
    t
  );
}
function La(e, t = "画布运行失败") {
  return Ss(Ka(e), t);
}
function Ss(e, t = "运行失败") {
  const n = ln(e);
  return n ? n.includes("InputImageSensitiveContentDetected") || n.includes("PrivacyInformation") ? "参考图片可能包含真人或隐私信息，请更换参考图后重试。" : n.includes("资产当前版本已变化") ? "引用的资产版本已变化，请刷新画布后重试。" : n.length > 500 ? `${n.slice(0, 497)}...` : n : t;
}
function Tf(...e) {
  for (const t of e) {
    const n = ln(t);
    if (n)
      return n;
  }
  return "";
}
const qa = /* @__PURE__ */ new Set([
  "画布运行失败",
  "节点运行失败",
  "节点执行失败",
  "运行失败",
  "执行出错"
]);
function Mf(e) {
  return qa.has(ln(e));
}
function Ga(...e) {
  let t = "";
  for (const n of e) {
    const r = ln(n);
    if (r && (t ||= r, !qa.has(r)))
      return r;
  }
  return t;
}
function ln(e) {
  if (typeof e == "string")
    return e.trim();
  if (e instanceof Error)
    return e.message.trim();
  if (!e || typeof e != "object" || Array.isArray(e))
    return "";
  const t = e, n = Tf(t.error, t.message, t.msg);
  if (n)
    return n;
  const r = ln(t.code), o = ln(t.detail);
  return [r, o].filter(Boolean).join(": ");
}
function Pf(e) {
  if (!e || typeof e != "object")
    return null;
  const t = Array.isArray(e.nodes) ? e.nodes.map(Df).filter(
    (r) => !!r
  ) : [], n = Array.isArray(e.edges) ? e.edges.map(Ef).filter(
    (r) => !!r
  ) : [];
  return {
    nodes: t,
    edges: n,
    incoming: yi(e.incoming),
    outgoing: yi(e.outgoing),
    order: Array.isArray(e.order) ? e.order.map((r) => String(r || "")).filter(Boolean) : t.map((r) => r.id)
  };
}
function Df(e) {
  const t = String(e?.id || "");
  return t ? {
    id: t,
    type: String(e?.type || ""),
    title: String(e?.title || ""),
    kind: String(e?.kind || ""),
    output_type: String(e?.output_type || ""),
    group_id: String(e?.group_id || ""),
    function_key: String(e?.function_key || ""),
    asset_cate_id: Number(e?.asset_cate_id || 0),
    persists_result: !!e?.persists_result,
    stops_flow: !!e?.stops_flow
  } : null;
}
function Ef(e) {
  const t = String(e?.source || ""), n = String(e?.target || "");
  return !t || !n ? null : {
    id: String(e?.id || `${t}-${n}`),
    source: t,
    target: n
  };
}
function yi(e) {
  const t = /* @__PURE__ */ new Map();
  if (!e || typeof e != "object" || Array.isArray(e))
    return t;
  for (const [n, r] of Object.entries(e)) {
    const o = Array.isArray(r) ? r.map((s) => String(s || "")).filter(Boolean) : [];
    t.set(String(n), o);
  }
  return t;
}
function Ff(e) {
  const t = String(e?.node_key || ""), n = Number(e?.node_run_id || 0);
  return !t || n <= 0 ? null : {
    node_run_id: n,
    node_id: Number(e?.node_id || 0),
    node_key: t,
    node_type: String(e?.node_type || ""),
    status: wr(e?.status),
    persists_result: !!e?.persists_result
  };
}
async function zf(e) {
  const t = String(e.requestId || "").trim();
  if (!t)
    throw new Error("request_id 不能为空");
  return Pu({
    streamApi: Of(e.projectId),
    requestID: t,
    lastID: e.lastId || "0-0",
    blockMs: 15e3,
    signal: e.signal,
    acceptErrorResult: !0,
    initialState: null,
    reduceFrame: (n, r) => (e.onFrame(r), n)
  });
}
function Of(e) {
  const t = new URL(xe("run/stream"), window.location.origin);
  return t.searchParams.set("project_id", String(e || 0)), t.toString();
}
function $f(e) {
  const t = e.default_value ?? "";
  return e.type === "switch" ? jf(t) : e.type === "multi_option" ? Ln(e, hi(t)) : e.type === "files" ? Ha(hi(t)) : e.type === "option" || e.type === "select" ? Xr(
    e,
    t || Yo(e.options?.[0]) || ""
  ) : Xr(e, t);
}
function vs(e) {
  const t = {};
  for (const n of e)
    !n.key || n.type === "description" || (t[n.key] = $f(n));
  return t;
}
function Xr(e, t) {
  if (e.value_type !== "number" || t === "")
    return t;
  const n = Number(t);
  return Number.isFinite(n) ? n : t;
}
function Ln(e, t) {
  return e.type === "multi_option" ? Ha(t).map(
    (n) => Xr(e, n)
  ) : Xr(e, t);
}
function Yo(e) {
  return e ? String(e.native_value || "").trim() || String(e.value || "").trim() || String(e.name || "").trim() || String(e.id || "") : "";
}
function Bn(e, t) {
  const n = [
    String(e.id || ""),
    String(e.native_value || "").trim(),
    String(e.value || "").trim(),
    String(e.name || "").trim()
  ].filter(Boolean);
  return t.some((r) => n.includes(String(r).trim()));
}
function hi(e) {
  if (typeof e != "string")
    return e;
  try {
    return JSON.parse(e);
  } catch {
    return e;
  }
}
function Ha(e) {
  return Array.isArray(e) ? e.map((t) => String(t)).filter(Boolean) : typeof e == "string" ? e ? [e] : [] : e ? [String(e)] : [];
}
function jf(e) {
  if (typeof e == "boolean")
    return e;
  const t = String(e ?? "").trim().toLowerCase();
  return t === "1" || t === "true" || t === "yes" || t === "on";
}
const Ya = "反馈已被新的运行替换";
function Bf(e) {
  return e instanceof Error && e.message === Ya;
}
function kn(e) {
  return (Array.isArray(e?.feedbackRequests) ? e.feedbackRequests : []).filter((n) => n && n.id);
}
function Uf(e, t) {
  const n = Number(t.approval?.id || 0);
  return {
    id: `${e.id}:${n || Date.now()}:${Math.random().toString(36).slice(2, 7)}`,
    nodeId: e.id,
    title: t.title || e.title || "补充信息",
    description: t.description || "",
    prompt: t,
    status: "pending",
    createdAt: (/* @__PURE__ */ new Date()).toISOString()
  };
}
function Vf(e, t, n) {
  return e.map(
    (r) => r.id === t ? {
      ...r,
      values: n,
      prompt: {
        ...r.prompt,
        values: n
      },
      status: "submitted",
      submittedAt: (/* @__PURE__ */ new Date()).toISOString()
    } : r
  );
}
function Kf(e, t, n) {
  if (!e)
    return !1;
  const r = t.find((s) => s.id === e.node.id) || e.node, o = kn(r).find(
    (s) => s.id === e.recordId
  );
  return o ? !(o.status === "pending" && n?.nodeId === e.node.id && n.recordId === e.recordId) : !1;
}
function Lf(e) {
  const t = e?.run || e?.data?.run || e || {}, n = Array.isArray(e?.approvals) ? e.approvals : Array.isArray(e?.data?.approvals) ? e.data.approvals : [], r = Array.isArray(e?.interactions) ? e.interactions : Array.isArray(e?.data?.interactions) ? e.data.interactions : [];
  return {
    runId: Number(t?.id || e?.run_id || 0),
    requestId: String(t?.request_id || e?.request_id || ""),
    status: wr(t?.status || e?.status || "running"),
    output: Qf(t?.output, e?.output, e?.data?.output),
    error: String(t?.error || e?.error || ""),
    approvals: n.map(Yf).filter(Boolean),
    interactions: r.map(Gf).filter(Boolean),
    raw: e
  };
}
function qf(e) {
  const t = e.interactions.find(
    (s) => !!(s.interaction?.id && s.interaction?.type)
  );
  if (t)
    return Wa(t);
  const n = e.approvals.find(Wf);
  if (!n?.id)
    return null;
  const r = Xf(n), o = Cs(r);
  return {
    approval: n,
    title: fn(r.title, n.title, "补充信息"),
    description: fn(r.description, "补充信息后继续执行流程。"),
    fields: o,
    values: Rs(r, o)
  };
}
function Wa(e) {
  const t = e.interaction, n = Cs(t);
  return {
    approval: {
      id: 0,
      title: String(t.title || ""),
      status: "pending",
      decision: "pending",
      content: {}
    },
    interaction: e,
    title: fn(t.title, "补充信息"),
    description: fn(t.description, "补充信息后继续执行流程。"),
    fields: n,
    values: Rs(t, n)
  };
}
function Gf(e) {
  const t = Du(e);
  return {
    runId: t.runId,
    nodeRunId: t.nodeRunId,
    interaction: t.interaction
  };
}
function Cs(e) {
  return (Array.isArray(e.fields) ? e.fields : Array.isArray(e.params) ? e.params : []).map((n, r) => Jf(n, r)).filter((n) => !!n.key);
}
function Rs(e, t) {
  const n = vs(t), r = e.values && typeof e.values == "object" ? e.values : {}, o = {
    ...n,
    ...r
  }, s = Number(
    e.source_target_id || e.sourceTargetId || 0
  );
  return s > 0 && (o.source_target_id = s), o;
}
function Hf(e, t) {
  const n = Zf(e);
  if (!n)
    return null;
  const r = Cs(n);
  return {
    approval: {
      id: 0,
      title: t,
      status: "pending",
      decision: "pending",
      content: { kind: "agent_interaction", interaction: n }
    },
    title: fn(n.title, t, "补充信息"),
    description: fn(n.description, "补充信息后继续执行智能体。"),
    fields: r,
    values: Rs(n, r)
  };
}
function Yf(e) {
  const t = e?.content && typeof e.content == "object" ? e.content : {};
  return {
    id: Number(e?.id || e?.approval_id || 0),
    title: String(e?.title || ""),
    status: String(e?.status || ""),
    decision: String(e?.decision || ""),
    content: t
  };
}
function Wf(e) {
  return e.status === "pending" || e.decision === "pending";
}
function Xf(e) {
  const t = e.content || {}, n = t.interaction;
  return n && typeof n == "object" ? n : t;
}
function Jf(e, t) {
  const n = Array.isArray(e?.options) ? e.options : [];
  return {
    id: Number(e?.id || t + 1),
    name: String(e?.name || e?.label || e?.title || e?.key || ""),
    key: String(e?.key || e?.name || `field_${t + 1}`),
    type: String(e?.type || "input"),
    preview_type: String(e?.preview_type || e?.previewType || "none"),
    value_type: e?.value_type || e?.valueType || "string",
    default_value: String(e?.default_value || e?.defaultValue || ""),
    required: !!e?.required,
    options: n.map((r, o) => ({
      id: Number(r?.id || o + 1),
      name: String(r?.name || r?.label || r?.value || ""),
      value: String(
        r?.value || r?.id || r?.label || r?.name || ""
      ),
      native_value: String(r?.native_value || r?.nativeValue || ""),
      preview_url: String(r?.preview_url || r?.previewUrl || ""),
      sort: Number(r?.sort || o + 1)
    }))
  };
}
function Zf(e) {
  const t = Mo(
    e?.output,
    e?.data?.output,
    e?.content,
    e
  );
  if (!t)
    return null;
  if (!String(t.event || "").toLowerCase().includes("interaction")) {
    const r = Mo(e?.interaction);
    return r && fn(r.type) ? r : null;
  }
  const n = Mo(t.interaction, t.content?.interaction);
  return n && fn(n.type) ? n : null;
}
function Mo(...e) {
  for (const t of e)
    if (t && typeof t == "object" && !Array.isArray(t))
      return t;
  return null;
}
function Qf(...e) {
  return e.find((t) => t != null);
}
function fn(...e) {
  for (const t of e)
    if (typeof t == "string" && t.trim())
      return t.trim();
  return "";
}
async function Xa(e) {
  return (await _d({
    teamID: e.teamID,
    projectID: e.projectID,
    files: e.files,
    ruleID: e.ruleID
  })).map(
    ({ sourceFile: n, uploadedFile: r, asset: o }) => tp(r, n, o)
  );
}
function ep(e) {
  const t = String(e.type || "").toLowerCase();
  return t.startsWith("image/") ? "image" : t.startsWith("video/") ? "video" : t.startsWith("audio/") ? "audio" : "file";
}
function tp(e, t, n) {
  const r = String(
    e?.url || e?.open_url || e?.download || ""
  ), o = String(e?.kind || ep(t));
  return {
    name: String(e?.name || t.name),
    alias: String(e?.name || t.name),
    kind: o,
    source: "upload",
    type: String(e?.mime || t.type || o),
    url: r,
    text: String(e?.name || t.name),
    output: e,
    asset: n
  };
}
function np({
  id: e,
  sourceX: t,
  sourceY: n,
  targetX: r,
  targetY: o,
  sourcePosition: s,
  targetPosition: i,
  markerEnd: a,
  style: d,
  data: u
}) {
  const [l, f, b] = Eu({
    sourceX: t,
    sourceY: n,
    sourcePosition: s,
    targetX: r,
    targetY: o,
    targetPosition: i
  }), _ = u || {}, w = !!_.isSelected, D = !!(_.isHighlighted || w), A = _.highlightColor || "#0ea5e9";
  return /* @__PURE__ */ v(Qt, { children: [
    D ? /* @__PURE__ */ c(
      Qs,
      {
        path: l,
        style: {
          stroke: A,
          strokeWidth: 7,
          opacity: 0.12
        }
      }
    ) : null,
    /* @__PURE__ */ c(
      Qs,
      {
        path: l,
        markerEnd: a,
        style: {
          ...d,
          stroke: w ? "var(--ws-edge-selected)" : D ? A : "var(--ws-edge)",
          strokeWidth: w ? 2.8 : D ? 2.4 : 1.45,
          opacity: D ? 0.96 : 0.62,
          transition: "stroke 160ms ease, stroke-width 160ms ease, opacity 160ms ease"
        }
      }
    ),
    w ? /* @__PURE__ */ c(Fu, { children: /* @__PURE__ */ c(
      "button",
      {
        type: "button",
        className: "ws-edge-delete nodrag nopan",
        style: {
          transform: `translate(-50%, -50%) translate(${f}px, ${b}px)`
        },
        "aria-label": "删除连线",
        onMouseDown: (G) => {
          G.preventDefault(), G.stopPropagation();
        },
        onClick: (G) => {
          G.preventDefault(), G.stopPropagation(), _.onDelete?.(String(e));
        },
        children: /* @__PURE__ */ c(Vu, { size: 15 })
      }
    ) }) : null,
    D ? /* @__PURE__ */ v(Qt, { children: [
      /* @__PURE__ */ c("circle", { r: "3", fill: A, children: /* @__PURE__ */ c("animateMotion", { dur: "2.8s", repeatCount: "indefinite", path: l }) }),
      /* @__PURE__ */ c("circle", { r: "1.8", fill: "rgba(255, 255, 255, 0.92)", children: /* @__PURE__ */ c("animateMotion", { dur: "2.8s", repeatCount: "indefinite", path: l }) })
    ] }) : null
  ] });
}
function Po({
  title: e,
  className: t,
  fallback: n = "未命名节点",
  onRename: r
}) {
  const [o, s] = $(!1), [i, a] = $(e), d = te(null);
  le(() => {
    o || a(e);
  }, [o, e]), le(() => {
    o && (d.current?.focus(), d.current?.select());
  }, [o]);
  const u = () => {
    const l = i.trim() || n;
    s(!1), a(l), l !== e && r?.(l);
  };
  return o && r ? /* @__PURE__ */ c(
    "input",
    {
      ref: d,
      className: `ws-canvas-node-title-input nodrag nowheel ${t || ""}`.trim(),
      value: i,
      maxLength: 64,
      "aria-label": "节点名称",
      onChange: (l) => a(l.target.value),
      onBlur: u,
      onPointerDown: (l) => l.stopPropagation(),
      onKeyDown: (l) => {
        l.stopPropagation(), l.key === "Enter" ? (l.preventDefault(), u()) : l.key === "Escape" && (l.preventDefault(), a(e), s(!1));
      }
    }
  ) : /* @__PURE__ */ c(Je, { label: r ? "双击重命名" : e, children: /* @__PURE__ */ c(
    "span",
    {
      className: t,
      onDoubleClick: r ? (l) => {
        l.preventDefault(), l.stopPropagation(), s(!0);
      } : void 0,
      children: e || n
    }
  ) });
}
const Ja = /* @__PURE__ */ new Set([
  "image",
  "video",
  "audio",
  "file"
]);
function ks(e) {
  return e.type === "file" || e.type === "files";
}
function xs(e) {
  const t = Array.from(
    new Set(
      (e.accepted_kinds || e.asset_kinds || []).map(Ts).filter((r) => !!r)
    )
  );
  if (t.length > 0)
    return t;
  const n = `${e.name || ""} ${e.key || ""}`.toLowerCase();
  return /video|视频/.test(n) ? ["video"] : /audio|music|音频|音乐/.test(n) ? ["audio"] : /image|img|photo|picture|图片|图像|参考图|首帧|尾帧/.test(n) ? ["image"] : /text|文本|提示词|文案/.test(n) ? ["file"] : ["image", "audio", "video", "file"];
}
function rp(e) {
  return (e.accepted_kinds || []).map(Ts).filter(
    (t) => !!t && Ja.has(t)
  );
}
function As(e) {
  const t = Ts(e);
  return t && Ja.has(t) ? t : void 0;
}
function op(e) {
  return e.type !== "files" ? 1 : Math.max(0, Number(e.max_files || 0));
}
function Ts(e) {
  const t = String(e || "").toLowerCase();
  return t === "rich" ? "richtext" : t === "music" ? "audio" : [
    "collection",
    "text",
    "image",
    "audio",
    "video",
    "richtext",
    "file"
  ].includes(t) ? t : void 0;
}
function sp(e, t) {
  return e.flatMap(({ edge: n, source: r }) => {
    const o = yp(t, r), s = Number(
      o?.refId || r.asset?.id || r.resultRef?.asset_id || 0
    );
    return s <= 0 ? [] : [
      {
        refType: "asset",
        refId: s,
        versionId: Number(
          o?.versionID || r.asset?.version?.id || r.asset?.version_id || r.resultRef?.version_id || 0
        ),
        label: String(o?.title || r.title || "媒体素材"),
        usage: String(n.mediaUsage || ""),
        trigger: "@",
        origin: "edge",
        originID: n.id
      }
    ];
  });
}
function _r(e) {
  if (e)
    for (const t of [e.kind, e.asset?.kind, e.power?.kind]) {
      const n = As(t);
      if (n)
        return n;
    }
}
function mo(e) {
  return !!_r(e);
}
function Wo(e) {
  const t = e.flatMap((n) => {
    if (n.type !== "file" && n.type !== "files")
      return [];
    const r = String(n.key || "").trim(), o = rp(n);
    return !r || o.length === 0 ? [] : [
      {
        key: r,
        label: String(n.name || r),
        maxFiles: op(n),
        acceptedKinds: o
      }
    ];
  });
  return Qa(
    t.map(
      (n) => ec(n) ? { ...n, maxFiles: 1 } : n
    )
  );
}
function ip(e, t) {
  const n = _r(t);
  return n ? e.filter((r) => r.acceptedKinds.includes(n)) : [];
}
function ap(e, t) {
  const n = e.find((s) => s.key === t);
  if (n || !Xo(t))
    return n;
  const r = e.filter(Za);
  if (r.length === 1)
    return r[0];
  const o = e.filter(
    (s) => !Xo(s.key)
  );
  return o.length === 1 ? o[0] : void 0;
}
function cp(e) {
  const t = go(e);
  return t === "firstframe" || t === "startframe";
}
function up(e) {
  const t = go(e);
  return t === "lastframe" || t === "endframe";
}
function Xo(e) {
  const t = go(e);
  return t === "firstframe" || t === "startframe" || t === "lastframe" || t === "endframe";
}
function Za(e) {
  if (!e.acceptedKinds.includes("image"))
    return !1;
  const t = go(e.key), n = String(e.label || "").trim();
  return ["images", "reference", "referenceimage", "referenceimages"].includes(
    t
  ) || n.includes("参考图") || n.includes("参考图片");
}
function Qa(e) {
  return e.map((t, n) => ({ option: t, index: n })).sort(
    (t, n) => wi(t.option) - wi(n.option) || t.index - n.index
  ).map(({ option: t }) => t);
}
function wi(e) {
  return cp(e.key) || e.label.includes("首帧") ? 0 : up(e.key) || e.label.includes("尾帧") ? 1 : Za(e) ? 2 : 3;
}
function ec(e) {
  return Xo(e.key) || e.label.includes("首帧") || e.label.includes("尾帧");
}
function go(e) {
  return String(e || "").trim().toLowerCase().replace(/[\s_-]+/g, "");
}
function bi(e, t, n, r, o = {}) {
  const s = e.flatMap(
    (u) => {
      const l = _r(u.source);
      return l ? [
        {
          referenceKey: gp(u),
          label: mp(u),
          kind: l,
          usage: fp(u, o),
          required: !0
        }
      ] : [];
    }
  ), i = new Map(
    n.flatMap((u) => {
      const l = Number(u.refId || 0);
      return l > 0 ? [[l, u]] : [];
    })
  );
  for (const [u, l] of (t?.parts || []).entries()) {
    if (l.type !== "reference" || l.ref_type !== "asset" || l.ref_origin === "edge")
      continue;
    const f = i.get(Number(l.ref_id || 0)), b = As(f?.kind);
    b && s.push({
      referenceKey: `asset:${l.ref_id}:${u}`,
      label: String(l.label || f?.title || "引用素材"),
      kind: b,
      usage: String(l.usage || ""),
      required: !1
    });
  }
  const a = /* @__PURE__ */ new Map(), d = /* @__PURE__ */ new Set();
  for (const u of s) {
    const l = `${u.referenceKey}:${u.usage}`;
    if (d.has(l))
      continue;
    d.add(l);
    const f = r.filter(
      (w) => w.acceptedKinds.includes(u.kind)
    );
    if (f.length === 0) {
      if (u.required)
        return `当前能力未配置可接收${rc(u.kind)}素材的参数`;
      continue;
    }
    const b = ap(f, u.usage);
    if (u.usage && !b)
      return `「${u.label}」的素材用途与当前能力参数不兼容`;
    const _ = b || (f.length === 1 ? f[0] : void 0);
    if (!_)
      return `请为「${u.label}」选择素材用途`;
    if (!Jo(_, a))
      return `${_.label}参数最多接收 ${nc(_)} 个素材`;
    Jr(a, _.key);
  }
  return "";
}
function dp(e, t, n, r) {
  const o = n.filter(mo), s = o.flatMap((u) => {
    const l = _r(u);
    return l ? [l] : [];
  }), i = t.filter(
    (u) => s.every((l) => u.acceptedKinds.includes(l))
  );
  if (s.length === 0 || i.length === 0) {
    const u = s[0];
    return {
      usage: void 0,
      error: s.length > 1 ? "当前能力未配置可同时接收该分组媒体素材的参数" : `当前能力未配置可接收${rc(u || "file")}素材的参数`
    };
  }
  const a = lp(e, t, r), d = tc(
    i,
    a,
    o.length
  );
  return d ? {
    usage: d.key,
    error: ""
  } : {
    usage: void 0,
    error: `${i[0].label}参数已达到素材数量上限`
  };
}
function Ni(e, t, n, r, o) {
  if (!t || r.length === 0)
    return { content: t, assignments: {} };
  const s = new Map(
    _i(e, n, o).map((f) => [
      f.key,
      f
    ])
  ), a = [..._i(t, n, o)].sort((f, b) => {
    const _ = Ii(
      f,
      s.get(f.key)
    ), w = Ii(
      b,
      s.get(b.key)
    );
    return _ - w || f.partIndex - b.partIndex;
  }), d = t.parts.map((f) => ({ ...f })), u = {}, l = /* @__PURE__ */ new Map();
  for (const f of a) {
    const b = s.get(f.key), _ = Qa(
      r.filter((V) => V.acceptedKinds.includes(f.kind))
    ), w = f.usage && f.usage !== b?.usage ? f.usage : "", A = pp(
      _,
      l,
      w,
      b?.usage || f.usage
    )?.key || "";
    A && Jr(l, A);
    const z = d[f.partIndex];
    z?.type === "reference" && (z.usage = A || void 0), f.connection && A !== String(f.connection.edge.mediaUsage || "") && (u[f.connection.edge.id] = A || void 0);
  }
  return { content: { ...t, parts: d }, assignments: u };
}
function lp(e, t, n) {
  const r = /* @__PURE__ */ new Map();
  for (const o of e) {
    const s = String(o.edge.mediaUsage || ""), i = ip(t, o.source).find(
      (a) => a.key === s
    );
    i && Jr(r, i.key);
  }
  for (const o of n?.parts || []) {
    if (o.type !== "reference" || o.ref_origin === "edge" || !o.usage)
      continue;
    const s = t.find((i) => i.key === o.usage);
    s && Jr(r, s.key);
  }
  return r;
}
function fp(e, t) {
  return String(
    Object.prototype.hasOwnProperty.call(t, e.edge.id) ? t[e.edge.id] || "" : e.edge.mediaUsage || ""
  );
}
function _i(e, t, n) {
  if (!e)
    return [];
  const r = new Map(
    n.map((s) => [s.edge.id, s])
  ), o = /* @__PURE__ */ new Map();
  return e.parts.flatMap((s, i) => {
    if (s.type !== "reference" || s.ref_type !== "asset")
      return [];
    const a = s.ref_origin_id ? r.get(s.ref_origin_id) : void 0, d = t.find(
      (b) => Number(b.refId || 0) === Number(s.ref_id || 0) && (!s.ref_version_id || Number(b.versionID || 0) === Number(s.ref_version_id))
    ), u = a ? _r(a.source) : As(d?.kind);
    if (!u)
      return [];
    const l = a ? `edge:${a.edge.id}` : `asset:${s.ref_id}:${s.ref_version_id || 0}`, f = o.get(l) || 0;
    return o.set(l, f + 1), [
      {
        key: a ? l : `${l}:${f}`,
        partIndex: i,
        kind: u,
        usage: String(s.usage || ""),
        connection: a
      }
    ];
  });
}
function Ii(e, t) {
  return e.usage && e.usage !== t?.usage ? 0 : t ? 1 : 2;
}
function pp(e, t, ...n) {
  return tc(
    e,
    t,
    1,
    ...n
  );
}
function tc(e, t, n, ...r) {
  for (const o of r) {
    const s = e.find((i) => i.key === o);
    if (s && Jo(s, t, n))
      return s;
  }
  return e.find(
    (o) => Jo(o, t, n)
  );
}
function nc(e) {
  return ec(e) ? 1 : e.maxFiles;
}
function Jo(e, t, n = 1) {
  const r = nc(e);
  return r <= 0 || (t.get(e.key) || 0) + n <= r;
}
function Jr(e, t) {
  e.set(t, (e.get(t) || 0) + 1);
}
function rc(e) {
  switch (e) {
    case "image":
      return "图片";
    case "video":
      return "视频";
    case "audio":
      return "音频";
    default:
      return "文件";
  }
}
function mp(e) {
  return String(e.source.title || "媒体素材").trim() || "媒体素材";
}
function gp(e) {
  const t = Number(
    e.source.asset?.id || e.source.resultRef?.asset_id || 0
  );
  return t > 0 ? `asset:${t}` : `node:${e.source.id}`;
}
function yp(e, t) {
  const n = Number(t.asset?.id || t.resultRef?.asset_id || 0), r = Number(
    t.asset?.version?.id || t.asset?.version_id || t.resultRef?.version_id || 0
  );
  return e.find(
    (o) => Number(o.refId || 0) === n && (!r || Number(o.versionID || 0) === r)
  ) || e.find((o) => o.id === t.id);
}
function Si(e) {
  return e?.service_name?.trim() || e?.name?.trim() || "来源";
}
const oc = ca(
  "@/components/agent/stream-request-params"
), hp = oc.PowerParamOptionDialog, wp = oc.normalizeParamPreviewType;
function vi({
  value: e,
  placeholder: t,
  running: n = !1,
  disabled: r = !1,
  textInputEnabled: o = !0,
  showMediaParamButtons: s = !1,
  mediaParamPower: i,
  submitDisabled: a = !1,
  submitDisabledReason: d = "",
  sourceOptions: u = [],
  selectedSourceId: l = 0,
  params: f = [],
  paramValues: b = {},
  assetLibrary: _ = { current: [] },
  referenceContent: w,
  assetReference: D,
  connectedMediaReferences: A = [],
  mediaUsageOptions: z = [],
  onConnectedMediaEdgeRemove: V,
  onChange: R,
  onParamChange: G,
  onSourceChange: ee,
  onLocalUpload: C,
  onSubmit: T
}) {
  const O = ue(
    () => f.filter(ks),
    [f]
  ), H = P(
    (W) => {
      const de = Np(W);
      _.current = [
        ..._.current.filter(
          (Qe) => Number(Qe.refId || 0) !== W.refId
        ),
        de
      ];
    },
    [_]
  ), re = P(
    async (W, de) => {
      if (!C)
        throw new Error("当前节点未配置本地上传");
      const Qe = Sp(O, de);
      if (!Qe)
        throw new Error("当前能力没有与所选素材类型匹配的上传参数");
      const Ce = (await C(W, Qe)).map((Ve) => Sa(Ve.asset)).filter((Ve) => Ve.id > 0);
      if (Ce.length === 0)
        throw new Error("上传成功，但没有生成可用资产");
      return Ce;
    },
    [C, O]
  ), X = rl({
    teamID: Number(D?.teamID || 0),
    scopeProjectID: Number(D?.projectID || 0),
    initialFilters: D?.projectID ? {
      sourceType: "project",
      projectID: D.projectID,
      assetCateID: Number(D.assetCateID || 0)
    } : void 0,
    onSelect: H,
    onUpload: C ? re : void 0
  }), [oe, J] = $(""), [Z, fe] = $(), ge = te(0), _e = P((W) => {
    J(""), ge.current += 1, fe({
      id: ge.current,
      trigger: "@",
      preferredUsage: W.key,
      acceptedKinds: xs(W)
    });
  }, []), ve = P(
    (W) => {
      fe(
        (de) => de?.id === W ? void 0 : de
      );
    },
    []
  ), at = f.filter(sc), M = u.find(
    (W) => W.target_id === l || W.id === l
  ), we = _.current, Ue = sp(
    A,
    we
  ), Pe = Ku(
    e,
    w,
    Ue
  ), ct = vp(Pe.content), Ze = Ri(
    e,
    w
  ), Et = Ri(
    Pe.value,
    Pe.content
  );
  return le(() => {
    (r || n) && J("");
  }, [r, n]), le(() => {
    Ze !== Et && R(Pe.value, Pe.content);
  }, [
    Ze,
    R,
    Et,
    Pe.content,
    Pe.value
  ]), /* @__PURE__ */ v(
    "div",
    {
      className: `ws-prompt-composer nowheel ${n ? "is-running" : ""}`,
      children: [
        /* @__PURE__ */ c("div", { className: "ws-prompt-main", children: /* @__PURE__ */ c("div", { className: "ws-prompt-editor-shell", children: /* @__PURE__ */ c(
          Lu,
          {
            className: "ws-prompt-reference-editor nodrag nopan",
            value: Pe.value,
            content: Pe.content,
            disabled: r || n,
            textEditable: o,
            placeholder: t,
            items: we,
            usageOptions: z.map((W) => ({
              key: W.key,
              label: W.label,
              acceptedKinds: W.acceptedKinds,
              maxFiles: W.maxFiles
            })),
            pickerRequest: Z,
            onPickerRequestConsumed: ve,
            assetReferenceProvider: D?.teamID ? X : void 0,
            onReferenceDelete: (W) => {
              W.ref_origin === "edge" && W.ref_origin_id && V?.(W.ref_origin_id);
            },
            onChange: R,
            onSubmit: !n && !a ? T : void 0
          }
        ) }) }),
        /* @__PURE__ */ v("div", { className: "ws-prompt-toolbar", children: [
          /* @__PURE__ */ v("div", { className: "ws-prompt-tools", children: [
            s ? O.map((W) => /* @__PURE__ */ c(
              bp,
              {
                param: W,
                power: i,
                selectedCount: ct.get(W.key) || 0,
                disabled: r || n,
                onClick: () => _e(W)
              },
              W.key
            )) : null,
            !s && O.length > 0 ? /* @__PURE__ */ c(
              Zo,
              {
                id: "attachments",
                openKey: oe,
                label: "添加素材",
                icon: /* @__PURE__ */ c(Sd, { size: 17 }),
                iconOnly: !0,
                variant: "attachments",
                disabled: r || n,
                onToggle: J,
                children: /* @__PURE__ */ c("div", { className: "ws-prompt-menu-list is-attachments", role: "menu", children: O.map((W) => /* @__PURE__ */ v(
                  "button",
                  {
                    type: "button",
                    className: "ws-prompt-menu-item",
                    role: "menuitem",
                    onClick: () => _e(W),
                    children: [
                      /* @__PURE__ */ c("span", { className: "ws-prompt-menu-kind-icon", children: /* @__PURE__ */ c(Id, { kind: Rp(W) }) }),
                      /* @__PURE__ */ c("span", { children: Ps(W) })
                    ]
                  },
                  W.key
                )) })
              }
            ) : null,
            u.length > 0 ? /* @__PURE__ */ c(
              Zo,
              {
                id: "source",
                openKey: oe,
                label: Si(M),
                icon: /* @__PURE__ */ c(ys, { size: 15 }),
                disabled: r || n,
                onToggle: J,
                children: /* @__PURE__ */ c("div", { className: "ws-prompt-menu-list", children: u.map((W) => {
                  const de = W.target_id || W.id, Qe = de === l;
                  return /* @__PURE__ */ v(
                    "button",
                    {
                      type: "button",
                      className: `ws-prompt-menu-item ${Qe ? "is-active" : ""}`,
                      disabled: r || n,
                      onClick: () => {
                        ee?.(de), J("");
                      },
                      children: [
                        /* @__PURE__ */ c("span", { children: Si(W) }),
                        Qe ? /* @__PURE__ */ c(cr, { size: 14 }) : null
                      ]
                    },
                    de
                  );
                }) })
              }
            ) : null,
            at.map((W) => /* @__PURE__ */ c(
              _p,
              {
                param: W,
                value: b[W.key],
                openKey: oe,
                disabled: r || n,
                onToggle: J,
                onChange: (de) => G?.(W.key, de)
              },
              W.key
            ))
          ] }),
          /* @__PURE__ */ c("div", { className: "ws-prompt-submit-group", children: /* @__PURE__ */ c(Je, { label: d || void 0, children: /* @__PURE__ */ c(
            "button",
            {
              type: "button",
              className: "ws-prompt-submit",
              disabled: r || n || a,
              onClick: T,
              "aria-label": d || "发送",
              children: n ? /* @__PURE__ */ c(Bt, { size: 17, className: "ws-spin" }) : /* @__PURE__ */ c(qu, { size: 18 })
            }
          ) }) })
        ] })
      ]
    }
  );
}
function bp({
  param: e,
  power: t,
  selectedCount: n,
  disabled: r,
  onClick: o
}) {
  const s = n > 0;
  return /* @__PURE__ */ c(Je, { label: Cp(e, n), children: /* @__PURE__ */ v(
    "button",
    {
      type: "button",
      className: `ws-prompt-tool is-media-param ${s ? "is-selected" : ""}`,
      disabled: r,
      "aria-pressed": s,
      onClick: o,
      children: [
        /* @__PURE__ */ c(ha, { power: t, size: 15 }),
        /* @__PURE__ */ c("span", { children: Ps(e) }),
        e.type === "files" && s ? /* @__PURE__ */ c("small", { className: "ws-prompt-media-count", children: n }) : null
      ]
    }
  ) });
}
function Np(e) {
  const t = String(e.preview?.kind || "file"), n = String(e.preview?.url || "");
  return {
    id: `asset:${e.refId}`,
    title: e.label,
    kind: t,
    source: "asset",
    refType: "asset",
    refId: e.refId,
    versionID: e.versionID,
    preview: {
      text: e.description || "",
      imageUrl: t === "image" ? n : "",
      videoUrl: t === "video" ? n : "",
      audioUrl: t === "audio" ? n : "",
      fileUrl: t === "file" ? n : ""
    }
  };
}
function _p({
  param: e,
  value: t,
  openKey: n,
  disabled: r,
  onToggle: o,
  onChange: s
}) {
  const [i, a] = $(!1), d = wp(e.preview_type);
  if (le(() => {
    (r || d === "none") && a(!1);
  }, [r, d]), (e.type === "option" || e.type === "select") && d !== "none") {
    const u = Ci(e, t);
    return /* @__PURE__ */ v(Qt, { children: [
      /* @__PURE__ */ c("span", { className: "ws-prompt-tool-wrap", children: /* @__PURE__ */ v(
        "button",
        {
          type: "button",
          className: "ws-prompt-tool",
          disabled: r,
          "aria-label": u,
          onClick: () => {
            o(""), a(!0);
          },
          children: [
            /* @__PURE__ */ c(ni, { name: e.icon, size: 15 }),
            /* @__PURE__ */ c("span", { children: u }),
            /* @__PURE__ */ c(uo, { size: 14 })
          ]
        }
      ) }),
      /* @__PURE__ */ c(
        hp,
        {
          open: i,
          title: e.name || e.key,
          previewType: d,
          options: e.options || [],
          value: t,
          disabled: r,
          onOpenChange: a,
          onConfirm: (l) => s(Ln(e, l))
        }
      )
    ] });
  }
  return /* @__PURE__ */ c(
    Zo,
    {
      id: e.key,
      openKey: n,
      label: Ci(e, t),
      icon: /* @__PURE__ */ c(ni, { name: e.icon, size: 15 }),
      disabled: r,
      onToggle: o,
      children: /* @__PURE__ */ c(
        Ip,
        {
          param: e,
          value: t,
          onChange: s,
          onClose: () => o("")
        }
      )
    }
  );
}
function Zo({
  id: e,
  openKey: t,
  label: n,
  icon: r,
  iconOnly: o = !1,
  variant: s = "default",
  disabled: i,
  children: a,
  onToggle: d
}) {
  const u = !i && t === e, l = s === "attachments" ? "is-attachments" : "", f = te(null), b = P(() => {
    f.current != null && (window.clearTimeout(f.current), f.current = null);
  }, []), _ = P(() => {
    b(), f.current = window.setTimeout(() => {
      f.current = null, d("");
    }, 240);
  }, [b, d]);
  return le(() => (u || b(), b), [b, u]), /* @__PURE__ */ v(
    "span",
    {
      className: `ws-prompt-tool-wrap ${l} ${u ? "is-open" : ""}`,
      onMouseEnter: () => {
        b(), i || d(e);
      },
      onMouseLeave: () => {
        u && _();
      },
      children: [
        /* @__PURE__ */ v(
          "button",
          {
            type: "button",
            className: `ws-prompt-tool ${o ? "is-icon-only" : ""} ${u ? "is-open" : ""}`,
            disabled: i,
            "aria-label": n,
            "aria-expanded": s === "attachments" ? u : void 0,
            "aria-haspopup": s === "attachments" ? "menu" : void 0,
            onClick: () => {
              i || (b(), d(u ? "" : e));
            },
            children: [
              r,
              o ? null : /* @__PURE__ */ c("span", { children: n }),
              o ? null : /* @__PURE__ */ c(uo, { size: 14 })
            ]
          }
        ),
        u ? /* @__PURE__ */ c(
          "div",
          {
            className: `ws-prompt-popover ${l}`,
            onMouseEnter: b,
            children: a
          }
        ) : null
      ]
    }
  );
}
function Ip({
  param: e,
  value: t,
  onChange: n,
  onClose: r
}) {
  if (e.type === "option" || e.type === "select")
    return /* @__PURE__ */ c("div", { className: "ws-prompt-menu-list", children: (e.options || []).map((o) => {
      const s = Bn(o, [
        String(t ?? "")
      ]);
      return /* @__PURE__ */ v(
        "button",
        {
          type: "button",
          className: `ws-prompt-menu-item ${s ? "is-active" : ""}`,
          onClick: () => {
            n(
              Ln(
                e,
                Yo(o)
              )
            ), r();
          },
          children: [
            /* @__PURE__ */ c("span", { children: o.name || o.value }),
            s ? /* @__PURE__ */ c(cr, { size: 14 }) : null
          ]
        },
        o.id || o.value
      );
    }) });
  if (e.type === "multi_option") {
    const o = ic(t);
    return /* @__PURE__ */ c("div", { className: "ws-prompt-menu-list", children: (e.options || []).map((s) => {
      const i = Bn(s, o);
      return /* @__PURE__ */ v(
        "button",
        {
          type: "button",
          className: `ws-prompt-menu-item ${i ? "is-active" : ""}`,
          onClick: () => {
            let a = [...o];
            i ? a = a.filter(
              (d) => !Bn(s, [d])
            ) : a.push(Yo(s)), n(Ln(e, a));
          },
          children: [
            /* @__PURE__ */ c("span", { children: s.name || s.value }),
            i ? /* @__PURE__ */ c(cr, { size: 14 }) : null
          ]
        },
        s.id || s.value
      );
    }) });
  }
  if (e.type === "switch") {
    const o = ac(t);
    return /* @__PURE__ */ v(
      "button",
      {
        type: "button",
        className: `ws-prompt-switch ${o ? "is-on" : ""}`,
        onClick: () => n(!o),
        children: [
          /* @__PURE__ */ c("span", { children: e.name }),
          /* @__PURE__ */ c("i", {})
        ]
      }
    );
  }
  return e.type === "prompt" || e.type === "textarea" ? /* @__PURE__ */ c(
    "textarea",
    {
      className: "ws-prompt-param-textarea",
      value: Qo(t),
      placeholder: e.name,
      onChange: (o) => n(o.target.value)
    }
  ) : /* @__PURE__ */ c(
    "input",
    {
      className: "ws-prompt-param-input",
      type: e.value_type === "number" ? "number" : "text",
      value: Qo(t),
      placeholder: e.name,
      onChange: (o) => n(
        e.value_type === "number" ? Number(o.target.value) : o.target.value
      )
    }
  );
}
function Ms(e) {
  return e.type === "prompt";
}
function sc(e) {
  return !(e.type === "hidden" || e.type === "description" || Ms(e) || ks(e));
}
function Ci(e, t) {
  if (e.type === "switch")
    return `${e.name}: ${ac(t) ? "开" : "关"}`;
  if (e.type === "multi_option") {
    const r = ic(t).length;
    return r > 0 ? `${e.name} ${r}` : e.name;
  }
  if (e.type === "option" || e.type === "select")
    return e.options?.find(
      (o) => Bn(o, [String(t ?? "")])
    )?.name || e.name;
  const n = Qo(t);
  return n ? `${e.name}: ${n}` : e.name;
}
function Sp(e, t) {
  const n = String(t.preferredUsage || "").trim();
  if (n) {
    const o = e.find((s) => s.key === n);
    if (o)
      return o;
  }
  const r = new Set(t.acceptedKinds || []);
  if (r.size > 0) {
    const o = e.find(
      (s) => xs(s).some((i) => r.has(i))
    );
    if (o)
      return o;
  }
  return e.length === 1 ? e[0] : void 0;
}
function Ri(e, t) {
  return JSON.stringify([String(e || ""), t?.parts || []]);
}
function vp(e) {
  const t = /* @__PURE__ */ new Map();
  for (const n of e?.parts || [])
    n.type !== "reference" || !n.usage || t.set(n.usage, (t.get(n.usage) || 0) + 1);
  return t;
}
function Cp(e, t) {
  const n = Ps(e);
  if (e.type !== "files")
    return t > 0 ? `${n}：已选择素材` : `选择${n}素材`;
  const r = Math.max(0, Number(e.max_files || 0));
  return t <= 0 ? r > 0 ? `选择${n}素材，最多 ${r} 个` : `选择${n}素材` : r > 0 ? `${n}：已选择 ${t} 个，最多 ${r} 个` : `${n}：已选择 ${t} 个`;
}
function Ps(e) {
  return String(e.name || e.key || "").trim() || "文件";
}
function Rp(e) {
  const t = xs(e);
  return t.length === 1 ? t[0] : "file";
}
function Qo(e) {
  return e == null ? "" : Array.isArray(e) ? e.join("、") : String(e);
}
function ic(e) {
  if (Array.isArray(e))
    return e.map((t) => String(t)).filter(Boolean);
  if (typeof e == "string") {
    const t = kp(e);
    return Array.isArray(t) ? t.map((n) => String(n)).filter(Boolean) : e ? [e] : [];
  }
  return e ? [String(e)] : [];
}
function ac(e) {
  if (typeof e == "boolean")
    return e;
  const t = String(e ?? "").trim().toLowerCase();
  return t === "1" || t === "true" || t === "yes" || t === "on";
}
function kp(e) {
  if (!e)
    return e;
  try {
    return JSON.parse(e);
  } catch {
    return e;
  }
}
const ki = 160, xi = 72, Ai = 72, Zr = 72, fr = 24, pr = 24, mr = 40, xn = 2, Qr = { width: 180, height: 180 }, xp = "storyboard-derived-layout-v7";
function Ap(e) {
  const t = e.groups.map((w) => ({
    ...w,
    size: Dp(w)
  })), n = /* @__PURE__ */ new Map(), r = [...t].sort(zr), o = Ep(
    "workspace",
    r
  ), s = t.filter((w) => w.direction === "upstream").sort(zr), i = t.filter(
    (w) => w.direction === "downstream" && w.powerKind !== "audio"
  ).sort(zr), a = t.filter(
    (w) => w.direction === "downstream" && w.powerKind === "audio"
  ).sort(zr), d = i.reduce(
    (w, D) => Math.max(w, D.size.height),
    0
  ), u = i.length ? e.sourceNode.y - d - Ai : e.sourceNode.y, l = e.sourceNode.x - ki;
  let f = u;
  for (const w of s)
    n.set(w.key, {
      bounds: {
        x: l - w.size.width,
        y: f,
        width: w.size.width,
        height: w.size.height
      },
      layoutKey: `${o}:${w.key}`
    }), f += w.size.height + Ai;
  let b = e.sourceNode.x;
  for (const w of i)
    n.set(w.key, {
      bounds: {
        x: b,
        y: u,
        width: w.size.width,
        height: w.size.height
      },
      layoutKey: `${o}:${w.key}`
    }), b += w.size.width + xi;
  let _ = e.sourceNode.x + e.sourceNode.width + ki;
  for (const w of a)
    n.set(w.key, {
      bounds: {
        x: _,
        y: e.sourceNode.y,
        width: w.size.width,
        height: w.size.height
      },
      layoutKey: `${o}:${w.key}`
    }), _ += w.size.width + xi;
  return n;
}
function Tp(e, t, n) {
  const r = t.filter((a) => a.groupId === e.id), o = n.kind === "audio", s = o ? n.width : Math.max(Qr.width, n.width), i = o ? n.height : Math.max(Qr.height, n.height);
  for (let a = 0; a < r.length + 100; a += 1) {
    const d = a % xn, u = Math.floor(a / xn), l = {
      x: e.x + fr + d * (s + pr),
      y: e.y + Zr + u * (i + mr)
    };
    if (r.every(
      (f) => !Fp(
        { ...l, width: n.width, height: n.height },
        f
      )
    ))
      return l;
  }
  return {
    x: e.x + fr,
    y: e.y + Zr
  };
}
function Mp(e, t) {
  const n = t.some((s) => s.kind === "audio"), r = {
    width: Math.max(
      n ? 0 : Qr.width,
      ...t.map((s) => s.width)
    ),
    height: Math.max(
      n ? 0 : Qr.height,
      ...t.map((s) => s.height)
    )
  }, o = /* @__PURE__ */ new Map();
  return t.forEach((s, i) => {
    const a = i % xn, d = Math.floor(i / xn);
    o.set(s.id, {
      x: e.x + fr + a * (r.width + pr),
      y: e.y + Zr + d * (r.height + mr)
    });
  }), {
    ...cc(t.length, r),
    positions: o
  };
}
function Pp(e) {
  return ws(e || void 0);
}
function Dp(e) {
  const t = Pp(
    e.power || { kind: e.powerKind, outputType: "" }
  );
  return cc(e.itemCount, t);
}
function cc(e, t) {
  const n = Math.max(1, Math.ceil(e / xn));
  return {
    width: fr * 2 + t.width * xn + pr * (xn - 1),
    height: Zr + fr + n * t.height + (n - 1) * mr
  };
}
function Ep(e, t) {
  return [
    xp,
    e,
    ...t.map(
      (n) => `${n.key}:${n.itemCount}:${n.size.width}x${n.size.height}`
    )
  ].join("|");
}
function zr(e, t) {
  return e.layoutIndex - t.layoutIndex || e.key.localeCompare(t.key);
}
function Fp(e, t) {
  return !(e.x + e.width + pr <= t.x || t.x + t.width + pr <= e.x || e.y + e.height + mr <= t.y || t.y + t.height + mr <= e.y);
}
const zp = [
  "characters",
  "scenes",
  "props"
], Ti = {
  character: "生成纯角色设定图，只展示当前角色的正面、侧面、背面和关键细节，不得出现其他人物、文字、水印或界面元素",
  scene: "生成纯场景设定图，只展示固定空间的外观、内部结构、光线和关键区域，不得出现任何人物、角色、动物、文字、水印或界面元素",
  prop: "生成纯道具设定图，只展示当前道具的多角度造型、比例、材质和关键细节，不得出现人物、手持者、文字、水印或界面元素"
}, es = [
  Do("characters", "角色组", "character", 0),
  Do("scenes", "场景组", "scene", 1),
  Do("props", "道具组", "prop", 2),
  {
    key: "shot_images",
    title: "镜头参考图组",
    itemType: "shot_image",
    powerKind: "image",
    outputType: "general",
    direction: "downstream",
    sourceGroupKeys: zp,
    layoutIndex: 0,
    enabled: va,
    items: (e) => e.shots.flatMap((t, n) => {
      if (t.continue_previous)
        return [];
      const r = Lp(e, t, n);
      return [
        {
          type: "shot_image",
          id: t.id,
          title: `镜头 ${t.order || n + 1} 参考图`,
          prompt: Hp(
            e,
            t,
            r.previousShot,
            r.externalReferences
          ),
          dependencyItems: r.dependencyItems,
          referenceItems: r.referenceItems,
          externalReferences: r.externalReferences,
          paramValues: em(e),
          shotId: t.id
        }
      ];
    })
  },
  {
    key: "shots",
    title: "镜头视频组",
    itemType: "shot",
    powerKind: "video",
    outputType: "general",
    direction: "downstream",
    sourceGroupKeys: ["shot_images"],
    layoutIndex: 1,
    enabled: vd,
    items: (e) => e.shots.map((t, n) => {
      const r = Gp(e, t, n);
      return {
        type: "shot",
        id: t.id,
        title: `镜头 ${t.order || n + 1}`,
        prompt: Yp(
          e,
          t,
          r.externalReferences
        ),
        ...r,
        paramValues: tm(e, t),
        shotId: t.id,
        shotDuration: t.duration,
        continuityAnchor: t.continuity_anchor
      };
    })
  },
  {
    key: "speech",
    title: "角色配音组",
    itemType: "speech",
    powerKind: "audio",
    outputType: "speech",
    direction: "downstream",
    layoutIndex: 2,
    enabled: Cd,
    items: Op
  },
  {
    key: "subtitles",
    title: "字幕组",
    itemType: "subtitle",
    powerKind: "text",
    outputType: "general",
    local: !0,
    direction: "downstream",
    layoutIndex: 3,
    enabled: Rd,
    items: jp
  },
  {
    key: "lip_sync",
    title: "口型同步组",
    itemType: "lip_sync",
    powerKind: "video",
    outputType: "lip_sync",
    direction: "downstream",
    sourceGroupKeys: ["shots", "speech"],
    layoutIndex: 4,
    enabled: kd,
    items: Bp
  }
];
function Do(e, t, n, r) {
  return {
    key: e,
    title: t,
    itemType: n,
    powerKind: "image",
    outputType: "general",
    direction: "upstream",
    layoutIndex: r,
    enabled: (o) => va(o) && o.materials.some((s) => s.type === n),
    items: (o) => o.materials.filter((s) => s.type === n).map((s) => {
      const i = Xp(
        o,
        s
      );
      return {
        type: n,
        id: s.id,
        title: s.name,
        prompt: Vp(
          o,
          s,
          i
        ),
        externalReferences: i
      };
    })
  };
}
function Op(e) {
  return e.shots.flatMap(
    (t, n) => t.speech.filter((r) => r.text.trim()).map((r, o) => {
      const s = $p(e, r);
      return {
        type: "speech",
        id: r.id,
        title: Up(
          e,
          r,
          t.order || n + 1,
          o
        ),
        prompt: r.text.trim(),
        ...s ? { paramValues: { voice: s } } : {},
        shotId: t.id,
        speechId: r.id,
        characterId: r.character_id,
        speechKind: r.kind,
        speakerMode: r.speaker_mode,
        startTime: r.start_time,
        shotDuration: t.duration
      };
    })
  );
}
function $p(e, t) {
  return t.kind === "narration" ? e.narrator_voice.trim() : (e.materials.find(
    (n) => n.type === "character" && n.id === t.character_id
  )?.voice || "").trim();
}
function jp(e) {
  return e.shots.flatMap((t, n) => {
    const r = xd(t);
    return r.length ? [
      {
        type: "subtitle",
        id: t.id,
        title: `镜头 ${t.order || n + 1} 字幕`,
        prompt: r.map((o) => o.text).join(" / "),
        localOutput: {
          type: "storyboard_subtitles",
          shot_id: t.id,
          tracks: r
        },
        shotId: t.id,
        shotDuration: t.duration
      }
    ] : [];
  });
}
function Bp(e) {
  return e.shots.flatMap((t, n) => {
    const r = t.speech.filter(Ad);
    if (!r.length)
      return [];
    const o = r.map((a) => a.id), s = r[0]?.character_id, i = t.speech.filter((a) => a.text.trim()).map((a) => a.id);
    return [
      {
        type: "lip_sync",
        id: t.id,
        title: `镜头 ${t.order || n + 1} 口型`,
        prompt: `同步镜头 ${t.order || n + 1} 的角色口型`,
        dependencyItems: [
          { type: "shot", id: t.id },
          ...i.map((a) => ({ type: "speech", id: a }))
        ],
        referenceItems: [
          { type: "shot", id: t.id },
          ...i.map((a) => ({ type: "speech", id: a }))
        ],
        shotId: t.id,
        speechIds: o,
        characterId: s,
        shotDuration: t.duration,
        optional: !0
      }
    ];
  });
}
function Up(e, t, n, r) {
  if (t.kind === "narration")
    return `镜头 ${n} 旁白 ${r + 1}`;
  const o = e.materials.find(
    (s) => s.type === "character" && s.id === t.character_id
  );
  return `镜头 ${n} ${o?.name || "角色"}配音`;
}
function Vp(e, t, n) {
  const r = dc(n), o = t.prompt.trim();
  if (o)
    return qr(
      e,
      `${r}${o}。${Ti[t.type]}`
    );
  const s = e.shots.filter((a) => a.material_ids.includes(t.id)).map((a) => a.description.trim()).filter(Boolean), i = s.length ? `相关镜头：${s.join("；")}` : "保持整部作品的统一视觉风格";
  return qr(
    e,
    `${r}${Ra[t.type]}“${t.name}”的素材生成图。${i}。${Ti[t.type]}`
  );
}
function Kp(e, t) {
  return Ca(e, t).map((n) => ({
    type: n.type,
    id: n.id
  }));
}
function Lp(e, t, n) {
  const r = Kp(e, t), o = Jp(e, t), s = t.match_previous ? qp(e, n) : void 0;
  if (!s)
    return {
      previousShot: void 0,
      dependencyItems: [],
      referenceItems: r,
      externalReferences: o
    };
  const i = {
    type: "shot_image",
    id: s.id
  };
  return {
    previousShot: s,
    dependencyItems: [i],
    referenceItems: [i, ...r],
    externalReferences: o
  };
}
function qp(e, t) {
  for (let n = t - 1; n >= 0; n -= 1) {
    const r = e.shots[n];
    if (!r.continue_previous)
      return r;
  }
}
function Gp(e, t, n) {
  const r = Zp(e, t), o = n > 0 ? e.shots[n - 1] : void 0;
  return t.continue_previous && o ? {
    dependencyItems: [{ type: "shot", id: o.id }],
    referenceItems: [],
    externalReferences: r
  } : {
    dependencyItems: [],
    referenceItems: [{ type: "shot_image", id: t.id }],
    externalReferences: r
  };
}
function Hp(e, t, n, r = []) {
  const o = Ca(e, t), s = [
    ...r.map(
      (a, d) => `参考图${d + 1}是${lc(a)}`
    ),
    n ? `参考图${r.length + 1}是前序镜头 ${n.order} 的参考画面` : "",
    ...o.map(
      (a, d) => `参考图${r.length + d + (n ? 2 : 1)}是${Ra[a.type]}“${a.name}”`
    )
  ].filter(Boolean).join("，"), i = [
    `镜头 ${t.order} 的单张参考画面`,
    s ? `图片顺序说明：${s}` : "",
    s ? "必须严格按照上述图片顺序识别素材，不得交换、合并或忽略参考对象" : "",
    n ? "当前镜头明确要求匹配上一镜画面；前序镜头只用于保持共同主体状态、光线与空间关系，当前素材清单中不存在的对象不得继续保留" : "",
    uc(e, t),
    "严格保持参考角色的五官、发型、服装、配色和体型，保持场景结构、道具造型以及整部作品画风一致",
    "不同参考对象必须保持各自独立的轮廓、材质和尺度，不得把角色与道具融合、机械化、穿戴化或互换材质",
    "角色必须保留参考图中的发饰数量与位置以及完整服装，道具必须保持参考图中的原始尺寸比例",
    t.description.trim(),
    t.camera_instruction.trim() ? `镜头语言：${t.camera_instruction.trim()}` : "",
    pc(t),
    `画幅：${e.aspect_ratio}`,
    "画面中不要出现字幕、对白文字、水印或界面元素"
  ].filter(Boolean);
  return qr(e, fc(i));
}
function Yp(e, t, n = []) {
  const r = t.video_prompt.trim() || Td(t), o = [
    uc(e, t),
    r,
    dc(n),
    t.continue_previous ? `使用上一镜头真实尾帧继续生成。连续性锚点：${t.continuity_anchor}。保持人物、服装、道具、场景光线和动作方向一致，但不要重复上一镜头内容` : "这是新的镜头段落，以当前镜头参考图为画面锚点建立画面",
    pc(t),
    `画幅：${e.aspect_ratio}`,
    "不生成可辨识对白、旁白、字幕或背景音乐，只保留环境声、动作声和不可辨识的人物声音",
    `时长 ${t.duration} 秒`
  ].filter(Boolean);
  return qr(e, fc(o));
}
function uc(e, t) {
  const n = e.shots.findIndex((s) => s.id === t.id), r = n <= 0 ? e.storyline.setup : n >= e.shots.length - 1 ? e.storyline.payoff : e.storyline.development, o = e.shots[n + 1]?.transition.trim();
  return [
    `故事目标：${e.summary.trim()}`,
    `当前叙事阶段：${r.trim()}`,
    `本镜变化：${t.beat.trim()}`,
    t.transition.trim() ? `从上一镜进入本镜：${t.transition.trim()}` : "",
    Wp(t, n),
    o ? `本镜结束需为下一镜建立：${o}` : ""
  ].filter(Boolean).join("；");
}
function Wp(e, t) {
  if (t <= 0)
    return "";
  const n = Dd[e.transition_type];
  return e.transition_type === "none" ? `进入本镜的剪辑方式：${n}` : `进入本镜的剪辑方式：${n}，时长 ${e.transition_duration_ms} 毫秒；这是后期剪辑信息，画面本身不要生成转场叠影`;
}
function Xp(e, t) {
  return Ds([
    ...e.references.filter(
      (n) => n.kind === "image" && t.reference_keys.includes(n.key)
    ),
    ...e.references.filter(
      (n) => n.kind === "image" && n.purpose === "visual_style"
    )
  ]);
}
function Jp(e, t) {
  return Ds([
    ...e.references.filter(
      (n) => n.kind === "image" && t.reference_keys.includes(n.key)
    ),
    ...e.references.filter(
      (n) => n.kind === "image" && n.purpose === "visual_style"
    )
  ]);
}
function Zp(e, t) {
  return Ds([
    ...e.references.filter(
      (n) => n.kind === "video" && t.reference_keys.includes(n.key)
    ),
    ...e.references.filter(
      (n) => n.kind === "video" && (n.purpose === "motion_style" || n.purpose === "visual_style")
    )
  ]);
}
function dc(e) {
  const t = e.map(lc);
  return t.length > 0 ? `参考素材：${t.join("；")}。` : "";
}
const Qp = {
  visual_style: "只参考画风、色彩、光线和材质，不复制其中的人物或剧情",
  motion_style: "只参考运镜、动作和剪辑节奏，不沿用原视频主体、剧情或声音",
  character: "作为指定角色的外观与身份锚点",
  scene: "作为指定场景的空间、陈设与光线锚点",
  prop: "作为指定道具的造型、材质与比例锚点",
  shot: "作为指定镜头的主体、构图与空间关系锚点"
};
function lc(e) {
  return `${e.label}（${[
    Qp[e.purpose],
    e.instruction
  ].filter(Boolean).join("；")}）`;
}
function Ds(e) {
  const t = [], n = /* @__PURE__ */ new Set();
  for (const r of e)
    n.has(r.asset_id) || (n.add(r.asset_id), t.push(r));
  return t;
}
function fc(e) {
  return e.map((t) => t.trim().replace(/[。！？!?；;，,：:]+$/g, "")).filter(Boolean).join("。");
}
function em(e) {
  return { aspectRatio: e.aspect_ratio, resolution: "2k" };
}
function tm(e, t) {
  return {
    aspectRatio: e.aspect_ratio,
    duration: t.duration
  };
}
function pc(e) {
  return !Md(e) || ![...Pd(e)][0] ? "" : "出镜说话角色是画面中唯一清晰可识别的正脸，其他人物使用背面、侧后方、远景或遮挡构图";
}
function nm(e) {
  const t = new Map(
    (e.current?.clips || []).map((r) => [r.id, r])
  ), n = e.storyboard.shots.map(
    (r, o) => rm({
      ...e,
      shot: r,
      index: o,
      current: t.get(r.id)
    })
  );
  return {
    version: 3,
    clips: Gu(
      n,
      (e.current?.clips || []).map((r) => r.id),
      (r) => r.id
    ),
    settings: {
      resolution: e.current?.settings.resolution || "auto",
      fps: e.current?.settings.fps ?? 0
    }
  };
}
function rm(e) {
  const t = eo(
    e.nodes,
    e.sourceNodeId,
    "shot",
    e.shot.id
  ), n = eo(
    e.nodes,
    e.sourceNodeId,
    "lip_sync",
    e.shot.id
  ), r = ts(t), o = n?.storyboardItem?.stale ? void 0 : ts(n), s = !!e.current?.useOriginalVideo, i = [];
  t?.power ? r || i.push("镜头视频尚未生成") : i.push("未配置镜头视频能力");
  const a = new Map(
    (e.current?.speechTracks || []).map((w) => [w.id, w])
  ), d = e.shot.speech.filter((w) => w.text.trim()).map(
    (w) => am(
      e.nodes,
      e.sourceNodeId,
      w,
      a.get(w.id),
      i
    )
  ), u = im(
    e.nodes,
    e.sourceNodeId,
    e.shot.id
  ), l = !s && o ? o : r, f = e.storyboard.shots[e.index + 1], b = f ? {
    type: f.transition_type,
    durationMs: f.transition_type === "none" ? 0 : f.transition_duration_ms
  } : { type: "none", durationMs: 0 }, _ = om(
    e.current,
    b
  );
  return {
    id: e.shot.id,
    title: `镜头 ${e.shot.order || e.index + 1}`,
    ...l ? { visualVideo: l } : {},
    ...r ? { originalAudioSource: r } : {},
    duration: e.shot.duration,
    originalVolume: e.current?.originalVolume ?? (d.length ? 0.45 : 1),
    speechTracks: d,
    subtitleTracks: u,
    useOriginalVideo: s,
    blockingIssues: cm(i),
    transitionToNext: _,
    storyboardTransitionToNext: b
  };
}
function om(e, t) {
  if (!e)
    return t;
  const n = e.storyboardTransitionToNext;
  return n && sm(
    e.transitionToNext,
    n
  ) ? t : e.transitionToNext;
}
function sm(e, t) {
  return e.type === t.type && e.durationMs === t.durationMs;
}
function im(e, t, n) {
  const r = eo(
    e,
    t,
    "subtitle",
    n
  ), o = Mi(r?.resultOutput);
  return (Array.isArray(o.tracks) ? o.tracks : []).flatMap((i) => {
    const a = Mi(i), d = Or(a.id), u = Or(a.text);
    if (!d || !u)
      return [];
    const l = Pi(a.end_time ?? a.endTime), f = Or(a.speech_id ?? a.speechId);
    return [
      {
        id: d,
        text: u,
        startTime: Math.max(
          0,
          Pi(a.start_time ?? a.startTime)
        ),
        ...l > 0 ? { endTime: l } : {},
        ...f ? { speechId: f } : {},
        source: Or(a.source) === "speech" ? "speech" : "caption"
      }
    ];
  });
}
function am(e, t, n, r, o) {
  const s = eo(e, t, "speech", n.id), i = ts(s);
  return s?.power ? i || o.push(`语音“${n.text}”尚未生成`) : o.push(`语音“${n.text}”未配置语音合成能力`), {
    id: n.id,
    ...i ? { audio: i } : {},
    startTime: n.start_time,
    kind: n.kind,
    ...n.character_id ? { characterId: n.character_id } : {},
    text: n.text,
    volume: r?.volume ?? 1
  };
}
function eo(e, t, n, r) {
  return e.find(
    (o) => o.storyboardItem?.sourceNodeId === t && o.storyboardItem.itemType === n && o.storyboardItem.itemId === r
  );
}
function ts(e) {
  const t = Number(e?.resultRef?.asset_id || 0), n = Number(e?.resultRef?.version_id || 0), r = t && n ? t : Number(e?.asset?.id || 0), o = t && n ? n : Number(e?.asset?.version_id || e?.asset?.version?.id || 0);
  if (!(!r || !o))
    return {
      assetId: r,
      versionId: o,
      label: e?.title || "素材"
    };
}
function cm(e) {
  return [...new Set(e.filter(Boolean))];
}
function Mi(e) {
  return e && typeof e == "object" && !Array.isArray(e) ? e : {};
}
function Or(e) {
  return typeof e == "string" ? e.trim() : "";
}
function Pi(e) {
  const t = Number(e);
  return Number.isFinite(t) ? t : 0;
}
function mc(e) {
  const t = String(
    e.storyboardItem?.generatedPrompt || ""
  ).trim(), n = String(e.composerDraft?.prompt || "").trim();
  return !!(t && n && n !== t);
}
function um(e, t) {
  const n = String(
    e.storyboardItem?.generatedPrompt || ""
  ).trim(), r = String(e.composerDraft?.prompt || "");
  if (!n || r.trim() === n)
    return null;
  const o = new Set(
    e.storyboardItem?.referenceNodeIds || []
  ), s = t.filter((b) => o.has(b.id)).map(
    (b) => yc(
      b,
      e.storyboardItem?.itemType
    )
  ).filter((b) => !!b), i = new Set(
    e.storyboardItem?.externalReferenceAssetIds || []
  ), a = da(
    e.composerDraft?.promptContent
  ).filter((b) => i.has(b.refId)), d = t.find(
    (b) => b.id === e.storyboardItem?.sourceNodeId
  ), l = ((d ? br([
    d.asset?.version?.content,
    d.resultOutput
  ]) : null)?.references || []).filter((b) => i.has(b.asset_id)).map(hc), f = [
    ...s,
    ...a,
    ...l
  ];
  return {
    ...e.composerDraft || {},
    prompt: n,
    promptContent: f.length ? gs(n, f) : void 0,
    paramValues: Nc(
      e.composerDraft?.paramValues,
      r,
      n
    )
  };
}
function to(e, t, n) {
  const r = Ei(n);
  return e.find(
    (o) => Number(o.id || 0) > 0 && String(o.kind || "").trim().toLowerCase() === t && Ei(o.outputType) === r
  ) || null;
}
function ns(e) {
  let t = e.canvas;
  for (const n of e.canvas.nodes) {
    if (n.type !== "power" || !bs(
      n.power,
      n.kind,
      n.outputType
    ))
      continue;
    const r = br([
      n.asset?.version?.content,
      n.resultOutput
    ]);
    if (!r || !Ed(r))
      continue;
    const o = t.nodes.find((u) => u.id === n.id) || n, s = dm(
      o,
      r
    ), i = String(
      o.storyboardMaterializedSignature || ""
    ), a = lm(
      t.nodes,
      o.id
    );
    t = (i ? i !== s : !a) ? gm({
      canvas: t,
      storyboardNode: o,
      storyboard: r,
      assetCate: e.assetCate,
      powers: e.powers
    }) : pm({
      canvas: t,
      storyboardNode: o,
      storyboard: r,
      assetCate: e.assetCate,
      powers: e.powers
    }), t = fm(
      t,
      o.id,
      s
    );
  }
  return t;
}
function dm(e, t) {
  return pt(
    JSON.stringify([
      e.id,
      Number(e.resultRef?.asset_id || e.asset?.id || 0),
      Number(
        e.resultRef?.version_id || e.asset?.version_id || e.asset?.version?.id || 0
      ),
      t.workflow.confirmed_at,
      t.production_plan,
      t.materials.map((n) => [n.type, n.id]),
      t.shots.map((n) => n.id)
    ])
  );
}
function lm(e, t) {
  return e.some(
    (n) => n.storyboardItem?.sourceNodeId === t || n.type === "group" && n.group?.origin === "script" && n.group.sourceNodeId === t
  );
}
function fm(e, t, n) {
  const r = e.nodes.findIndex((s) => s.id === t);
  if (r < 0 || e.nodes[r].storyboardMaterializedSignature === n)
    return e;
  const o = [...e.nodes];
  return o[r] = {
    ...o[r],
    storyboardMaterializedSignature: n
  }, { ...e, nodes: o };
}
function pm(e) {
  const t = [...e.canvas.nodes];
  let n = !1;
  const r = es.filter(
    (o) => o.enabled(e.storyboard)
  );
  for (const o of r) {
    const s = o.local ? null : to(e.powers, o.powerKind, o.outputType);
    for (const i of o.items(e.storyboard)) {
      const a = t.findIndex(
        (f) => gr(
          f,
          e.storyboardNode.id,
          i.type,
          i.id
        )
      );
      if (a < 0)
        continue;
      const d = gc(
        i,
        t,
        e.storyboardNode.id
      ), u = t[a], l = wc(
        u,
        u.groupId || "",
        d,
        o,
        s,
        { preserveStructure: !0 }
      );
      l !== u && (t[a] = l, n = !0);
    }
  }
  if (ka(e.storyboard)) {
    const o = vc({
      nodes: t,
      storyboardNode: e.storyboardNode,
      storyboard: e.storyboard,
      assetCate: e.assetCate,
      power: to(e.powers, "video", "video_compose"),
      nextNodeNo: e.canvas.nextNodeNo,
      createMissing: !1,
      preservePosition: !0
    });
    n = n || !!o?.changed;
  }
  return n ? { ...e.canvas, nodes: t } : e.canvas;
}
function mm(e) {
  return JSON.stringify(
    e.nodes.filter(
      (t) => !!t.storyboardItem || t.type === "power" && bs(
        t.power,
        t.kind,
        t.outputType
      )
    ).map((t) => [
      t.id,
      Number(t.resultRef?.asset_id || 0),
      Number(t.resultRef?.version_id || 0),
      Number(t.asset?.id || 0),
      Number(t.asset?.version_id || t.asset?.version?.id || 0)
    ])
  );
}
function gm(e) {
  const t = [...e.canvas.nodes], n = /* @__PURE__ */ new Set(), r = new Set(
    es.map((_) => _.itemType)
  ), o = es.filter(
    (_) => _.enabled(e.storyboard)
  ), s = ka(
    e.storyboard
  );
  r.add("video_compose"), s && n.add(
    Lr(
      e.storyboardNode.id,
      "video_compose",
      "composition"
    )
  );
  const i = o.map((_) => ({
    spec: _,
    items: _.items(e.storyboard),
    power: _.local ? null : to(e.powers, _.powerKind, _.outputType)
  }));
  for (const { items: _ } of i)
    for (const w of _)
      n.add(
        Lr(e.storyboardNode.id, w.type, w.id)
      );
  let a = !1;
  for (let _ = t.length - 1; _ >= 0; _ -= 1) {
    const w = t[_].storyboardItem;
    !w || w.sourceNodeId !== e.storyboardNode.id || !r.has(w.itemType) || n.has(
      Lr(
        w.sourceNodeId,
        w.itemType,
        w.itemId
      )
    ) || (t.splice(_, 1), a = !0);
  }
  const d = Ap({
    sourceNode: e.storyboardNode,
    groups: i.map(({ spec: _, items: w, power: D }) => ({
      key: _.key,
      layoutIndex: _.layoutIndex,
      itemCount: w.length,
      power: D,
      powerKind: _.powerKind,
      direction: _.direction
    }))
  });
  let u = xa(t, e.canvas.nextNodeNo);
  a = a || u !== e.canvas.nextNodeNo;
  for (const { spec: _, items: w, power: D } of i) {
    const A = d.get(_.key);
    if (!A)
      continue;
    const z = wm({
      nodes: t,
      storyboardNode: e.storyboardNode,
      spec: _,
      layout: A,
      assetCate: e.assetCate
    });
    a = a || z.changed;
    for (const C of w) {
      const T = gc(
        C,
        t,
        e.storyboardNode.id
      ), O = t.findIndex(
        (re) => gr(
          re,
          e.storyboardNode.id,
          T.type,
          T.id
        )
      );
      if (O >= 0) {
        const re = t[O], X = wc(
          re,
          z.node.id,
          T,
          _,
          D
        );
        X !== re && (t[O] = X, a = !0);
        continue;
      }
      const H = bm({
        nodes: t,
        group: z.node,
        storyboardNode: e.storyboardNode,
        item: T,
        assetCate: e.assetCate,
        spec: _,
        power: D
      });
      H.nodeNo = u++, t.push(H), a = !0;
    }
    const V = w.map(
      (C) => t.find(
        (T) => T.groupId === z.node.id && gr(
          T,
          e.storyboardNode.id,
          C.type,
          C.id
        )
      )
    ).filter((C) => !!C), R = Mp(
      z.node,
      V
    );
    for (const C of V) {
      const T = R.positions.get(C.id);
      if (!T || C.x === T.x && C.y === T.y)
        continue;
      const O = t.indexOf(C);
      t[O] = { ...C, ...T }, a = !0;
    }
    const G = t.findIndex((C) => C.id === z.node.id), ee = t[G];
    (ee.width !== R.width || ee.height !== R.height) && (t[G] = {
      ...ee,
      width: R.width,
      height: R.height
    }, a = !0);
  }
  const l = new Set(o.map((_) => _.key));
  for (let _ = t.length - 1; _ >= 0; _ -= 1) {
    const w = t[_];
    w.type !== "group" || w.group?.origin !== "script" || w.group.sourceNodeId !== e.storyboardNode.id || !w.group.syncKey || l.has(
      w.group.syncKey
    ) || (t.splice(_, 1), a = !0);
  }
  const f = s ? vc({
    nodes: t,
    storyboardNode: e.storyboardNode,
    storyboard: e.storyboard,
    assetCate: e.assetCate,
    power: to(e.powers, "video", "video_compose"),
    nextNodeNo: u
  }) : null;
  f && (u = f.nextNodeNo, a = a || f.changed);
  let b = Im(
    e.canvas.edges,
    t,
    e.storyboardNode.id,
    o
  );
  return b = Sm(b, t, e.storyboardNode.id), b = f ? Cm(
    b,
    t,
    e.storyboardNode.id,
    f.node.id,
    o
  ) : Rm(b, e.storyboardNode.id), b !== e.canvas.edges && (a = !0), a ? { ...e.canvas, nextNodeNo: u, nodes: t, edges: b } : e.canvas;
}
function gc(e, t, n) {
  const r = (w) => (w || []).map(
    (D) => t.find(
      (A) => gr(A, n, D.type, D.id)
    )
  ).filter((D) => !!D), o = r(e.dependencyItems), s = r(e.referenceItems), i = xm([...o, ...s]), a = e.externalReferences || [], d = {
    ...e,
    dependencyNodeIds: o.map((w) => w.id),
    referenceNodeIds: s.map((w) => w.id),
    sourceSignatureParts: [
      ...e.sourceSignatureParts || [],
      ...i.map(Am),
      ...a.map(hm)
    ]
  };
  if (!["character", "scene", "prop", "shot_image", "shot", "lip_sync"].includes(
    e.type
  ))
    return d;
  const u = ym(
    e.prompt,
    s,
    a
  ), l = u === e.prompt ? d : { ...d, prompt: u }, f = a.map(
    hc
  );
  if (f.push(
    ...s.map((w) => yc(w, e.type)).filter((w) => !!w)
  ), !f.length)
    return l;
  const b = gs(
    u,
    f
  ), _ = Hu(b);
  return ua(b) ? { ...d, prompt: _, promptContent: b } : { ...l, prompt: _ };
}
function ym(e, t, n = []) {
  const r = [], o = /* @__PURE__ */ new Set();
  for (const s of n) {
    const i = ei(s.label), a = i ? `@${i}` : "";
    !a || o.has(a) || e.includes(a) || (o.add(a), r.push(a));
  }
  for (const s of t) {
    const i = ei(s.title), a = i ? `@${i}` : "";
    !a || o.has(a) || e.includes(a) || (o.add(a), r.push(a));
  }
  return [r.join(" "), e].filter(Boolean).join(" ").trim();
}
function yc(e, t) {
  const n = Number(e.resultRef?.asset_id || e.asset?.id || 0), r = Number(
    e.resultRef?.version_id || e.asset?.version_id || e.asset?.version?.id || 0
  );
  return !n || !r ? null : {
    refType: "asset",
    refId: n,
    versionId: r,
    label: e.title,
    usage: t === "shot" && e.storyboardItem?.itemType === "shot_image" ? "firstFrame" : void 0
  };
}
function hc(e) {
  return {
    refType: "asset",
    refId: e.asset_id,
    versionId: e.version_id,
    label: e.label
  };
}
function hm(e) {
  return [
    "asset",
    e.asset_id,
    e.version_id || 0,
    e.kind,
    e.purpose,
    e.instruction
  ].join(":");
}
function wm(e) {
  const t = Es(
    e.nodes,
    e.storyboardNode.id,
    e.spec.key
  );
  if (t) {
    const o = t.title !== e.spec.title;
    if (t.group?.layoutKey === e.layout.layoutKey && !o)
      return { node: t, changed: !1 };
    if (t.group?.layoutKey === e.layout.layoutKey) {
      const d = { ...t, title: e.spec.title };
      return e.nodes[e.nodes.indexOf(t)] = d, { node: d, changed: !0 };
    }
    const s = e.layout.bounds.x - t.x, i = e.layout.bounds.y - t.y;
    let a = t;
    for (const [d, u] of e.nodes.entries()) {
      if (u.id !== t.id && u.groupId !== t.id)
        continue;
      const l = {
        ...u,
        x: u.x + s,
        y: u.y + i,
        ...u.id === t.id ? {
          title: e.spec.title,
          width: e.layout.bounds.width,
          height: e.layout.bounds.height,
          group: {
            ...u.group || {},
            layoutKey: e.layout.layoutKey
          }
        } : {}
      };
      e.nodes[d] = l, u.id === t.id && (a = l);
    }
    return { node: a, changed: !0 };
  }
  const n = e.layout.bounds, r = fo("group", e.assetCate, e.nodes.length, {
    x: n.x,
    y: n.y
  });
  return r.id = Fs(
    e.nodes,
    `script-group-${pt(e.storyboardNode.id)}-${e.spec.key}`
  ), r.title = e.spec.title, r.width = n.width, r.height = n.height, r.group = {
    origin: "script",
    sourceNodeId: e.storyboardNode.id,
    syncKey: e.spec.key,
    layoutKey: e.layout.layoutKey
  }, e.nodes.push(r), { node: r, changed: !0 };
}
function Es(e, t, n) {
  return e.find(
    (r) => r.type === "group" && r.group?.origin === "script" && r.group.sourceNodeId === t && r.group.syncKey === n
  );
}
function bm(e) {
  const t = fo(
    "power",
    e.assetCate,
    e.nodes.length,
    { x: e.group.x, y: e.group.y },
    e.power ? { power: e.power } : void 0
  );
  t.id = Fs(
    e.nodes,
    `script-item-${pt(
      Lr(
        e.storyboardNode.id,
        e.item.type,
        e.item.id
      )
    )}`
  ), t.title = e.item.title, t.description = e.item.prompt, t.kind = e.spec.powerKind, t.outputType = e.spec.outputType, e.power || Object.assign(
    t,
    ws({
      kind: e.spec.powerKind,
      outputType: e.spec.outputType
    })
  ), !e.power && !e.spec.local && (t.subtitle = `未配置${km(e.spec)}能力`, t.description = `${t.subtitle}。配置并启用能力后可运行此条目。`), e.spec.local && (t.subtitle = "本地字幕轨", t.description = e.item.prompt || "当前镜头字幕轨", t.resultOutput = e.item.localOutput), t.groupId = e.group.id, t.composerDraft = {
    prompt: e.item.prompt,
    promptContent: e.item.promptContent,
    paramValues: e.item.paramValues
  }, t.storyboardItem = _c(
    e.storyboardNode.id,
    e.item
  );
  const n = Tp(
    e.group,
    e.nodes,
    t
  );
  return t.x = n.x, t.y = n.y, t;
}
function wc(e, t, n, r, o, s = {}) {
  const i = e.storyboardItem;
  if (!i)
    return e;
  const a = i.sourceSignature || Ic({
    ...n,
    prompt: i.generatedPrompt,
    promptContent: e.composerDraft?.promptContent
  }), d = String(e.composerDraft?.prompt || ""), u = !d.trim() || d === i.generatedPrompt, l = u ? n.prompt : d, f = l !== d, b = Nm(
    e.composerDraft?.paramValues,
    d,
    l,
    n.paramValues
  ), _ = b !== e.composerDraft?.paramValues, w = u ? n.promptContent : _m(
    d,
    e.composerDraft?.promptContent,
    n.promptContent
  ), D = JSON.stringify(e.composerDraft?.promptContent || null) !== JSON.stringify(w || null), A = _c(i.sourceNodeId, n), z = bc(e), V = i.resultSourceSignature || (z ? a : "");
  V && !r.local && (A.resultSourceSignature = V), A.stale = r.local ? !1 : !!(z && V !== A.sourceSignature);
  const R = s.preserveStructure && e.titleMode === "manual" ? e.title : n.title, G = e.title !== R, ee = s.preserveStructure ? e.groupId || "" : t, C = !e.power && !!o, T = e.kind !== r.powerKind, O = e.outputType !== r.outputType, H = r.local && JSON.stringify(e.resultOutput || null) !== JSON.stringify(n.localOutput || null);
  return (e.groupId || "") === ee && !f && !_ && !D && !G && !C && !T && !O && !H && Sc(i, A) ? e : {
    ...e,
    title: R,
    kind: r.powerKind,
    outputType: r.outputType,
    ...C ? {
      power: o || void 0,
      subtitle: o?.output?.name || o?.name || e.subtitle
    } : {},
    description: f || C ? l : e.description,
    ...r.local ? { resultOutput: n.localOutput } : {},
    groupId: ee,
    composerDraft: f || _ || D ? {
      ...e.composerDraft || {},
      prompt: l,
      promptContent: w,
      paramValues: b
    } : e.composerDraft,
    storyboardItem: A
  };
}
function bc(e) {
  return Number(e.resultRef?.version_id || 0) > 0 || Number(e.asset?.version_id || e.asset?.version?.id || 0) > 0 || e.resultOutput != null;
}
function Nc(e, t, n) {
  if (!e || !t || t === n)
    return e;
  let r;
  for (const [o, s] of Object.entries(e))
    s === t && (r ||= { ...e }, r[o] = n);
  return r || e;
}
function Nm(e, t, n, r) {
  let o = Nc(
    e,
    t,
    n
  );
  for (const [s, i] of Object.entries(r || {}))
    o?.[s] !== i && (o = { ...o || {}, [s]: i });
  return o;
}
function _m(e, t, n) {
  return !n || !ua(n) ? t : gs(
    e,
    da(n)
  );
}
function _c(e, t) {
  return {
    sourceNodeId: e,
    itemType: t.type,
    itemId: t.id,
    generatedPrompt: t.prompt,
    dependencyNodeIds: t.dependencyNodeIds,
    referenceNodeIds: t.referenceNodeIds,
    externalReferenceAssetIds: (t.externalReferences || []).map(
      (n) => n.asset_id
    ),
    shotId: t.shotId,
    speechId: t.speechId,
    speechIds: t.speechIds,
    characterId: t.characterId,
    speechKind: t.speechKind,
    speakerMode: t.speakerMode,
    startTime: t.startTime,
    shotDuration: t.shotDuration,
    continuityAnchor: t.continuityAnchor,
    optional: t.optional,
    sourceSignature: Ic(t),
    stale: !1
  };
}
function Ic(e) {
  return pt(
    JSON.stringify([
      e.prompt,
      e.promptContent || null,
      e.paramValues || null,
      e.localOutput || null,
      e.sourceSignatureParts || [],
      e.shotId || "",
      e.speechId || "",
      e.speechIds || [],
      e.characterId || "",
      e.speechKind || "",
      e.speakerMode || "",
      e.startTime ?? null,
      e.shotDuration ?? null,
      e.continuityAnchor || "",
      !!e.optional
    ])
  );
}
function Sc(e, t) {
  return e.sourceNodeId === t.sourceNodeId && e.itemType === t.itemType && e.itemId === t.itemId && e.generatedPrompt === t.generatedPrompt && $r(e.dependencyNodeIds, t.dependencyNodeIds) && $r(e.referenceNodeIds, t.referenceNodeIds) && $r(
    e.externalReferenceAssetIds,
    t.externalReferenceAssetIds
  ) && e.shotId === t.shotId && e.speechId === t.speechId && $r(e.speechIds, t.speechIds) && e.characterId === t.characterId && e.speechKind === t.speechKind && e.speakerMode === t.speakerMode && e.startTime === t.startTime && e.shotDuration === t.shotDuration && e.continuityAnchor === t.continuityAnchor && !!e.optional == !!t.optional && e.sourceSignature === t.sourceSignature && e.resultSourceSignature === t.resultSourceSignature && !!e.stale == !!t.stale;
}
function gr(e, t, n, r) {
  const o = e.storyboardItem;
  return !!(o && o.sourceNodeId === t && o.itemType === n && o.itemId === r);
}
function Im(e, t, n, r) {
  const o = r.map((u) => ({
    spec: u,
    group: t.find(
      (l) => l.type === "group" && l.group?.origin === "script" && l.group.sourceNodeId === n && l.group.syncKey === u.key
    )
  })).filter(
    (u) => !!u.group
  ), s = new Set(o.map(({ group: u }) => u.id)), i = /* @__PURE__ */ new Set([n, ...s]), a = e.filter((u) => {
    const l = u.logicalFrom || u.from, f = u.logicalTo || u.to;
    return !(i.has(l) && i.has(f));
  });
  for (const { spec: u, group: l } of o) {
    const f = u.direction === "upstream", b = (u.sourceGroupKeys || []).map((w) => Es(t, n, w)).filter((w) => !!w), _ = f ? [l] : b.length ? b : [t.find((w) => w.id === n)].filter(
      (w) => !!w
    );
    for (const w of _) {
      const D = w.id, A = f ? n : l.id;
      a.push({
        id: zs(
          a,
          `script-edge-${pt(n)}-${u.key}-${pt(D)}`
        ),
        from: D,
        to: A,
        logicalFrom: D,
        logicalTo: A,
        executionMode: f ? void 0 : "manual"
      });
    }
  }
  const d = Xt(t, a);
  return Cc(e, d) ? e : d;
}
function Sm(e, t, n) {
  const r = `script-item-edge-${pt(n)}-`, o = e.filter((a) => !a.id.startsWith(r)), s = t.filter(
    (a) => a.storyboardItem?.sourceNodeId === n && (!!a.groupId || a.storyboardItem.itemType === "video_compose")
  ), i = new Map(s.map((a) => [a.id, a]));
  for (const a of s)
    for (const d of a.storyboardItem?.dependencyNodeIds || []) {
      const u = i.get(d);
      !u || u.id === a.id || o.push({
        id: zs(
          o,
          `${r}${pt(u.id)}-${pt(a.id)}`
        ),
        from: u.id,
        to: a.id,
        logicalFrom: u.id,
        logicalTo: a.id,
        executionMode: u.groupId && u.groupId === a.groupId ? void 0 : "manual"
      });
    }
  return Xt(t, o);
}
function vc(e) {
  const t = e.nodes.find(
    (a) => gr(
      a,
      e.storyboardNode.id,
      "video_compose",
      "composition"
    )
  ), n = nm({
    storyboard: e.storyboard,
    sourceNodeId: e.storyboardNode.id,
    nodes: e.nodes,
    current: t?.composerDraft?.videoComposition
  }), r = vm(n), o = {
    sourceNodeId: e.storyboardNode.id,
    itemType: "video_compose",
    itemId: "composition",
    generatedPrompt: "",
    sourceSignature: r,
    stale: !1
  };
  if (t) {
    const a = e.preservePosition ? { x: t.x, y: t.y } : Di(e.nodes, e.storyboardNode), d = bc(t), u = t.storyboardItem?.resultSourceSignature || (d ? t.storyboardItem?.sourceSignature : "");
    u && (o.resultSourceSignature = u), o.stale = !!(d && u !== r);
    const l = !t.power && !!e.power, f = JSON.stringify(t.composerDraft?.videoComposition || null) !== JSON.stringify(n), b = t.x !== a.x || t.y !== a.y;
    if (!l && !f && !b && t.kind === "video" && t.outputType === "video_compose" && Sc(t.storyboardItem, o))
      return {
        node: t,
        changed: !1,
        nextNodeNo: e.nextNodeNo
      };
    const _ = {
      ...t,
      x: a.x,
      y: a.y,
      kind: "video",
      outputType: "video_compose",
      ...l ? {
        power: e.power || void 0,
        subtitle: e.power?.output?.name || e.power?.name || "视频合成",
        description: "按镜头顺序合成画面、原声和配音。"
      } : {},
      composerDraft: {
        ...t.composerDraft || {},
        videoComposition: n
      },
      storyboardItem: o
    };
    return e.nodes[e.nodes.indexOf(t)] = _, {
      node: _,
      changed: !0,
      nextNodeNo: e.nextNodeNo
    };
  }
  if (e.createMissing === !1)
    return null;
  const s = Di(
    e.nodes,
    e.storyboardNode
  ), i = fo(
    "power",
    e.assetCate,
    e.nodes.length,
    s,
    e.power ? { power: e.power } : void 0
  );
  return i.id = Fs(
    e.nodes,
    `script-compose-${pt(e.storyboardNode.id)}`
  ), i.nodeNo = e.nextNodeNo, i.title = "视频合成", i.kind = "video", i.outputType = "video_compose", i.description = "按镜头顺序合成画面、原声和配音。", e.power || (i.subtitle = "未配置视频合成能力", i.description = "未配置视频合成能力。配置并启用后可生成最终视频。"), i.composerDraft = { videoComposition: n }, i.storyboardItem = o, e.nodes.push(i), {
    node: i,
    changed: !0,
    nextNodeNo: e.nextNodeNo + 1
  };
}
function vm(e) {
  const t = e.clips.map((n) => {
    const r = { ...n };
    return delete r.storyboardTransitionToNext, r;
  });
  return pt(JSON.stringify({ ...e, clips: t }));
}
function Di(e, t) {
  const n = e.filter(
    (s) => s.type === "group" && s.group?.origin === "script" && s.group.sourceNodeId === t.id
  ), r = n.reduce(
    (s, i) => Math.max(s, i.x + i.width),
    t.x + t.width + 160
  ), o = n.reduce(
    (s, i) => Math.min(s, i.y),
    t.y
  );
  return { x: r + 72, y: o };
}
function Cm(e, t, n, r, o) {
  const i = ["shots", "speech", "subtitles", "lip_sync"].filter(
    (l) => o.some((f) => f.key === l)
  ).map((l) => Es(t, n, l)).filter((l) => !!l), a = /* @__PURE__ */ new Set([
    n,
    ...t.filter(
      (l) => l.type === "group" && l.group?.origin === "script" && l.group.sourceNodeId === n
    ).map((l) => l.id)
  ]), d = e.filter((l) => {
    const f = l.logicalFrom || l.from;
    return !((l.logicalTo || l.to) === r && a.has(f));
  });
  for (const l of i.length ? i : t.filter((f) => f.id === n))
    d.push({
      id: zs(
        d,
        `script-compose-edge-${pt(n)}-${pt(l.id)}`
      ),
      from: l.id,
      to: r,
      logicalFrom: l.id,
      logicalTo: r,
      executionMode: "manual"
    });
  const u = Xt(t, d);
  return Cc(e, u) ? e : u;
}
function Rm(e, t) {
  const n = `script-compose-edge-${pt(t)}-`, r = e.filter((o) => !o.id.startsWith(n));
  return r.length === e.length ? e : r;
}
function Cc(e, t) {
  return e.length === t.length && e.every((n, r) => {
    const o = t[r];
    return n.id === o.id && n.from === o.from && n.to === o.to && (n.logicalFrom || "") === (o.logicalFrom || "") && (n.logicalTo || "") === (o.logicalTo || "") && (n.executionMode || "auto") === (o.executionMode || "auto");
  });
}
function Lr(e, t, n) {
  return `${e}\0${t}\0${n}`;
}
function Fs(e, t) {
  return Rc(new Set(e.map((n) => n.id)), t);
}
function zs(e, t) {
  return Rc(new Set(e.map((n) => n.id)), t);
}
function Rc(e, t) {
  if (!e.has(t))
    return t;
  let n = 2;
  for (; e.has(`${t}-${n}`); )
    n += 1;
  return `${t}-${n}`;
}
function pt(e) {
  let t = 2166136261;
  for (const n of e)
    t ^= n.codePointAt(0) || 0, t = Math.imul(t, 16777619);
  return (t >>> 0).toString(36);
}
function Ei(e) {
  return String(e || "general").trim().toLowerCase() || "general";
}
function km(e) {
  return e.outputType === "speech" ? "语音合成" : e.outputType === "lip_sync" ? "口型同步" : e.powerKind === "image" ? "图片" : "视频";
}
function $r(e, t) {
  return JSON.stringify(e || []) === JSON.stringify(t || []);
}
function xm(e) {
  const t = /* @__PURE__ */ new Set();
  return e.filter((n) => t.has(n.id) ? !1 : (t.add(n.id), !0));
}
function Am(e) {
  return [
    e.id,
    Number(e.resultRef?.version_id || 0),
    Number(e.asset?.version_id || e.asset?.version?.id || 0),
    e.storyboardItem?.sourceSignature || "",
    e.storyboardItem?.resultSourceSignature || ""
  ].join(":");
}
const Tm = {
  characters: { section: "materials", materialType: "character" },
  scenes: { section: "materials", materialType: "scene" },
  props: { section: "materials", materialType: "prop" }
};
function kc(e) {
  if (!e)
    return;
  if (e.type === "group") {
    const n = String(e.group?.syncKey || "");
    return Tm[n] || { section: "shots" };
  }
  const t = e.storyboardItem;
  if (t)
    return t.itemType === "character" || t.itemType === "scene" || t.itemType === "prop" ? {
      section: "materials",
      materialType: t.itemType,
      materialId: t.itemId
    } : t.itemType === "video_compose" ? { section: "shots" } : {
      section: "shots",
      shotId: t.shotId || t.itemId
    };
}
const Fi = 52, zi = 72, Mm = 48, Pm = {
  width: 360,
  height: 52
};
function rs(e) {
  return $s(e);
}
function Oi(e, t) {
  if (t.storyboardItem?.sourceNodeId)
    return t.storyboardItem.sourceNodeId;
  if (t.type === "group" && t.group?.origin === "script")
    return t.group.sourceNodeId || "";
  if ($s(e).has(t.id))
    return t.id;
  if (!t.groupId)
    return "";
  const r = e.find((o) => o.id === t.groupId);
  return r?.group?.origin === "script" && r.group.sourceNodeId || "";
}
function xc(e, t) {
  const n = $s(e), r = [];
  for (const o of n) {
    const s = e.find((f) => f.id === o);
    if (!s)
      continue;
    const i = e.filter(
      (f) => f.type === "group" && f.group?.origin === "script" && f.group.sourceNodeId === o
    ), a = new Set(i.map((f) => f.id)), d = e.filter(
      (f) => f.id === o || f.group?.sourceNodeId === o || f.storyboardItem?.sourceNodeId === o || !!(f.groupId && a.has(f.groupId))
    );
    if (d.length <= 1)
      continue;
    const u = d.filter(
      (f) => f.storyboardItem?.sourceNodeId === o && !f.storyboardItem.optional
    ), l = zm(d);
    r.push({
      id: Os(o),
      sourceNodeId: o,
      title: s.title || "分镜脚本",
      memberNodeIds: d.map((f) => f.id),
      workNodeIds: u.map((f) => f.id),
      groupCount: i.length,
      workNodeCount: u.length,
      completedCount: u.filter(
        (f) => !f.storyboardItem?.stale && t(f)
      ).length,
      bounds: l
    });
  }
  return r.sort(
    (o, s) => o.bounds.y - s.bounds.y || o.bounds.x - s.bounds.x
  );
}
function Ac(e, t, n) {
  const r = new Map(t.map((l) => [l.id, l])), o = e.workNodeIds.map((l) => r.get(l)).filter((l) => !!l), s = new Set(
    o.filter((l) => l.storyboardItem?.stale || !n(l)).map((l) => l.id)
  );
  let i = !0;
  for (; i; ) {
    i = !1;
    for (const l of o)
      s.has(l.id) || l.storyboardItem?.itemType === "video_compose" || Fm(l).some(
        (f) => s.has(f)
      ) && (s.add(l.id), i = !0);
  }
  const a = o.find(
    (l) => l.storyboardItem?.itemType === "video_compose"
  );
  s.size > 0 && a && s.add(a.id);
  const d = o.filter(
    (l) => s.has(l.id)
  );
  if (d.length === 0)
    return { pendingNodeIds: [], blockedReason: "制作区已完成" };
  const u = d.find(
    (l) => !po(l)
  );
  return u ? {
    pendingNodeIds: d.map((l) => l.id),
    blockedReason: `“${u.title || "未命名节点"}”未配置可用能力`
  } : {
    pendingNodeIds: d.map((l) => l.id),
    blockedReason: za({
      targets: d,
      nodesByID: r,
      hasResult: n
    })
  };
}
function Dm(e, t, n) {
  let r = !1;
  const o = e.map((s) => {
    const i = s.storyboardItem;
    return !i || i.sourceNodeId !== t || !n.has(s.id) ? s : (r = !0, {
      ...s,
      storyboardItem: {
        ...i,
        resultSourceSignature: i.sourceSignature || i.resultSourceSignature,
        stale: !1
      }
    });
  });
  return r ? o : e;
}
function $i(e, t) {
  return t ? {
    x: e.bounds.x,
    y: e.bounds.y,
    ...Pm
  } : e.bounds;
}
function Em(e, t, n) {
  const r = Tc(t, n);
  if (r.x === 0 && r.y === 0)
    return e;
  const o = new Set(t.memberNodeIds);
  return e.map(
    (s) => o.has(s.id) ? { ...s, x: s.x + r.x, y: s.y + r.y } : s
  );
}
function Tc(e, t) {
  return {
    x: t.x - e.bounds.x,
    y: t.y - e.bounds.y
  };
}
function Os(e) {
  return `storyboard-frame:${e}`;
}
function Fm(e) {
  return [
    ...e.storyboardItem?.dependencyNodeIds || [],
    ...e.storyboardItem?.referenceNodeIds || []
  ];
}
function $s(e) {
  const t = /* @__PURE__ */ new Set();
  for (const n of e)
    n.storyboardMaterializedSignature && t.add(n.id), n.group?.origin === "script" && n.group.sourceNodeId && t.add(n.group.sourceNodeId), n.storyboardItem?.sourceNodeId && t.add(n.storyboardItem.sourceNodeId);
  return t;
}
function zm(e) {
  let t = Number.POSITIVE_INFINITY, n = Number.POSITIVE_INFINITY, r = Number.NEGATIVE_INFINITY, o = Number.NEGATIVE_INFINITY;
  for (const s of e) {
    const i = ji(s.width, 180), a = ji(s.height, 180);
    t = Math.min(t, s.x), n = Math.min(n, s.y), r = Math.max(r, s.x + i), o = Math.max(o, s.y + a);
  }
  return {
    x: t - Fi,
    y: n - zi,
    width: r - t + Fi * 2,
    height: o - n + zi + Mm
  };
}
function ji(e, t) {
  return Number.isFinite(e) && e > 0 ? e : t;
}
function Om({ data: e }) {
  const t = e;
  return /* @__PURE__ */ v(
    "section",
    {
      className: `ws-storyboard-frame ${t.collapsed ? "is-collapsed" : ""}`,
      "aria-label": `${t.title} 分镜制作区`,
      children: [
        /* @__PURE__ */ v("header", { className: "ws-storyboard-frame-header", children: [
          /* @__PURE__ */ c("span", { className: "ws-storyboard-frame-icon", "aria-hidden": "true", children: /* @__PURE__ */ c(ad, { size: 15 }) }),
          /* @__PURE__ */ v("strong", { children: [
            t.title,
            " · 分镜制作区"
          ] }),
          /* @__PURE__ */ v("span", { className: "ws-storyboard-frame-progress", children: [
            t.groupCount,
            " 组 · ",
            t.completedCount,
            "/",
            t.workNodeCount,
            " ",
            "完成"
          ] }),
          /* @__PURE__ */ c(
            Je,
            {
              label: t.running ? "制作区正在执行" : t.runBlockedReason || "按依赖层级并行执行制作区",
              children: /* @__PURE__ */ c(
                "button",
                {
                  type: "button",
                  className: "nodrag nopan ws-storyboard-frame-run",
                  "aria-label": "执行制作区",
                  disabled: t.running || !!t.runBlockedReason,
                  onClick: Eo(t.onRun),
                  children: t.running ? /* @__PURE__ */ c(Bt, { size: 14, className: "ws-spin" }) : /* @__PURE__ */ c(lo, { size: 14, fill: "currentColor" })
                }
              )
            }
          ),
          /* @__PURE__ */ c(Je, { label: "聚焦制作区", children: /* @__PURE__ */ c(
            "button",
            {
              type: "button",
              className: "nodrag nopan",
              "aria-label": "聚焦制作区",
              onClick: Eo(t.onFocus),
              children: /* @__PURE__ */ c(ul, { size: 14 })
            }
          ) }),
          /* @__PURE__ */ c(Je, { label: t.collapsed ? "展开制作区" : "折叠制作区", children: /* @__PURE__ */ c(
            "button",
            {
              type: "button",
              className: "nodrag nopan",
              "aria-label": t.collapsed ? "展开制作区" : "折叠制作区",
              onClick: Eo(t.onToggleCollapsed),
              children: t.collapsed ? /* @__PURE__ */ c(uo, { size: 15 }) : /* @__PURE__ */ c(od, { size: 15 })
            }
          ) })
        ] }),
        t.collapsed ? null : /* @__PURE__ */ c("div", { className: "ws-storyboard-frame-surface", "aria-hidden": "true" })
      ]
    }
  );
}
function Eo(e) {
  return (t) => {
    t.preventDefault(), t.stopPropagation(), e();
  };
}
const Un = /* @__PURE__ */ new Map(), $m = 100;
function jm(e, t) {
  const n = String(t.runError || "").trim(), r = t.id, o = Number(t.resultRef?.execution_id || 0), s = String(t.resultRef?.request_id || "").trim(), i = Number(t.resultRef?.run_id || 0), a = Um(
    e,
    r,
    o,
    s,
    i
  ), [d, u] = $(n), [l, f] = $(!1);
  return le(() => {
    if (u(n), !n || !a || !Mf(n)) {
      f(!1);
      return;
    }
    let b = !0;
    return f(!0), Bm({
      projectId: e,
      nodeId: r,
      executionId: o,
      requestId: s,
      runId: i,
      cacheKey: a,
      fallback: n
    }).then((_) => {
      b && _ && u(_);
    }).catch(() => {
    }).finally(() => {
      b && f(!1);
    }), () => {
      b = !1;
    };
  }, [
    o,
    n,
    r,
    e,
    a,
    s,
    i
  ]), { error: d || n, loading: l };
}
function Bm({
  projectId: e,
  nodeId: t,
  executionId: n,
  requestId: r,
  runId: o,
  cacheKey: s,
  fallback: i
}) {
  const a = Un.get(s);
  if (a)
    return a;
  const d = Ea({
    projectId: e,
    executionId: n,
    requestId: r,
    runId: o
  }).then((u) => {
    const l = Wn(u), f = [...l.node_results || []].reverse().find((_) => _.node_key === t), b = Is(f) || Ka(l);
    return Ss(b, i);
  });
  return Un.set(s, d), Vm(), d.catch(() => Un.delete(s)), d;
}
function Um(e, t, n, r, o) {
  const s = n ? `execution:${n}` : r ? `request:${r}` : o ? `run:${o}` : "";
  return e > 0 && s ? `${e}:${s}:${t}` : "";
}
function Vm() {
  for (; Un.size > $m; ) {
    const e = Un.keys().next().value;
    if (!e)
      return;
    Un.delete(e);
  }
}
function Km({
  space: e,
  canvases: t,
  cache: n
}) {
  const [r, o] = $([]), [s, i] = $([]), [a, d] = $([]), [u, l] = $(!1), f = `${e?.project.id || 0}:${e?.release.id || e?.project.release_id || 0}`, b = te(f), _ = ue(
    () => Lm(t),
    [t]
  );
  le(() => {
    b.current = f, o([]), i([]), d([]), l(!1);
  }, [f]);
  const w = P(
    async (D = !1) => {
      if (!e)
        return !1;
      const A = f;
      try {
        const z = await n.loadCatalog(
          e.project.id,
          Number(e.release?.id || e.project.release_id || 0),
          () => Cl(e.project.id),
          D
        );
        return b.current !== A ? !1 : (o(z.roles), i(z.powers), d(z.powerCategories), l(!0), !0);
      } catch (z) {
        return ne.error(z instanceof Error ? z.message : "加载能力列表失败"), !1;
      }
    },
    [n, f, e]
  );
  return le(() => {
    !e || !_ || u || w();
  }, [w, u, _, e]), {
    roles: r,
    powers: s,
    powerCategories: a,
    loaded: u,
    required: _,
    load: w
  };
}
function Lm(e) {
  return Object.values(e).some(
    (t) => t.nodes.some((n) => n.type !== "power" ? !1 : n.storyboardItem && !n.power ? !0 : bs(n.power, n.kind, n.outputType) ? !!br([
      n.asset?.version?.content,
      n.resultOutput
    ]) : !1)
  );
}
const qm = Nt(
  () => import("./interaction-panel-8pDUKmyq.js").then((e) => e.default).then((e) => ({
    default: e.AgentInteractionPanel
  }))
), Gm = Nt(
  () => import("./space-add-node-menu-BVYHjUGM.js").then((e) => ({
    default: e.AddNodeMenu
  }))
), Mc = Nt(
  () => import("./upload-asset-api-DAbIOMVJ.js").then((e) => e.b4).then((e) => ({
    default: e.AssetAudioPreview
  }))
), Hm = Nt(
  () => import("./upload-asset-api-DAbIOMVJ.js").then((e) => e.b5).then((e) => ({
    default: e.AssetBrowser
  }))
), Ym = Nt(
  () => import("./asset-reference-provider-qeDIBhpP.js").then((e) => e.a).then((e) => ({
    default: e.AssetPickerDialog
  }))
), Wm = Nt(
  () => import("./space-run-history-B_O8H-Nh.js").then((e) => ({
    default: e.CanvasRunHistoryDrawer
  }))
), Xm = Nt(
  () => import("./space-agent-result-DkqYRIQu.js").then((e) => ({
    default: e.CanvasAgentResultContent
  }))
), Jm = Nt(
  () => import("./space-group-node-Du5xiX5M.js").then((e) => ({
    default: e.CanvasGroupNodeView
  }))
), Pc = Nt(
  () => import("./space-result-view-CynzVjJS.js").then((e) => ({
    default: e.CanvasResultView
  }))
), Zm = Nt(
  () => import("./node-detail-dialog-CBSi8L6_.js").then((e) => ({
    default: e.NodeDetailDialog
  }))
), Qm = Nt(
  () => import("./space-video-compose-view-DmkdHGLn.js").then((e) => e.s).then((e) => ({
    default: e.VideoComposeView
  }))
), eg = Nt(
  () => import("./space-storyboard-reference-editor-CjJ-kyNh.js").then((e) => ({
    default: e.StoryboardInputReferenceEditor
  }))
), tg = Nt(
  () => import("./space-storyboard-node-DFkYyl00.js").then((e) => ({
    default: e.StoryboardNodeContent
  }))
), { normalizeAgentResultOutputValue: ng } = ca(
  "@/lib/agent-result-protocol"
), rg = {}, Bi = [], og = [], os = [], sg = 800;
function un(e, t) {
  return ss(e, /* @__PURE__ */ new Set([t]));
}
function ig(e) {
  const t = te([]), n = te(0), r = P(() => {
    n.current && (window.cancelAnimationFrame(n.current), n.current = 0);
    const s = t.current;
    t.current = [], s.length !== 0 && e(
      (i) => s.reduce((a, d) => d(a), i)
    );
  }, [e]), o = P(
    (s) => {
      t.current.push(s), !n.current && (n.current = window.requestAnimationFrame(() => {
        n.current = 0, r();
      }));
    },
    [r]
  );
  return le(
    () => () => {
      n.current && window.cancelAnimationFrame(n.current), t.current = [];
    },
    []
  ), ue(() => ({ enqueue: o, flush: r }), [o, r]);
}
function ss(e, t) {
  let n = null;
  for (const r of t)
    Object.prototype.hasOwnProperty.call(e, r) && (n ||= { ...e }, delete n[r]);
  return n || e;
}
function Dc(e, t) {
  const n = /* @__PURE__ */ new Set();
  for (const r of t)
    if (n.add(r.id), r.type === "group")
      for (const o of Hd(e, r.id))
        n.add(o.id);
  return n;
}
function ag(e) {
  return Object.values(e).some(Pt);
}
function Pt(e) {
  return e?.status === "running" || e?.status === "waiting";
}
function cg(e) {
  return e instanceof Element && e.classList.contains("react-flow__pane");
}
function ug(e, t, n) {
  return {
    left: Math.min(e.x, t.x) - n.left,
    top: Math.min(e.y, t.y) - n.top,
    width: Math.abs(t.x - e.x),
    height: Math.abs(t.y - e.y)
  };
}
function dg(e, t, n) {
  const r = {
    left: Math.min(t.x, n.x),
    top: Math.min(t.y, n.y),
    right: Math.max(t.x, n.x),
    bottom: Math.max(t.y, n.y)
  };
  return e.filter((o) => {
    const s = Nu(o), i = o.x + s.width, a = o.y + s.height;
    return o.type === "group" ? r.left <= o.x && r.top <= o.y && r.right >= i && r.bottom >= a : r.left <= i && r.right >= o.x && r.top <= a && r.bottom >= o.y;
  }).map((o) => o.id);
}
function lg(e, t) {
  return [.../* @__PURE__ */ new Set([...e, ...t])];
}
const fg = {
  id: 0,
  name: "上传",
  key: "files",
  type: "files",
  usage: 2,
  max_files: 6
}, pg = [fg], mg = {
  workSpace: Rh,
  storyboardFrame: Om
}, gg = {
  animated: np
};
function yg({
  onInitialLoadComplete: e
}) {
  const t = xu(), n = fd(), r = ue(() => Mh(), []), o = ue(() => new Bl(), []), [s, i] = $(null), [a, d] = $(0), u = te(0), [l, f] = $(null), b = te(null), [_, w] = $([]), D = _[_.length - 1] || "", [A, z] = $({}), V = te(A), [R, G] = $("create"), { resolvedTheme: ee, setTheme: C } = Au();
  pd(n.site.appearance, ee);
  const [T, O] = $(null), [H, re] = $(!0), X = te(!0), [oe, J] = $({}), Z = ig(J), [fe, ge] = $({}), [_e, ve] = $(null), [at, M] = $(), [we, Ue] = $(
    null
  ), [Pe, ct] = $(null), [Ze, Et] = $(!1), [W, de] = $(""), [Qe, De] = $(""), [Ce, Ve] = $([]), [Ee, Vt] = $([]), [yn, hn] = $(!1), [_t, qe] = $(""), [Ft, et] = $(!1), [ut, At] = $(1), [mt, gt] = $(!1), [Re, tt] = $(null), [zt, nt] = $(!1), Ie = te(null), rt = te(/* @__PURE__ */ new Set()), yt = te(/* @__PURE__ */ new Set()), It = te(/* @__PURE__ */ new Set()), Ke = te([]), dt = te(!1), rn = te(!1), Kt = te([0]), Lt = te(null), Ge = te(null), {
    roles: Se,
    powers: Fe,
    powerCategories: k,
    loaded: j,
    required: U,
    load: me
  } = Km({
    space: s,
    canvases: A,
    cache: o
  });
  le(() => {
    V.current = A;
  }, [A]), le(() => {
    u.current = a;
  }, [a]), le(() => {
    Ke.current = Ce;
  }, [Ce]);
  const Ae = P((m) => {
    ne.error(m instanceof Error ? m.message : "保存画布失败");
  }, []), { markCanvasDirty: Le, resetCanvasAutosave: qt, canvasSaveStatus: vr } = Ol({
    projectId: r,
    enabled: !!s,
    canvases: A,
    setCanvases: z,
    onError: Ae
  });
  le(() => {
    if (It.current.size === 0)
      return;
    const m = [...It.current];
    It.current.clear();
    for (const y of m)
      Le(y);
  }, [A, Le]);
  const Ot = P(async () => {
    if (!r) {
      De("缺少作品 ID"), re(!1);
      return;
    }
    re(!0), De("");
    try {
      const m = await Sl(
        r,
        u.current
      ), y = Xy(
        m.canvases || {},
        m.assets || []
      ), h = Number(m.initialAssetCateId || 0) || Fd(m);
      i(m), V.current = y, z(y), qt(y), u.current = h, d(h), f(null), b.current = null, rt.current = /* @__PURE__ */ new Set(), yt.current = /* @__PURE__ */ new Set(), Ke.current = [], Ve([]), Vt([]), At(1), gt(!1), Kt.current = [0], sn(
        r,
        m,
        y,
        "recovery",
        { assetCateId: h }
      );
    } catch (m) {
      De(m instanceof Error ? m.message : "加载创作空间失败");
    } finally {
      re(!1);
    }
  }, [r, qt]), _o = P((m) => {
    Ue(m);
  }, []);
  le(() => {
    Ot();
  }, [Ot]), le(() => {
    H || !X.current || (X.current = !1, e());
  }, [H, e]);
  const $t = s?.assetCates, Mn = ue(() => s ? zd(s) : [], [s]), Jn = s ? s.assetCates.length > 0 : !1, Y = ue(
    () => s ? Ur(s, a) : null,
    [a, s]
  ), Pn = ue(
    () => s && Y ? Od(s, Y.id) : [],
    [Y, s]
  ), Zn = ue(() => Se.filter($d), [Se]), Tt = ue(() => Fe.filter(jd), [Fe]), Io = ue(() => Pn, [Pn]), ye = ue(
    () => Y ? A[String(Y.id)] || Er(Y.id) : Er(0),
    [Y, A]
  ), So = ue(
    () => Object.entries(A).map(
      ([m, y]) => `${m}:${mm(y)}`
    ).join("|"),
    [A]
  ), $e = ue(
    () => Sg(ye, fe),
    [ye, fe]
  ), Cr = ue(
    () => Rf({
      nodes: $e.nodes,
      assets: s?.assets || [],
      assetCateId: Y?.id || 0,
      nodeOutput: nn,
      nodePreview: Xn,
      assetPreview: (m) => {
        const y = m.version?.content ?? m.name, h = gn(
          y,
          String(m.kind || "")
        );
        return pn(h) || (h.text = m.name), h;
      },
      nodeHasResult: ft
    }),
    [Y?.id, $e.nodes, s?.assets]
  ), Rr = ue(
    () => Gy(Cr),
    [Cr]
  );
  le(() => {
    !$t || U && !j || z((m) => {
      let y = m;
      for (const [h, I] of Object.entries(m)) {
        const S = Number(h || I.assetCateId || 0), K = ns({
          canvas: I,
          assetCate: ii($t, S),
          powers: Fe
        }), F = ra(K, S);
        oa(I, F) || (y === m && (y = { ...m }), y[h] = F, It.current.add(S));
      }
      return y;
    });
  }, [
    $t,
    j,
    U,
    Fe,
    So
  ]);
  const Qn = P(
    (m = "") => {
      de(m), Ie.current = ye.nodes.find((y) => y.id === m) || (Ie.current?.id === m ? Ie.current : null), O(null), G("create"), Et(!0);
    },
    [ye.nodes]
  ), Gt = P(
    (m, y) => {
      if (!Number.isInteger(m) || m < 0)
        return;
      const h = (K) => {
        const F = String(m), L = K[F] || Er(m), se = ra(
          y(L),
          m
        );
        return oa(L, se) ? K : (It.current.add(m), {
          ...K,
          [F]: se
        });
      }, I = V.current, S = h(I);
      V.current = S, z((K) => {
        const F = K === I ? S : h(K);
        return V.current = F, F;
      });
    },
    []
  ), be = P(
    (m) => {
      Y && Gt(Y.id, m);
    },
    [Y, Gt]
  ), er = P(
    (m, y, h) => {
      ge(
        (I) => vg(I, y, h)
      ), Gt(m, (I) => {
        const S = {
          ...I,
          nodes: I.nodes.map(
            (F) => F.id === y ? { ...F, ...h } : F
          )
        };
        return $t ? ns({
          canvas: S,
          assetCate: ii($t, m),
          powers: Fe
        }) : S;
      });
    },
    [$t, Fe, Gt]
  ), He = P(
    (m, y) => {
      er(Number(Y?.id || 0), m, y), ve(
        (h) => h?.id === m ? { ...h, ...y } : h
      );
    },
    [Y?.id, er]
  ), Ht = P(
    (m, y, h) => {
      if (!cy(y, h))
        return;
      const I = Jc(h), S = `${y.id}:${I}`;
      if (rt.current.has(S))
        return;
      rt.current.add(S);
      const K = y.title.trim(), F = V.current[String(m)];
      kl({
        projectId: r,
        nodeKey: y.id,
        versionId: I,
        prompt: ay(y, F)
      }).then((L) => {
        const se = L.title.trim();
        !se || se === K || L.versionId !== I || Gt(m, (Q) => {
          const q = Q.nodes.find(
            (ie) => ie.id === y.id
          );
          return !q || q.titleMode !== "auto" || q.title.trim() !== K || !Xc(q) ? Q : {
            ...Q,
            nodes: Q.nodes.map(
              (ie) => ie.id === y.id ? { ...ie, title: se } : ie
            )
          };
        });
      }).catch(() => {
        rt.current.delete(S);
      });
    },
    [r, Gt]
  ), on = P(
    async (m) => {
      const y = Number(m.assetCate.id || 0);
      y && Le(y);
    },
    [Le]
  ), kr = P(
    (m, y) => {
      be((h) => ({
        ...h,
        nodes: h.nodes.map(
          (I) => I.id === m ? {
            ...I,
            composerDraft: js(y)
          } : I
        )
      }));
    },
    [be]
  ), xr = P(
    (m, y) => {
      M(y), ve(m);
    },
    []
  ), Dn = P(
    (m, y) => {
      ge((h) => {
        const I = ye.nodes.find(
          (F) => F.id === m
        );
        if (!I)
          return h;
        const S = h[m] || {}, K = {
          ...I,
          ...S
        };
        return {
          ...h,
          [m]: {
            ...S,
            feedbackRequests: y(kn(K))
          }
        };
      });
    },
    [ye.nodes]
  ), wn = P((m) => {
    const y = new Set(m.filter(Boolean));
    if (y.size !== 0) {
      if (Ge.current && y.has(Ge.current.nodeId)) {
        const h = Ge.current;
        Ge.current = null, h.reject(new Error(Ya));
      }
      tt(
        (h) => h && y.has(h.node.id) ? null : h
      ), ge((h) => {
        const I = { ...h };
        let S = !1;
        for (const K of y) {
          const F = I[K] || {};
          I[K] = {
            ...F,
            feedbackRequests: []
          }, S = !0;
        }
        return S ? I : h;
      });
    }
  }, []), lt = P((m) => {
    !m || !m.id || i((y) => y && {
      ...y,
      assets: li(y.assets, [m])
    });
  }, []), pe = P(
    ({ node: m, prompt: y }) => {
      const h = Bs(m, y), I = is(
        kn(m),
        h
      );
      return Dn(
        m.id,
        (S) => is(S, h)
      ), nt(!1), new Promise((S, K) => {
        Ge.current = {
          nodeId: m.id,
          recordId: h.id,
          resolve: S,
          reject: K
        }, tt({
          node: { ...m, feedbackRequests: I },
          recordId: h.id,
          prompt: y
        });
      });
    },
    [Dn]
  ), bn = P(
    async (m) => {
      const y = Ge.current;
      if (!(!y || zt)) {
        nt(!0);
        try {
          await y.submit?.(m), Dn(
            y.nodeId,
            (h) => Vf(h, y.recordId, m)
          ), Ge.current = null, tt(null), y.resolve(m);
        } catch (h) {
          ne.error(h instanceof Error ? h.message : "提交反馈失败");
        } finally {
          nt(!1);
        }
      }
    },
    [Dn, zt]
  ), En = P(() => {
    tt(null);
  }, []), Fn = P(
    (m, y) => {
      if (Ge.current?.nodeId === m.id && Ge.current.recordId === y.id && y.status === "pending") {
        tt({
          node: m,
          recordId: y.id,
          prompt: y.prompt
        });
        return;
      }
      if (y.status === "pending") {
        const h = iy(
          Ke.current,
          m,
          y
        );
        if (h) {
          nt(!1), Ge.current = {
            nodeId: m.id,
            recordId: y.id,
            resolve: () => {
            },
            reject: () => {
            },
            submit: async (I) => {
              await Yc(
                r,
                h.run,
                h.pending,
                h.prompt,
                I
              ), ne.success("已提交反馈，流程继续执行"), window.setTimeout(() => Lt.current?.(), 0);
            }
          }, tt({
            node: m,
            recordId: y.id,
            prompt: h.prompt
          });
          return;
        }
      }
      tt({
        node: m,
        recordId: y.id,
        prompt: {
          ...y.prompt,
          values: y.values || y.prompt.values || {}
        }
      });
    },
    [r]
  ), Nn = P(
    async (m) => {
      if (!s || !Y)
        return;
      let y = null;
      try {
        wn(
          Kl(
            m.id,
            $e.nodes,
            $e.edges
          )
        ), y = {
          projectId: r,
          assetCate: Y,
          space: s,
          startNode: m,
          nodes: $e.nodes,
          edges: $e.edges,
          viewport: ye.viewport,
          onNodeResult: He,
          onAssetCreated: lt,
          setRunningNode: J,
          runningNodeBatcher: Z,
          requestFlowFeedback: pe,
          requestNodeTitle: (h, I) => Ht(Y.id, h, I)
        }, await Fo(y), await on(y), ne.success("开始节点执行完成");
      } catch (h) {
        if (Bf(h))
          return;
        const I = h instanceof Error ? h.message : "开始节点执行失败";
        J((S) => ({
          ...S,
          [m.id]: {
            nodeId: m.id,
            title: m.title,
            startedAt: Date.now(),
            progress: 92,
            status: "error"
          }
        })), ne.error(I), window.setTimeout(() => {
          J((S) => un(S, m.id));
        }, 1400);
      } finally {
        await sn(
          r,
          s,
          V.current,
          "recovery",
          { runIds: [Number(y?.canvasRun?.run_id || 0)] }
        );
      }
    },
    [
      Y,
      ye.viewport,
      $e.edges,
      $e.nodes,
      A,
      wn,
      r,
      Ht,
      pe,
      Z,
      on,
      J,
      s,
      He,
      lt
    ]
  ), St = P(
    async (m) => {
      if (!s || !Y)
        return;
      const y = Number(Y.id || 0), h = V.current[String(y)] || ye, I = h.nodes.find(
        (Q) => Q.id === m
      );
      if (!I) {
        ne.error("分镜脚本节点不存在");
        return;
      }
      const S = xc(
        h.nodes,
        ft
      ).find((Q) => Q.sourceNodeId === m);
      if (!S) {
        ne.error("当前分镜脚本尚未生成制作组");
        return;
      }
      const K = Ac(
        S,
        h.nodes,
        ft
      );
      if (K.blockedReason) {
        ne.error(K.blockedReason);
        return;
      }
      const F = Os(m), L = {
        projectId: r,
        assetCate: Y,
        space: s,
        startNode: I,
        executionScope: "storyboard_frame",
        patchStartNodeResult: !1,
        nodes: h.nodes,
        edges: h.edges,
        viewport: h.viewport,
        onNodeResult: He,
        onAssetCreated: lt,
        setRunningNode: J,
        runningNodeBatcher: Z,
        requestFlowFeedback: pe,
        requestNodeTitle: (Q, q) => Ht(y, Q, q)
      };
      wn(K.pendingNodeIds), J((Q) => ({
        ...Q,
        [F]: {
          nodeId: F,
          title: `${I.title || "分镜脚本"}制作区`,
          startedAt: Date.now(),
          progress: 0,
          status: "running"
        }
      }));
      let se = 650;
      try {
        await Fo(L), ne.success("制作区执行完成");
      } catch (Q) {
        se = 1400;
        const q = Q instanceof Error ? Q.message : "制作区执行失败";
        J((ie) => ({
          ...ie,
          [F]: {
            ...ie[F] || {
              nodeId: F,
              title: `${I.title || "分镜脚本"}制作区`,
              startedAt: Date.now(),
              progress: 0
            },
            status: "error"
          }
        })), ne.error(q);
      } finally {
        const Q = new Set(
          (L.canvasRun?.node_results || []).filter(
            (q) => en(q) === "success"
          ).map((q) => q.node_key).filter(Boolean)
        );
        Q.size > 0 && (be(
          (q) => Ui({
            canvas: q,
            sourceNodeId: m,
            successfulNodeIds: Q,
            assetCate: Y,
            powers: Fe
          })
        ), await on(L)), window.setTimeout(() => {
          J((q) => un(q, F));
        }, se), await sn(
          r,
          s,
          V.current,
          "recovery",
          { runIds: [Number(L.canvasRun?.run_id || 0)] }
        );
      }
    },
    [
      ye,
      Y,
      wn,
      on,
      Fe,
      r,
      Ht,
      pe,
      Z,
      s,
      be,
      He,
      lt
    ]
  ), Ar = P(
    async (m, y) => {
      if (!s || !Y)
        return;
      const h = V.current[String(Y.id)] || ye, I = h.nodes.find((se) => se.id === m.id) || m, S = kg({
        ...I,
        composerDraft: {
          ...I.composerDraft || {},
          ...m.composerDraft || {}
        }
      }), K = h.nodes.map(
        (se) => se.id === S.id ? S : se
      ), F = au(
        m.id,
        K,
        h.edges
      ), L = {
        projectId: r,
        assetCate: Y,
        space: s,
        startNode: S,
        singleNode: !0,
        nodes: K,
        edges: h.edges,
        viewport: h.viewport,
        runInput: {
          _manual_input_context: F || void 0,
          _agent_turn_input: y?.agentInput,
          manual_node_id: m.id
        },
        onNodeResult: He,
        onAssetCreated: lt,
        setRunningNode: J,
        runningNodeBatcher: Z,
        requestFlowFeedback: pe,
        requestNodeTitle: (se, Q) => Ht(Y.id, se, Q)
      };
      He(S.id, { runError: "" }), J((se) => ({
        ...se,
        [S.id]: {
          ...se[S.id] || {},
          nodeId: S.id,
          title: S.title,
          startedAt: se[S.id]?.startedAt || Date.now(),
          progress: Math.max(se[S.id]?.progress || 0, 8),
          status: "running"
        }
      }));
      try {
        await Fo(L), await on(L);
      } catch (se) {
        throw He(S.id, {
          runError: se instanceof Error ? se.message : "节点运行失败"
        }), J((Q) => ({
          ...Q,
          [S.id]: {
            ...Q[S.id] || {
              nodeId: S.id,
              title: S.title,
              startedAt: Date.now()
            },
            progress: 92,
            status: "error"
          }
        })), window.setTimeout(() => {
          J(
            (Q) => un(Q, S.id)
          );
        }, 1400), se;
      } finally {
        await sn(
          r,
          s,
          V.current,
          "recovery",
          { runIds: [Number(L.canvasRun?.run_id || 0)] }
        );
      }
    },
    [
      Y,
      ye,
      r,
      Ht,
      pe,
      Z,
      on,
      J,
      s,
      He,
      lt
    ]
  ), vo = P(
    async (m) => {
      if (!Y)
        throw new Error("当前分类不存在");
      return Iu({
        node: m,
        projectId: r,
        assetCate: Y,
        inputContext: m.inputContext || null,
        onNodeResult: He,
        onAssetCreated: lt,
        onRunStartNode: Nn,
        onOpenImportPicker: Qn
      });
    },
    [
      Y,
      Qn,
      r,
      Nn,
      He,
      lt
    ]
  );
  le(() => {
    s && nr(
      Ce,
      ye,
      a,
      s
    );
  }, [ye, a, Ce, s]);
  async function Tr(m) {
    if (b.current != null)
      return !1;
    const y = String(m);
    let h = !1;
    if (!Object.prototype.hasOwnProperty.call(V.current, y)) {
      b.current = m, f(m);
      try {
        const S = await vl({ projectId: r, assetCateId: m }), K = mu(
          Ld(S.canvas, Fe),
          S.assets
        );
        i(
          (L) => L && {
            ...L,
            assets: li(L.assets, S.assets)
          }
        );
        const F = {
          ...V.current,
          [y]: K
        };
        V.current = F, z(F), h = !0;
      } catch (S) {
        return ne.error(S instanceof Error ? S.message : "加载分类画布失败"), !1;
      } finally {
        b.current = null, f(null);
      }
    }
    if (u.current = m, d(m), w([]), ct(null), O(null), !s)
      return !0;
    const I = V.current[String(m)] || Er(m);
    return nr(Ce, I, m, s), h && sn(
      r,
      s,
      V.current,
      "recovery",
      { assetCateId: m }
    ), !0;
  }
  function tr(m) {
    ct((y) => ({
      nodeId: m,
      nonce: (y?.nonce || 0) + 1
    }));
  }
  const Co = P((m) => {
    ct((y) => !y || y.nodeId !== m.nodeId || y.nonce !== m.nonce ? y : null);
  }, []);
  async function sn(m, y, h, I, S = {}) {
    if (!dt.current) {
      dt.current = !0;
      try {
        const K = I === "active" ? Kc(Ke.current) : [], F = (S.runIds || []).filter((Ne) => Ne > 0), L = F.length > 0 ? F : I === "active" ? K.map((Ne) => Number(Ne.run_id || 0)) : [], se = I === "recovery" && L.length === 0;
        let Q = await xo({
          projectId: m,
          scope: I,
          assetCateId: S.assetCateId,
          runIds: L,
          summaryOnly: se
        }), q = zo(Q.items);
        if (se) {
          const Ne = Wg(q, h);
          Ne.length === 0 ? q = [] : (Q = await xo({
            projectId: m,
            scope: I,
            assetCateId: S.assetCateId,
            runIds: Ne
          }), q = zo(Q.items));
        }
        if (I === "active" && K.length > 0) {
          const Ne = new Set(q.map(oo)), jt = K.filter(
            (Ct) => !Ne.has(oo(Ct))
          );
          if (jt.length > 0) {
            const Ct = await Promise.all(
              jt.map(async (an) => {
                try {
                  const ht = await Ea({
                    projectId: m,
                    executionId: Number(an.execution_id || 0),
                    runId: Number(an.run_id || 0),
                    requestId: String(an.request_id || "")
                  });
                  return Lc(ht);
                } catch {
                  return null;
                }
              })
            );
            q = Hi(
              q,
              Ct.filter(
                (an) => !!an
              )
            );
          }
        }
        const ie = Hi(
          Ke.current,
          q
        );
        Ke.current = ie, Ve(ie);
        for (const [Ne, jt] of Object.entries(h)) {
          const Ct = Number(jt.assetCateId || Ne || 0);
          nr(q, jt, Ct, y);
        }
      } catch {
      } finally {
        dt.current = !1;
      }
    }
  }
  async function zn(m, y = 0) {
    if (rn.current)
      return;
    const h = Kt.current[y] || 0;
    rn.current = !0, hn(!0), qe("");
    try {
      const I = await xo({
        projectId: m,
        scope: "history",
        beforeId: h,
        limit: 20
      });
      Vt(
        zo(I.items)
      ), At(y + 1), gt(I.hasMore);
      const S = Kt.current.slice(0, y + 1);
      I.hasMore && I.beforeId > 0 && (S[y + 1] = I.beforeId), Kt.current = S;
    } catch (I) {
      qe(
        I instanceof Error ? I.message : "读取画布运行记录失败"
      );
    } finally {
      rn.current = !1, hn(!1);
    }
  }
  const Mr = Yg(
    Ce
  );
  Lt.current = s ? () => {
    sn(
      r,
      s,
      V.current,
      "active",
      { assetCateId: u.current }
    );
  } : null, le(() => {
    if (!s || !Mr)
      return;
    const m = window.setInterval(() => {
      Lt.current?.();
    }, Oc);
    return () => window.clearInterval(m);
  }, [Mr, r, s]);
  function nr(m, y, h, I) {
    const S = m.filter(
      (L) => Hg(L, h) && !qc(L, y)
    );
    if (S.length === 0)
      return;
    const K = /* @__PURE__ */ new Set(), F = /* @__PURE__ */ new Set();
    for (const L of S) {
      const se = new Set(
        Us(L).filter((q) => F.has(q) ? !1 : (F.add(q), !0))
      );
      if (se.size === 0)
        continue;
      const Q = (L.node_results || []).filter((q) => {
        const ie = q.node_key;
        return !ie || !se.has(ie) || K.has(ie) ? !1 : (K.add(ie), !0);
      });
      Ro(
        L,
        y,
        I,
        Q,
        se
      );
    }
  }
  function Ro(m, y, h, I = m.node_results || [], S) {
    const K = ty(m, y.nodes);
    if (!K)
      return;
    const F = Ur(h, y.assetCateId);
    if (!F)
      return;
    const L = {
      projectId: r,
      assetCate: F,
      space: h,
      startNode: K,
      nodes: y.nodes,
      edges: y.edges,
      viewport: y.viewport,
      onNodeResult: (Q, q) => er(y.assetCateId, Q, q),
      onAssetCreated: lt,
      setRunningNode: J,
      requestFlowFeedback: pe,
      requestNodeTitle: (Q, q) => Ht(y.assetCateId, Q, q),
      canvasRun: m
    }, se = I.filter((Q) => {
      const q = ny(m, Q);
      return !q || yt.current.has(q) ? !1 : (yt.current.add(q), !0);
    });
    Wc(L, se), Uc(L, se), ko(L, m, K), Vn(L, m, S), Vc(L, m, S);
  }
  function ko(m, y, h) {
    if (y.single_node || String(y.status || "").trim().toLowerCase() !== "success" || !br(h.resultOutput))
      return;
    const I = new Set(
      (y.node_results || []).filter((S) => en(S) === "success").map((S) => S.node_key).filter(Boolean)
    );
    Gt(
      m.assetCate.id,
      (S) => Ui({
        canvas: S,
        sourceNodeId: h.id,
        successfulNodeIds: I,
        assetCate: m.assetCate,
        powers: Fe
      })
    );
  }
  function Yt(m, y, h) {
    if (!Y)
      return null;
    const I = fo(
      m,
      Y,
      ye.nodes.length,
      y,
      h
    ), S = s ? Ur(s, ps(I) || Y.id) : Y;
    m === "asset" && (I.cardinality = S.cardinality);
    const K = m === "asset" && h?.replaceSingleAssetNode ? ps(I) || Number(S.id || 0) : 0, F = K ? ta(
      ye.nodes,
      ye.edges,
      K,
      h?.connectFromNodeId
    ) : null, L = F ? na(
      ye.nodes,
      ye.edges,
      K,
      h?.connectFromNodeId,
      F.id
    ) : /* @__PURE__ */ new Set(), se = F?.id || I.id, Q = T?.connection;
    if (be((q) => {
      let ie = q.edges;
      const Ne = K ? ta(
        q.nodes,
        q.edges,
        K,
        h?.connectFromNodeId
      ) : null;
      if (Ne) {
        const Ct = na(
          q.nodes,
          q.edges,
          K,
          h?.connectFromNodeId,
          Ne.id
        );
        if (ie = q.edges.filter(
          (ht) => !Ct.has(ht.from) && !Ct.has(ht.to)
        ), Q) {
          const ht = ea(
            Q,
            Ne.id
          );
          ie = In(ie, ht.source, ht.target);
        } else h?.connectFromNodeId ? ie = In(
          ie,
          h.connectFromNodeId || "",
          Ne.id
        ) : h?.connectToNodeId && (ie = In(
          ie,
          Ne.id,
          h.connectToNodeId || ""
        ));
        const an = q.nodes.filter((ht) => !Ct.has(ht.id)).map(
          (ht) => ht.id === Ne.id ? Bo(ht, I) : ht
        );
        return {
          ...q,
          nodes: an,
          edges: Xt(an, ie)
        };
      }
      if (Q) {
        const Ct = ea(Q, I.id);
        ie = In(ie, Ct.source, Ct.target);
      } else h?.connectFromNodeId ? ie = In(
        ie,
        h.connectFromNodeId || "",
        I.id
      ) : h?.connectToNodeId && (ie = In(ie, I.id, h.connectToNodeId || ""));
      const jt = qo(
        [...q.nodes, I],
        I.id,
        { x: I.x, y: I.y }
      );
      return {
        ...q,
        nodes: jt,
        edges: Xt(jt, ie)
      };
    }), F) {
      const q = Bo(F, I);
      ge((ie) => {
        const Ne = { ...ie };
        for (const jt of L)
          delete Ne[jt];
        return Ne[F.id] = {
          ...Ne[F.id] || {},
          ...Yy(q)
        }, Ne;
      });
    }
    return h?.selectCreated !== !1 && (w([se]), tr(se)), G("create"), O(null), F ? Bo(F, I) : I;
  }
  function p(m, y) {
    if (!Y)
      return;
    if (rs(ye.nodes).has(m.id)) {
      ne.info("脚本托管节点不能复制，请在分镜脚本中修改结构");
      return;
    }
    const h = Fy(
      m,
      Y.id,
      ye.nodes.length,
      y
    );
    be((I) => {
      const S = qo(
        [...I.nodes, h],
        h.id,
        { x: h.x, y: h.y }
      );
      return {
        ...I,
        nodes: S,
        edges: Xt(S, I.edges)
      };
    }), ge((I) => {
      const S = I[m.id];
      return S ? { ...I, [h.id]: S } : I;
    }), w([h.id]), tr(h.id), O(null), ne.success("已复制节点");
  }
  function g(m, y = {}) {
    const h = Dc(
      ye.nodes,
      m
    );
    if (h.size === 0)
      return;
    const I = rs(
      ye.nodes
    );
    if (!y.allowStoryboardFrame && [...h].some((S) => I.has(S))) {
      ne.info("脚本托管节点不能单独删除，请编辑分镜脚本或删除整个制作区");
      return;
    }
    be((S) => {
      const K = S.nodes.filter((F) => !h.has(F.id));
      return {
        ...S,
        nodes: K,
        edges: Xt(K, S.edges)
      };
    }), ge(
      (S) => ss(S, h)
    ), J((S) => ss(S, h)), w([]), ct(
      (S) => S && h.has(S.nodeId) ? null : S
    ), ve(
      (S) => S && h.has(S.id) ? null : S
    ), de(
      (S) => h.has(S) ? "" : S
    ), Ie.current && h.has(Ie.current.id) && (Ie.current = null), ne.success(
      m.length > 1 || h.size > 1 ? `已删除 ${h.size} 个节点` : "已删除节点"
    );
  }
  function x(m, y) {
    Yt("asset", y, { asset: m });
  }
  function N(m, y) {
    if (!m)
      return;
    const h = ye.nodes.find((S) => S.id === m) || (Ie.current?.id === m ? Ie.current : null);
    if (!h)
      return;
    const I = io(y.version?.content) || bt(y.version?.content);
    He(
      m,
      Jt(
        {
          ...h,
          kind: y.kind || Y.kind,
          assetCateId: Number(y.asset_cate_id || Y.id || 0)
        },
        {
          output: I,
          asset: y
        },
        "导入资产"
      )
    ), E(m, I);
  }
  async function E(m, y) {
    if (!m)
      return;
    const h = ye.edges.filter((I) => I.from === m).map((I) => ye.nodes.find((S) => S.id === I.to)).filter(
      (I) => !!I && I.type === "function" && I.functionOption?.key === "display"
    );
    if (h.length !== 0)
      for (const I of h)
        He(
          I.id,
          Jt(I, { output: y }, "展示导入结果")
        );
  }
  function B(m) {
    const y = W;
    if (!y) {
      x(m);
      return;
    }
    if (!(ye.nodes.find((I) => I.id === y) || (Ie.current?.id === y ? Ie.current : null))) {
      x(m), de(""), Ie.current = null;
      return;
    }
    N(y, m);
  }
  function ae() {
    Et(!1), de(""), Ie.current = null;
  }
  function ce(m, y) {
    Yt("power", y, { power: m });
  }
  function he(m = "") {
    Qn(m);
  }
  async function ot(m) {
    const y = await Xa({
      projectID: r,
      teamID: Number(s?.project.team_id || 0),
      files: m
    }), h = [];
    for (const I of y) {
      const S = Ut(I.asset);
      S.id && lt(S);
      const K = Sa(I.asset);
      K.id && h.push(K);
    }
    return h;
  }
  function Ye(m, y) {
    Yt("agent", y, { role: m });
  }
  function We(m, y) {
    Yt("flow", y, { flow: m });
  }
  function vt(m) {
    Yt("group", m);
  }
  function On(m, y) {
    const h = Yt("function", y, { functionOption: m });
    m.key === "import" && (Ie.current = h, he(h?.id || ""));
  }
  function Pr(m, y, h) {
    G("create"), O({
      x: m.x,
      y: m.y,
      position: y,
      connection: h
    }), me();
  }
  function Dr() {
    C(ee === "dark" ? "light" : "dark");
  }
  return H ? X.current ? null : /* @__PURE__ */ c(ol, {}) : Qe || !s || !Y ? /* @__PURE__ */ c("main", { className: `ws-page is-${ee} ws-loading-screen`, children: /* @__PURE__ */ c("div", { className: "ws-loading-card ws-error-card", children: /* @__PURE__ */ c("span", { children: Qe || "创作空间不存在" }) }) }) : /* @__PURE__ */ v("main", { className: `ws-page is-${ee} is-${R}-view`, children: [
    /* @__PURE__ */ c(
      _g,
      {
        activeCate: Y,
        mode: R,
        interactive: R === "create",
        nodes: $e.nodes,
        edges: $e.edges,
        viewport: ye.viewport,
        selectedNodeId: D,
        selectedNodeIds: _,
        onSelectNodes: (m) => {
          w(m), O(null);
        },
        onOpenNodeMenu: Pr,
        onAddConfiguredNode: Yt,
        onCopyNode: p,
        onDeleteNodes: g,
        onShowNodeDetail: xr,
        onNodesCommit: (m) => be((y) => ({ ...y, nodes: m })),
        onEdgesCommit: (m) => be((y) => ({ ...y, edges: m })),
        onViewportCommit: (m) => be((y) => y.nodes.length === 0 && y.edges.length === 0 ? y : { ...y, viewport: m }),
        focusNodeRequest: Pe,
        onFocusNodeRequestConsumed: Co,
        projectId: r,
        space: s,
        canvasReferenceItems: Rr,
        catalogCache: o,
        runningNodes: oe,
        setRunningNode: J,
        onNodeResult: He,
        onNodeDraftChange: kr,
        onAssetCreated: lt,
        onRunStartNode: Nn,
        onRunStoryboardFrame: St,
        onRunFunctionNode: vo,
        onRunBackendNode: Ar,
        onOpenImportPicker: he,
        onClearFeedbackRecords: wn,
        requestConfirm: _o,
        onOpenFeedbackRecord: Fn
      }
    ),
    /* @__PURE__ */ c(
      hg,
      {
        space: s,
        cates: Mn,
        activeCate: Y,
        saveStatus: vr[String(Y.id)] || "saved",
        hasAssetCates: Jn,
        loadingCateId: l,
        onBack: () => t({ to: "/bot/work" }),
        onSelectCate: Tr,
        onRefresh: Ot,
        onOpenRunHistory: () => {
          et(!0), zn(r, 0);
        },
        theme: ee,
        onToggleTheme: Dr
      }
    ),
    Ft ? /* @__PURE__ */ c(
      st,
      {
        fallback: /* @__PURE__ */ c(it, { label: "正在加载运行历史", overlay: !0 }),
        children: /* @__PURE__ */ c(
          Wm,
          {
            open: !0,
            runs: Ee,
            loading: yn,
            error: _t,
            page: ut,
            hasNextPage: mt,
            onOpenChange: et,
            onRefresh: () => zn(r, 0),
            onPreviousPage: () => zn(
              r,
              Math.max(0, ut - 2)
            ),
            onNextPage: () => zn(r, ut),
            onLocateRun: (m) => {
              const y = String(m.start_node_id || "");
              y && (et(!1), Tr(Number(m.asset_cate_id || a)).then(
                (h) => {
                  h && window.requestAnimationFrame(() => tr(y));
                }
              ));
            }
          }
        )
      }
    ) : null,
    /* @__PURE__ */ c(
      Ng,
      {
        mode: R,
        onSelectMode: (m) => {
          G(m), O(null);
        }
      }
    ),
    Ze ? /* @__PURE__ */ c(
      st,
      {
        fallback: /* @__PURE__ */ c(it, { label: "正在加载资产选择器", overlay: !0 }),
        children: /* @__PURE__ */ c(
          Ym,
          {
            open: !0,
            teamID: s.project.team_id,
            scopeProjectID: s.project.id,
            title: "导入资产",
            description: "选择已有资产或上传本地文件，确认后加入当前画布。",
            initialFilters: {
              sourceType: "project",
              projectID: s.project.id
            },
            confirmSelection: !0,
            contentMode: "full",
            validateAsset: (m) => m.versionID > 0 ? "" : "该资产没有可用版本，无法导入。",
            onUpload: ot,
            onClose: ae,
            onConfirm: (m) => {
              const y = m[0];
              y && B(Ut(y));
            }
          }
        )
      }
    ) : null,
    R === "result" ? /* @__PURE__ */ c(If, { className: "ws-asset-workspace", children: /* @__PURE__ */ c(st, { fallback: /* @__PURE__ */ c(it, { label: "正在加载资产" }), children: /* @__PURE__ */ c(
      Hm,
      {
        teamID: s.project.team_id,
        scopeProjectID: s.project.id,
        onLocalUpload: ot,
        initialFilters: {
          sourceType: "project",
          projectID: s.project.id,
          assetCateID: Jn ? Y.id : 0
        },
        headerAction: /* @__PURE__ */ c(Je, { label: "关闭资产", children: /* @__PURE__ */ v("button", { type: "button", onClick: () => G("create"), children: [
          /* @__PURE__ */ c(wa, { "aria-hidden": "true" }),
          /* @__PURE__ */ c("span", { className: "sr-only", children: "关闭资产" })
        ] }) })
      }
    ) }) }) : null,
    Re ? /* @__PURE__ */ c(
      _h,
      {
        prompt: Re.prompt,
        running: zt,
        readonly: Kf(
          Re,
          $e.nodes,
          Ge.current
        ),
        history: kn(
          $e.nodes.find(
            (m) => m.id === Re.node.id
          ) || Re.node
        ),
        activeRecordId: Re.recordId,
        onSelectRecord: (m) => {
          const y = $e.nodes.find(
            (h) => h.id === Re.node.id
          ) || Re.node;
          tt({
            node: y,
            recordId: m.id,
            prompt: {
              ...m.prompt,
              values: m.values || m.prompt.values || {}
            }
          });
        },
        onClose: En,
        onSubmit: bn
      },
      `${Re.node.id}-${Re.recordId}`
    ) : null,
    we ? /* @__PURE__ */ c(
      Ig,
      {
        request: we,
        onClose: () => Ue(null)
      }
    ) : null,
    T ? /* @__PURE__ */ c(
      st,
      {
        fallback: /* @__PURE__ */ c(it, { label: "正在加载节点菜单", overlay: !0 }),
        children: /* @__PURE__ */ c(
          Gm,
          {
            menu: T,
            flows: Io,
            powers: Tt,
            powerCategories: k,
            roles: Zn,
            onClose: () => O(null),
            onSelectFlow: (m) => We(m, T.position),
            onSelectFunction: (m) => On(m, T.position),
            onSelectGroup: () => vt(T.position),
            onSelectRole: (m) => Ye(m, T.position),
            onSelectPower: (m) => ce(m, T.position)
          }
        )
      }
    ) : null,
    _e ? /* @__PURE__ */ c(
      st,
      {
        fallback: /* @__PURE__ */ c(it, { label: "正在加载节点详情", overlay: !0 }),
        children: /* @__PURE__ */ c(
          Zm,
          {
            projectId: s.project.id,
            teamId: s.team.id,
            assetCateId: Number(
              _e.assetCateId || _e.asset?.asset_cate_id || Y?.id || 0
            ),
            node: _e,
            storyboardFocus: at,
            canvasReferenceItems: Rr.filter(
              (m) => m.source !== "current" || m.id !== _e.id
            ),
            onNodeDraftChange: (m) => {
              m && (kr(_e.id, m), ve(
                (y) => y?.id === _e.id ? { ...y, composerDraft: m } : y
              ));
            },
            onRunNode: Ar,
            onAssetUpdated: (m) => {
              const y = Rn(
                m,
                _e.asset
              );
              lt(y);
              const h = Ec(
                _e,
                y
              );
              _e.id.startsWith("asset-detail-") || He(_e.id, h), ve(
                (I) => I?.id === _e.id ? {
                  ...I,
                  ...h
                } : I
              );
            },
            onClose: () => {
              ve(null), M(void 0);
            }
          }
        )
      }
    ) : null
  ] });
}
function hg({
  space: e,
  cates: t,
  activeCate: n,
  saveStatus: r,
  hasAssetCates: o,
  loadingCateId: s,
  onBack: i,
  onSelectCate: a,
  onRefresh: d,
  onOpenRunHistory: u,
  theme: l,
  onToggleTheme: f
}) {
  const b = Math.max(
    0,
    t.findIndex((_) => _.id === n.id)
  );
  return /* @__PURE__ */ v("header", { className: "ws-topbar", children: [
    /* @__PURE__ */ v("div", { className: "ws-project-head", children: [
      /* @__PURE__ */ c(
        "button",
        {
          type: "button",
          className: "ws-back-button",
          onClick: i,
          "aria-label": "返回工作台",
          children: /* @__PURE__ */ c(Wu, { size: 18 })
        }
      ),
      /* @__PURE__ */ v("div", { className: "ws-project-copy", children: [
        /* @__PURE__ */ c("strong", { children: e.project.name }),
        /* @__PURE__ */ c("span", { children: e.team.name || e.project.team?.name || "自由团队" })
      ] })
    ] }),
    o ? /* @__PURE__ */ v(
      "nav",
      {
        className: "ws-cate-strip",
        "aria-label": "资产类型",
        style: {
          "--ws-cate-total": t.length,
          "--ws-cate-active": b
        },
        children: [
          /* @__PURE__ */ c("span", { className: "ws-cate-indicator" }),
          t.map((_) => /* @__PURE__ */ v(
            "button",
            {
              type: "button",
              className: `ws-cate ${_.id === n.id ? "is-active" : ""}`,
              disabled: s != null,
              onClick: () => {
                a(_.id);
              },
              children: [
                s === _.id ? /* @__PURE__ */ c(Bt, { size: 12, className: "animate-spin" }) : null,
                /* @__PURE__ */ c("span", { className: "ws-cate-name", children: _.name })
              ]
            },
            _.id
          ))
        ]
      }
    ) : null,
    /* @__PURE__ */ v("div", { className: "ws-top-actions", children: [
      /* @__PURE__ */ c(wg, { status: r }),
      /* @__PURE__ */ c(Je, { label: "查看画布运行记录", children: /* @__PURE__ */ v(
        "button",
        {
          type: "button",
          className: "ws-action",
          onClick: u,
          children: [
            /* @__PURE__ */ c(la, { size: 15 }),
            "运行记录"
          ]
        }
      ) }),
      /* @__PURE__ */ v("button", { type: "button", className: "ws-action", onClick: f, children: [
        l === "dark" ? /* @__PURE__ */ c(sd, { size: 15 }) : /* @__PURE__ */ c(id, { size: 15 }),
        l === "dark" ? "亮色" : "暗色"
      ] }),
      /* @__PURE__ */ v("button", { type: "button", className: "ws-action", onClick: d, children: [
        /* @__PURE__ */ c(cr, { size: 15 }),
        "刷新"
      ] })
    ] })
  ] });
}
function wg({ status: e }) {
  const t = e === "saving" ? "保存中" : e === "error" ? "保存失败，正在重试" : e === "dirty" ? "未保存" : "已保存";
  return /* @__PURE__ */ c(Je, { label: t, children: /* @__PURE__ */ v("span", { className: `ws-save-indicator is-${e}`, children: [
    e === "saving" ? /* @__PURE__ */ c(Bt, { size: 14, className: "ws-spin" }) : /* @__PURE__ */ c(ya, { size: 14 }),
    t
  ] }) });
}
const bg = [
  { key: "create", label: "创作", icon: Bu },
  { key: "result", label: "资产", icon: Ta }
];
function Ng({
  mode: e,
  onSelectMode: t
}) {
  return /* @__PURE__ */ c("nav", { className: "ws-dock", "aria-label": "画布视角", children: bg.map((n) => {
    const r = n.icon;
    return /* @__PURE__ */ v(
      "button",
      {
        type: "button",
        className: `ws-dock-button ${n.key === e ? "is-active" : ""}`,
        onClick: () => t(n.key),
        children: [
          /* @__PURE__ */ c(r, { size: 20 }),
          n.label
        ]
      },
      n.key
    );
  }) });
}
function _g({
  activeCate: e,
  mode: t,
  interactive: n,
  nodes: r,
  edges: o,
  viewport: s,
  selectedNodeId: i,
  selectedNodeIds: a,
  onSelectNodes: d,
  onOpenNodeMenu: u,
  onAddConfiguredNode: l,
  onCopyNode: f,
  onDeleteNodes: b,
  onShowNodeDetail: _,
  onNodesCommit: w,
  onEdgesCommit: D,
  onViewportCommit: A,
  focusNodeRequest: z,
  onFocusNodeRequestConsumed: V,
  projectId: R,
  space: G,
  canvasReferenceItems: ee,
  catalogCache: C,
  runningNodes: T,
  setRunningNode: O,
  onNodeResult: H,
  onNodeDraftChange: re,
  onAssetCreated: X,
  onRunStartNode: oe,
  onRunStoryboardFrame: J,
  onRunFunctionNode: Z,
  onRunBackendNode: fe,
  onOpenImportPicker: ge,
  onClearFeedbackRecords: _e,
  requestConfirm: ve,
  onOpenFeedbackRecord: at
}) {
  const [M, we] = $(null), [Ue, Pe] = $(""), [ct, Ze] = $(""), [Et, W] = $(""), [de, Qe] = $(null), [De, Ce] = $(null), [Ve, Ee] = $(""), [Vt, yn] = $(!0), [hn, _t] = $(!1), [qe, Ft] = $(() => /* @__PURE__ */ new Set()), [et, ut] = $(1), [At, mt] = $(null), gt = ue(
    () => new Set(a),
    [a]
  ), Re = te(null), tt = te(/* @__PURE__ */ new Map()), zt = te(null), nt = te(!1), Ie = te(!1), rt = te(!1), yt = te(null), It = te(!1), Ke = te(o);
  Ke.current = o;
  const dt = P(
    (p) => {
      Ke.current = p, D(p);
    },
    [D]
  ), rn = P(
    (p) => {
      const g = Object.entries(p);
      if (g.length === 0)
        return;
      const x = new Map(g);
      let N = !1;
      const E = Ke.current.map((B) => {
        if (!x.has(B.id))
          return B;
        const ae = x.get(B.id) || void 0;
        return (B.mediaUsage || void 0) === ae ? B : (N = !0, { ...B, mediaUsage: ae });
      });
      N && dt(E);
    },
    [dt]
  ), Kt = P(
    (p) => {
      const g = Ke.current.filter((x) => x.id !== p);
      g.length !== Ke.current.length && (Ee((x) => x === p ? "" : x), dt(g));
    },
    [dt]
  ), Lt = (p, g) => {
    if (W(""), !n)
      return;
    const x = Zl(r, p, g);
    x !== r && w(x);
  }, Ge = (p, g) => {
    if (W(""), !n)
      return;
    const x = Ql(r, p, g);
    x !== r && w(x);
  }, Se = te({
    onAddConfiguredNode: l,
    onNodeResult: H,
    onNodeDraftChange: re,
    onAssetCreated: X,
    onRunStartNode: oe,
    onRunFunctionNode: Z,
    onOpenImportPicker: ge,
    onClearFeedbackRecords: _e,
    onOpenFeedbackRecord: at,
    onShowNodeDetail: _,
    requestConfirm: ve,
    onRunBackendNode: fe,
    onConnectedMediaUsagesChange: rn,
    onConnectedMediaEdgeRemove: Kt,
    onNodeResizeStart: W,
    onNodeResizeEnd: Lt,
    onResultViewResizeEnd: Ge
  });
  Se.current = {
    onAddConfiguredNode: l,
    onNodeResult: H,
    onNodeDraftChange: re,
    onAssetCreated: X,
    onRunStartNode: oe,
    onRunFunctionNode: Z,
    onOpenImportPicker: ge,
    onClearFeedbackRecords: _e,
    onOpenFeedbackRecord: at,
    onShowNodeDetail: _,
    requestConfirm: ve,
    onRunBackendNode: fe,
    onConnectedMediaUsagesChange: rn,
    onConnectedMediaEdgeRemove: Kt,
    onNodeResizeStart: W,
    onNodeResizeEnd: Lt,
    onResultViewResizeEnd: Ge
  };
  const Fe = ue(
    () => ({
      onAddConfiguredNode: (p, g, x) => Se.current.onAddConfiguredNode?.(p, g, x),
      onNodeResult: (p, g) => Se.current.onNodeResult(p, g),
      onNodeDraftChange: (p, g) => Se.current.onNodeDraftChange(p, g),
      onAssetCreated: (p) => Se.current.onAssetCreated(p),
      onRunStartNode: (p) => Se.current.onRunStartNode(p),
      onRunFunctionNode: (p) => Se.current.onRunFunctionNode(p),
      onOpenImportPicker: (p) => Se.current.onOpenImportPicker(p),
      onClearFeedbackRecords: (p) => Se.current.onClearFeedbackRecords(p),
      onOpenFeedbackRecord: (p, g) => Se.current.onOpenFeedbackRecord(p, g),
      onShowNodeDetail: (p, g) => Se.current.onShowNodeDetail(p, g),
      requestConfirm: (p) => Se.current.requestConfirm(p),
      onRunBackendNode: (p, g) => Se.current.onRunBackendNode(p, g),
      onConnectedMediaUsagesChange: (p) => Se.current.onConnectedMediaUsagesChange(p),
      onConnectedMediaEdgeRemove: (p) => Se.current.onConnectedMediaEdgeRemove(p),
      onNodeResizeStart: (p) => Se.current.onNodeResizeStart(p),
      onNodeResizeEnd: (p, g) => Se.current.onNodeResizeEnd(p, g),
      onResultViewResizeEnd: (p, g) => Se.current.onResultViewResizeEnd(p, g)
    }),
    []
  ), k = ue(
    () => xc(r, ft),
    [r]
  ), j = ue(
    () => new Map(k.map((p) => [p.id, p])),
    [k]
  ), U = ue(
    () => rs(r),
    [r]
  ), me = ue(() => {
    const p = /* @__PURE__ */ new Map();
    for (const g of k)
      for (const x of g.memberNodeIds)
        p.set(x, g.sourceNodeId);
    return p;
  }, [k]);
  le(() => {
    const p = new Set(k.map((g) => g.id));
    Ft((g) => {
      const x = new Set(
        [...g].filter((E) => p.has(E))
      );
      return x.size === g.size && [...x].every((E) => g.has(E)) ? g : x;
    });
  }, [k]);
  const Ae = ue(() => {
    const p = /* @__PURE__ */ new Set();
    for (const g of k)
      if (qe.has(g.id))
        for (const x of g.memberNodeIds)
          p.add(x);
    return p;
  }, [qe, k]), Le = P(
    (p) => {
      const g = j.get(p), x = Re.current?.getBoundingClientRect();
      if (!g || !M || !x)
        return;
      const N = $i(
        g,
        qe.has(g.id)
      ), E = Math.max(1, x.width - 144), B = Math.max(1, x.height - 144), ae = Math.max(
        0.35,
        Math.min(
          0.9,
          E / N.width,
          B / N.height
        )
      );
      M.setCenter?.(
        N.x + N.width / 2,
        N.y + N.height / 2,
        { zoom: ae, duration: 320 }
      ), ut(ae);
    },
    [qe, M, j]
  ), qt = P(
    (p) => {
      const g = j.get(p);
      if (!g)
        return;
      const x = !qe.has(p);
      if (Ft((N) => {
        const E = new Set(N);
        return E.has(p) ? E.delete(p) : E.add(p), E;
      }), x) {
        const N = new Set(g.memberNodeIds);
        d(
          a.filter((E) => !N.has(E))
        ), Pe(""), Ee(""), Ce(null);
      }
    },
    [
      qe,
      d,
      a,
      j
    ]
  ), vr = ue(() => r.length === 0 ? "" : `${e.id}:${r.map((p) => p.id).join("|")}`, [e.id, r]), Ot = ue(
    () => iu(r, o),
    [o, r]
  ), _o = ue(() => {
    const p = /* @__PURE__ */ new Set(), g = r.filter((N) => !Ae.has(N.id)).map((N) => {
      p.add(N.id);
      const E = { x: N.x, y: N.y }, B = gt.has(N.id), ae = B && a.length === 1, ce = N.type === "power" ? Nr(N.power, N.kind, N.outputType).viewMode : "", he = ae || N.type === "flow", ot = he ? G : null, Ye = he || ce === "storyboard" || ce === "video_compose" ? ee : og, We = ae && (N.type === "power" || N.type === "agent") && Ot.incomingMediaReferencesByNodeId.get(N.id) || os, vt = U.has(N.id), On = me.get(N.id) || Oi(r, N), Pr = On && Ot.nodeById.get(On) || null, Dr = !!(On && Pt(
        T[Os(On)]
      )), m = `ws-flow-node ws-flow-node-${N.type}`, y = T[N.id] || null, h = N.type === "group" && Ot.groupMembersById.get(N.id) || Bi, I = N.type === "group" || N.type === "function" && N.functionOption?.key === "start" ? T : rg, S = Ot.inputContextByNodeId.get(N.id) || null, K = za({
        targets: N.type === "group" ? h : [N],
        nodesByID: Ot.nodeById,
        hasResult: ft
      }), F = tt.current.get(N.id), L = F?.data, Q = L?.sourceNode === N && L.projectId === R && L.space === ot && L.runningNode === y && gu(L.groupMembers || [], h) && L.canvasRunningNodes === I && L.canvasReferenceItems === Ye && L.connectedMediaReferences === We && L.interactive === n && L.structureLocked === vt && L.storyboardSourceNode === Pr && L.storyboardFrameRunning === Dr && L.runBlockedReason === K && L.showNodeSettings === ae && $y(L.inputContext, S) ? L : {
        ...N,
        sourceNode: N,
        projectId: R,
        space: ot,
        catalogCache: C,
        runningNode: y,
        groupMembers: h,
        canvasRunningNodes: I,
        canvasReferenceItems: Ye,
        connectedMediaReferences: We,
        interactive: n,
        structureLocked: vt,
        storyboardSourceNode: Pr,
        storyboardFrameRunning: Dr,
        runBlockedReason: K,
        showNodeSettings: ae,
        setRunningNode: O,
        ...Fe,
        inputContext: S
      }, q = F?.style, ie = Nu(N);
      if (F && F.position.x === E.x && F.position.y === E.y && F.data === Q && F.selected === B && F.className === m && F.draggable === (n && !vt) && F.deletable === !vt && F.zIndex === (N.type === "group" ? 0 : N.groupId ? 2 : 1) && q?.width === ie.width && q?.height === ie.height)
        return F;
      const Ne = {
        ...F,
        id: N.id,
        type: "workSpace",
        position: E,
        data: Q,
        selected: B,
        className: m,
        draggable: n && !vt,
        deletable: !vt,
        zIndex: N.type === "group" ? 0 : N.groupId ? 2 : 1,
        style: {
          ...F?.style,
          width: ie.width,
          height: ie.height
        }
      };
      return tt.current.set(N.id, Ne), Ne;
    });
    for (const N of tt.current.keys())
      p.has(N) || tt.current.delete(N);
    return [...k.map((N) => {
      const E = qe.has(N.id), B = $i(N, E), ae = Ac(
        N,
        r,
        ft
      ), ce = Pt(T[N.id]) || N.workNodeIds.some(
        (ot) => Pt(T[ot])
      ), he = {
        type: "storyboardFrame",
        title: N.title,
        groupCount: N.groupCount,
        workNodeCount: N.workNodeCount,
        completedCount: N.completedCount,
        running: ce,
        runBlockedReason: ae.blockedReason,
        collapsed: E,
        onRun: () => {
          J(N.sourceNodeId);
        },
        onFocus: () => Le(N.id),
        onToggleCollapsed: () => qt(N.id)
      };
      return {
        id: N.id,
        type: "storyboardFrame",
        position: { x: B.x, y: B.y },
        data: he,
        selected: gt.has(N.id),
        className: "ws-flow-node ws-flow-node-storyboard-frame",
        zIndex: -1,
        draggable: n,
        selectable: n,
        connectable: !1,
        deletable: !1,
        focusable: n,
        dragHandle: ".ws-storyboard-frame-header",
        style: { width: B.width, height: B.height }
      };
    }), ...g];
  }, [
    qe,
    Ot,
    Le,
    Ae,
    n,
    U,
    r,
    J,
    R,
    T,
    i,
    a.length,
    gt,
    O,
    G,
    C,
    ee,
    Fe,
    k,
    me,
    qt
  ]), { flowNodes: $t, setFlowNodes: Mn } = Wl(
    _o,
    ct || Et
  ), Jn = P(
    (p) => {
      n && (Ee(""), D(o.filter((g) => g.id !== p)));
    },
    [o, n, D]
  ), Y = P(
    (p) => {
      !n || !o.some((g) => g.id === p) || ve({
        title: "删除连线",
        description: "删除后，上下游节点将不再通过这条连线传递内容。",
        confirmText: "删除",
        tone: "danger",
        onConfirm: () => Jn(p)
      });
    },
    [Jn, o, n, ve]
  ), Pn = P(
    (p) => {
      if (!n || p.length === 0)
        return;
      const g = Dc(r, p);
      if (g.size === 0)
        return;
      if ([...g].some(
        (E) => U.has(E)
      )) {
        ne.info(
          "脚本托管节点不能单独删除，请编辑分镜脚本或删除整个制作区"
        );
        return;
      }
      const x = p.length === 1 ? p[0] : null, N = p.some((E) => E.type === "group");
      ve({
        title: x ? `删除「${x.title}」` : `删除 ${g.size} 个节点`,
        description: N ? "会同时删除组内节点，并移除与这些节点相连的连线。" : x ? "会同时移除与该节点相连的连线。" : "会同时移除与这些节点相连的连线。",
        confirmText: "删除",
        tone: "danger",
        onConfirm: () => {
          Ee(""), b(p);
        }
      });
    },
    [
      n,
      U,
      r,
      b,
      ve
    ]
  ), Zn = P(
    (p, g = []) => {
      if (!n || p.length === 0)
        return;
      const x = new Set(
        p.flatMap((he) => he.memberNodeIds)
      ), N = /* @__PURE__ */ new Set([
        ...x,
        ...g.map((he) => he.id)
      ]), E = r.filter((he) => N.has(he.id));
      if (E.length === 0)
        return;
      const B = p.length === 1 && g.every((he) => x.has(he.id)) ? p[0] : null, ae = E.filter(
        (he) => he.type === "group"
      ).length, ce = E.length - ae;
      ve({
        title: B ? `删除「${B.title}」制作区` : `删除 ${E.length} 个节点`,
        description: B ? `将删除其中 ${ae} 个分组和 ${ce} 个节点，并移除相关连线。已生成素材会归档保留。` : "会同时删除所选制作区内的节点、分组及相关连线；已生成素材会归档保留。",
        confirmText: "删除",
        tone: "danger",
        onConfirm: () => {
          Ee(""), b(E, { allowStoryboardFrame: !0 });
        }
      });
    },
    [n, r, b, ve]
  ), Tt = ue(() => {
    const p = new Map(r.map((B) => [B.id, B])), g = sa(
      i,
      r,
      o
    ), x = sa(
      Ue,
      r,
      o
    ), N = /* @__PURE__ */ new Set([
      ...g,
      ...x
    ]), E = g.size > 0 ? i : x.size > 0 ? Ue : "";
    return o.filter(
      (B) => {
        const { sourceNodeId: ae, targetNodeId: ce } = qn(B);
        return p.has(ae) && p.has(ce) && !Ae.has(ae) && !Ae.has(ce);
      }
    ).map((B) => ({
      id: B.id,
      source: B.logicalFrom || B.from,
      sourceHandle: "output-0",
      target: B.logicalTo || B.to,
      targetHandle: "input-0",
      type: "animated",
      animated: !1,
      data: {
        physicalFrom: B.from,
        physicalTo: B.to,
        logicalFrom: B.logicalFrom,
        logicalTo: B.logicalTo,
        executionMode: B.executionMode,
        mediaUsage: B.mediaUsage
      }
    })).map(
      (B) => rh(
        B,
        p,
        Ue,
        i,
        Ve,
        N,
        E,
        Y
      )
    );
  }, [
    o,
    Ae,
    Ue,
    r,
    Y,
    Ve,
    i
  ]), Io = ue(
    () => de ? [...Tt, de] : Tt,
    [Tt, de]
  );
  le(() => {
    if (!M || !vr || z || s.zoom != null || typeof window > "u")
      return;
    const p = setTimeout(() => {
      M.fitView?.({ padding: 0.32, duration: 250, maxZoom: 0.72 });
    }, 150);
    return () => clearTimeout(p);
  }, [vr, M, z, s.zoom]), le(() => {
    if (!M || !z || typeof window > "u")
      return;
    const p = r.find((N) => N.id === z.nodeId);
    if (!p) {
      V(z);
      return;
    }
    const g = k.find(
      (N) => qe.has(N.id) && N.memberNodeIds.includes(p.id)
    );
    if (g) {
      Ft((N) => {
        const E = new Set(N);
        return E.delete(g.id), E;
      });
      return;
    }
    const x = window.setTimeout(() => {
      const N = { x: p.x, y: p.y }, E = p.type === "power" ? 1.02 : 0.96;
      M.setCenter?.(
        N.x + (p.width || 180) / 2,
        N.y + (p.height || 180) / 2,
        { zoom: E, duration: 320 }
      ), ut(E), V(z);
    }, 80);
    return () => window.clearTimeout(x);
  }, [
    qe,
    M,
    z,
    r,
    V,
    k
  ]);
  const ye = P(
    (p) => {
      if (!n)
        return;
      Mn((N) => {
        const E = zu(p, N);
        for (const B of E)
          tt.current.set(B.id, B);
        return E;
      });
      const g = new Set(a);
      let x = !1;
      for (const N of p)
        N.type === "select" && (x = !0, N.selected ? (g.delete(N.id), g.add(N.id)) : g.delete(N.id));
      x && d([...g]);
    },
    [n, d, a]
  ), So = P(
    (p) => {
      if (!n)
        return;
      let g = "", x = !1;
      for (const B of p)
        B.type === "select" && (x = !0, B.selected && (g = B.id));
      x && Ee(g);
      const N = p.filter(
        (B) => B.type !== "select"
      );
      if (N.length === 0)
        return;
      const E = Ou(N, Tt);
      dt(Wy(E));
    },
    [dt, Tt, n]
  ), $e = P(
    async (p, g) => {
      if (Ke.current.some((ce) => {
        const he = qn(ce);
        return he.sourceNodeId === p && he.targetNodeId === g;
      }))
        return;
      const N = r.find((ce) => ce.id === g), B = Gr(r, p).filter(mo);
      let ae;
      if (N?.type === "power" && N.power && B.length > 0)
        try {
          const ce = Number(
            N.composerDraft?.selectedTargetId || 0
          ), he = Number(
            G.release?.id || G.project.release_id || 0
          ), ot = await C.loadPowerForm(
            {
              projectId: R,
              releaseId: he,
              flowId: Number(N.flow?.id || 0),
              powerId: Number(N.power?.id || 0),
              powerKey: N.power?.key || "",
              targetId: ce
            },
            () => Ho({
              projectId: R,
              flowId: Number(N.flow?.id || 0),
              powerId: Number(N.power?.id || 0),
              powerKey: N.power?.key || "",
              targetId: ce
            })
          ), Ye = Wo(ot.params || []), We = dp(
            zy(
              r,
              Ke.current,
              g
            ),
            Ye,
            B,
            N.composerDraft?.promptContent
          );
          if (We.error) {
            ne.error(We.error);
            return;
          }
          ae = We.usage;
        } catch (ce) {
          ne.error(
            ce instanceof Error ? `媒体用途加载失败，未建立连线：${ce.message}` : "媒体用途加载失败，未建立连线"
          );
          return;
        }
      dt(
        Xt(
          r,
          In(
            Ke.current,
            p,
            g,
            ae
          )
        )
      );
    },
    [C, dt, r, R, G]
  ), Cr = P(
    (p) => {
      n && (nt.current = !0, !(!p.source || !p.target || p.source === p.target) && $e(
        p.source || "",
        p.target || ""
      ));
    },
    [$e, n]
  ), Rr = P(
    (p, g) => {
      if (!n)
        return;
      const x = String(g?.nodeId || "");
      x && (rt.current = !0, d([]), Ce(null)), zt.current = x ? {
        nodeId: x,
        handleId: g?.handleId || null,
        handleType: g?.handleType || null
      } : null, nt.current = !1, Ee("");
    },
    [n, d]
  ), Qn = P(
    (p) => {
      if (!n) {
        zt.current = null, nt.current = !1;
        return;
      }
      const g = zt.current;
      if (zt.current = null, g?.nodeId && typeof window < "u" && window.setTimeout(() => {
        rt.current = !1;
      }, 0), nt.current) {
        nt.current = !1;
        return;
      }
      if (!g?.nodeId)
        return;
      const x = sh(p);
      x && (Ie.current = !0, u(
        x,
        $n(M, x),
        g
      ));
    },
    [M, n, u]
  ), Gt = P(
    (p, g) => {
      n && (p.preventDefault(), p.stopPropagation(), Ce(null), d([]), Ee(g.id));
    },
    [n, d]
  );
  le(() => {
    if (!Ve || typeof window > "u")
      return;
    function p(g) {
      !n || !ia(g) || (g.preventDefault(), Y(Ve));
    }
    return window.addEventListener("keydown", p), () => window.removeEventListener("keydown", p);
  }, [n, Y, Ve]), le(() => {
    if (a.length === 0 || Ve || typeof window > "u")
      return;
    function p(g) {
      if (!n || !ia(g))
        return;
      const x = r.filter(
        (E) => gt.has(E.id)
      ), N = a.map((E) => j.get(E)).filter((E) => !!E);
      if (!(x.length === 0 && N.length === 0)) {
        if (g.preventDefault(), N.length > 0) {
          Zn(N, x);
          return;
        }
        Pn(x);
      }
    }
    return window.addEventListener("keydown", p), () => window.removeEventListener("keydown", p);
  }, [
    n,
    r,
    Pn,
    Zn,
    Ve,
    a.length,
    a,
    gt,
    j
  ]);
  const be = P((p) => {
    Qe(
      (g) => th(g, p) ? g : p
    );
  }, []), er = P(
    (p) => {
      if (!n)
        return !1;
      const g = r.find(
        (N) => N.id === p.source
      ), x = r.find(
        (N) => N.id === p.target
      );
      return fs(g, x);
    },
    [n, r]
  ), He = P(
    (p, g) => {
      if (!n)
        return;
      const x = j.get(g.id);
      if (x) {
        const ce = Tc(
          x,
          g.position
        ), he = new Set(x.memberNodeIds);
        Mn(
          (ot) => ot.map((Ye) => {
            if (!he.has(Ye.id))
              return Ye;
            const We = r.find((vt) => vt.id === Ye.id);
            return We ? {
              ...Ye,
              position: {
                x: We.x + ce.x,
                y: We.y + ce.y
              }
            } : Ye;
          })
        ), be(null);
        return;
      }
      if (U.has(g.id)) {
        be(null);
        return;
      }
      const N = r.find((ce) => ce.id === g.id);
      if (!N) {
        be(null);
        return;
      }
      if (N.type === "group") {
        const ce = g.position.x - N.x, he = g.position.y - N.y;
        Mn(
          (ot) => ot.map((Ye) => {
            const We = r.find((vt) => vt.id === Ye.id);
            return We?.groupId !== N.id ? Ye : {
              ...Ye,
              position: {
                x: We.x + ce,
                y: We.y + he
              }
            };
          })
        ), be(null);
        return;
      }
      if (Tt.some(
        (ce) => ce.source === g.id || ce.target === g.id
      )) {
        be(null);
        return;
      }
      const B = nh(g, $t, r);
      if (!B) {
        be(null);
        return;
      }
      const ae = Qy(
        N,
        B.domainNode
      );
      if (!ae) {
        be(null);
        return;
      }
      be(eh(ae));
    },
    [
      Tt,
      $t,
      n,
      U,
      r,
      Mn,
      j,
      be
    ]
  ), Ht = P(
    (p, g) => {
      if (!n) {
        Ze(""), be(null);
        return;
      }
      const x = j.get(g.id);
      if (x) {
        const ae = Em(
          r,
          x,
          g.position
        );
        ae !== r && w(ae), Ze(""), be(null);
        return;
      }
      if (U.has(g.id)) {
        Ze(""), be(null);
        return;
      }
      const N = r.find((ae) => ae.id === g.id);
      let E = !1;
      if (N) {
        const ae = Kd(
          r,
          g.id,
          g.position
        ), ce = qo(
          ae,
          g.id,
          g.position
        );
        E = (N.groupId || "") !== (ce.find((ot) => ot.id === N.id)?.groupId || ""), ce !== r && w(ce);
        const he = Xt(ce, o);
        yu(o, he) || D(he);
      }
      if (Ze(""), E) {
        be(null);
        return;
      }
      if (!de) {
        be(null);
        return;
      }
      Tt.some(
        (ae) => ae.source === de.source && ae.target === de.target
      ) || $e(
        de.source,
        de.target
      ), be(null);
    },
    [
      o,
      $e,
      Tt,
      n,
      U,
      w,
      r,
      de,
      j,
      be
    ]
  ), on = P(
    (p) => {
      !n || p.button !== 2 || !cg(p.target) || (yt.current = {
        pointerId: p.pointerId,
        start: { x: p.clientX, y: p.clientY },
        baseNodeIds: p.ctrlKey || p.metaKey ? [...a] : [],
        moved: !1
      }, It.current = !1, p.preventDefault(), p.stopPropagation(), p.currentTarget.setPointerCapture?.(p.pointerId));
    },
    [n, a]
  ), kr = P(
    (p) => {
      const g = yt.current;
      if (!g || g.pointerId !== p.pointerId || !M || !Re.current)
        return;
      const x = p.clientX - g.start.x, N = p.clientY - g.start.y;
      if (!g.moved && Math.hypot(x, N) < 5)
        return;
      g.moved = !0, p.preventDefault(), p.stopPropagation();
      const E = Re.current.getBoundingClientRect();
      mt(
        ug(
          g.start,
          {
            x: p.clientX,
            y: p.clientY
          },
          E
        )
      );
      const B = dg(
        r,
        $n(M, g.start),
        $n(M, {
          x: p.clientX,
          y: p.clientY
        })
      );
      d(lg(g.baseNodeIds, B)), Ee(""), Ce(null);
    },
    [M, r, d]
  ), xr = P(
    (p) => {
      const g = yt.current;
      !g || g.pointerId !== p.pointerId || (yt.current = null, p.currentTarget.hasPointerCapture?.(p.pointerId) && p.currentTarget.releasePointerCapture(p.pointerId), mt(null), g.moved && (It.current = !0, p.preventDefault(), p.stopPropagation()));
    },
    []
  ), Dn = P(
    (p) => {
      if (!n)
        return;
      if (Ie.current) {
        Ie.current = !1;
        return;
      }
      if (d([]), Ee(""), Ce(null), !("detail" in p) || p.detail !== 2)
        return;
      p.preventDefault(), p.stopPropagation();
      const g = { x: p.clientX, y: p.clientY };
      u(g, $n(M, g));
    },
    [M, n, u, d]
  ), wn = P(
    (p) => {
      if (!n)
        return;
      if (p.preventDefault(), p.stopPropagation(), It.current) {
        It.current = !1;
        return;
      }
      const g = { x: p.clientX, y: p.clientY };
      u(g, $n(M, g));
    },
    [M, n, u]
  ), lt = P(
    (p, g) => {
      if (n) {
        if (p.preventDefault(), p.stopPropagation(), j.has(g.id)) {
          Ee(""), gt.has(g.id) || d([g.id]), Ce({
            nodeId: g.id,
            x: p.clientX,
            y: p.clientY
          });
          return;
        }
        Ee(""), gt.has(g.id) || d([g.id]), Ce({
          nodeId: g.id,
          x: p.clientX,
          y: p.clientY
        });
      }
    },
    [n, d, gt, j]
  ), pe = De && r.find((p) => p.id === De.nodeId) || null, bn = De && j.get(De.nodeId) || null, En = !!(pe && U.has(pe.id)), Fn = pe && r.find(
    (p) => p.id === Oi(r, pe)
  ) || null, Nn = pe ? { x: pe.x, y: pe.y } : void 0;
  function St() {
    Ce(null);
  }
  function Ar() {
    if (!(!n || !pe)) {
      if (En) {
        ne.info("脚本托管节点不能复制，请在分镜脚本中修改结构"), St();
        return;
      }
      f(
        pe,
        Nn ? { x: Nn.x + 34, y: Nn.y + 34 } : void 0
      ), St();
    }
  }
  function vo() {
    if (!n || !pe && !bn)
      return;
    if (bn) {
      const g = bn;
      St(), Zn([g]);
      return;
    }
    if (!pe)
      return;
    if (En) {
      ne.info(
        "脚本托管节点不能单独删除，请编辑分镜脚本或删除整个制作区"
      ), St();
      return;
    }
    const p = pe;
    St(), Pn([p]);
  }
  function Tr() {
    pe && (_(pe), St());
  }
  function tr() {
    Fn && (_(
      Fn,
      kc(pe)
    ), St());
  }
  function Co() {
    if (!pe)
      return;
    const p = um(pe, r);
    if (!p) {
      St();
      return;
    }
    re(pe.id, p), ne.success("已恢复脚本生成的提示词"), St();
  }
  const sn = P(
    (p) => {
      n && (p.preventDefault(), p.dataTransfer && (p.dataTransfer.dropEffect = "move"));
    },
    [n]
  ), zn = P(
    (p) => {
      if (!n || (p.preventDefault(), !M || !l)) return;
      const g = p.dataTransfer.getData(
        "application/shemic-nodetype"
      );
      if (!g) return;
      const x = p.dataTransfer.getData("application/shemic-detail"), N = x ? JSON.parse(x) : void 0, E = M.screenToFlowPosition ? M.screenToFlowPosition({
        x: p.clientX,
        y: p.clientY
      }) : $n(M, {
        x: p.clientX,
        y: p.clientY
      });
      g === "asset" && N ? l("asset", E, { asset: N }) : g === "power" && N ? l("power", E, { power: N }) : g === "agent" && N ? l("agent", E, { role: N }) : g === "flow" && N ? l("flow", E, { flow: N }) : g === "function" && N ? l("function", E, { functionOption: N }) : l(g, E);
    },
    [M, n, l]
  ), Mr = P(() => {
    M?.fitView?.({ padding: 0.32, duration: 260, maxZoom: 0.9 });
  }, [M]), nr = P(
    (p) => {
      const g = Math.max(0.35, Math.min(1.45, p));
      ut(g), M?.zoomTo?.(g, { duration: 120 });
    },
    [M]
  ), Ro = P(() => {
    const p = Math.min(1.45, et + 0.12);
    ut(p), M?.zoomIn?.({ duration: 140 });
  }, [M, et]), ko = P(() => {
    const p = Math.max(0.35, et - 0.12);
    ut(p), M?.zoomOut?.({ duration: 140 });
  }, [M, et]), Yt = [
    "ws-canvas-wrap",
    ct ? "is-dragging" : "",
    At ? "is-selecting" : "",
    Et ? "is-resizing" : "",
    n ? "is-interactive" : "is-passive",
    t === "result" ? "is-result-mode" : ""
  ].filter(Boolean).join(" ");
  return /* @__PURE__ */ v(
    "section",
    {
      ref: Re,
      className: Yt,
      style: Sh(et),
      onPointerDownCapture: on,
      onPointerMoveCapture: kr,
      onPointerUpCapture: xr,
      onPointerCancelCapture: xr,
      children: [
        /* @__PURE__ */ c(
          $u,
          {
            nodes: $t,
            edges: Io,
            onlyRenderVisibleElements: !0,
            nodeTypes: mg,
            edgeTypes: gg,
            onNodesChange: ye,
            onEdgesChange: So,
            onConnect: Cr,
            onConnectStart: Rr,
            onConnectEnd: Qn,
            isValidConnection: er,
            connectionLineStyle: {
              stroke: "var(--ws-green)",
              strokeWidth: 2.5,
              strokeLinecap: "round",
              strokeDasharray: "8 6"
            },
            onEdgeClick: Gt,
            onNodeClick: (p, g) => {
              if (n) {
                if (rt.current) {
                  rt.current = !1;
                  return;
                }
                Ee(""), Ce(null);
              }
            },
            onNodeContextMenu: lt,
            onNodeDragStart: (p, g) => {
              n && Ze(g.id);
            },
            onNodeDrag: He,
            onNodeDragStop: Ht,
            onDragOver: sn,
            onDrop: zn,
            onNodeMouseEnter: (p, g) => Pe(g.id),
            onNodeMouseLeave: () => Pe(""),
            onInit: (p) => {
              const g = p;
              we(g), s.x != null && s.y != null && s.zoom != null ? (g.setViewport?.({
                x: s.x,
                y: s.y,
                zoom: s.zoom
              }), ut(s.zoom)) : ut(g.getZoom?.() || 1);
            },
            onMove: (p, g) => {
              ut(
                (x) => Math.abs(x - g.zoom) > 5e-3 ? g.zoom : x
              );
            },
            onMoveEnd: (p, g) => {
              A({
                x: g.x,
                y: g.y,
                zoom: g.zoom
              });
            },
            onPaneClick: Dn,
            onPaneContextMenu: wn,
            nodesDraggable: n,
            nodesConnectable: n,
            nodesFocusable: n,
            edgesFocusable: n,
            elementsSelectable: n,
            deleteKeyCode: null,
            multiSelectionKeyCode: ["Control", "Meta"],
            panOnDrag: n,
            panOnScroll: !1,
            zoomOnScroll: n,
            zoomOnPinch: n,
            snapToGrid: hn,
            snapGrid: [18, 18],
            zoomOnDoubleClick: !1,
            minZoom: 0.35,
            maxZoom: 1.45,
            defaultEdgeOptions: {
              type: "animated",
              animated: !1
            },
            fitView: s.zoom == null,
            fitViewOptions: { padding: 0.32, maxZoom: 0.72 },
            children: n && Vt && r.length > 0 ? /* @__PURE__ */ c(
              ju,
              {
                position: "bottom-left",
                pannable: !0,
                zoomable: !0,
                nodeClassName: (p) => p.type === "storyboardFrame" ? "ws-minimap-storyboard-frame" : "",
                nodeColor: (p) => p.type === "storyboardFrame" ? "transparent" : Th(p.data)
              }
            ) : null
          }
        ),
        At ? /* @__PURE__ */ c(
          "div",
          {
            className: "ws-canvas-selection-marquee",
            style: At,
            "aria-hidden": "true"
          }
        ) : null,
        n ? /* @__PURE__ */ c(
          Jl,
          {
            showMiniMap: Vt,
            snapToGrid: hn,
            zoom: et,
            onToggleMiniMap: () => yn((p) => !p),
            onToggleSnap: () => _t((p) => !p),
            onReset: Mr,
            onZoomIn: Ro,
            onZoomOut: ko,
            onZoomChange: nr
          }
        ) : null,
        n && r.length === 0 ? /* @__PURE__ */ v("div", { className: "ws-empty-note", role: "note", children: [
          /* @__PURE__ */ v("span", { className: "ws-empty-action", children: [
            /* @__PURE__ */ c(Ma, { size: 16 }),
            /* @__PURE__ */ c("strong", { children: "双击屏幕" })
          ] }),
          /* @__PURE__ */ c("span", { className: "ws-empty-copy", children: "画布自由生成" })
        ] }) : null,
        n && De && (pe || bn) ? /* @__PURE__ */ c(
          Xl,
          {
            point: De,
            canShowDetail: !!(pe && ft(pe)),
            canCopy: !!(pe && !En),
            canDelete: !!(bn || !En),
            canEditStructure: !!(Fn && Fn.id !== pe?.id),
            canResetStoryboardPrompt: !!(pe && mc(pe)),
            onClose: St,
            onCopy: Ar,
            onDelete: vo,
            onDetail: Tr,
            onEditStructure: tr,
            onResetStoryboardPrompt: Co
          }
        ) : null
      ]
    }
  );
}
function Ig({
  request: e,
  onClose: t
}) {
  const [n, r] = $(!1);
  async function o() {
    if (n)
      return;
    r(!0);
    const s = e.onConfirm;
    t();
    try {
      Promise.resolve(s()).catch((i) => {
        ne.error(i instanceof Error ? i.message : "操作失败");
      });
    } catch (i) {
      ne.error(i instanceof Error ? i.message : "操作失败");
    }
  }
  return /* @__PURE__ */ c(
    "div",
    {
      className: "ws-confirm-backdrop",
      role: "dialog",
      "aria-modal": "true",
      onMouseDown: t,
      children: /* @__PURE__ */ v(
        "section",
        {
          className: "ws-confirm-card",
          onMouseDown: (s) => s.stopPropagation(),
          children: [
            /* @__PURE__ */ v("div", { className: "ws-confirm-copy", children: [
              /* @__PURE__ */ c("h3", { children: e.title }),
              /* @__PURE__ */ c("p", { children: e.description })
            ] }),
            /* @__PURE__ */ v("div", { className: "ws-confirm-actions", children: [
              /* @__PURE__ */ c("button", { type: "button", disabled: n, onClick: t, children: "取消" }),
              /* @__PURE__ */ c(
                "button",
                {
                  type: "button",
                  className: e.tone === "danger" ? "is-danger" : "is-primary",
                  disabled: n,
                  onClick: () => {
                    o();
                  },
                  children: n ? "处理中..." : e.confirmText || "确认"
                }
              )
            ] })
          ]
        }
      )
    }
  );
}
function wt(e) {
  try {
    return Ia(e);
  } catch {
    return null;
  }
}
function vn(e) {
  try {
    return Xd(e);
  } catch {
    return "";
  }
}
function Sg(e, t) {
  return Object.keys(t).length === 0 ? e : {
    ...e,
    nodes: e.nodes.map((n) => {
      const r = t[n.id];
      return r ? { ...n, ...r } : n;
    })
  };
}
function vg(e, t, n) {
  const r = e[t];
  if (!r)
    return e;
  const o = Object.keys(n);
  if (!o.some(
    (a) => Object.prototype.hasOwnProperty.call(r, a)
  ))
    return e;
  const s = { ...r };
  for (const a of o)
    delete s[a];
  const i = { ...e };
  return Object.keys(s).length === 0 ? delete i[t] : i[t] = s, i;
}
function Ui({
  canvas: e,
  sourceNodeId: t,
  successfulNodeIds: n,
  assetCate: r,
  powers: o
}) {
  let s = e;
  const i = Math.max(2, n.size + 1);
  for (let a = 0; a < i; a += 1) {
    const d = {
      ...s,
      nodes: Dm(
        s.nodes,
        t,
        n
      )
    };
    if (s = ns({
      canvas: d,
      assetCate: r,
      powers: o
    }), !s.nodes.some((l) => {
      const f = l.storyboardItem;
      return !f || f.sourceNodeId !== t || !n.has(l.id) ? !1 : !!(f.stale || f.sourceSignature && f.resultSourceSignature !== f.sourceSignature);
    }))
      break;
  }
  return s;
}
function Jt(e, t, n) {
  const r = Zt(
    t?.output,
    t?.asset?.version?.content,
    t?.version?.content,
    t?.result?.output,
    t?.result?.asset?.version?.content,
    t?.data?.output,
    t?.data?.content,
    t?.data?.result,
    t?.data
  ), o = e.type === "agent" && r != null ? ke(r) : io(r) || bt(r), s = e.type === "power" && Nr(e.power, e.kind, e.outputType).viewMode === "storyboard" ? br([
    r,
    t?.asset?.version?.content,
    t?.version?.content,
    t?.result,
    o
  ]) : null, i = Dt(
    String(t?.asset?.kind || ""),
    String(t?.kind || ""),
    yo(e, o)
  ), a = gn(o, i), d = kt(o, ""), u = s?.summary || a.text || (tn(d) ? "" : d) || a.imageUrl || a.videoUrl || a.audioUrl || a.fileUrl || (n ? `已按提示生成：${n}` : "生成完成");
  return {
    ...s?.title && e.titleMode === "auto" ? { title: s.title } : {},
    description: u,
    resultRef: Wr(t),
    resultOutput: s || o,
    asset: t?.asset || e.asset,
    kind: t?.asset?.kind || e.power?.kind || e.kind
  };
}
function Ec(e, t) {
  const n = t.version?.content;
  return {
    ...Jt(
      e,
      {
        asset: t,
        output: n
      },
      qd(n)
    ),
    asset: t
  };
}
function js(e) {
  return Vd(e) || {
    prompt: "",
    paramValues: {},
    selectedTargetId: 0
  };
}
function Cg(e) {
  return JSON.stringify([
    e.prompt,
    e.promptContent || null,
    e.paramValues || {},
    e.selectedTargetId || 0,
    e.storyboardReferences || []
  ]);
}
function Fc(e) {
  const t = js(e), n = Rg(t.promptContent);
  return n ? { ...t, prompt: n } : t;
}
function zc(e) {
  return Fc(e.composerDraft);
}
function Rg(e) {
  return e?.parts?.some((t) => t.type === "reference") ? e.parts.map((t) => {
    if (t.type === "text")
      return t.text;
    const n = String(t.label || "").trim();
    return n.startsWith("@") ? n : `@${n}`;
  }).join("") : "";
}
function kg(e) {
  const t = zc(e);
  return e.type !== "power" && e.type !== "agent" ? e : {
    ...e,
    composerDraft: {
      ...e.composerDraft,
      prompt: t.prompt,
      promptContent: t.promptContent,
      paramValues: t.paramValues,
      selectedTargetId: t.selectedTargetId
    }
  };
}
function Vi(e, t) {
  const n = vs(e), r = t.paramValues || {};
  for (const s of e)
    s.key && Object.prototype.hasOwnProperty.call(r, s.key) && (n[s.key] = Ln(
      s,
      r[s.key]
    ));
  const o = e.find(Ms);
  return o?.key && t.prompt.trim() && (n[o.key] = t.prompt), n;
}
function no(e) {
  try {
    return JSON.stringify(e);
  } catch {
    return "";
  }
}
function Bs(e, t) {
  const n = Uf(e, t);
  return {
    ...n,
    id: xg(e, t, n.id)
  };
}
function xg(e, t, n) {
  const r = Number(t.approval?.id || 0);
  if (r > 0)
    return `${e.id}:${r}`;
  const o = String(t.interaction?.interaction?.id || "");
  if (o)
    return `${e.id}:${o}`;
  const s = no({
    title: t.title,
    fields: (t.fields || []).map((i) => i.key),
    content: t.approval?.content
  });
  return s ? `${e.id}:feedback:${Hc(s)}` : n;
}
function is(e, t) {
  const n = Number(t.prompt?.approval?.id || 0), r = e.findIndex(
    (o) => o.id === t.id || n > 0 && Number(o.prompt?.approval?.id || 0) === n
  );
  return r < 0 ? [...e, t] : e.map((o, s) => {
    if (s !== r)
      return o;
    const i = o.status === "submitted", a = o.values || o.prompt?.values || t.values;
    return {
      ...o,
      title: t.title,
      description: t.description,
      prompt: {
        ...t.prompt,
        values: a || t.prompt.values || {}
      },
      values: o.values,
      status: i ? o.status : t.status,
      submittedAt: o.submittedAt
    };
  });
}
const Ki = 3600 * 1e3, Oc = 2e3, Ag = 3;
async function Fo(e) {
  const t = ry(e.startNode.id), n = /* @__PURE__ */ new Set();
  let r = !1, o = "0-0", s = await Rl({
    projectId: e.projectId,
    assetCateId: Number(e.assetCate.id || 0),
    startNodeId: e.startNode.id,
    requestId: t,
    singleNode: e.singleNode,
    executionScope: e.executionScope,
    canvas: {
      assetCateId: Number(e.assetCate.id || 0),
      nextNodeNo: xa(e.nodes),
      nodes: e.nodes,
      edges: e.edges,
      viewport: e.viewport || {}
    },
    runInput: {
      ...e.runInput || {},
      start_node_id: e.startNode.id
    }
  });
  for (let i = 0; i < 8; i += 1) {
    const a = await Tg(
      e,
      s,
      o,
      (l) => {
        const f = Wc(
          e,
          l,
          n
        );
        Uc(e, l), r = r || f > 0;
      },
      () => r,
      (l) => {
        o = l;
      }
    );
    if (e.canvasRun = a, Vn(e, a), ro(
      e,
      a,
      r
    ) && (a.status === "running" || a.status === "pending")) {
      ne.info("节点结果已返回，后台运行仍在收尾");
      return;
    }
    const d = Number(a.executed || 0);
    !e.singleNode && e.patchStartNodeResult !== !1 && e.onNodeResult(
      e.startNode.id,
      ch(
        a,
        ly(a, d)
      )
    );
    const u = String(a.status || "").toLowerCase();
    if (u !== "waiting") {
      if (Vc(e, a), u === "fail" || u === "error")
        throw new Error(La(a));
      if (u === "canceled" || u === "cancelled")
        throw new Error("画布运行已取消");
      return;
    }
    await sy(e, a), s = {
      ...a,
      status: "running",
      pending_node: null
    };
  }
  throw new Error("画布运行多次等待反馈，请稍后继续");
}
async function Tg(e, t, n, r, o, s) {
  let i = ar(
    e,
    Wn(t)
  );
  if (i = cs(e, i), r(i.node_results || []), Vn(e, i), i.status !== "running" && i.status !== "pending" || !i.run_id && !i.request_id)
    return i;
  const a = String(i.request_id || ""), d = new AbortController(), u = Date.now() + Ki;
  let l = !1, f = null;
  const b = window.setTimeout(() => {
    l = !0, d.abort();
  }, Ki);
  try {
    const _ = await Og(
      e,
      a,
      n,
      (w) => {
        w.stream_id && s(w.stream_id);
        const D = jg(w, e);
        if (!D) {
          Kg(e, w);
          return;
        }
        i = ar(
          e,
          us(i, D)
        ), r(i.node_results || []), Vn(e, i);
      },
      d.signal
    );
    _ && (i = ar(
      e,
      us(i, _)
    ), Vn(e, i)), r(i.node_results || []);
  } catch (_) {
    f = _;
  } finally {
    window.clearTimeout(b), d.abort(), e.runningNodeBatcher?.flush();
  }
  if (i = cs(e, i), as(e, i) && !ro(
    e,
    i,
    o()
  ))
    try {
      i = await Mg(
        e,
        i,
        a,
        r,
        o,
        u
      );
    } catch (_) {
      throw f instanceof Error && !l ? f : _;
    }
  if (!as(e, i) || ro(
    e,
    i,
    o()
  ))
    return i;
  throw f instanceof Error && !l ? f : new Error("画布仍在运行，请稍后刷新查看结果");
}
async function Mg(e, t, n, r, o, s) {
  let i = t, a = 0;
  for (; ; ) {
    if (Date.now() >= s)
      throw new Error("画布仍在运行，请稍后刷新查看结果");
    try {
      i = await Dg(
        e,
        i,
        n,
        r
      ), a = 0;
    } catch (d) {
      if (a += 1, a >= Ag)
        throw d;
    }
    if (!as(e, i) || ro(
      e,
      i,
      o()
    ))
      return i;
    await zg(
      Math.min(Oc, s - Date.now())
    );
  }
}
function ro(e, t, n) {
  return !!(e.singleNode && !jc(e) && n && !Pg(t));
}
function Pg(e) {
  return String(e.status || "").trim().toLowerCase() === "waiting" || Bc(e) ? !0 : [
    e.pending_node,
    e.output,
    ...e.node_results || []
  ].some((r) => !!dn(r));
}
async function Dg(e, t, n, r) {
  let o = t;
  const s = Number(o.run_id || 0), i = String(o.request_id || n || "");
  if (!s && !i)
    return o;
  const a = await Al({
    projectId: e.projectId,
    runId: s,
    requestId: i
  });
  return o = ar(
    e,
    us(o, Wn(a))
  ), o = cs(e, o), r(o.node_results || []), Vn(e, o), o;
}
function as(e, t) {
  const n = String(t.status || "").trim().toLowerCase();
  return n !== "running" && n !== "pending" ? !1 : !$c(e, t);
}
function $c(e, t) {
  return Zg(
    t
  ) ? !0 : e.singleNode ? (t.node_results || []).some(
    (n) => n.node_key === e.startNode.id && Ir(en(n))
  ) : !1;
}
function cs(e, t) {
  return Eg(e, t) ? {
    ...t,
    status: Fg(t.node_results || [])
  } : t;
}
function Eg(e, t) {
  const n = String(t.status || "").trim().toLowerCase();
  return n !== "running" && n !== "pending" ? !1 : $c(e, t);
}
function Fg(e) {
  for (const t of e) {
    const n = en(t);
    if (n === "fail")
      return "fail";
    if (n === "canceled" || n === "cancelled")
      return "canceled";
  }
  return "success";
}
function zg(e) {
  return new Promise((t) => {
    window.setTimeout(t, e);
  });
}
async function Og(e, t, n, r, o) {
  let s = null;
  return await zf({
    projectId: e.projectId,
    requestId: t,
    lastId: n,
    signal: o,
    onFrame: (i) => {
      if (r(i), String(i.type || "").toLowerCase() === "result") {
        if ($g(i))
          throw new Error(i.msg || "画布流返回失败");
        s = ar(
          e,
          Wn(i.output || {})
        );
      }
    }
  }), s;
}
function $g(e) {
  return Number(e.status || 0) === 2;
}
function ar(e, t) {
  if (!e.singleNode || jc(e))
    return t;
  const n = (t.node_results || []).find(
    (d) => d.node_key === e.startNode.id
  ), r = String(t.status || n?.status || "");
  if (n && (r !== "waiting" || t.pending_node))
    return t;
  const o = Bc(t), s = Zt(n?.output, t.output), i = {
    execution_id: Number(t.execution_id || n?.execution_id || 0),
    node_key: e.startNode.id,
    node_type: e.startNode.type,
    node_run_id: Number(n?.node_run_id || 0),
    run_id: Number(t.run_id || n?.run_id || 0),
    request_id: String(t.request_id || n?.request_id || ""),
    status: r || "success",
    error: n?.error || t.error,
    output: s,
    asset: n?.asset,
    version: n?.version,
    result: {
      ...n?.result || {},
      run_id: t.run_id,
      request_id: t.request_id,
      flow_run_id: t.flow_run_id,
      release_id: t.release_id,
      status: r || "success",
      error: n?.error || t.error,
      output: s,
      approval: o
    },
    approval: Zt(n?.approval, o),
    interaction: Zt(
      n?.interaction,
      dn(n?.result),
      dn(t.pending_node),
      dn(t.output)
    ),
    persists_result: !!n?.persists_result,
    agent_run_id: Number(n?.agent_run_id || 0)
  }, a = [
    ...(t.node_results || []).filter(
      (d) => d.node_key !== e.startNode.id
    ),
    i
  ];
  return {
    ...t,
    node_results: a,
    pending_node: r === "waiting" ? {
      ...i,
      status: "waiting",
      approval: i.approval,
      interaction: i.interaction
    } : t.pending_node
  };
}
function jc(e) {
  return e.singleNode && e.startNode.type === "group";
}
function Bc(e) {
  const t = e.output;
  return (Array.isArray(e.approvals) ? e.approvals : t && typeof t == "object" && Array.isArray(t.approvals) ? t.approvals : t && typeof t == "object" && Array.isArray(t.data?.approvals) ? t.data.approvals : []).find(
    (r) => r && typeof r == "object" && (r.status === "pending" || r.decision === "pending")
  );
}
function jg(e, t) {
  if (String(e.type || "").toLowerCase() === "result")
    return Wn(e.output || {});
  const n = e.output || {}, r = String(n.event || "");
  if (String(n.scope || "") === "canvas_child" && r !== "waiting" || r !== "node_finished" && r !== "waiting")
    return null;
  const o = Bg(n, {
    requireDisplayableResult: r !== "waiting",
    node: t?.nodes.find(
      (s) => s.id === String(n.node_key || n.node_id || "")
    )
  });
  return o ? {
    execution_id: Number(n.execution_id || 0),
    request_id: String(e.request_id || n.parent_request_id || ""),
    run_id: Number(n.parent_run_id || n.run_id || 0),
    flow_run_id: Number(n.parent_flow_run_id || n.flow_run_id || 0),
    release_id: Number(n.release_id || 0),
    status: r === "waiting" ? "waiting" : "running",
    node_results: [o],
    pending_node: r === "waiting" ? o : null
  } : null;
}
function Bg(e, t = {}) {
  const n = String(e.node_key || e.node_id || "");
  if (!n)
    return null;
  const r = e.output, o = r && typeof r == "object" ? r : {};
  return t.requireDisplayableResult && !Vg(e, o, t.node) ? null : {
    node_key: n,
    execution_id: Number(
      e.execution_id || o.execution_id || 0
    ),
    node_type: String(e.node_type || ""),
    node_run_id: Number(e.node_run_id || 0),
    run_id: Number(e.run_id || o.run_id || 0),
    request_id: String(e.request_id || o.request_id || ""),
    child_run_id: Number(
      e.child_run_id || o.child_run_id || 0
    ),
    child_request_id: String(
      e.child_request_id || o.child_request_id || ""
    ),
    status: String(e.status || ""),
    error: String(e.error || o.error || ""),
    output: o.output ?? r,
    asset: o.asset,
    version: o.version,
    result: o,
    approval: Ug(e, o),
    interaction: Zt(
      e.interaction,
      o.interaction,
      dn(o)
    ),
    persists_result: !!e.persists_result,
    agent_run_id: Number(
      e.agent_run_id || o.agent_run_id || 0
    )
  };
}
function Ug(e, t) {
  const n = Zt(
    e.approval,
    t.approval,
    t.result?.approval
  );
  if (n && typeof n == "object")
    return n;
  const r = Number(
    Zt(
      e.approval_id,
      t.approval_id,
      t.result?.approval_id
    ) || 0
  );
  return r > 0 ? { id: r } : void 0;
}
function dn(e, t = 0) {
  if (!e || typeof e != "object" || Array.isArray(e) || t > 6)
    return;
  const n = e;
  if (n.interaction && typeof n.interaction == "object" && !Array.isArray(n.interaction))
    return n.interaction;
  for (const o of ["result", "pending_node"]) {
    const s = dn(n[o], t + 1);
    if (s)
      return s;
  }
  const r = Array.isArray(n.node_results) ? n.node_results : [];
  for (const o of r) {
    const s = dn(o, t + 1);
    if (s)
      return s;
  }
}
function Vg(e, t, n) {
  return e.persists_result || String(e.node_type || "") !== "function" || String(
    e.function_key || t.function_key || n?.functionOption?.key || ""
  ) === "display" ? !0 : !!(t.asset || t.version || t.asset?.version || t.data?.asset || t.data?.version);
}
function Kg(e, t) {
  if (!e.setRunningNode)
    return;
  const n = t.output || {}, r = String(n.event || ""), o = String(n.node_key || n.node_id || "");
  if (o) {
    if (r !== "node_output" && e.runningNodeBatcher?.flush(), r === "node_started") {
      e.setRunningNode((s) => ({
        ...s,
        [o]: {
          nodeId: o,
          title: String(n.node_name || n.node_key || o),
          startedAt: Date.now(),
          status: "running",
          progress: Math.max(s[o]?.progress || 0, 18),
          agent: s[o]?.agent
        }
      }));
      return;
    }
    if (r === "node_output") {
      const s = (i) => {
        const a = i[o];
        if (!a || a.status !== "running")
          return i;
        const d = Li(n.output), u = String(n.node_type || "").toLowerCase(), l = u === "power", f = u === "agent", b = String(
          d.semantic_event || d.event || ""
        ).toLowerCase(), _ = Li(d.meta), w = l && b === "status" && !!String(_.output_type || ""), D = Number(_.generated_count || 0), A = w ? Math.max(
          a.generatedCount || 0,
          Number.isFinite(D) ? D : 0
        ) : a.generatedCount, z = l && typeof d.text == "string" && (b === "delta" || !b) ? d.text : "", V = l && b === "audio_ready" ? d : a.streamOutput;
        return {
          ...i,
          [o]: {
            ...a,
            progress: Math.max(a.progress, 72),
            streamText: z ? `${a.streamText || ""}${z}` : a.streamText,
            streamOutput: V,
            streamStarted: a.streamStarted || !!z || !!V,
            ...w ? { streamStarted: !0, generatedCount: A } : {},
            agent: f ? sf(a.agent, d) : a.agent
          }
        };
      };
      e.runningNodeBatcher ? e.runningNodeBatcher.enqueue(s) : e.setRunningNode(s);
    }
  }
}
function Li(e) {
  return !e || typeof e != "object" || Array.isArray(e) ? {} : e;
}
function us(e, t) {
  const n = [...e.node_results || []];
  for (const r of t.node_results || []) {
    const o = so(r), s = n.findIndex(
      (i) => so(i) === o
    );
    s >= 0 ? n[s] = r : n.push(r);
  }
  return {
    ...e,
    ...t,
    node_runs: t.node_runs?.length ? t.node_runs : e.node_runs,
    execution_plan: t.execution_plan || e.execution_plan,
    node_results: n,
    pending_node: t.status === "waiting" ? t.pending_node || e.pending_node : t.pending_node || null
  };
}
function Uc(e, t) {
  if (!e.setRunningNode)
    return;
  const n = t.filter(
    (r) => Ir(r.status)
  );
  n.length !== 0 && (e.setRunningNode(
    (r) => Lg(e, r, n)
  ), window.setTimeout(() => {
    e.setRunningNode?.((r) => {
      let o = !1, s = r;
      for (const i of n) {
        const a = i.node_key, d = s[a];
        !d || d.status === "running" || (s === r && (s = { ...r }), delete s[a], o = !0);
      }
      return o ? s : r;
    });
  }, 650));
}
function Lg(e, t, n) {
  let r = !1;
  const o = { ...t };
  for (const s of n) {
    const i = s.node_key, a = e.nodes.find((u) => u.id === i);
    if (s.status === "waiting") {
      o[i] = {
        nodeId: i,
        title: a?.title || i,
        startedAt: t[i]?.startedAt || Date.now(),
        progress: 92,
        status: "waiting"
      }, r = !0;
      continue;
    }
    const d = o[i];
    d && (o[i] = {
      ...d,
      progress: 100,
      status: s.status === "success" ? "success" : "error",
      agent: a?.type === "agent" ? af(s.output) : d.agent
    }, r = !0);
  }
  return r ? o : t;
}
function Vc(e, t, n) {
  if (!e.setRunningNode || t.status === "running" || t.status === "pending" || t.status === "waiting")
    return;
  const r = t.status === "success" || t.status === "canceled" ? "success" : "error";
  e.setRunningNode((o) => {
    let s = !1;
    const i = { ...o }, a = qi(
      e,
      t,
      o,
      n
    );
    for (const d of a) {
      const u = i[d];
      u && (i[d] = {
        ...u,
        progress: r === "success" ? 100 : Math.max(u.progress, 92),
        status: r
      }, s = !0);
    }
    return s ? i : o;
  }), window.setTimeout(
    () => {
      e.setRunningNode?.((o) => {
        let s = !1, i = o;
        const a = qi(
          e,
          t,
          o,
          n
        );
        for (const d of a) {
          const u = i[d];
          !u || u.status === "running" || (i === o && (i = { ...o }), delete i[d], s = !0);
        }
        return s ? i : o;
      });
    },
    r === "success" ? 650 : 1200
  );
}
function qi(e, t, n, r) {
  const o = new Set(
    Gg(e, t).filter(
      (s) => !r || r.has(s)
    )
  );
  return Object.keys(n).filter((s) => o.has(s));
}
function qg(e, t, n) {
  if (!e.setRunningNode)
    return;
  const r = String(t.status || "").trim().toLowerCase();
  if (!["running", "pending", "waiting"].includes(r))
    return;
  const o = new Set(
    (t.node_results || []).filter(
      (l) => Ir(en(l))
    ).map((l) => l.node_key)
  ), s = /* @__PURE__ */ new Map();
  let i = !1, a = "";
  for (const l of t.node_runs || []) {
    const f = String(l.node_key || "");
    if (!f || o.has(f) || n && !n.has(f))
      continue;
    i = !0;
    const b = String(l.status || "").trim().toLowerCase();
    b === "running" ? s.set(f, "running") : b === "waiting" ? s.set(f, "waiting") : b === "pending" && !a && (a = f);
  }
  const d = String(t.pending_node?.node_key || "");
  if (d && !o.has(d) && (!n || n.has(d)) && s.set(d, "waiting"), s.size === 0 && a && s.set(a, "running"), s.size === 0 && !i) {
    const l = String(t.start_node_id || "");
    l && !o.has(l) && (!n || n.has(l)) && s.set(
      l,
      r === "waiting" ? "waiting" : "running"
    );
  }
  if (s.size === 0)
    return;
  const u = Date.parse(String(t.created_at || ""));
  e.setRunningNode((l) => {
    let f = !1;
    const b = { ...l };
    for (const [_, w] of s) {
      const D = e.nodes.find((z) => z.id === _);
      if (!D)
        continue;
      const A = l[_];
      A?.status !== w && (b[_] = {
        nodeId: _,
        title: D.title,
        startedAt: A?.startedAt || (Number.isFinite(u) ? u : Date.now()),
        progress: w === "waiting" ? 92 : A?.progress || 0,
        status: w
      }, f = !0);
    }
    return f ? b : l;
  });
}
function Gg(e, t) {
  const n = new Set(Us(t));
  return e.singleNode && n.add(e.startNode.id), [...n];
}
function Hg(e, t) {
  const n = Number(e.asset_cate_id || 0);
  return n === 0 || n === Number(t || 0);
}
function Us(e) {
  const t = /* @__PURE__ */ new Set(), n = String(e.start_node_id || "");
  n && t.add(n);
  for (const r of e.node_runs || [])
    r.node_key && t.add(r.node_key);
  for (const r of e.node_results || [])
    r.node_key && t.add(r.node_key);
  for (const r of e.execution_plan?.nodes || [])
    r.id && t.add(r.id);
  return [...t];
}
function Yg(e) {
  return Kc(e).length > 0;
}
function Kc(e) {
  const t = /* @__PURE__ */ new Set(), n = [];
  for (const r of e) {
    let o = !1;
    for (const i of Us(r))
      t.has(i) || (t.add(i), o = !0);
    if (!o)
      continue;
    const s = String(r.status || "").trim().toLowerCase();
    (s === "running" || s === "pending" || s === "waiting") && n.push(r);
  }
  return n;
}
function zo(e) {
  return e.map(Lc).filter((t) => !!t);
}
function Lc(e) {
  const t = Wn(e);
  return t.run_id || t.request_id ? t : null;
}
function Wg(e, t) {
  const n = /* @__PURE__ */ new Set();
  for (const r of e) {
    const o = Number(r.run_id || 0);
    o > 0 && !Xg(r, t) && n.add(o);
  }
  return [...n];
}
function Xg(e, t) {
  const n = Number(e.asset_cate_id || 0);
  return (n ? [t[String(n)]].filter(
    (o) => !!o
  ) : Object.values(t)).some(
    (o) => qc(e, o)
  );
}
function qc(e, t) {
  const n = String(e.status || "").trim().toLowerCase();
  if (!["success", "fail", "failed", "error", "canceled", "cancelled"].includes(
    n
  ))
    return !1;
  const r = new Map(t.nodes.map((s) => [s.id, s]));
  if (!e.single_node) {
    const s = (e.node_results || []).filter((a) => a.node_key);
    if (s.length === 0)
      return !1;
    let i = 0;
    for (const a of s) {
      const d = r.get(a.node_key);
      if (d && (i += 1, !Gi(d.resultRef, e, a)))
        return !1;
    }
    return i > 0;
  }
  const o = r.get(String(e.start_node_id || ""));
  return Gi(o?.resultRef, e);
}
function Gi(e, t, n) {
  if (!e)
    return !1;
  const r = Number(t.execution_id || 0), o = Number(e.execution_id || 0);
  if (r > 0 && o > 0 && o >= r)
    return !0;
  const s = Number(t.run_id || 0), i = Number(e.run_id || 0);
  if (s > 0 && i > 0 && i >= s)
    return !0;
  const a = Number(n?.node_run_id || 0), d = Number(e.node_run_id || 0);
  if (a > 0 && d > 0 && d >= a)
    return !0;
  const u = String(n?.request_id || t.request_id || "");
  return !!(u && e.request_id === u);
}
function oo(e) {
  const t = Number(e.execution_id || 0);
  if (t > 0)
    return `execution:${t}`;
  const n = Number(e.run_id || 0);
  return n > 0 ? `run:${n}` : `request:${String(e.request_id || "")}`;
}
function Hi(e, t) {
  const n = /* @__PURE__ */ new Map();
  for (const r of e)
    n.set(oo(r), r);
  for (const r of t)
    n.set(oo(r), r);
  return [...n.values()].sort(Jg).slice(0, 50);
}
function Jg(e, t) {
  const n = Number(t.execution_id || 0) - Number(e.execution_id || 0);
  if (n !== 0)
    return n;
  const r = Yi(t) - Yi(e);
  return r !== 0 ? r : Number(t.run_id || 0) - Number(e.run_id || 0);
}
function Yi(e) {
  const t = Date.parse(String(e.updated_at || e.created_at || ""));
  return Number.isFinite(t) ? t : 0;
}
function Zg(e) {
  const t = Qg(e);
  if (t.size === 0)
    return !1;
  const n = /* @__PURE__ */ new Set();
  for (const r of e.node_results || []) {
    const o = en(r);
    if (o === "waiting" || o === "running" || o === "pending")
      return !1;
    Ir(o) && n.add(r.node_key);
  }
  for (const r of t)
    if (!n.has(r))
      return !1;
  return !0;
}
function Qg(e) {
  const t = /* @__PURE__ */ new Set();
  for (const o of e.execution_plan?.nodes || [])
    ey(o) && t.add(o.id);
  if (t.size > 0)
    return t;
  const n = /* @__PURE__ */ new Set();
  for (const o of e.node_runs || [])
    o.node_key && n.add(o.node_key);
  if (n.size > 0)
    return n;
  const r = String(e.start_node_id || "");
  return r && (e.node_results || []).some((o) => o.node_key === r) ? /* @__PURE__ */ new Set([r]) : /* @__PURE__ */ new Set();
}
function ey(e) {
  return ["asset", "power", "agent", "flow"].includes(String(e.type || "")) ? !0 : e.type !== "function" ? !1 : e.function_key === "save" || e.function_key === "display";
}
function ty(e, t) {
  const n = e.start_node_id || e.execution_plan?.order?.[0] || e.execution_plan?.nodes?.[0]?.id || "";
  if (n) {
    const r = t.find((o) => o.id === n);
    if (r)
      return r;
  }
  return t.find(ah) || t[0] || null;
}
function ny(e, t) {
  return [
    e.run_id || e.request_id || "",
    so(t)
  ].join(":");
}
function Ir(e) {
  const t = String(e || "").trim().toLowerCase();
  return t === "success" || t === "fail" || t === "canceled" || t === "cancelled";
}
function en(e) {
  if (!e)
    return "";
  const t = String(e.status || e.result?.status || "").trim().toLowerCase();
  return t === "error" ? "fail" : t === "cancelled" ? "canceled" : t;
}
function ry(e) {
  return `canvas-${typeof crypto < "u" && typeof crypto.randomUUID == "function" ? crypto.randomUUID() : `${Date.now()}-${Math.random().toString(36).slice(2)}`}-${e}`.slice(0, 64);
}
function Gc(e, t, n) {
  const r = typeof n == "string" ? n : no(n), o = Math.floor(Date.now() / 5e3);
  return `${e}-${t}-${o}-${Hc(r)}`.slice(
    0,
    96
  );
}
function Hc(e) {
  let t = 5381;
  for (let n = 0; n < e.length; n += 1)
    t = t * 33 ^ e.charCodeAt(n);
  return (t >>> 0).toString(36);
}
function Vn(e, t, n) {
  oy(e, t), qg(e, t, n);
}
function oy(e, t) {
  if (t.status !== "waiting")
    return;
  const n = t.pending_node;
  if (!n?.node_key)
    return;
  const r = e.nodes.find((d) => d.id === n.node_key);
  if (!r)
    return;
  const o = Vs(n, r);
  if (!o)
    return;
  const s = Bs(r, o), i = kn(r), a = is(
    i,
    s
  );
  if (no(i) !== no(a)) {
    const d = {
      ...r,
      feedbackRequests: a
    };
    e.nodes = e.nodes.map(
      (u) => u.id === r.id ? d : u
    ), e.onNodeResult(r.id, { feedbackRequests: a });
  }
  e.setRunningNode?.((d) => ({
    ...d,
    [r.id]: {
      nodeId: r.id,
      title: r.title,
      startedAt: d[r.id]?.startedAt || Date.now(),
      progress: 92,
      status: "waiting"
    }
  }));
}
async function sy(e, t) {
  const n = t.pending_node;
  if (!n?.node_key)
    throw new Error("画布运行等待反馈，但缺少等待节点");
  const r = e.nodes.find((i) => i.id === n.node_key);
  if (!r)
    throw new Error("画布运行等待节点不存在");
  const o = Vs(n, r);
  if (!o || !e.requestFlowFeedback)
    throw new Error(`${r.title} 需要补充信息，请单独处理后继续`);
  const s = await e.requestFlowFeedback({ node: r, prompt: o });
  return Yc(
    e.projectId,
    t,
    n,
    o,
    s
  );
}
async function Yc(e, t, n, r, o) {
  return r.interaction ? Tl({
    projectId: e,
    runId: Number(r.interaction.runId || n.child_run_id || 0),
    nodeRunId: Number(r.interaction.nodeRunId || 0),
    interactionId: String(r.interaction.interaction.id || ""),
    data: o
  }) : xl({
    projectId: e,
    runId: Number(t.run_id || 0),
    requestId: String(t.request_id || ""),
    nodeKey: n.node_key,
    approvalId: Number(r.approval.id || 0),
    feedback: o
  });
}
function iy(e, t, n) {
  for (const r of e) {
    if (String(r.status || "").trim().toLowerCase() !== "waiting")
      continue;
    const o = r.pending_node;
    if (!o || o.node_key !== t.id)
      continue;
    const s = Vs(o, t);
    if (!s)
      continue;
    if (Bs(t, s).id === n.id)
      return { run: r, pending: o, prompt: s };
  }
  return null;
}
function Vs(e, t) {
  const n = e.interaction && typeof e.interaction == "object" ? e.interaction : dn(e);
  if (n?.interaction?.id)
    return Wa({
      runId: Number(n.run_id || e.child_run_id || 0),
      nodeRunId: Number(n.node_run_id || 0),
      interaction: n.interaction
    });
  if ((e.node_type || t.type) === "flow") {
    const o = e.output && typeof e.output == "object" ? { ...e.output } : e.result && typeof e.result == "object" ? { ...e.result } : {}, s = Zt(
      e.approval,
      e.result?.approval,
      e.output?.approval
    );
    s && !Array.isArray(o.approvals) && (o.approvals = [s]);
    const i = Lf(o);
    return qf(i);
  }
  return Hf(
    Zc(e),
    t.title
  );
}
function Wc(e, t, n) {
  let r = 0;
  const o = new Map(e.nodes.map((s) => [s.id, s]));
  for (const s of t) {
    const i = o.get(s.node_key), a = en(s);
    if (!i || !Ir(a))
      continue;
    const d = so(s);
    if (a === "success" && d && n?.has(d))
      continue;
    const u = uy(e, i, s);
    e.onNodeResult(i.id, u), d && n?.add(d), a === "success" && (r += 1);
    const l = {
      ...i,
      ...u
    };
    o.set(i.id, l), e.nodes = e.nodes.map(
      (f) => f.id === i.id ? l : f
    ), a === "success" && u.asset && e.onAssetCreated(u.asset), a === "success" && e.requestNodeTitle?.(l, s);
  }
  return r;
}
function ay(e, t) {
  const n = zc(e).prompt.trim(), o = (t ? au(e.id, t.nodes, t.edges) : null)?.text.trim() || "", s = [
    n,
    o ? `上游内容：
${o}` : ""
  ].filter(Boolean).join(`

`);
  return Array.from(s).slice(0, sg).join("");
}
function cy(e, t) {
  return e.type !== "power" || e.titleMode !== "auto" || e.storyboardItem || en(t) !== "success" || Jc(t) <= 0 || !Xc(e) ? !1 : Nr(e.power, e.kind, e.outputType).viewMode !== "storyboard";
}
function Xc(e) {
  const t = Number(e.nodeNo || 0);
  return t > 0 && e.title.trim() === Ud(e, t).trim();
}
function Jc(e) {
  const t = e, n = [
    t.version_id,
    t.versionId,
    t.version?.id,
    t.asset?.version_id,
    t.asset?.versionId,
    t.asset?.version?.id,
    t.result?.version_id,
    t.result?.versionId,
    t.result?.version?.id,
    t.result?.asset?.version_id,
    t.result?.asset?.version?.id,
    t.output?.version_id,
    t.output?.version?.id,
    t.output?.asset?.version_id,
    t.output?.asset?.version?.id,
    t.data?.version_id,
    t.data?.version?.id,
    t.data?.asset?.version_id,
    t.data?.asset?.version?.id
  ];
  for (const r of n) {
    const o = Number(r || 0);
    if (Number.isInteger(o) && o > 0)
      return o;
  }
  return 0;
}
function so(e) {
  return [
    e.node_key,
    e.execution_id || "",
    e.request_id || "",
    e.node_run_id || "",
    e.child_run_id || "",
    e.status || "",
    e.source_signature || "",
    Number(
      e.version?.id || e.asset?.version?.id || e.result?.version?.id || 0
    ),
    Number(e.asset?.id || e.result?.asset?.id || 0)
  ].join(":");
}
function uy(e, t, n) {
  const r = Zc(n), o = en(n);
  if (o === "fail")
    return Oo(t, {
      resultRef: Wr(r),
      runError: Af(n)
    });
  if (o === "canceled" || o === "cancelled")
    return Oo(t, {
      resultRef: Wr(r),
      runError: "节点运行已取消"
    });
  const s = ff({
    result: r,
    previousAsset: t.asset,
    previousAssets: e.space.assets
  }), i = (a) => Oo(
    t,
    dy(
      t,
      a,
      n.source_signature
    )
  );
  return i(s ? {
    ...Jt(
      t,
      pf(r, s),
      "后端执行结果"
    ),
    runError: ""
  } : {
    ...Jt(
      t,
      r,
      "后端执行结果"
    ),
    runError: ""
  });
}
function dy(e, t, n) {
  const r = e.storyboardItem, o = String(n || "").trim();
  if (!r || !o)
    return t;
  const s = String(r.sourceSignature || "").trim();
  return {
    ...t,
    storyboardItem: {
      ...r,
      resultSourceSignature: o,
      stale: s ? o !== s : !!r.stale
    }
  };
}
function Oo(e, t) {
  const n = kn(e);
  return n.length === 0 || Array.isArray(t.feedbackRequests) ? t : {
    ...t,
    feedbackRequests: n
  };
}
function Zc(e) {
  return {
    ...e.result && typeof e.result == "object" ? e.result : {},
    execution_id: e.execution_id || e.result?.execution_id,
    run_id: e.run_id || e.result?.run_id,
    request_id: e.request_id || e.result?.request_id,
    node_run_id: e.node_run_id || e.result?.node_run_id,
    child_run_id: e.child_run_id || e.result?.child_run_id,
    child_request_id: e.child_request_id || e.result?.child_request_id,
    status: e.status || e.result?.status,
    error: e.error || e.result?.error,
    output: e.output ?? e.result?.output,
    asset: e.asset || e.result?.asset,
    version: e.version || e.result?.version || e.asset?.version,
    agent_run_id: e.agent_run_id || e.result?.agent_run_id
  };
}
function ly(e, t) {
  return e.status === "waiting" ? `已执行 ${t} 个连接节点，等待补充信息` : e.status === "fail" || e.status === "error" ? La(
    e,
    `画布运行失败，已执行 ${t} 个连接节点`
  ) : `已执行 ${t} 个连接节点`;
}
async function fy(e) {
  const t = py(e.assetCateId);
  if (!t)
    throw new Error("当前团队没有配置资产分类，不能保存作品");
  const n = await Dl({
    projectId: e.projectId,
    assetCateId: t,
    name: e.name,
    kind: e.kind,
    content: e.content,
    runId: Number(e.runRef?.run_id || 0),
    nodeRunId: Number(e.runRef?.node_run_id || 0),
    releaseId: Number(e.runRef?.release_id || 0),
    nodeKey: e.nodeKey,
    requestId: e.requestId,
    source: e.source
  }), r = e.previousAsset || e.previousAssets?.find((o) => o.id === n.id) || null;
  return Rn(n, r);
}
function py(e) {
  return Math.max(0, Number(e || 0));
}
function Xn(e) {
  const t = nn(e), n = gn(
    t,
    yo(e, t)
  );
  return pn(n) || (n.text = kt(t, "")), n;
}
function yo(e, t) {
  const n = ky(t);
  return e.type === "power" ? Dt(
    String(e.power?.kind || ""),
    n,
    String(e.asset?.kind || ""),
    String(e.kind || "")
  ) : Dt(
    String(e.asset?.kind || ""),
    String(e.power?.kind || ""),
    n,
    String(e.kind || "")
  );
}
function $o(e) {
  const t = Xn(e);
  if (pn(t))
    return t;
  const n = gn(
    nn(e),
    String(e.kind || e.power?.kind || "")
  );
  return pn(n) || (n.text = kt(nn(e), "")), n;
}
function Ks(e) {
  return gy(e) || Iy(e);
}
function Cn(e) {
  return e.storyboardItem?.itemType === "subtitle" ? { text: e.description || "字幕轨已准备" } : nn(e);
}
function my(e) {
  return [
    e.asset?.version?.content,
    e.resultOutput,
    Cn(e)
  ];
}
function gy(e) {
  return yy(
    e.asset?.version?.content,
    e.resultOutput
  );
}
function yy(...e) {
  for (const t of e) {
    const n = An(t);
    if (n)
      return n;
  }
  return null;
}
function io(...e) {
  for (const t of e) {
    const n = Qc(t);
    if (Te(n))
      return n;
  }
  return "";
}
function Qc(e) {
  const t = ke(e), n = Sr(t);
  if (n !== t)
    return Qc(n);
  const r = ng?.(t) ?? t, o = Sn(r, /* @__PURE__ */ new Set());
  if (Te(o))
    return o;
  if (r !== t) {
    const a = Sn(t, /* @__PURE__ */ new Set());
    if (Te(a))
      return a;
  }
  const s = ho(t);
  if (s)
    return Go?.(s) ?? s;
  const i = Ls(e);
  return i !== t && Te(i) ? i : "";
}
function Sn(e, t) {
  const n = ke(e), r = Sr(n);
  if (r !== n)
    return Sn(r, t);
  if (typeof n == "string") {
    const d = tu(n);
    if (Te(d))
      return d;
    const u = dr(n);
    return u ? { text: u } : mn(n) ? "" : n;
  }
  if (Array.isArray(n)) {
    const d = n.map((u) => Sn(u, t)).filter(Te);
    return d.length > 0 ? d : "";
  }
  if (!n || typeof n != "object")
    return n;
  if (Tn(n)) {
    const d = cu(n);
    if (d !== void 0)
      return Sn(d, t);
    const u = uu(n);
    if (u)
      return { text: u };
    const l = An(n) || wt(n);
    return l ? { rich: l } : n;
  }
  if (t.has(n))
    return "";
  if (t.add(n), Ys(n))
    return ds(n);
  if (nu(n))
    return n;
  for (const d of ["output", "result", "data", "content", "json", "value"]) {
    if (!(d in n))
      continue;
    const u = Sn(n[d], t);
    if (Te(u))
      return u;
  }
  const o = qs(n);
  if (o) {
    const d = { rich: o };
    return Go?.(d) ?? d;
  }
  const s = wt(n);
  if (s)
    return { rich: s };
  const i = ho(n);
  if (i)
    return Go?.(i) ?? i;
  const a = bt(n);
  if (a !== n)
    return Sn(a, t);
  if (hy(n))
    return ds(n);
  if (No(n)) {
    const d = Dt(n.message, n.error, n.status);
    return d ? { text: d } : "";
  }
  return wo(n) ? n : "";
}
function hy(e) {
  return e && typeof e == "object" && !Array.isArray(e) && (e.format || e.result_mode || e.rich || e.images || e.videos || e.audios || e.files);
}
function ds(e) {
  const t = {}, n = ke(e.content);
  n && typeof n == "object" && !Array.isArray(n) && Xi(t, n), Xi(t, e);
  const r = wy(e);
  return r && (t.text = r), !Te(t) && n && typeof n == "object" ? n : wo(t) ? t : "";
}
function wy(e) {
  const t = Dt(e.text);
  if (t)
    return t;
  const n = ke(e.content);
  return typeof n == "string" ? n.trim() : n && typeof n == "object" && !Array.isArray(n) ? Dt(n.text) : "";
}
function Ls(e) {
  const t = Sr(e);
  if (t !== e)
    return Ls(t);
  const n = ho(e);
  if (n)
    return n;
  const r = tu(e);
  if (Te(r))
    return r;
  const o = ke(e), s = wt(o);
  if (s)
    return { rich: s };
  const i = bt(o);
  if (i !== o) {
    const a = wt(i);
    return a ? { rich: a } : i;
  }
  return o;
}
function ho(e) {
  const t = An(e);
  return t ? { rich: t } : null;
}
function An(e, t = /* @__PURE__ */ new Set()) {
  const n = ke(e), r = Sr(n);
  if (r !== n)
    return An(r, t);
  if (Array.isArray(n)) {
    if (t.has(n))
      return null;
    t.add(n);
    for (const i of n) {
      const a = An(i, t);
      if (a)
        return a;
    }
    return null;
  }
  if (!n || typeof n != "object" || t.has(n))
    return null;
  if (t.add(n), Tn(n))
    return eu(n, t) || n;
  const o = n, s = [
    ze(o, ["output", "content", "rich"]),
    ze(o, ["output", "rich"]),
    ze(o, ["content", "rich"]),
    o.content,
    o.rich,
    o.text,
    o.summary
  ];
  for (const i of s) {
    if (i === n)
      continue;
    const a = An(i, t);
    if (a)
      return a;
  }
  return null;
}
function eu(e, t) {
  const n = ao(e);
  for (const r of n) {
    const o = Wi(r, t);
    if (o)
      return o;
  }
  return Wi(n.join(""), t);
}
function Wi(e, t) {
  const n = String(e || "").trim();
  if (!mn(n))
    return null;
  const r = Aa(n);
  return r === n || r === e ? null : An(r, t);
}
function by(e) {
  return ao(e).join("");
}
function ao(e, t = /* @__PURE__ */ new Set()) {
  if (!e)
    return [];
  if (typeof e == "string")
    return [e];
  if (Array.isArray(e))
    return e.flatMap((r) => ao(r, t));
  if (typeof e != "object")
    return [];
  if (t.has(e))
    return [];
  t.add(e);
  const n = [];
  return typeof e.text == "string" && n.push(e.text), Array.isArray(e.content) && n.push(...ao(e.content, t)), n;
}
function Ny(e) {
  const t = /* @__PURE__ */ new Set();
  return e.filter((n) => {
    const r = String(n || "").trim();
    return !r || t.has(r) ? !1 : (t.add(r), !0);
  });
}
function tu(e) {
  const t = xt(e);
  if (t)
    return { rich: t };
  const n = ke(e);
  if (!n)
    return "";
  if (typeof n == "string") {
    const r = dr(n);
    return r ? { text: r } : "";
  }
  return "";
}
function xt(e, t = /* @__PURE__ */ new Set()) {
  const n = ke(e);
  if (Tn(n))
    return eu(n, t) || n;
  if (Array.isArray(n))
    return wt(Hs(n));
  if (!n || typeof n != "object" || t.has(n))
    return null;
  t.add(n);
  const r = n, o = qs(r);
  if (o)
    return o;
  const s = [
    ze(r, ["output", "content", "rich"]),
    ze(r, ["output", "content"]),
    ze(r, ["output", "rich"]),
    ze(r, ["content", "output", "content", "rich"]),
    ze(r, ["content", "output", "content"]),
    ze(r, ["content", "rich"]),
    ze(r, ["data", "output", "content", "rich"]),
    ze(r, ["data", "output", "content"]),
    ze(r, ["data", "content", "rich"]),
    r.rich,
    r.output,
    r.result,
    r.content,
    r.data,
    r.value,
    r.json,
    r.text,
    r.message
  ];
  for (const i of s) {
    if (i == null || i === n)
      continue;
    const a = xt(i, t);
    if (a)
      return a;
  }
  for (const [i, a] of Object.entries(r)) {
    if (!du(i) || !a || typeof a != "object")
      continue;
    const d = xt(a, t);
    if (d)
      return d;
  }
  return null;
}
function qs(e) {
  return Tn(e) ? e : Array.isArray(e.content) && (String(e.format || "").toLowerCase() === "rich_json" || String(e.content?.format || "").toLowerCase() === "rich_json" || e.type === void 0) ? wt({
    type: "doc",
    content: e.content
  }) : (String(e.format || "").toLowerCase() === "rich_json" || String(e.content?.format || "").toLowerCase() === "rich_json") && e.rich != null ? xt(e.rich) : (String(e.format || "").toLowerCase() === "rich_json" || String(e.content?.format || "").toLowerCase() === "rich_json") && e.content?.rich != null ? xt(e.content.rich) : null;
}
function Tn(e) {
  return !!(e && typeof e == "object" && !Array.isArray(e) && e.type === "doc" && Array.isArray(e.content));
}
function Xi(e, t) {
  for (const n of [
    "format",
    "title",
    "text",
    "reasoning",
    "rich",
    "images",
    "videos",
    "audios",
    "files",
    "json",
    "error",
    "progress",
    "meta"
  ])
    Te(t[n]) && (e[n] = n === "rich" ? _y(t[n]) : t[n]);
}
function _y(e) {
  return xt(e) || wt(Hs(e)) || wt(e) || e;
}
function wo(e) {
  return Object.entries(e).some(([t, n]) => t.startsWith("_") || t === "format" ? !1 : Te(n));
}
function nu(e) {
  return !e || typeof e != "object" || Array.isArray(e) || "output" in e || "result" in e || "data" in e || "content" in e || "kind" in e || "event" in e ? !1 : [
    "text",
    "rich",
    "images",
    "videos",
    "audios",
    "files",
    "json",
    "error"
  ].some((t) => Te(e[t]));
}
function Te(e) {
  if (e == null || e === "")
    return !1;
  if (typeof e == "string") {
    const t = e.trim();
    return t.length > 0 && !mn(t) && !Rt(t);
  }
  return typeof e == "number" || typeof e == "boolean" ? !0 : Array.isArray(e) ? e.some(Te) : typeof e != "object" ? !1 : xt(e) ? !0 : No(e) ? !1 : wo(e);
}
function Gs(e) {
  return kt(
    nn(e),
    e.description || e.title
  );
}
function Iy(e) {
  const t = [
    nn(e),
    e.asset?.version?.content,
    e.description
  ];
  for (const n of t) {
    const r = xt(n) || wt(Ls(n)) || wt(bt(n)) || wt(n);
    if (r)
      return r;
  }
  return null;
}
function kt(e, t = "") {
  const n = bt(e), r = wt(n), o = r ? vn(r).trim() : "";
  if (o && !Rt(o))
    return o;
  const s = vn(n).trim();
  if (Rt(s))
    return "";
  const i = dr(s);
  if (i)
    return i;
  if (s && !mn(s))
    return s;
  if (Ws(s)) {
    const l = vn(
      bt(ke(s))
    ).trim();
    if (l && l !== s && !Rt(l))
      return l;
  }
  const a = String(t || "").trim();
  if (Rt(a))
    return "";
  const d = dr(a);
  if (d)
    return d;
  if (!mn(a))
    return a;
  const u = vn(
    bt(ke(a))
  ).trim();
  return u && u !== a && !Rt(u) ? u : "";
}
function Rt(e) {
  const t = e.trim();
  return t ? Sy(t) || vy(t) : !1;
}
function Sy(e) {
  const t = e.trim();
  return t === "map[]" || t === "<nil>";
}
function vy(e) {
  const t = e.trim();
  return t ? /^(i\s+(will|ll|'ll)\s+(start|begin)|let'?s\s+(list|check|inspect)|first,\s*i\s+(will|ll|'ll)|i'?m\s+going\s+to\s+(check|inspect))/i.test(
    t
  ) : !1;
}
function gn(e, t) {
  const n = {
    text: "",
    imageUrl: "",
    videoUrl: "",
    audioUrl: "",
    fileUrl: ""
  }, r = bt(e);
  return yr(n, r, t), !pn(n) && r !== e && yr(n, e, t), Kn(n) && mn(n.text) && (n.text = ""), n;
}
function Cy(e, t) {
  return {
    text: Dt(e.text, t.text),
    imageUrl: e.imageUrl || t.imageUrl,
    videoUrl: e.videoUrl || t.videoUrl,
    audioUrl: e.audioUrl || t.audioUrl,
    fileUrl: e.fileUrl || t.fileUrl
  };
}
function yr(e, t, n, r = /* @__PURE__ */ new Set(), o = 0) {
  if (o > 12 || t == null)
    return;
  if (typeof t == "string") {
    Ji(e, t, n);
    return;
  }
  if (Array.isArray(t)) {
    for (const u of t)
      if (yr(e, u, n, r, o + 1), Kn(e))
        return;
    return;
  }
  if (typeof t != "object") {
    e.text = String(t);
    return;
  }
  if (r.has(t))
    return;
  r.add(t);
  const s = t, i = Ry(e, s, n), a = kt(t, "");
  a && a !== i && !tn(a) && (e.text ||= a);
  const d = jn(s.url, s.src, s.href);
  if (d && d !== i && Ji(e, d, n), e.imageUrl ||= jn(
    s.image,
    s.image_url,
    s.imageUrl,
    Xe(s.images),
    Xe(s.imageUrls)
  ), e.videoUrl ||= jn(
    s.video,
    s.video_url,
    s.videoUrl,
    Xe(s.videos),
    Xe(s.videoUrls)
  ), e.audioUrl ||= jn(
    s.audio,
    s.audio_url,
    s.audioUrl,
    Xe(s.audios),
    Xe(s.audioUrls)
  ), e.fileUrl ||= jn(
    s.file,
    s.file_url,
    s.fileUrl,
    Xe(s.files),
    Xe(s.fileUrls)
  ), !Kn(e)) {
    for (const u of ["output", "result", "content", "body", "data", "rich"])
      if (s[u] && typeof s[u] == "object" && (yr(e, s[u], n, r, o + 1), Kn(e)))
        return;
  }
  if (!e.text && !pn(e) && !Dy(s) && wo(s))
    try {
      const u = JSON.stringify(t, null, 2);
      Gn(u) || (e.text = u);
    } catch {
      const u = String(t);
      Gn(u) || (e.text = u);
    }
}
function Ry(e, t, n) {
  const r = ru(
    n,
    t.kind,
    t.media_kind,
    t.mediaKind,
    t.media_type,
    t.mediaType,
    t.type,
    t.name
  );
  if (!r)
    return "";
  const o = jn(...xy(t, r));
  return o ? (r === "image" && (e.imageUrl ||= o), r === "video" && (e.videoUrl ||= o), r === "audio" && (e.audioUrl ||= o), r === "file" && (e.fileUrl ||= o), o) : "";
}
function ky(e) {
  if (!e || typeof e != "object" || Array.isArray(e))
    return "";
  const t = e;
  return ru(
    t.kind,
    t.media_kind,
    t.mediaKind,
    t.media_type,
    t.mediaType,
    t.type,
    t.name
  );
}
function ru(...e) {
  for (const t of e) {
    const n = bo(String(t || ""));
    if (n)
      return n;
  }
  return "";
}
function bo(e) {
  const t = e.trim().toLowerCase();
  return t === "image" || t === "images" || t === "picture" || t === "pictures" || t === "mediaimage" || t === "editor media image" || t === "editormediaimage" || t.includes("image") || t === "图片" || t === "图像" ? "image" : t === "video" || t === "videos" || t === "mediavideo" || t === "editor media video" || t === "editormediavideo" || t.includes("video") || t === "视频" ? "video" : t === "audio" || t === "audios" || t === "music" || t === "voice" || t === "mediaaudio" || t === "editor media audio" || t === "editormediaaudio" || t.includes("audio") || t === "音频" || t === "音乐" || t === "语音" ? "audio" : t === "file" || t === "files" || t === "attachment" || t === "attachments" || t === "mediafile" || t === "editorfile" || t === "editor media file" || t === "editormediafile" || t === "文件" || t === "附件" ? "file" : "";
}
function xy(e, t) {
  const n = [
    e.url,
    e.src,
    e.href,
    e.path,
    e.file_url,
    e.fileUrl,
    e.text,
    e.content,
    e.value,
    ze(e, ["attrs", "src"]),
    ze(e, ["attrs", "url"]),
    ze(e, ["attrs", "href"])
  ];
  return t === "image" ? [
    e.image,
    e.image_url,
    e.imageUrl,
    Xe(e.images),
    Xe(e.imageUrls),
    ...n
  ] : t === "video" ? [
    e.video,
    e.video_url,
    e.videoUrl,
    Xe(e.videos),
    Xe(e.videoUrls),
    ...n
  ] : t === "audio" ? [
    e.audio,
    e.audio_url,
    e.audioUrl,
    Xe(e.audios),
    Xe(e.audioUrls),
    ...n
  ] : [
    e.file,
    e.file_url,
    e.fileUrl,
    Xe(e.files),
    Xe(e.fileUrls),
    ...n
  ];
}
function Ji(e, t, n) {
  const r = t.trim();
  if (!r || Rt(r))
    return;
  if (Ws(r)) {
    const a = ke(r);
    if (a !== r && (yr(e, a, n), Kn(e)))
      return;
    const d = kt(a, "");
    d && !tn(d) && (e.text ||= d);
    return;
  }
  const o = dr(r);
  if (o) {
    e.text ||= o;
    return;
  }
  const s = vn(r);
  if (s && s !== r) {
    e.text ||= s;
    return;
  }
  const i = Ay(r, n);
  if (i) {
    My(e, i.kind, i.url), e.text ||= i.caption;
    return;
  }
  if (tn(r)) {
    const a = bo(n);
    if (a === "image" || /\.(png|jpe?g|gif|webp|avif|svg)(\?.*)?$/i.test(r)) {
      e.imageUrl ||= r;
      return;
    }
    if (a === "video" || /\.(mp4|webm|mov|m4v)(\?.*)?$/i.test(r)) {
      e.videoUrl ||= r;
      return;
    }
    if (a === "audio" || /\.(mp3|wav|ogg|m4a|aac)(\?.*)?$/i.test(r)) {
      e.audioUrl ||= r;
      return;
    }
    e.fileUrl ||= r;
    return;
  }
  e.text ||= r;
}
function Ay(e, t) {
  const n = ou(e, t), r = e.match(
    /!\[([^\]]*)\]\(\s*<?([^)\s>]+)>?(?:\s+["'][^"']*["'])?\s*\)/
  );
  if (r) {
    const u = jo(r[2]);
    if (u)
      return {
        kind: "image",
        url: u,
        caption: jr(e, r[0], r[1])
      };
  }
  const o = e.match(
    /!\[[^\]]*]\(\s*<?((?:https?:\/\/|data:|blob:)[^\s<>)]+)/i
  );
  if (o) {
    const u = jo(o[1]);
    if (u)
      return {
        kind: "image",
        url: u,
        caption: jr(e, o[0], "")
      };
  }
  const s = /\[([^\]]+)\]\(\s*<?([^)\s>]+)>?(?:\s+["'][^"']*["'])?\s*\)/g;
  let i;
  for (; i = s.exec(e); ) {
    const u = jo(i[2]), l = Zi(u, n);
    if (l)
      return {
        kind: l,
        url: u,
        caption: jr(e, i[0], i[1])
      };
  }
  const a = Py(e), d = Zi(a, n);
  return d ? {
    kind: d,
    url: a,
    caption: jr(e, a, "")
  } : null;
}
function ou(e, t) {
  return bo(t) ? t : Ty(e) ? "image" : t;
}
function Ty(e) {
  const t = /(?:图片|图像|image|photo|picture).{0,40}(?:https?:\/\/|data:|blob:)/i;
  return /!\[[^\]]*]\(/.test(e) || t.test(e);
}
function My(e, t, n) {
  t === "image" && (e.imageUrl ||= n), t === "video" && (e.videoUrl ||= n), t === "audio" && (e.audioUrl ||= n), t === "file" && (e.fileUrl ||= n);
}
function Zi(e, t) {
  if (!e || !tn(e))
    return "";
  const n = bo(t);
  return n === "image" || /\.(png|jpe?g|gif|webp|avif|svg)(\?.*)?$/i.test(e) ? "image" : n === "video" || /\.(mp4|webm|mov|m4v)(\?.*)?$/i.test(e) ? "video" : n === "audio" || /\.(mp3|wav|ogg|m4a|aac)(\?.*)?$/i.test(e) ? "audio" : n === "file" ? "file" : "";
}
function jr(e, t, n) {
  const r = e.replace(t, "").replace(/\s+/g, " ").trim();
  return r && r !== e.trim() && !tn(r) ? r : String(n || "").trim();
}
function jo(e) {
  const t = su(e);
  return tn(t) ? t : "";
}
function Py(e) {
  const t = e.match(/(?:https?:\/\/|data:|blob:)[^\s<>)]+/i);
  return t ? su(t[0]) : "";
}
function su(e) {
  return String(e || "").trim().replace(/^<|>$/g, "").replace(/[.,，。；;]+$/g, "");
}
function Dy(e) {
  return !!(e.output || e.result || e.content || e.rich || e.agent_run_id || e.approval_id);
}
function pn(e) {
  const t = String(e.text || "").trim();
  return !!(t && !Gn(t) || e.imageUrl || e.videoUrl || e.audioUrl || e.fileUrl);
}
function Kn(e) {
  return !!(e.imageUrl || e.videoUrl || e.audioUrl || e.fileUrl);
}
function Zt(...e) {
  return e.find((t) => t != null);
}
function Dt(...e) {
  for (const t of e)
    if (typeof t == "string" && t.trim())
      return t.trim();
  return "";
}
function Ey(...e) {
  for (const t of e) {
    if (typeof t == "string" && t.trim())
      return t.trim();
    if (t && typeof t == "object") {
      const n = Dt(
        t.url,
        t.src,
        t.href,
        t.path,
        t.file,
        t.file_url,
        t.fileUrl,
        t.image,
        t.image_url,
        t.imageUrl,
        t.video,
        t.video_url,
        t.videoUrl,
        t.audio,
        t.audio_url,
        t.audioUrl,
        ze(t, ["attrs", "src"]),
        ze(t, ["attrs", "url"]),
        ze(t, ["attrs", "href"])
      );
      if (n)
        return n;
    }
  }
  return "";
}
function jn(...e) {
  const t = Ey(...e);
  return tn(t) ? t : "";
}
function Xe(e) {
  return Array.isArray(e) ? e[0] : void 0;
}
function tn(e) {
  return /^(https?:\/\/|\/|data:|blob:)/i.test(e);
}
function $n(e, t) {
  return e?.screenToFlowPosition ? e.screenToFlowPosition(t) : e?.project ? e.project(t) : t;
}
function Fy(e, t, n, r) {
  const o = r?.x ?? e.x + 34, s = r?.y ?? e.y + 34;
  return {
    ...e,
    id: `local-${e.type}-${Date.now()}-${n}`,
    nodeNo: void 0,
    title: `${e.title} 副本`,
    titleMode: "manual",
    x: o,
    y: s,
    assetCateId: e.assetCateId || t,
    group: e.type === "group" && e.group?.origin === "script" ? { origin: "manual" } : e.group,
    storyboardItem: void 0,
    storyboardMaterializedSignature: void 0,
    local: !0
  };
}
function iu(e, t) {
  const n = new Map(e.map((l) => [l.id, l])), r = new Set(
    t.flatMap(
      (l) => Gr(
        e,
        qn(l).sourceNodeId
      ).map((f) => f.id)
    )
  ), o = /* @__PURE__ */ new Map(), s = /* @__PURE__ */ new Map();
  for (const l of e) {
    if (l.groupId) {
      const f = o.get(l.groupId) || [];
      f.push(l), o.set(l.groupId, f);
    }
    if (r.has(l.id)) {
      const f = Oy(l);
      f && Qi(f).trim() !== "" && s.set(l.id, f);
    }
  }
  const i = /* @__PURE__ */ new Map(), a = /* @__PURE__ */ new Map(), d = /* @__PURE__ */ new Map();
  for (const l of t) {
    const { sourceNodeId: f, targetNodeId: b } = qn(l);
    if (n.has(b))
      for (const _ of Gr(
        e,
        f
      )) {
        if (mo(_)) {
          const z = a.get(b) || [];
          z.push({ edge: l, source: _ }), a.set(b, z);
        }
        const w = s.get(_.id), D = d.get(b) || /* @__PURE__ */ new Set();
        if (!w || D.has(_.id))
          continue;
        D.add(_.id), d.set(b, D);
        const A = i.get(b) || [];
        A.push(w), i.set(b, A);
      }
  }
  const u = /* @__PURE__ */ new Map();
  for (const [l, f] of i)
    u.set(l, {
      sources: f,
      text: f.map(Qi).join(`

`)
    });
  return {
    nodeById: n,
    groupMembersById: o,
    inputContextByNodeId: u,
    incomingMediaReferencesByNodeId: a
  };
}
function qn(e) {
  return {
    sourceNodeId: e.logicalFrom || e.from,
    targetNodeId: e.logicalTo || e.to
  };
}
function zy(e, t, n) {
  const r = [];
  for (const o of t) {
    const s = qn(o);
    if (s.targetNodeId === n)
      for (const i of Gr(
        e,
        s.sourceNodeId
      ))
        mo(i) && r.push({ edge: o, source: i });
  }
  return r;
}
function au(e, t, n) {
  return iu(t, n).inputContextByNodeId.get(e) || null;
}
function Oy(e) {
  if (!ft(e))
    return null;
  const t = nn(e), n = gn(
    t,
    yo(e, t)
  );
  return pn(n) || (n.text = kt(
    t,
    e.description || e.title
  )), {
    nodeId: e.id,
    title: e.title,
    type: e.type,
    output: t,
    preview: n,
    resultRef: e.resultRef
  };
}
function $y(e, t) {
  return e === t ? !0 : !e || !t || e.text !== t.text ? !1 : e.sources.length === t.sources.length && e.sources.every((n, r) => {
    const o = t.sources[r];
    return n.nodeId === o.nodeId && n.title === o.title && n.type === o.type && n.output === o.output && n.resultRef === o.resultRef && n.preview.text === o.preview.text && n.preview.imageUrl === o.preview.imageUrl && n.preview.videoUrl === o.preview.videoUrl && n.preview.audioUrl === o.preview.audioUrl && n.preview.fileUrl === o.preview.fileUrl;
  });
}
function Qi(e) {
  const t = e.preview, n = t.text || t.imageUrl || t.videoUrl || t.audioUrl || t.fileUrl || pu(e.output);
  return String(n || "").trim() ? `[${e.title}]
${n}` : "";
}
function nn(e) {
  return jy(
    Gd(
      e.asset?.version?.content,
      e.resultOutput
    )
  );
}
function jy(...e) {
  let t;
  for (const n of e) {
    if (n == null)
      continue;
    t === void 0 && (t = n);
    const r = cu(n);
    if (r !== void 0)
      return r;
    const o = uu(n);
    if (o)
      return { text: o };
    const s = io(n) || bt(n);
    if (Te(s) || Xs(s))
      return s;
  }
  if (t !== void 0)
    return io(t) || bt(t);
}
function cu(e) {
  const t = ke(e), n = Tn(t) ? t : xt(t);
  if (!n)
    return;
  const r = by(n).trim();
  if (!mn(r))
    return;
  const o = Aa(r);
  if (o === r)
    return;
  const s = bt(o);
  if (Te(s) || Xs(s))
    return s;
}
function uu(e) {
  const t = ke(e), n = Tn(t) ? t : xt(t);
  return _a(n);
}
function bt(e) {
  const t = ke(e), n = Sr(t);
  if (n !== t)
    return bt(n);
  if (nu(t))
    return t;
  if (Ys(t)) {
    const s = ds(t);
    if (Te(s))
      return s;
  }
  const r = ho(t);
  if (r)
    return r.rich;
  if (Tn(t))
    return t;
  const o = xt(t);
  return o || Hs(co(t, /* @__PURE__ */ new Set()));
}
function co(e, t) {
  if (!e || typeof e != "object" || t.has(e))
    return e;
  t.add(e);
  const n = e, r = lu(n);
  if (r !== void 0)
    return r;
  const o = By(n, t);
  if (o !== void 0)
    return o;
  for (const s of Uy) {
    const i = ze(n, s);
    if (i === void 0 || i === e)
      continue;
    const a = co(
      ke(i),
      t
    );
    if (ls(a))
      return a;
  }
  if (No(n))
    for (const s of ["output", "result", "data", "body"]) {
      if (n[s] === void 0 || n[s] === e)
        continue;
      const i = co(
        ke(n[s]),
        t
      );
      if (ls(i))
        return i;
    }
  return e;
}
function By(e, t) {
  for (const [n, r] of Object.entries(e)) {
    if (!du(n) || !r || typeof r != "object")
      continue;
    const o = co(ke(r), t);
    if (ls(o))
      return o;
  }
}
function du(e) {
  return /^(node|step|task|power|agent)[_-]?\d+$/i.test(e);
}
const Uy = [
  ["output", "content", "rich"],
  ["output", "content"],
  ["output", "rich"],
  ["content", "output", "content", "rich"],
  ["content", "output", "content"],
  ["content", "output", "rich"],
  ["content", "data", "text"],
  ["content", "data", "content"],
  ["content", "rich"],
  ["content", "text"],
  ["data", "output", "content", "rich"],
  ["data", "output", "content"],
  ["data", "content", "rich"],
  ["data", "content"],
  ["rich"]
];
function lu(e) {
  const t = qs(e);
  if (t)
    return t;
  if (String(e.result_mode || "").toLowerCase() === "inline" && e.content != null) {
    const n = ke(e.content);
    if (n && typeof n == "object") {
      const r = lu(n);
      if (r !== void 0)
        return r;
    }
  }
}
function Hs(e) {
  const t = ke(e);
  return t && typeof t == "object" && !Array.isArray(t) && !t.type && Array.isArray(t.content) ? {
    type: "doc",
    content: t.content
  } : Array.isArray(t) ? {
    type: "doc",
    content: t
  } : t;
}
function No(e) {
  return !!(e.agent_run_id || e.approval_id || e.node_run_id || e.request_id || e.approved !== void 0 || e.message !== void 0);
}
function ls(e) {
  if (e == null)
    return !1;
  if (typeof e == "string") {
    const n = e.trim();
    return n.length > 0 && !mn(n) && !Rt(n);
  }
  if (Array.isArray(e))
    return e.length > 0;
  if (typeof e != "object" || xt(e) || wt(e))
    return !0;
  if (No(e))
    return !1;
  const t = vn(e).trim();
  return !!(t && !Gn(t));
}
function ze(e, t) {
  let n = e;
  for (const r of t) {
    if (!n || typeof n != "object" || !(r in n))
      return;
    n = n[r];
  }
  return n;
}
function Sr(e) {
  if (typeof e != "string")
    return e;
  const t = e.trim();
  for (const n of ["agent-result", "agent-output", "json"]) {
    const r = Vy(t, n);
    if (r !== void 0)
      return r;
  }
  return e;
}
function Vy(e, t) {
  const n = `\`\`\`${t}`, r = e.toLowerCase().indexOf(n);
  if (r < 0)
    return;
  let o = r + n.length;
  for (; o < e.length && /\s/.test(e[o] || ""); )
    o += 1;
  let s = o;
  for (; s < e.length; ) {
    const i = e.indexOf("```", s), a = i >= 0 ? e.slice(o, i) : e.slice(o), d = Ky(a, t === "json");
    if (d)
      return d;
    if (i < 0)
      return;
    s = i + 3;
  }
}
function Ky(e, t = !1) {
  const n = e.trim(), r = Zd(n);
  for (const o of Ny([n, r])) {
    const s = ke(o);
    if (s !== o && (t ? Ly(s) : Ys(s)))
      return s;
  }
  return null;
}
function Ly(e) {
  if (!e || typeof e != "object" || Array.isArray(e))
    return !1;
  const t = String(e.kind || e.type || e.event || "").toLowerCase().trim();
  return [
    "final",
    "result",
    "final_result",
    "answer",
    "tool",
    "tool_result",
    "power_result"
  ].includes(t) || "content" in e || "tasks" in e || "suggestions" in e || "rich" in e;
}
function Ys(e) {
  if (!e || typeof e != "object" || Array.isArray(e))
    return !1;
  const t = String(e.kind || e.type || e.event || "").toLowerCase().trim();
  return [
    "final",
    "result",
    "final_result",
    "answer",
    "tool",
    "tool_result",
    "power_result"
  ].includes(t) || "content" in e || "tasks" in e || "suggestions" in e || [
    "title",
    "text",
    "rich",
    "images",
    "videos",
    "audios",
    "files",
    "json"
  ].some((n) => Te(e[n]));
}
function Ws(e) {
  const t = String(e || "").trim();
  return t.startsWith("{") && t.endsWith("}") || t.startsWith("[") && t.endsWith("]");
}
function mn(e) {
  const t = String(e || "").trim();
  return !!(t && (Ws(t) || t.startsWith("{") || t.startsWith("[") || t.includes('"agent_run_id"') || t.includes('"node_run_id"') || t.includes('"approval_id"')));
}
function qy(e, t = []) {
  return { current: Hy([
    ...t,
    ...(e?.sources || []).map((r) => ({
      id: r.nodeId,
      title: r.title,
      kind: fu(r.preview, String(r.type || "")),
      source: "current",
      output: r.output,
      preview: r.preview
    }))
  ]) };
}
function Gy(e) {
  return e.map(
    (t) => ({
      id: t.role === "material" ? t.nodeId || t.key : String(t.assetId || t.key),
      title: t.title,
      kind: fu(t.preview, t.nodeType),
      role: t.role,
      source: t.role === "material" ? "current" : "asset",
      refType: "asset",
      refId: t.assetId,
      versionID: t.versionId,
      output: t.output,
      preview: t.preview,
      asset: t.asset
    })
  );
}
function Hy(e) {
  const t = [], n = /* @__PURE__ */ new Set();
  for (const r of e) {
    const o = `${r.source}:${r.id}`;
    n.has(o) || (n.add(o), t.push(r));
  }
  return t;
}
function fu(e, t) {
  return e.imageUrl ? "image" : e.videoUrl ? "video" : e.audioUrl ? "audio" : e.fileUrl ? "file" : e.text ? "text" : String(t || "file").toLowerCase();
}
function pu(e) {
  if (e == null)
    return "";
  if (typeof e == "string")
    return Rt(e) ? "" : e;
  const t = vn(e).trim();
  if (t && Rt(t))
    return "";
  try {
    const n = JSON.stringify(e);
    return Gn(n) ? "" : n;
  } catch {
    const n = String(e);
    return Rt(n) ? "" : n;
  }
}
function Xs(e) {
  const t = pu(e).trim();
  return !!(t && !Gn(t));
}
function Gn(e) {
  const t = e.trim();
  return !t || t === "{}" || t === "[]" || t === "null" || Rt(t);
}
function fs(e, t) {
  return Yd(e, t);
}
function ea(e, t) {
  return e.handleType === "target" ? {
    source: t,
    target: e.nodeId
  } : {
    source: e.nodeId,
    target: t
  };
}
function In(e, t, n, r) {
  return !t || !n || t === n || e.some(
    (s) => {
      const i = qn(s);
      return i.sourceNodeId === t && i.targetNodeId === n;
    }
  ) ? e : [
    ...e,
    {
      id: `edge-${t}-${n}-${Date.now()}`,
      from: t,
      to: n,
      ...r ? { mediaUsage: r } : {}
    }
  ];
}
function ps(e) {
  return Number(e.asset?.asset_cate_id || e.assetCateId || 0);
}
function ms(e, t) {
  return e.type === "asset" && ps(e) === t;
}
function ta(e, t, n, r) {
  const o = new Map(e.map((s) => [s.id, s]));
  if (r)
    for (const s of t) {
      if (s.from !== r)
        continue;
      const i = o.get(s.to);
      if (i && ms(i, n))
        return i;
    }
  return e.find((s) => ms(s, n)) || null;
}
function na(e, t, n, r, o) {
  const s = /* @__PURE__ */ new Set();
  if (!r || !n)
    return s;
  const i = new Map(e.map((a) => [a.id, a]));
  for (const a of t) {
    if (a.from !== r || a.to === o)
      continue;
    const d = i.get(a.to);
    d && ms(d, n) && s.add(d.id);
  }
  return s;
}
function Bo(e, t) {
  return {
    ...t,
    id: e.id,
    x: e.x,
    y: e.y,
    width: e.width || t.width,
    height: e.height || t.height,
    groupId: e.groupId,
    local: e.local !== !1
  };
}
function Yy(e) {
  return {
    title: e.title,
    subtitle: e.subtitle,
    description: e.description,
    assetCateId: e.assetCateId,
    kind: e.kind,
    cardinality: e.cardinality,
    asset: e.asset
  };
}
function Wy(e) {
  return e.map((t) => ({
    id: String(t.id || `edge-${t.source}-${t.target}`),
    from: String(t.data?.physicalFrom || t.source || ""),
    to: String(t.data?.physicalTo || t.target || ""),
    logicalFrom: String(t.data?.logicalFrom || "") || void 0,
    logicalTo: String(t.data?.logicalTo || "") || void 0,
    executionMode: String(t.data?.executionMode || "") === "manual" ? "manual" : void 0,
    mediaUsage: String(t.data?.mediaUsage || "") || void 0
  })).filter((t) => t.from && t.to && t.from !== t.to);
}
function ra(e, t) {
  const n = new Set(e.nodes.map((i) => i.id));
  let r = !1;
  const o = e.nodes.map((i) => {
    const a = Number(i.assetCateId ?? t), d = i.local !== !1;
    return i.assetCateId === a && i.local === d ? i : (r = !0, {
      ...i,
      assetCateId: a,
      local: d
    });
  }), s = e.edges.filter(
    (i) => n.has(i.from) && n.has(i.to)
  );
  return Bd({
    assetCateId: t,
    nextNodeNo: e.nextNodeNo,
    nodes: r ? o : e.nodes,
    edges: s.length === e.edges.length ? e.edges : s,
    viewport: e.viewport || {},
    updatedAt: e.updatedAt
  });
}
function Xy(e, t) {
  return Object.fromEntries(
    Object.entries(e).map(([n, r]) => [
      n,
      mu(r, t)
    ])
  );
}
function mu(e, t) {
  if (!t.length)
    return e;
  const n = new Map(t.map((o) => [o.id, o])), r = new Map(
    t.filter(
      (o) => String(o.role || "") === "material" && String(o.status || "") !== "archived" && String(o.node_key || o.version?.node_key || "").trim()
    ).map((o) => [
      String(o.node_key || o.version?.node_key || "").trim(),
      o
    ])
  );
  return {
    ...e,
    nodes: e.nodes.map(
      (o) => Jy(o, n, r)
    )
  };
}
function Jy(e, t, n) {
  const r = Zy(e), o = (r > 0 ? t.get(r) : void 0) || (e.type === "power" || e.type === "agent" || e.type === "flow" ? n.get(e.id) : void 0);
  if (!o)
    return e;
  const s = Ec(e, o), i = Number(s.resultRef?.run_id || 0), a = Number(e.resultRef?.run_id || 0);
  return {
    ...e,
    ...s,
    ...e.runError && i > a ? { runError: "" } : {},
    asset: o
  };
}
function Zy(e) {
  return Number(e.resultRef?.asset_id || e.asset?.id || 0);
}
function oa(e, t) {
  return e === t || e.assetCateId === t.assetCateId && gu(e.nodes, t.nodes) && yu(e.edges, t.edges) && e.viewport.x === t.viewport.x && e.viewport.y === t.viewport.y && e.viewport.zoom === t.viewport.zoom && e.updatedAt === t.updatedAt;
}
function gu(e, t) {
  return e === t || e.length === t.length && e.every((n, r) => n === t[r]);
}
function yu(e, t) {
  return e === t || e.length === t.length && e.every((n, r) => {
    const o = t[r];
    return n === o || n.id === o.id && n.from === o.from && n.to === o.to && (n.logicalFrom || "") === (o.logicalFrom || "") && (n.logicalTo || "") === (o.logicalTo || "") && (n.executionMode || "auto") === (o.executionMode || "auto") && (n.mediaUsage || "") === (o.mediaUsage || "");
  });
}
function Qy(e, t) {
  return fs(e, t) ? { source: e.id, target: t.id } : fs(t, e) ? { source: t.id, target: e.id } : null;
}
function eh(e) {
  return {
    id: "proximity-preview",
    source: e.source,
    sourceHandle: "output-0",
    target: e.target,
    targetHandle: "input-0",
    type: "animated",
    animated: !0,
    style: {
      stroke: "#0ea5e9",
      strokeWidth: 2,
      strokeDasharray: "5 5",
      opacity: 0.86,
      animation: "ws-dashdraw 0.5s linear infinite"
    },
    data: {
      isHighlighted: !0,
      highlightColor: "#0ea5e9"
    }
  };
}
function th(e, t) {
  return !e && !t ? !0 : !e || !t ? !1 : e.source === t.source && e.target === t.target;
}
function nh(e, t, n) {
  const o = new Map(n.map((i) => [i.id, i]));
  let s = null;
  for (const i of t) {
    if (i.id === e.id)
      continue;
    const a = o.get(i.id);
    if (!a)
      continue;
    const d = i.position.x - e.position.x, u = i.position.y - e.position.y, l = Math.sqrt(d * d + u * u);
    l < 150 && (!s || l < s.distance) && (s = { distance: l, domainNode: a });
  }
  return s;
}
function rh(e, t, n, r, o, s, i, a) {
  const d = e.id === o, u = s.has(e.id), l = d || u || e.source === n || e.target === n || e.source === r || e.target === r, f = e.source === n || e.target === n ? n : r;
  return {
    ...e,
    data: {
      ...e.data,
      isHighlighted: l,
      isSelected: d,
      onDelete: a,
      highlightColor: l ? oh(
        t.get(
          u ? i : f
        )
      ) : "var(--ws-edge)"
    }
  };
}
function sa(e, t, n) {
  const r = t.find((u) => u.id === e);
  if (!r || r.type !== "function" || r.functionOption?.key !== "start")
    return /* @__PURE__ */ new Set();
  const o = /* @__PURE__ */ new Map();
  for (const u of n)
    o.set(u.from, [...o.get(u.from) || [], u]);
  const s = new Map(t.map((u) => [u.id, u])), i = /* @__PURE__ */ new Set(), a = /* @__PURE__ */ new Set([e]), d = [...o.get(e) || []];
  for (; d.length > 0; ) {
    const u = d.shift();
    if (!u || i.has(u.id) || (i.add(u.id), a.has(u.to)))
      continue;
    a.add(u.to);
    const l = s.get(u.to);
    l && Fa(l) || d.push(...o.get(u.to) || []);
  }
  return i;
}
function oh(e) {
  return e?.type === "asset" ? "#10b981" : e?.type === "power" ? "#8b5cf6" : e?.type === "agent" ? "#f59e0b" : e?.type === "flow" ? "#3b82f6" : e?.type === "function" || e?.type === "group" ? "#f43f5e" : "#3b82f6";
}
function sh(e) {
  const t = e?.changedTouches?.[0] || e?.touches?.[0];
  return t ? { x: t.clientX, y: t.clientY } : typeof e?.clientX == "number" && typeof e?.clientY == "number" ? { x: e.clientX, y: e.clientY } : null;
}
function ih(e) {
  return e instanceof HTMLElement ? !!e.closest("input, textarea, select, [contenteditable='true']") : !1;
}
function ia(e) {
  return !e.repeat && (e.key === "Delete" || e.key === "Backspace") && !ih(e.target);
}
function hu(e) {
  return e === "start" ? lo : e === "import" ? ud : e === "display" ? ur : ya;
}
function ah(e) {
  return e.type !== "function" ? !1 : e.functionOption?.key === "start" || e.title === "开始";
}
function wu(e) {
  if (e.type !== "function")
    return !1;
  const t = e.functionOption?.key || "";
  return t === "import" || t === "save" || t === "display";
}
function Js(e) {
  return wu(e) && ft(e);
}
function bu(e) {
  return {
    description: e
  };
}
function ch(e, t) {
  return {
    ...bu(t),
    resultRef: Wr({
      ...e,
      asset: void 0,
      version: void 0,
      role: void 0
    })
  };
}
function Nu(e) {
  return e.type === "function" ? Js(e) ? Ns(e) ? uh(e) : { width: e.width, height: e.height } : { width: 128, height: 46 } : {
    width: e.width,
    height: e.height
  };
}
const _u = 44;
function uh(e) {
  const t = Xn(e), n = t.audioUrl ? "audio" : t.videoUrl ? "video" : t.imageUrl ? "image" : String(e.kind || ""), r = ws({
    kind: n,
    outputType: "",
    output: void 0
  });
  return {
    width: r.width,
    height: r.height + _u
  };
}
function Be({
  id: e,
  type: t,
  position: n,
  className: r,
  style: o
}) {
  return /* @__PURE__ */ c(
    Uu,
    {
      id: e,
      type: t,
      position: n,
      className: `ws-rf-handle ${r}`,
      style: o,
      children: /* @__PURE__ */ c("span", { "aria-hidden": "true", children: t === "target" ? /* @__PURE__ */ c(fa, { size: 12 }) : /* @__PURE__ */ c(hs, { size: 12 }) })
    }
  );
}
function Wt({
  node: e,
  selected: t
}) {
  const n = e.onNodeResizeStart, r = e.onNodeResizeEnd, o = e.type === "asset" || e.type === "power" || e.type === "group" || e.type === "function" && Js(e), s = !!e.showNodeSettings, i = /* @__PURE__ */ c(
    ef,
    {
      node: e,
      enabled: !!e.interactive && !e.structureLocked,
      resizable: o,
      onResizeStart: n,
      onResizeEnd: r
    }
  );
  if (e.type === "asset" || e.type === "group" || e.type === "function" || e.type === "power" && Wd(e.power, e.kind, e.outputType) || (!t || !s) && e.type !== "flow")
    return i;
  const { projectId: a, runningNode: d, setRunningNode: u } = e, l = e.onNodeResult, f = e.onNodeDraftChange, b = e.onAddConfiguredNode, _ = e.onAssetCreated, w = e.onRunStartNode, D = e.onOpenImportPicker, A = e.onClearFeedbackRecords, z = e.requestConfirm, V = e.onRunBackendNode;
  return /* @__PURE__ */ v(Qt, { children: [
    i,
    /* @__PURE__ */ c(
      Nh,
      {
        node: e,
        projectId: a,
        runningNode: d,
        setRunningNode: u,
        onNodeResult: l || (() => {
        }),
        onNodeDraftChange: f || (() => {
        }),
        onAddConfiguredNode: b,
        onAssetCreated: _,
        onRunStartNode: w,
        onOpenImportPicker: D,
        onClearFeedbackRecords: A,
        requestConfirm: z,
        onRunBackendNode: V
      },
      e.id
    )
  ] });
}
function rr({
  node: e,
  onShowNodeDetail: t
}) {
  return !t || !ft(e) ? null : /* @__PURE__ */ c(
    "button",
    {
      type: "button",
      className: "ws-node-quick-view nodrag nopan",
      "aria-label": "查看详情",
      onMouseDown: (n) => n.stopPropagation(),
      onClick: (n) => {
        n.preventDefault(), n.stopPropagation(), t(e);
      },
      children: /* @__PURE__ */ c(ur, { size: 14 })
    }
  );
}
const dh = {
  width: 270,
  height: 250,
  offsetX: 0,
  offsetY: 0
};
function Uo({
  node: e,
  runningNode: t,
  onShowNodeDetail: n
}) {
  const r = lr(
    e.resultView || dh
  ), {
    width: o,
    height: s,
    offsetX: i,
    offsetY: a
  } = r, [d, u] = $(r), [l, f] = $(!1);
  le(() => {
    u({
      width: o,
      height: s,
      offsetX: i,
      offsetY: a
    });
  }, [
    s,
    i,
    a,
    o
  ]);
  const b = e.type === "agent" ? t?.agent : void 0, _ = uf(b);
  if (!ft(e) && !_)
    return null;
  const w = Xn(e), D = Gs(e), A = Dt(
    kt(w.text, ""),
    kt(D, ""),
    kt(e.description, ""),
    e.title,
    "暂无结果"
  ), z = Ks(e), V = Cn(e), R = Te(V) ? V : z ? { rich: z } : A, G = Kn(w) ? w : Cy(
    w,
    gn(A, ou(A, ""))
  ), ee = e.onNodeResizeStart, C = e.onResultViewResizeEnd, T = !!(e.interactive && C), O = e.setRunningNode, H = e.onRunBackendNode, re = Zt(
    e.resultOutput,
    e.asset?.version?.content
  ), X = e.type === "agent" && O && H ? async (oe) => {
    if (!Pt(t)) {
      O((J) => ({
        ...J,
        [e.id]: {
          nodeId: e.id,
          title: e.title,
          startedAt: Date.now(),
          progress: 0,
          status: "running",
          agent: Ua()
        }
      }));
      try {
        await H(e, { agentInput: oe });
      } catch (J) {
        O((Z) => {
          const fe = Z[e.id];
          return fe ? {
            ...Z,
            [e.id]: {
              ...fe,
              status: "error",
              progress: Math.max(fe.progress, 92)
            }
          } : Z;
        }), ne.error(
          J instanceof Error ? J.message : "智能体继续运行失败"
        ), window.setTimeout(() => {
          O(
            (Z) => Z[e.id]?.status === "error" ? un(Z, e.id) : Z
          );
        }, 1200);
      }
    }
  } : void 0;
  return /* @__PURE__ */ c(st, { fallback: /* @__PURE__ */ c(it, { label: "正在加载节点结果" }), children: /* @__PURE__ */ c(
    Pc,
    {
      output: R,
      fallback: A,
      preview: G,
      mediaLabel: hr(G),
      className: `ws-agent-result-bubble ${l ? "is-resizing" : ""}`,
      followContent: !!(t && t.status !== "error"),
      followKey: b,
      style: {
        width: d.width,
        height: d.height,
        left: `calc(100% + 12px + ${Number(d.offsetX || 0)}px)`,
        top: `calc(50% + ${Number(d.offsetY || 0)}px)`
      },
      onOpen: n ? () => n(e) : void 0,
      resizeControls: /* @__PURE__ */ c(
        tf,
        {
          value: d,
          enabled: T,
          onResizeStart: () => {
            f(!0), ee?.(e.id);
          },
          onResize: u,
          onResizeEnd: (oe) => {
            u(oe), f(!1), C?.(e.id, oe);
          }
        }
      ),
      children: e.type === "agent" ? /* @__PURE__ */ c(
        st,
        {
          fallback: /* @__PURE__ */ c(it, { label: "正在加载智能体结果", compact: !0 }),
          children: /* @__PURE__ */ c(
            Xm,
            {
              output: re,
              runtime: b,
              fallback: A,
              running: Pt(t),
              onContinue: X
            }
          )
        }
      ) : void 0
    }
  ) });
}
function lh({
  node: e,
  running: t = !1,
  onShowNodeDetail: n
}) {
  const r = Xn(e), o = Ks(e), s = Cn(e), i = Dt(
    Gs(e),
    kt(r.text, ""),
    kt(e.description, ""),
    "暂无内容"
  ), a = Te(s) ? s : o ? { rich: o } : i, d = !sr(a, r) && !!(r.imageUrl || r.videoUrl || r.audioUrl), u = e.onNodeResult, l = d && !r.audioUrl && Ns(e) && !!u;
  return /* @__PURE__ */ c(st, { fallback: /* @__PURE__ */ c(it, { label: "正在加载节点结果" }), children: /* @__PURE__ */ c(
    Pc,
    {
      output: a,
      fallback: i,
      preview: r,
      mediaLabel: hr(r),
      className: `ws-node-function-result-card ${d ? "has-media" : ""}`,
      customContentIsPureMedia: d,
      onOpen: n ? () => n(e) : void 0,
      children: d ? /* @__PURE__ */ c(
        Cu,
        {
          preview: r,
          output: a,
          fallback: i,
          generating: t,
          showMediaCaption: !1,
          onMediaSize: l ? (f, b) => {
            const _ = Ru(f, b);
            !_ || !u || u(e.id, {
              width: _.width,
              height: _.height + _u
            });
          } : void 0
        }
      ) : void 0
    }
  ) });
}
function Vo({
  node: e,
  onOpenFeedbackRecord: t
}) {
  const n = kn(e);
  if (!t || n.length === 0)
    return null;
  const r = n.filter(
    (s) => s.status === "pending"
  ).length, o = [...n].reverse().find((s) => s.status === "pending") || n[n.length - 1];
  return /* @__PURE__ */ v(
    "button",
    {
      type: "button",
      className: `ws-node-feedback-beacon nodrag nopan ${r > 0 ? "is-pending" : "is-done"}`,
      "aria-label": r > 0 ? "继续填写反馈" : "查看反馈记录",
      onMouseDown: (s) => s.stopPropagation(),
      onClick: (s) => {
        s.preventDefault(), s.stopPropagation(), t(e, o);
      },
      children: [
        /* @__PURE__ */ c(ll, { size: 15, fill: "currentColor" }),
        n.length > 1 ? /* @__PURE__ */ c("span", { children: n.length }) : null
      ]
    }
  );
}
function fh(e) {
  const t = e.resultRef;
  return !!(t?.run_id || t?.node_run_id || t?.asset_id || t?.version_id || t?.request_id);
}
function ft(e) {
  if (!ph(e) || !fh(e) && e.asset?.version?.content == null && e.resultOutput == null)
    return !1;
  const t = nn(e);
  if (t == null)
    return !1;
  const n = gn(
    t,
    yo(e, t)
  );
  return pn(n) || Xs(t);
}
function ph(e) {
  return e.type !== "function" ? !0 : wu(e);
}
async function Iu(e) {
  const t = e.node.functionOption?.key || "", n = hh(e.inputContext);
  if (t === "display") {
    if (n == null)
      throw new Error("展示节点没有可展示的上游结果");
    return e.onNodeResult(
      e.node.id,
      Jt(
        e.node,
        { output: n },
        "展示上游结果"
      )
    ), ne.success("已展示上游结果"), !0;
  }
  if (t === "save") {
    if (n == null)
      throw new Error("保存节点没有可保存的上游结果");
    const r = await fy({
      projectId: e.projectId,
      assetCateId: Number(e.node.assetCateId || e.assetCate?.id || 0),
      name: bh(e.node, e.inputContext),
      kind: mf(e.node),
      content: n,
      nodeKey: e.node.id,
      requestId: Gc(
        "save",
        e.node.id,
        n
      ),
      source: wh(e.inputContext),
      previousAsset: e.node.asset
    });
    return e.onAssetCreated?.(r), e.onNodeResult(
      e.node.id,
      Jt(
        e.node,
        {
          output: r.version?.content || n,
          asset: r
        },
        "保存上游结果"
      )
    ), ne.success("资产已保存"), !0;
  }
  return t === "start" ? e.onRunStartNode ? (await e.onRunStartNode(e.node), ne.success("开始节点执行完成"), !0) : (e.onNodeResult(
    e.node.id,
    bu("开始节点已启动，请运行后续连接节点。")
  ), ne.success("开始节点已启动"), !0) : t === "import" ? (e.onOpenImportPicker?.(e.node.id), !0) : (e.onNodeResult(
    e.node.id,
    Jt(
      e.node,
      { output: "操作已应用" },
      "操作已应用"
    )
  ), ne.success("操作已应用"), !0);
}
function mh(e, t, n = []) {
  const r = vs(e), o = new Map(
    n.map((s) => [s.key, s])
  );
  for (const s of e) {
    const i = o.get(s.key);
    s.key && i && Object.prototype.hasOwnProperty.call(t, s.key) && gh(s, i, t[s.key]) && (r[s.key] = Ln(s, t[s.key]));
  }
  return r;
}
function gh(e, t, n) {
  if (e.type !== t.type || e.value_type !== t.value_type)
    return !1;
  if (e.type === "option" || e.type === "select") {
    const r = e.options || [];
    return r.length === 0 || r.some(
      (o) => Bn(o, [String(n ?? "")])
    );
  }
  if (e.type === "multi_option") {
    const r = e.options || [];
    return r.length === 0 || yh(n).every(
      (o) => r.some((s) => Bn(s, [o]))
    );
  }
  return e.value_type === "number" ? n === "" || Number.isFinite(Number(n)) : !0;
}
function yh(e) {
  return Array.isArray(e) ? e.map((t) => String(t)).filter(Boolean) : typeof e == "string" ? e ? [e] : [] : e == null ? [] : [String(e)];
}
function Zs(e) {
  const t = e?.sources || [];
  return t.length > 0 ? t[t.length - 1] : null;
}
function hh(e) {
  const t = Zs(e);
  return t?.output != null ? t.output : e?.text ? { text: e.text } : null;
}
function wh(e) {
  const t = Zs(e);
  return _f(t);
}
function bh(e, t) {
  const n = Zs(t);
  return Dt(n?.title, e.title, "画布资产");
}
function Br(e) {
  return Number(e?.source_rule || 0) === 2;
}
function Nh({
  node: e,
  projectId: t,
  runningNode: n,
  setRunningNode: r,
  onNodeResult: o,
  onNodeDraftChange: s,
  onAddConfiguredNode: i,
  onAssetCreated: a,
  onRunStartNode: d,
  onOpenImportPicker: u,
  onClearFeedbackRecords: l,
  requestConfirm: f,
  onRunBackendNode: b
}) {
  const _ = e.composerDraft, w = ue(
    () => Fc(_),
    [_]
  ), D = ue(
    () => Cg(w),
    [w]
  ), A = te(w), [z, V] = $(w.prompt || ""), [R, G] = $(w.promptContent), [ee, C] = $(w.storyboardReferences || []), [T, O] = $(!1), [H, re] = $(null), [X, oe] = $(!1), [J, Z] = $(
    w.selectedTargetId || 0
  ), [fe, ge] = $(
    w.paramValues || {}
  ), _e = te(H), ve = e.inputContext || null, at = String(e.runBlockedReason || "");
  le(() => {
    A.current = w;
  }, [w]), le(() => {
    _e.current = H;
  }, [H]);
  const M = T || Pt(n), we = e.type, Ue = e.id, Pe = e.flow?.id || 0, ct = e.type === "power" && e.power?.id || 0, Ze = e.type === "power" && e.power?.key || "", Et = e.type === "agent" ? Number(e.role?.agent_id || 0) : 0, W = vh(), de = e.space || null, Qe = e.canvasReferenceItems || [], De = e.connectedMediaReferences || os, Ce = e.onConnectedMediaUsagesChange, Ve = e.onConnectedMediaEdgeRemove, Ee = e.catalogCache, Vt = Number(
    de?.release?.id || de?.project.release_id || 0
  ), yn = Number(e.assetCateId || 0), hn = de ? Ur(de, yn) : null, _t = ue(
    () => qy(
      ve,
      Qe.filter((k) => k.id !== e.id)
    ),
    [Qe, ve, e.id]
  ), qe = e.type === "power" && Nr(e.power, e.kind, e.outputType)?.viewMode === "storyboard";
  le(() => {
    if (we === "power" && (ct || Ze)) {
      const k = A.current.selectedTargetId || 0;
      let j = !1;
      return oe(!0), Ee.loadPowerForm(
        {
          projectId: t,
          releaseId: Vt,
          flowId: Pe,
          powerId: ct,
          powerKey: Ze,
          targetId: k
        },
        () => Ho({
          projectId: t,
          flowId: Pe,
          powerId: ct,
          powerKey: Ze,
          targetId: k
        })
      ).then((U) => {
        if (j)
          return;
        const me = A.current;
        re(U), Z(
          Br(U) && (U.selected_target_id || k) || 0
        ), ge(
          Vi(U.params || [], me)
        ), V(me.prompt || ""), G(me.promptContent), C(me.storyboardReferences || []);
      }).catch((U) => {
        j || ne.error(
          U instanceof Error ? U.message : "加载能力参数失败"
        );
      }).finally(() => {
        j || oe(!1);
      }), () => {
        j = !0;
      };
    }
    if (re(null), we === "agent") {
      const k = A.current;
      ge(k.paramValues || {}), V(k.prompt || ""), G(k.promptContent), C(k.storyboardReferences || []), Z(0);
      return;
    }
    ge({}), Z(0), V(""), G(void 0), C([]);
  }, [
    Ee,
    t,
    Vt,
    Pe,
    Et,
    Ue,
    we,
    ct,
    Ze
  ]);
  const Ft = H?.params || [], et = ue(
    () => we === "power" ? Wo(H?.params || []) : [],
    [H, we]
  ), ut = ue(
    () => we === "power" && !X ? bi(
      De,
      R,
      _t.current,
      et
    ) : "",
    [
      _t,
      De,
      et,
      X,
      R,
      we
    ]
  ), At = at || ut, mt = ue(
    () => Ft.find(Ms) || null,
    [Ft]
  ), gt = ue(
    () => Ft.filter(
      (k) => k.key !== mt?.key && (ks(k) || sc(k))
    ),
    [Ft, mt?.key]
  ), Re = mt ? String(fe[mt.key] ?? "") : z, zt = String(
    H?.power?.description || e.power?.description || ""
  ).trim() || (mt ? "在此处为该能力输入生成提示词..." : "当前能力无需填写提示词"), nt = Br(H), Ie = nt ? J : 0;
  le(() => {
    if (we !== "power" && we !== "agent")
      return;
    const k = A.current, j = _e.current;
    V(k.prompt || ""), G(k.promptContent), C(k.storyboardReferences || []), Z(
      we === "power" && Br(j) && (k.selectedTargetId || j?.selected_target_id) || 0
    ), ge(
      we === "power" && j ? Vi(
        j.params || [],
        k
      ) : k.paramValues || {}
    );
  }, [D, we]);
  function rt(k) {
    const j = Object.prototype.hasOwnProperty.call(
      k,
      "promptContent"
    ) ? k.promptContent : R, U = js({
      ...A.current,
      ...k,
      promptContent: j
    });
    A.current = U, s(e.id, U);
  }
  function yt(k, j) {
    ge(k), rt({ ...j, paramValues: k });
  }
  function It(k, j) {
    const U = Ni(
      R,
      j,
      _t.current,
      et,
      De
    ), me = U.content;
    Object.keys(U.assignments).length > 0 && Ce?.(U.assignments), V(k), G(me);
    const Ae = qe ? Jd(
      me,
      ee,
      _t.current,
      k
    ) : ee;
    C(Ae);
    const Le = A.current.paramValues || fe, qt = mt ? { ...Le, [mt.key]: k } : Le;
    yt(qt, {
      prompt: k,
      promptContent: me,
      selectedTargetId: Ie,
      storyboardReferences: Ae
    });
  }
  function Ke(k) {
    C(k), rt({
      prompt: Re,
      promptContent: R,
      paramValues: fe,
      selectedTargetId: Ie,
      storyboardReferences: k
    });
  }
  function dt(k, j) {
    const U = {
      ...A.current.paramValues || fe,
      [k]: j
    };
    yt(U, {
      prompt: Re,
      promptContent: R,
      selectedTargetId: Ie
    });
  }
  function rn(k, j) {
    V(k), G(j), rt({
      prompt: k,
      promptContent: j,
      paramValues: fe,
      selectedTargetId: 0
    });
  }
  function Kt(k, j) {
    const U = {
      ...A.current.paramValues || fe,
      [k]: j
    };
    yt(U, {
      prompt: z,
      selectedTargetId: 0
    });
  }
  async function Lt(k, j) {
    const U = await Xa({
      projectID: t,
      teamID: Number(de?.project.team_id || 0),
      files: k,
      ruleID: j.upload_rule_id
    });
    for (const me of U) {
      const Ae = Ut(me.asset);
      Ae.id && a?.(Ae);
    }
    return U;
  }
  async function Ge(k) {
    if (!(M || !nt) && !(e.type !== "power" || !e.power))
      try {
        const j = await Ee.loadPowerForm(
          {
            projectId: t,
            releaseId: Vt,
            flowId: e.flow?.id || 0,
            powerId: e.power.id,
            powerKey: e.power.key,
            targetId: k
          },
          () => Ho({
            projectId: t,
            flowId: e.flow?.id || 0,
            powerId: e.power?.id || 0,
            powerKey: e.power?.key || "",
            targetId: k
          })
        ), U = Wo(j.params || []), me = Ni(
          R,
          R,
          _t.current,
          U,
          De
        ), Ae = bi(
          De,
          me.content,
          _t.current,
          U,
          me.assignments
        );
        if (Ae) {
          ne.error(`无法切换能力来源：${Ae}`);
          return;
        }
        Object.keys(me.assignments).length > 0 && Ce?.(me.assignments), re(j);
        const Le = Br(j) ? j.selected_target_id || k : 0;
        Z(Le), G(me.content);
        const qt = mh(
          j.params || [],
          A.current.paramValues || fe,
          H?.params || []
        );
        yt(qt, {
          prompt: Re,
          promptContent: me.content,
          selectedTargetId: Le
        });
      } catch (j) {
        ne.error(j instanceof Error ? j.message : "加载能力参数失败");
      }
  }
  const Se = async () => {
    l?.([e.id]), e.runError && o(e.id, { runError: "" }), O(!0), r((U) => ({
      ...U,
      [e.id]: {
        nodeId: e.id,
        title: e.title,
        startedAt: Date.now(),
        progress: 0,
        status: "running"
      }
    }));
    let k = !1, j = "error";
    try {
      if (e.type === "asset") {
        const U = e.asset?.version?.content ?? e.resultOutput ?? e.asset;
        if (U == null)
          throw new Error("资产节点没有可载入的内容");
        o(
          e.id,
          Jt(
            e,
            {
              output: U,
              asset: e.asset,
              version: e.asset?.version,
              request_id: Gc("asset", e.id, U)
            },
            "载入资产结果"
          )
        ), ne.success("资产结果已载入"), k = !0;
      } else if (e.type === "power" && e.power) {
        if (!b)
          throw new Error("当前节点缺少后端运行入口");
        rt({
          prompt: Re,
          promptContent: R,
          paramValues: fe,
          selectedTargetId: Ie,
          storyboardReferences: ee
        }), await b({
          ...e,
          composerDraft: {
            prompt: Re,
            promptContent: R,
            paramValues: fe,
            selectedTargetId: Ie,
            storyboardReferences: ee
          }
        }), ne.success("能力节点执行成功"), k = !0;
      } else if (e.type === "agent" && e.role) {
        if (!b)
          throw new Error("当前节点缺少后端运行入口");
        rt({
          prompt: z,
          promptContent: R,
          paramValues: fe,
          selectedTargetId: 0
        }), await b({
          ...e,
          composerDraft: {
            prompt: z,
            promptContent: R,
            paramValues: fe,
            selectedTargetId: 0
          }
        }), k = !0, j = "success";
      } else if (e.type === "flow" && e.flow) {
        if (!b)
          throw new Error("当前节点缺少后端运行入口");
        await b(e), k = !0, j = "success";
      } else e.type === "function" && (k = await Iu({
        node: e,
        projectId: t,
        assetCate: hn,
        inputContext: ve,
        onNodeResult: o,
        onAssetCreated: a,
        onRunStartNode: d,
        onOpenImportPicker: u
      }));
      k && (j = "success");
    } catch (U) {
      j = "error";
      const me = U instanceof Error ? U.message : "执行出错";
      o(e.id, { runError: me }), r((Ae) => {
        const Le = Ae[e.id];
        return Le ? {
          ...Ae,
          [e.id]: {
            ...Le,
            status: "error",
            progress: Math.max(Le.progress, 92)
          }
        } : Ae;
      }), ne.error(me);
    } finally {
      O(!1), r((U) => {
        const me = U[e.id];
        return me ? {
          ...U,
          [e.id]: {
            ...me,
            status: j,
            progress: j === "success" ? 100 : Math.max(me.progress, 92)
          }
        } : U;
      }), window.setTimeout(
        () => {
          r((U) => {
            const me = U[e.id];
            return me?.status === "error" && me.streamStarted ? U : un(U, e.id);
          });
        },
        j === "success" ? 650 : 1200
      );
    }
  }, Fe = async () => {
    if (!(M || T)) {
      if (At) {
        ne.error(At);
        return;
      }
      if (f && Su(e)) {
        f({
          title: e.type === "function" ? `执行「${e.title}」` : `运行「${e.title}」`,
          description: vu(e),
          confirmText: e.type === "flow" || e.type === "function" ? "执行" : "运行",
          onConfirm: Se
        });
        return;
      }
      await Se();
    }
  };
  return le(() => {
    if (!T)
      return;
    const k = window.setInterval(() => {
      r((j) => {
        const U = j[e.id];
        return !U || U.status !== "running" ? j : {
          ...j,
          [e.id]: {
            ...U,
            progress: Math.min(94, U.progress + 7)
          }
        };
      });
    }, 1e3);
    return () => window.clearInterval(k);
  }, [e.id, T, r]), e.type === "power" ? /* @__PURE__ */ c(
    "div",
    {
      className: "ws-node-bottom-settings is-composer nodrag nowheel",
      onClick: (k) => k.stopPropagation(),
      style: Ko,
      children: X && !M && !H ? /* @__PURE__ */ v("div", { className: "ws-prompt-loading", children: [
        /* @__PURE__ */ c(Bt, { size: 16, className: "ws-spin" }),
        /* @__PURE__ */ c("span", { children: "正在加载能力参数..." })
      ] }) : /* @__PURE__ */ v(Qt, { children: [
        /* @__PURE__ */ c(
          vi,
          {
            value: Re,
            referenceContent: R,
            placeholder: zt,
            running: M,
            textInputEnabled: !!mt,
            showMediaParamButtons: !0,
            mediaParamPower: H?.power || e.power,
            sourceOptions: nt ? H?.sources || [] : [],
            selectedSourceId: Ie,
            params: gt,
            paramValues: fe,
            assetLibrary: _t,
            assetReference: {
              teamID: Number(de?.project.team_id || 0),
              projectID: t,
              assetCateID: yn
            },
            connectedMediaReferences: De,
            mediaUsageOptions: et,
            onConnectedMediaEdgeRemove: Ve,
            disabled: X,
            submitDisabled: !!At,
            submitDisabledReason: At,
            onChange: It,
            onParamChange: dt,
            onSourceChange: nt ? (k) => {
              Ge(k);
            } : void 0,
            onLocalUpload: Lt,
            onSubmit: Fe
          }
        ),
        qe ? /* @__PURE__ */ c(
          st,
          {
            fallback: /* @__PURE__ */ c(
              it,
              {
                label: "正在加载分镜参考",
                compact: !0
              }
            ),
            children: /* @__PURE__ */ c(
              eg,
              {
                references: ee,
                disabled: X || M,
                onChange: Ke
              }
            )
          }
        ) : null
      ] })
    }
  ) : e.type === "agent" ? /* @__PURE__ */ c(
    "div",
    {
      className: "ws-node-bottom-settings is-composer nodrag nowheel",
      onClick: (k) => k.stopPropagation(),
      style: Ko,
      children: /* @__PURE__ */ c(
        vi,
        {
          value: z,
          referenceContent: R,
          placeholder: "向智能体发送任务指令...",
          running: M,
          params: pg,
          paramValues: fe,
          assetLibrary: _t,
          assetReference: {
            teamID: Number(de?.project.team_id || 0),
            projectID: t,
            assetCateID: yn
          },
          connectedMediaReferences: De,
          onConnectedMediaEdgeRemove: Ve,
          onChange: rn,
          onParamChange: Kt,
          onLocalUpload: Lt,
          onSubmit: Fe
        }
      )
    }
  ) : e.type === "flow" && e.flow ? /* @__PURE__ */ c(
    "div",
    {
      className: "ws-node-bottom-settings is-flow-run-only nodrag nowheel",
      onClick: (k) => k.stopPropagation(),
      style: W,
      children: /* @__PURE__ */ v(
        "button",
        {
          type: "button",
          className: "ws-node-flow-run",
          disabled: M,
          onClick: Fe,
          children: [
            M ? /* @__PURE__ */ c(Bt, { size: 15, className: "ws-spin" }) : /* @__PURE__ */ c(lo, { size: 15, fill: "currentColor" }),
            /* @__PURE__ */ c("span", { children: M ? "运行中" : "执行" })
          ]
        }
      )
    }
  ) : /* @__PURE__ */ v(
    "div",
    {
      className: "ws-node-bottom-settings nodrag nowheel",
      onClick: (k) => k.stopPropagation(),
      style: Ko,
      children: [
        /* @__PURE__ */ v("div", { className: "ws-node-settings-head", children: [
          /* @__PURE__ */ v("div", { className: "ws-node-settings-icon", children: [
            /* @__PURE__ */ c(hs, { size: 16 }),
            /* @__PURE__ */ c("span", { children: "配置" })
          ] }),
          /* @__PURE__ */ v("div", { className: "ws-node-settings-copy", children: [
            /* @__PURE__ */ v("div", { children: [
              /* @__PURE__ */ c("strong", { children: e.title }),
              /* @__PURE__ */ v("span", { children: [
                e.type,
                " /",
                " ",
                String(
                  e.kind || e.functionOption?.key || e.subtitle || "default"
                )
              ] })
            ] }),
            /* @__PURE__ */ c("p", { children: e.description || "配置该节点的专属参数和功能动作。" })
          ] })
        ] }),
        /* @__PURE__ */ c("div", { className: "ws-node-settings-line" }),
        /* @__PURE__ */ v("div", { className: "ws-node-settings-stack", style: { gap: "12px" }, children: [
          e.type === "asset" && /* @__PURE__ */ v("div", { className: "ws-node-settings-row", children: [
            /* @__PURE__ */ v("div", { className: "ws-node-settings-actions", children: [
              /* @__PURE__ */ c(or, { icon: ur, label: "放大预览", accent: "green" }),
              /* @__PURE__ */ c(or, { icon: il, label: "查看来源" }),
              /* @__PURE__ */ c(or, { icon: la, label: "查看版本" }),
              /* @__PURE__ */ c(or, { icon: Xu, label: "查看引用关系" }),
              /* @__PURE__ */ c(or, { icon: Ta, label: "查看生成记录" })
            ] }),
            /* @__PURE__ */ v(
              "button",
              {
                type: "button",
                className: "ws-node-save-run",
                disabled: M,
                onClick: Fe,
                style: { cursor: "pointer", marginLeft: "12px" },
                children: [
                  M ? /* @__PURE__ */ c(Bt, { size: 12, className: "ws-spin" }) : /* @__PURE__ */ c(ur, { size: 12 }),
                  /* @__PURE__ */ c("span", { children: M ? "载入中" : "载入结果" })
                ]
              }
            )
          ] }),
          e.type === "function" && /* @__PURE__ */ v("div", { className: "ws-node-settings-row", children: [
            /* @__PURE__ */ c("span", { className: "ws-node-settings-state is-function-desc", children: e.functionOption?.description || "执行该功能节点。" }),
            /* @__PURE__ */ v(
              "button",
              {
                type: "button",
                className: "ws-node-save-run",
                disabled: M,
                onClick: Fe,
                style: { cursor: "pointer", marginLeft: "12px" },
                children: [
                  M ? /* @__PURE__ */ c(Bt, { size: 12, className: "ws-spin" }) : (() => {
                    const k = hu(e.functionOption?.key || "");
                    return /* @__PURE__ */ c(k, { size: 12 });
                  })(),
                  /* @__PURE__ */ c("span", { children: e.functionOption?.key === "start" ? "开始" : e.functionOption?.key === "display" ? "展示" : e.functionOption?.key === "save" ? "保存" : "执行" })
                ]
              }
            )
          ] })
        ] })
      ]
    }
  );
}
function _h({
  prompt: e,
  running: t,
  readonly: n,
  history: r,
  activeRecordId: o,
  onSelectRecord: s,
  onClose: i,
  onSubmit: a
}) {
  const d = ue(
    () => Ih(e),
    [e]
  );
  if (typeof document > "u")
    return null;
  const u = document.querySelector(".ws-page") || document.body;
  return Tu(
    /* @__PURE__ */ c("div", { className: "ws-flow-feedback-backdrop", onMouseDown: i, children: /* @__PURE__ */ v(
      "div",
      {
        className: "ws-flow-feedback-modal",
        onMouseDown: (l) => l.stopPropagation(),
        children: [
          /* @__PURE__ */ v("header", { className: "ws-flow-feedback-head", children: [
            /* @__PURE__ */ v("div", { children: [
              /* @__PURE__ */ c("strong", { children: e.title || "补充信息" }),
              n ? /* @__PURE__ */ c("span", { children: "已提交的反馈记录，可查看之前填写的内容。" }) : e.description ? /* @__PURE__ */ c("span", { children: e.description }) : null
            ] }),
            /* @__PURE__ */ c(
              "button",
              {
                type: "button",
                className: "ws-flow-feedback-close",
                disabled: t,
                onClick: i,
                "aria-label": "关闭",
                children: /* @__PURE__ */ c(wa, { size: 18 })
              }
            )
          ] }),
          r && r.length > 1 ? /* @__PURE__ */ c("div", { className: "ws-flow-feedback-tabs", children: r.map((l, f) => /* @__PURE__ */ v(
            "button",
            {
              type: "button",
              className: l.id === o ? "is-active" : "",
              onClick: () => s?.(l),
              children: [
                /* @__PURE__ */ c("span", { children: f + 1 }),
                l.status === "pending" ? "待反馈" : "已提交"
              ]
            },
            l.id
          )) }) : null,
          /* @__PURE__ */ c("div", { className: "ws-flow-feedback-body custom-scrollbar", children: /* @__PURE__ */ c(
            st,
            {
              fallback: /* @__PURE__ */ c(it, { label: "正在加载交互表单" }),
              children: /* @__PURE__ */ c(
                qm,
                {
                  interaction: d,
                  disabled: t,
                  readonly: n,
                  hideHeader: !0,
                  layout: "dialog",
                  initialData: n ? e.values : void 0,
                  onSubmit: (l) => a({
                    ...e.values || {},
                    ...l.data
                  })
                }
              )
            }
          ) }),
          n ? /* @__PURE__ */ c("footer", { className: "ws-flow-feedback-foot", children: /* @__PURE__ */ v(
            "button",
            {
              type: "button",
              className: "ws-flow-feedback-submit",
              onClick: i,
              children: [
                /* @__PURE__ */ c(cr, { size: 16 }),
                /* @__PURE__ */ c("span", { children: "知道了" })
              ]
            }
          ) }) : null
        ]
      }
    ) }),
    u
  );
}
function Ih(e) {
  const t = e.interaction?.interaction || {}, n = Array.isArray(t.fields) ? t.fields : e.fields.length > 0 ? e.fields : [
    {
      id: 0,
      key: "text",
      name: "补充信息",
      type: "textarea",
      required: !0
    }
  ];
  return {
    ...t,
    id: String(t.id || `flow-feedback-${e.approval?.id || 0}`),
    type: String(t.type || "").toLowerCase() === "power_params" && n.length > 0 ? "form" : String(t.type || "form"),
    title: String(t.title || e.title || "补充信息"),
    description: String(t.description || e.description || ""),
    fields: n,
    values: e.values
  };
}
const Ko = { zIndex: 999 };
function Sh(e) {
  const t = Math.max(
    0.35,
    Math.min(1.45, Number.isFinite(e) ? e : 1)
  );
  return {
    "--ws-node-overlay-scale": String(1 / t),
    "--ws-node-overlay-gap": `${16 / t}px`
  };
}
function vh() {
  return {
    zIndex: 999,
    "--ws-node-overlay-scale": "1",
    "--ws-node-overlay-gap": "16px"
  };
}
function Su(e) {
  return e.type === "function" && e.functionOption?.key === "start";
}
function vu(e) {
  return e.type === "function" && e.functionOption?.key === "start" ? "将从该开始节点沿连接线执行后续节点，直到保存或展示。" : e.type === "flow" ? "将执行该流程，并把最终结果展示在节点旁。" : e.type === "agent" ? "将把当前提示词、文件和上下文发送给该智能体。" : e.type === "power" ? "将使用当前参数运行该能力节点。" : "确认后开始执行该节点。";
}
function or({
  icon: e,
  label: t,
  accent: n,
  menu: r
}) {
  return /* @__PURE__ */ v(
    "button",
    {
      type: "button",
      className: `ws-node-setting-button ${n ? `is-${n}` : ""}`,
      children: [
        /* @__PURE__ */ c(e, { size: 12 }),
        /* @__PURE__ */ c("span", { children: t }),
        r ? /* @__PURE__ */ c(uo, { size: 11 }) : null
      ]
    }
  );
}
async function Ch(e, t, n, r) {
  if (e.group?.origin !== "script") {
    await r(t);
    return;
  }
  const o = n.filter(po), s = o.filter(
    (a) => a.storyboardItem?.stale || !ft(a)
  ), i = s.length > 0 ? s : o;
  await Gl(i, r);
}
function Rh({ data: e, selected: t }) {
  const n = e, r = e.sourceNode || n, o = Number(e.projectId || 0), s = e.runningNode, i = e.canvasRunningNodes || {}, a = e.setRunningNode, d = e.onShowNodeDetail, u = e.onNodeResult;
  e.onAssetCreated;
  const l = e.onOpenFeedbackRecord, f = n.type === "power" ? Nr(n.power, n.kind, n.outputType) : null, b = f?.viewMode === "storyboard", _ = f?.viewMode === "video_compose", w = e.canvasReferenceItems || [], D = e.onNodeDraftChange, A = e.onRunBackendNode, z = !!e.structureLocked, V = e.storyboardSourceNode;
  if (n.type === "group") {
    const R = e.groupMembers || [], G = !!e.storyboardFrameRunning, ee = ql({
      members: R,
      runningNodes: i,
      groupState: s,
      hasResult: ft
    }), C = e.onRunBackendNode, T = String(e.runBlockedReason || "");
    let O;
    return C && !T && !G && (O = () => {
      a?.((H) => ({
        ...H,
        [n.id]: {
          nodeId: n.id,
          title: n.title,
          startedAt: Date.now(),
          progress: 8,
          status: "running"
        }
      })), Ch(
        n,
        r,
        R,
        C
      ).then(() => {
        a?.((H) => un(H, n.id));
      }).catch((H) => {
        a?.((re) => ({
          ...re,
          [n.id]: {
            ...re[n.id] || {
              nodeId: n.id,
              title: n.title,
              startedAt: Date.now(),
              progress: 8
            },
            status: "error"
          }
        })), ne.error(
          H instanceof Error ? H.message : "分组运行失败"
        ), window.setTimeout(() => {
          a?.(
            (re) => un(re, n.id)
          );
        }, 1400);
      });
    }), /* @__PURE__ */ c(st, { fallback: /* @__PURE__ */ c(it, { label: "正在加载分组" }), children: /* @__PURE__ */ v(
      Jm,
      {
        node: n,
        memberCount: ee.memberCount,
        runnableCount: ee.runnableCount,
        completedCount: ee.completedCount,
        failedCount: ee.failedCount,
        staleCount: ee.staleCount,
        status: ee.status,
        frameRunning: G,
        selected: t,
        managed: z,
        onRename: u && !z ? (H) => u(n.id, { title: H, titleMode: "manual" }) : void 0,
        onEditStructure: V && d ? () => d(
          V,
          kc(n)
        ) : void 0,
        onRun: O,
        runBlockedReason: T,
        children: [
          /* @__PURE__ */ c(
            Be,
            {
              id: "input-0",
              type: "target",
              position: je.Left,
              className: "is-in"
            }
          ),
          /* @__PURE__ */ c(
            Be,
            {
              id: "output-0",
              type: "source",
              position: je.Right,
              className: "is-out"
            }
          ),
          /* @__PURE__ */ c(Wt, { node: n, selected: t })
        ]
      }
    ) });
  }
  if (n.type === "agent") {
    const R = Pt(s) || s?.status === "success";
    return /* @__PURE__ */ v(
      "div",
      {
        className: `ws-node-agent-wrap ${t ? "is-selected" : ""} ${R ? "is-running" : ""}`,
        children: [
          /* @__PURE__ */ c(
            Be,
            {
              id: "input-0",
              type: "target",
              position: je.Left,
              className: "is-in",
              style: { left: "4px" }
            }
          ),
          /* @__PURE__ */ c(
            Be,
            {
              id: "output-0",
              type: "source",
              position: je.Right,
              className: "is-out",
              style: { right: "4px" }
            }
          ),
          /* @__PURE__ */ v("div", { className: "ws-node-circle", children: [
            /* @__PURE__ */ c("div", { className: "ws-node-circle-avatar", children: /* @__PURE__ */ c(dd, { size: 20, className: "ws-icon-amber" }) }),
            /* @__PURE__ */ c(
              Po,
              {
                className: "ws-node-circle-title",
                title: n.title,
                onRename: u ? (G) => u(n.id, { title: G, titleMode: "manual" }) : void 0
              }
            )
          ] }),
          R ? /* @__PURE__ */ v(
            "svg",
            {
              className: "ws-node-running-border is-spin is-circle is-agent",
              "aria-hidden": "true",
              viewBox: "0 0 100 100",
              preserveAspectRatio: "none",
              children: [
                /* @__PURE__ */ c(
                  "circle",
                  {
                    className: "ws-node-running-track",
                    cx: "50",
                    cy: "50",
                    r: "47",
                    pathLength: "100"
                  }
                ),
                /* @__PURE__ */ c(
                  "circle",
                  {
                    className: "ws-node-running-progress",
                    cx: "50",
                    cy: "50",
                    r: "47",
                    pathLength: "100",
                    strokeDasharray: "18 82",
                    strokeDashoffset: "0"
                  }
                )
              ]
            }
          ) : null,
          /* @__PURE__ */ c(
            Vo,
            {
              node: n,
              onOpenFeedbackRecord: l
            }
          ),
          /* @__PURE__ */ c(
            Uo,
            {
              node: n,
              runningNode: s,
              onShowNodeDetail: d
            }
          ),
          /* @__PURE__ */ c(Wt, { node: n, selected: t })
        ]
      }
    );
  }
  if (n.type === "flow") {
    const R = Pt(s) || s?.status === "success";
    return /* @__PURE__ */ v(
      "div",
      {
        className: `ws-node-flow-wrap ${t ? "is-selected" : ""} ${R ? "is-running" : ""}`,
        children: [
          /* @__PURE__ */ c(
            "svg",
            {
              className: "ws-hexagon-svg",
              viewBox: "0 0 100 100",
              fill: "currentColor",
              children: /* @__PURE__ */ c(
                "polygon",
                {
                  points: "50,4 93,27 93,73 50,96 7,73 7,27",
                  stroke: t ? "var(--ws-blue)" : "var(--ws-border)",
                  strokeWidth: "1.5",
                  strokeLinejoin: "round"
                }
              )
            }
          ),
          R ? /* @__PURE__ */ v(
            "svg",
            {
              className: "ws-node-running-border is-spin is-hexagon",
              "aria-hidden": "true",
              viewBox: "0 0 100 100",
              preserveAspectRatio: "none",
              children: [
                /* @__PURE__ */ c(
                  "polygon",
                  {
                    className: "ws-node-running-track",
                    points: "50,5 92,28 92,72 50,95 8,72 8,28",
                    pathLength: "100"
                  }
                ),
                /* @__PURE__ */ c(
                  "polygon",
                  {
                    className: "ws-node-running-progress",
                    points: "50,5 92,28 92,72 50,95 8,72 8,28",
                    pathLength: "100",
                    strokeDasharray: "18 82",
                    strokeDashoffset: "0"
                  }
                )
              ]
            }
          ) : null,
          /* @__PURE__ */ v("div", { className: "ws-node-flow-content", children: [
            /* @__PURE__ */ c("div", { className: "ws-node-flow-avatar", children: /* @__PURE__ */ c(ld, { size: 16, className: "ws-icon-blue" }) }),
            /* @__PURE__ */ c(
              Po,
              {
                className: "ws-node-flow-title",
                title: n.title,
                onRename: u ? (G) => u(n.id, { title: G, titleMode: "manual" }) : void 0
              }
            )
          ] }),
          /* @__PURE__ */ c(
            Be,
            {
              id: "input-0",
              type: "target",
              position: je.Left,
              className: "is-in",
              style: { left: "11px" }
            }
          ),
          /* @__PURE__ */ c(
            Be,
            {
              id: "output-0",
              type: "source",
              position: je.Right,
              className: "is-out",
              style: { right: "11px" }
            }
          ),
          /* @__PURE__ */ c(
            Vo,
            {
              node: n,
              onOpenFeedbackRecord: l
            }
          ),
          /* @__PURE__ */ c(Uo, { node: n, onShowNodeDetail: d }),
          /* @__PURE__ */ c(Wt, { node: n, selected: t })
        ]
      }
    );
  }
  if (n.type === "function") {
    const R = n.functionOption?.key || (n.title.includes("保存") ? "save" : ""), G = R === "start", ee = hu(R), C = n.onRunStartNode, T = n.onRunFunctionNode, O = n.onOpenImportPicker, H = n.requestConfirm, re = Pt(s), X = G && ag(i), oe = re, J = Js(n), Z = J ? { left: "0px", top: "19px" } : { left: "0px" }, fe = J ? { left: "128px", right: "auto", top: "19px" } : { right: "0px" }, ge = (M) => {
      a && (a((we) => ({
        ...we,
        [n.id]: {
          nodeId: n.id,
          title: n.title,
          startedAt: Date.now(),
          progress: M === "success" ? 100 : M === "error" ? 92 : 0,
          status: M
        }
      })), M !== "running" && M !== "waiting" && window.setTimeout(
        () => a((we) => un(we, n.id)),
        M === "success" ? 650 : 1200
      ));
    }, _e = () => {
      if (oe || X)
        return;
      const M = !G, we = T ? R === "start" && C ? () => C(n).then(() => !0) : () => T(n) : R === "start" && C ? () => C(n).then(() => !0) : R === "import" && O ? () => (O(n.id), Promise.resolve(!0)) : null;
      if (we) {
        if (H && Su(n)) {
          H({
            title: `执行「${n.title}」`,
            description: vu(n),
            confirmText: "执行",
            onConfirm: () => {
              M && ge("running"), we().then(() => {
                M && ge("success");
              }).catch((Ue) => {
                M && ge("error"), ne.error(Ue instanceof Error ? Ue.message : "执行出错");
              });
            }
          });
          return;
        }
        M && ge("running"), we().then(() => {
          M && ge("success");
        }).catch((Ue) => {
          M && ge("error"), ne.error(Ue instanceof Error ? Ue.message : "执行出错");
        });
      }
    }, ve = (M) => {
      M.preventDefault(), M.stopPropagation(), _e();
    }, at = !!T || R === "start" && !!C || R === "import" && !!O;
    return /* @__PURE__ */ v(
      "div",
      {
        className: `ws-node-function-wrap ${t ? "is-selected" : ""} ${oe ? "is-running" : ""} ${J ? "has-result-card" : ""} is-${R || "default"}`,
        children: [
          /* @__PURE__ */ v(
            "div",
            {
              className: "ws-node-function-pill",
              role: at ? "button" : void 0,
              tabIndex: at ? 0 : void 0,
              "aria-disabled": at ? oe : void 0,
              onClick: at ? ve : void 0,
              onKeyDown: (M) => {
                !at || M.key !== "Enter" && M.key !== " " || (M.preventDefault(), M.stopPropagation(), _e());
              },
              children: [
                /* @__PURE__ */ c("div", { className: "ws-node-function-icon", children: re ? /* @__PURE__ */ c(Bt, { size: 15, className: "ws-spin" }) : /* @__PURE__ */ c(
                  ee,
                  {
                    size: 15,
                    fill: G ? "currentColor" : "none"
                  }
                ) }),
                /* @__PURE__ */ c("span", { className: "ws-node-function-title", children: re ? s?.status === "waiting" ? "等待中" : "运行中" : n.title })
              ]
            }
          ),
          J ? /* @__PURE__ */ c(
            lh,
            {
              node: n,
              running: oe,
              onShowNodeDetail: d
            }
          ) : null,
          /* @__PURE__ */ c(
            Be,
            {
              id: "input-0",
              type: "target",
              position: je.Left,
              className: "is-in",
              style: Z
            }
          ),
          /* @__PURE__ */ c(
            Be,
            {
              id: "output-0",
              type: "source",
              position: je.Right,
              className: "is-out",
              style: fe
            }
          ),
          /* @__PURE__ */ c(
            Vo,
            {
              node: n,
              onOpenFeedbackRecord: l
            }
          ),
          J ? null : /* @__PURE__ */ c(Uo, { node: n, onShowNodeDetail: d }),
          /* @__PURE__ */ c(Wt, { node: n, selected: t })
        ]
      }
    );
  }
  if (n.type === "asset") {
    if (n.kind === "image") {
      const X = $o(n), oe = Cn(n), J = sr(oe, X), Z = [
        "ws-node-image-wrap",
        t ? "is-selected" : "",
        X.imageUrl ? "has-media" : ""
      ].filter(Boolean).join(" ");
      return /* @__PURE__ */ v("div", { className: Z, children: [
        /* @__PURE__ */ v("div", { className: "ws-node-floating-label", children: [
          /* @__PURE__ */ c(ti, { size: 13, className: "ws-icon-green" }),
          /* @__PURE__ */ c("span", { children: n.title || "图片资产" })
        ] }),
        /* @__PURE__ */ c("div", { className: "ws-node-image-container ws-node-content-container", children: J ? /* @__PURE__ */ c("div", { className: "ws-node-scroll-content nowheel", children: /* @__PURE__ */ c(
          Vr,
          {
            output: oe,
            fallback: X.text || n.description || "图片资产",
            className: "ws-canvas-content-view"
          }
        ) }) : X.imageUrl ? /* @__PURE__ */ c(
          "img",
          {
            src: X.imageUrl,
            alt: n.title,
            className: "ws-node-image-raw",
            loading: "lazy",
            decoding: "async"
          }
        ) : /* @__PURE__ */ v("div", { className: "ws-node-image-empty", children: [
          /* @__PURE__ */ c(ti, { size: 24 }),
          /* @__PURE__ */ c("span", { children: X.text || n.description || "图片资产" })
        ] }) }),
        /* @__PURE__ */ c(
          Be,
          {
            id: "input-0",
            type: "target",
            position: je.Left,
            className: "is-in"
          }
        ),
        /* @__PURE__ */ c(
          Be,
          {
            id: "output-0",
            type: "source",
            position: je.Right,
            className: "is-out"
          }
        ),
        /* @__PURE__ */ c(
          rr,
          {
            node: n,
            onShowNodeDetail: d
          }
        ),
        /* @__PURE__ */ c(Wt, { node: n, selected: t })
      ] });
    }
    if (n.kind === "video") {
      const X = $o(n), oe = Cn(n), J = sr(oe, X), Z = [
        "ws-node-video-wrap",
        t ? "is-selected" : "",
        X.videoUrl || X.imageUrl ? "has-media" : ""
      ].filter(Boolean).join(" ");
      return /* @__PURE__ */ v("div", { className: Z, children: [
        /* @__PURE__ */ v("div", { className: "ws-node-floating-label", children: [
          /* @__PURE__ */ c(ri, { size: 13, className: "ws-icon-green" }),
          /* @__PURE__ */ c("span", { children: n.title || "视频资产" })
        ] }),
        /* @__PURE__ */ v("div", { className: "ws-node-video-container ws-node-content-container", children: [
          J ? /* @__PURE__ */ c("div", { className: "ws-node-scroll-content nowheel", children: /* @__PURE__ */ c(
            Vr,
            {
              output: oe,
              fallback: X.text || n.description || "视频资产",
              className: "ws-canvas-content-view"
            }
          ) }) : X.videoUrl ? /* @__PURE__ */ c(
            "video",
            {
              src: X.videoUrl,
              className: "ws-node-video-raw",
              muted: !0,
              playsInline: !0,
              preload: "metadata"
            },
            X.videoUrl
          ) : X.imageUrl ? /* @__PURE__ */ c(
            "img",
            {
              src: X.imageUrl,
              alt: n.title,
              className: "ws-node-video-raw",
              loading: "lazy",
              decoding: "async"
            }
          ) : /* @__PURE__ */ v("div", { className: "ws-node-image-empty", children: [
            /* @__PURE__ */ c(ri, { size: 24 }),
            /* @__PURE__ */ c("span", { children: X.text || n.description || "视频资产" })
          ] }),
          J ? null : /* @__PURE__ */ c("div", { className: "ws-node-video-play", children: /* @__PURE__ */ c("div", { children: /* @__PURE__ */ c(lo, { size: 14, fill: "currentColor" }) }) })
        ] }),
        /* @__PURE__ */ c(
          Be,
          {
            id: "input-0",
            type: "target",
            position: je.Left,
            className: "is-in"
          }
        ),
        /* @__PURE__ */ c(
          Be,
          {
            id: "output-0",
            type: "source",
            position: je.Right,
            className: "is-out"
          }
        ),
        /* @__PURE__ */ c(
          rr,
          {
            node: n,
            onShowNodeDetail: d
          }
        ),
        /* @__PURE__ */ c(Wt, { node: n, selected: t })
      ] });
    }
    const R = $o(n), G = Ks(n), ee = Cn(n), C = Gs(n), T = Te(ee) ? ee : G ? { rich: G } : C || R.text, O = sr(T, R), H = !!(R.imageUrl || R.videoUrl || R.audioUrl), re = [
      "ws-node-text-wrap",
      t ? "is-selected" : "",
      H ? "has-media" : ""
    ].filter(Boolean).join(" ");
    return /* @__PURE__ */ v("div", { className: re, children: [
      /* @__PURE__ */ v("div", { className: "ws-node-floating-label", children: [
        /* @__PURE__ */ c(cd, { size: 13, className: "ws-icon-green" }),
        /* @__PURE__ */ c("span", { children: n.title })
      ] }),
      /* @__PURE__ */ c("div", { className: "ws-node-text-card", children: !O && R.imageUrl ? /* @__PURE__ */ c("div", { className: "ws-node-text-media", children: /* @__PURE__ */ c(
        "img",
        {
          src: R.imageUrl,
          alt: hr(R) || n.title,
          loading: "lazy",
          decoding: "async"
        }
      ) }) : !O && R.videoUrl ? /* @__PURE__ */ c("div", { className: "ws-node-text-media", children: /* @__PURE__ */ c(
        "video",
        {
          src: R.videoUrl,
          muted: !0,
          playsInline: !0,
          preload: "metadata"
        },
        R.videoUrl
      ) }) : !O && R.audioUrl ? /* @__PURE__ */ c("div", { className: "ws-node-text-media is-audio", children: /* @__PURE__ */ c(
        st,
        {
          fallback: /* @__PURE__ */ c(it, { label: "正在加载音频", compact: !0 }),
          children: /* @__PURE__ */ c(Mc, { src: R.audioUrl })
        }
      ) }) : !O && R.fileUrl ? /* @__PURE__ */ v("div", { className: "ws-node-text-file", children: [
        /* @__PURE__ */ c(ys, { size: 16 }),
        /* @__PURE__ */ c("span", { children: hr(R) || "文件内容" })
      ] }) : /* @__PURE__ */ c("div", { className: "ws-node-scroll-content nowheel", children: /* @__PURE__ */ c(
        Vr,
        {
          output: T,
          fallback: C || R.text || "暂无内容",
          className: "ws-canvas-content-view"
        }
      ) }) }),
      /* @__PURE__ */ c(
        Be,
        {
          id: "input-0",
          type: "target",
          position: je.Left,
          className: "is-in"
        }
      ),
      /* @__PURE__ */ c(
        Be,
        {
          id: "output-0",
          type: "source",
          position: je.Right,
          className: "is-out"
        }
      ),
      /* @__PURE__ */ c(
        rr,
        {
          node: n,
          onShowNodeDetail: d
        }
      ),
      /* @__PURE__ */ c(Wt, { node: n, selected: t })
    ] });
  }
  if (n.type === "power") {
    const R = Pt(s), G = Na(n.power, n.kind), ee = ft(n), C = R ? "running" : s?.status === "error" ? "error" : s?.status === "success" && !ee ? "running" : ee ? "complete" : "empty", T = !!(!b && !_ && s?.streamStarted && (s.streamText || s.streamOutput) && s.status !== "success"), O = T ? s?.streamOutput ? gn(s.streamOutput, "audio") : {
      text: s?.streamText || "",
      imageUrl: "",
      videoUrl: "",
      audioUrl: "",
      fileUrl: ""
    } : Xn(n), H = T ? s?.streamOutput || { text: s?.streamText || "" } : Cn(n), re = b || _ || T || ee, X = !b && !_ && !!(O.imageUrl || O.videoUrl || O.audioUrl || O.fileUrl), oe = !n.groupId && Ns(n), J = [
      "ws-node-power-wrap",
      t ? "is-selected" : "",
      R ? "is-running" : "",
      n.runError && !R ? "is-error" : "",
      b ? "is-storyboard" : "",
      _ ? "is-video-compose" : "",
      G ? "is-audio" : "",
      re ? "has-content" : "",
      X ? "has-media" : ""
    ].filter(Boolean).join(" ");
    return /* @__PURE__ */ v("div", { className: J, children: [
      /* @__PURE__ */ v("div", { className: "ws-node-floating-label", children: [
        /* @__PURE__ */ c(
          ha,
          {
            power: n.power,
            kind: n.kind,
            outputType: n.outputType,
            size: 13,
            className: "ws-icon-violet"
          }
        ),
        /* @__PURE__ */ c(
          Po,
          {
            title: n.title,
            onRename: u && !z ? (Z) => u(n.id, { title: Z, titleMode: "manual" }) : void 0
          }
        ),
        n.storyboardItem?.stale ? /* @__PURE__ */ c(Je, { label: "上游素材或提示词已变化；当前结果仍可使用，重新运行可更新", children: /* @__PURE__ */ c("span", { className: "ws-node-stale-badge", children: "可更新" }) }) : null,
        mc(n) ? /* @__PURE__ */ c("span", { className: "ws-node-prompt-override-badge", children: "提示词已修改" }) : null
      ] }),
      /* @__PURE__ */ v("div", { className: "ws-node-power-card", children: [
        R ? /* @__PURE__ */ v("svg", { className: "ws-node-running-border is-spin", "aria-hidden": "true", children: [
          /* @__PURE__ */ c(
            "rect",
            {
              className: "ws-node-running-track",
              x: "0",
              y: "0",
              width: "100%",
              height: "100%",
              rx: "6",
              pathLength: "100"
            }
          ),
          /* @__PURE__ */ c(
            "rect",
            {
              className: "ws-node-running-progress",
              x: "0",
              y: "0",
              width: "100%",
              height: "100%",
              rx: "6",
              pathLength: "100",
              strokeDasharray: "18 82",
              strokeDashoffset: "0"
            }
          )
        ] }) : null,
        _ ? /* @__PURE__ */ c(
          st,
          {
            fallback: /* @__PURE__ */ c(it, { label: "正在加载视频合成" }),
            children: /* @__PURE__ */ c(
              Qm,
              {
                composition: n.composerDraft?.videoComposition,
                referenceItems: w.filter(
                  (Z) => Z.source !== "current" || Z.id !== n.id
                ),
                running: R,
                onChange: D ? (Z) => D(n.id, {
                  ...n.composerDraft || {},
                  videoComposition: Z
                }) : void 0,
                onRun: A ? (Z) => {
                  A({
                    ...n,
                    composerDraft: {
                      ...n.composerDraft || {},
                      videoComposition: Z
                    }
                  }).catch(
                    (fe) => ne.error(
                      fe instanceof Error ? fe.message : "视频合成失败"
                    )
                  );
                } : void 0,
                onOpenDetail: d ? () => d(n) : void 0
              }
            )
          }
        ) : b ? /* @__PURE__ */ c(
          st,
          {
            fallback: /* @__PURE__ */ c(it, { label: "正在加载分镜内容", compact: !0 }),
            children: /* @__PURE__ */ c(
              tg,
              {
                output: R ? s?.streamText || "" : my(n),
                status: C,
                started: !!s?.streamStarted,
                generatedShotCount: s?.generatedCount || 0,
                referenceItems: w.filter(
                  (Z) => Z.source !== "current" || Z.id !== n.id
                ),
                onOpenDetail: ee && d ? () => d(n) : void 0
              }
            )
          }
        ) : re ? /* @__PURE__ */ c(
          Cu,
          {
            preview: O,
            output: H,
            fallback: n.description,
            streaming: R && T,
            generating: R && X && !T,
            onMediaSize: oe ? (Z, fe) => {
              const ge = Ru(Z, fe);
              !ge || !u || (Math.abs((n.width || 0) - ge.width) > 2 || Math.abs((n.height || 0) - ge.height) > 2) && u(n.id, ge);
            } : void 0
          }
        ) : /* @__PURE__ */ c(Ah, {})
      ] }),
      n.runError && !R ? /* @__PURE__ */ c(
        kh,
        {
          projectId: o,
          node: n,
          onOpenDetail: d ? () => d(n) : void 0
        }
      ) : null,
      /* @__PURE__ */ c(
        Be,
        {
          id: "input-0",
          type: "target",
          position: je.Left,
          className: "is-in"
        }
      ),
      /* @__PURE__ */ c(
        Be,
        {
          id: "output-0",
          type: "source",
          position: je.Right,
          className: "is-out"
        }
      ),
      b || _ ? null : /* @__PURE__ */ c(
        rr,
        {
          node: n,
          onShowNodeDetail: d
        }
      ),
      /* @__PURE__ */ c(Wt, { node: n, selected: t })
    ] });
  }
  return /* @__PURE__ */ v("div", { className: `ws-node ${t ? "is-selected" : ""}`, children: [
    /* @__PURE__ */ c(
      Be,
      {
        id: "input-0",
        type: "target",
        position: je.Left,
        className: "is-in"
      }
    ),
    /* @__PURE__ */ c(
      Be,
      {
        id: "output-0",
        type: "source",
        position: je.Right,
        className: "is-out"
      }
    ),
    /* @__PURE__ */ c("div", { className: "ws-node-title", children: n.title }),
    /* @__PURE__ */ c("div", { className: "ws-node-desc", children: n.description }),
    /* @__PURE__ */ c(rr, { node: n, onShowNodeDetail: d }),
    /* @__PURE__ */ c(Wt, { node: n, selected: t })
  ] });
}
function kh({
  projectId: e,
  node: t,
  onOpenDetail: n
}) {
  const { error: r } = jm(e, t), o = /* @__PURE__ */ v(Qt, { children: [
    /* @__PURE__ */ c(Yu, { size: 14 }),
    /* @__PURE__ */ c("span", { children: r })
  ] });
  return /* @__PURE__ */ c(Je, { label: r, children: n ? /* @__PURE__ */ c(
    "button",
    {
      type: "button",
      className: "ws-node-run-error is-action nodrag nowheel",
      "aria-label": `打开错误详情：${r}`,
      onClick: (s) => {
        s.stopPropagation(), n();
      },
      children: o
    }
  ) : /* @__PURE__ */ c("div", { className: "ws-node-run-error", children: o }) });
}
function Cu({
  preview: e,
  output: t,
  fallback: n,
  streaming: r,
  generating: o = !1,
  onMediaSize: s,
  showMediaCaption: i = !0
}) {
  const a = te(null), d = te(!0), u = i ? hr(e) : "", l = sr(t, e);
  return le(() => {
    if (!r) {
      d.current = !0;
      return;
    }
    const f = a.current;
    !f || !d.current || (f.scrollTop = f.scrollHeight);
  }, [e.text, r]), !l && e.imageUrl ? /* @__PURE__ */ v(
    "div",
    {
      className: `ws-node-generated-media ${o ? "is-generating" : ""}`,
      children: [
        /* @__PURE__ */ c(
          xh,
          {
            src: e.imageUrl,
            alt: u || "生成图片",
            onMediaSize: s
          }
        ),
        u ? /* @__PURE__ */ c("p", { children: u }) : null,
        /* @__PURE__ */ c(Lo, { active: o })
      ]
    }
  ) : !l && e.videoUrl ? /* @__PURE__ */ v(
    "div",
    {
      className: `ws-node-generated-media ${o ? "is-generating" : ""}`,
      children: [
        /* @__PURE__ */ c(
          "video",
          {
            src: e.videoUrl,
            className: "nopan nowheel",
            controls: !0,
            playsInline: !0,
            preload: "metadata",
            onLoadedMetadata: (f) => s?.(
              f.currentTarget.videoWidth,
              f.currentTarget.videoHeight
            )
          },
          e.videoUrl
        ),
        u ? /* @__PURE__ */ c("p", { children: u }) : null,
        /* @__PURE__ */ c(Lo, { active: o })
      ]
    }
  ) : !l && e.audioUrl ? /* @__PURE__ */ v(
    "div",
    {
      className: `ws-node-generated-media is-audio ${o ? "is-generating" : ""}`,
      children: [
        /* @__PURE__ */ c(
          st,
          {
            fallback: /* @__PURE__ */ c(it, { label: "正在加载音频", compact: !0 }),
            children: /* @__PURE__ */ c(Mc, { src: e.audioUrl, autoPlay: r })
          }
        ),
        /* @__PURE__ */ c(Lo, { active: o })
      ]
    }
  ) : !l && e.fileUrl ? /* @__PURE__ */ v("div", { className: "ws-node-generated-file", children: [
    /* @__PURE__ */ c(ys, { size: 16 }),
    /* @__PURE__ */ c("span", { children: u || "文件内容" })
  ] }) : /* @__PURE__ */ c(
    "div",
    {
      ref: a,
      className: "ws-node-generated-text ws-node-scroll-content nowheel",
      onScroll: (f) => {
        const b = f.currentTarget;
        d.current = b.scrollHeight - b.scrollTop - b.clientHeight < 12;
      },
      children: /* @__PURE__ */ c(
        Vr,
        {
          output: t,
          fallback: e.text || n,
          streaming: r,
          className: "ws-canvas-content-view"
        }
      )
    }
  );
}
function xh({
  src: e,
  alt: t,
  onMediaSize: n
}) {
  const [r, o] = $(e);
  return le(() => {
    if (!e || e === r)
      return;
    let s = !0;
    const i = new Image();
    return i.onload = () => {
      (i.decode?.() || Promise.resolve()).catch(() => {
      }).then(() => {
        s && o(e);
      });
    }, i.src = e, () => {
      s = !1, i.onload = null;
    };
  }, [r, e]), /* @__PURE__ */ c(
    "img",
    {
      src: r,
      alt: t,
      loading: "lazy",
      decoding: "async",
      onLoad: (s) => n?.(
        s.currentTarget.naturalWidth,
        s.currentTarget.naturalHeight
      )
    }
  );
}
function Lo({ active: e }) {
  return e ? /* @__PURE__ */ v(
    "div",
    {
      className: "ws-node-media-generating nodrag nopan nowheel",
      role: "status",
      "aria-live": "polite",
      onPointerDown: (t) => t.stopPropagation(),
      onClick: (t) => t.stopPropagation(),
      children: [
        /* @__PURE__ */ c(Bt, { size: 18, className: "ws-spin" }),
        /* @__PURE__ */ c("span", { children: "生成中" })
      ]
    }
  ) : null;
}
function hr(e) {
  const t = String(e.text || "").trim();
  return !t || tn(t) ? "" : t;
}
function Ru(e, t) {
  if (!Number.isFinite(e) || !Number.isFinite(t) || e <= 0 || t <= 0)
    return null;
  const n = e / t, r = 330, o = 340;
  let s = r, i = s / n;
  return i > o && (i = o, s = i * n), {
    width: Math.round(aa(s, 150, r)),
    height: Math.round(aa(i, 150, o))
  };
}
function aa(e, t, n) {
  return Math.max(t, Math.min(n, e));
}
function Ah() {
  return /* @__PURE__ */ v("div", { className: "ws-node-power-empty", "aria-hidden": "true", children: [
    /* @__PURE__ */ c("span", {}),
    /* @__PURE__ */ c("span", {}),
    /* @__PURE__ */ c("span", {})
  ] });
}
function Th(e) {
  return e.type === "group" ? "#e85d75" : e.type === "asset" ? "#23c483" : e.type === "power" ? "#8b5cf6" : e.type === "agent" ? "#f59e0b" : e.type === "flow" ? "#3b82f6" : "#e85d75";
}
function Mh() {
  if (typeof window > "u")
    return 0;
  const e = new URLSearchParams(window.location.search);
  return Number(e.get("project_id") || e.get("id") || 0);
}
const _w = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  WorkSpacePage: yg
}, Symbol.toStringTag, { value: "Module" }));
export {
  Ss as a,
  yf as b,
  Ka as c,
  yw as d,
  hw as e,
  ww as f,
  Nw as g,
  bw as h,
  gw as i,
  _w as j,
  Rn as m,
  af as r,
  mw as s,
  jm as u
};
