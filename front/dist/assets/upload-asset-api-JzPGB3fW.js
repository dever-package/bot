import { c as we, j as s, a as f, F as Le } from "./createLucideIcon-Gw0gLVQ5.js";
import { L as V } from "./loader-circle-3ZsHTZm7.js";
import { n as nn, A as Ir } from "./power-menu-m-V-QDwl.js";
import { F as rn, A as on, C as Cr } from "./folder-open-Cq24veMe.js";
import { C as Tr } from "./chevron-right-2QoHuRdg.js";
import { R as xr } from "./refresh-cw-Dj0geApF.js";
import { U as Dr } from "./upload-A6t_uL3M.js";
import { m as Or } from "./confirm-dialog-BTqZnhxN.js";
import { r as F, j as v, g as sn, A as Mr, z as Rr, a as pe, u as N, b as Z, c as De, e as an } from "./runtime-entry-CkPHMDB1.js";
import { t as le } from "./index-wo12HRHg.js";
import { i as cn } from "./api-response-C-VXY2RJ.js";
import { c as dn } from "./in-flight-request-vHkSgDHd.js";
import { C as Xe } from "./check-_lGX5Mgn.js";
import { P as Nt } from "./pencil-WDd5tOSC.js";
import { R as Qe } from "./rotate-ccw-CYQko_-D.js";
import { T as zr } from "./trash-2-Cga0ORNu.js";
import { F as et } from "./file-text-Cka6tQvG.js";
import { I as Er, D as Pr } from "./image-C-kQbAhR.js";
import { m as $r } from "./content-view-BWYCBIVh.js";
import { E as Lr } from "./external-link-YRs6ZQq6.js";
import { m as Br } from "./tooltip-CL6cI5VZ.js";
import { C as jr } from "./chevron-down-DXFjwlDo.js";
import { X as un } from "./x-CDJG94MJ.js";
import { c as ln } from "./react-dom-C2oimP4o.js";
import { g as Vr } from "./site-config-9qHTjCAH.js";
import { A as Fr } from "./arrow-left-CNjrQxkH.js";
import { u as vr } from "./auth-scope-ibH_W2SO.js";
const Kr = [
  [
    "path",
    {
      d: "M4 6.835V4a2 2 0 0 1 2-2h8a2.4 2.4 0 0 1 1.706.706l3.588 3.588A2.4 2.4 0 0 1 20 8v12a2 2 0 0 1-2 2h-.343",
      key: "1vfytu"
    }
  ],
  ["path", { d: "M14 2v5a1 1 0 0 0 1 1h5", key: "wfsgrz" }],
  [
    "path",
    {
      d: "M2 19a2 2 0 0 1 4 0v1a2 2 0 0 1-4 0v-4a6 6 0 0 1 12 0v4a2 2 0 0 1-4 0v-1a2 2 0 0 1 4 0",
      key: "1etmh7"
    }
  ]
], Ur = we("file-headphone", Kr);
const Yr = [
  ["rect", { width: "18", height: "18", x: "3", y: "3", rx: "2", key: "afitv7" }],
  ["path", { d: "M7 3v18", key: "bbkbws" }],
  ["path", { d: "M3 7.5h4", key: "zfgn84" }],
  ["path", { d: "M3 12h18", key: "1i2n21" }],
  ["path", { d: "M3 16.5h4", key: "1230mu" }],
  ["path", { d: "M17 3v18", key: "in4fa5" }],
  ["path", { d: "M17 7.5h4", key: "myr1c1" }],
  ["path", { d: "M17 16.5h4", key: "go4c1d" }]
], Jr = we("film", Yr);
const Wr = [
  ["path", { d: "m22 11-1.296-1.296a2.4 2.4 0 0 0-3.408 0L11 16", key: "9kzy35" }],
  ["path", { d: "M4 8a2 2 0 0 0-2 2v10a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2", key: "1t0f0t" }],
  ["circle", { cx: "13", cy: "7", r: "1", fill: "currentColor", key: "1obus6" }],
  ["rect", { x: "8", y: "2", width: "14", height: "14", rx: "2", key: "1gvhby" }]
], qr = we("images", Wr);
const Hr = [
  [
    "path",
    {
      d: "M22 17a2 2 0 0 1-2 2H6.828a2 2 0 0 0-1.414.586l-2.202 2.202A.71.71 0 0 1 2 21.286V5a2 2 0 0 1 2-2h16a2 2 0 0 1 2 2z",
      key: "18887p"
    }
  ],
  ["path", { d: "M12 11h.01", key: "z322tv" }],
  ["path", { d: "M16 11h.01", key: "xkw8gn" }],
  ["path", { d: "M8 11h.01", key: "1dfujw" }]
], Gr = we("message-square-more", Hr);
const Zr = [
  [
    "path",
    {
      d: "m16 6-8.414 8.586a2 2 0 0 0 2.829 2.829l8.414-8.586a4 4 0 1 0-5.657-5.657l-8.379 8.551a6 6 0 1 0 8.485 8.485l8.379-8.551",
      key: "1miecu"
    }
  ]
], Xr = we("paperclip", Zr);
const Qr = [
  ["path", { d: "M21 12a9 9 0 1 1-9-9c2.52 0 4.93 1 6.74 2.74L21 8", key: "1p45f6" }],
  ["path", { d: "M21 3v5h-5", key: "1q7to0" }]
], eo = we("rotate-cw", Qr), to = dn(), no = dn();
function ro(e, t, n = "") {
  const r = JSON.stringify({
    requestScopeKey: n,
    teamID: e,
    catalogOptions: t || null
  });
  return to(r, async () => {
    const o = F(
      v("workbench/asset_filters"),
      "get",
      { team_id: e }
    ), [i, a] = t ? [null, await o] : await Promise.all([
      F(v("workbench/catalog"), "get", {
        team_id: e
      }),
      o
    ]), d = t ? {
      powers: t.tools,
      roles: t.dialogues,
      asset_cates: t.assetCates
    } : Y(i, "加载团队资产配置失败"), u = Y(a, "加载资产筛选项失败");
    return {
      projects: oe(u.projects).map(At).filter(Ee),
      tools: vt(d.powers, u.tools),
      dialogues: vt(d.roles, u.dialogues),
      assetCates: oe(d.asset_cates).map(yo).filter(Ee),
      nodes: oe(u.nodes).map(ho).filter(_o)
    };
  });
}
function oo(e) {
  const t = {
    ...e,
    pageSize: e.pageSize || 24,
    view: e.view || "assets",
    contentMode: e.contentMode || "preview"
  };
  return no(JSON.stringify(t), async () => {
    const n = await F(v("workbench/assets"), "get", {
      team_id: t.teamID,
      source_type: t.filters.sourceType || void 0,
      source_id: t.filters.sourceID || void 0,
      project_id: t.filters.projectID || void 0,
      scope_project_id: t.scopeProjectID || void 0,
      asset_cate_id: t.filters.assetCateID || void 0,
      collection_id: t.collectionID || void 0,
      node_key: t.filters.nodeKey || void 0,
      role: t.filters.role || void 0,
      kind: t.filters.kind || void 0,
      exclude_collections: t.excludeCollections ? 1 : void 0,
      view: t.view,
      content_mode: t.contentMode,
      page: t.page,
      page_size: t.pageSize
    }), r = Y(n, "加载资产失败");
    return {
      items: oe(r.items).map(Be).filter(Ee),
      page: pt(r.page, t.page),
      pageSize: pt(r.page_size, t.pageSize),
      total: nt(r.total),
      hasMore: !!r.has_more
    };
  });
}
async function io(e, t) {
  const n = await F(v("workbench/asset_detail"), "get", {
    team_id: e,
    asset_id: t
  });
  return mo(Y(n, "加载资产详情失败"));
}
async function so(e) {
  const t = await F(v("workbench/asset_versions"), "get", {
    team_id: e.teamID,
    asset_id: e.assetID,
    page: e.page,
    page_size: e.pageSize || 20
  }), n = Y(t, "加载资产版本失败");
  return {
    items: oe(n.items).map(tt).filter(Ee),
    total: nt(n.total),
    hasMore: !!n.has_more
  };
}
async function ao(e) {
  const t = await F(v("workbench/asset_version"), "get", {
    team_id: e.teamID,
    asset_id: e.assetID,
    version_id: e.versionID
  }), n = Y(t, "加载资产版本失败");
  return tt(n.version);
}
async function co(e) {
  const t = await F(
    v("workbench/asset_set_current"),
    "post",
    {
      team_id: e.teamID,
      asset_id: e.assetID,
      version_id: e.versionID
    }
  ), n = Y(t, "设置当前版本失败");
  return Be(n.asset);
}
async function uo(e) {
  const t = await F(v("workbench/asset_rename"), "post", {
    team_id: e.teamID,
    asset_id: e.assetID,
    name: e.name
  }), n = Y(t, "修改资产标题失败");
  return Be(n.asset);
}
async function lo(e) {
  const t = await F(v("workbench/asset_delete"), "post", {
    team_id: e.teamID,
    asset_id: e.assetID
  });
  Y(t, "删除资产失败");
}
async function fo(e) {
  const t = await F(v("workbench/asset_restore"), "post", {
    team_id: e.teamID,
    asset_id: e.assetID
  }), n = Y(t, "恢复资产失败");
  return Be(n.asset);
}
function mo(e) {
  return {
    asset: Be(e.asset),
    versions: oe(e.versions).map(tt).filter(Ee),
    versionTotal: nt(e.version_total),
    hasMore: !!e.has_more
  };
}
function Be(e) {
  const t = It(e?.version) ? tt(e.version) : null;
  return {
    id: R(e?.id),
    projectID: R(e?.project_id),
    bodyID: R(e?.body_id),
    teamID: R(e?.team_id),
    flowID: R(e?.flow_id),
    assetCateID: R(e?.asset_cate_id),
    collectionID: R(e?.collection_id),
    nodeKey: C(e?.node_key),
    sourceType: C(e?.source_type),
    sourceID: R(e?.source_id),
    sourceName: C(e?.source_name),
    name: C(e?.name) || "未命名资产",
    nameMode: C(e?.name_mode) === "manual" ? "manual" : "auto",
    kind: C(e?.kind) || "text",
    role: C(e?.role) || "material",
    versionID: R(e?.version_id),
    status: C(e?.status),
    summary: C(e?.summary || t?.summary),
    collectionCount: nt(e?.collection_count),
    collectionPreviews: oe(e?.collection_previews).map(po).filter((n) => !!n),
    createdAt: C(e?.created_at),
    deletedAt: C(e?.deleted_at),
    version: t
  };
}
function po(e) {
  const t = C(e?.kind);
  return t !== "image" && t !== "video" || !e?.content ? null : {
    id: R(e?.id),
    kind: t,
    content: e.content
  };
}
function tt(e) {
  return {
    id: R(e?.id),
    assetID: R(e?.asset_id),
    runID: R(e?.run_id),
    nodeRunID: R(e?.node_run_id),
    releaseID: R(e?.release_id),
    requestID: C(e?.request_id),
    nodeKey: C(e?.node_key),
    source: It(e?.source) ? e.source : {},
    version: pt(e?.version, 1),
    content: e?.content,
    summary: C(e?.summary),
    createdAt: C(e?.created_at),
    updatedAt: C(e?.updated_at || e?.created_at)
  };
}
function At(e) {
  return {
    id: R(e?.id),
    name: C(e?.name) || "未命名"
  };
}
function vt(...e) {
  const t = [], n = /* @__PURE__ */ new Set();
  for (const r of e)
    for (const o of oe(r)) {
      const i = At(o);
      i.id <= 0 || n.has(i.id) || (n.add(i.id), t.push(i));
    }
  return t;
}
function yo(e) {
  return {
    ...At(e),
    kind: C(e?.kind) || "text",
    cardinality: C(e?.cardinality) || "single"
  };
}
function ho(e) {
  return {
    projectID: R(e?.project_id),
    assetCateID: R(e?.asset_cate_id),
    nodeKey: C(e?.node_key),
    name: C(e?.name) || "未命名节点"
  };
}
function Y(e, t) {
  if (!cn(e))
    throw new Error(String(e?.message || e?.msg || t));
  return It(e?.data) ? e.data : {};
}
function Ee(e) {
  return e.id > 0;
}
function _o(e) {
  return e.projectID > 0 && !!e.nodeKey;
}
function oe(e) {
  return Array.isArray(e) ? e : [];
}
function It(e) {
  return !!e && typeof e == "object" && !Array.isArray(e);
}
function R(e) {
  const t = Number(e || 0);
  return Number.isFinite(t) && t > 0 ? t : 0;
}
function pt(e, t) {
  return R(e) || t;
}
function nt(e) {
  const t = Number(e || 0);
  return Number.isFinite(t) && t >= 0 ? t : 0;
}
function C(e) {
  return e == null ? "" : String(e).trim();
}
const fn = [
  { key: "project", label: "创作" },
  { key: "tool", label: "工具" },
  { key: "dialogue", label: "对话" },
  { key: "upload", label: "上传" }
], mn = [
  { key: "work", label: "作品" },
  { key: "material", label: "素材" }
], Ct = [
  { key: "collection", label: "集合" },
  { key: "text", label: "文本" },
  { key: "image", label: "图片" },
  { key: "audio", label: "音频" },
  { key: "video", label: "视频" },
  { key: "richtext", label: "富文本" },
  { key: "file", label: "文件" }
];
function pn(e, t = {}) {
  const n = t.fallback || "资产", r = Tt(fn, e, n);
  return t[e] || r;
}
function go(e) {
  return Tt(mn, e, "素材");
}
function _e(e) {
  return Tt(Ct, e, "资产");
}
function Oa(e) {
  if (e.length === 0 || e.some((r) => ["text", "richtext", "file"].includes(r)))
    return;
  const t = {
    image: "image/*",
    audio: "audio/*",
    video: "video/*"
  }, n = e.map((r) => t[r]).filter((r) => !!r);
  return n.length > 0 ? Array.from(new Set(n)).join(",") : void 0;
}
function Tt(e, t, n) {
  return e.find((r) => r.key === t)?.label || n;
}
const bo = Br.HoverTip, wo = 12e3;
function L({
  label: e,
  side: t = "top",
  sideOffset: n = 7,
  className: r = "",
  children: o
}) {
  return /* @__PURE__ */ s(
    bo,
    {
      content: e,
      side: t,
      sideOffset: n,
      layerZIndex: wo,
      className: `max-w-80 whitespace-normal break-words ${r}`.trim(),
      children: o
    }
  );
}
function ko(e) {
  const t = yn(e);
  return !t || t.hasMedia ? "" : t.markdown;
}
function yn(e) {
  const t = No(e);
  return !t || !hn(t) ? null : {
    markdown: t.content.map(_n).join(`

`).trim(),
    plainText: t.content.map(gn).join(`

`).trim(),
    hasMedia: bn(t)
  };
}
function So(e) {
  return /(^|\n)\s*(#{1,6}\s|[-*+]\s|>\s|\d+\.\s|```)/m.test(e) || /(\*\*[^*]+\*\*|__[^_]+__|\[[^\]]+\]\([^)]+\)|`[^`]+`)/.test(
    e
  );
}
function No(e) {
  return yt(e) ? e.type === "doc" && Array.isArray(e.content) ? e : yt(e.rich) && e.rich.type === "doc" && Array.isArray(e.rich.content) ? e.rich : null : null;
}
function hn(e) {
  return yt(e) ? e.type === "text" ? !Array.isArray(e.marks) || e.marks.length === 0 : e.type === "hardBreak" ? !0 : rt(e) ? !!wn(e) : e.type !== "doc" && e.type !== "paragraph" ? !1 : Array.isArray(e.content) && e.content.every(hn) : !1;
}
function _n(e) {
  return e.type === "text" ? String(e.text || "") : e.type === "hardBreak" ? `
` : rt(e) ? `![${Ao(
    String(e.attrs?.alt || e.attrs?.caption || "图片")
  )}](<${Io(wn(e))}>)` : Array.isArray(e.content) ? e.content.map(_n).join("") : "";
}
function gn(e) {
  return e.type === "text" ? String(e.text || "") : e.type === "hardBreak" ? `
` : rt(e) ? "" : Array.isArray(e.content) ? e.content.map(gn).join("") : "";
}
function bn(e) {
  return rt(e) || !!e.content?.some((t) => bn(t));
}
function rt(e) {
  return ["image", "mediaImage", "editorMediaImage"].includes(
    String(e.type || "")
  );
}
function wn(e) {
  return String(e.attrs?.src || "").trim();
}
function Ao(e) {
  return e.replace(/([\\\[\]])/g, "\\$1");
}
function Io(e) {
  return e.replace(/</g, "%3C").replace(/>/g, "%3E");
}
function yt(e) {
  return !!e && typeof e == "object" && !Array.isArray(e);
}
function Co(e) {
  if (typeof e != "string")
    return e;
  const t = e.trim();
  if (!Mo(t))
    return e;
  try {
    return JSON.parse(t);
  } catch {
    return e;
  }
}
function kn(e) {
  const t = String(e || "").trim(), n = xo(t), r = Eo(n);
  for (const o of Nn([t, n, r])) {
    const i = Co(o);
    if (i !== o)
      return i;
    const a = Ro(o);
    if (a !== o)
      return a;
  }
  return e;
}
function Ma(e) {
  const t = String(e || "").trim();
  for (const n of Sn(t)) {
    const r = kn(n);
    if (r !== n)
      return r;
  }
  return e;
}
function To(e) {
  const t = [];
  for (const n of Sn(
    String(e || "").trim()
  )) {
    const r = kn(n);
    r !== n && t.push(r);
  }
  return t;
}
function xo(e) {
  let t = "", n = !1, r = !1;
  for (const o of e) {
    if (r) {
      t += o, r = !1;
      continue;
    }
    if (o === "\\") {
      t += o, r = n;
      continue;
    }
    if (o === '"') {
      n = !n, t += o;
      continue;
    }
    if (n && o.charCodeAt(0) < 32) {
      t += zo(o);
      continue;
    }
    t += o;
  }
  return t;
}
function Sn(e) {
  const t = [e];
  for (const n of e.matchAll(/```(?:json|storyboard)?\s*([\s\S]*?)```/gi))
    t.push(String(n[1] || "").trim());
  return t.push(...Do(e)), Nn(t);
}
function Do(e) {
  const t = [];
  for (let n = 0; n < e.length; n += 1) {
    const r = e[n];
    if (r !== "{" && r !== "[")
      continue;
    const o = Oo(e, n);
    o && (t.push(o), n += o.length - 1);
  }
  return t;
}
function Oo(e, t) {
  const n = [];
  let r = !1, o = !1;
  for (let i = t; i < e.length; i += 1) {
    const a = e[i];
    if (o) {
      o = !1;
      continue;
    }
    if (r && a === "\\") {
      o = !0;
      continue;
    }
    if (a === '"') {
      r = !r;
      continue;
    }
    if (r)
      continue;
    if (a === "{" || a === "[") {
      n.push(a);
      continue;
    }
    if (a !== "}" && a !== "]")
      continue;
    const d = a === "}" ? "{" : "[";
    if (n.pop() !== d)
      return "";
    if (n.length === 0)
      return e.slice(t, i + 1).trim();
  }
  return "";
}
function Mo(e) {
  return e.startsWith("{") && e.endsWith("}") || e.startsWith("[") && e.endsWith("]");
}
function Ro(e) {
  if (!e.startsWith('"') || !e.endsWith('"'))
    return e;
  try {
    const t = JSON.parse(e);
    return typeof t == "string" ? t : e;
  } catch {
    return e;
  }
}
function zo(e) {
  switch (e) {
    case `
`:
      return "\\n";
    case "\r":
      return "\\r";
    case "	":
      return "\\t";
    default:
      return `\\u${e.charCodeAt(0).toString(16).padStart(4, "0")}`;
  }
}
function Eo(e) {
  const t = e.trim();
  return !t.includes('\\"') || !t.startsWith("{") && !t.startsWith("[") ? e : t.replace(/\\"/g, '"');
}
function Nn(e) {
  const t = /* @__PURE__ */ new Set();
  return e.filter((n) => {
    const r = String(n || "").trim();
    return !r || t.has(r) ? !1 : (t.add(r), !0);
  });
}
const Po = [
  "visual_style",
  "motion_style",
  "character",
  "scene",
  "prop",
  "shot"
], $o = {
  visual_style: "视觉风格",
  motion_style: "动态风格",
  character: "角色参考",
  scene: "场景参考",
  prop: "道具参考",
  shot: "镜头参考"
};
function ot(e) {
  if (!Array.isArray(e))
    return [];
  const t = [], n = /* @__PURE__ */ new Set(), r = /* @__PURE__ */ new Set();
  for (const o of e) {
    if (!vo(o))
      continue;
    const i = Re(o.asset_id ?? o.assetId), a = An(o.kind), d = jo(o.purpose);
    if (!i || !a || !d || !Bo(a, d) || r.has(i))
      continue;
    let u = String(o.key || "").trim() || ht(i);
    if (n.has(u) && (u = ht(i)), n.has(u))
      continue;
    const m = Re(o.version_id ?? o.versionId), g = String(o.label || "").trim() || `参考素材 ${t.length + 1}`;
    t.push({
      key: u,
      asset_id: i,
      ...m ? { version_id: m } : {},
      label: g,
      kind: a,
      purpose: d,
      instruction: String(o.instruction || "").trim()
    }), n.add(u), r.add(i);
  }
  return t;
}
function Ra(e, t, n, r) {
  const o = new Map(
    ot(t).map((u) => [
      u.asset_id,
      u
    ])
  ), i = new Map(
    n.flatMap((u) => {
      const m = Re(u.refId);
      return m ? [[m, u]] : [];
    })
  ), a = [], d = /* @__PURE__ */ new Set();
  for (const u of e?.parts || []) {
    if (u.type !== "reference" || u.ref_type !== "asset")
      continue;
    const m = Re(u.ref_id);
    if (!m || d.has(m))
      continue;
    const g = o.get(m), p = i.get(m), _ = An(p?.kind) || g?.kind;
    if (!_)
      continue;
    const h = Re(p?.versionID || u.ref_version_id), w = String(p?.title || u.label || g?.label || "").trim() || `参考素材 ${a.length + 1}`;
    a.push({
      key: g?.key || ht(m),
      asset_id: m,
      ...h ? { version_id: h } : {},
      label: w,
      kind: _,
      purpose: g?.purpose || Vo(r, w, _),
      instruction: g?.instruction || ""
    }), d.add(m);
  }
  return a;
}
function Lo(e) {
  return (e === "video" ? ["motion_style", "visual_style", "shot"] : ["visual_style", "character", "scene", "prop", "shot"]).map((n) => ({
    value: n,
    label: $o[n]
  }));
}
function Bo(e, t) {
  return Lo(e).some(
    (n) => n.value === t
  );
}
function ht(e) {
  return `ref-${e}`;
}
function jo(e) {
  const t = String(e || "");
  return Po.includes(t) ? t : void 0;
}
function Vo(e, t, n) {
  const r = Fo(e, t);
  return /角色|人物|主角|外貌|长相|形象/.test(r) && n === "image" ? "character" : /场景|环境|地点|空间/.test(r) && n === "image" ? "scene" : /道具|产品|商品|物品/.test(r) && n === "image" ? "prop" : /镜头|构图|画面/.test(r) ? "shot" : /运镜|节奏|动作|转场|剪辑/.test(r) && n === "video" ? "motion_style" : /风格|画风|色调|光线|质感|视觉/.test(r) ? "visual_style" : n === "video" ? "motion_style" : "visual_style";
}
function Fo(e, t) {
  const n = `@${String(t || "").replace(/^@+/, "")}`, r = e.indexOf(n);
  return r < 0 ? e : e.slice(Math.max(0, r - 24), r + n.length + 32);
}
function An(e) {
  const t = String(e || "").trim().toLowerCase();
  return t === "image" || t === "video" ? t : void 0;
}
function Re(e) {
  const t = Number(e || 0);
  return Number.isInteger(t) && t > 0 ? t : 0;
}
function vo(e) {
  return !!(e && typeof e == "object" && !Array.isArray(e));
}
const _t = 8, it = 4, In = 50, Ko = [
  "none",
  "fade",
  "crossfade",
  "fadeblack",
  "fadewhite",
  "wipeleft",
  "wiperight"
], za = {
  none: "硬切",
  fade: "淡化",
  crossfade: "交叉溶解",
  fadeblack: "黑场淡化",
  fadewhite: "白场淡化",
  wipeleft: "向左擦除",
  wiperight: "向右擦除"
}, Uo = ["photoreal", "stylized"], Ea = {
  photoreal: "写实影像",
  stylized: "非写实影像"
}, Yo = [
  "16:9",
  "9:16",
  "1:1",
  "4:3",
  "3:4",
  "21:9"
], Jo = "16:9", Pa = {
  character: "角色",
  scene: "场景",
  prop: "道具"
}, Wo = [
  "shot_images",
  "final_video",
  "shot_videos",
  "storyboard_only"
], qo = {
  output_target: "shot_images",
  voice_mode: "auto",
  subtitle_mode: "auto",
  lip_sync_mode: "off",
  shot_visual_strategy: "auto"
}, Ho = {
  output_target: "final_video",
  voice_mode: "auto",
  subtitle_mode: "auto",
  lip_sync_mode: "auto",
  shot_visual_strategy: "auto"
}, Go = [
  "storyboard",
  "json",
  "output",
  "result",
  "data",
  "content",
  "body",
  "value",
  "text",
  "finalOutput",
  "final_output",
  "rich"
];
function Cn(e) {
  return Oe(e, /* @__PURE__ */ new Set(), 0);
}
function $a(e) {
  return e.shots.reduce(
    (t, n) => t + Math.max(0, Number(n.duration) || 0),
    0
  );
}
function Zo(e) {
  return Number.isInteger(e) && e >= it;
}
function La(e, t) {
  const n = new Map(
    e.materials.map((r) => [r.id, r])
  );
  return t.material_ids.map((r) => n.get(r)).filter((r) => !!r);
}
function Ba(e) {
  return {
    id: `shot-${e + 1}`,
    order: e + 1,
    duration: it,
    beat: "",
    transition: "",
    transition_type: "none",
    transition_duration_ms: 0,
    description: "",
    camera_instruction: "",
    video_prompt: "",
    material_ids: [],
    reference_keys: [],
    match_previous: !1,
    continue_previous: !1,
    continuity_anchor: "",
    speech: [],
    captions: []
  };
}
function ja(e, t) {
  const n = new Set(e.map((i) => i.id));
  let r = e.filter((i) => i.type === t).length + 1, o = `${t}-${r}`;
  for (; n.has(o); )
    r += 1, o = `${t}-${r}`;
  return {
    id: o,
    type: t,
    name: "",
    prompt: "",
    voice: "",
    reference_keys: []
  };
}
function Va(e, t) {
  const n = [], r = [];
  for (const o of e.shots) {
    o.material_ids.includes(t) && n.push(o.id);
    for (const i of o.speech)
      i.character_id === t && r.push(i.id);
  }
  return { shotIds: n, speechIds: r };
}
function Fa(e, t = "dialogue") {
  const n = new Set(e.speech.map((i) => i.id));
  let r = e.speech.length + 1, o = `${e.id}-speech-${r}`;
  for (; n.has(o); )
    r += 1, o = `${e.id}-speech-${r}`;
  return {
    id: o,
    kind: t,
    text: "",
    start_time: 0,
    subtitle_enabled: !0,
    subtitle_text: "",
    ...t === "dialogue" ? { character_id: "", speaker_mode: "offscreen" } : {}
  };
}
function va(e) {
  const t = new Set(e.captions.map((o) => o.id));
  let n = e.captions.length + 1, r = `${e.id}-caption-${n}`;
  for (; t.has(r); )
    n += 1, r = `${e.id}-caption-${n}`;
  return {
    id: r,
    type: "caption",
    text: "",
    start_time: 0,
    end_time: Math.min(e.duration, 2)
  };
}
function Ka(e) {
  const t = zn(e.workflow), n = ot(e.references), r = new Set(n.map((i) => i.key)), o = new Set(
    e.materials.map((i) => i.id)
  );
  return {
    ...e,
    version: _t,
    workflow: t,
    production_plan: Tn(
      e.production_plan,
      t.status === "confirmed"
    ),
    target_duration: Math.max(
      it,
      Math.round(Number(e.target_duration) || 0)
    ),
    target_shot_count: Math.min(
      In,
      Math.max(1, Math.round(Number(e.target_shot_count) || 0))
    ),
    narrator_voice: e.narrator_voice.trim(),
    aspect_ratio: On(e.aspect_ratio),
    references: n,
    materials: e.materials.map((i) => ({
      ...i,
      voice: i.type === "character" ? i.voice.trim() : "",
      reference_keys: ze(i.reference_keys).filter(
        (a) => r.has(a)
      )
    })),
    shots: e.shots.map((i, a) => ({
      ...i,
      id: i.id || `shot-${a + 1}`,
      order: a + 1,
      transition: a > 0 ? i.transition.trim() : "",
      transition_type: a > 0 ? Mn(i.transition_type) : "none",
      transition_duration_ms: a > 0 && i.transition_type !== "none" ? Math.min(5e3, Math.max(100, Math.round(i.transition_duration_ms))) : 0,
      material_ids: ze(i.material_ids).filter(
        (d) => o.has(d)
      ),
      reference_keys: ze(i.reference_keys).filter(
        (d) => r.has(d)
      ),
      match_previous: a > 0 && !i.continue_previous && !!i.match_previous,
      continue_previous: a > 0 && !!i.continue_previous,
      continuity_anchor: a > 0 && i.continue_previous ? i.continuity_anchor.trim() : ""
    }))
  };
}
function Ua(e, t) {
  const n = /* @__PURE__ */ new Map();
  return e.shots.forEach((r, o) => {
    o > 0 && n.set(r.id, e.shots[o - 1].id);
  }), {
    ...t,
    shots: t.shots.map((r, o) => {
      const i = o > 0 ? t.shots[o - 1].id : "", a = o === 0 || n.get(r.id) !== i;
      return {
        ...r,
        transition: a ? "" : r.transition,
        transition_type: a ? "none" : r.transition_type,
        transition_duration_ms: a ? 0 : r.transition_duration_ms,
        match_previous: !a && !r.continue_previous ? r.match_previous : !1,
        continue_previous: !a && !!r.continue_previous,
        continuity_anchor: !a && r.continue_previous ? r.continuity_anchor : ""
      };
    })
  };
}
function Ya(e) {
  return e.workflow.status === "confirmed";
}
function Tn(e, t = !1) {
  const n = t ? Ho : qo;
  if (!J(e))
    return { ...n };
  const r = D(e.output_target).toLowerCase();
  return {
    output_target: Wo.includes(
      r
    ) ? r : n.output_target,
    voice_mode: ut(
      e.voice_mode,
      n.voice_mode
    ),
    subtitle_mode: ut(
      e.subtitle_mode,
      n.subtitle_mode
    ),
    lip_sync_mode: ut(
      e.lip_sync_mode,
      n.lip_sync_mode
    ),
    shot_visual_strategy: "auto"
  };
}
function Ja(e) {
  return e.production_plan.output_target !== "storyboard_only";
}
function xn(e) {
  return ["shot_videos", "final_video"].includes(
    e.production_plan.output_target
  );
}
function Wa(e) {
  return e.production_plan.output_target === "final_video";
}
function Xo(e) {
  return xn(e) && e.production_plan.voice_mode === "auto" && Qo(e) > 0;
}
function qa(e) {
  return xn(e) && e.production_plan.subtitle_mode === "auto" && ei(e) > 0;
}
function Ha(e) {
  return Xo(e) && e.production_plan.lip_sync_mode === "auto" && e.shots.some(ri);
}
function Qo(e) {
  return e.shots.reduce(
    (t, n) => t + n.speech.filter(Pn).length,
    0
  );
}
function ei(e) {
  return e.shots.reduce(
    (t, n) => t + ti(n).length,
    0
  );
}
function ti(e) {
  const t = e.speech.filter(
    (r) => r.subtitle_enabled && !!r.text.trim()
  ).map((r) => ({
    id: `subtitle-${r.id}`,
    text: r.subtitle_text.trim() || r.text.trim(),
    start_time: r.start_time,
    speech_id: r.id,
    source: "speech"
  })), n = e.captions.filter((r) => !!r.text.trim()).map((r) => ({
    id: r.id,
    text: r.text.trim(),
    start_time: r.start_time,
    end_time: r.end_time,
    source: "caption"
  }));
  return [...t, ...n].sort(
    (r, o) => r.start_time - o.start_time
  );
}
function ni(e) {
  return e.kind === "narration" ? "旁白" : e.speaker_mode === "visible" ? "出镜对白" : "画外音";
}
function Dn(e) {
  return e.kind === "dialogue" && e.speaker_mode === "visible" && !!e.text.trim();
}
function ri(e) {
  return e.speech.some(Dn);
}
function Ga(e) {
  return new Set(
    e.speech.filter(Dn).map((t) => t.character_id?.trim()).filter((t) => !!t)
  );
}
function Za(e) {
  return `${e.title.trim() || "分镜脚本"} · ${e.shots.length} 个镜头`;
}
function Xa(e) {
  return e.summary.trim() || Rn("", e.shots);
}
function Qa(e, t) {
  const n = e.style_prompt.trim(), r = { ...e, style_prompt: t };
  return !n || n === t.trim() ? r : {
    ...r,
    materials: e.materials.map((o) => ({
      ...o,
      prompt: Kt(
        o.prompt,
        n
      )
    })),
    shots: e.shots.map((o) => ({
      ...o,
      video_prompt: Kt(
        o.video_prompt,
        n
      )
    }))
  };
}
function ec(e, t) {
  const n = e.visual_mode === "photoreal" ? "画面类型：写实影像，人物五官、身体比例、光线和材质保持真实自然" : "画面类型：非写实影像，保持统一造型语言，不得漂移为真人摄影";
  let r = Ut(t.trim(), n);
  const o = e.style_prompt.trim();
  if (!o)
    return r;
  const i = `统一视觉风格：${o}`;
  return r = Ut(r, i), r;
}
function Kt(e, t) {
  const n = `统一视觉风格：${t}`, r = e.trimEnd().replace(/[。！？!?；;，,\s]+$/g, "");
  return r.endsWith(n) ? r.slice(0, -n.length).replace(/[。！？!?；;，,：:\s]+$/g, "").trimEnd() : e;
}
function Ut(e, t) {
  if (!t || e.includes(t))
    return e;
  if (!e)
    return t;
  const n = /[。！？!?；;，,：:]$/.test(e) ? "" : "。";
  return `${e}${n}${t}`;
}
function On(e) {
  const t = D(e);
  return Yo.includes(t) ? t : Jo;
}
function Mn(e) {
  const t = D(e);
  return Ko.includes(t) ? t : "none";
}
function tc(e) {
  const t = e.speech.filter(Pn).map((r) => `${ni(r)}：${r.text.trim()}`).join("；");
  return [
    e.description,
    e.camera_instruction ? `镜头语言：${e.camera_instruction}` : "",
    e.continue_previous && e.continuity_anchor ? `连续性锚点：${e.continuity_anchor}` : "",
    t,
    e.duration > 0 ? `时长：${e.duration} 秒` : ""
  ].filter(Boolean).join("。") || `镜头 ${e.order} 视频生成提示词`;
}
function Oe(e, t, n) {
  if (e == null || n > 10)
    return null;
  if (typeof e == "string") {
    for (const a of To(e)) {
      const d = Oe(a, t, n + 1);
      if (d)
        return d;
    }
    return null;
  }
  if (typeof e != "object" || t.has(e))
    return null;
  if (t.add(e), Array.isArray(e)) {
    for (const a of e) {
      const d = Oe(a, t, n + 1);
      if (d)
        return d;
    }
    return null;
  }
  const r = e, o = oi(r);
  if (o)
    return o;
  const i = li(r);
  if (i) {
    const a = Oe(i, t, n + 1);
    if (a)
      return a;
  }
  for (const a of Go) {
    const d = r[a];
    if (d == null || d === e)
      continue;
    const u = Oe(d, t, n + 1);
    if (u)
      return u;
  }
  return null;
}
function oi(e) {
  const t = D(e.visual_mode).toLowerCase();
  if (D(e.type).toLowerCase() !== "storyboard" || ie(e.version) !== _t || typeof e.title != "string" || typeof e.narrator_voice != "string" || typeof e.style_prompt != "string" || !si(t) || !Array.isArray(e.references) || !Array.isArray(e.materials) || !Array.isArray(e.shots))
    return null;
  const n = ii(e.storyline);
  if (!n)
    return null;
  const r = ot(e.references);
  if (r.length !== e.references.length)
    return null;
  const o = e.materials.map(ai);
  if (o.some((h) => !h))
    return null;
  const i = o, a = /* @__PURE__ */ new Set();
  for (const h of i) {
    if (a.has(h.id))
      return null;
    a.add(h.id);
  }
  const d = /* @__PURE__ */ new Set(), u = e.shots.map(
    (h, w) => ci(h, w, a)
  );
  if (u.some((h) => !h))
    return null;
  const m = u;
  for (const h of m) {
    if (d.has(h.id))
      return null;
    d.add(h.id);
  }
  const g = ie(e.target_duration), p = ie(e.target_shot_count);
  if (g == null || !Number.isInteger(g) || g < it || p == null || !Number.isInteger(p) || p < 1 || p > In)
    return null;
  const _ = zn(e.workflow);
  return {
    ...e,
    type: "storyboard",
    version: _t,
    workflow: _,
    production_plan: Tn(
      e.production_plan,
      _.status === "confirmed"
    ),
    title: e.title,
    summary: Rn(
      D(e.summary),
      m
    ),
    target_duration: g,
    target_shot_count: p,
    narrator_voice: e.narrator_voice.trim(),
    storyline: n,
    style_prompt: e.style_prompt,
    visual_mode: t,
    aspect_ratio: On(e.aspect_ratio),
    references: r,
    materials: i,
    shots: m
  };
}
function ii(e) {
  if (!J(e))
    return null;
  const t = D(e.setup), n = D(e.development), r = D(e.payoff);
  return !t || !n || !r ? null : { setup: t, development: n, payoff: r };
}
function Rn(e, t) {
  const n = e.trim();
  if (n)
    return n;
  const r = t.map((o) => o.description.trim()).filter(Boolean);
  return r.length > 0 ? r.join("；") : "暂无内容简介";
}
function si(e) {
  return Uo.includes(e);
}
function ai(e) {
  if (!J(e))
    return null;
  const t = D(e.type).toLowerCase();
  return !fi(t) || typeof e.id != "string" || !e.id.trim() || typeof e.name != "string" || typeof e.prompt != "string" || typeof e.voice != "string" || !Array.isArray(e.reference_keys) ? null : {
    ...e,
    id: e.id.trim(),
    type: t,
    name: e.name.trim().replace(/^[@#]+/, ""),
    prompt: e.prompt.trim(),
    voice: t === "character" ? e.voice.trim() : "",
    reference_keys: ze(e.reference_keys.map(D))
  };
}
function ci(e, t, n) {
  if (!J(e) || typeof e.id != "string" || !e.id.trim() || typeof e.beat != "string" || !e.beat.trim() || typeof e.transition != "string" || typeof e.transition_type != "string" || typeof e.match_previous != "boolean" || typeof e.description != "string" || typeof e.camera_instruction != "string" || typeof e.video_prompt != "string" || typeof e.continue_previous != "boolean" || typeof e.continuity_anchor != "string" || !Array.isArray(e.material_ids) || !Array.isArray(e.reference_keys) || !Array.isArray(e.speech) || !Array.isArray(e.captions))
    return null;
  const r = ie(e.duration);
  if (r == null || !Zo(r))
    return null;
  const o = e.material_ids.map(D);
  if (o.some((h) => !h || !n.has(h)) || new Set(o).size !== o.length)
    return null;
  const i = e.speech.map(di);
  if (i.some((h) => !h))
    return null;
  const a = e.captions.map(ui);
  if (a.some(
    (h) => !h || h.end_time > r
  ))
    return null;
  const d = t > 0 && e.continue_previous;
  if (t === 0 && (e.match_previous || e.continue_previous) || e.match_previous && e.continue_previous)
    return null;
  const u = t > 0 && !d && e.match_previous, m = e.transition.trim();
  if (t > 0 && !m)
    return null;
  const g = e.continuity_anchor.trim();
  if (d && !g)
    return null;
  const p = Mn(
    e.transition_type
  ), _ = ie(e.transition_duration_ms);
  return p !== e.transition_type || _ == null || !Number.isInteger(_) || _ < 0 || _ > 5e3 || t === 0 && (p !== "none" || _ !== 0) || t > 0 && p === "none" && _ !== 0 || t > 0 && p !== "none" && _ < 100 ? null : {
    ...e,
    id: e.id.trim(),
    order: t + 1,
    duration: r,
    beat: e.beat.trim(),
    transition: t > 0 ? m : "",
    transition_type: t > 0 ? p : "none",
    transition_duration_ms: t > 0 && p !== "none" ? _ : 0,
    description: e.description,
    camera_instruction: e.camera_instruction,
    video_prompt: e.video_prompt,
    material_ids: o,
    reference_keys: ze(e.reference_keys.map(D)),
    match_previous: u,
    continue_previous: d,
    continuity_anchor: d ? g : "",
    speech: i,
    captions: a
  };
}
function di(e) {
  if (!J(e) || typeof e.id != "string" || !e.id.trim() || typeof e.text != "string" || typeof e.subtitle_enabled != "boolean" || typeof e.subtitle_text != "string")
    return null;
  const t = D(e.kind).toLowerCase(), n = ie(e.start_time);
  if (t !== "dialogue" && t !== "narration" || n == null || n < 0)
    return null;
  if (t === "narration") {
    const o = {
      ...e,
      id: e.id.trim(),
      kind: t,
      text: e.text,
      start_time: n,
      subtitle_enabled: e.subtitle_enabled,
      subtitle_text: e.subtitle_text
    };
    return delete o.character_id, delete o.speaker_mode, o;
  }
  const r = D(e.speaker_mode).toLowerCase();
  return typeof e.character_id != "string" || r !== "visible" && r !== "offscreen" ? null : {
    ...e,
    id: e.id.trim(),
    kind: t,
    text: e.text,
    start_time: n,
    character_id: e.character_id.trim(),
    speaker_mode: r,
    subtitle_enabled: e.subtitle_enabled,
    subtitle_text: e.subtitle_text
  };
}
function ui(e) {
  if (!J(e) || typeof e.id != "string" || !e.id.trim() || typeof e.text != "string")
    return null;
  const t = D(e.type).toLowerCase(), n = ie(e.start_time), r = ie(e.end_time);
  return !mi(t) || n == null || r == null || n < 0 || r <= n ? null : {
    ...e,
    id: e.id.trim(),
    type: t,
    text: e.text,
    start_time: n,
    end_time: r
  };
}
function zn(e) {
  const t = J(e) ? e : {}, n = D(t.status).toLowerCase() === "confirmed" ? "confirmed" : "draft";
  return {
    status: n,
    confirmed_at: n === "confirmed" ? D(t.confirmed_at) : ""
  };
}
function ut(e, t) {
  const n = D(e).toLowerCase();
  return n === "auto" || n === "off" ? n : t;
}
function li(e) {
  const t = ko(e);
  if (t)
    return t;
  if (!J(e))
    return "";
  const n = e.type === "doc" ? e : J(e.rich) && e.rich.type === "doc" ? e.rich : null;
  return n ? En(n).trim() : "";
}
function En(e) {
  if (!J(e))
    return "";
  if (e.type === "text")
    return D(e.text);
  if (e.type === "hardBreak")
    return `
`;
  if (!Array.isArray(e.content))
    return "";
  const t = e.type === "doc" || e.type === "paragraph" || e.type === "codeBlock" ? `
` : "";
  return e.content.map(En).join(t);
}
function fi(e) {
  return e === "character" || e === "scene" || e === "prop";
}
function mi(e) {
  return e === "caption" || e === "title" || e === "highlight";
}
function Pn(e) {
  return e.text.trim().length > 0;
}
function ze(e) {
  return [...new Set(e.map((t) => t.trim()).filter(Boolean))];
}
function ie(e) {
  const t = typeof e == "number" ? e : Number.NaN;
  return Number.isFinite(t) ? t : null;
}
function D(e) {
  return typeof e == "string" ? e.trim() : "";
}
function J(e) {
  return !!e && typeof e == "object" && !Array.isArray(e);
}
const pi = Mr(
  () => import("./space-storyboard-view-AfR5eZqu.js").then((e) => e.i).then((e) => ({
    default: e.StoryboardView
  }))
), xt = [
  "image",
  "video",
  "audio"
], yi = {
  image: ["image", "image_url", "imageUrl", "images", "imageUrls"],
  video: ["video", "video_url", "videoUrl", "videos", "videoUrls"],
  audio: ["audio", "audio_url", "audioUrl", "audios", "audioUrls"]
}, gt = sn(
  "@/components/energon/content-view"
), Yt = gt.ContentView || gt.EnergonContentView, hi = gt.normalizeEnergonOutput;
function $n({
  output: e,
  fallback: t = "",
  streaming: n = !1,
  emptyText: r = "暂无内容",
  className: o,
  markdownClassName: i,
  richClassName: a,
  mediaLayout: d = "default",
  storyboardEditable: u = !1,
  storyboardDisabled: m = !1,
  onStoryboardSave: g
}) {
  const p = ge(e) ? e : t ? { text: t } : e, _ = Cn(p);
  return _ ? /* @__PURE__ */ s(
    "div",
    {
      className: o,
      onPointerDown: (h) => {
        ve(h.target) && h.stopPropagation();
      },
      onClick: (h) => {
        ve(h.target) && h.stopPropagation();
      },
      children: /* @__PURE__ */ s(
        Rr,
        {
          fallback: /* @__PURE__ */ s("div", { className: "min-h-24", "aria-busy": "true" }),
          children: /* @__PURE__ */ s(
            pi,
            {
              storyboard: _,
              editable: u,
              disabled: m,
              onSave: g
            }
          )
        }
      )
    }
  ) : Yt ? /* @__PURE__ */ s(
    "div",
    {
      className: o,
      onPointerDown: (h) => {
        ve(h.target) && h.stopPropagation();
      },
      onClick: (h) => {
        ve(h.target) && h.stopPropagation();
      },
      children: /* @__PURE__ */ s(
        Yt,
        {
          output: p,
          streaming: n,
          emptyText: r,
          markdownClassName: i,
          richClassName: a,
          mediaLayout: d
        }
      )
    }
  ) : t ? /* @__PURE__ */ s("div", { className: o, children: /* @__PURE__ */ s("p", { children: t }) }) : null;
}
function nc(e, t) {
  if (_i(e, t))
    return !1;
  const n = Bn(e);
  return n.length > 1 || Ln(e) > 1 ? !0 : n.some((r) => !r || typeof r != "object" || Array.isArray(r) ? ge(r) : [
    r.title,
    r.text,
    r.reasoning,
    r.rich,
    r.progress,
    r.error,
    r.json
  ].some(ge));
}
function _i(e, t) {
  const n = bt(e, /* @__PURE__ */ new Set(), 0);
  return !n || !t ? !1 : [
    t.imageUrl,
    t.videoUrl,
    t.audioUrl,
    t.fileUrl
  ].some((r) => String(r || "").trim() === n);
}
function bt(e, t, n) {
  if (e == null || n > 12)
    return "";
  if (typeof e == "string")
    return e.trim();
  if (Array.isArray(e))
    return e.length === 1 ? bt(e[0], t, n + 1) : "";
  if (typeof e != "object" || t.has(e))
    return "";
  t.add(e);
  const o = Object.entries(e).filter(
    ([i, a]) => !["type", "kind", "format", "version"].includes(i) && ge(a)
  ).map(([, i]) => i);
  return o.length === 1 ? bt(o[0], t, n + 1) : "";
}
function rc(e) {
  return gi(e).length > 0;
}
function gi(e) {
  const t = Dt(e);
  return xt.filter((n) => t[n].size > 0);
}
function Ln(e) {
  const t = Dt(e);
  return xt.reduce(
    (n, r) => n + t[r].size,
    0
  );
}
function oc(e, t) {
  return Array.from(Dt(e)[t]);
}
function ic(...e) {
  let t, n, r = 0;
  for (const o of e) {
    if (!ge(o))
      continue;
    t === void 0 && (t = o);
    const i = Ln(o);
    i > r && (n = o, r = i);
  }
  return r > 0 ? n : t;
}
function Dt(e) {
  const t = {
    image: /* @__PURE__ */ new Set(),
    video: /* @__PURE__ */ new Set(),
    audio: /* @__PURE__ */ new Set()
  }, n = /* @__PURE__ */ new Set();
  for (const r of Bn(e))
    me(r, t, n, 0);
  return t;
}
function Bn(e) {
  const t = hi?.(e);
  return Array.isArray(t) ? t : Array.isArray(e) ? e : [e];
}
function me(e, t, n, r, o) {
  if (e == null || r > 12)
    return;
  if (Array.isArray(e)) {
    e.forEach(
      (u) => me(u, t, n, r + 1, o)
    );
    return;
  }
  if (typeof e == "string") {
    const u = o || ki(e);
    if (u) {
      const m = e.trim();
      m && t[u].add(m);
    }
    return;
  }
  if (typeof e != "object" || n.has(e))
    return;
  n.add(e);
  const i = e;
  if (o)
    for (const u of [
      i.url,
      i.src,
      i.thumbnail,
      i.download_url,
      i.downloadUrl
    ])
      me(u, t, n, r + 1, o);
  for (const u of xt)
    for (const m of bi(i, u))
      me(m, t, n, r + 1, u);
  const a = wi(i.type), d = i.attrs && typeof i.attrs == "object" ? i.attrs : void 0;
  if (a && [i.url, i.src, d?.src, d?.url].some(ge))
    for (const u of [i.url, i.src, d?.src, d?.url])
      me(u, t, n, r + 1, a);
  for (const u of [
    i.rich,
    i.content,
    i.output,
    i.result,
    i.data,
    i.body,
    i.value,
    i.json,
    i.media_files,
    i.mediaFiles
  ])
    me(u, t, n, r + 1);
}
function bi(e, t) {
  return yi[t].map((n) => e[n]);
}
function wi(e) {
  const t = String(e || "").trim().toLowerCase().replace(/[\s_-]+/g, "");
  if (["image", "mediaimage", "editormediaimage"].includes(t))
    return "image";
  if (["video", "mediavideo", "editormediavideo"].includes(t))
    return "video";
  if (["audio", "music", "voice", "mediaaudio", "editormediaaudio"].includes(
    t
  ))
    return "audio";
}
function ki(e) {
  const t = e.trim();
  if (/\.(png|jpe?g|gif|webp|avif|svg)(?:[?#].*)?$/i.test(t))
    return "image";
  if (/\.(mp4|webm|mov|m4v)(?:[?#].*)?$/i.test(t))
    return "video";
  if (/\.(mp3|wav|ogg|m4a|aac)(?:[?#].*)?$/i.test(t))
    return "audio";
}
function ge(e) {
  return e == null || e === "" ? !1 : Array.isArray(e) ? e.length > 0 : typeof e == "object" ? Object.keys(e).length > 0 : !0;
}
function ve(e) {
  return e instanceof Element && !!e.closest(
    "a, button, input, textarea, select, audio, video, [role='button']"
  );
}
const Si = $r.EnergonAudioPlayer;
function Jt({
  src: e,
  prompt: t = "",
  detailed: n = !1,
  autoPlay: r = !1
}) {
  const o = /* @__PURE__ */ s(
    "div",
    {
      className: [
        "wb-asset-audio-preview",
        n ? "is-detail" : ""
      ].filter(Boolean).join(" "),
      children: /* @__PURE__ */ s(
        Si,
        {
          src: e,
          detailed: n,
          autoPlay: r,
          className: "h-full min-h-0 border-0 bg-transparent p-0 shadow-none"
        }
      )
    }
  );
  return n ? /* @__PURE__ */ f("div", { className: "wb-asset-audio-detail", children: [
    o,
    t ? /* @__PURE__ */ f("section", { className: "wb-asset-audio-prompt", children: [
      /* @__PURE__ */ f("header", { children: [
        /* @__PURE__ */ s(et, { "aria-hidden": "true" }),
        /* @__PURE__ */ s("strong", { children: "语音文本" })
      ] }),
      /* @__PURE__ */ s("p", { children: t })
    ] }) : null
  ] }) : o;
}
const Ni = { width: 720, height: 420 }, sc = { width: 360, height: 240 }, ac = { width: 2400, height: 1600 }, Ai = 48, lt = 16;
function cc(e, t) {
  return e.filter((n) => n.groupId === t);
}
function dc(e, t) {
  return !(!e || !t || e.id === t.id || e.type === "group" && t.groupId === e.id || t.type === "group" && e.groupId === t.id || e.groupId !== t.groupId && (e.type !== "group" && e.groupId || t.type !== "group" && t.groupId));
}
function uc(e, t, n) {
  const r = e.find((a) => a.id === t);
  if (!r || r.x === n.x && r.y === n.y)
    return e;
  const o = n.x - r.x, i = n.y - r.y;
  return e.map((a) => a.id === t ? { ...a, ...n } : r.type === "group" && a.groupId === r.id ? {
    ...a,
    x: a.x + o,
    y: a.y + i
  } : a);
}
function lc(e, t, n) {
  const r = e.find((a) => a.id === t);
  if (!r || r.type === "group")
    return e;
  const o = { ...r, ...n }, i = Ii(e, o);
  return (r.groupId || "") === i ? e : e.map(
    (a) => a.id === t ? { ...a, groupId: i || void 0 } : a
  );
}
function fc(e, t) {
  const n = new Map(e.map((i) => [i.id, i])), r = /* @__PURE__ */ new Set(), o = [];
  for (const i of t) {
    const a = i.logicalFrom || i.from, d = i.logicalTo || i.to, u = n.get(a), m = n.get(d);
    if (!u || !m)
      continue;
    const g = u.groupId !== m.groupId, p = g && u.type !== "group" && u.groupId ? u.groupId : u.id, _ = g && m.type !== "group" && m.groupId ? m.groupId : m.id;
    if (!p || !_ || p === _)
      continue;
    const h = `${a}\0${d}`;
    r.has(h) || (r.add(h), o.push({ ...i, from: p, to: _, logicalFrom: a, logicalTo: d }));
  }
  return o;
}
function Ii(e, t) {
  const n = t.x + t.width / 2, r = t.y + t.height / 2;
  return e.filter(
    (i) => i.type === "group" && i.id !== t.id && n >= i.x + lt && n <= i.x + i.width - lt && r >= i.y + Ai && r <= i.y + i.height - lt
  ).sort(
    (i, a) => i.width * i.height - a.width * a.height
  )[0]?.id || "";
}
const Ci = {
  general: { name: "通用", viewMode: "content" },
  storyboard: { name: "分镜脚本", viewMode: "storyboard" },
  speech: { name: "语音合成", viewMode: "content" },
  lip_sync: { name: "口型同步", viewMode: "content" },
  video_compose: { name: "视频合成", viewMode: "video_compose" }
}, Ti = {
  text: "文本",
  llm: "文本",
  image: "图片",
  audio: "音频",
  music: "音频",
  video: "视频",
  file: "文件",
  mixed: "图文",
  role: "角色",
  multi: "多模态",
  embeddings: "向量",
  workflow: "工作流"
};
function st(e, t = "", n = "") {
  const r = ye(e?.kind || t) || "text", o = ye(n || e?.outputType) || "general", i = Ci[o], a = ye(e?.output?.key) === o ? e?.output : void 0;
  return {
    outputType: o,
    outputName: r === "text" || r === "llm" || o !== "general" ? String(a?.name || "").trim() || i?.name || o : "",
    kindName: xi(r),
    viewMode: ye(a?.viewMode) || i?.viewMode || "content"
  };
}
function jn(e, t = "", n = "") {
  return st(e, t, n).viewMode === "storyboard";
}
function mc(e, t = "", n = "") {
  return st(e, t, n).viewMode === "video_compose";
}
function Vn(e, t = "") {
  const n = ye(e?.kind || t);
  return n === "audio" || n === "music";
}
function xi(e) {
  const t = ye(e) || "text";
  return Ti[t] || "文本";
}
function ye(e) {
  return String(e || "").trim().toLowerCase();
}
const Di = [
  {
    name: "基础",
    options: [
      { key: "none", name: "无转场" },
      { key: "fade", name: "淡化" },
      { key: "crossfade", name: "交叉溶解" },
      { key: "fadeblack", name: "黑场淡化" },
      { key: "fadewhite", name: "白场淡化" }
    ]
  },
  {
    name: "擦除",
    options: [
      { key: "wipeleft", name: "向左擦除" },
      { key: "wiperight", name: "向右擦除" },
      { key: "wipeup", name: "向上擦除" },
      { key: "wipedown", name: "向下擦除" }
    ]
  },
  {
    name: "滑动",
    options: [
      { key: "slideleft", name: "向左滑动" },
      { key: "slideright", name: "向右滑动" },
      { key: "slideup", name: "向上滑动" },
      { key: "slidedown", name: "向下滑动" }
    ]
  },
  {
    name: "平滑",
    options: [
      { key: "smoothleft", name: "向左平滑" },
      { key: "smoothright", name: "向右平滑" },
      { key: "smoothup", name: "向上平滑" },
      { key: "smoothdown", name: "向下平滑" }
    ]
  },
  {
    name: "镜头",
    options: [
      { key: "zoomin", name: "放大切换" },
      { key: "circleopen", name: "圆形展开" },
      { key: "circleclose", name: "圆形收拢" }
    ]
  },
  {
    name: "覆盖",
    options: [
      { key: "coverleft", name: "向左覆盖" },
      { key: "coverright", name: "向右覆盖" },
      { key: "coverup", name: "向上覆盖" },
      { key: "coverdown", name: "向下覆盖" }
    ]
  },
  {
    name: "揭示",
    options: [
      { key: "revealleft", name: "向左揭示" },
      { key: "revealright", name: "向右揭示" },
      { key: "revealup", name: "向上揭示" },
      { key: "revealdown", name: "向下揭示" }
    ]
  }
];
function pc() {
  return {
    version: 3,
    clips: [],
    settings: {
      resolution: "auto",
      fps: 0
    }
  };
}
function Oi(e) {
  const t = se(e);
  if (Number(t.version || 0) !== 3)
    return;
  const n = Array.isArray(t.clips) ? t.clips.map(Mi).filter(Boolean) : [], r = se(t.settings);
  return {
    version: 3,
    clips: n,
    settings: {
      resolution: $(r.resolution) || "auto",
      fps: Pe(r.fps, 0, 120, 0)
    }
  };
}
function yc(e) {
  const t = e.clips.reduce(
    (r, o) => r + Math.max(0, o.duration),
    0
  ), n = e.clips.reduce((r, o, i) => i >= e.clips.length - 1 || o.transitionToNext.type === "none" ? r : r + o.transitionToNext.durationMs / 1e3, 0);
  return Math.max(0, t - n);
}
function hc(e) {
  return e.clips.flatMap(
    (t, n) => t.blockingIssues.map(
      (r) => `${t.title || `镜头 ${n + 1}`}：${r}`
    )
  );
}
function _c(e) {
  return e ? `${e.assetId}:${e.versionId}` : "";
}
function Mi(e) {
  const t = se(e), n = $(t.id);
  if (!n)
    return null;
  const r = wt(
    t.visualVideo ?? t.visual_video
  ), o = wt(
    t.originalAudioSource ?? t.original_audio_source
  ), i = se(
    t.transitionToNext ?? t.transition_to_next
  ), a = se(
    t.storyboardTransitionToNext ?? t.storyboard_transition_to_next
  ), d = Array.isArray(t.speechTracks ?? t.speech_tracks) ? (t.speechTracks ?? t.speech_tracks).map(Ei).filter(Boolean) : [], u = Array.isArray(
    t.subtitleTracks ?? t.subtitle_tracks
  ) ? (t.subtitleTracks ?? t.subtitle_tracks).map(zi).filter(Boolean) : [], m = Ri(
    a
  ), g = Fn(i.type);
  return {
    id: n,
    title: $(t.title) || r?.label || "镜头",
    ...r ? { visualVideo: r } : {},
    ...o ? { originalAudioSource: o } : {},
    duration: Math.max(0, be(t.duration)),
    originalVolume: Pe(
      t.originalVolume ?? t.original_volume,
      0,
      1,
      1
    ),
    speechTracks: d,
    subtitleTracks: u,
    useOriginalVideo: $i(
      t.useOriginalVideo ?? t.use_original_video
    ),
    blockingIssues: Pi(
      t.blockingIssues ?? t.blocking_issues
    ),
    transitionToNext: {
      type: g,
      durationMs: g === "none" ? 0 : Pe(
        i.durationMs ?? i.duration_ms,
        100,
        5e3,
        500
      )
    },
    ...m ? { storyboardTransitionToNext: m } : {}
  };
}
function Ri(e) {
  if (!Object.keys(e).length)
    return;
  const t = Fn(e.type);
  return {
    type: t,
    durationMs: t === "none" ? 0 : Pe(e.durationMs ?? e.duration_ms, 100, 5e3, 500)
  };
}
function zi(e) {
  const t = se(e), n = $(t.id), r = $(t.text);
  if (!n || !r)
    return null;
  const o = $(t.source) === "speech" ? "speech" : "caption", i = $(t.speechId ?? t.speech_id), a = be(t.endTime ?? t.end_time);
  return {
    id: n,
    text: r,
    startTime: Math.max(0, be(t.startTime ?? t.start_time)),
    ...a > 0 ? { endTime: a } : {},
    ...i ? { speechId: i } : {},
    source: o
  };
}
function Ei(e) {
  const t = se(e), n = $(t.id);
  if (!n)
    return null;
  const r = wt(t.audio), o = $(t.kind) === "narration" ? "narration" : "dialogue", i = $(t.characterId ?? t.character_id);
  return {
    id: n,
    ...r ? { audio: r } : {},
    startTime: Math.max(0, be(t.startTime ?? t.start_time)),
    kind: o,
    ...i ? { characterId: i } : {},
    text: $(t.text),
    volume: Pe(t.volume, 0, 1, 1)
  };
}
function wt(e) {
  const t = se(e), n = be(t.assetId ?? t.asset_id), r = be(t.versionId ?? t.version_id);
  if (!(!n || !r))
    return {
      assetId: n,
      versionId: r,
      label: $(t.label)
    };
}
function Fn(e) {
  const t = $(e);
  return Di.some(
    (n) => n.options.some((r) => r.key === t)
  ) ? t : "none";
}
function se(e) {
  return e && typeof e == "object" && !Array.isArray(e) ? e : {};
}
function $(e) {
  return String(e ?? "").trim();
}
function Pi(e) {
  return Array.isArray(e) ? e.map($).filter(Boolean) : [];
}
function be(e) {
  const t = Number(e || 0);
  return Number.isFinite(t) ? t : 0;
}
function Pe(e, t, n, r) {
  const o = Number(e);
  return Number.isFinite(o) ? Math.min(n, Math.max(t, o)) : r;
}
function $i(e) {
  return e === !0 || e === 1 || e === "1" || e === "true";
}
const kt = {
  id: 0,
  team_id: 0,
  name: "自由",
  kind: "richtext",
  cardinality: "multiple",
  status: 1,
  sort: 0,
  virtual: !0
}, He = { width: 180, height: 180 }, vn = { width: 240, height: 160 }, Li = [
  { width: 360, height: 64 },
  { width: 280, height: 210 }
], Kn = { width: 620, height: 360 };
function gc(e) {
  const t = k(e), n = k(t.canvas_config), r = P(y(t.powers, n.powers)).map(
    Ot
  ), o = Hn(
    rs(y(t.canvases, t.canvas)),
    r
  );
  return {
    project: qi(t.project),
    team: Hi(t.team || t.type),
    release: Gi(t.release),
    assetCates: P(
      y(t.asset_cates, k(t.team).asset_cates)
    ).map(Zi),
    roles: P(
      y(t.roles, k(t.team).roles, n.roles)
    ).map(Xi),
    flows: P(y(t.flows, k(t.team).flows)).map(
      Qi
    ),
    nodesByFlow: ns(
      y(t.nodes_by_flow, k(t.team).nodes_by_flow)
    ),
    canvases: o,
    assets: P(y(k(t.assets).items, t.assets)).map(
      Wn
    ),
    powers: r,
    powerCategories: P(
      y(t.power_cates, n.power_cates)
    ).map(nn),
    powerKinds: P(
      y(t.power_kinds, n.power_kinds)
    ).map(Jn),
    outputTypes: P(
      y(t.output_types, n.output_types)
    ).map(Mt),
    initialAssetCateId: l(t.active_asset_cate_id)
  };
}
function bc(e) {
  return {
    assetCateId: e,
    nextNodeNo: 1,
    nodes: [],
    edges: [],
    viewport: {}
  };
}
function Bi(e, t = 0) {
  const n = k(e), r = l(
    y(n.asset_cate_id, t)
  );
  return ji({
    assetCateId: r,
    nextNodeNo: Math.max(1, l(n.next_node_no)),
    nodes: P(n.nodes).map(os).filter((o) => !!o),
    edges: P(n.edges).map(_s).filter((o) => !!o),
    viewport: gs(n.viewport),
    updatedAt: c(n.updated_at)
  });
}
function ji(e) {
  let t = Vi(e.nodes, e.nextNodeNo), n = t !== e.nextNodeNo;
  const r = e.nodes.map((o) => {
    if (!Fi(o) || Number(o.nodeNo || 0) > 0)
      return o;
    const i = t++, a = o.titleMode || "manual";
    return n = !0, {
      ...o,
      nodeNo: i,
      titleMode: a,
      ...a === "auto" && !o.storyboardItem ? { title: vi(o, i) } : {}
    };
  });
  return n ? { ...e, nextNodeNo: t, nodes: r } : e;
}
function Vi(e, t = 1) {
  return Math.max(
    1,
    Number(t || 1),
    ...e.map((n) => Number(n.nodeNo || 0) + 1)
  );
}
function Fi(e) {
  return e.type === "power" || e.type === "agent" || e.type === "flow";
}
function vi(e, t) {
  let n = "节点";
  if (e.type === "power") {
    const r = st(
      e.power,
      e.kind,
      e.outputType
    );
    n = r.outputType !== "general" && r.outputName || r.kindName;
  } else e.type === "agent" ? n = String(e.role?.name || "智能体").trim() || "智能体" : e.type === "flow" && (n = String(e.flow?.name || "流程").trim() || "流程");
  return `${n}-${t}`;
}
function wc(e) {
  const t = k(e);
  return {
    powers: P(t.powers).map(Ot),
    powerCategories: P(t.power_cates).map(nn),
    powerKinds: P(t.power_kinds).map(Jn),
    outputTypes: P(t.output_types).map(Mt)
  };
}
function kc(e) {
  return Wn(k(e));
}
function Ki(e) {
  return e.assetCates.length > 0 ? e.assetCates : [kt];
}
function Sc(e) {
  return Ki(e)[0]?.id ?? 0;
}
function Ui(e, t) {
  const n = e.length > 0 ? e : [kt];
  return n.find((r) => r.id === t) || n[0] || kt;
}
function Nc(e, t) {
  return Ui(e.assetCates, t);
}
function Ac(e, t) {
  return t === 0 ? e.flows.slice(0, 4) : e.flows.filter((n) => bs(e, n).has(t)).slice(0, 4);
}
function Ic(e) {
  return e.create_status !== 2;
}
function Cc(e) {
  return e.createStatus !== 2;
}
function Tc(e, t, n, r, o) {
  const i = r?.x ?? 420 + n % 3 * 190, a = r?.y ?? 610 + Math.floor(n / 3) * 170, d = o?.asset, u = o?.flow, m = o?.functionOption, g = o?.power, p = o?.role, _ = g ? st(g) : null, h = Number(d?.asset_cate_id || t.id), w = {
    asset: [
      d?.name || "资产引用",
      _e(d ? d.kind : t.kind),
      d && Yi(d.version?.content) || "引用已有资产，作为其他节点的上下文。"
    ],
    power: [
      g?.name || ws(t.kind),
      _?.outputName || _?.kindName || "能力节点",
      g ? `调用 ${g.name} 能力，按参数生成内容。` : "输入提示词和参数，直接生成文本、图片、视频或音频。"
    ],
    agent: [
      p?.name || "智能体节点",
      p?.role_type || "角色执行",
      p?.assignment || "调用团队角色或指定智能体完成一段任务。"
    ],
    flow: [
      u?.name || "流程节点",
      "团队流程",
      u?.goal || "执行一组团队预设流程。"
    ],
    function: [
      m?.label || "保存节点",
      "功能",
      m?.description || "开始、导入、保存、展示等功能节点。"
    ],
    group: [
      "未命名分组",
      "分组",
      "拖入节点后，可统一接收上下文并执行组内节点。"
    ]
  }, [O, A, x] = w[e], K = Zn(e, g);
  return {
    id: `local-${e}-${Date.now()}-${n}`,
    type: e,
    title: O,
    titleMode: e === "power" || e === "agent" || e === "flow" ? "auto" : void 0,
    subtitle: A,
    description: x,
    x: i,
    y: a,
    width: K.width,
    height: K.height,
    assetCateId: h,
    kind: d?.kind || g?.kind || t.kind,
    outputType: g?.outputType,
    cardinality: t.cardinality,
    asset: d,
    flow: u,
    functionOption: m,
    power: g,
    role: p,
    group: e === "group" ? { origin: "manual" } : void 0,
    local: !0
  };
}
function Yi(e) {
  const t = Ji(e);
  return t ? t.length > 120 ? `${t.slice(0, 120)}...` : t : "";
}
function Ji(e) {
  return We(e).replace(/\s+/g, " ").trim();
}
function Wi(e) {
  if (typeof e != "string")
    return "";
  const t = e.trim();
  if (!xs(t))
    return "";
  const n = t.search(/"rich"\s*:/), r = n >= 0 ? t.slice(n) : t, o = [], i = /"text"\s*:\s*"((?:\\.|[^"\\])*)"/g;
  let a = null;
  for (; (a = i.exec(r)) !== null; ) {
    const d = Ds(a[1]).trim();
    d && o.push(d);
  }
  return o.join(" ").replace(/\s+/g, " ").trim();
}
function Un(e) {
  const t = Me(e, /* @__PURE__ */ new Set());
  return Rt(t) ? t : null;
}
function qi(e) {
  const t = k(e), n = k(t.team);
  return {
    id: l(t.id),
    body_id: l(t.body_id),
    team_id: l(t.team_id),
    release_id: l(t.release_id),
    name: c(t.name) || "未命名作品",
    description: c(t.description),
    mode: c(t.mode) || "team",
    team: {
      id: l(n.id),
      name: c(n.name),
      version: l(n.version)
    }
  };
}
function Hi(e) {
  const t = k(e), n = k(t.team), r = l(n.id) > 0 || c(n.name) ? n : t;
  return {
    id: l(r.id),
    name: c(r.name) || "自由团队",
    description: c(r.description)
  };
}
function Gi(e) {
  const t = k(e);
  return {
    id: l(t.id),
    team_id: l(t.team_id),
    version: l(t.version),
    status: c(t.status)
  };
}
function Zi(e) {
  return {
    id: l(e.id),
    team_id: l(e.team_id),
    name: c(e.name) || "未命名资产",
    kind: c(e.kind) || "text",
    cardinality: c(e.cardinality) || "single",
    status: l(e.status),
    sort: l(e.sort)
  };
}
function Xi(e) {
  return {
    id: l(e.id),
    team_id: l(e.team_id),
    role_type: c(e.role_type),
    role_key: c(e.role_key),
    name: c(e.name),
    agent_id: l(e.agent_id),
    assignment: c(e.assignment),
    create_status: Yn(e.create_status)
  };
}
function Qi(e) {
  return {
    id: l(e.id),
    name: c(e.name),
    key: c(e.key),
    goal: c(e.goal),
    config: k(e.config),
    status: l(e.status),
    sort: l(e.sort)
  };
}
function es(e) {
  return {
    id: l(e.id),
    node_key: c(e.node_key),
    name: c(e.name),
    type: c(e.type),
    role_id: l(e.role_id),
    role_key: c(e.role_key),
    agent_id: l(e.agent_id),
    power_id: l(e.power_id),
    sub_team_id: l(e.sub_team_id),
    asset_cate_id: l(e.asset_cate_id),
    config: k(e.config)
  };
}
function Ot(e) {
  const t = c(e.kind) || "text", n = Mt(k(e.output)), r = c(e.output_type) || "general";
  return {
    id: l(e.id),
    cate_id: l(e.cate_id),
    name: c(e.name) || c(e.key) || "未命名能力",
    key: c(e.key),
    icon: c(e.icon),
    outputType: r,
    output: n.key ? n : void 0,
    kind: t,
    createStatus: Yn(e.create_status)
  };
}
function Yn(e) {
  return Number(e) === 2 ? 2 : 1;
}
function Mt(e) {
  return {
    key: c(e.key || e.id),
    name: c(e.name || e.value),
    allowedKinds: Je(e.allowed_kinds || e.allowedKinds),
    viewMode: c(e.view_mode || e.viewMode),
    defaultWidth: l(e.default_width || e.defaultWidth),
    defaultHeight: l(e.default_height || e.defaultHeight),
    structured: !!e.structured,
    sort: l(e.sort)
  };
}
function Jn(e) {
  return {
    id: c(e.id),
    value: c(e.value) || c(e.name) || c(e.id)
  };
}
function Wn(e) {
  const t = qn(k(e.version)), n = ts(e.versions);
  return {
    id: l(e.id),
    project_id: l(y(e.project_id, e.projectID)),
    body_id: l(y(e.body_id, e.bodyID)),
    team_id: l(y(e.team_id, e.teamID)),
    flow_id: l(y(e.flow_id, e.flowID)),
    asset_cate_id: l(
      y(e.asset_cate_id, e.assetCateID)
    ),
    node_key: c(y(e.node_key, e.nodeKey)),
    name: c(e.name),
    kind: c(e.kind) || "text",
    role: c(e.role),
    version_id: l(y(e.version_id, e.versionID)),
    status: c(e.status),
    sort: l(e.sort),
    created_at: c(y(e.created_at, e.createdAt)),
    version: t,
    versions: n.length ? n : void 0
  };
}
function qn(e) {
  const t = l(e.id);
  if (!(!t && e.content == null))
    return {
      id: t,
      asset_id: l(y(e.asset_id, e.assetID)),
      run_id: l(y(e.run_id, e.runID)),
      node_run_id: l(y(e.node_run_id, e.nodeRunID)),
      release_id: l(y(e.release_id, e.releaseID)),
      request_id: c(y(e.request_id, e.requestID)),
      node_key: c(y(e.node_key, e.nodeKey)),
      source: k(e.source),
      version: l(e.version),
      summary: c(e.summary),
      content: e.content,
      created_at: c(y(e.created_at, e.createdAt)),
      updated_at: c(y(e.updated_at, e.updatedAt))
    };
}
function ts(e) {
  return P(e).map(qn).filter((t) => !!t);
}
function ns(e) {
  const t = k(e), n = {};
  for (const [r, o] of Object.entries(t))
    n[r] = P(o).map(es);
  return n;
}
function rs(e) {
  const t = k(e), n = {};
  for (const [r, o] of Object.entries(t)) {
    const i = Bi(o, l(r));
    n[String(i.assetCateId)] = i;
  }
  return n;
}
function Hn(e, t) {
  const n = new Map(
    t.filter((o) => o.id > 0).map((o) => [o.id, o])
  ), r = new Map(
    t.filter((o) => o.key).map((o) => [o.key, o])
  );
  for (const o of Object.values(e))
    o.nodes = o.nodes.map((i) => {
      if (i.type !== "power" || !i.power)
        return i;
      const a = n.get(Number(i.power.id || 0)) || r.get(i.power.key);
      if (!a)
        return i;
      const d = i.outputType || i.power.outputType || a.outputType;
      return Gn({
        ...i,
        outputType: d,
        power: {
          ...a,
          outputType: d,
          output: a.outputType === d ? a.output : i.power.output
        }
      });
    });
  return e;
}
function xc(e, t) {
  return Hn(
    { [String(e.assetCateId)]: e },
    t
  )[String(e.assetCateId)];
}
function os(e) {
  const t = c(e.id), n = c(e.type);
  if (!t || !n)
    return null;
  const r = {
    id: t,
    nodeNo: l(y(e.node_no, e.nodeNo)) || void 0,
    type: n,
    title: c(e.title),
    titleMode: c(y(e.title_mode, e.titleMode)) === "manual" ? "manual" : c(y(e.title_mode, e.titleMode)) === "auto" ? "auto" : void 0,
    subtitle: c(e.subtitle),
    description: c(e.description),
    x: l(e.x),
    y: l(e.y),
    width: l(e.width),
    height: l(e.height),
    groupId: c(y(e.group_id, e.groupId)),
    group: as(e.group),
    storyboardItem: cs(
      y(
        e.storyboard_item,
        e.storyboardItem,
        e.storyboard_material,
        e.storyboardMaterial
      )
    ),
    storyboardMaterializedSignature: c(
      y(
        e.storyboard_materialized_signature,
        e.storyboardMaterializedSignature
      )
    ),
    assetCateId: l(e.asset_cate_id),
    outputType: c(e.output_type),
    count: e.count == null ? void 0 : l(e.count),
    functionOption: ms(e.function_option),
    composerDraft: ps(e.composer_draft),
    resultRef: hs(e.result_ref),
    resultOutput: y(e.result_output, e.resultOutput),
    resultView: ss(
      y(e.result_view, e.resultView)
    ),
    runError: c(y(e.run_error, e.runError)),
    local: e.local !== !1
  }, o = c(e.kind), i = c(
    e.cardinality
  ), a = ds(e.flow), d = us(e.role), u = ls(e.asset), m = fs(e.power);
  return o && (r.kind = o), i && (r.cardinality = i), a && (r.flow = a), d && (r.role = d), u && (r.asset = u), m && (r.power = m, r.outputType = r.outputType || m.outputType), Gn(r);
}
function Gn(e) {
  const t = is(e), n = Ss(t);
  return n ? { ...t, ...n } : t;
}
function is(e) {
  return e.type !== "power" || !jn(e.power, e.kind, e.outputType) || e.width !== He.width || e.height !== He.height ? e : {
    ...e,
    ...Kn
  };
}
function ss(e) {
  const t = k(e), n = he(t.width), r = he(t.height);
  if (n == null || r == null || n <= 0 || r <= 0)
    return;
  const o = he(y(t.offset_x, t.offsetX)), i = he(y(t.offset_y, t.offsetY));
  return {
    width: n,
    height: r,
    ...o == null ? {} : { offsetX: o },
    ...i == null ? {} : { offsetY: i }
  };
}
function as(e) {
  const t = k(e);
  if (Object.keys(t).length)
    return {
      origin: c(t.origin),
      sourceNodeId: c(
        y(t.source_node_id, t.sourceNodeId)
      ),
      syncKey: c(y(t.sync_key, t.syncKey)),
      layoutKey: c(y(t.layout_key, t.layoutKey))
    };
}
function cs(e) {
  const t = k(e), n = c(
    y(t.source_node_id, t.sourceNodeId)
  ), r = c(
    y(
      t.item_type,
      t.itemType,
      t.material_type,
      t.materialType
    )
  ), o = c(
    y(t.item_id, t.itemId, t.material_id, t.materialId)
  );
  if (!(!n || !o || ![
    "character",
    "scene",
    "prop",
    "shot_image",
    "shot",
    "speech",
    "subtitle",
    "lip_sync",
    "video_compose"
  ].includes(r)))
    return {
      sourceNodeId: n,
      itemType: r,
      itemId: o,
      generatedPrompt: c(
        y(t.generated_prompt, t.generatedPrompt)
      ),
      dependencyNodeIds: Je(
        y(t.dependency_node_ids, t.dependencyNodeIds)
      ),
      referenceNodeIds: Je(
        y(t.reference_node_ids, t.referenceNodeIds)
      ),
      externalReferenceAssetIds: Ns(
        y(
          t.external_reference_asset_ids,
          t.externalReferenceAssetIds
        )
      ),
      shotId: c(y(t.shot_id, t.shotId)),
      speechId: c(y(t.speech_id, t.speechId)),
      speechIds: Je(y(t.speech_ids, t.speechIds)),
      characterId: c(
        y(t.character_id, t.characterId)
      ),
      speechKind: c(
        y(t.speech_kind, t.speechKind)
      ),
      speakerMode: c(
        y(t.speaker_mode, t.speakerMode)
      ),
      startTime: he(y(t.start_time, t.startTime)),
      shotDuration: he(
        y(t.shot_duration, t.shotDuration)
      ),
      continuityAnchor: c(
        y(t.continuity_anchor, t.continuityAnchor)
      ),
      optional: t.optional === !0 || t.optional === 1 || String(t.optional || "").toLowerCase() === "true",
      sourceSignature: c(
        y(t.source_signature, t.sourceSignature)
      ),
      resultSourceSignature: c(
        y(t.result_source_signature, t.resultSourceSignature)
      ),
      stale: t.stale === !0 || t.stale === 1 || String(t.stale || "").toLowerCase() === "true"
    };
}
function ds(e) {
  const t = k(e), n = l(t.id), r = c(t.key), o = c(t.name);
  if (!(!n && !r && !o))
    return {
      id: n,
      key: r,
      name: o,
      goal: c(t.goal)
    };
}
function us(e) {
  const t = k(e), n = l(t.id), r = c(t.name);
  if (!(!n && !r))
    return {
      id: n,
      name: r,
      role_type: c(t.role_type),
      agent_id: l(t.agent_id)
    };
}
function ls(e) {
  const t = k(e), n = l(t.id);
  if (n)
    return {
      id: n,
      project_id: 0,
      body_id: 0,
      team_id: 0,
      flow_id: 0,
      asset_cate_id: l(t.asset_cate_id),
      name: c(t.name),
      kind: c(t.kind),
      role: c(t.role),
      version_id: l(t.version_id),
      sort: 0
    };
}
function fs(e) {
  const t = k(e), n = l(t.id), r = c(t.key);
  if (!(!n && !r))
    return Ot({
      ...t,
      id: n,
      key: r,
      name: c(t.name)
    });
}
function ms(e) {
  const t = k(e), n = c(t.key);
  if (n)
    return {
      key: n,
      label: c(t.label),
      description: c(t.description)
    };
}
function ps(e) {
  const t = k(e);
  if (Object.keys(t).length)
    return {
      prompt: c(t.prompt),
      promptContent: ys(
        y(t.promptContent, t.prompt_content)
      ),
      paramValues: k(y(t.paramValues, t.param_values)),
      selectedTargetId: l(
        y(t.selectedTargetId, t.selected_target_id)
      ),
      videoComposition: Oi(
        y(t.videoComposition, t.video_composition)
      ),
      storyboardReferences: ot(
        y(t.storyboardReferences, t.storyboard_references)
      )
    };
}
function ys(e) {
  const t = k(e), n = Array.isArray(t.parts) ? t.parts.filter((r) => {
    const o = k(r);
    return o.type === "text" || o.type === "reference";
  }) : [];
  if (!(Number(t.version) !== 1 || n.length === 0))
    return { version: 1, parts: n };
}
function hs(e) {
  const t = k(e);
  if (Object.keys(t).length)
    return {
      execution_id: l(t.execution_id),
      run_id: l(t.run_id),
      request_id: c(t.request_id),
      flow_run_id: l(t.flow_run_id),
      node_run_id: l(t.node_run_id),
      asset_id: l(t.asset_id),
      version_id: l(t.version_id),
      release_id: l(t.release_id),
      role: c(t.role),
      status: c(t.status),
      updated_at: c(t.updated_at)
    };
}
function _s(e) {
  const t = c(e.from), n = c(e.to);
  return !t || !n ? null : {
    id: c(e.id) || `edge-${t}-${n}`,
    from: t,
    to: n,
    logicalFrom: c(y(e.logical_from, e.logicalFrom)) || void 0,
    logicalTo: c(y(e.logical_to, e.logicalTo)) || void 0,
    executionMode: c(y(e.execution_mode, e.executionMode)) === "manual" ? "manual" : void 0,
    mediaUsage: c(y(e.media_usage, e.mediaUsage)) || void 0
  };
}
function gs(e) {
  const t = k(e), n = {};
  return t.x != null && (n.x = l(t.x)), t.y != null && (n.y = l(t.y)), t.zoom != null && (n.zoom = l(t.zoom)), n;
}
function bs(e, t) {
  const n = /* @__PURE__ */ new Set();
  for (const r of e.nodesByFlow[t.key] || []) {
    if (String(r.type || "").toLowerCase() !== "save")
      continue;
    const o = l(
      y(r.asset_cate_id, r.config?.asset_cate_id)
    );
    o > 0 && n.add(o);
  }
  return n;
}
function ws(e) {
  switch (e) {
    case "image":
      return "生图能力";
    case "video":
      return "生视频能力";
    case "audio":
      return "生音频能力";
    default:
      return "文生文能力";
  }
}
function Zn(e, t) {
  switch (e) {
    case "agent":
      return { width: 154, height: 154 };
    case "flow":
      return { width: 210, height: 160 };
    case "function":
      return { width: 128, height: 46 };
    case "group":
      return { ...Ni };
    case "power":
      return ks(t);
    default:
      return { width: 250, height: 170 };
  }
}
function ks(e) {
  if (Vn(e))
    return { ...vn };
  const t = Xn(e);
  return t || (jn(e) ? { ...Kn } : { ...He });
}
function Ss(e) {
  if (e.type !== "power" || !Vn(e.power, e.kind))
    return null;
  const t = Xn(e.power) || He;
  return e.width === t.width && e.height === t.height || Li.some(
    (r) => e.width === r.width && e.height === r.height
  ) ? { ...vn } : null;
}
function Xn(e) {
  const t = Number(e?.output?.defaultWidth || 0), n = Number(e?.output?.defaultHeight || 0);
  return t > 0 && n > 0 ? { width: t, height: n } : null;
}
function Dc(e) {
  const t = Zn(
    e.type,
    e.power || {
      kind: String(e.kind || ""),
      outputType: e.outputType || ""
    }
  );
  return e.width === t.width && e.height === t.height;
}
function Je(e) {
  return Array.isArray(e) ? e.map(c).filter(Boolean) : [];
}
function Ns(e) {
  if (!Array.isArray(e))
    return [];
  const t = [], n = /* @__PURE__ */ new Set();
  for (const r of e) {
    const o = l(r);
    !o || o <= 0 || n.has(o) || (n.add(o), t.push(o));
  }
  return t;
}
const As = {
  audio: "editorMediaAudio",
  image: "editorMediaImage",
  mediaAudio: "editorMediaAudio",
  mediaImage: "editorMediaImage",
  mediaVideo: "editorMediaVideo",
  video: "editorMediaVideo"
}, Qn = [
  "rich",
  "value",
  "doc",
  "document",
  "content",
  "data",
  "output",
  "result",
  "body"
];
function We(e) {
  if (typeof e == "string") {
    const i = e.trim();
    if (rr(i)) {
      const d = or(i);
      if (d !== void 0) {
        const u = We(d).trim();
        return u || "";
      }
    }
    const a = Wi(i);
    return a || e;
  }
  if (Array.isArray(e))
    return e.map(We).filter(Boolean).join(" ");
  if (!e || typeof e != "object")
    return "";
  const t = e, n = Un(t);
  if (n)
    return nr(n);
  const o = [typeof t.text == "string" ? t.text : "", typeof t.markdown == "string" ? t.markdown : ""];
  for (const i of Qn)
    t[i] != null && o.push(We(t[i]));
  return o.filter(Boolean).join(" ");
}
function Me(e, t) {
  if (typeof e == "string") {
    const o = e.trim();
    if (!rr(o))
      return null;
    const i = or(o);
    return i === void 0 ? null : Me(i, t);
  }
  if (Array.isArray(e)) {
    const o = Wt({ type: "doc", content: e });
    if (Rt(o))
      return o;
    for (const i of e) {
      const a = Me(i, t);
      if (a)
        return a;
    }
    return null;
  }
  if (!e || typeof e != "object" || t.has(e))
    return null;
  t.add(e);
  const n = e, r = Wt(n);
  if (r)
    return r;
  if (String(n.format || "").toLowerCase() === "rich_json" && n.rich != null) {
    const o = Me(n.rich, t);
    if (o)
      return o;
  }
  for (const o of Qn) {
    if (n[o] == null)
      continue;
    const i = Me(n[o], t);
    if (i)
      return i;
  }
  return null;
}
function Wt(e) {
  return X(e) && tr(e.type) === "doc" ? {
    type: "doc",
    attrs: X(e.attrs) ? e.attrs : void 0,
    content: er(e.content)
  } : null;
}
function er(e) {
  return Array.isArray(e) ? e.map(Is).filter((t) => !!t) : [];
}
function Is(e) {
  if (!X(e))
    return null;
  const t = tr(e.type) || Cs(e);
  if (!t)
    return null;
  const n = { type: t }, r = X(e.attrs) ? { ...e.attrs } : {};
  if (t === "heading" && l(r.level) <= 0) {
    const a = l(e.level);
    a > 0 && (r.level = a);
  }
  Object.keys(r).length > 0 && (n.attrs = r);
  const o = Ts(e.marks);
  if (o.length > 0 && (n.marks = o), t === "text") {
    const a = c(e.text);
    return a ? (n.text = a, n) : null;
  }
  const i = er(e.content);
  return i.length > 0 && (n.content = i), n;
}
function Cs(e) {
  if (typeof e.text == "string")
    return "text";
  const t = X(e.attrs) ? e.attrs : {};
  return l(t.level) > 0 || l(e.level) > 0 ? "heading" : "";
}
function Ts(e) {
  return Array.isArray(e) ? e.map((t) => {
    if (!X(t))
      return null;
    const n = c(t.type);
    return n ? {
      type: n,
      attrs: X(t.attrs) ? t.attrs : void 0
    } : null;
  }).filter(
    (t) => !!t
  ) : [];
}
function tr(e) {
  const t = c(e);
  return As[t] || t;
}
function nr(e) {
  return e ? e.type === "text" ? e.text || "" : e.type === "editorMediaImage" || e.type === "editorMediaVideo" || e.type === "editorMediaAudio" ? c(e.attrs?.alt || e.attrs?.title || e.attrs?.src) : (e.content || []).map(nr).filter(Boolean).join(" ") : "";
}
function Rt(e) {
  return e ? e.type === "text" ? !!c(e.text) : e.type === "editorMediaImage" || e.type === "editorMediaVideo" || e.type === "editorMediaAudio" ? !!c(e.attrs?.src) : (e.content || []).some(Rt) : !1;
}
function rr(e) {
  return e.startsWith("{") && e.endsWith("}") || e.startsWith("[") && e.endsWith("]");
}
function or(e) {
  try {
    return JSON.parse(e);
  } catch {
    return;
  }
}
function xs(e) {
  return e.includes("rich_json") || e.includes('"rich"') || e.includes("agent_run_id") || e.includes("node_run_id");
}
function Ds(e) {
  try {
    return JSON.parse(`"${e}"`);
  } catch {
    return e.replace(/\\"/g, '"').replace(/\\n/g, `
`).replace(/\\t/g, "	").replace(/\\\\/g, "\\");
  }
}
function P(e) {
  return Array.isArray(e) ? e.filter(X) : [];
}
function k(e) {
  return X(e) ? e : {};
}
function X(e) {
  return e != null && typeof e == "object" && !Array.isArray(e);
}
function y(...e) {
  return e.find((t) => t != null);
}
function l(e) {
  const t = Number(e || 0);
  return Number.isFinite(t) ? t : 0;
}
function he(e) {
  if (e == null || e === "")
    return;
  const t = Number(e);
  return Number.isFinite(t) ? t : void 0;
}
function c(e) {
  return e == null ? "" : String(e).trim();
}
const ir = {
  image: "images",
  audio: "audios",
  video: "videos",
  file: "files"
};
function sr(e, t) {
  const n = ir[e], r = n ? $e(t, e) : "";
  if (n && r)
    return { [n]: [r] };
  const o = Un(t);
  if (!o)
    return t;
  const i = yn(o);
  return i && (e === "text" || So(i.plainText)) ? { text: i.markdown } : { rich: o };
}
function $e(e, t, n = 0) {
  if (n > 10 || e == null) return "";
  if (typeof e == "string")
    return Ze(e) ? e : "";
  if (Array.isArray(e)) {
    for (const a of e) {
      const d = $e(a, t, n + 1);
      if (d) return d;
    }
    return "";
  }
  if (typeof e != "object") return "";
  const r = e, o = {
    collection: [],
    text: [],
    image: ["src", "url", "image", "image_url", "file_url"],
    audio: ["src", "url", "audio", "audio_url", "file_url"],
    video: ["src", "url", "video", "video_url", "file_url"],
    richtext: ["src", "url"],
    file: ["src", "url", "file", "file_url"]
  };
  for (const a of o[t]) {
    const d = typeof r[a] == "string" ? String(r[a]) : "";
    if (Ze(d)) return d;
  }
  const i = [
    ir[t],
    "content",
    "output",
    "result",
    "data",
    "attrs",
    "text"
  ].filter((a) => !!a);
  for (const a of i) {
    const d = $e(r[a], t, n + 1);
    if (d) return d;
  }
  return "";
}
function Ge(e, t = 0) {
  if (t > 8 || e == null) return "";
  if (typeof e == "string") return Ze(e) ? "" : e.trim();
  if (Array.isArray(e))
    return e.map((r) => Ge(r, t + 1)).filter(Boolean)[0] || "";
  if (typeof e != "object") return "";
  const n = e;
  for (const r of [
    "summary",
    "title",
    "text",
    "caption",
    "content",
    "output",
    "result"
  ]) {
    const o = Ge(n[r], t + 1);
    if (o) return o;
  }
  return "";
}
function Os(e) {
  const t = e?.source?.prompt;
  return typeof t == "string" ? t.trim() : "";
}
function Ms(e) {
  const t = $e(e, "file"), n = qe(e) || Rs(t), r = n.match(/\.([a-z0-9]{1,10})$/i)?.[1] || "";
  return { url: t, name: n, extension: r };
}
function qe(e, t = 0) {
  if (e == null || t > 8) return "";
  if (typeof e == "string") {
    const r = e.trim();
    return Ze(r) ? "" : r;
  }
  if (Array.isArray(e)) {
    for (const r of e) {
      const o = qe(r, t + 1);
      if (o) return o;
    }
    return "";
  }
  if (typeof e != "object") return "";
  const n = e;
  for (const r of [
    "name",
    "file_name",
    "fileName",
    "filename",
    "label",
    "title"
  ]) {
    const o = qe(n[r], t + 1);
    if (o) return o;
  }
  for (const r of [
    "file",
    "files",
    "attrs",
    "data",
    "content",
    "output",
    "result"
  ]) {
    const o = qe(n[r], t + 1);
    if (o) return o;
  }
  return "";
}
function Rs(e) {
  if (!e || /^(data|blob):/.test(e)) return "";
  const t = e.split(/[?#]/, 1)[0], n = t.slice(t.lastIndexOf("/") + 1);
  if (!n) return "";
  try {
    return decodeURIComponent(n);
  } catch {
    return n;
  }
}
function Ze(e) {
  return /^(https?:\/\/|\/|data:|blob:)/.test(e.trim());
}
function qt({
  content: e,
  summary: t,
  compact: n = !1
}) {
  const r = Ms(e), o = r.name || t || "文件", i = r.extension ? r.extension.toUpperCase() : "FILE", a = r.extension ? `${r.extension.toUpperCase()} 文件` : "文件";
  return n ? /* @__PURE__ */ f("div", { className: "wb-asset-file-card-preview", children: [
    /* @__PURE__ */ s("strong", { children: i }),
    /* @__PURE__ */ s(L, { label: o, children: /* @__PURE__ */ s("p", { children: o }) }),
    /* @__PURE__ */ s("span", { children: "文件" })
  ] }) : /* @__PURE__ */ f("section", { className: "wb-asset-file-preview", children: [
    /* @__PURE__ */ s("span", { className: "wb-asset-file-icon", children: /* @__PURE__ */ s(et, { "aria-hidden": "true" }) }),
    /* @__PURE__ */ f("div", { className: "wb-asset-file-copy", children: [
      /* @__PURE__ */ s(L, { label: o, children: /* @__PURE__ */ s("strong", { children: o }) }),
      /* @__PURE__ */ s("span", { children: a })
    ] }),
    r.url ? /* @__PURE__ */ f("a", { href: r.url, target: "_blank", rel: "noreferrer", children: [
      /* @__PURE__ */ s(Lr, { "aria-hidden": "true" }),
      /* @__PURE__ */ s("span", { children: "打开文件" })
    ] }) : /* @__PURE__ */ s("span", { className: "wb-asset-file-unavailable", children: "文件暂不可用" })
  ] });
}
function Ht({
  kind: e,
  src: t
}) {
  const n = pe(null), [r, o] = N(""), [i, a] = N(""), [d, u] = N(""), m = r === t, g = i === t, p = d === t;
  Z(() => {
    const w = n.current;
    if (!t || !w || typeof IntersectionObserver > "u") {
      o(t);
      return;
    }
    const O = new IntersectionObserver(
      (A) => {
        A.some((x) => x.isIntersecting) && (o(t), O.disconnect());
      },
      { rootMargin: "320px 0px" }
    );
    return O.observe(w), () => O.disconnect();
  }, [t]);
  function _() {
    a(t), u("");
  }
  function h() {
    a(""), u(t);
  }
  return /* @__PURE__ */ f(
    "div",
    {
      ref: n,
      className: [
        "wb-asset-lazy-cover",
        `is-${e}`,
        g ? "is-loaded" : "is-pending",
        p ? "is-failed" : ""
      ].filter(Boolean).join(" "),
      children: [
        m && !p ? e === "image" ? /* @__PURE__ */ s(
          "img",
          {
            src: t,
            alt: "",
            loading: "lazy",
            decoding: "async",
            onLoad: _,
            onError: h
          }
        ) : /* @__PURE__ */ s(
          "video",
          {
            src: t,
            muted: !0,
            playsInline: !0,
            preload: "metadata",
            "aria-hidden": "true",
            onLoadedMetadata: _,
            onLoadedData: _,
            onError: h
          }
        ) : null,
        p ? /* @__PURE__ */ s("span", { children: "封面加载失败" }) : null
      ]
    }
  );
}
function zs({
  kind: e,
  content: t,
  summary: n
}) {
  const r = sr(e, t), o = n || Ge(t) || _e(e), i = Cn(r) ? { text: o } : r;
  return /* @__PURE__ */ s("div", { className: `wb-asset-card-text-preview is-${e}`, children: /* @__PURE__ */ s(
    $n,
    {
      output: i,
      fallback: o,
      emptyText: o,
      className: "wb-asset-card-text-content",
      markdownClassName: "wb-asset-card-prose",
      richClassName: "wb-asset-card-prose"
    }
  ) });
}
function zt({
  kind: e,
  content: t,
  summary: n,
  prompt: r,
  compact: o = !1
}) {
  const i = $e(t, e);
  if (!o) {
    if (e === "audio")
      return /* @__PURE__ */ s(Jt, { src: i, prompt: r, detailed: !0 });
    if (e === "file")
      return /* @__PURE__ */ s(qt, { content: t, summary: n });
    const a = sr(e, t);
    return /* @__PURE__ */ s(
      $n,
      {
        output: a,
        fallback: n || "",
        emptyText: "该版本暂无可预览内容",
        className: "wb-asset-preview-content",
        markdownClassName: "wb-asset-detail-prose",
        richClassName: "wb-asset-detail-prose",
        mediaLayout: "detail"
      }
    );
  }
  return e === "image" && i ? /* @__PURE__ */ s(Ht, { kind: "image", src: i }) : e === "video" && i ? /* @__PURE__ */ s(Ht, { kind: "video", src: i }) : e === "audio" ? /* @__PURE__ */ s(Jt, { src: i }) : e === "file" ? /* @__PURE__ */ s(qt, { content: t, summary: n, compact: !0 }) : e === "text" || e === "richtext" ? /* @__PURE__ */ s(
    zs,
    {
      kind: e,
      content: t,
      summary: n
    }
  ) : /* @__PURE__ */ s("div", { className: "wb-asset-card-fallback", children: /* @__PURE__ */ s("p", { children: n || Ge(t) || _e(e) }) });
}
function Et({ kind: e }) {
  switch (e) {
    case "collection":
      return /* @__PURE__ */ s(rn, { "aria-hidden": "true" });
    case "image":
      return /* @__PURE__ */ s(Er, { "aria-hidden": "true" });
    case "audio":
      return /* @__PURE__ */ s(Ur, { "aria-hidden": "true" });
    case "video":
      return /* @__PURE__ */ s(Jr, { "aria-hidden": "true" });
    case "file":
      return /* @__PURE__ */ s(Xr, { "aria-hidden": "true" });
    default:
      return /* @__PURE__ */ s(et, { "aria-hidden": "true" });
  }
}
function Es({
  asset: e,
  sourceLabels: t,
  view: n = "assets",
  selectable: r = !1,
  selected: o = !1,
  used: i = !1,
  busy: a = !1,
  onOpen: d,
  onRename: u,
  onDelete: m,
  onRestore: g,
  onSelect: p
}) {
  const _ = n === "trash", h = e.kind === "collection", w = h ? /* @__PURE__ */ s(Ps, { asset: e }) : /* @__PURE__ */ s(
    zt,
    {
      kind: e.kind,
      content: e.version?.content,
      summary: e.summary,
      compact: !0
    }
  );
  return /* @__PURE__ */ f(
    "article",
    {
      className: `wb-asset-card ${h ? "is-collection" : ""} ${o ? "is-selected" : ""} ${i ? "is-used" : ""} ${_ ? "is-trash" : ""}`.trim(),
      children: [
        /* @__PURE__ */ f("div", { className: "wb-asset-card-main", children: [
          /* @__PURE__ */ f("div", { className: "wb-asset-card-preview", children: [
            w,
            e.kind !== "audio" ? /* @__PURE__ */ s(
              "button",
              {
                type: "button",
                className: "wb-asset-card-preview-open",
                onClick: () => d(e),
                "aria-label": `${h ? "打开集合" : "查看"}${e.name}`
              }
            ) : null
          ] }),
          /* @__PURE__ */ s(L, { label: e.name, children: /* @__PURE__ */ f(
            "button",
            {
              type: "button",
              className: "wb-asset-card-copy",
              onClick: () => d(e),
              children: [
                /* @__PURE__ */ s("strong", { children: e.name }),
                /* @__PURE__ */ s("span", { children: h ? `集合 · ${e.collectionCount} 项素材` : `${pn(e.sourceType, t)} · ${_e(e.kind)}` })
              ]
            }
          ) })
        ] }),
        /* @__PURE__ */ s("span", { className: "wb-asset-card-kind-icon", children: /* @__PURE__ */ s(Et, { kind: e.kind }) }),
        /* @__PURE__ */ f("div", { className: "wb-asset-card-actions", children: [
          _ ? null : /* @__PURE__ */ s(L, { label: "修改标题", children: /* @__PURE__ */ f(
            "button",
            {
              type: "button",
              disabled: a,
              onClick: () => u(e),
              children: [
                /* @__PURE__ */ s(Nt, { "aria-hidden": "true" }),
                /* @__PURE__ */ s("span", { className: "sr-only", children: "修改标题" })
              ]
            }
          ) }),
          _ && g ? /* @__PURE__ */ s(L, { label: "恢复资产", children: /* @__PURE__ */ f(
            "button",
            {
              type: "button",
              className: "is-restore",
              disabled: a,
              onClick: () => g(e),
              children: [
                a ? /* @__PURE__ */ s(V, { className: "is-spinning", "aria-hidden": "true" }) : /* @__PURE__ */ s(Qe, { "aria-hidden": "true" }),
                /* @__PURE__ */ s("span", { className: "sr-only", children: "恢复资产" })
              ]
            }
          ) }) : m ? /* @__PURE__ */ s(L, { label: "移入回收站", children: /* @__PURE__ */ f(
            "button",
            {
              type: "button",
              className: "is-danger",
              disabled: a,
              onClick: () => m(e),
              children: [
                a ? /* @__PURE__ */ s(V, { className: "is-spinning", "aria-hidden": "true" }) : /* @__PURE__ */ s(zr, { "aria-hidden": "true" }),
                /* @__PURE__ */ s("span", { className: "sr-only", children: "移入回收站" })
              ]
            }
          ) }) : null,
          !h && !_ && r && p ? /* @__PURE__ */ f(
            "button",
            {
              type: "button",
              className: `is-primary ${o ? "is-selected" : ""} ${i ? "is-used" : ""}`.trim(),
              disabled: a || i,
              onClick: () => p(e),
              children: [
                /* @__PURE__ */ s(Xe, { "aria-hidden": "true" }),
                i ? "已使用" : o ? "已选" : "使用"
              ]
            }
          ) : null
        ] })
      ]
    }
  );
}
function Ps({ asset: e }) {
  const t = e.collectionPreviews.slice(0, 4);
  return t.length === 0 ? /* @__PURE__ */ f("div", { className: "wb-asset-collection-empty", children: [
    /* @__PURE__ */ s(Et, { kind: "collection" }),
    /* @__PURE__ */ s("span", { children: e.collectionCount > 0 ? `${e.collectionCount} 项素材` : "空集合" })
  ] }) : /* @__PURE__ */ f("div", { className: `wb-asset-collection-preview has-${t.length}`, children: [
    t.map((n) => /* @__PURE__ */ s("div", { children: /* @__PURE__ */ s(zt, { kind: n.kind, content: n.content, compact: !0 }) }, n.id)),
    /* @__PURE__ */ f("span", { children: [
      e.collectionCount,
      " 项"
    ] })
  ] });
}
function $s({
  ariaLabel: e,
  header: t,
  children: n,
  onRequestClose: r
}) {
  return typeof document > "u" ? null : ln(/* @__PURE__ */ s(
    "div",
    {
      className: "wb-detail-backdrop",
      role: "presentation",
      onMouseDown: () => {
        r();
      },
      children: /* @__PURE__ */ f(
        "section",
        {
          className: "wb-detail-dialog",
          role: "dialog",
          "aria-modal": "true",
          "aria-label": e,
          onMouseDown: (i) => i.stopPropagation(),
          children: [
            t,
            n
          ]
        }
      )
    }
  ), document.body);
}
function Ls({
  icon: e,
  title: t,
  subtitle: n,
  versionSelect: r,
  state: o,
  updatedAt: i,
  actions: a,
  downloadUrl: d,
  onClose: u
}) {
  return /* @__PURE__ */ f("header", { className: "wb-detail-head", children: [
    /* @__PURE__ */ f("div", { className: "wb-detail-heading", children: [
      /* @__PURE__ */ s("span", { className: "wb-detail-kind-icon", "aria-hidden": "true", children: e }),
      /* @__PURE__ */ f("div", { children: [
        /* @__PURE__ */ s("strong", { children: t || "详情" }),
        n ? /* @__PURE__ */ s("span", { children: n }) : null
      ] })
    ] }),
    /* @__PURE__ */ f("div", { className: "wb-detail-meta", children: [
      r,
      o,
      i ? /* @__PURE__ */ s("time", { children: i }) : null
    ] }),
    /* @__PURE__ */ f("div", { className: "wb-detail-actions", children: [
      a,
      d ? /* @__PURE__ */ s(L, { label: "下载内容", children: /* @__PURE__ */ s(
        "a",
        {
          href: d,
          download: !0,
          className: "wb-detail-icon-button",
          "aria-label": "下载内容",
          children: /* @__PURE__ */ s(Pr, { size: 17 })
        }
      ) }) : null,
      /* @__PURE__ */ s(L, { label: "关闭", children: /* @__PURE__ */ s(
        "button",
        {
          type: "button",
          className: "wb-detail-icon-button",
          onClick: u,
          "aria-label": "关闭详情",
          children: /* @__PURE__ */ s(un, { size: 18 })
        }
      ) })
    ] })
  ] });
}
function Bs({
  options: e,
  currentVersionId: t,
  selectedVersionId: n,
  total: r,
  hasMore: o,
  loading: i,
  loadingMore: a,
  error: d,
  disabled: u = !1,
  onSelect: m,
  onLoadMore: g,
  onRetry: p
}) {
  const [_, h] = N(!1), w = pe(null), O = e.find((A) => A.id === n) || e.find((A) => A.id === t);
  return Z(() => {
    if (!_) return;
    const A = (x) => {
      w.current?.contains(x.target) || h(!1);
    };
    return document.addEventListener("mousedown", A), () => document.removeEventListener("mousedown", A);
  }, [_]), !n && !i ? null : /* @__PURE__ */ f("div", { className: "wb-detail-version-select", ref: w, children: [
    /* @__PURE__ */ f(
      "button",
      {
        type: "button",
        className: "wb-detail-version-trigger",
        "aria-haspopup": "listbox",
        "aria-expanded": _,
        disabled: u || i && e.length === 0,
        onClick: () => h((A) => !A),
        children: [
          i && e.length === 0 ? /* @__PURE__ */ s(V, { size: 12, className: "wb-detail-spin" }) : null,
          /* @__PURE__ */ s("span", { children: Zt(O?.version) }),
          r > 0 ? /* @__PURE__ */ f("small", { children: [
            r,
            " 个版本"
          ] }) : null,
          /* @__PURE__ */ s(jr, { size: 13 })
        ]
      }
    ),
    _ ? /* @__PURE__ */ s("div", { className: "wb-detail-version-menu", role: "listbox", children: /* @__PURE__ */ f(
      "div",
      {
        className: "wb-detail-version-options",
        onScroll: (A) => {
          const x = A.currentTarget;
          o && !a && x.scrollHeight - x.scrollTop - x.clientHeight < 36 && g();
        },
        children: [
          e.length > 0 ? e.map((A) => {
            const x = A.id === n, K = A.id === t;
            return /* @__PURE__ */ f(
              "button",
              {
                type: "button",
                role: "option",
                "aria-selected": x,
                className: x ? "is-selected" : "",
                onClick: () => {
                  h(!1), m(A.value);
                },
                children: [
                  /* @__PURE__ */ f("span", { children: [
                    /* @__PURE__ */ s("strong", { children: Zt(A.version) }),
                    K ? /* @__PURE__ */ s("small", { children: "当前" }) : null
                  ] }),
                  /* @__PURE__ */ s("time", { children: ar(A.updatedAt) }),
                  x ? /* @__PURE__ */ s(Xe, { size: 13 }) : /* @__PURE__ */ s("i", { "aria-hidden": "true" })
                ]
              },
              A.id
            );
          }) : d ? /* @__PURE__ */ s(Gt, { error: d, onRetry: p }) : /* @__PURE__ */ f("div", { className: "wb-detail-version-message", children: [
            i ? /* @__PURE__ */ s(V, { size: 14, className: "wb-detail-spin" }) : null,
            /* @__PURE__ */ s("span", { children: i ? "正在读取版本" : "暂无版本" })
          ] }),
          d && e.length > 0 ? /* @__PURE__ */ s(Gt, { error: d, onRetry: p }) : null,
          a ? /* @__PURE__ */ f("div", { className: "wb-detail-version-loading", children: [
            /* @__PURE__ */ s(V, { size: 13, className: "wb-detail-spin" }),
            "正在加载更多"
          ] }) : null
        ]
      }
    ) }) : null
  ] });
}
function Gt({
  error: e,
  onRetry: t
}) {
  return /* @__PURE__ */ f("div", { className: "wb-detail-version-message is-error", children: [
    /* @__PURE__ */ s("span", { children: e }),
    /* @__PURE__ */ f("button", { type: "button", onClick: t, children: [
      /* @__PURE__ */ s(eo, { size: 12 }),
      "重试"
    ] })
  ] });
}
function Zt(e) {
  const t = Number(e || 0);
  return t > 0 ? `第${t}版` : "版本";
}
function ar(e) {
  const t = String(e || "").trim();
  return t ? t.replace("T", " ").replace(/\.\d+(Z)?$/, "").replace(/Z$/, "") : "";
}
function cr({
  teamID: e,
  asset: t,
  onClose: n,
  onRenamed: r
}) {
  const [o, i] = N(""), [a, d] = N(!1), [u, m] = N("");
  Z(() => {
    t && (i(t.name), d(!1), m(""));
  }, [t]), Z(() => {
    if (!t) return;
    const p = (_) => {
      _.key === "Escape" && (_.preventDefault(), _.stopImmediatePropagation(), a || n());
    };
    return window.addEventListener("keydown", p, !0), () => window.removeEventListener("keydown", p, !0);
  }, [t, n, a]);
  async function g(p) {
    p.preventDefault();
    const _ = o.trim();
    if (!(!t || !_ || a)) {
      d(!0), m("");
      try {
        const h = await uo({
          teamID: e,
          assetID: t.id,
          name: _
        });
        r(h), n();
      } catch (h) {
        m(js(h, "修改资产标题失败"));
      } finally {
        d(!1);
      }
    }
  }
  return !t || typeof document > "u" ? null : ln(
    /* @__PURE__ */ s(
      "div",
      {
        className: "wb-asset-rename-backdrop",
        role: "dialog",
        "aria-modal": "true",
        "aria-label": "修改资产标题",
        onMouseDown: (p) => {
          p.target === p.currentTarget && !a && n();
        },
        children: /* @__PURE__ */ f("form", { className: "wb-asset-rename-dialog", onSubmit: g, children: [
          /* @__PURE__ */ f("header", { children: [
            /* @__PURE__ */ f("div", { children: [
              /* @__PURE__ */ s("span", { className: "wb-asset-rename-icon", children: /* @__PURE__ */ s(Nt, { "aria-hidden": "true" }) }),
              /* @__PURE__ */ f("div", { children: [
                /* @__PURE__ */ s("h2", { children: "修改资产标题" }),
                /* @__PURE__ */ s("p", { children: "只修改资产库中的显示名称。" })
              ] })
            ] }),
            /* @__PURE__ */ s(L, { label: "关闭", children: /* @__PURE__ */ s("button", { type: "button", disabled: a, onClick: n, children: /* @__PURE__ */ s(un, { "aria-hidden": "true" }) }) })
          ] }),
          /* @__PURE__ */ f("label", { children: [
            /* @__PURE__ */ s("span", { children: "资产标题" }),
            /* @__PURE__ */ s(
              "input",
              {
                autoFocus: !0,
                value: o,
                maxLength: 128,
                disabled: a,
                placeholder: "请输入资产标题",
                onChange: (p) => i(p.target.value)
              }
            )
          ] }),
          u ? /* @__PURE__ */ s("p", { className: "wb-asset-rename-error", children: u }) : null,
          /* @__PURE__ */ f("footer", { children: [
            /* @__PURE__ */ s("button", { type: "button", disabled: a, onClick: n, children: "取消" }),
            /* @__PURE__ */ f(
              "button",
              {
                type: "submit",
                className: "is-primary",
                disabled: a || !o.trim(),
                children: [
                  a ? /* @__PURE__ */ s(V, { className: "is-spinning" }) : null,
                  a ? "保存中" : "保存"
                ]
              }
            )
          ] })
        ] })
      }
    ),
    document.body
  );
}
function js(e, t) {
  return e instanceof Error ? e.message : t;
}
function dr() {
  const e = Vr().site.homeMenu;
  return De(
    () => ({
      project: e.works.name,
      tool: e.function.name,
      dialogue: e.dialogue.name,
      fallback: e.assets.name
    }),
    [
      e.assets.name,
      e.dialogue.name,
      e.function.name,
      e.works.name
    ]
  );
}
function Vs({
  teamID: e,
  assetID: t,
  selectable: n = !1,
  onClose: r,
  onSelect: o,
  onContinue: i,
  canContinue: a,
  onAssetChanged: d
}) {
  const u = dr(), [m, g] = N(null), [p, _] = N(
    null
  ), [h, w] = N(!0), [O, A] = N(0), [x, K] = N(!1), [at, Q] = N(!1), [ae, W] = N(""), [q, ke] = N(""), H = an(async () => {
    w(!0), W(""), ke("");
    try {
      const S = Xt(await io(e, t));
      g(S), _(S.asset.version);
    } catch (S) {
      W(Ke(S, "加载资产详情失败"));
    } finally {
      w(!1);
    }
  }, [t, e]);
  Z(() => {
    H();
  }, [H]), Z(() => {
    const S = (M) => {
      M.key === "Escape" && (M.preventDefault(), r());
    };
    return window.addEventListener("keydown", S), () => window.removeEventListener("keydown", S);
  }, [r]);
  async function G(S) {
    if (!(O || S.id === p?.id)) {
      if (S.id === m?.asset.versionID && m.asset.version) {
        W(""), _(m.asset.version);
        return;
      }
      A(S.id), W("");
      try {
        _(
          await ao({ teamID: e, assetID: t, versionID: S.id })
        );
      } catch (M) {
        W(Ke(M, "加载资产版本失败"));
      } finally {
        A(0);
      }
    }
  }
  async function ee() {
    if (!m || !m.hasMore || O) return;
    const S = Math.floor(m.versions.length / 20) + 1;
    A(-1), ke("");
    try {
      const M = await so({
        teamID: e,
        assetID: t,
        page: S,
        pageSize: 20
      });
      g(
        (z) => z && {
          ...z,
          versions: ur([...z.versions, ...M.items]),
          versionTotal: M.total,
          hasMore: M.hasMore
        }
      );
    } catch (M) {
      ke(Ke(M, "加载资产版本失败"));
    } finally {
      A(0);
    }
  }
  async function j() {
    if (!(!m || !p || x)) {
      K(!0), W("");
      try {
        const S = await co({
          teamID: e,
          assetID: t,
          versionID: p.id
        }), M = Xt({ ...m, asset: S });
        g(M), _(S.version), d?.(S);
      } catch (S) {
        W(Ke(S, "设置当前版本失败"));
      } finally {
        K(!1);
      }
    }
  }
  const I = m?.asset, E = I?.status === "deleted", ce = !!(I && p && I.versionID === p.id);
  return /* @__PURE__ */ f(
    $s,
    {
      ariaLabel: `${I?.name || "资产"}详情`,
      onRequestClose: r,
      header: /* @__PURE__ */ s(
        Ls,
        {
          icon: I ? /* @__PURE__ */ s(Et, { kind: I.kind }) : /* @__PURE__ */ s(et, { size: 16 }),
          title: I?.name || "资产详情",
          subtitle: I ? `${Ks(I, u)} · ${_e(I.kind)} · ${go(I.role)}` : "",
          versionSelect: m && I && p ? /* @__PURE__ */ s(
            Bs,
            {
              options: m.versions.map((S) => ({
                id: S.id,
                version: S.version,
                updatedAt: S.updatedAt || S.createdAt,
                value: S
              })),
              currentVersionId: I.versionID,
              selectedVersionId: p.id,
              total: m.versionTotal,
              hasMore: m.hasMore,
              loading: O > 0,
              loadingMore: O === -1,
              error: q,
              disabled: x,
              onSelect: (S) => {
                G(S);
              },
              onLoadMore: () => {
                ee();
              },
              onRetry: () => {
                ee();
              }
            }
          ) : void 0,
          state: E ? /* @__PURE__ */ s("span", { className: "wb-detail-state", children: "回收站" }) : O > 0 ? /* @__PURE__ */ f("span", { className: "wb-detail-state is-saving", children: [
            /* @__PURE__ */ s(V, { size: 12, className: "wb-detail-spin" }),
            "读取中"
          ] }) : /* @__PURE__ */ s("span", { className: "wb-detail-state", children: "只读预览" }),
          updatedAt: ar(
            p?.updatedAt || p?.createdAt
          ),
          actions: I && p && !E ? /* @__PURE__ */ f(Le, { children: [
            /* @__PURE__ */ f(
              "button",
              {
                type: "button",
                className: "wb-detail-command",
                onClick: () => Q(!0),
                children: [
                  /* @__PURE__ */ s(Nt, { size: 13 }),
                  /* @__PURE__ */ s("span", { children: "修改标题" })
                ]
              }
            ),
            ce ? /* @__PURE__ */ s(
              vs,
              {
                asset: I,
                selectable: n,
                onSelect: o,
                onContinue: i,
                canContinue: a
              }
            ) : /* @__PURE__ */ s(
              Fs,
              {
                currentVersion: I.version,
                loading: !!O,
                saving: x,
                onReturn: (S) => {
                  G(S);
                },
                onMakeCurrent: () => {
                  j();
                }
              }
            )
          ] }) : void 0,
          onClose: r
        }
      ),
      children: [
        /* @__PURE__ */ s("main", { className: "wb-detail-workspace", children: /* @__PURE__ */ s("div", { className: "wb-detail-scroll", children: h ? /* @__PURE__ */ f("div", { className: "wb-detail-content-state", children: [
          /* @__PURE__ */ s(V, { size: 18, className: "wb-detail-spin" }),
          /* @__PURE__ */ s("span", { children: "正在读取资产" })
        ] }) : !I || !p ? /* @__PURE__ */ f("div", { className: "wb-detail-content-state is-error", children: [
          /* @__PURE__ */ s("span", { children: ae || "资产不存在" }),
          /* @__PURE__ */ f("button", { type: "button", onClick: () => {
            H();
          }, children: [
            /* @__PURE__ */ s(Qe, { size: 13 }),
            "重试"
          ] })
        ] }) : /* @__PURE__ */ f("div", { className: `wb-detail-readonly-content is-${I.kind}`, children: [
          ae ? /* @__PURE__ */ s("p", { className: "wb-detail-error-banner", children: ae }) : null,
          /* @__PURE__ */ s(
            zt,
            {
              kind: I.kind,
              content: p.content,
              summary: p.summary || I.summary,
              prompt: Os(p)
            },
            p.id
          )
        ] }) }) }),
        /* @__PURE__ */ s(
          cr,
          {
            teamID: e,
            asset: at && I || null,
            onClose: () => Q(!1),
            onRenamed: (S) => {
              g(
                (M) => M && { ...M, asset: S }
              ), d?.(S);
            }
          }
        )
      ]
    }
  );
}
function Fs({
  currentVersion: e,
  loading: t,
  saving: n,
  onReturn: r,
  onMakeCurrent: o
}) {
  const i = t || n;
  return /* @__PURE__ */ f(Le, { children: [
    e ? /* @__PURE__ */ f(
      "button",
      {
        type: "button",
        className: "wb-detail-command",
        disabled: i,
        onClick: () => r(e),
        children: [
          /* @__PURE__ */ s(Qe, { size: 13 }),
          /* @__PURE__ */ s("span", { children: "返回当前版本" })
        ]
      }
    ) : null,
    /* @__PURE__ */ f(
      "button",
      {
        type: "button",
        className: "wb-detail-command is-primary",
        disabled: i,
        onClick: o,
        children: [
          n ? /* @__PURE__ */ s(V, { size: 13, className: "wb-detail-spin" }) : /* @__PURE__ */ s(Xe, { size: 13 }),
          /* @__PURE__ */ s("span", { children: n ? "设置中" : "设为当前版本" })
        ]
      }
    )
  ] });
}
function vs({
  asset: e,
  selectable: t,
  onSelect: n,
  onContinue: r,
  canContinue: o
}) {
  return /* @__PURE__ */ f(Le, { children: [
    r && Us(e) && (o?.(e) ?? !0) ? /* @__PURE__ */ f(
      "button",
      {
        type: "button",
        className: "wb-detail-command",
        onClick: () => r(e),
        children: [
          e.sourceType === "dialogue" ? /* @__PURE__ */ s(Gr, { size: 14 }) : /* @__PURE__ */ s(Qe, { size: 14 }),
          /* @__PURE__ */ s("span", { children: e.sourceType === "dialogue" ? "继续对话" : "重新生成" })
        ]
      }
    ) : null,
    t && n ? /* @__PURE__ */ f(
      "button",
      {
        type: "button",
        className: "wb-detail-command is-primary",
        onClick: () => n(e),
        children: [
          /* @__PURE__ */ s(Xe, { size: 14 }),
          /* @__PURE__ */ s("span", { children: "使用" })
        ]
      }
    ) : null
  ] });
}
function Xt(e) {
  const t = ur(
    e.asset.version ? [e.asset.version, ...e.versions] : e.versions
  );
  return {
    ...e,
    versions: t,
    versionTotal: Math.max(e.versionTotal, t.length)
  };
}
function ur(e) {
  return Array.from(
    new Map(e.map((t) => [t.id, t])).values()
  );
}
function Ks(e, t) {
  const n = pn(e.sourceType, t);
  return e.sourceName && e.sourceName !== n ? `${n} / ${e.sourceName}` : n;
}
function Us(e) {
  return e.role === "material" && (e.sourceType === "tool" || e.sourceType === "dialogue");
}
function Ke(e, t) {
  return e instanceof Error ? e.message : t;
}
const Ys = [
  { key: "", label: "全部" },
  ...mn
], Qt = [
  { key: "", label: "全部" },
  ...Ct
];
function Js({
  filters: e,
  options: t,
  scopeProjectID: n = 0,
  sourceLabels: r = {},
  allowedKinds: o = [],
  view: i,
  collectionName: a,
  onCollectionBack: d,
  onChange: u,
  onViewChange: m
}) {
  const g = [
    { key: "", label: "全部" },
    ...fn.map((w) => ({
      ...w,
      label: r[w.key] || w.label
    }))
  ], p = t.assetCates.length > 0, _ = o.length > 0 ? Qt.filter(
    (w) => w.key && o.includes(w.key)
  ) : Qt;
  function h(w) {
    const O = w === "project" ? n : 0;
    u({
      ...e,
      sourceType: w,
      sourceID: O,
      projectID: O,
      assetCateID: 0,
      nodeKey: "",
      role: ""
    });
  }
  return /* @__PURE__ */ f("div", { className: "wb-asset-filters", children: [
    a && d ? /* @__PURE__ */ s(Ue, { label: "集合", children: /* @__PURE__ */ f(
      "button",
      {
        type: "button",
        className: "wb-asset-collection-back",
        onClick: d,
        children: [
          /* @__PURE__ */ s(Fr, { "aria-hidden": "true" }),
          /* @__PURE__ */ s(rn, { "aria-hidden": "true" }),
          /* @__PURE__ */ s("span", { children: a })
        ]
      }
    ) }) : /* @__PURE__ */ f(Ue, { label: "来源", children: [
      /* @__PURE__ */ s(
        ft,
        {
          options: g,
          value: e.sourceType,
          onChange: h
        }
      ),
      e.sourceType === "project" ? /* @__PURE__ */ f(Le, { children: [
        n > 0 ? null : /* @__PURE__ */ s(
          Ye,
          {
            label: r.project || "创作",
            value: e.projectID,
            options: t.projects,
            onChange: (w) => u({
              ...e,
              projectID: w,
              sourceID: w,
              assetCateID: 0,
              nodeKey: ""
            })
          }
        ),
        p ? /* @__PURE__ */ s(
          Ye,
          {
            label: "资产分类",
            value: e.assetCateID,
            options: t.assetCates,
            onChange: (w) => u({ ...e, assetCateID: w, nodeKey: "" })
          }
        ) : null
      ] }) : null,
      e.sourceType === "tool" ? /* @__PURE__ */ s(
        Ye,
        {
          label: r.tool || "工具",
          value: e.sourceID,
          options: t.tools,
          onChange: (w) => u({ ...e, sourceID: w })
        }
      ) : null,
      e.sourceType === "dialogue" ? /* @__PURE__ */ s(
        Ye,
        {
          label: "角色",
          value: e.sourceID,
          options: t.dialogues,
          onChange: (w) => u({ ...e, sourceID: w })
        }
      ) : null
    ] }),
    !a && e.sourceType === "project" && p ? /* @__PURE__ */ s(Ue, { label: "资产", children: /* @__PURE__ */ s(
      ft,
      {
        options: Ys,
        value: e.role,
        onChange: (w) => u({ ...e, role: w })
      }
    ) }) : null,
    /* @__PURE__ */ s(
      Ue,
      {
        label: "类型",
        trailing: /* @__PURE__ */ s(Ws, { view: i, onChange: m }),
        children: /* @__PURE__ */ s(
          ft,
          {
            options: _,
            value: e.kind,
            onChange: (w) => u({ ...e, kind: w })
          }
        )
      }
    )
  ] });
}
function Ue({
  label: e,
  children: t,
  trailing: n
}) {
  return /* @__PURE__ */ f("div", { className: "wb-asset-filter-row", children: [
    /* @__PURE__ */ s("strong", { children: e }),
    /* @__PURE__ */ f("div", { className: "wb-asset-filter-controls", children: [
      t,
      n
    ] })
  ] });
}
function Ws({
  view: e,
  onChange: t
}) {
  return /* @__PURE__ */ f("div", { className: "wb-asset-view-switch", role: "tablist", "aria-label": "资产视图", children: [
    /* @__PURE__ */ f(
      "button",
      {
        type: "button",
        role: "tab",
        "aria-selected": e === "assets",
        className: e === "assets" ? "is-active" : "",
        onClick: () => t("assets"),
        children: [
          /* @__PURE__ */ s(qr, { "aria-hidden": "true" }),
          /* @__PURE__ */ s("span", { children: "资产" })
        ]
      }
    ),
    /* @__PURE__ */ f(
      "button",
      {
        type: "button",
        role: "tab",
        "aria-selected": e === "trash",
        className: e === "trash" ? "is-active" : "",
        onClick: () => t("trash"),
        children: [
          /* @__PURE__ */ s(on, { "aria-hidden": "true" }),
          /* @__PURE__ */ s("span", { children: "回收站" })
        ]
      }
    )
  ] });
}
function ft({
  options: e,
  value: t,
  onChange: n
}) {
  return /* @__PURE__ */ s("div", { className: "wb-asset-segments", children: e.map((r) => /* @__PURE__ */ s(
    "button",
    {
      type: "button",
      className: t === r.key ? "is-active" : "",
      onClick: () => n(r.key),
      children: r.label
    },
    r.key || "all"
  )) });
}
function Ye({
  label: e,
  value: t,
  options: n,
  onChange: r
}) {
  return /* @__PURE__ */ f("label", { className: "wb-asset-select", children: [
    /* @__PURE__ */ s("span", { className: "sr-only", children: e }),
    /* @__PURE__ */ f(
      "select",
      {
        value: t || "",
        onChange: (o) => r(Number(o.target.value)),
        children: [
          /* @__PURE__ */ f("option", { value: "", children: [
            "全部",
            e
          ] }),
          n.map((o) => /* @__PURE__ */ s("option", { value: o.id, children: o.name }, o.id))
        ]
      }
    )
  ] });
}
const St = {
  sourceType: "",
  sourceID: 0,
  projectID: 0,
  assetCateID: 0,
  nodeKey: "",
  role: "",
  kind: ""
}, qs = Or.ConfirmDialog, en = {
  projects: [],
  tools: [],
  dialogues: [],
  assetCates: [],
  nodes: []
}, fe = {
  items: [],
  page: 1,
  pageSize: 24,
  total: 0,
  hasMore: !1
};
function Oc({
  teamID: e,
  scopeProjectID: t = 0,
  initialFilters: n,
  selectable: r = !1,
  excludeCollections: o = !1,
  selectedAssetIDs: i,
  usedAssetIDs: a,
  allowedKinds: d,
  onSelect: u,
  onContinue: m,
  canContinue: g,
  onAssetChanged: p,
  onAssetRemoved: _,
  onLocalUpload: h,
  uploadAccept: w,
  headerAction: O,
  reloadSignal: A = 0,
  catalogOptions: x,
  contentMode: K = "preview",
  className: at = ""
}) {
  const Q = vr(), ae = dr(), W = JSON.stringify(d || []), q = De(
    () => Gs(d),
    [W]
  ), ke = JSON.stringify({ initialFilters: n, normalizedAllowedKinds: q }), H = De(
    () => Hs(n, q),
    [ke]
  ), [G, ee] = N(H), [j, I] = N(
    null
  ), [E, ce] = N("assets"), [S, M] = N(en), [z, te] = N(fe), [ct, U] = N(0), [lr, je] = N(null), [de, Se] = N(null), [ue, Ne] = N(0), [Ae, Pt] = N(!0), [Ve, dt] = N(!1), [fr, $t] = N(!0), [Lt, ne] = N(""), [mr, pr] = N(0), re = pe(0), Bt = pe(null), Ie = pe(H), Ce = pe("assets"), jt = JSON.stringify(i || []), yr = De(
    () => new Set(JSON.parse(jt)),
    [jt]
  ), Vt = JSON.stringify(a || []), hr = De(
    () => new Set(JSON.parse(Vt)),
    [Vt]
  );
  Z(() => {
    re.current += 1, ee(H), Ie.current = H, I(null), ce("assets"), Ce.current = "assets", M(en), te(fe), U(0), je(null), Se(null), Ne(0), dt(!1), ne("");
  }, [Q, H, t, e]), Z(() => {
    let b = !0;
    return $t(!0), ro(e, x, Q).then((T) => {
      b && M(T);
    }).catch((T) => {
      b && ne(xe(T, "加载资产筛选项失败"));
    }).finally(() => {
      b && $t(!1);
    }), () => {
      b = !1;
    };
  }, [x, Q, e]);
  const Fe = an(
    async (b) => {
      const T = ++re.current;
      Pt(!0), ne("");
      try {
        const B = await oo({
          teamID: e,
          scopeProjectID: t,
          filters: G,
          view: E,
          contentMode: K,
          page: b,
          pageSize: 24,
          collectionID: j?.id,
          excludeCollections: o,
          requestScopeKey: Q
        });
        T === re.current && te(B);
      } catch (B) {
        T === re.current && ne(xe(B, "加载资产失败"));
      } finally {
        T === re.current && Pt(!1);
      }
    },
    [
      j?.id,
      K,
      o,
      G,
      Q,
      t,
      e,
      E
    ]
  );
  Z(() => {
    Fe(1);
  }, [Fe, A, mr]);
  function _r(b) {
    ee(b), j || (Ie.current = b), te((T) => ({ ...T, page: 1 }));
  }
  function Te() {
    pr((b) => b + 1);
  }
  function gr(b) {
    if (b.kind !== "collection") {
      U(b.id);
      return;
    }
    Ie.current = G, Ce.current = E, I(b), ee({
      ...St,
      kind: G.kind === "collection" ? q.length === 1 ? q[0] : "" : G.kind
    }), te(fe), U(0), ne("");
  }
  function br() {
    re.current += 1, I(null), ee(Ie.current), ce(Ce.current), te(fe), U(0), ne("");
  }
  function wr(b) {
    b === E || ue || (re.current += 1, ce(b), j || (Ce.current = b), te(fe), U(0), je(null), Se(null), ne(""));
  }
  async function kr() {
    if (!de || ue) return;
    const b = de.id;
    Ne(b);
    try {
      await lo({ teamID: e, assetID: b }), Se(null), ct === b && U(0), _?.(b), le.success("资产已移入回收站"), Te();
    } catch (T) {
      le.error(xe(T, "删除资产失败"));
    } finally {
      Ne(0);
    }
  }
  async function Sr(b) {
    if (!ue) {
      Ne(b.id);
      try {
        const T = await fo({ teamID: e, assetID: b.id });
        p?.(T), le.success("资产已恢复"), Te();
      } catch (T) {
        le.error(xe(T, "恢复资产失败"));
      } finally {
        Ne(0);
      }
    }
  }
  async function Nr(b) {
    const T = Array.from(b.target.files || []);
    if (b.target.value = "", !(!h || T.length === 0 || Ve)) {
      dt(!0);
      try {
        const B = await h(T);
        if (B.length === 0)
          throw new Error("上传完成，但没有生成可用资产");
        B.forEach((Ar) => p?.(Ar));
        const Ft = {
          ...St,
          sourceType: "upload",
          kind: q.length === 1 ? q[0] : ""
        };
        re.current += 1, I(null), ce("assets"), Ce.current = "assets", ee(Ft), Ie.current = Ft, te(fe), U(0), ne(""), le.success(`已上传 ${B.length} 项资产`);
      } catch (B) {
        le.error(xe(B, "上传资产失败"));
      } finally {
        dt(!1);
      }
    }
  }
  return /* @__PURE__ */ f("section", { className: `wb-asset-browser ${at}`.trim(), children: [
    /* @__PURE__ */ f("header", { className: "wb-asset-browser-head", children: [
      /* @__PURE__ */ s(
        Js,
        {
          filters: G,
          options: S,
          scopeProjectID: t,
          sourceLabels: ae,
          allowedKinds: q,
          view: E,
          collectionName: j?.name,
          onCollectionBack: j ? br : void 0,
          onChange: _r,
          onViewChange: wr
        }
      ),
      /* @__PURE__ */ f("div", { className: "wb-asset-browser-actions", children: [
        /* @__PURE__ */ s("span", { children: Ae ? "正在加载" : `${z.total} 项` }),
        /* @__PURE__ */ s(L, { label: "刷新资产", children: /* @__PURE__ */ f("button", { type: "button", onClick: Te, children: [
          /* @__PURE__ */ s(xr, { className: Ae ? "is-spinning" : "" }),
          /* @__PURE__ */ s("span", { className: "sr-only", children: "刷新资产" })
        ] }) }),
        !j && h ? /* @__PURE__ */ f(Le, { children: [
          /* @__PURE__ */ s(L, { label: "本地上传", children: /* @__PURE__ */ f(
            "button",
            {
              type: "button",
              className: "wb-asset-local-upload",
              disabled: Ve,
              onClick: () => Bt.current?.click(),
              children: [
                Ve ? /* @__PURE__ */ s(V, { className: "is-spinning", "aria-hidden": "true" }) : /* @__PURE__ */ s(Dr, { "aria-hidden": "true" }),
                /* @__PURE__ */ s("span", { children: Ve ? "上传中" : "本地上传" })
              ]
            }
          ) }),
          /* @__PURE__ */ s(
            "input",
            {
              ref: Bt,
              type: "file",
              hidden: !0,
              multiple: !0,
              accept: w,
              onChange: Nr
            }
          )
        ] }) : null,
        j ? null : O
      ] })
    ] }),
    /* @__PURE__ */ s("div", { className: "wb-asset-browser-body", children: Ae && z.items.length === 0 ? /* @__PURE__ */ s(mt, { icon: /* @__PURE__ */ s(V, { className: "is-spinning" }) }) : Lt ? /* @__PURE__ */ s(mt, { text: Lt, error: !0 }) : z.items.length === 0 ? /* @__PURE__ */ s(
      mt,
      {
        icon: E === "trash" ? /* @__PURE__ */ s(on, {}) : /* @__PURE__ */ s(Ir, {}),
        text: fr ? "正在读取资产配置" : E === "trash" ? "回收站为空" : j ? "集合内暂无符合条件的资产" : "暂无符合条件的资产"
      }
    ) : /* @__PURE__ */ s("div", { className: "wb-asset-grid", children: z.items.map((b) => /* @__PURE__ */ s(
      Es,
      {
        asset: b,
        sourceLabels: ae,
        view: E,
        selectable: b.kind !== "collection" && r && E === "assets",
        selected: E === "assets" && yr.has(b.id),
        used: E === "assets" && hr.has(b.id),
        busy: ue === b.id,
        onOpen: gr,
        onRename: je,
        onDelete: E === "assets" ? Se : void 0,
        onRestore: E === "trash" ? Sr : void 0,
        onSelect: u
      },
      b.id
    )) }) }),
    z.total > z.pageSize ? /* @__PURE__ */ f("footer", { className: "wb-asset-pagination", children: [
      /* @__PURE__ */ s(L, { label: "上一页", children: /* @__PURE__ */ s(
        "button",
        {
          type: "button",
          disabled: z.page <= 1 || Ae,
          onClick: () => {
            Fe(z.page - 1);
          },
          children: /* @__PURE__ */ s(Cr, {})
        }
      ) }),
      /* @__PURE__ */ f("span", { children: [
        z.page,
        " / ",
        Math.max(1, Math.ceil(z.total / z.pageSize))
      ] }),
      /* @__PURE__ */ s(L, { label: "下一页", children: /* @__PURE__ */ s(
        "button",
        {
          type: "button",
          disabled: !z.hasMore || Ae,
          onClick: () => {
            Fe(z.page + 1);
          },
          children: /* @__PURE__ */ s(Tr, {})
        }
      ) })
    ] }) : null,
    ct ? /* @__PURE__ */ s(
      Vs,
      {
        teamID: e,
        assetID: ct,
        selectable: r && E === "assets",
        onClose: () => U(0),
        onSelect: u ? (b) => {
          U(0), u(b);
        } : void 0,
        onContinue: m ? (b) => {
          U(0), m(b);
        } : void 0,
        canContinue: g,
        onAssetChanged: (b) => {
          Te(), p?.(b);
        }
      }
    ) : null,
    /* @__PURE__ */ s(
      cr,
      {
        teamID: e,
        asset: lr,
        onClose: () => je(null),
        onRenamed: (b) => {
          te((T) => ({
            ...T,
            items: T.items.map(
              (B) => B.id === b.id ? b : B
            )
          })), p?.(b), Te();
        }
      }
    ),
    /* @__PURE__ */ s(
      qs,
      {
        open: !!de,
        onOpenChange: (b) => {
          !b && !ue && Se(null);
        },
        title: "移入回收站？",
        desc: de?.kind === "collection" ? `“${de.name}”及集合内素材将移入回收站，你可以稍后恢复。` : `“${de?.name || "该资产"}”将从资产列表移除，你可以稍后在回收站中恢复。`,
        confirmText: "移入回收站",
        destructive: !0,
        isLoading: !!ue,
        handleConfirm: () => {
          kr();
        }
      }
    )
  ] });
}
function mt({
  icon: e,
  text: t = "",
  error: n = !1
}) {
  return /* @__PURE__ */ f("div", { className: `wb-asset-state ${n ? "is-error" : ""}`.trim(), children: [
    e,
    t ? /* @__PURE__ */ s("p", { children: t }) : null
  ] });
}
function Hs(e, t) {
  const n = { ...St, ...e || {} };
  return t.length > 0 && !t.includes(n.kind) && (n.kind = t[0]), n.projectID && (n.sourceType = "project", n.sourceID = n.projectID), n.sourceType !== "project" && (n.projectID = 0, n.assetCateID = 0, n.nodeKey = "", n.role = ""), n;
}
function Gs(e) {
  const t = Ct.filter(
    (r) => r.key !== "collection"
  ), n = t.map((r) => r.key).filter((r) => e?.includes(r));
  return n.length === t.length ? [] : n;
}
function xe(e, t) {
  return e instanceof Error ? e.message : t;
}
const Zs = "bot_work", Xs = "神创工作台", { uploadFileByRule: tn } = sn("@/lib/upload");
async function Mc(e) {
  if (!tn)
    throw new Error("当前页面缺少上传能力");
  const t = [];
  for (const n of e.files) {
    const r = ta(e.kind) || ea(n), o = Number(e.ruleID || 0) || na(r), i = await tn(o, n, {
      kind: r,
      bizKey: Zs,
      bizName: Xs
    }), [a] = await Qs({
      teamID: e.teamID,
      projectID: e.projectID,
      files: [i]
    });
    if (!a)
      throw new Error(`${n.name} 保存到资产库失败`);
    t.push({ sourceFile: n, uploadedFile: i, asset: a });
  }
  return t;
}
async function Qs(e) {
  const t = [], n = /* @__PURE__ */ new Set();
  for (const r of e.files) {
    const o = Number(r.id || 0);
    if (!Number.isFinite(o) || o <= 0)
      throw new Error("上传文件标识无效");
    if (n.has(o))
      continue;
    const i = await F(
      v("workbench/upload_save_asset"),
      "post",
      {
        team_id: e.teamID,
        project_id: e.projectID || void 0,
        file_id: o
      }
    );
    if (!cn(i))
      throw new Error(
        String(i?.message || i?.msg || "保存上传资产失败")
      );
    const a = i?.data?.asset;
    if (!a || typeof a != "object" || Array.isArray(a))
      throw new Error("保存上传资产结果为空");
    n.add(o), t.push(a);
  }
  return t;
}
function ea(e) {
  const t = String(e.type || "").toLowerCase();
  return t.startsWith("image/") ? "image" : t.startsWith("video/") ? "video" : t.startsWith("audio/") ? "audio" : "file";
}
function ta(e) {
  const t = String(e || "").toLowerCase();
  return ["image", "video", "audio", "file"].includes(t) ? t : "";
}
function na(e) {
  return e === "image" ? 1 : e === "video" ? 2 : e === "audio" ? 3 : 4;
}
export {
  ts as $,
  Oc as A,
  Xs as B,
  Ga as C,
  Lo as D,
  $o as E,
  Ya as F,
  Va as G,
  Xa as H,
  Uo as I,
  Ea as J,
  Yo as K,
  it as L,
  In as M,
  Ka as N,
  Ua as O,
  ja as P,
  za as Q,
  Fa as R,
  Pa as S,
  va as T,
  Ba as U,
  Qa as V,
  Oi as W,
  ot as X,
  gc as Y,
  kc as Z,
  Bi as _,
  Zs as a,
  dc as a$,
  qn as a0,
  wc as a1,
  Vn as a2,
  ac as a3,
  sc as a4,
  nc as a5,
  $n as a6,
  ko as a7,
  Un as a8,
  Fi as a9,
  yc as aA,
  hc as aB,
  _c as aC,
  pc as aD,
  Di as aE,
  ge as aF,
  mc as aG,
  gi as aH,
  $s as aI,
  Sc as aJ,
  Ki as aK,
  Nc as aL,
  Ac as aM,
  Ic as aN,
  Cc as aO,
  bc as aP,
  Ui as aQ,
  ji as aR,
  vi as aS,
  ps as aT,
  uc as aU,
  lc as aV,
  xc as aW,
  Yi as aX,
  Co as aY,
  Dc as aZ,
  cc as a_,
  Et as aa,
  Xr as ab,
  ks as ac,
  Ja as ad,
  Dn as ae,
  ec as af,
  tc as ag,
  jn as ah,
  Cn as ai,
  Vi as aj,
  Ss as ak,
  Tc as al,
  fc as am,
  Za as an,
  yn as ao,
  So as ap,
  ic as aq,
  rc as ar,
  Ji as as,
  hi as at,
  oc as au,
  zt as av,
  Ls as aw,
  eo as ax,
  ar as ay,
  Bs as az,
  L as b,
  Jt as b0,
  Wi as b1,
  Ma as b2,
  Ra as b3,
  xo as b4,
  sr as c,
  Oa as d,
  Vs as e,
  $e as f,
  La as g,
  ni as h,
  ti as i,
  ri as j,
  Qo as k,
  io as l,
  ei as m,
  Be as n,
  xn as o,
  $a as p,
  Tn as q,
  st as r,
  Qs as s,
  Xo as t,
  Mc as u,
  qa as v,
  Ha as w,
  Wa as x,
  Zo as y,
  Ko as z
};
